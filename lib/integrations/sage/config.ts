/** Sage Business Cloud Accounting API base (v3.1, pinned by Nango's `authorization_params:
 * { filter: apiv3.1 }` on the `sage` provider template — #378). Reached through the Nango proxy;
 * DocuBite no longer talks to Sage's OAuth hosts directly (ADR 0005). */
export const SAGE_API_BASE = "https://api.accounting.sage.com/v3.1"
