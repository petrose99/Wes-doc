# Notes

- Workspace lives at `docs/teach/token-usage/` to keep the repo root clean; the /teach skill's "current directory" is taken to be this folder.
- The learner types fast and terse; answer in the same register. Prefers real numbers from their own logs over analogies.
- They run the autopilot on a VPS with no display: do not try to open lessons in a browser; print the path and offer a `python3 -m http.server` one-liner instead.
- Recurring confusion to keep an eye on: "load" (first-turn context) vs "what I am charged" (every turn re-sends everything). Lesson 1 targets exactly this.
