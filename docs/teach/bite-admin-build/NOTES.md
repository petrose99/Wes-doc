# Notes

- Workspace lives at `docs/teach/bite-admin-build/`; the /teach skill's "current directory" is taken to be this folder. Assets copied from `../token-usage/assets/`.
- Learner types fast and terse; answer in the same register. Owner of the product, not the author of this code.
- VPS with no display: print the lesson path, offer `python3 -m http.server`; a Cloudflare quick tunnel to the dev server on :3000 is usually up (`.preview/tunnel.log`), so "open the real screen" is possible.
- Wants to be quizzed in chat as well as in the lesson file.
- Recurring confusion the mission targets: organization membership vs company role — which one opens a company.
