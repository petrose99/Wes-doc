# Area primer — Approvals (queue + phone lane)

Shipped by #236 (Approvals landing: Invoice approvals · PO mismatches), #257 (phone approval lane below `lg`), #271 (Approval email notice: sender, stop link, switch) and #273 (expense claims: Add to claim on Receipts, the claim on the Approval tab, an Expense claims segment). Mode: Operate. Decisions live on #226, #232 (tab bar), #237 (removals), #249 (close-with-toast), #247 (expense-claim action) — do not re-decide here. The same `QueueScreen` shell also renders Invoices, Exceptions, Payments (#251), PO rows (#250); #261 brings the card list to those.

## Expense claims (#273)
- `components/queue/add-to-claim-dialog.tsx` (from Receipts bulk bar/pane): new-claim vs existing-draft radio group, mixed-currency rows disable Submit with a reason (spec §98), body inset `px-5 py-4` like every other Dialog.
- `components/queue/claim-card.tsx` renders inside `history-tabs.tsx`'s Approval tab when the receipt belongs to a claim.
- `models/expense-claims.ts`: `classifyClaimableDocuments` derives claim state from `reviewTask` (no `approvalStatus`/`blockedByCheck` columns on `Document`); extracted totals are **strings**, parse with `asNumber`; `ExpenseClaimItem.createMany` needs `workspaceId`; rejection reason lives in audit `detail.reason`. `countClaimsReadyToApprove` (called from `models/approvals.ts`'s `countReadyToApprove`) needs `expense-approvals` in `getWorkspaceCapabilities` — **model tests mocking `prisma` as `{}` must also mock `db.workspace.findUniqueOrThrow`, `db.workspaceModule.findMany`, `db.expenseClaim.findMany`** or `countReadyToApprove` throws.
- Routes: `app/(app)/workspaces/[workspaceId]/(queue)/approvals/expense-claims/page.tsx` (queue) and `.../expense-claims/[claimId]/page.tsx` (shell notice, not a read-only pane — decided at #273's close, not a re-open). `receipts?mode=claims` segment; legacy `(chrome)/expenses` route now `notFound()`.
- One creation breakpoint: `useCanCreateClaims` gates Add-to-claim at `md`. `?approver=anyone` shows all claims; default shows the caller's own.
- Legacy `ExpenseClaimsPage`/`ExpenseClaimForm`/`ExpenseClaimRow`/`addExpenseClaimItemsAction` are unreferenced but not deleted — removal is #274 (owner sign-off).

## Approval notice (#271)
- `models/approval-notices.ts` — sender: candidates from `models/review-tasks.ts`/`models/approval-workflows.ts` stage entry (`stageReachedAt`), coalesces same-decider mail, nudge after N days idle, "sent back" notice. `lib/notices/{stop-token,kick,fixtures,stop-urls,stop-state}.ts`; email component `components/emails/approval-notice-email.tsx`. Preview at `/dev/emails/approval-notice?case=single|plural|nudge|mixed|sent-back|long-names` (`&text=1` for plain text).
- The switch: `components/shell/approval-emails.tsx` `ApprovalEmailsControl`, mounted in the desktop account-menu `ApprovalEmailsDialog` (`#approval-emails-switch`) and the phone Account panel (`#approval-emails-switch-panel`). `components/ui/switch.tsx`'s `busy` prop (aria-busy, stays focusable) is the reusable pending-while-saving primitive — reach for it before writing another disabled-while-saving toggle.
- Stop link: `app/notices/stop/page.tsx` (GET-only render: confirm/done/already-off/invalid) + `app/notices/stop/confirm/route.ts` (POST-only write; a page and a route can't share one segment in Next, hence `/confirm`). No session — the signed token in `t` is the whole authority; `List-Unsubscribe-Post: One-Click` on the mail header gives mail clients the same one-click stop.
- `stageReachedAt` on `ReviewTask` is the notice's clock (kicked whenever `decideReviewTaskStage`/`startWorkflowOnReviewTask`/`startApprovalOnInvoice` advance a stage; cleared to `null` by `sendReviewTaskBackForReview`, left untouched by `cancelApprovalOnDocument`'s stage-0 withdrawal). Any test asserting a `reviewTask.update` call on a stage-advance path needs this field in its expectation.
- Capture: `docs/wayfinder-reports/226/logs/scratch-271/round-271.mjs` (33 states × 1440/390: emails, account-menu, switch-dialog (1440-only), account-panel (the 390 keyboard contract — pending/off/save-failure/offline all probed there), stop-*, admin-band, arrival), `run-round.mjs <r> [only]`.

## Routes
`app/(app)/workspaces/[workspaceId]/(queue)/approvals/` — `invoices/page.tsx`, `po-mismatches/page.tsx` (+ `[documentId]` deep-link variants render the same queue with `initialSelectedId`), `actions.ts`, `loading.tsx`. Pages hand **all** rows to the client; filtering is client-side (`lib/approvals/filters.ts`) so the Filter sheet's "Show n rows" and the empty state's "N waiting on other approvers" are live. Segment counts run the other queue's rows through the same predicate. Phone tab bar (`components/shell/mobile-tab-bar.tsx`): Approvals (badge = `countReadyToApprove`) · Invoices (no badge, #232) · Exceptions · Account; Account page carries "Also in this workspace" links (Payments excluded — desktop-only, #251).

## Primitives — use these, never a bespoke one beside them (pre-flight B4)
- `components/queue/queue-screen.tsx` `QueueScreen<T>`: list + `DetailPane`. #257 props: `cards={{ below: "lg", title, render }}` (card `<ul>` below the breakpoint, table above — both in the DOM, use `:visible` locators), `filterRows(rows, params)`, `pinned` (decided row stays while its pane is open), `initialMissingNotice`, `onOpenChange`, `empty.action`/`filteredAction`, `phoneReadOnly`. `PaneHelpers = { close, refresh, next }`. Below `lg`, opening a row `pushState`s; Back/Escape/phone gesture all `history.back()` and the popstate handler closes; Escape closes only the innermost `[data-inner] [role=dialog]`.
- `components/queue/queue-segments.tsx` `QueueSegments` — the one segment picker (tablist of links, ←/→). `ApprovalsQueuePicker` (`components/typed-destinations/`) is unused, pending owner sign-off for removal.
- `components/queue/filter-sheet.tsx` `FilterButton` ("Filter · n", 44px) + `FilterSheet` (Dialog sheet: Sort radiogroup + one radiogroup per `Facet`, Clear · "Show n rows", `aria-live` count). Replaces the sort select/facet chips below the breakpoint.
- `components/queue/detail-pane.tsx` `DetailPane`/`PaneFrame`: below `lg` a full-screen `role=dialog aria-modal` sheet, siblings `inert`, Back labelled "Back to ‹title›", slide-in `db-sheet-in`; title suffix (number · amount) `hidden lg:inline`. `PaneMenuItem` supports `disabled` + `hint` (Send back for review is desktop-only below `lg`).
- `components/ui/dialog.tsx` `placement="sheet"` (bottom sheet below `md`), `initialFocus` selector, `data-inner`; `components/list-screen/reason-dialog-button.tsx` `ReasonDialog` (`placement`, `initialFocus="textarea"`, `disabledReason`, `pendingLabel`, 48px half-width footer below `md`).
- `components/queue/decision-result.tsx` `DecisionResultStrip` ("Approved/Rejected · just now" or the server's refusal sentence, takes focus, Back to list · Next to approve disabled with "Nothing else waiting on you") + the one `OFFLINE_REASON` / `NETWORK_ERROR` sentences.
- `components/queue/history-tabs.tsx` `ApprovalTab` (status line, `ApprovalTimeline` with avatars, PO-match `<dl>`, "From this supplier", Possible-duplicate link with `?from=`); `split-pane.tsx` tabs are a roving tablist, order below `lg` Approval · Details · Checks · Audit · Note ("Audit" kept, not "History"), "Hide/Show source" toggle in `sessionStorage["dp.source"]`.
- `lib/client/use-phone-lane.ts` `usePhoneLane()` / `isPhoneLane()` = `(max-width: 1023px)`; the only phone-lane query.
- Client components never import `models/*` (Prisma in the browser bundle); pure helpers go in `lib/approvals/`.

## Data and actions
`models/approvals.ts`: `listApprovalInvoiceRows`, `listPoMismatchRows`, `countReadyToApprove` (badge), `getApprovalDetailFacts` (approval, supplier, nearDuplicate, mismatch). Row eligibility is `lib/approvals/row-eligibility.ts` and **must mirror every server precondition** in `models/review-tasks.ts` (hard gate, open exception, no approver, `assertPaymentConfirmed` → "Confirm whether this has been paid before approving it."); the server still re-checks on decide and a refusal shows in the result strip. Actions: `decideReviewTaskStageAction`, `overrideGateAction`, `sendApprovalBackForReviewAction` (`app/(app)/workspaces/[workspaceId]/actions.ts`); `getSelectionAuditPanelDataAction` returns `facts`. Model tests mock `prisma` as a bare object — add any new table to `beforeEach`.

## Seed, dev server, capture
- Dev workspace `af91555d-7450-4b21-a8ac-73db092617c8`, `DEV_AUTH_BYPASS=true`, devdb `docubite-devdb` on `127.0.0.1:55433`.
- Seed: `.impeccable/live/seed257b.ts` (ready rows for the dev owner), `paid257b.ts` (mark `paymentStatus=paid` so Approve is enabled), `state257.ts` prints what the owner sees. Run with `npx tsx --env-file .env`.
- Servers `node scripts/dev/dev.mjs start <ws>|stop` (dev server :3000 and the in-page detector :8400 together; if Turbopack panics, `rm -rf .next/dev .next/cache`). Stop both (`dev.mjs stop`) before `tsc`/`eslint`/vitest/`next build` (`docs/wayfinder-reports/226/logs/scratch-257/stop-servers.mjs`).
- Capture harness `docs/wayfinder-reports/226/logs/scratch-257/pw/round.mjs <round> [--decide-row "NW-5490"]` — 20 states × 1440/390, `detector.json`, `keyboard.json`; probes `probe-back.mjs`, `probe-type.mjs`, `probe-detect.mjs`; `contact-sheet.mjs --cols 5`. The `[documentId]` route cold-compiles ~45 s — 90 s screenshot timeout.

## Detector residue (report, do not chase)
App-wide five per state (rail avatar palette ×2, Inter `overused-font`, dev overlay `layout-transition` + `dark-glow`). Queue adds: rail hover `text-occlusion` on the Supplier column at 1440, `hidden lg:table` wrapper "children flush" at 1440 (display:none), detail tab strip "flush against border-bottom" (tabs sit on the border by design), `clipped-overflow-container` on the source panel chain, "Could not render this PDF" when the seed has no file.

## Conventions the bar checks
One term per concept: "Ready to Approve" (phone h1), "Invoice approvals" / "PO mismatches" (segments), "Audit" (tab), the two offline/network sentences above. Every ⚠ action confirms with its consequence in the sheet ("This ends the approval run…"; "Approving moves it to ‹stage›" / "completes the Approval"). Keys: cards and segments in Tab order, ←/→ on tablists, ↑/↓ in Filter radiogroups, Escape innermost-only with the typed reason kept, focus returns to the card `<a>` on Back, the result strip takes focus. States: loading frame, "Nothing needs your approval" (+ "N waiting on other approvers" link), "No rows match these filters" + Clear filters, offline (submit disabled + reason), refused (strip), network error (sheet stays open, reason kept), deep link to a decided row (`initialMissingNotice`). Type scale below `lg`: h1 20px on 16px body; controls ≥ 44px.
