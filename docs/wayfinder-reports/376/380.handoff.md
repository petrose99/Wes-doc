milestone: spec-done

Ticket body is the spec (exact file list, definition of done); no separate
spec doc needed. Scope confirmed by grep: ~70 files outside git history
reference `bigcapital` (case-insensitive), see
`docs/wayfinder-reports/376/logs/scratch-380/grep-baseline.txt`.

## Plan

- step: 1 — delete lib/integrations/bigcapital + its tests, models/bigcapital.ts,
  models/bigcapital-members.ts, migration dropping their tables — done
- step: 2 — strip Bigcapital branches from lib/integration-push.ts,
  lib/integration-rate-limits.ts, lib/integration-token-refresh.ts,
  lib/integrations/sync.ts, lib/health/* (checks + sync + actions),
  lib/reconciliation/close-loop.ts, lib/automation/autopublish.ts,
  lib/finance/*, lib/config.ts, lib/solutions.ts, lib/workspace-scope.ts,
  worker/job-worker.ts, models/integrations.ts, models/workspaces.ts,
  models/audit-events.ts, models/review-tasks.ts — todo
- step: 3 — remove app/api/accounting/session, Finance dashboard route +
  components/accounting/*, components/files/from-accounting-card.tsx,
  worksheets accounting-report-actions, sidebar/account-page/integrations
  page Bigcapital references, app/api/internal/jobs/process route branch — todo
- step: 4 — Accounting page (`/finance` route, keep redirect) reduced to
  "no ledger connected" + disabled Connect button; queue banners say why
  posting is unavailable above the rows; docker-compose services + env +
  config.integrations.bigcapital removed; prisma seed.ts cleaned — todo
- step: 5 — final grep-clean pass (no `bigcapital` outside git history and
  this ticket file), tsc --noEmit, vitest affected paths, migration applied
  on devdb, next build — todo
- step: G1 — round script (capture Accounting page + an Invoices queue at
  1440/390, detector) — todo
- step: G2 — r0 and fixes — todo
- step: G3 — build-done (tsc full, Part B checks) — todo

## Artifacts

- Grep baseline: docs/wayfinder-reports/376/logs/scratch-380/grep-baseline.txt

## Open findings

(none yet)

## Next step

Build step 2: strip Bigcapital branches from lib/integration-push.ts,
lib/integration-rate-limits.ts, lib/integration-token-refresh.ts,
lib/integrations/sync.ts, lib/health/*, lib/reconciliation/close-loop.ts,
lib/automation/autopublish.ts, lib/finance/*, lib/config.ts, lib/solutions.ts,
lib/workspace-scope.ts, worker/job-worker.ts, models/integrations.ts,
models/workspaces.ts, models/audit-events.ts, models/review-tasks.ts (and
their .test.ts files that reference the deleted models — models/documents.test.ts,
models/workspaces.test.ts, models/review-tasks.test.ts, lib/health/actions.test.ts,
lib/health/sync.test.ts, lib/workspace-scope.test.ts, lib/reconciliation/close-loop.test.ts).
schema.prisma already has BigcapitalAccount/BigcapitalMemberAccount/
IntegrationProvisionJob and their User/Workspace relation fields removed;
migration 20260922010000_drop_bigcapital drops the tables (not yet applied
to devdb — do that in step 5 or when devdb is up).
