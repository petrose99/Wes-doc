milestone: measured

Ticket body is the spec (exact file list, definition of done); no separate
spec doc needed.

## Plan

- step: 1-5 — delete/strip Bigcapital everywhere, reduce Accounting page,
  update area primers, tsc/vitest/next build green, migration applied on
  devdb — done (see prior commits 28882b2/75223bd/dca383d/43ed43e)
- step: G1/G2 — capture round, gate, fix loop — done, gate CLEAN (rounds
  r0-r3 in scratch-380/). r0/r1 found a real P0: Invoices queue had no
  "no ledger connected" banner (the ticket's explicit DoD line). Fixed:
  `lib/integration-push.ts` `getLedgerConnectionBandStatus` no longer
  returns null just because `config.integrations.enabled` is false — it
  now returns "disconnected" (renamed in copy to "No ledger connected —
  posts are waiting.") so the band always shows while no ledger exists,
  not only after a prior connection breaks. `components/queue/
  connection-band.tsx` splits the link label from the status message
  ("Connect" for the never-had-one case, "Reconnect" for reauth/
  no-default-account) so it never says "Reconnect" to a user who's never
  connected anything (H2/H4). Also deleted one stray leftover active
  `integration_connections` row on devdb workspace
  af91555d-7450-4b21-a8ac-73db092617c8 (an earlier session's manual
  bigcapital→xero swap left a live "connected" row that isn't
  reproducible from any committed seed script — it was masking the
  no-ledger state this ticket requires). r3 round after both fixes: gate
  CLEAN (0 real findings against `docs/agents/areas/admin.md`'s app-wide
  residue), tsc clean, vitest 284/284 files green, `next build` clean.
- step: G3 — close: report + scores line — todo (this is the only thing
  left)

## Artifacts

- Round scripts/captures: docs/wayfinder-reports/376/logs/scratch-380/
  round-380.mjs, r0/ (before-fix baseline), r3/ (after-fix, gate CLEAN)
- Scores: docs/wayfinder-reports/376/logs/scratch-380/r3-scores.md
  (evaluate: 86/100, Clean) and r3-critique.md (critique: 35/40, every
  heuristic ≥3 — written by a background sub-agent, see its
  task-notification in this session's transcript for the exact table)
- Step detail: docs/wayfinder-reports/376/logs/scratch-380/step-notes.md

## Open findings

None blocking. r3-critique.md/r3-scores.md both note P1 polish items
(native `title`-only disclosure on Finance's Connect button vs the
Invoices queue's visible menu-item `hint` text) — cosmetic, not below the
3/4 or 80-health bar, safe to leave as future polish, not a new ticket.

## Next step

Write the close report at docs/wayfinder-reports/376/380.md (template:
scripts/wayfinder-autopilot/report.md), building session history from
`git log --format='%h %s%n%b' -- <ticket files>`. Use scores: critique
35/40 (all heuristics ≥3), evaluate 86/100 Clean — both already computed,
no new capture round needed. Then `gh issue close 380` with a resolution
comment, append one line to map #376's Decisions so far, `git add`/commit
the report (docs(wayfinder): autopilot report for #380). No dev/live
servers are running (both stopped this session) — nothing to clean up.
