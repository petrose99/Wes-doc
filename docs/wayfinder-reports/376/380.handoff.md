milestone: build-done

Ticket body is the spec (exact file list, definition of done); no separate
spec doc needed.

## Plan

- step: 1 — delete lib/integrations/bigcapital + tests, models/bigcapital*.ts,
  migration dropping their tables — done
- step: 2 — strip Bigcapital branches from ~25 lib/ and models/ files — done
  (commit 75223bd)
- step: 3 — remove app/api/accounting/session, Finance route, accounting
  components, integrations-manager Bigcapital branch, worksheet/account/
  admin/jobs Bigcapital branches — done (mostly landed in 886cf09, loose
  ends fixed here — commit dca383d). See scratch/step-notes.md.
- step: 4 — Accounting page reduced + queue banners + docker-compose/env
  cleanup (already done) + area primers/ADR updated — done (43ed43e)
- step: 5 — final grep-clean pass, tsc, vitest, next build, migration
  applied on devdb — done. See scratch/step-notes.md.
- step: G1 — round script (Finance page + Invoices queue, 1440/390,
  detector) — todo, started and interrupted, see scratch/step-notes.md
- step: G2 — r0 and fixes — todo
- step: G3 — build-done gate (tsc full, Part B checks), report, close — todo

## Artifacts

- Round script (untracked scratch, present on disk):
  docs/wayfinder-reports/376/logs/scratch-380/round-380.mjs
- Step detail: docs/wayfinder-reports/376/logs/scratch-380/step-notes.md

## Open findings

None yet — no capture round has completed. See scratch/step-notes.md "G1"
for exactly where the dev-server check needs to resume.

## Next step

Start dev server (`node .impeccable/live/dev.mjs start
af91555d-7450-4b21-a8ac-73db092617c8`), verify `ss -ltn | grep -E
':3000|:8400'`, run the round script (needs Bash `timeout` ~300000+), read
detector.json + gate.mjs, run impeccable critique/evaluate on Finance +
Invoices, fix findings, close with report + scores: line. Stop dev/live
servers before ending.
