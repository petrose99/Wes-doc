milestone: spec-done

Ticket body is the spec (exact file list, definition of done); no separate
spec doc needed.

## Plan

- step: 1 — delete lib/integrations/bigcapital + tests, models/bigcapital*.ts,
  migration dropping their tables — done
- step: 2 — strip Bigcapital branches from ~25 lib/ and models/ files — done
  (commit 75223bd). lib/ and models/ grep-clean of "bigcapital".
- step: 3 — remove app/api/accounting/session, Finance dashboard route +
  components/accounting/*, from-accounting-card.tsx,
  integrations-manager.tsx's Bigcapital branch, worksheets/
  accounting-report-actions.ts, account/page.tsx, admin/integrations/
  page.tsx, jobs/process route branch — not started (a partial edit to
  integrations-manager.tsx was reverted this session to keep the tree
  buildable). See scratch/step3-scope.md for the exact file list.
- step: 4 — Accounting page reduced to "no ledger connected" + disabled
  Connect button; queue banners; docker-compose + env cleanup; seed.ts
  check — todo
- step: 5 — final grep-clean pass, tsc, vitest, migration applied on
  devdb, next build — todo
- step: G1 — round script (Accounting page + an Invoices queue, 1440/390,
  detector) — todo
- step: G2 — r0 and fixes — todo
- step: G3 — build-done (tsc full, Part B checks) — todo

## Artifacts

- Step 3 scope + notes: docs/wayfinder-reports/376/logs/scratch-380/step3-scope.md
- Step 2 diff: `git show 75223bd`

## Open findings

See docs/wayfinder-reports/376/logs/scratch-380/step3-scope.md.

## Next step

Re-run `npx prisma generate && npx tsc --noEmit 2>&1 | grep -i bigcapital`
for the current exact file list, then work through
scratch/step3-scope.md top to bottom, starting with
components/integrations/integrations-manager.tsx (has a client-side
BigcapitalRow polling flow to delete, not just an import). Commit as step
3 when tsc shows zero bigcapital errors outside prisma/schema.prisma and
the migration file, then continue to step 4.
