milestone: spec-done

Ticket body is the spec (exact file list, definition of done); no separate
spec doc needed.

## Plan

- step: 1 — delete lib/integrations/bigcapital + tests, models/bigcapital*.ts,
  migration dropping their tables — done
- step: 2 — strip Bigcapital branches from ~25 lib/ and models/ files — in progress, see Artifacts
- step: 3 — remove app/api/accounting/session, Finance dashboard route +
  components/accounting/*, from-accounting-card.tsx, worksheets/sidebar/
  account-page/integrations-page references, jobs/process branch — todo
- step: 4 — Accounting page reduced to "no ledger connected" + disabled
  Connect button; queue banners; docker-compose + env + config cleanup;
  seed.ts — todo
- step: 5 — final grep-clean pass, tsc, vitest, migration applied on
  devdb, next build — todo
- step: G1 — round script (Accounting page + an Invoices queue, 1440/390,
  detector) — todo
- step: G2 — r0 and fixes — todo
- step: G3 — build-done (tsc full, Part B checks) — todo

## Artifacts

- Grep baseline (stale, many already fixed): docs/wayfinder-reports/376/logs/scratch-380/grep-baseline.txt
- Step 2 remaining file list, exact line refs and order: docs/wayfinder-reports/376/logs/scratch-380/step2-remaining.md

## Open findings

No tsc/test run yet this session. worker/job-worker.ts and
models/workspaces.ts currently have dangling imports to files deleted in
step 1 — expected mid-step-2 state (fixed by step2-remaining.md items 9, 11),
not a regression.

## Next step

Work through docs/wayfinder-reports/376/logs/scratch-380/step2-remaining.md
in order (close-loop.ts first), re-grep to confirm step 2's file list is
clean, run `tsc --noEmit`, commit (`wip(autopilot): #380 build step 2`),
then continue to step 3.
