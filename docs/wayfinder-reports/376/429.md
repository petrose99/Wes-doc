# Autopilot report — #429 Build: per-line Accounts, guessed Default account and supplier account rules

- Map: #376 Map: Ledger connections through Nango — QuickBooks, Xero, Sage Accounting; Bigcapital removed
- Type: wayfinder:task · Started: (spec session, see hand-off history) · Finished: 2026-09-24
- Outcome: resolved

## What I did

This was a continuation (close) session on a ticket already at build-done/measured. Read the
hand-off (`docs/wayfinder-reports/376/429.handoff.md`) and `close.md`'s triage, then:

1. Brought dev servers up, re-ran the confirm round (shots-c1) with the two fixes already
   committed by the previous session (390px `flex-wrap` on the Default row, `aria-live="polite"`
   on the chart-sync loading copy). Gate clean (0 real, 0 new vs. baseline).
2. Ran confirm readers (critique/evaluate/include, fresh `sonnet` agents) on c1: critique 28/40
   (still below the 30-point hook minimum), evaluate 82/100, include provisional-pass both widths.
3. Diagnosed the critique's lowest heuristic (Flexibility and Efficiency of Use, 2/4: no real
   keyboard/Tab-order trace existed for this ticket's new controls) and fixed the capture tooling
   (`scripts/wayfinder-autopilot/round-429.mjs`): added a real `tabWalk` focus trace from the
   Supplier accounts table's "Forget" button, and discovered + recorded that the per-line Account
   `<select>` is intentionally `disabled` by design (`components/documents/line-items-editor.tsx`
   — a display-only field, no per-line override write path in this ticket's scope), so it correctly
   has no tab stop. Re-ran (shots-c3), gate clean, critique rose to 29/40 — sole remaining blocker:
   Help and Documentation (2/4, no contextual help for the Supplier accounts table).
4. Fixed that: added a one-line explanatory paragraph above the Supplier accounts table
   (`components/settings/supplier-accounts-table.tsx`) explaining what "Forget" does and what
   happens next, matching the existing empty-state copy's tone. First attempt overflowed the
   craft-floor's line-measure ceiling at 1440 (new `line-length` detector finding); constrained
   with `max-w-prose` and re-captured (shots-c5) — gate clean, no new findings.
5. Final confirm readers on c5: critique 30/40 (hook minimum met, no heuristic under 3), evaluate
   85/100 (anti-pattern Clean, 0 P0, 1 named/accepted P1).
6. `tsc --noEmit` clean. Stopped dev servers.
7. Created and wired the follow-up ticket for the one open gap (Supplier table's missing "Change"
   action vs. spec §B) as a child of map #376, then updated the map's Decisions-so-far with #429's
   one-line entry.
8. Posted the resolution comment and closed #429.

## Questions I answered on the owner's behalf

None — this was a close session on an already-specced, already-built ticket; no new design
decisions were made, only fixes to reach the measured bar plus one scoped-out gap turned into a
follow-up ticket per the standing delegation rule (never remove/redesign without sign-off, and a
UX-decision-shaped gap becomes a ticket, not an autopilot decision).

## Decisions recorded

Resolution comment on #429: shipped as built (per-line Account select, Default row Guessed badge,
Supplier accounts table Forget-only), confirm-round bar met after two fix batches (real keyboard
trace + Supplier-table help copy), Supplier table's "Change" action deferred to a named follow-up
ticket.

## Tickets created / changed

- Created [Supplier accounts table: Change action for learned account rules](https://github.com/petrose99/Wes-doc/issues/442),
  child of map #376 (linked via the tracker's native sub-issue relation). Carries the spec §B gap:
  Forget-only vs. spec's "Change / Forget", needs a UX decision (inline vs. modal editor) before
  buildable.
- No fog graduated beyond this — #442 is itself the graduation of the gap this ticket's close
  round surfaced.

## Scores and counts (rendered surfaces only)

scores: predicted-critique=n/a first-critique=25 close-critique=30 first-evaluate=76 close-evaluate=85

Gate: clean at every round (r1, c1, c3, c5) — 0 real findings, 0 new vs. baseline, all detector
finding types matched to `residue.txt` and verified pre-existing/app-wide in `close.md` (git-log-S
evidence, cross-referenced against `docs/agents/areas/detail-pane.md` and prior tickets #297/#361).
include: provisional pass both widths throughout (no round supplies a full ARIA/DOM trace, only
static captures + a real Tab-focus probe added this session) — the r1 P1 (chart-sync loading copy
needed `aria-live`) is fixed and confirmed in code; the keyboard-trace gap flagged as the include
reader's top finding at c1 is resolved at c3 with a real `tabWalk` sequence.
Pre-build step weak spot: the original capture script (`round-429.mjs`) never exercised a keyboard
probe on the ticket's own new controls (only content-state probes) — that gap is what held
Flexibility and Efficiency of Use at 2/4 through r1/c1; fixed in this session, not the build step.

## First-pass findings → lessons (rendered surfaces only)

| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| H7 Flexibility, no real keyboard trace (P2, r1/c1) | Capture-round scripts for a ticket with new interactive controls should always include at least one `tabWalk` probe on those controls, not just content-state probes | wrong (build-step gap) | Added `tabWalk` from the Supplier table's Forget button; discovered the Account select is intentionally non-interactive (disabled by design) |
| H10 Help and Documentation, no contextual help (P2, r1/c1/c3) | A populated (non-empty) state should carry the same explanatory copy the empty state already has, not less | wrong (copy gap) | Added a one-line explainer above the Supplier accounts table, matching the empty-state's tone |
| 390px Default-row overflow clipping the Guessed badge (P1, r1) | craft-floor's "run the real copy at every breakpoint" check | none (caught by the previous session's own r1 round) | `flex-wrap` fix, already committed before this session |
| Chart-sync loading copy missing `aria-live` (P1, r1, include) | craft-floor's "States" checklist (loading state should announce) | none (caught by the previous session's own r1 round) | `aria-live="polite"`, already committed before this session |
| Supplier table Change action missing vs. spec §B (P1, all rounds) | Spec conformance check at build gate should diff the built surface against every named column/action in the spec, not just render-and-capture | new class (scope decision needed) | Deferred to follow-up ticket #442, not fixed here |

## Needs the owner

None. The one open gap (Supplier table's "Change" action) is a UX-decision-shaped follow-up, not a
removal or a credentials/blocking issue — ticketed as #442, not resolved on the owner's behalf.
