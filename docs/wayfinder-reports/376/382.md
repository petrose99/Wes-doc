# Autopilot report — #382 Ledger connection model and the Nango boundary

- Map: #376 Map: Ledger connections through Nango — QuickBooks, Xero, Sage Accounting; Bigcapital removed
- Type: wayfinder:grilling · Started: 2026-09-22 · Finished: 2026-09-22
- Outcome: resolved

## What I did
1. Claimed #382 (`gh issue edit 382 --add-assignee @me`).
2. Established facts from code before grilling: `prisma/schema.prisma`'s `IntegrationConnection` model, `lib/integration-token-refresh.ts` (row-locked refresh), `lib/integration-push.ts` / `lib/integration-push-policy.ts` (push loop, attempt cap, `forceTerminal`), the four `app/api/integrations/{quickbooks,xero}/{connect,callback}` routes, and `CONTEXT.md`'s existing "Ledger connection" glossary entry (already names `Connected`/`Needs reconnecting`/absent).
3. Read the closed research tickets #378 (provider readiness/scopes/tenant-id shapes) and #379 (Nango Connect UI handshake, `AUTH` webhook, delete/proxy shapes) that this ticket's question depends on.
4. Ran one grilling round (7 questions, `grilling` + `domain-modeling`) — logged in full on the [resolution comment](https://github.com/petrose99/Wes-doc/issues/382#issuecomment-5772804675); every `➡️` answered per standing delegation, each grounded in the map's Notes, `CONTEXT.md`, or #378/#379's findings.
5. Wrote [ADR 0005 — Nango owns the Ledger connection's auth](../../../adr/0005-nango-owns-ledger-connection-auth.md): the retiring/added columns, the `connected`/`needs_reconnect` vocabulary and its triggers, the token boundary (Nango proxy, no local token ever), the push-pause bug fix (today's code burns an attempt on a disabled connection, contradicting the map's own Notes), the reconnect-not-import cutover, the Owner-email trigger, four stress scenarios, and seven ordered build steps.
6. Posted the resolution comment (Q&A table + ADR link + Action Summary) and closed #382.
7. Updated the map's Decisions so far with a one-line pointer to #382.
8. Created [Build: Nango boundary — IntegrationConnection, proxy, webhook, retire local token refresh](https://github.com/petrose99/Wes-doc/issues/394), carrying ADR 0005's backend steps 1–6; wired as a sub-issue of map #376, blocked by [Provision Nango and the three provider sandboxes](https://github.com/petrose99/Wes-doc/issues/381), and blocking both [Design the Accounting page and the connect flow](https://github.com/petrose99/Wes-doc/issues/383) and [Sage Accounting bill mapping](https://github.com/petrose99/Wes-doc/issues/384).

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Which columns retire, what's added? | Drop the 4 token/expiry columns + `scope`; add `providerConfigKey`; keep `externalTenantId`/`tenantName`; reuse `id` as the Nango connection id | Map Notes (one token owner); CONTEXT.md treats tenant identity as DocuBite's concern |
| 2 | Status vocabulary and triggers? | `connected`/`needs_reconnect`, absent = no row; flipped by the `AUTH` webhook, with a synchronous fallback on a caught auth error | CONTEXT.md's existing glossary; #379's finding that the webhook is authoritative, not the frontend promise |
| 3 | How does a push get a token with no local vault? | Nango proxy call per request; token never held by DocuBite; `integration-token-refresh.ts` retires | Map Notes — Nango is auth+proxy only |
| 4 | Does pausing on `needs_reconnect` cost an attempt? | No — pre-check before `computePushUpdate`, leave pending, fixed 5 min poke, no attempt increment (today's code is a bug against this) | Map Notes state this explicitly |
| 5 | Cutover for existing rows? | One-time reconnect; delete pre-Nango rows in the migration, no token import | Smallest reversible option — rows are demo data, no pilot firm exists |
| 6 | Owner email trigger/frequency? | The `needs_reconnect` transition, once per break | CONTEXT.md — "Owners are told once" |
| 7 | Do the four stress scenarios force a different shape? | Yes: `IntegrationPush`/`LedgerTransaction` move from `onDelete: Cascade` to `SetNull` so disconnect doesn't erase push history | `IntegrationPush`'s own doc comment calls itself the audit trail |

## Decisions recorded
ADR 0005 (`docs/adr/0005-nango-owns-ledger-connection-auth.md`): the full `IntegrationConnection` column/status/boundary redesign, four stress scenarios, and the seven-step ordered build plan (steps 1–6 backend, now #394; step 7 surface, #383).

## Tickets created / changed
- Created #394 "Build: Nango boundary — IntegrationConnection, proxy, webhook, retire local token refresh" (`wayfinder:task`), sub-issue of #376, blocked by #381, blocking #383 and #384.
- Closed #382.
- No fog graduated beyond what #394 now covers explicitly (the map's "Not yet specified" items on default-account heuristics, Sage VAT, notice copy, and Nango ops setup are untouched — none of them were sharpened by this ticket's answer).

## Scores and counts (rendered surfaces only)
No score: this ticket touched no rendered surface — `docs/adr/`, tracker only. Per CLAUDE.md's path-based gate, the design-ticket Intent/Impeccable agenda does not apply; `grilling` + `domain-modeling` were the skills called, per the map's Notes.

## First-pass findings → lessons (rendered surfaces only)
N/A — no rendered surface changed.

## Needs the owner
Nothing blocking. #394 is itself blocked by #381 (owner's HITL provisioning task) before it can be built and tested against real sandboxes.
