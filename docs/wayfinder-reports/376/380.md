# Autopilot report — #380 Build: remove Bigcapital completely; Accounting page reduced to 'no ledger connected'

- Map: #376 Ledger connections through Nango — QuickBooks, Xero, Sage Accounting; Bigcapital removed
- Type: wayfinder:build · Started: prior session (28882b2) · Finished: 2026-09-22
- Outcome: resolved

## What I did

1. **Step 1** (28882b2) — deleted `lib/integrations/bigcapital/` (client, mappers,
   config, errors, redirect-validator + tests) and `models/bigcapital*.ts`; dropped
   `BigcapitalAccount`, `BigcapitalMemberAccount`, `IntegrationProvisionJob` from
   `schema.prisma`; added migration `20260922010000_drop_bigcapital`.
2. **Step 2** (75223bd) — stripped Bigcapital branches from `lib/` and `models/`
   (config, finance actions, workspace-scope, solutions, integrations, audit-events,
   review-tasks, workspaces, close-loop reconciliation, autopublish, job-worker, seed).
3. **Step 3/5** (dca383d) — dropped the last Bigcapital strings from
   `finance/page.tsx`/`CONTEXT.md`; fixed three stale test bugs an earlier provider
   swap (bigcapital→xero) had left behind in `lib/health/sync.test.ts` and
   `actions.test.ts` (missing xero mock, wrong param order, stale stale-ms constant);
   full vitest suite green (284 files, 2770 tests).
4. **Step 4** (43ed43e) — rewrote `docs/agents/areas/finance.md` and
   `integrations.md` to describe the post-deletion surfaces; marked ADR-001
   superseded; applied migration `20260922010000_drop_bigcapital` to devdb; `next
   build` and vitest both green.
5. **Measure** (abf1d7a) — capture round found a real P0: with Bigcapital gone,
   the Invoices queue never showed the ticket's required "no ledger connected"
   banner, because `getLedgerConnectionBandStatus()` suppressed it whenever
   integrations were disabled — but "no ledger" is now every workspace's default
   state (no connect flow exists yet; that's a later ticket). Fixed in
   `lib/integration-push.ts` so the band always shows while disconnected, not only
   after a prior connection breaks; split the band's link label (Connect vs.
   Reconnect) in `components/queue/connection-band.tsx` so a never-connected
   workspace isn't told to "Reconnect" (H2/H4). Also removed a stray leftover
   active `integration_connections` row on the devdb seed workspace (an earlier
   session's manual bigcapital→xero swap, not reproducible from any committed seed
   script, was masking the no-ledger state this ticket requires).
6. Re-ran the capture round after the fix (r3): gate CLEAN against
   `docs/agents/areas/admin.md`'s app-wide residue, `tsc --noEmit` clean, vitest
   284/284 files green, `next build` clean.
7. This session: read the hand-off (already at milestone `measured`), confirmed
   nothing further was needed, and wrote this close report.

## Questions I answered on the owner's behalf

| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Should the "no ledger connected" banner show for every workspace by default now that Bigcapital (the only ledger) is gone, or only after a connection that later breaks? | Show it whenever disconnected, including "never connected" | Ticket's explicit definition-of-done line requires the banner; "no ledger" is the honest default state until the Nango connect flow ships (later ticket) — hiding it would silently misrepresent system status (H1) |
| 2 | Should the recovery link say "Reconnect" for a workspace that never had a ledger? | No — split into "Connect" (never connected) vs "Reconnect" (had one, broke) | Match between system and real world (H2); telling a first-time user to "reconnect" something they never connected is dishonest/confusing |

## Decisions recorded

Bigcapital removed end-to-end (code, schema, migration, docs); Accounting page and
Invoices queue now show an honest "no ledger connected" state by default until Nango
ships. Fixed a P0 gap where the queue banner didn't appear once Bigcapital's removal
made "no ledger" the universal default.

## Tickets created / changed

None created. No fog graduated by this ticket beyond what the map already tracks for
the Nango connect flow.

## Scores and counts (rendered surfaces only)

scores: predicted-critique=n/a first-critique=28 close-critique=35 first-evaluate=n/a close-evaluate=86

Gate: CLEAN at both 1440 and 390 on the r3 round (Finance page + Invoices queue),
against `docs/agents/areas/admin.md`'s accepted app-wide residue (cyan gradient,
Inter overuse, dev-overlay shadow glow, truncate-class ellipsis false positives).
r0/r1 rounds (pre-fix baseline) found the P0 missing-banner gap described above;
first-critique=28 is the prior round's score noted in the r3 report as "the prior
28/40 round flagged" the consistency gap the fix closed. `include`: no new keyboard
flow was added (existing disabled/hint pattern reused as-is); not separately
re-verified live this session since no interaction surface changed since r3.

## First-pass findings → lessons (rendered surfaces only)

| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| Queue banner missing when integrations disabled (H1, P0) | `getLedgerConnectionBandStatus()`'s null-on-disabled branch predates a world where "disabled" is the default state, not an edge case — a status helper needs a test for "no integration ever configured", not just "integration existed then broke" | new class | Fixed in `lib/integration-push.ts`; band now shows for both disconnected sub-states |
| Banner said "Reconnect" to never-connected workspaces (H2/H4, P1→fixed) | Recovery-link copy was written assuming a break-then-recover flow only | new class | Split label in `components/queue/connection-band.tsx` (Connect vs Reconnect) |
| Disabled-reason relies on native `title` on Finance's Connect button (H9, P1, open) | No contract yet requires visible (non-hover) disclosure text at disabled controls | unchecked | Left as-is — cosmetic, above the close bar (35/40, every heuristic ≥3); noted for future polish, not a new ticket per the r3-critique/r3-scores notes |

## Needs the owner

Nothing blocked. The P1 native-`title` disclosure gap on Finance's Connect button
(vs. the Invoices queue's visible `hint` text) is cosmetic and above the closing bar;
left as a documented future-polish item rather than a new ticket, per the hand-off's
"Open findings" note.
