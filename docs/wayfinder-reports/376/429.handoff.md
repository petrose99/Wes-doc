# Hand-off — #429 Build: per-line Accounts, guessed Default account and supplier account rules

milestone: spec-done

## Step plan

- step: 1 — schema migration — kind: backend — done
- step: 2 — chart sync on connect + Default guess function + tests — kind: backend — done
- step: 3a — pure per-line resolution chain + bill mappers — kind: backend — done
- step: 3b — wire resolver into document-review, eligibility check — kind: backend — done
- step: 4 — learn-on-approval upsert + archived-account fallback — kind: backend — done
- step: 5 — Detail pane per-line Account select + provenance + pre-approval hint — kind: surface — done
- step: 6 — Accounting page Default row states + Supplier accounts table — kind: surface — done
- step: 7 — Post dialog override removal + banner + queue ineligibility reason — kind: surface — moved to #441 (split 2026-09-24, owner's call: ticket too big)
- step: 8 — build gate: detector before/after, evaluate, critique — scoped to step 5 (Detail pane per-line Account) and step 6 (Accounting page) surfaces only — kind: surface — todo

## Artifacts

- Spec: docs/wayfinder-reports/376/429.spec.md (§B/§C are step 6/7's brief)
- Step 6: enhanced the existing `AccountingConnectionCard` (components/integrations/
  integrations-manager.tsx) rather than a new component — it already held the Default-account
  picker; added the neutral "Guessed" Badge, "Reading your chart of accounts…" / "Couldn't read…
  [Try again]" states, and flipped `defaultExpenseAccountGuessed` to `false` in
  `setWorkspaceIntegrationDefaultAccount` (models/integrations.ts) on an Owner's pick. New
  `components/settings/supplier-accounts-table.tsx` (filter box above 20 rows, empty state,
  archived-account inline note using the connection's Default name as the copy matrix specifies)
  wired into `app/(app)/workspaces/[workspaceId]/admin/integrations/page.tsx` as a new Panel below
  Account mapping. New `models/supplier-account-rules.ts` (list/delete) +
  `forgetSupplierAccountRuleAction` (integration-connection-actions.ts). New
  `listAccountingEntitiesIncludingInactive` (models/accounting-entities.ts) — the existing
  active-only query would otherwise hide archived accounts the table needs to name.
  Tests: models/integrations.test.ts, models/supplier-account-rules.test.ts,
  models/accounting-entities.test.ts — all new/changed model functions covered.
  `tsc --noEmit` clean; `npx vitest run models/ lib/integrations/ "app/(app)/workspaces/"` 540/540
  green.
- Live dev-server/in-page-detector check deferred to step 8's build gate (covers the whole
  surface at once), same call as step 5's close notes.
- craft-floor.md read this session.

## Open findings

- Devdb migration-history drift (`20260922010000_drop_bigcapital`), pre-existing and unrelated.
- Step 7 still needs to delete the dead per-row override `<select>` in
  `components/queue/post-confirm-dialog.tsx` (noted since step 3b) — untouched.
- Step 6's Supplier accounts table has no live-widths/keyboard verification yet — first pass at
  step 8's detector round.
- `LineAccountCell`'s select (step 5) stays deliberately `disabled` — no per-line override write
  path in this ticket's backend; unchanged this session.

## Next step

Step 7 is no longer this ticket's: the owner split it into #441 (Post dialog banner, override removal, queue "needs an Account" reason), blocked by #429. Do not touch `post-confirm-dialog.tsx` or the queues here.

Start step 8, the build gate, for the two surfaces this ticket ships: the Detail pane per-line Account (step 5) and the Accounting page Default row + Supplier accounts table (step 6). Detector before/after, `evaluate`, `critique`, `polish`, then set `milestone: build-done` and go on to measure/close. Re-read craft-floor.md at the start.
