# Hand-off — #429 Build: per-line Accounts, guessed Default account and supplier account rules

milestone: build-done

## Step plan

- step: 1 — schema migration — kind: backend — done
- step: 2 — chart sync on connect + Default guess function + tests — kind: backend — done
- step: 3a — pure per-line resolution chain + bill mappers — kind: backend — done
- step: 3b — wire resolver into document-review, eligibility check — kind: backend — done
- step: 4 — learn-on-approval upsert + archived-account fallback — kind: backend — done
- step: 5 — Detail pane per-line Account select + provenance + pre-approval hint — kind: surface — in progress, resume from scratch notes
- step: 6 — Accounting page Default row states + Supplier accounts table — kind: surface — todo
- step: 7 — Post dialog override removal + banner + queue ineligibility reason — kind: surface — todo
- step: 8 — build gate: detector before/after, evaluate, critique — kind: surface — todo

## Artifacts

- Spec: docs/wayfinder-reports/376/429.spec.md (§A is step 5's brief)
- Step 5 resume notes (what's wired, what's left, exact edit to make):
  docs/wayfinder-reports/376/logs/scratch-429/step5-notes.md
- craft-floor.md read this session.

## Open findings

- See scratch notes for: devdb migration drift (pre-existing, unrelated), step 7's dead
  post-dialog override select still pending, and why the new account select is `disabled`
  (no per-line override write path in this ticket's backend — flag to owner before adding one).

## Next step

Read docs/wayfinder-reports/376/logs/scratch-429/step5-notes.md and finish wiring
`LineAccountCell` into `LineItemsEditor`'s two Account-column render sites (data plumbing through
page.tsx/split-pane.tsx/line-items-section.tsx is done; the component is written but not yet
rendered). Then `tsc --noEmit`, add model tests, dev-server sanity check, mark step 5 done.
