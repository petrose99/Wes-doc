# Hand-off — #429 Build: per-line Accounts, guessed Default account and supplier account rules

milestone: build-done

## Step plan

- step: 1 — schema migration — kind: backend — done
- step: 2 — chart sync on connect + Default guess function + tests — kind: backend — done
- step: 3a — pure per-line resolution chain + bill mappers — kind: backend — done
- step: 3b — wire resolver into document-review, eligibility check — kind: backend — done
- step: 4 — learn-on-approval upsert + archived-account fallback — kind: backend — done
- step: 5 — Detail pane per-line Account select + provenance + pre-approval hint — kind: surface — done
- step: 6 — Accounting page Default row states + Supplier accounts table — kind: surface — done
- step: 7 — Post dialog override removal + banner + queue ineligibility reason — kind: surface — moved to #441
- step: 8 — build gate: detector before/after, evaluate, critique — kind: surface — done (detector clean; readers not yet run)

## Artifacts

- Spec: docs/wayfinder-reports/376/429.spec.md
- Round script (fixed this session — see scratch notes): scripts/wayfinder-autopilot/round-429.mjs
- Captures: docs/wayfinder-reports/376/logs/scratch-429/shots-r0 (build), shots-r1 (measure) — both
  gated CLEAN (0 real findings, 0 new vs baseline; residue: scratch-429/residue.txt); gate-r0.json,
  gate-r1.json alongside.
- `tsc --noEmit`: clean.
- Session notes (fixture fixes, triage detail, screenshot paths):
  docs/wayfinder-reports/376/logs/scratch-429/session-notes.md
- Servers stopped clean at hand-off.

## Open findings

- Pointer: docs/wayfinder-reports/376/logs/scratch-429/session-notes.md — round-script route fix,
  doc-fixture codingData.items fix, detector residue triage detail.
- Devdb migration-history drift (`20260922010000_drop_bigcapital`), pre-existing, unrelated.

## Next step

Build the contact sheet from shots-r1, then launch the 3 fresh-context readers
(critique/evaluate/include) per phases/measure.md step 5 (they have not run yet). Record
`scores:`/`milestone: measured`, then run phases/close.md (report, resolution comment, close #429).
