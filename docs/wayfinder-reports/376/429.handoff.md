# Hand-off — #429 Build: per-line Accounts, guessed Default account and supplier account rules

milestone: build-done

## Step plan

- step: 1 — schema migration (SupplierAccountRule, defaultExpenseAccountGuessed, per-line account fields) — kind: backend — done
- step: 2 — chart sync on connect + Default guess function + tests — kind: backend — done
- step: 3a — pure per-line resolution chain (lib/finance/line-account-resolution.ts) + bill mappers post per line + drop accountRef param — kind: backend — done
- step: 3b — wire resolver into document-review (populate codingData.items once), "every line has an Account" eligibility check, drop accountOverrides — kind: backend — done
- step: 4 — learn-on-approval upsert + archived-account fallback — kind: backend — todo
- step: 5 — Detail pane per-line Account select + provenance + pre-approval hint — kind: surface — todo
- step: 6 — Accounting page Default row states + Supplier accounts table — kind: surface — todo
- step: 7 — Post dialog: remove per-row override, add first-post/guessed banner, queue ineligibility reason — kind: surface — todo
- step: 8 — build gate: detector before/after, evaluate, critique per design-tickets.md bar — kind: surface — todo

## Artifacts

- Spec: docs/wayfinder-reports/376/429.spec.md
- Step 3b (this session, committed after this hand-off):
  - `models/documents.ts`: new exported `resolveDocumentCodingItems` (DB-touching orchestration
    around the pure chain in lib/finance/line-account-resolution.ts) called from
    `updateDocumentReview`, stamping `codingData.items` once at Save review. Legacy documents
    (`usesLegacyAccountChain`) route through `resolveCategoryAccount`; current documents through
    supplier rule (`SupplierAccountRule` by `connectionId` + `normalizeSupplierName`) → connection
    Default. 5 new tests in `models/documents.test.ts` (orchestration only — the pure chain's own
    branches are already covered in `lib/finance/line-account-resolution.test.ts`).
  - `lib/integration-push-selection.ts`: `resolveSelectionEligibility` now reads
    `codingData.items`, ineligible with reason `"needs an Account"` if any line has no
    `account_external_id`. `filterOverridesToEligible` deleted (test file updated to match).
  - `app/(app)/workspaces/[workspaceId]/integration-push-actions.ts`: `pushDocumentToConnection`
    / `pushDocumentToAccountingAction` / `pushAllReadyDocumentsAction` all dropped their
    `expenseAccountId`/`accountOverrides` param; `pushDocumentToConnection` now reads
    `codingData.items` and passes it as `normalizeBillFromDocument`'s `lineAccounts`. The old
    per-push category-mapping resolution is gone (resolution now happens once at review time);
    `payload.expenseAccountId` still carries the connection Default for the preflight cache check.
  - `app/(app)/workspaces/[workspaceId]/post-selected-documents-actions.ts`: dropped
    `accountOverrides` param and the `filterOverridesToEligible` call.
  - `components/queue/post-confirm-dialog.tsx`: **compile-only** fix — its call to
    `postSelectedDocumentsAction` no longer passes the (now nonexistent) 4th `overrides` arg. The
    per-row account-override `<select>`/`overrides` state is still there, now dead (never sent to
    the server) — step 7 (surface) is where spec item C ("remove the per-row extraColumn
    account-override select") actually deletes it; do not re-touch it as a "fix" outside step 7.
  - `npx tsc --noEmit` clean; full `npx vitest run` green (290 files, 2811 tests).

## Open findings

- Pre-existing devdb migration-history drift on 20260922010000_drop_bigcapital blocks
  `prisma migrate dev` on the shared devdb — out of scope, unrelated to this ticket.
- Step 4 (learn-on-approval + archived-account fallback) is next: find the "document's status
  transitions to approved" point (grep `status.*approved` in `lib/workflow`/`review-actions.ts` —
  NOT `updateDocumentReview`, which sets status "reviewed"/"needs_review", not "approved"; the
  approval stage is distinct — `models/documents.ts:187` has a `case "approved":` worth reading
  first) and upsert `SupplierAccountRule` there with the account of the line with the largest
  `amount`, keyed on `[connectionId, normalizeSupplierName(vendor)]` (unique constraint already
  matches that shape). Also: bump `lastUsedAt` on every later push that uses the rule, and the
  archived-account fallback (a resolved `accountExternalId` whose `AccountingEntity` is no longer
  active falls back to Default, flagged for the surface layer).

## Next step

Implement step 4: the learn-on-approval upsert (`SupplierAccountRule`) and the archived-account
fallback, with red-first tests, per spec.md backend points 5-6. Re-read spec.md points 5-6 before
writing code — do not re-research steps 1-3b, already done and tested.
