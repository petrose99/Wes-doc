# Hand-off — #429 Build: per-line Accounts, guessed Default account and supplier account rules

milestone: measured

## Step plan

- step: 1 — schema migration — kind: backend — done
- step: 2 — chart sync on connect + Default guess function + tests — kind: backend — done
- step: 3a — pure per-line resolution chain + bill mappers — kind: backend — done
- step: 3b — wire resolver into document-review, eligibility check — kind: backend — done
- step: 4 — learn-on-approval upsert + archived-account fallback — kind: backend — done
- step: 5 — Detail pane per-line Account select + provenance + pre-approval hint — kind: surface — done
- step: 6 — Accounting page Default row states + Supplier accounts table — kind: surface — done
- step: 7 — Post dialog override removal + banner + queue ineligibility reason — kind: surface — moved to #441
- step: 8 — build gate: detector before/after, evaluate, critique — kind: surface — done, measured

## Artifacts

- Spec: docs/wayfinder-reports/376/429.spec.md
- Round script: scripts/wayfinder-autopilot/round-429.mjs
- Captures: docs/wayfinder-reports/376/logs/scratch-429/shots-r0 (build), shots-r1 (measure).
  gate-r0.json (raw, not clean: real=69, byWidth 1440=41/390=28); gate-r1.json (with
  --residue-file, real=0, newSinceBaseline=0). Contact sheet:
  docs/wayfinder-reports/376/logs/scratch-429/shots-r1/contact-sheet.png.
- `tsc --noEmit`: clean.

## Raw findings (untriaged)

scores: critique 25/40 (bar 32, NOT MET) | evaluate health 76/100, anti-pattern Clean | include
provisional pass at 1440 and 390 (keyboard.json is a content-state probe, not a real Tab-order
trace — flagged as a P1 gap, not evidence of failure)

- critique.md: docs/wayfinder-reports/376/logs/scratch-429/readers-r1/critique.md — lowest
  heuristics Consistency & Standards 2/4 (layout clipping, 397% column overflow), Aesthetic &
  Minimalist 2/4 (repeated cyan-gradient/dark-glow/nested-cards noise); also 2/4 on Flexibility &
  Efficiency, Help & Documentation.
- evaluate.md: docs/wayfinder-reports/376/logs/scratch-429/readers-r1/evaluate.md — P1: Supplier
  accounts table has no "Change" action (spec calls for Change/Forget, only Forget shipped); P1:
  chart-sync pending/failed(retry) states uncaptured.
- include.md: docs/wayfinder-reports/376/logs/scratch-429/readers-r1/include.md — P1: no real
  keyboard-trace capture exists (keyboard.json is badge/copy-visibility probes only, not
  Tab-order); P1: chart-sync loading copy aria-live unconfirmed; P1: per-line Account select label
  association unconfirmed (screenshots show adjacent caption text only,
  components/pipeline/document-detail/line-items-section.tsx).
- Devdb migration-history drift (`20260922010000_drop_bigcapital`), pre-existing, unrelated.

## Next step

Run phases/close.md: triage the three readers' raw findings above against gate-r0/r1 (the
critique reader's clipping/overflow findings and gate-r0's 69 raw hits need reconciling — the
419 raw-vs-residue-filtered gap between gate-r0 and gate-r1 is unexplained and should be checked
first, since critique is well under the 32/40 bar). Fix what's real, re-measure, then write the
report and close #429 only once close-critique >= 30 and close-evaluate >= 80 are real.
