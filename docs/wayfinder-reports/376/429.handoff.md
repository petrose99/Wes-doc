# Hand-off — #429 Build: per-line Accounts, guessed Default account and supplier account rules

milestone: build-done

## Step plan

- step: 1 — schema migration (SupplierAccountRule, defaultExpenseAccountGuessed, per-line account fields) — kind: backend — done
- step: 2 — chart sync on connect + Default guess function + tests — kind: backend — todo
- step: 3 — per-line resolution chain + bill mappers post per line + eligibility + drop accountOverrides — kind: backend — todo
- step: 4 — learn-on-approval upsert + archived-account fallback — kind: backend — todo
- step: 5 — Detail pane per-line Account select + provenance + pre-approval hint — kind: surface — todo
- step: 6 — Accounting page Default row states + Supplier accounts table — kind: surface — todo
- step: 7 — Post dialog: remove per-row override, add first-post/guessed banner, queue ineligibility reason — kind: surface — todo
- step: 8 — build gate: detector before/after, evaluate, critique per design-tickets.md bar — kind: surface — todo

## Artifacts

- Spec: docs/wayfinder-reports/376/429.spec.md
- Migration: prisma/migrations/20260924180000_add_supplier_account_rules/migration.sql (additive:
  `integration_connections.default_expense_account_guessed`, new `supplier_account_rules` table)
- Schema: prisma/schema.prisma — IntegrationConnection.defaultExpenseAccountGuessed, new
  SupplierAccountRule model (~line 2173), codingData doc comment extended for per-line
  account_external_id/account_source (~line 601). Per-line fields live in the existing
  codingData JSON, no schema change needed for them.
- Applied directly to devdb (127.0.0.1:55433) via psql + manual _prisma_migrations row, because
  the shared devdb has pre-existing drift on 20260922010000_drop_bigcapital (checksum mismatch)
  that makes `prisma migrate dev` demand a full reset — did not reset (would drop other sessions'
  data). Migration SQL was generated with
  `prisma migrate diff --from-schema <git-show HEAD schema> --to-schema prisma/schema.prisma --script`.
  `prisma generate` run after; `npx vitest run` — 44 files / 486 tests green.

## Open findings

- Pre-existing devdb migration-history drift on 20260922010000_drop_bigcapital blocks
  `prisma migrate dev` for anyone on this shared devdb until reconciled (out of this ticket's
  scope — not caused by this session).

## Next step

Start step 2: chart sync on connect (call lib/integrations/sync.ts from
integration-connection-actions.ts's connect-completion path, per spec "Backend changes" point 1)
+ the Default-account guess function (new default-account-guess.ts per provider: exact match on
the provider's standard catch-all name, expense-type accounts only) + tests (red first). Read
spec.md "Backend changes" points 1–2 for the exact contract.
