# Hand-off — #429 Build: per-line Accounts, guessed Default account and supplier account rules

milestone: spec-done

## Step plan

- step: 1 — schema migration — kind: backend — done
- step: 2 — chart sync on connect + Default guess function + tests — kind: backend — done
- step: 3a — pure per-line resolution chain + bill mappers — kind: backend — done
- step: 3b — wire resolver into document-review, eligibility check — kind: backend — done
- step: 4 — learn-on-approval upsert + archived-account fallback — kind: backend — done
- step: 5 — Detail pane per-line Account select + provenance + pre-approval hint — kind: surface — done
- step: 6 — Accounting page Default row states + Supplier accounts table — kind: surface — done
- step: 7 — Post dialog override removal + banner + queue ineligibility reason — kind: surface — moved to #441
- step: 8 — build gate: detector before/after, evaluate, critique — kind: surface — todo (in progress, see below)

## Artifacts

- Spec: docs/wayfinder-reports/376/429.spec.md
- Round script (this session): scripts/wayfinder-autopilot/round-429.mjs — covers Detail pane
  per-line Account (documents/<id>?full=1) and Accounting page Default row + Supplier accounts
  table (admin/integrations). Reuses shared `round()`/`roundArgs()` from capture-round.mjs.
- Dev-only fixtures seeded this session (NOT in prisma/seed.ts — re-run if the devdb resets):
  scripts/wayfinder-autopilot/tmp-seed429.mjs seeds one IntegrationConnection (provider
  quickbooks, backdated createdAt 2020-01-01 so existing docs resolve through the new chain, not
  the legacy fallback), 3 AccountingEntity rows (General Expenses/Office Supplies active, "Old
  Travel Account" inactive/archived), and 2 SupplierAccountRule rows ("acme supplies" →
  Office Supplies, "old vendor co" → the archived account, to exercise the archived-fallback
  row copy). Workspace: af91555d-7450-4b21-a8ac-73db092617c8. Re-running tmp-seed429.mjs will
  fail on the unique connection constraint if the connection already exists — check
  `integrationConnection.findMany` first (see scripts/wayfinder-autopilot/tmp-q429.mjs) before
  re-seeding.
- First partial round captured to docs/wayfinder-reports/376/logs/scratch-429/shots-r0/ (in
  progress — only state 01 re-captured at 1440 with the corrected URL, states 02/03/04 are from
  the earlier (working) run against admin/integrations). Not yet gated with gate.mjs.
- craft-floor.md read this session (continuation — no re-read needed next session unless a UI
  edit is made).

## Open findings

- **Route fix found this session**: the Detail pane's per-line Account select only renders under
  `BillSplitPane`, which the invoice detail route is `/workspaces/<ws>/documents/<documentId>`
  (not `/invoices/<documentId>`, which 302s back into the queue pane and never shows the select
  in a plain fetch preflight — see `app/(app)/workspaces/[workspaceId]/documents/[documentId]/
  page.tsx:189` `docType === "invoice"` branch → `BillSplitPane`). The round script now uses
  `${base}/documents/${DOC}?full=1`. Doc used: 684033b8-ae7c-449f-a53a-b0185c868609 (supplier
  "Lesotho Electricity Company", status ready_for_review, in the invoices queue).
- Detector findings on the corrected 01 state (1440 only, not yet triaged): ai-color-palette×3,
  text-overflow×1, clipped-overflow-container×4, overused-font×1, layout-transition×1,
  dark-glow×1, nested-cards×1, first-viewport-column-overflow×1 — all match app-wide/pane residue
  named in docs/agents/areas/detail-pane.md's "Detector residue" section (ai-color-palette,
  overused-font, dark-glow, layout-transition are the app-wide four; clipped-overflow-container
  is the named pane scroll-scaffolding residue) EXCEPT `nested-cards`×1 and
  `first-viewport-column-overflow`×1 and `text-overflow`×1 which need a live-DOM check against
  the new Account cell/provenance markup before ruling on — not yet done.
- 02/03/04 states (Accounting page) were captured against the OLD round script version before
  this session's URL fix — re-run the whole round (`node scripts/wayfinder-autopilot/round-429.mjs
  docs/wayfinder-reports/376/logs/scratch-429/shots-r0` — full 1440+390, no `--only`) fresh next
  session before triaging, since a partial dir mixes two runs. Prior run's keyboard probes DID
  pass: `guessed-badge-visible` true, `archived-row-copy` true (both widths) — the "Guessed" badge
  and archived-account inline note both render correctly against the seeded fixtures.
  `account-cell-visible` probe still says `false` in that stale keyboard.json (pre-URL-fix) —
  ignore it, re-run.
- gate.mjs has not been run yet this session — do that first next session, after the full re-run.
- Servers stopped clean (`node .impeccable/live/dev.mjs stop`) at hand-off.
- Devdb migration-history drift (`20260922010000_drop_bigcapital`), pre-existing and unrelated
  (noted by earlier sessions, still present, not this ticket's).

## Next step

Step 8 (build gate) is mid-flight. Next session: restart servers
(`node .impeccable/live/dev.mjs start af91555d-7450-4b21-a8ac-73db092617c8`), confirm the seeded
fixtures are still present (tmp-q429.mjs), run the FULL round fresh (both widths, no --only) to
`shots-r0`, then `node scripts/wayfinder-autopilot/gate.mjs shots-r0 --residue-file
<residue.txt built from detail-pane.md + integrations.md residue lines>` and fix real findings —
especially triage the three detail-pane findings named above (nested-cards,
first-viewport-column-overflow, the new text-overflow) since they're not yet confirmed as
pre-existing residue. Then `evaluate`/`critique` against the two surfaces, `tsc --noEmit`, write
`milestone: build-done`, and continue to measure/close per the autopilot phase briefs.
