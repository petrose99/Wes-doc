# Hand-off — #429 Build: per-line Accounts, guessed Default account and supplier account rules

milestone: build-done

## Step plan

- step: 1 — schema migration — kind: backend — done
- step: 2 — chart sync on connect + Default guess function + tests — kind: backend — done
- step: 3a — pure per-line resolution chain + bill mappers — kind: backend — done
- step: 3b — wire resolver into document-review, eligibility check — kind: backend — done
- step: 4 — learn-on-approval upsert + archived-account fallback — kind: backend — done
- step: 5 — Detail pane per-line Account select + provenance + pre-approval hint — kind: surface — done
- step: 6 — Accounting page Default row states + Supplier accounts table — kind: surface — todo
- step: 7 — Post dialog override removal + banner + queue ineligibility reason — kind: surface — todo
- step: 8 — build gate: detector before/after, evaluate, critique — kind: surface — todo

## Artifacts

- Spec: docs/wayfinder-reports/376/429.spec.md (§A is step 5's brief, §B/§C are step 6's)
- Step 5 close notes: docs/wayfinder-reports/376/logs/scratch-429/step5-notes.md (kept as history;
  its "NOT done" section is now done — both `LineAccountCell` render sites wired in
  components/documents/line-items-editor.tsx, `tsc --noEmit` clean, `getBillAccountPickerData`
  tests added to models/documents.test.ts, `npx vitest run models/documents.test.ts` 49/49 green).
  Live dev-server/in-page-detector check for step 5 deferred to step 8's build gate (which covers
  the whole surface at once) rather than spent twice.
- craft-floor.md read this session.

## Open findings

- Devdb migration-history drift (`20260922010000_drop_bigcapital`), pre-existing and unrelated.
- Step 7 still needs to delete the dead per-row override `<select>` in
  `components/queue/post-confirm-dialog.tsx` (noted since step 3b) — untouched.
- `LineAccountCell`'s select is deliberately `disabled` — no per-line override write path exists
  in this ticket's backend (one account per document). If a future need for an editable per-line
  override surfaces, that's new backend scope — flag to the owner, don't infer it from the spec's
  "native `<select>`" wording alone.

## Next step

Start step 6 (Accounting page): read spec §B/§C for the Default row states (guessed/confirmed/
retry) and the Supplier accounts table (list + "Forget"), find the Accounting page's existing
integration-connection card, and follow the same pattern as step 5 (read craft-floor.md is already
done this session if continuing same-session; otherwise re-read at start of the surface step).
