# Hand-off — #429 Build: per-line Accounts, guessed Default account and supplier account rules

milestone: build-done

## Step plan

- step: 1 — schema migration (SupplierAccountRule, defaultExpenseAccountGuessed, per-line account fields) — kind: backend — done
- step: 2 — chart sync on connect + Default guess function + tests — kind: backend — done
- step: 3a — pure per-line resolution chain (lib/finance/line-account-resolution.ts) + bill mappers post per line + drop accountRef param — kind: backend — done
- step: 3b — wire resolver into document-review (populate codingData.items once), "every line has an Account" eligibility check, drop accountOverrides — kind: backend — todo
- step: 4 — learn-on-approval upsert + archived-account fallback — kind: backend — todo
- step: 5 — Detail pane per-line Account select + provenance + pre-approval hint — kind: surface — todo
- step: 6 — Accounting page Default row states + Supplier accounts table — kind: surface — todo
- step: 7 — Post dialog: remove per-row override, add first-post/guessed banner, queue ineligibility reason — kind: surface — todo
- step: 8 — build gate: detector before/after, evaluate, critique per design-tickets.md bar — kind: surface — todo

## Artifacts

- Spec: docs/wayfinder-reports/376/429.spec.md
- Step 3a (this session, committed after this hand-off): new lib/finance/line-account-resolution.ts
  (pure, tested 10/10). lib/integration-bill-mapping.ts's NormalizedLineItem gained
  `accountExternalId`; normalizeBillFromDocument threads a new `lineAccounts` param. Both
  lib/integrations/{quickbooks,xero}/bill-mapper.ts post each line to its own accountExternalId
  (BillMappingError if missing), dropping the old accountRef/accountCode param.
  lib/integration-push.ts's pushToQuickbooks/pushToXero updated to match. `npx vitest run` green;
  `tsc --noEmit` clean.
- Step 3b research (not yet implemented): docs/wayfinder-reports/376/logs/scratch-429/step3b-notes.md

## Open findings

- Pre-existing devdb migration-history drift on 20260922010000_drop_bigcapital blocks
  `prisma migrate dev` on the shared devdb — out of scope.
- Step 3b wiring point, SupplierAccountRule lookup key, eligibility check, accountOverrides
  removal call sites: all researched and written up in the scratch file above — read that before
  starting 3b, do not re-research.

## Next step

Implement step 3b per docs/wayfinder-reports/376/logs/scratch-429/step3b-notes.md: wire the
resolver into `models/documents.ts::updateDocumentReview`, add the eligibility check to
resolveSelectionEligibility, drop accountOverrides from postSelectedDocumentsAction/
pushDocumentToConnection. Re-read spec.md points 3-7 before writing code. Commit, then step 4.
