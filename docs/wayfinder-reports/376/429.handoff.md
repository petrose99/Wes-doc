# Hand-off — #429 Build: per-line Accounts, guessed Default account and supplier account rules

milestone: build-done

## Step plan

- step: 1 — schema migration — kind: backend — done
- step: 2 — chart sync on connect + Default guess function + tests — kind: backend — done
- step: 3a — pure per-line resolution chain + bill mappers — kind: backend — done
- step: 3b — wire resolver into document-review, eligibility check — kind: backend — done
- step: 4 — learn-on-approval upsert + archived-account fallback — kind: backend — done
- step: 5 — Detail pane per-line Account select + provenance + pre-approval hint — kind: surface — done
- step: 6 — Accounting page Default row states + Supplier accounts table — kind: surface — done
- step: 7 — Post dialog override removal + banner + queue ineligibility reason — kind: surface — moved to #441
- step: 8 — build gate: detector before/after, evaluate, critique — kind: surface — done (detector clean; readers launched, not yet returned)

## Artifacts

- Spec: docs/wayfinder-reports/376/429.spec.md
- Round script: scripts/wayfinder-autopilot/round-429.mjs
- Captures: docs/wayfinder-reports/376/logs/scratch-429/shots-r0 (build), shots-r1 (measure) — both
  gated CLEAN (0 real findings, 0 new vs baseline; residue: scratch-429/residue.txt); gate-r0.json,
  gate-r1.json alongside.
- Contact sheet: docs/wayfinder-reports/376/logs/scratch-429/shots-r1/contact-sheet.png (built this
  session).
- `tsc --noEmit`: clean.
- Servers stopped clean at hand-off.

## Open findings

- Reader agents (critique, evaluate, include) were launched this session as fresh-context Agent
  calls against the shots-r1 contact sheet + detector.json + keyboard.json + spec, per
  phases/measure.md step 5. They run in the background and had not returned when this session hit
  its stop point — the driver killed them along with the session, so **they must be re-launched**
  by the next session, not awaited. Their target output paths (not yet written, or partially
  written if a run got far enough): docs/wayfinder-reports/376/logs/scratch-429/readers-r1/
  {critique,evaluate,include}.md
- Devdb migration-history drift (`20260922010000_drop_bigcapital`), pre-existing, unrelated.

## Next step

Re-launch the 3 fresh-context readers (critique/evaluate/include) per phases/measure.md step 5,
each a foreground `Agent` call (do not end the turn until they return — background agents die with
the session). Point them at shots-r1/contact-sheet.png, shots-r1/detector.json,
shots-r1/keyboard.json, and 429.spec.md. Record the `scores:`/headline lines and
`milestone: measured` on this hand-off, commit, then run phases/close.md (report, resolution
comment, close #429).
