# Hand-off — #429 Build: per-line Accounts, guessed Default account and supplier account rules

milestone: build-done

## Step plan

- step: 1 — schema migration (SupplierAccountRule, defaultExpenseAccountGuessed, per-line account fields) — kind: backend — done
- step: 2 — chart sync on connect + Default guess function + tests — kind: backend — done
- step: 3 — per-line resolution chain + bill mappers post per line + eligibility + drop accountOverrides — kind: backend — todo
- step: 4 — learn-on-approval upsert + archived-account fallback — kind: backend — todo
- step: 5 — Detail pane per-line Account select + provenance + pre-approval hint — kind: surface — todo
- step: 6 — Accounting page Default row states + Supplier accounts table — kind: surface — todo
- step: 7 — Post dialog: remove per-row override, add first-post/guessed banner, queue ineligibility reason — kind: surface — todo
- step: 8 — build gate: detector before/after, evaluate, critique per design-tickets.md bar — kind: surface — todo

## Artifacts

- Spec: docs/wayfinder-reports/376/429.spec.md
- Migration: prisma/migrations/20260924180000_add_supplier_account_rules/migration.sql (additive)
- Schema: prisma/schema.prisma — IntegrationConnection.defaultExpenseAccountGuessed,
  SupplierAccountRule model (~line 2173), codingData doc comment for per-line
  account_external_id/account_source (~line 601).
- Step 2 commit 80caf3d: app/api/webhooks/nango/route.ts and
  app/(app)/workspaces/[workspaceId]/integration-connection-actions.ts
  (confirmSageBusinessAction) now call lib/integrations/sync.ts's
  syncAccountingEntities on connect completion; sync.ts guesses the Default
  account off the freshly-synced chart (only while defaultExpenseAccountGuessed)
  via lib/integrations/{quickbooks,xero}/default-account-guess.ts (pure,
  tested). QuickBooks/Xero client.ts listAccounts now carry accountType/
  accountClass. `npx vitest run` — 289 files / 2797 tests green; `tsc --noEmit`
  clean.

## Open findings

- Pre-existing devdb migration-history drift on 20260922010000_drop_bigcapital
  blocks `prisma migrate dev` for anyone on this shared devdb — out of scope.

## Next step

Start step 3: per-line account resolution chain in lib/finance/actions.ts
(supplier rule → Default, replacing the mappings→category-map→default chain
for lines whose codingSource postdates connection; populate account_external_id/
account_source once, at the trigger spec.md names — "Backend changes" point 3).
Then extend NormalizedLineItem with accountExternalId (lib/integration-bill-
mapping.ts) and change both bill-mappers to read it per line instead of a
single accountRef param; add "every line has an Account" to
resolveSelectionEligibility (lib/integration-push-selection.ts). Read spec.md
"Backend changes" points 3–4 for the exact contract. Red-first tests per
CODING_STANDARDS.md.
