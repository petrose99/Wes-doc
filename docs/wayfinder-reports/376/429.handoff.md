# Hand-off — #429 Build: per-line Accounts, guessed Default account and supplier account rules

milestone: spec-done

## Step plan

- step: 1 — schema migration (SupplierAccountRule, defaultExpenseAccountGuessed, per-line account fields) — kind: backend — todo
- step: 2 — chart sync on connect + Default guess function + tests — kind: backend — todo
- step: 3 — per-line resolution chain + bill mappers post per line + eligibility + drop accountOverrides — kind: backend — todo
- step: 4 — learn-on-approval upsert + archived-account fallback — kind: backend — todo
- step: 5 — Detail pane per-line Account select + provenance + pre-approval hint — kind: surface — todo
- step: 6 — Accounting page Default row states + Supplier accounts table — kind: surface — todo
- step: 7 — Post dialog: remove per-row override, add first-post/guessed banner, queue ineligibility reason — kind: surface — todo
- step: 8 — build gate: detector before/after, evaluate, critique per design-tickets.md bar — kind: surface — todo

## Artifacts

- Spec: docs/wayfinder-reports/376/429.spec.md (problem, data model, backend + surface changes,
  copy matrix, fortify state inventory, ethical review, this same step plan)
- Source decision: #402 resolution / ADR 0011 (gh issue view 402)
- Recon notes folded into the spec's "Backend changes" section (file:line pointers to
  lib/integrations/{quickbooks,xero}/bill-mapper.ts, lib/finance/actions.ts,
  components/queue/post-confirm-dialog.tsx, components/settings/category-account-mapping-table.tsx,
  lib/integrations/sync.ts, prisma/schema.prisma ~601/1290-1320/2130-2195)

## Open findings

None yet — no code written this session (spec only).

## Next step

Start step 1: additive Prisma migration for SupplierAccountRule, IntegrationConnection.
defaultExpenseAccountGuessed, and per-line account_external_id/account_source in codingData, per
the spec's "Data model changes" section; write the migration and a model-layer test, run
`prisma migrate dev`, commit, mark step 1 done.
