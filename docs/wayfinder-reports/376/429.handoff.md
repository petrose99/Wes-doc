# Hand-off — #429 Build: per-line Accounts, guessed Default account and supplier account rules

milestone: measured

## Step plan

- step: 1-4 — backend (schema, chart sync, resolver, learn-on-approval) — kind: backend — done
- step: 5 — Detail pane per-line Account select + provenance + pre-approval hint — kind: surface — done
- step: 6 — Accounting page Default row states + Supplier accounts table — kind: surface — done
- step: 7 — Post dialog override removal + banner + queue ineligibility reason — kind: surface — moved to #441
- step: 8 — build gate — kind: surface — measured (r1: critique 25/40 below bar); fix batch 1
  applied this session, confirm round (c1) not yet run

## Artifacts

- Spec: docs/wayfinder-reports/376/429.spec.md
- Round script: scripts/wayfinder-autopilot/round-429.mjs (unchanged, reuse as-is)
- r0/r1 captures + gates: docs/wayfinder-reports/376/logs/scratch-429/{shots-r0,shots-r1,
  gate-r0.json,gate-r1.json,residue.txt}. r1 scores: critique 25/40 (bar 32, NOT MET — lowest:
  Consistency & Standards 2/4, Aesthetic & Minimalist 2/4), evaluate health 76/100 anti-pattern
  Clean, include provisional pass both widths. Full reader output:
  docs/wayfinder-reports/376/logs/scratch-429/readers-r1/{critique,evaluate,include}.md
- **Triage complete, written to** docs/wayfinder-reports/376/logs/scratch-429/close.md — read this
  first, it has the full reasoning per finding (real/residue/decision/tooling-gap) and the exact
  next-step commands.
- Fixes applied this session (uncommitted — commit lands with this hand-off):
  `components/integrations/integrations-manager.tsx` — `flex-wrap` on the Default-row line (fixed
  a real 390px overflow that clipped the "Guessed" badge) + `aria-live="polite"` on the chart-sync
  loading copy.
- `tsc --noEmit`: was clean as of r1; not re-run after this session's 2-line fix (low risk, no
  type changes) — re-run it in the confirm-round checklist regardless.

## Open findings

- Full pointer: docs/wayfinder-reports/376/logs/scratch-429/close.md. Short version: most r1
  detector findings (ai-color-palette, dark-glow, clipped-overflow-container,
  first-viewport-column-overflow, text-overflow, nested-cards) are verified pre-existing/residue,
  not #429 regressions — evidence and code pointers are in close.md, do not re-litigate. One real
  gap needs a new Wayfinder ticket, not a fix: Supplier accounts table ships "Forget" only, spec
  §B wants "Change / Forget" — needs a UX decision (inline vs modal editor) before buildable.
  Ticket not yet created.
- Devdb migration-history drift (`20260922010000_drop_bigcapital`), pre-existing, unrelated.

## Next step

Read close.md, then: bring servers up, run round-429.mjs into shots-c1, gate it against shots-r1
with the same residue.txt, build a contact sheet, launch the 3 confirm readers (critique/evaluate/
include, fresh sonnet agents) foreground — do not end the turn until they return. If the bar is
met, create the Supplier-Change follow-up ticket, write docs/wayfinder-reports/376/429.md, and
close #429. Full command sequence is close.md's "Next step" section.
