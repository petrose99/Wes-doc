milestone: closed

# #396 — Nango boundary build hand-off

`kind: backend`. ADR 0005's "Ordered build steps" 1–7 all done. Ticket closed.

## Plan (ADR order)

- step: 1 — Schema migration — done
- step: 2 — `lib/nango.ts` (session mint, `nangoProxy`, HMAC verify) — done
- step: 3 — `app/api/webhooks/nango/route.ts` — done
- step: 4 — `app/api/integrations/connect-session/route.ts` + delete 4 old QB/Xero routes — done
- step: 5 — QuickBooks/Xero clients onto `nangoProxy`; Sage client (per #378) — done
- step: 6 — Push-loop pre-check, status-string updates, drop `no_default_account` — done
- step: 7 — build gate: `tsc --noEmit`, targeted tests, standards review — done

## Artifacts

- Commits `8f5a0f6`, `b06be62`, `7f52945`, `21a464c`/`6c43016`, `52b1f84`.
- `docs/wayfinder-reports/376/logs/scratch-396/step6-notes.md` — step 6 detail.
- `docs/wayfinder-reports/376/396.md` — close report.
- `lib/integration-push.test.ts` — new, 5 tests, passing.

## Open findings

None. `npx tsc --noEmit -p .` clean; `npx vitest run` → 286 files, 2780 tests, all passing
(re-confirmed at close). Follow-on connect-UI ticket #397 already exists (`kind: surface`),
no creation needed.

## Next step

None — ticket closed.
