milestone: build-step-6-todo

# #396 — Nango boundary build hand-off

`kind: backend`. ADR 0005's "Ordered build steps" 1–6 are the plan; step 7 is #383.

## Plan (ADR order)

- step: 1 — Schema migration — done
- step: 2 — `lib/nango.ts` (session mint, `nangoProxy`, HMAC verify) — done
- step: 3 — `app/api/webhooks/nango/route.ts` — done
- step: 4 — `app/api/integrations/connect-session/route.ts` + delete 4 old QB/Xero routes — done
- step: 5 — QuickBooks/Xero clients onto `nangoProxy`; Sage client (per #378) — done
- step: 6 — Push-loop pre-check, status-string updates, drop `no_default_account` — todo
- step: 7 — build gate: `tsc --noEmit`, targeted tests, standards review — todo

## Artifacts

- `app/api/webhooks/nango/route.ts` + `route.test.ts` (9 tests) — HMAC-verifies, handles `type:
  "auth"` only (others acked 200/ignored); `operation: "creation", success: true` reads
  `externalTenantId`/`tenantName` off `getConnectionConfig` for quickbooks/xero (`realmId`/
  `tenantId`), skips the lookup for sage (no such field — ADR 0005), upserts via
  `createIntegrationConnectionFromNango`; `success: false` flips via
  `markIntegrationConnectionNeedsReconnect` and emails owners (`resolveOwnerRecipients` +
  `sendReminderEmail`, reused from models/reminders.ts/lib/email.ts, no new template) only on
  `isNewBreak` (idempotent on redelivery); workspaceId comes off `endUser.endUserId` per Nango's
  documented auth-webhook shape (WebSearch-confirmed, no local fixture existed for it).
  `vitest.config.ts` gained an include line for this route (shipped-dark-webhook pattern, same as
  inbound-email/inbound-whatsapp).
- `lib/integrations/quickbooks/{client,config}.ts`, `lib/integrations/xero/{client,config}.ts` —
  rewritten onto `nangoProxy`; OAuth-flow-only exports deleted.
- `lib/integrations/sage/{client,config,errors}.ts` — new, scoped to proxy plumbing +
  `listBusinesses` only (full entity CRUD deferred to #384, which fixes the real field shapes first).
- `lib/integrations/sync.ts`, `lib/health/sync.ts`, `lib/health/actions.ts` — call sites moved off
  `getValidAccessToken` onto `connection.id` (which doubles as Nango's connectionId, confirmed via
  `models/integrations.ts`'s `createIntegrationConnectionFromNango` using it as the row's own `id`).
- `lib/integrations/sync.test.ts`, `lib/health/sync.test.ts`, `lib/health/actions.test.ts` — updated
  (dropped stale `integration-token-refresh` mocks, fixed one assertion) — **5 test files, 31 tests,
  all pass**: `npx vitest run lib/integrations/quickbooks/client.test.ts lib/integrations/xero/client.test.ts lib/integrations/sync.test.ts lib/health/sync.test.ts lib/health/actions.test.ts`.
- `lib/health/actions.ts` — also fixed a pre-existing (step-1 fallout, not Nango-shaped) TS error:
  `transaction.connectionId` is nullable post-migration, added an early return.
- `models/ledger-payments.ts` — same step-1 fallout, filtered nulls out of `connectionIds`.

## Open findings

`npx tsc --noEmit -p .` is still red, but **only** in files explicitly deferred to step 6 —
confirmed after step 4 landed (the old QB/Xero connect/callback routes are gone, so their errors
are gone too): `app/(app)/workspaces/[workspaceId]/admin/integrations/page.tsx` (2 errors,
`config.integrations.quickbooks/xero`), `lib/integration-push.ts` (17 errors, nullable
`connection`), `lib/integration-token-refresh.ts` (2 errors, `refreshTokens` no longer exported —
this file retires at step 6, callers move to the push-loop pre-check). No other file errors. Do
not chase these; they resolve when step 6 lands.

Step 4 added `app/api/integrations/connect-session/route.ts` (`POST`, owner+plan-gated, same
checks the retired routes ran, mints via `createConnectSession` from lib/nango.ts) and deleted the
four old route files. No frontend wiring here — that's #383 (ADR step 7, `kind: surface`), which
will call this route and Nango's `nango.auth()` SDK. `lib/integration-oauth-state.ts` (+ its test)
is now unused dead code from the old flow; left alone — not named by any step, and removing it is
a judgment call for step 6 or a cleanup pass, not this one.

`app/(app)/workspaces/[workspaceId]/admin/integrations/page.tsx` also errors (reads
`config.integrations.quickbooks/xero`, removed at step 2) — this is the "mechanical rename, not a
redesign" UI fix already named for step 6 in the ADR (status strings + `accountingProviders` →
`nango.enabled`). Left untouched this step on purpose; it's a `kind: surface`-adjacent one-line
prop change, not a Nango-client change, and belongs with step 6's other status-string work.

Full original step-5 scope notes (now done) were at
`docs/wayfinder-reports/376/logs/scratch-396/step5-notes.md` — superseded by this file, kept for
history.

## Next step

Step 6: push-loop pre-check in `lib/integration-push.ts` (pause on `needs_reconnect` without
burning an attempt — fixes its 17 nullable-`connection` errors as a side effect of the rewrite),
retire/rewrite `lib/integration-token-refresh.ts`'s callers (no more `refreshTokens` — Nango owns
refresh), and the mechanical status-string/prop rename in
`app/(app)/workspaces/[workspaceId]/admin/integrations/page.tsx` (`accountingProviders` →
`config.integrations.nango.enabled`). Then the step-7 gate: `tsc --noEmit` clean, targeted tests,
standards review.
