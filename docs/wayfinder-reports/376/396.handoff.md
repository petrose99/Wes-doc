milestone: build-step-7-todo

# #396 — Nango boundary build hand-off

`kind: backend`. ADR 0005's "Ordered build steps" 1–6 are the plan; step 7 is #383.

## Plan (ADR order)

- step: 1 — Schema migration — done
- step: 2 — `lib/nango.ts` (session mint, `nangoProxy`, HMAC verify) — done
- step: 3 — `app/api/webhooks/nango/route.ts` — done
- step: 4 — `app/api/integrations/connect-session/route.ts` + delete 4 old QB/Xero routes — done
- step: 5 — QuickBooks/Xero clients onto `nangoProxy`; Sage client (per #378) — done
- step: 6 — Push-loop pre-check, status-string updates, drop `no_default_account` — done
- step: 7 — build gate: `tsc --noEmit`, targeted tests, standards review — todo (checks green,
  report/close remaining)

## Artifacts

- Commit `52b1f84` (step 6). Full detail:
  `docs/wayfinder-reports/376/logs/scratch-396/step6-notes.md`.
- `lib/integration-push.test.ts` — new, 5 tests, passing.

## Open findings

None outstanding. `npx tsc --noEmit -p .` clean. `npx vitest run` → 286 files, 2780 tests, all
passing. See the scratch file for what each check covered and the self-applied standards review
(no `/code-review` slash command available headless).

## Next step

Close session: write the report at `docs/wayfinder-reports/376/396.md` (template
`scripts/wayfinder-autopilot/report.md`), session history from
`git log --format='%h %s%n%b' -- lib/integration-push.ts lib/nango.ts app/api/webhooks/nango
app/api/integrations/connect-session lib/integrations app/(app)/workspaces/[workspaceId]/admin/integrations`.
This ticket is `kind: backend`; every `.tsx` touch across the build is mechanical (step 4 route
delete, step 6 status-string/prop rename) with no visual change — report "no score; no rendered
surface changed, see hand-off" rather than running readers. Then close `#396` (resolution comment
summarizing the ADR-0005 boundary build, all 6 steps + this gate), one-line Decisions-so-far gist
on map `#376`, and check whether `#383` (step 7, the connect UI, `kind: surface`) is already
ticketed/blocked-on-this or needs creating.
