milestone: build-step-mid-5

# #396 — Nango boundary build hand-off

`kind: backend`. ADR 0005's "Ordered build steps" 1–6 are the plan; step 7 is #383.

## Plan (ADR order)

- step: 1 — Schema migration — done
- step: 2 — `lib/nango.ts` (session mint, `nangoProxy`, HMAC verify) — done
- step: 3 — `app/api/webhooks/nango/route.ts` — todo
- step: 4 — `app/api/integrations/connect-session/route.ts` + delete 4 old QB/Xero routes — todo
- step: 5 — QuickBooks/Xero clients onto `nangoProxy`; Sage client (per #378) — in progress, mid-edit
- step: 6 — Push-loop pre-check, status-string updates, drop `no_default_account` — todo
- step: 7 — build gate: `tsc --noEmit`, targeted tests, standards review — todo

## Artifacts

- `prisma/migrations/20260922020000_nango_boundary_connections/migration.sql`, `prisma/schema.prisma`
- `lib/nango.ts`, `lib/nango.test.ts` (5 tests pass)
- `lib/config.ts`, `lib/finance/provider-flags.ts`, `models/integrations.ts` (all edited, see git diff)

## Open findings

Full detail on the mid-edit state, exact files needing fixes, and the gate order is in
[`docs/wayfinder-reports/376/logs/scratch-396/step5-notes.md`](logs/scratch-396/step5-notes.md).
Tree has uncommitted changes in `lib/integrations/{quickbooks,xero}/client.ts` that do NOT
compile yet (mechanical `accessToken`→`connectionId` rename done, but bodies still use Bearer
auth) — read the scratch file before touching them further.

## Next step

Finish step 5 per the scratch file's first section, then steps 3, 4, 6 in that order, then the
step-7 gate. Do not run `tsc --noEmit` as a checkpoint until steps 3–6 are all done.
