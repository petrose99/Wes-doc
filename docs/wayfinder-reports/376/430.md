# Autopilot report — #430 Build: correcting posted bills' Accounts in the ledger

- Map: #376 Wayfinder map
- Type: Build · Started: prior sessions (see Sessions) · Finished: 2026-09-24
- Outcome: resolved

## What I did

1. Read the hand-off (build-done milestone, first evaluate pass at 61/100 with 0 P0 / 4 P1) and
   resumed at the close gate.
2. Started the dev server + devdb (workspace `af91555d-7450-4b21-a8ac-73db092617c8`), reseeded the
   account-correction fixture and the mismatch condition.
3. Ran a sequence of live evaluate re-runs, fixing what each one caught, verifying the fix live
   before moving on:
   - 61 → fixed `LineAccountCell`'s fallback `<option>` (real bug: coded account vanished from the
     Screen 2 select when missing from the synced chart) — commit `9870377`.
   - 76 → fixed the new fallback option's raw-slug label — commit `637d1ea`.
   - 83 → extracted `formatUnresolvedAccountId` to `lib/finance/line-account-resolution.ts` and used
     it at all three copy sites (Screen 1/3 reminder row, Review dialog, Screen 2 select) — commit
     `e4cb8a0`, with a unit test.
   - 79 (regression caught) → root-caused to `resolveAccountNames` (`models/integrations.ts`) itself
     masking a lookup miss with the raw id, making the prior fix's guard clauses dead code — fixed
     by having it omit the miss instead — commit `ab771e1`, with a unit test.
   - 88, Clean, 0 P0/P1 — clears the bar.
4. `tsc --noEmit` clean throughout; unit tests for both new/changed seams pass
   (`lib/finance/line-account-resolution.test.ts`, `models/integrations.test.ts`).
5. Stopped the dev server and live-server.

Two P1s from the baseline evaluate were investigated and closed as non-defects rather than fixed:
"silent Leave-them dismiss" (false positive — toast fires ~800ms after click, verified live) and
"two under-differentiated dismiss actions" (assessed as intentional, consistent naming per H4). A
fourth baseline finding ("contradictory supplier-match state" on the Screen 2 Supplier panel) was
confirmed pre-existing from #362 via git blame, not introduced by #430 — left open as a candidate
follow-up, out of scope for this ticket.

## Questions I answered on the owner's behalf

| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Fix the four evaluate P1s, or triage some as non-defects? | Fixed the one real bug (Screen 2 fallback option); reclassified two as false-positive/intentional with live verification; deferred the pre-existing one as out of scope | Autopilot standing delegation: take the grounded recommendation — evidence (Playwright verification, git blame) over restating the finding as-is |
| 2 | How to close the gap from 83→85 (raw-id leak)? | Traced to the real root cause (`resolveAccountNames` masking misses) rather than patching the symptom again | CODING_STANDARDS.md — seams named first; a second surface-level patch would have left the same bug reachable from other callers |

## Decisions recorded

Resolution comment on #430: Screen 1–3 of the account-correction flow (Review dialog, per-row
Leave-them, Screen 2 inline Account edit) is built and closes the bar — evaluate 88/100, Clean,
0 P0/P1; critique 35/40 (all heuristics ≥3, prior session, no rerun needed — no layout/type/motion
changes since). Four fix commits landed fixing a real Screen 2 display bug and two rounds of
raw-identifier leakage into UI copy, the second traced to its actual model-layer root cause.

## Tickets created / changed

None. No new fog graduated; the pre-existing Screen 2 Supplier-panel contradiction (from #362) is
noted in the resolution comment as a candidate follow-up but not ticketed — it predates and is
unrelated to #430's scope.

## Scores and counts (rendered surfaces only)

scores: predicted-critique=n/a first-critique=35 close-critique=35 first-evaluate=61 close-evaluate=88

Critique (35/40, all heuristics ≥3) was run once, in a prior session, against the built surface and
not re-run this session: none of this session's four commits touched layout, type, color, or motion
— all were either a display-logic fix (`LineAccountCell`'s option list) or copy/label formatting
(`formatUnresolvedAccountId`), so the critique's structural/visual verdict still holds. Evaluate ran
five times this session (61 → 76 → 83 → 79 regression → 88), each re-run driven by the previous
one's findings and confirmed live before moving on.

Detector: prior round (shots-r4) — all findings pre-existing/ambient, not re-blocking. Not re-run
this session since no rendering/CSS changed (only copy strings and a fallback `<option>`'s value
source).

`keyboard.json`'s `account-select-enabled: false` confirmed correct in a prior session (Paid-bill
un-offerable per spec).

## First-pass findings → lessons (rendered surfaces only)

| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| Screen 2 Account select silently shows no account (H1/H6/H9, P1) | Fallback-injection logic gated on a lookup that could only be truthy when already redundant — a dead-code review before spec-done would have caught the tautology | wrong | Fixed: `9870377` |
| Fallback option shows raw slug next to formatted peers (H4/H6, P2) | New finding from the H1/H6/H9 fix itself — no established contract missed this, it's inherent to introducing a display fallback | new class | Fixed: `637d1ea`, `e4cb8a0` |
| Raw account id leaks into reminder row / Review dialog copy (H1/H6, P1→P2→P1 on re-check) | A caller-side `?? formatUnresolvedAccountId(id)` guard is not sufficient when the callee itself already masks misses with a default — check the seam's actual failure mode, not just the caller | wrong (first attempted fix incomplete) | Root-caused and fixed: `ab771e1`, with a regression-catching evaluate re-run in between |
| Silent Leave-them dismiss (H1, P1) | None — verified false positive live (toast at ~800ms) | none | No change |
| Two under-differentiated dismiss actions (H4/H6, P1) | None — assessed intentional per H4 (consistent, correct naming for two different consequences) | none | No change |
| Contradictory supplier-match state on Screen 2 (H4, P1) | Pre-existing from #362, confirmed via git blame — not #430's regression | none | Out of scope, not fixed |

## Needs the owner

Nothing. No removal, credential, or ambiguous-scope decision arose this session.
