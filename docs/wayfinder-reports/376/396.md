# Autopilot report — #396 Build: Nango boundary — schema, client, webhook, connect-session, provider clients, push loop

- Map: #376 Map: Ledger connections through Nango — QuickBooks, Xero, Sage Accounting; Bigcapital removed
- Type: wayfinder:build (`kind: backend`, ADR 0005) · Started: earlier sessions (see hand-off) · Finished: 2026-09-22 12:26 SAST
- Outcome: resolved

## What I did

This ticket is the ADR-0005 backend boundary build; every step is `kind: backend` (no `app/**`/`components/**`
UI redesign — the two `.tsx` touches are mechanical prop/status renames with no visual change), so the Intent/
Impeccable design gate does not apply per `CLAUDE.md`'s path-based rule. Work spanned several prior sessions,
carried on the hand-off file (`docs/wayfinder-reports/376/396.handoff.md`):

1. Step 1 (prior session) — `IntegrationConnection` schema migration to the Nango-shaped columns/status vocabulary.
2. Step 2 (`8f5a0f6`) — `lib/nango.ts`: session mint, `connection_config` reader, delete, `nangoProxy()`, HMAC
   webhook-signature verification; config collapsed to one `NANGO_SECRET_KEY`/`NANGO_WEBHOOK_SECRET` gate.
3. Step 3 (`b06be62`) — `app/api/webhooks/nango/route.ts`: HMAC-verifies, upserts the connection on successful
   creation, flips to `needs_reconnect` on failure with an owner email once per break. 9 tests.
4. Step 4 (`7f52945`) — `app/api/integrations/connect-session/route.ts` (owner+plan-gated session mint); deleted
   the four retired QuickBooks/Xero connect/callback routes.
5. Step 5 (`6c43016`, `21a464c`) — QuickBooks/Xero clients rewired onto `nangoProxy`; Sage client scaffolded
   per Research #378's provider readiness.
6. Step 6 (`52b1f84`) — `attemptIntegrationPush` pauses (no attempt/lease burn) on `needs_reconnect` or a
   mid-attempt `IntegrationAuthError`; retired `lib/integration-token-refresh.ts` entirely (every caller now
   passes `connectionId`, not a token); status vocabulary renamed to the glossary's `connected`/`needs_reconnect`
   everywhere (`no_default_account` dropped — never a glossary state); mechanical `accountingProviders` →
   `nangoEnabled` prop rename in the admin Integrations page / `IntegrationsManager`; `IntegrationPush.connection`
   guarded nullable as a terminal `integration_connection_disabled`. New `lib/integration-push.test.ts` (5 tests).
7. Step 7 (this session) — build gate: re-ran `npx tsc --noEmit -p .` (clean) and `npx vitest run` (286 files,
   2780 tests, all passing) to confirm the tree is still green after the hand-off; self-applied
   `CODING_STANDARDS.md` review (no `/code-review` slash command available headless) — seams named before code,
   red→green tests at each step, no P0/P1-shaped finding. Confirmed the follow-on connect-UI ticket
   ([#397](https://github.com/petrose99/Wes-doc/issues/397), `kind: surface`) already exists and is blocked
   appropriately rather than needing creation.
8. Posted the resolution comment, closed #396, added the one-line Decisions-so-far gist to map #376.

## Questions I answered on the owner's behalf

None — this was a build ticket executing ADR 0005's already-decided plan (#382), not a fresh decision point.

## Decisions recorded

Nango boundary shipped end to end: schema, `lib/nango.ts` client, webhook route, connect-session route (old
QB/Xero OAuth routes deleted), QuickBooks/Xero clients and a Sage scaffold on `nangoProxy`, and the push-loop
pause-without-burning-attempts + status-vocabulary rename. `lib/integration-token-refresh.ts` retired. Build
gate green: `tsc --noEmit` clean, 2780/2780 tests passing.

## Tickets created / changed

- No new tickets. [#397 Build: Accounting page and connect flow through Nango](https://github.com/petrose99/Wes-doc/issues/397)
  is already ticketed as ADR-0005 step 7 (the `kind: surface` connect UI) and needs no creation here.
- Closed #396.

## Scores and counts (rendered surfaces only)

no score; no rendered surface changed (`kind: backend`; the two `.tsx` touches were mechanical prop/status
renames with no visual change) — see hand-off and step6-notes.md for detail.

## First-pass findings → lessons (rendered surfaces only)

n/a — no rendered surface changed in this ticket.

## Needs the owner

Nothing new. Operational Nango Cloud environment/project setup and secrets remain in the map's "Not yet
specified" for a later task ticket, as already noted on #376.
