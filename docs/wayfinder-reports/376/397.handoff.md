milestone: build-done

## Ticket
Build: Accounting page and connect flow through Nango (#397), map #376.
Spec = ticket body + resolution/Action Summary on #383 (already fully grilled,
no further grilling needed). Backend boundary (#396) shipped.

## Plan (build steps, one per session)
- step: 1 — delete dead Finance page — done (606eabb)
- step: 2 — provider picker (disconnected state) — done (kind: surface)
- step: 3 — connect flow states — done (kind: surface)
- step: 4 — connected-state summary row — done (f3bdae3, kind: surface)
  Amber "needs reconnect" tag (red reserved for Disconnect only),
  default-account nudge line, Disconnect confirm copy now states the
  spec's exact consequence (records stay posted / pushes stop until
  reconnected / mappings forgotten).
- step: 5 — connection-band + email migration — done (no code change needed)
  connection-band.tsx already connected/needs_reconnect-only (shipped by
  #396). notifyOwnersOfBreak's ReminderEmail body already task-toned,
  non-destructive ("Reconnect it"), matches ApprovalNoticeEmail's voice —
  confirmed by reading both files, no gap.
- step: 6 — gate: detector before/after, evaluate, staging walkthrough — todo
  **Blocked this session on sandbox tool access**: this worktree's Bash/Read
  are confined to the lane directory. `impeccable` binary resolves to
  `~/.impeccable/bin/...` (outside the lane) and `.impeccable/live/dev.mjs`
  is a symlink to the main checkout (outside the lane) — both paths are
  refused by the sandbox ("may only list/search files in the allowed
  working directories"). Static `impeccable detect --json` also refused
  (command requires interactive approval that headless can't grant). tsc
  --noEmit is clean (verified this session). Next session: try running
  the gate from the *main checkout* context or confirm with the driver
  whether lane sandboxing is expected to permit `~/.impeccable` and
  `.impeccable/live/dev.mjs` — if still blocked, escalate as
  `Autopilot: blocked — detector/live-server unreachable from lane sandbox`
  rather than closing without the bar's required scores.

## Artifacts
- docs/wayfinder-reports/376/logs/scratch-397/383-comments.txt (full
  resolution + Action Summary + state inventory)
- docs/wayfinder-reports/376/logs/scratch-397/survey.md
- docs/wayfinder-reports/376/logs/scratch-397/step3-notes.md

## Open findings
None outstanding in code. tsc --noEmit clean. Detector/critique/evaluate
not yet run — see step 6 blocker above.

## Next step
Resolve the sandbox access to `impeccable` (static + in-page detector) and
`.impeccable/live/dev.mjs`, then run step 6's gate (detect before/after at
1440/390, evaluate ≥80 no heuristic >1, critique ≥32/40 no heuristic <3,
staging walkthrough of the 10 captured states) and write the close report.
