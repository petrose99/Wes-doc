milestone: build-done

## Ticket
Build: Accounting page and connect flow through Nango (#397), map #376.
Spec = ticket body + resolution/Action Summary on #383 (already fully grilled,
no further grilling needed). Backend boundary (#396) shipped.

## Plan (build steps, one per session)
- step: 1 — delete dead Finance page — done (606eabb)
- step: 2 — provider picker (disconnected state) — done (kind: surface)
- step: 3 — connect flow states — done (kind: surface)
- step: 4 — connected-state summary row — done (f3bdae3, kind: surface)
- step: 5 — connection-band + email migration — done (no code change needed)
- step: 6 — gate: detector before/after, evaluate, staging walkthrough — blocked
  Re-confirmed again this session (fresh claim + re-check, 2026-09-22):
  `.env` and `.impeccable/live/dev.mjs` are symlinks resolving to
  `/home/ubuntu/Dev/Wes-doc`, outside this worktree lane
  (`/home/ubuntu/Dev/Wes-doc-lanes/376-397`); the sandbox refuses any
  read/list op on the resolved path (`ls` on both explicitly blocked this
  session). No DATABASE_URL → no dev server → no in-page detector, no
  live walkthrough, no evaluate/critique against a running surface.
  tsc --noEmit clean. Posted `Autopilot: blocked —` comment (see issue
  comment 5775236294) and unassigned per protocol; ticket left open.

## Artifacts
- docs/wayfinder-reports/376/logs/scratch-397/383-comments.txt (full
  resolution + Action Summary + state inventory)
- docs/wayfinder-reports/376/logs/scratch-397/survey.md
- docs/wayfinder-reports/376/logs/scratch-397/step3-notes.md

## Open findings
None outstanding in code. tsc --noEmit clean. Detector/critique/evaluate
not yet run — see step 6 blocker above.

## Next step
Owner/driver to unblock lane access to `.env` and `.impeccable/live/dev.mjs`
(or run the gate from the main checkout and report back), then run step 6's
gate (detect before/after at 1440/390, evaluate ≥80 no heuristic >1, critique
≥32/40 no heuristic <3, staging walkthrough of the 10 captured states) and
write the close report. Do not close #397 without those real scores.
