milestone: spec-done

## Ticket
Build: Accounting page and connect flow through Nango (#397), map #376.
Spec = ticket body + resolution/Action Summary on #383 (already fully grilled,
no further grilling needed). Backend boundary (#396) shipped: lib/nango.ts,
webhook route, connect-session route, QB/Xero/Sage clients, push-loop pause.

## Plan (build steps, one per session)
- step: 1 — delete dead Finance page — done (606eabb)
- step: 2 — provider picker (disconnected state) — done (kind: surface)
- step: 3 — connect flow states — done (kind: surface)
  ProviderConnectTile + SageBusinessPicker wired to connect-session +
  @nangohq/frontend. Notes: docs/wayfinder-reports/376/logs/scratch-397/step3-notes.md
- step: 4 — connected-state summary row — todo
  Provider, tenant, last synced, default-account nudge, Disconnect w/
  consequence copy (destructive ConfirmDialog convention, already close —
  verify copy matches spec's "existing records stay posted / mappings
  forgotten" wording). Amber status color throughout (page + band).
- step: 5 — connection-band + email migration — todo
  needs_reauth/no_default_account already gone from connection-band.tsx
  (confirmed step-1 session, done by #396) — only remaining work: check
  notifyOwnersOfBreak's ReminderEmail (lib/email.ts, called from
  app/api/webhooks/nango/route.ts) matches ApprovalNoticeEmail's voice.
- step: 6 — gate: detector before/after, evaluate, staging walkthrough — todo
  impeccable detect --json + in-page detect.js at 1440/390, evaluate ≥80,
  critique ≥32/40 no heuristic <3, tsc --noEmit clean. Then close session
  writes docs/wayfinder-reports/376/397.md.

Captured states (10): idle/disconnected · popup-blocked ·
popup-closed-early · provider-error · Sage business-picker · Sage
empty-business-list · connecting · connected · needs_reconnect (page+band) ·
disconnect-confirm.

Action Summary skills (from #383, run per corresponding step):
Intent — journey, articulate, fortify. Impeccable — shape, layout, clarify,
polish (polish at step 6).

## Artifacts
- docs/wayfinder-reports/376/logs/scratch-397/383-comments.txt (full
  resolution + Action Summary + state inventory)
- docs/wayfinder-reports/376/logs/scratch-397/survey.md (pre-step-1 code survey)
- docs/wayfinder-reports/376/logs/scratch-397/step3-notes.md (step 3 detail)

## Open findings
None open — step 5's band migration confirmed already done by #396; only
the email-voice check remains, folded into step 5 above.

## Next step
Start step 4 (connected-state summary row): tighten the connected card
against the spec's exact copy (tenant, last synced, default-account nudge,
Disconnect consequence text) and confirm the amber status color convention
matches between the integrations page and connection-band.tsx. kind: surface.
