milestone: spec-done

## Ticket
Build: Accounting page and connect flow through Nango (#397), map #376.
Spec = ticket body + resolution/Action Summary on #383 (already fully grilled,
no further grilling needed). Backend boundary (#396) shipped: lib/nango.ts,
webhook route, connect-session route, QB/Xero/Sage clients, push-loop pause.

## Plan (build steps, one per session)
- step: 1 — delete dead Finance page — todo
  Files: app/(app)/workspaces/[workspaceId]/(chrome)/finance/page.tsx and its
  /accounting → /finance redirect. Check: route removed, no remaining
  references (grep "finance/page" and the redirect config).
- step: 2 — provider picker (disconnected state) — todo
  Files: app/(app)/admin/integrations/page.tsx, IntegrationsManager (locate
  via grep). Three-tile Card: QuickBooks, Xero, Sage (disabled "Coming
  soon"). No logos — name + one-line description + small monochrome Lucide
  glyph at most. kind: surface — read craft-floor.md first.
- step: 3 — connect flow states — todo
  popup-blocked / closed-early / provider-error / Xero (no DocuBite picker,
  reads tenantId post AUTH webhook) / Sage in-page business picker (+ empty
  list dead end) / connecting / connected. Reload-mid-handshake: no special
  code, connection.status is authoritative. Uses connect-session route from
  #396.
- step: 4 — connected-state summary row — todo
  Provider, tenant, last synced, default-account nudge, Disconnect w/
  consequence copy (destructive ConfirmDialog convention). Amber status
  color. native-select swap for AccountingConnectionCard's raw <select>.
- step: 5 — connection-band + email migration — todo
  components/queue/connection-band.tsx off needs_reauth/no_default_account
  onto connected/needs_reconnect. Owner-email copy in ApprovalNoticeEmail
  voice, fires once per break.
- step: 6 — gate: detector before/after, evaluate, staging walkthrough — todo
  impeccable detect --json + in-page detect.js at 1440/390, evaluate ≥80,
  critique ≥32/40 no heuristic <3, tsc --noEmit clean. Then close session
  writes docs/wayfinder-reports/376/397.md.

Captured states (10): idle/disconnected · popup-blocked ·
popup-closed-early · provider-error · Sage business-picker · Sage
empty-business-list · connecting · connected · needs_reconnect (page+band) ·
disconnect-confirm.

Action Summary skills (from #383, run per corresponding step):
Intent — journey, articulate, fortify. Impeccable — shape, layout, clarify,
polish (polish at step 6).

## Artifacts
- docs/wayfinder-reports/376/logs/scratch-397/383-body.txt
- docs/wayfinder-reports/376/logs/scratch-397/383-comments.txt (full
  resolution + Action Summary + state inventory — read this before step 1)

## Open findings
See docs/wayfinder-reports/376/logs/scratch-397/survey.md — Explore's
survey of current code, returned after the session ended. Key points:
real target is app/(app)/workspaces/[workspaceId]/admin/integrations/page.tsx
+ components/integrations/integrations-manager.tsx (not app/(app)/admin/...);
Sage missing from PROVIDER_LABELS/provider array; unconnected-provider link
still hits the retired /api/integrations/{provider}/connect route (needs
connect-session + nango.auth()); connection-band.tsx may already only have
connected/needs_reconnect (step 5 might be mostly done — verify by grep).

## Next step
Read the survey file above first. Then start step 1: grep repo-wide for
"needs_reauth" and "no_default_account" (0 hits = step 5's band migration
already done by #396, only email-voice check remains) and delete the dead
finance/page.tsx + its /accounting redirect (app/(app)/workspaces/
[workspaceId]/(chrome)/{finance,accounting}/page.tsx), verify no remaining
references, commit, then move to step 2.
