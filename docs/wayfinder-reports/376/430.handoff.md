# Hand-off — #430 Build: correcting posted bills' Accounts in the ledger

milestone: spec-done

## Step plan

- step: 1 — Backend: provider pre-check + account-only update calls — kind: backend — done
- step: 2 — Backend: find-affected-bills query + server actions + "Leave them" flag — kind: backend — done
- step: 3 — Surface: Screen 1 "N bills already posted" dialog — kind: surface — in progress
  (dialog built, all three triggers wired, migration applied, tsc clean, tests green; this
  session found and fixed the *route* to reach the approve-and-retarget trigger live — see
  Open findings — and hit one more fixture gap, not yet cleared; critique/evaluate still to run)
- step: 4 — Surface: Screen 2 Detail pane Account-only edit on Posted/Paid rows — kind: surface — todo
- step: 5 — Surface: Screen 3 rule-row reminder + Review/Leave-them wiring — kind: surface — todo
- step: 6 — build gate — kind: surface — todo (detector before/after, critique/evaluate/include, tsc)

## Artifacts

- Spec: docs/wayfinder-reports/376/430.spec.md
- Components: components/integrations/account-correction-dialog.tsx (Screen 1 dialog),
  components/assistant/finance-proposal.tsx, components/pipeline/document-detail/bill-pane.tsx
- Seeds: scripts/dev/seed-account-correction.ts, scripts/dev/seed-account-correction-approve.ts
- Scratch probe: scripts/dev/check430.ts (delete once step 3 is done)
- Verify scripts: docs/wayfinder-reports/376/logs/scratch-430/verify-approve-trigger.mjs (doc URL
  — reaches only the field-edit Details view, dead end, kept for reference),
  docs/wayfinder-reports/376/logs/scratch-430/verify-queue-approve.mjs (queue URL — the correct
  route, gets to the real Approve button)
- Prior art: #429 commit ea6d033, its spec docs/wayfinder-reports/376/429.spec.md

## Open findings

Full detail: docs/wayfinder-reports/376/logs/scratch-430/findings.md (prior session's "overwrite"
theory) — **refuted this session**: read `maybeConfirmAiCoding` (models/review-tasks.ts:380), it
never touches `codingData`, only flips `codingSource`. Not the cause.

**Real cause of "dialog never opens", found this session**: the seeded review task's real Approve
button (`BillFooterActions`, bill-pane.tsx:244) only mounts inside the **Invoices queue's**
split-pane (`components/queue/invoice-queue.tsx` `paneActions`) — reached via
`/workspaces/<ws>/invoices` (click the `tr[data-row-id="<documentId>"]` row to open the pane).
The standalone `/workspaces/<ws>/invoices/<documentId>` and
`/workspaces/<ws>/approvals/invoices/<documentId>` pages the prior session was driving only show
`FieldNavForm`'s "Save review" (same DOM id `save-review-submit`, collides with the real button —
a pre-existing, harmless-so-far duplicate-id smell, not in scope to fix here) and an "Approval"
tab that literally says "Approve from the pane." `verify-queue-approve.mjs` now reaches the real
Approve button (confirmed visible, confirmed clickable, pane opens correctly — see
`q3-pane.png`).

**New fixture gap, not yet cleared**: clicking Approve now surfaces `assertPaymentConfirmed`'s
gate ("Confirm whether this has been paid before approving it.") —
`models/review-tasks.ts:34`, gated by `isPaymentConfirmationRequired` (lib/doc-types.ts:370) on
`invoice`/expense doc types. The task never reaches `updateReviewTaskStatus`'s transaction (task
status is still `open` in the DB after the click — confirmed via `check430.ts`). This is a
seed-fixture gap, not an app bug: `seed-account-correction-approve.ts` needs to set
`document.paymentStatus` (or whatever field satisfies `isPaymentConfirmationRequired`) so the
gate is already cleared, matching what a real reviewed invoice would have before approval.

## Next step

Read `isPaymentConfirmationRequired`/`assertPaymentConfirmed` (lib/doc-types.ts:370,
models/review-tasks.ts:34-50) to see which field(s) clear the gate, add them to
`seed-account-correction-approve.ts`'s `document.create`/`update` data, rerun both seeds, then
rerun `docs/wayfinder-reports/376/logs/scratch-430/verify-queue-approve.mjs` against
`/workspaces/af91555d-7450-4b21-a8ac-73db092617c8/invoices` (doc id
`ecc8bced-51db-4e66-b9b5-7baea720ba21`) to confirm the approve click now succeeds, the
`SupplierAccountRule` retargets to `fuel`, and the "N bills already posted" dialog opens (it
should find the `sundry-expenses`-posted bill `seed-account-correction.ts` seeds). If it opens:
close step 3 on live confirmation + code review + tsc/tests, delete `check430.ts`, run
critique/evaluate/include, then move to step 4. If a further gap appears, keep narrowing the same
way — do not fall back to declaring the path unreachable until every gate above it is confirmed,
not assumed. Re-read `craft-floor.md` before any visual fix. Stop `next dev`/live-server (`node
.impeccable/live/dev.mjs stop`) before ending the session — it was left running this session end.
