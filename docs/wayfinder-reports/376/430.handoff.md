# Hand-off — #430 Build: correcting posted bills' Accounts in the ledger

milestone: build-done

## Step plan

- step: 1 — Backend: provider pre-check + account-only update calls — kind: backend — done
- step: 2 — Backend: find-affected-bills query + server actions + "Leave them" flag — kind: backend — done
- step: 3 — Surface: Screen 1 "N bills already posted" dialog — kind: surface — done
- step: 4 — Surface: Screen 2 Detail pane Account-only edit on Posted/Paid rows — kind: surface — todo (in progress, plan below)
- step: 5 — Surface: Screen 3 rule-row reminder + Review/Leave-them wiring — kind: surface — todo
- step: 6 — build gate — kind: surface — todo (detector before/after, critique/evaluate/include, tsc)

## Artifacts

- Spec: docs/wayfinder-reports/376/430.spec.md (§Screen 2 = lines 121-172)
- Step 4 remaining-work plan (read this first): docs/wayfinder-reports/376/logs/scratch-430/step4-plan.md
- Backend, committed 5e7b03e: models/documents.ts `recordDocumentLineAccountsCorrected`;
  account-correction-actions.ts `resolveDocumentPushConnection`,
  `checkDocumentAccountCorrectableAction`, `updateDocumentLineAccountsAction`. tsc clean.
- UI, uncommitted (working tree dirty, do not discard):
  components/documents/line-items-editor.tsx — `LineAccountCell` editable/pendingValue/
  onChange/locked props + refusal-sentence helper; `BillLineItemsTotals` gained
  ledgerFact/workspaceId/documentId. tsc clean as of last check.

## Open findings

None beyond the step4-plan.md list.

## Next step

Follow docs/wayfinder-reports/376/logs/scratch-430/step4-plan.md's "Remaining, in order" list:
wire pendingAccounts/refusal state and the two LineAccountCell call sites in
line-items-editor.tsx, add the "Update in {Provider}" bar, thread ledgerFact/workspaceId/
documentId from page.tsx:243 through BillSplitPane → LineItemsSection → the editor's `bill`
prop, add the mount-time pre-check effect, then `tsc --noEmit` and live-verify with
scripts/dev/seed-account-correction-approve.ts. Commit as one step-4 commit, then move to step
5. Run the build gate (step 6) only once all three surface steps are built.
