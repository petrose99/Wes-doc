# Hand-off — #430 Build: correcting posted bills' Accounts in the ledger

milestone: spec-done

## Step plan

- step: 1 — Backend: provider pre-check + account-only update calls — kind: backend — done
- step: 2 — Backend: find-affected-bills query + server actions + "Leave them" flag — kind: backend — done
- step: 3 — Surface: Screen 1 "N bills already posted" dialog — kind: surface — in progress
  (dialog built, all three triggers wired; migration applied, tsc clean, tests green; live
  click-through of the approve-and-retarget trigger this session did not yet reach the dialog —
  see docs/wayfinder-reports/376/logs/scratch-430/findings.md; critique/evaluate still to run)
- step: 4 — Surface: Screen 2 Detail pane Account-only edit on Posted/Paid rows — kind: surface — todo
- step: 5 — Surface: Screen 3 rule-row reminder + Review/Leave-them wiring — kind: surface — todo
- step: 6 — build gate — kind: surface — todo (detector before/after, critique/evaluate/include, tsc)

## Artifacts

- Spec: docs/wayfinder-reports/376/430.spec.md
- Components: components/integrations/account-correction-dialog.tsx (Screen 1 dialog),
  components/assistant/finance-proposal.tsx, components/pipeline/document-detail/bill-pane.tsx
- Seeds: scripts/dev/seed-account-correction.ts, scripts/dev/seed-account-correction-approve.ts
  (new this session)
- Scratch probe: scripts/dev/check430.ts (delete once step 3 is done)
- Verify script: docs/wayfinder-reports/376/logs/scratch-430/verify-approve-trigger.mjs
- Prior art: #429 commit ea6d033, its spec docs/wayfinder-reports/376/429.spec.md

## Open findings

Full detail: docs/wayfinder-reports/376/logs/scratch-430/findings.md — the approve-and-retarget
trigger's Approve click succeeds live, but the dialog never opens: the SupplierAccountRule stays
on its old account after approval instead of retargeting, likely because `maybeConfirmAiCoding`
(called before `learnSupplierAccountRuleFromApproval` in review-actions.ts:47) overwrites the
seeded manual account choice back to the rule's account — **not yet confirmed**, read its source
next. The Default-account trigger is still confirmed unreachable (http 400, prior session).

## Next step

Read `maybeConfirmAiCoding`'s source to confirm/refute the overwrite theory in the findings file.
If confirmed, either find a fixture shape it won't touch, or record this path (like the
Default-account path) as live-unreachable in this environment and close step 3 on code review +
`tsc`/tests, noting the ceiling explicitly on the build-gate step, then move to step 4. Re-read
`craft-floor.md` before any visual fix. Stop `next dev`/live-server (`node .impeccable/live/dev.mjs
stop`) before ending the session — it was left running this session end.
