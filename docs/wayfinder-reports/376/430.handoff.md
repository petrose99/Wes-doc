# Hand-off — #430 Build: correcting posted bills' Accounts in the ledger

milestone: spec-done

## Step plan

- step: 1 — Backend: provider pre-check + account-only update calls — kind: backend — done
- step: 2 — Backend: find-affected-bills query + server actions + "Leave them" flag — kind: backend — done
- step: 3 — Surface: Screen 1 "N bills already posted" dialog — kind: surface — in progress
  (dialog built, both triggers wired, migration applied, tsc clean, tests green; live
  verification/detector/critique still to run — see scratch notes before marking `done`)
- step: 4 — Surface: Screen 2 Detail pane Account-only edit on Posted/Paid rows — kind: surface — todo
- step: 5 — Surface: Screen 3 rule-row reminder + Review/Leave-them wiring — kind: surface — todo
- step: 6 — build gate — kind: surface — todo (detector before/after, critique/evaluate/include, tsc)

## Artifacts

- Spec: docs/wayfinder-reports/376/430.spec.md
- New component: components/integrations/account-correction-dialog.tsx
- Prior art: #429 commit ea6d033, its spec docs/wayfinder-reports/376/429.spec.md
- This session's detail: docs/wayfinder-reports/376/logs/scratch-430/session-step3.md

## Open findings

Pointer: docs/wayfinder-reports/376/logs/scratch-430/session-step3.md — bulk-approve trigger
unwired, `ledgerBillUrl` shapes unverified, migration-apply-under-sandbox trick.

## Next step

Continue step 3: start the dev server, verify the Default-save dialog and single-approve dialog
live at 1440/390, wire the bulk-approve path's `ruleChanges[]` (see scratch notes for the caller
to find), run the in-page detector + `impeccable critique`/`evaluate` on the dialog states, fix
findings, mark step 3 `done`, then move to step 4. Re-read `craft-floor.md` before any visual fix.
