# Hand-off — #430 Build: correcting posted bills' Accounts in the ledger

milestone: spec-done

## Step plan

- step: 1 — Backend: provider pre-check + account-only update calls — kind: backend — done
- step: 2 — Backend: find-affected-bills query + server actions + "Leave them" flag — kind: backend — done
- step: 3 — Surface: Screen 1 "N bills already posted" dialog — kind: surface — in progress
  (dialog built, all three triggers wired — Default-save, single-approve (bill-pane), bulk-approve
  (finance-proposal.tsx); migration applied, tsc clean, tests green; live click-through blocked
  this session — see Open findings; critique/evaluate still to run)
- step: 4 — Surface: Screen 2 Detail pane Account-only edit on Posted/Paid rows — kind: surface — todo
- step: 5 — Surface: Screen 3 rule-row reminder + Review/Leave-them wiring — kind: surface — todo
- step: 6 — build gate — kind: surface — todo (detector before/after, critique/evaluate/include, tsc)

## Artifacts

- Spec: docs/wayfinder-reports/376/430.spec.md
- Components: components/integrations/account-correction-dialog.tsx (Screen 1 dialog),
  components/assistant/finance-proposal.tsx, components/pipeline/document-detail/bill-pane.tsx
  (the three triggers)
- Seed: scripts/dev/seed-account-correction.ts (posted bill + matching SupplierAccountRule +
  IntegrationConnection for workspace af91555d-7450-4b21-a8ac-73db092617c8)
- This session's script: docs/wayfinder-reports/376/logs/scratch-430/verify-screen1.mjs (Playwright,
  uses repo's own node_modules/playwright — no scratchpad install needed on this repo)
- Prior art: #429 commit ea6d033, its spec docs/wayfinder-reports/376/429.spec.md

## Open findings

- **Confirmed this session: the Default-account trigger (integrations-manager.tsx) cannot be
  reached live at all**, one step earlier than previously thought. Loading the chart-of-accounts
  (`loadAccounts`, a real provider API call) fails first with `http 400` against the seeded fake
  `externalTenantId` — the row never reaches the `NativeSelect` that fires `onSelectAccount`, so
  the Default-save path to Screen 1 cannot be click-tested at all in this environment, not even to
  the previously-reported "every row refused" state. Verified live at 1440 and 390 with
  `verify-screen1.mjs` (Playwright against `next dev` :3000 + `af91555d-…` workspace): body text
  shows "Default expense account / http 400 Try again / Guessed" instead of a select.
- **Single-approve (bill-pane.tsx) and bulk-approve (finance-proposal.tsx) triggers not yet tried
  live this session** — they fire from `ruleChange` data returned by
  `updateReviewTaskStatusAction`/`bulkUpdateReviewTaskStatusAction` when an approval retargets a
  `SupplierAccountRule` to a different account, which does not require the chart-of-accounts UI to
  load. This may be reachable without provider credentials (worth trying next: seed a review task
  whose approved account differs from an existing rule's account, approve it via the UI, and see
  whether `checkAffectedByRuleChangeAction`'s provider pre-check call itself succeeds/fails the
  same way as the Default-account path, or gets further). Needs new seed fixture — none exists yet
  for this specific path (current `seed-account-correction.ts` only covers the already-posted-bill
  side, not a fresh review task to approve-and-retarget).
- Carried over: no dev mock for `checkQuickBooksBillCorrectable`/`checkXeroBillCorrectable`
  (`lib/integrations/{quickbooks,xero}/client.ts`); no real QuickBooks/Xero sandbox tenant in this
  environment (prod also has 0 connections per `docubite_nango_connections` memory).
- `ledgerBillUrl` in the dialog still guesses QuickBooks/Xero bill-edit URL shapes — unverified.

## Next step

Write a seed fixture for the approve-and-retarget path (a review task whose extracted account
differs from `Acme Fuel Co`'s existing `SupplierAccountRule`, plus an older posted bill on the old
account so `checkAffectedByRuleChangeAction` has something to find), then drive `bill-pane.tsx`'s
Approve button live (Playwright, `next dev` on :3000 via `node .impeccable/live/dev.mjs start
<ws-alias>`, then `npx tsx --env-file .env scripts/dev/prep-workspace.ts
af91555d-7450-4b21-a8ac-73db092617c8` — the alias arg to `dev.mjs start` is not a workspace id, run
prep-workspace separately) to see whether Screen 1 opens and in what state. If the provider
pre-check also fails outright (not just "refused rows" but no dialog at all, mirroring the
Default-account finding above), record that as a hard environment ceiling on step 3's live
verification and move forward on the strength of code review + `tsc`/tests instead of a live
click-through, noting the gap explicitly on the build-gate step. Otherwise capture the refused-row
state at 1440/390, run `impeccable critique`/`evaluate` and the in-page detector against it, fix
findings in scope, mark step 3 `done`, and move to step 4. Re-read `craft-floor.md` before any
visual fix. Stop `next dev`/live-server (`node .impeccable/live/dev.mjs stop`) before ending the
session.
