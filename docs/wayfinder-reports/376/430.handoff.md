# Hand-off — #430 Build: correcting posted bills' Accounts in the ledger

milestone: build-done

## Step plan

- step: 1 — Backend: provider pre-check + account-only update calls — kind: backend — done
- step: 2 — Backend: find-affected-bills query + server actions + "Leave them" flag — kind: backend — done
- step: 3 — Surface: Screen 1 "N bills already posted" dialog — kind: surface — done
- step: 4 — Surface: Screen 2 Detail pane Account-only edit on Posted/Paid rows — kind: surface — done
- step: 5 — Surface: Screen 3 rule-row reminder + Review/Leave-them wiring — kind: surface — todo
- step: 6 — build gate — kind: surface — todo (detector before/after, critique/evaluate/include, tsc)

## Artifacts

- Spec: docs/wayfinder-reports/376/430.spec.md (§Screen 3 = read next by range for step 5)
- Backend, committed 5e7b03e: models/documents.ts `recordDocumentLineAccountsCorrected`;
  account-correction-actions.ts `resolveDocumentPushConnection`,
  `checkDocumentAccountCorrectableAction`, `updateDocumentLineAccountsAction`.
- Step 4, committed b721113: components/documents/line-items-editor.tsx (pendingAccounts/
  updateState/updateError/refusal state, mount-time pre-check effect, "Update in {Provider}"
  bar); components/pipeline/document-detail/split-pane.tsx (`FieldNavForm`'s new `ledgerFact`
  prop, `BillSplitPane` passes its own `ledger`); components/pipeline/document-detail/
  line-items-section.tsx (`accountLedgerFact`/`accountWorkspaceId`/`accountDocumentId` props
  threaded into the `bill` object literal). tsc --noEmit clean. Not yet live-verified against a
  running dev server — do that alongside step 5's own verification pass, once both surface
  pieces exist, rather than bringing the dev server up twice.

## Open findings

None yet for step 5 — not scoped out.

## Next step

Read docs/wayfinder-reports/376/430.spec.md's Screen 3 section (grep for "Screen 3"), then
build the rule-row reminder ("N posted bills still on … · Review") and the Review/Leave-them
wiring per that section — reuse `components/integrations/account-correction-dialog.tsx`'s
existing Screen 1 flow and `updateSelectedBillsAction`/Leave-them action from step 2's backend
(account-correction-actions.ts) rather than re-deriving. After step 5's tsc is clean, live-verify
steps 4 and 5 together (seed with a posted/paid bill — reuse or extend
scripts/dev/seed-account-correction-approve.ts) before running the step 6 build gate (detector
before/after at 1440/390, `impeccable critique`/`evaluate`/`include`, tsc).
