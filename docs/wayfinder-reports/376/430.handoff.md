# Hand-off — #430 Build: correcting posted bills' Accounts in the ledger

milestone: spec-done

## Step plan

- step: 1 — Backend: provider pre-check + account-only update calls — kind: backend — done
- step: 2 — Backend: find-affected-bills query + server actions + "Leave them" flag — kind: backend — done
- step: 3 — Surface: Screen 1 "N bills already posted" dialog — kind: surface — in progress
  (dialog built, all three triggers now wired — Default-save, single-approve (bill-pane), and
  bulk-approve (chat assistant's finance-proposal card, not a queue toolbar — see below);
  migration applied, tsc clean, tests green; live click-through + critique/evaluate still to run)
- step: 4 — Surface: Screen 2 Detail pane Account-only edit on Posted/Paid rows — kind: surface — todo
- step: 5 — Surface: Screen 3 rule-row reminder + Review/Leave-them wiring — kind: surface — todo
- step: 6 — build gate — kind: surface — todo (detector before/after, critique/evaluate/include, tsc)

## Artifacts

- Spec: docs/wayfinder-reports/376/430.spec.md
- Components: components/integrations/account-correction-dialog.tsx (Screen 1 dialog),
  components/assistant/finance-proposal.tsx (bulk-approve trigger, this session)
- Prior art: #429 commit ea6d033, its spec docs/wayfinder-reports/376/429.spec.md
- This session's detail: docs/wayfinder-reports/376/logs/scratch-430/session-step3.md (prior
  session) — this session added the bulk-approve wiring, no new scratch file needed

## Open findings

- Bulk-approve trigger: `bulkUpdateReviewTaskStatusAction`'s only caller in components/ is
  `finance-proposal.tsx` (chat assistant's Accept button on an `approve_review_tasks` proposal) —
  the queue's own bulk-select toolbar does not call it. Wired there: on Accept success, each
  returned `ruleChanges[]` entry is checked via `checkAffectedByRuleChangeAction`; entries that
  find affected bills queue into `corrections[]` state and show one `AccountCorrectionDialog` at a
  time (`onClose` pops the array). `tsc --noEmit` clean; `models/documents.test.ts` 60/60 still
  green (untouched by this change).
- **Environment limit (this session):** the provider pre-check (`checkQuickBooksBillCorrectable`
  / `checkXeroBillCorrectable`) makes a real HTTP call to the ledger API; there is no dev mock for
  either client (checked `lib/integrations/{quickbooks,xero}/client.ts` and `scripts/dev/` — only
  unit tests stub `fetch`). Seeded against a fake `externalTenantId`, every row's check will throw
  and get caught to `{code:"not_found"}`, so **every row renders refused** — the happy-path
  ticking/enabling of "Update N bills" cannot be exercised live without a real QuickBooks/Xero
  sandbox tenant (none configured in this environment — prod also has 0 connections per
  `docubite_nango_connections` memory). The refused-row render (disabled checkbox, reason text,
  bottom-sorted, "Open in ledger") is still real and checkable live.
- `ledgerBillUrl` in the dialog still guesses QuickBooks/Xero bill-edit URL shapes — unverified
  against a real tenant (carried over from prior session, unchanged).

## Session note (this session)

Wrote and ran `scripts/dev/seed-account-correction.ts` (new, committed) — a posted bill (Acme Fuel
Co, $240, coded to "sundry-expenses") plus a SupplierAccountRule for the same supplier on the same
account, plus an IntegrationConnection, so changing the rule's account or the connection Default in
the Integrations settings UI triggers `checkAffectedByRuleChangeAction` and opens the Screen 1
dialog. Ran once against the devdb workspace (`af91555d-…`) — succeeds, prints the created ids.
Did not get to the dev-server click-through this session (no server was up on :3000 for this
worktree branch; standing one up + Playwright + detector + critique is the next session's first
block of work). No code changes to app/components this session — seed script only, so no new
detector/critique run is owed yet.

## Next step

Bring up `next dev` for this worktree (check `ss -ltn | grep :3000` first; if occupied by another
worktree's server, use the port it prints), run the seed script above, then in the Integrations
settings UI change the connection's Default account (or the Acme Fuel Co rule's account) away from
"Sundry Expenses" to pop the dialog. Click through at 1440/390 with `scripts/dev/detect-in-page.mjs`
against this (refused-row) state; run `impeccable critique`/`evaluate` on it; fix findings within
this state's scope (do not block on the happy-path ticking state — that needs real provider
credentials, out of this environment's reach, note it as a carried-over open finding rather than a
blocker). Repeat for the single-approve (bill-pane.tsx) and bulk-approve (finance-proposal.tsx)
triggers if time allows, else same dialog component covers all three. Mark step 3 `done` once the
refused-row state is verified and clean; move to step 4. Re-read `craft-floor.md` before any visual
fix.
