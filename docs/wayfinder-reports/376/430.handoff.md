# Hand-off — #430 Build: correcting posted bills' Accounts in the ledger

milestone: build-done

## Step plan

- step: 1-5 — done (prior commits e1dae2e, b721113, c253a4a et al.)
- step: 6 — build gate — kind: surface — in progress

## Artifacts

- Spec: docs/wayfinder-reports/376/430.spec.md
- **Uncommitted fix (working tree)**: `models/documents.ts` — `findAccountCorrectionReminders` now
  normalizes the doc's raw vendor name before comparing to `SupplierAccountRule.supplierName`
  (always normalized) — the mismatch meant the Screen 3 reminder line never rendered. Fixed +
  regression test in `models/documents.test.ts` (65/65 passing). `tsc --noEmit` clean. Commit this
  first regardless of critique outcome — it's a verified, independent bug fix.
  Untracked scratch: `scripts/wayfinder-autopilot/debug430.mjs` (delete or keep, caller's call).
- Current round: docs/wayfinder-reports/376/logs/scratch-430/shots-r4/ (detector.json,
  keyboard.json, PNGs @1440/390, 6 states) — confirms the fix live: reminder line renders, Review
  reopens Screen 1's dialog correctly, both widths.
- Dev + live-server still running (ws af91555d-7450-4b21-a8ac-73db092617c8) — `node
  .impeccable/live/dev.mjs stop` before closing.
- Critique scratch: docs/wayfinder-reports/376/logs/scratch-430/close.md — write triage there.

## Open findings (see close.md for full detail once written)

- 29-42 ambient detector findings (ai-color-palette, overused-font, dark-glow, layout-transition,
  text-overflow, tiny-text, em-dash-overuse, clipped-overflow-container, first-viewport-column-
  overflow): triaged as pre-existing site-wide chrome, not introduced by #430 (confirmed via diff
  review of all three touched files — no gradient/glow/font/overflow classes added). Assessment B
  subagent (`aca716aaa141e622a`) independently agreed.
- `cramped-padding`/`nested-cards` on the Screen 1 dialog (new in step 3): Assessment B called it
  "real but minor, P2/P3, borderline" — not blocking.
- Assessment B also flagged two NEW points worth a look: (1) the dialog's primary button reads
  "Update 0 bills in QuickBooks" while unchecked by default — possibly confusing copy; (2) the
  reminder line's "Review"/"Leave them" links sit close together with only a middot separator.
- Assessment B's read of `account-select-enabled: false` in keyboard.json as a P0/P1 bug is a
  **misreading — do not act on it as-is**: the seeded bill is Paid, and 430.spec.md (~line 25) says
  QBO pre-checks treat a Paid bill as un-offerable (verified against
  `checkQuickBooksBillCorrectable`, lib/integrations/quickbooks/client.ts:170-177) — the disabled
  select + refusal sentence on this seeded bill is correct, spec-compliant behavior, not a defect.
- Assessment A (design review, Nielsen /40) — **DONE, result below** (agentId `a39457e531a471730`
  if re-reading its transcript is ever needed, but the result is fully captured here).

## Assessment A result (critique clears the bar)

Total **35/40**, all 10 heuristics scored (none n/a), lowest score **3** (H1, H5, H7, H8, H10) —
clears "≥32/40, no heuristic <3". Strengths: copy discipline, honest Leave-them/Not-now pairing,
Screen 2 never reverts a failed edit. 4 priority issues, all Medium/Low, refinements not blockers:
(1) Screen 3 reminder line visually dense — pack count+account+2 links with no separation; (2)
"Leave them" and "Review" same-weight inline text links risk mis-click; (3) no loading affordance
between clicking Review and the dialog opening; (4) Screen 1's rightmost table column mixes 3
content shapes across row states. None require a code fix to clear the bar; optional polish only.

## Next step

Close-session progress and full next-step detail: docs/wayfinder-reports/376/logs/scratch-430/close.md
(bug fix confirmed committed, tsc/tests clean, detector re-triaged clean, keyboard probe
reconfirmed non-defect, critique already cleared 35/40; evaluate-skill Agent launched against
live server, output at .../scratch-430/evaluate-430.md — read it, confirm health ≥85/0 P0P1/
Clean, then write the close report + resolution comment + close #430 + Decisions-so-far line,
then stop dev/live servers).
