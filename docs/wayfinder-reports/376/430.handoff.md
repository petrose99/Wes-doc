# Hand-off — #430 Build: correcting posted bills' Accounts in the ledger

milestone: build-done

## Step plan

- step: 1 — Backend: provider pre-check + account-only update calls — kind: backend — done
- step: 2 — Backend: find-affected-bills query + server actions + "Leave them" flag — kind: backend — done
- step: 3 — Surface: Screen 1 "N bills already posted" dialog — kind: surface — done
- step: 4 — Surface: Screen 2 Detail pane Account-only edit on Posted/Paid rows — kind: surface — done
- step: 5 — Surface: Screen 3 rule-row reminder + Review/Leave-them wiring — kind: surface — done
- step: 6 — build gate — kind: surface — todo (detector before/after, critique/evaluate/include, tsc)

## Artifacts

- Spec: docs/wayfinder-reports/376/430.spec.md
- Backend, committed 5e7b03e: models/documents.ts `recordDocumentLineAccountsCorrected`;
  account-correction-actions.ts `resolveDocumentPushConnection`,
  `checkDocumentAccountCorrectableAction`, `updateDocumentLineAccountsAction`.
- Step 4, committed b721113: components/documents/line-items-editor.tsx,
  components/pipeline/document-detail/split-pane.tsx, line-items-section.tsx.
- Step 5, committed e1dae2e: models/documents.ts `findAccountCorrectionReminders` (+ tests in
  documents.test.ts); account-correction-actions.ts `leaveAllAffectedByRuleAction`;
  components/settings/supplier-accounts-table.tsx (reminder line, "Review"/"Leave them", reuses
  `AccountCorrectionDialog` via `checkAffectedByRuleChangeAction`); admin/integrations/page.tsx
  wires reminders in. tsc --noEmit clean, `vitest run models/documents.test.ts` 64/64 passing.
  Not yet live-verified against a running dev server.

## Open findings

None yet.

## Next step

Step 6, the build gate: bring up the dev server, seed a posted/paid bill with a stale-account
line plus a corresponding SupplierAccountRule (reuse/extend
scripts/dev/seed-account-correction-approve.ts), live-verify Screens 1/2/3 together at 1440 and
390 (detector before/after via `impeccable detect --json` + the in-page overlay), then run
`impeccable critique`/`evaluate`/`include` against the Detail pane and Supplier accounts table
and close out per the autopilot bar (critique ≥32/40 no heuristic <3, evaluate ≥85 health
0 P0/P1, tsc clean already). Stop the dev server/detector before ending the session.
