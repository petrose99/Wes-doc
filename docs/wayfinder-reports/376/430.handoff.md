# Hand-off — #430 Build: correcting posted bills' Accounts in the ledger

milestone: build-done

## Step plan

- step: 1 — Backend: provider pre-check + account-only update calls — kind: backend — done
- step: 2 — Backend: find-affected-bills query + server actions + "Leave them" flag — kind: backend — done
- step: 3 — Surface: Screen 1 "N bills already posted" dialog — kind: surface — done
- step: 4 — Surface: Screen 2 Detail pane Account-only edit on Posted/Paid rows — kind: surface — done
- step: 5 — Surface: Screen 3 rule-row reminder + Review/Leave-them wiring — kind: surface — done
- step: 6 — build gate — kind: surface — todo (fix batch, then critique/evaluate/include, tsc re-run)

## Artifacts

- Spec: docs/wayfinder-reports/376/430.spec.md
- Step 5, committed e1dae2e: models/documents.ts `findAccountCorrectionReminders` (+ tests);
  account-correction-actions.ts `leaveAllAffectedByRuleAction`;
  components/settings/supplier-accounts-table.tsx (reminder line, Review/Leave-them, reuses
  `AccountCorrectionDialog`); admin/integrations/page.tsx wires reminders in.
- Dev seed used for verification (workspace af91555d-7450-4b21-a8ac-73db092617c8, already running
  from #429): `npx tsx --env-file .env scripts/dev/seed-account-correction.ts <ws>`, then
  `scripts/dev/seed-account-correction-approve.ts <ws>`, then a one-off retarget helper
  scripts/wayfinder-autopilot/tmp-retarget430.mjs (updates the "acme fuel" rule to
  accountExternalId "fuel" directly rather than round-tripping the approval flow, reproducing the
  Screen 3 mismatch: rule=fuel, posted bill still coded sundry-expenses).
- Round script: scripts/wayfinder-autopilot/round-430.mjs — state 01 = Supplier accounts table
  reminder + Review click reopening Screen 1's dialog; state 02 = Detail pane on the posted bill
  (documentId 570d8e7a-479b-41ee-9fe5-e10c9415d3d8), Account select visible/enabled probe.
- Capture: /home/ubuntu/Dev/Wes-doc/docs/wayfinder-reports/376/logs/scratch-430/shots-r1/
  (detector.json + keyboard.json + PNGs, both widths). Ran clean (no errored states): 4 states,
  29 non-residue findings total — **not yet triaged** (findings by type: ai-color-palette 10,
  text-overflow 2, overused-font 4, layout-transition 4, dark-glow 4,
  clipped-overflow-container 5, tiny-text 2, em-dash-overuse 1, first-viewport-column-overflow 1).
  This is the FIRST round (no baseline diff run yet) — some of these findings likely pre-exist on
  this page from #429's own build (the Supplier accounts table and Detail pane are both reused
  surfaces, not newly drawn) rather than being introduced by step 5. Do not assume all 29 are new.

## Open findings

Not yet triaged against a baseline or read against the PNGs — this is the next session's first
task. Dev server and live-server were stopped cleanly at session end
(`node .impeccable/live/dev.mjs stop`) — a fresh session must `start af91555d-7450-4b21-a8ac-73db092617c8`
again before re-running the round or re-seeding (seed scripts are idempotent, safe to re-run;
tmp-retarget430.mjs is also idempotent — safe to re-run if the rule needs re-pointing to "fuel").

## Next step

1. `node .impeccable/live/dev.mjs start af91555d-7450-4b21-a8ac-73db092617c8`.
2. Open detector.json in the scratch folder and the 4 flagged PNGs at both widths; sort each
   finding into "pre-existing on this reused surface" (check against #429's own critique snapshot
   in .impeccable/critique/) vs "introduced by step 5's new reminder line/dialog reuse" — only the
   latter needs a fix in this ticket.
3. Fix what's step 5's own, re-run round-430.mjs to confirm clean (or down to only pre-existing
   residue), then run `impeccable critique` and `impeccable evaluate` against the Supplier
   accounts table (Screen 3) and the Detail pane's Account edit (Screen 2) — bar is critique
   ≥32/40 no heuristic <3, evaluate health ≥85 with 0 P0/P1, plus `impeccable include` for the
   keyboard flow already captured in keyboard.json.
4. Re-run `npx tsc --noEmit` (was clean as of e1dae2e; unaffected by this round) and
   `npx vitest run models/documents.test.ts` (64/64 passing as of e1dae2e).
5. Close per the autopilot bar: report at docs/wayfinder-reports/376/430.md using
   scripts/wayfinder-autopilot/report.md's format, resolution comment + close on #430, one line on
   the map's Decisions-so-far. Delete the tmp-retarget430.mjs scratch script before closing if it
   isn't otherwise useful (it's a verification aid, not part of the shipped surface).
