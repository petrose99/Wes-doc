# Hand-off — #430 Build: correcting posted bills' Accounts in the ledger

milestone: build-done

## Step plan

- step: 1-5 — done (prior commits e1dae2e, b721113, c253a4a et al.)
- step: 6 — build gate — kind: surface — in progress

## Artifacts

- Spec: docs/wayfinder-reports/376/430.spec.md
- Critique (prior session): 35/40, all heuristics ≥3 — clears the bar, no rerun needed.
- Evaluate (this session, first pass): docs/wayfinder-reports/376/logs/scratch-430/evaluate-430.md
  — 61/100, Clean verdict, 0 P0, 4 P1. Fixes applied this session:
  - P1 "silent Leave-them dismiss": **false positive** — verified live
    (scratch-430/verify-toast.mjs) the toast *does* fire (~800ms after click,
    `toast.success` in supplier-accounts-table.tsx already present). No code change.
  - P1 "Screen 2 Account select silently shows no account": **real bug, fixed** — commit 9870377.
    `LineAccountCell`'s fallback `<option>` was gated on a lookup that could only be truthy when
    already redundant; the real coded account (present in `codingData`) never rendered when it
    wasn't in the synced chart. Fixed in components/documents/line-items-editor.tsx (both editable
    and disabled branches). Verified before/after with scratch-430/verify-screen2-select.mjs.
  - P1 "two under-differentiated dismiss actions" (dialog's Leave-them/Not-now vs row's Leave
    them): assessed as intentional, consistent naming (H4) rather than a defect — not fixed.
  - P1 "contradictory supplier-match state" (`bill-pane.tsx` SupplierCard "No supplier matched"):
    confirmed pre-existing, **not touched by #430** (git blame — last changed in #362, and #430's
    only edit to that file added the AccountCorrectionDialog trigger, nothing near SupplierCard).
    Out of scope for this ticket; worth a follow-up ticket on the map if it's still live.
  - tsc --noEmit clean after the fix.
- **Next**: rerun the evaluate-skill Agent for an updated close-evaluate score (the 61 above is the
  first pass, pre-fix) — needs ≥85, 0 P0/P1, Clean to clear the bar.
- Dev + live-server running (ws af91555d-7450-4b21-a8ac-73db092617c8) — stop with
  `node .impeccable/live/dev.mjs stop` before ending the ticket.
- Detector (shots-r4, prior round): all findings pre-existing/ambient — not re-blocking.
- keyboard.json's `account-select-enabled: false`: confirmed correct (Paid-bill un-offerable
  per spec), not a defect.

## Open findings

docs/wayfinder-reports/376/logs/scratch-430/close.md — triage detail, now superseded by this file
for the fix status; evaluate-430.md for the full first-pass write-up.

## Next step

1. Rerun the evaluate skill (Agent, general-purpose/sonnet) against the live server — same URLs as
   before (settings/integrations for Screen 1/3, documents/570d8e7a-... for Screen 2) — to get a
   post-fix close-evaluate score. Reseed if reminders/data were consumed by a prior test run:
   `npx tsx --env-file .env scripts/dev/seed-account-correction.ts <ws>` then
   `npx tsx --env-file .env scripts/wayfinder-autopilot/tmp-retarget430.mjs <ws>`.
2. If ≥85/0 P0-P1/Clean: write the close report (docs/wayfinder-reports/376/430.md), resolution
   comment + close #430, one Decisions-so-far line on map #376, then stop dev/live servers.
3. If still short: fix remaining P1s or downgrade with evidence (as done above), re-evaluate once.
