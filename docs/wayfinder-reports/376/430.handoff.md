# Hand-off — #430 Build: correcting posted bills' Accounts in the ledger

milestone: spec-done

## Step plan

- step: 1 — Backend: provider pre-check + account-only update calls — kind: backend — todo
  (QBO `getBookCloseDate`, live `getBillRef`; Xero `getOrganisationLockDates`, live line read;
  `updateBillAccounts()` in each `lib/integrations/{quickbooks,xero}/client.ts` — full resend,
  tax type unchanged, one stale-token retry. Spec: Engineering questions, Screen 1 pre-check text.
  Files: `lib/integrations/quickbooks/client.ts`, `lib/integrations/xero/client.ts`, their
  `bill-mapper.ts`, `errors.ts`. Check: unit tests per provider — offered/refused/stale-retry.)
- step: 2 — Backend: find-affected-bills query + server actions + "Leave them" flag — kind: backend
  — todo (query: `Document.codingData.items[].account_external_id` = old account AND ledger fact
  posted/paid for the connection, per `lib/documents/stages.ts`; actions alongside
  `app/(app)/workspaces/[workspaceId]/integration-push-actions.ts`: list-affected, update-selected
  (partial outcome), leave-them (per-document flag, new column or side table). Fires on
  `SupplierAccountRule` save and `IntegrationConnection` Default save. Check: action tests for the
  0-affected no-op, partial outcome, leave-them idempotency.)
- step: 3 — Surface: Screen 1 "N bills already posted" dialog — kind: surface — todo (list path,
  bulk select, disabled/refused rows sorted last, submit → per-row Partial outcome. Reuse
  `components/list-screen/list-screen-shell.tsx` `ListScreenBulkActionBar` + `components/ui/
  dialog.tsx`. Spec: Screen 1 in full.)
- step: 4 — Surface: Screen 2 Detail pane Account-only edit on Posted/Paid rows — kind: surface —
  todo (`components/documents/line-items-editor.tsx`: lock every field but Account when ledger
  fact is posted/paid; dirty state; "Update in {Provider}" button; inline error, edit never
  clears. Spec: Screen 2 in full.)
- step: 5 — Surface: Screen 3 rule-row reminder + Review/Leave-them wiring —  kind: surface — todo
  (`components/settings/supplier-accounts-table.tsx`: "{N} posted bills still on … · Review" line,
  wired to steps 2/3's actions and back into Screen 1. Spec: Screen 3 in full.)
- step: 6 — build gate — kind: surface — todo (detector before/after on steps 3–5, `impeccable
  critique`/`evaluate`/`include` per the closing bar, `tsc --noEmit` clean since TypeScript
  changed.)

## Artifacts

- Spec: docs/wayfinder-reports/376/430.spec.md
- Research: docs/research/ledger-bill-account-update.md (already existed, cited by the ticket)
- Prior art: #429 commit ea6d033, its spec docs/wayfinder-reports/376/429.spec.md

## Open findings

None yet — spec session only. Engineering questions section of the spec carries the one open
sandbox check (QBO paid-bill payment-link survival); it does not block step 1.

## Next step

Build session: start step 1 (provider client update calls), reading only
`lib/integrations/quickbooks/client.ts` and `lib/integrations/xero/client.ts` by range plus the
spec's Engineering questions section — do not re-read the whole spec or re-run recon.
