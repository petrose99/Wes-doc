# Autopilot report — #400 Research: how document-first tools without cards split capture from the ledger — Dext, Hubdoc, AutoEntry

- Map: #376 Map: Ledger connections through Nango — QuickBooks, Xero, Sage Accounting; Bigcapital removed
- Type: wayfinder:research · Started: 2026-09-23 · Finished: 2026-09-23
- Outcome: resolved

## What I did
1. Claimed the ticket (`gh issue edit 400 --add-assignee @me`).
2. Read the map body and `CONTEXT.md` for DocuBite's own typed-queue vocabulary (Post/Posted, Closed, Intake) to check findings against DocuBite's existing design, not just the vendors' docs.
3. Researched Dext Prepare, Hubdoc and AutoEntry via `WebSearch`/`WebFetch` against their own help-centre and product pages, covering the ticket's five questions: publish-view shape, connection/publishing settings, how account defaults are first set up, post-publish correction, and bank-statement handling.
4. Posted the [resolution comment](https://github.com/petrose99/Wes-doc/issues/400#issuecomment-5788887667) with findings, sources, and where DocuBite's document-first/WhatsApp/LS-ZA/nothing-un-posts position stops each pattern from transferring as-is.
5. Closed #400.
6. Appended one line to the map's Decisions so far and committed the report.

## Questions I answered on the owner's behalf
None — this is a research ticket with no HITL grilling round; it answers the map's own research question directly from primary sources.

## Decisions recorded
Document-first tools publish per document, not through a cross-type queue (matches DocuBite's existing typed-queue design — no change needed). Default account/category rules are learned per supplier from use, not guessed in bulk from the chart of accounts — DocuBite's map already commits to a bulk day-one guess instead, a deliberate deviation, not a gap. Post-publish correction is undocumented by all three vendors and cannot be borrowed as "edit and re-sync" without contradicting DocuBite's "disconnect never un-posts" rule — flagged unverified, not transferable. Bank-statement handling is the least confirmed finding (AutoEntry extracts/publishes; Hubdoc's docs don't distinguish statements from bills) — closer to attach-only than a bank-feed import, which fits DocuBite's own statements-for-reconciliation design better than a transaction-import pattern.

## Tickets created / changed
No new tickets. No fog graduated — the ticket's answer confirms existing map decisions (per-document publish, bulk-guess defaults) rather than opening new questions; it explicitly feeds the already-existing guessed-defaults ticket (#383/#397 territory) and the ticket this one blocks, #402.

## Scores and counts (rendered surfaces only)
n/a — research ticket, no rendered surface touched this session.

## First-pass findings → lessons (rendered surfaces only)
n/a

## Needs the owner
None. Two findings are marked unverified in the resolution (post-publish correction path; bank-statement posting behaviour) rather than stated as fact — worth a closer look only if a later ticket (bank statements or guessed defaults) needs certainty there.
