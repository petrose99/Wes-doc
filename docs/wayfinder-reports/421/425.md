# Autopilot report — #425 Model the Payout destination: bank account or mobile-money wallet, and the Payout details change

- Map: #421 Pay Payees in Lesotho through Chaperone: wallets, three banks, and a rail nothing else
- Type: wayfinder:grilling · Started: 2026-09-23 · Finished: 2026-09-23
- Outcome: resolved

## What I did
1. Claimed #425 (`gh issue edit 425 --add-assignee @me`).
2. Read map #421's Notes and Decisions so far (owner's 2026-09-23 scoping: supported destinations are Chaperone's five wallets plus bank accounts at Standard Lesotho Bank, FNB Lesotho, Nedbank Lesotho), and the two blocking research tickets (#422 wallet API, #427 bank-connection, unconfirmed).
3. Read the existing model: ADR 0006 (`docs/adr/0006-bank-details-saved-only-by-people-held-on-change-snapshotted-at-submit.md`), ADR 0003 (Claimant bank details on the membership), `CONTEXT.md`'s *Bank details change*/*Bank confirmation*/*Bill Pay*/*Payment batch* entries, `lib/checks/bank-details.ts` (the IBAN-only Check), and `prisma/schema.prisma`'s `Supplier.bankDetails` field, to ground the shape in what already exists rather than invent one.
4. Resolved the ticket's questions myself under the standing delegation (grounded in the ticket's own owner-scoping text, ADR 0006's existing singular-version shape, and #422's five-network list) — see Q&A below.
5. Edited `CONTEXT.md`: added a *Payout destination* glossary entry, renamed *Bank details change* → *Payout details change* and widened its body for both kinds and a kind switch, touched *Bank confirmation* and *Bill Pay* wording.
6. Renamed and widened ADR 0006 (`git mv` to `0006-payout-details-saved-only-by-people-...md`), adding a "Widened 2026-09-23" paragraph and a Consequences bullet giving the concrete field shape.
7. Committed as `4845c55`.
8. Posted the resolution comment, closed #425, and updated map #421's *Decisions so far* with a one-line gist and link.

No design-skill pass (`intent`/`impeccable`): the change touches only `CONTEXT.md` and `docs/adr/`, no `app/**` or `components/**` — CLAUDE.md's path-based gate does not apply, and there is no rendered surface yet to critique or score. Both `grilling` and `domain-modeling` were consulted per map #421's Notes; the "questions" below were answered under the run's standing delegation rather than a live human exchange, since this is an autopilot session.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|----------|--------------|----------------------|
| 1 | One destination per Payee, or several with a default? | One active destination per Payee; no multiple destinations, no default. | ADR 0006 already models one "acknowledged details" version per Payee; nothing in the ticket or map #421 asked for multiple, and a default-among-several UI is unrequested scope. |
| 2 | Shape of bank vs wallet fields, and which banks/networks are selectable? | Bank: bankName (Standard Lesotho Bank \| FNB Lesotho \| Nedbank Lesotho), branchCode, accountNumber. Wallet: network (C-Pay \| M-Pesa \| EcoCash \| MyWallet \| Khetsi), number, accountName. Form-only validation, no verification. | Owner's 2026-09-23 scoping note on the ticket names exactly these three banks; #422's research names exactly these five networks. "DocuBite still verifies nothing" is the ticket's own instruction. |
| 3 | Does "first details are not a change" hold when a wallet is first and a bank is added later? | No — only the very first destination ever entered is exempt; adding the other kind later is a replacement (one destination per Payee), so it holds the Payee like any change. | Falls directly out of the "one destination" answer to Q1: there is nothing to add beside an existing destination, only something to replace. |
| 4 | What does "details differ from those on file" mean when the file holds a wallet and the invoice shows a bank account? | A kind mismatch fails the Check by itself, before any field is compared. | The clearest possible "these don't match" signal for the fraud-control Check ADR 0006 already makes hard-blocking; consistent with the Check's existing "fail, never warn" posture on any difference. |
| 5 | Rename *Bank details change* → *Payout details change* in CONTEXT.md; amend ADR 0006. | Done — glossary entry renamed and widened; ADR 0006 file renamed and widened in place (not a new ADR: same decision, extended scope). | Ticket's explicit instruction; the Bill Pay and Payment batch glossary entries already used the new name, so the definition entry was the one lagging. |

## Decisions recorded
Payout destination modelled (bank or wallet, one per Payee, three banks / five wallet networks, form-only validation); ADR 0006 widened to a kind switch under the same hold/decide/restore/snapshot rules, with "first details" now understood as the Payee's true first destination only; the invoice Check's "differ" now covers a kind mismatch. Glossary renamed throughout.

## Tickets created / changed
None created. #425 closed. Map #421's Decisions so far gained one line for #425; no fog in *Not yet specified* named this question directly, so nothing there needed clearing (the remaining fog — the real bank-adapter mapping, the Chaperone adapter build, onboarding, fees, ledger, reconciliation — is unaffected).

## Scores and counts (rendered surfaces only)
No score; surface not built yet — this ticket is a domain-model decision (`CONTEXT.md` + `docs/adr/`), never `app/**` or `components/**`.

## First-pass findings → lessons (rendered surfaces only)
N/A — no rendered surface touched.

## Needs the owner
Nothing deferred. The bank route's real API details remain the owner's separate task (map #421 fog: "the real bank-payout adapter's request mapping"), unaffected by this ticket, which models the destination shape independent of which rail eventually carries it.
