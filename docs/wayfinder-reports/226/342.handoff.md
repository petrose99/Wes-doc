milestone: spec-done
milestone: step-1-done
milestone: step-2-done
milestone: step-3-done
milestone: step-4-done
milestone: step-5-done
milestone: step-G1-done
milestone: step-G2-done
milestone: build-done

# #342 — Build the reorganized rail (execution of #291)

## Artifacts
- Spec: docs/wayfinder-reports/226/logs/scratch-342/spec.md
- Pre-flight (filled, gate passed): docs/wayfinder-reports/226/logs/scratch-342/preflight.md
- Area primer: docs/agents/areas/admin.md (Admin nav, primitives, seed/capture recipe)
- Surface: components/shell/sidebar.tsx, admin/layout.tsx nav, account/page.tsx,
  components/shell/account-menu.tsx, components/shell/how-it-works.tsx,
  components/shell/keyboard-shortcuts.tsx, prisma/schema.prisma (User.railWidth),
  actions.ts (rail-width get/set), CONTEXT.md (Finance→Accounting entry)

## Open findings
None outstanding — spec critic (foreground Agent) predicted 33/40 critique, evaluate sum-5
(≈90+), 0 P0/P1, Clean; two spec fixes made in response (H10 Help section, Admin-row width
pinned to a concrete value). See preflight.md Part D/E for the full reconciliation.

## Plan

- step: 1 — Today group + Archive move + Accounting rail item (§1, §2 of spec: reordered
  `primaryItems`/`bottomItems`, added Accounting item + badge/comment rename, `g c` shortcut,
  legacy `/accounting` prefix, minimal Admin-vs-Accounting isActive fix ahead of §5's full
  centralization) — files: sidebar.tsx, keyboard-shortcuts.tsx — tsc --noEmit clean — done
- step: 2 — Admin always-visible row + Activity/Health into Admin nav (§3: `alwaysLabelled` navLink
  option, `w-16`/`text-[13px]` committed width, remove Activity/Health from bottomItems, add to
  admin/layout.tsx `groups`) — files: sidebar.tsx, admin/layout.tsx — done
- step: 3 — Three-state rail-width: schema + actions (§4 persistence: `User.railWidth` migration,
  `getRailWidthAction`/`setRailWidthAction` in actions.ts mirroring approvalNoticeEmails) — files:
  prisma/schema.prisma + migration, actions.ts — tsc --noEmit clean — done
- step: 4 — Three-state rail-width: UI (§4 UI: `RailWidthControl` + `RailWidthMenuSelect` in new
  components/shell/rail-width-control.tsx mirroring `ApprovalEmailsControl`, Account page field,
  account-menu inline select (no dialog — select not role=menuitem, menu keydown skips a focused
  select), Sidebar reads `railWidth` prop replacing `pinned`/`RAIL_PIN_KEY`/`togglePin`,
  `expandsOnHover` gates the hover-expand classes for "icons") — files:
  components/shell/rail-width-control.tsx (new), account/page.tsx, account-menu.tsx, sidebar.tsx,
  layout.tsx — tsc --noEmit clean — done
- step: 5 — Centralize isActive (§5: `RailNavItem.matchPrefixes`, longest-prefix-wins reduce over
  `allNavItems`, dropped the dead `exact` field — confirmed by grep, always `false`, never read) +
  Help section (H10: HowItWorksDialog two lines re Accounting/`g c` and rail-width, `?` dialog's
  `g c` row already wired in step 1 via SHORTCUT_DESTINATIONS) + CONTEXT.md Finance→Accounting
  entry — files: sidebar.tsx, how-it-works.tsx, CONTEXT.md — tsc --noEmit clean,
  `grep -n "togglePin\|RAIL_PIN_KEY\|exact:" sidebar.tsx` empty — done
- step: G1 — round script (capture states table: rail icons/labels/auto×hover, Accounting
  item absent/present/badge/active, Admin row, rail-width control idle/saving/error, Admin nav
  Activity/Health — at 1440 and 390) — done
- step: G2 — r0 capture + detector fixes (gate.mjs, tsc --noEmit) — done
- step: G3 — build-done: full-project `tsc --noEmit` (servers down) clean, Part B contract
  checks re-verified — done

Sized within limits: 5 build steps + 3 gate steps ≤ 6 substantive steps before gates; ~9 capture
states × 2 widths ≤ 10 states. No split needed.

milestone: measured
milestone: close-done

## Close

Triage, confirm re-score (critique 39/45, evaluate 96/100 Clean, both 0 P0/P1) and checks
(tsc/eslint/tests/build all clean) are in `docs/wayfinder-reports/226/logs/scratch-342/close.md`.
Report: `docs/wayfinder-reports/226/342.md`. Closed at the bar, no follow-up tickets.
