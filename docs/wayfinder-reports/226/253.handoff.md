# Hand-off — #253 Build Approval Flows and PO Mismatch Flows in Admin

Written by the owner's session on 2026-09-16 after the 09:26 autopilot session exited without closing (it delegated the build to a background agent and ended its turn waiting; the agent died with it). Continue from here.

## Milestones

| Milestone | State | Where |
|---|---|---|
| Spec (`intent specify`) | done (10:xx session, reconciled against the build) | `logs/scratch-253/preflight.md` §Spec |
| Pre-flight (parts A–E incl. Part D opus critic: 28/40 → 11 fixes applied → predicted 32/40, 0 P1) | done | `logs/scratch-253/preflight.md` |
| Schema + migration | done | `prisma/schema.prisma`, `prisma/migrations/20260916060000_add_default_approval_flow_and_mismatch_approvers/` |
| Models | done | `models/approval-defaults.ts` (default flow, `estimateAutoStarts`, `AUTO_START_ESTIMATE_DAYS`), `models/mismatch-approvers.ts` (`canOverrideMismatch`, `setMismatchApprovers`), auto-start wired in `models/review-tasks.ts` |
| Server actions | done | `app/(app)/workspaces/[workspaceId]/actions.ts`: `setPoMismatchPolicyAction` (tolerances + approvers under one save), `overrideGate` refuses non-approvers on match-variance gates and names who can; old tolerance action left `@deprecated` (an orphan for owner sign-off, not deleted) |
| Surface | built, unverified | `components/workspace/default-approval-flow.tsx`, `components/workspace/po-mismatch-policy.tsx`, `approval-workflow-form.tsx`, `admin/approval-flows/page.tsx`, `admin/po-mismatch-flows/page.tsx`; `po-quantity-tolerance.tsx` removed (no importers) |
| Typecheck | clean after the Part B/D fixes | — |
| First measurement (capture · critique · evaluate · include) | **not done** | — |
| Fix batch · confirm · polish · close | **not done** | — |

All of the above is committed as WIP in `3bdc1ec` on `codex/post-login-ux-implementation`.

## Next step

1. (done) tsc · Part B checks · Part D critic fixes (vocabulary → "flow", inline refusals, resync default on refresh, Counting…, ⌘S suspended under confirm, Reload errorAction, tolerances copy, pruned approver ids, threshold validation).
2. Seed, dev server, one capture pass of every state at 1440 and 390 (`#252`'s `admin252.mjs` in `logs/scratch-252/` is the recipe; Admin pages: approval-flows default/dirty/confirm-with-30-day-estimate, po-mismatch-flows default/dirty/save-error, both at read-only member).
4. Critique, evaluate, include on that set; fix batch; confirm; polish; close at the bar with counts on the ticket.

## Open questions answered on the owner's behalf

None yet — the decisions on the ticket body stand (defaults off by default, consequence sentences, confirm-with-30-day-estimate, match-variance approver source).
