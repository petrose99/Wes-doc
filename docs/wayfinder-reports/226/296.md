# Autopilot report — #296 Build the reversible batch approval: Reject on Approved batches and the tightened self-approval note

- Map: #226 Vic.ai-style operator app
- Type: wayfinder:task (execution) · Started: 2026-09-20 · Finished: 2026-09-20
- Outcome: partial (spec phase only — this is a 4-phase build ticket; build/measure/close run in later sessions)

## What I did
1. Claimed #296 (`gh issue edit 296 --add-assignee @me`).
2. Read map #226's Notes/Decisions and #278's resolution comment (ground truth for this ticket).
3. Read the current code: `models/payment-batches.ts` (approve/reject/loadForDecision),
   `components/payments/batch-queue.tsx` (Approved/Pending pane bars, Approve-confirm,
   Reject ReasonDialog), `components/payments/batch-detail.tsx` (Fact block, self-approved
   tag, audit detail rendering), `actions.tsx` (error message map).
4. Wrote the spec: `docs/wayfinder-reports/226/logs/scratch-296/spec.md` (9 sections: model
   change, pane bar, dynamic dialog description, rejected-view confirmation, self-approval
   note in both places, audit-detail rendering, Bill Pay verification note, phone note, tests).
5. Filled the pre-flight (`.../preflight.md`, Parts A–E) — read both lessons files in full
   first (generic + project).
6. Ran the spec critic as a fresh-context `Agent` against spec.md + preflight.md. It scored
   33/40 critique, 0 P0/P1 predicted, Clean verdict, and named 3 gaps (audit-detail rendering
   left undecided, pane-state-after-reject not stated in prose, `fromStatus` had no stated
   consumer).
7. Reconciled all 3 gaps into the spec (§6 now decides "(from Approved)" audit-line rendering
   and states the pane stays open/selected after a reject) and recorded the reconciliation
   table in preflight.md Part D. Gate met: critique 33/40 (H1=4, H5=4, rest 3), evaluate 0
   P0/P1 sum 0, Clean.
8. Sized the ticket to the phases: 3 build steps + 3 gate steps, 5 states × 2 widths — well
   under the 6-step/10-state split threshold, so it stays one ticket.
9. Wrote the build plan onto the hand-off file (`docs/wayfinder-reports/226/296.handoff.md`)
   with `milestone: spec-done`.

## Questions I answered on the owner's behalf
None — no HITL grilling round this session; the ticket's decision (#278) was already resolved
by a prior grilling session, this session only specs the build.

## Decisions recorded
None (this session doesn't resolve the ticket — spec phase of a 4-phase build; #296 stays
open, unassigned by the driver's convention until it completes all four phases and closes).

## Tickets created / changed
None created. #296 remains open (spec phase complete, build/measure/close pending).

## Scores and counts (rendered surfaces only)
scores: predicted-critique=33 first-critique= close-critique= first-evaluate= close-evaluate=

Predicted only — no code built yet. Predicted gate counts: 5 states (Approved-pane-owner,
Reject-confirm-no-download, Reject-confirm-with-download, Approve-confirm-self-submitted,
Rejected-from-Approved-pane) × 2 widths (1440, 390) = 10 captures planned for the measure
round; include check planned live at close.

## First-pass findings → lessons (rendered surfaces only)
N/A — no build yet this session.

## Needs the owner
Nothing — this is a small, fully-specified extension of an existing reversible pattern with
no removal and no owner-only decision left open.

## Session 3 (build, continuation)

Continued from `milestone: build-step-3-done` (steps 1–3 already committed by a prior session).
Confirmed `tsc --noEmit` output has no errors in the 4 touched files (all pre-existing,
unrelated). Built step G1 (round script proof):

- Ran the round script's smoke pass; found 2 states erroring at 390 width
  (`02-reject-confirm-no-download`, `05-rejected-from-approved-pane`, both `locator.click`
  timeouts). Root cause: state 05 fires a real reject on the shared NO_DOWNLOAD batch; the round
  runs all 5 states once per width, so the second width (390) hit an already-rejected batch and
  the Reject… button no longer existed.
- Fixed `round296.mjs`: state 02 now resets that batch back to `approved` via a raw `pg` query
  (table `payment_runs`) + `page.reload()` at the start of every run, so each width pass starts
  clean.
- Re-ran the smoke round: 10/10 states, 0 errored — G1 done.
- Stopped the dev server (port 3000 confirmed free) before ending.

Marked `step: G1 — done` on the hand-off, wrote G2's exact next command. Committed as
`wip(autopilot): #296 build step G1`.

## Scores and counts (update)
scores: predicted-critique=33 first-critique= close-critique= first-evaluate= close-evaluate=

G1 proved the round script executes cleanly across both widths (10/10 states, 0 errored); no
detector/critique/evaluate scoring yet — that's G2.

## Session 4 (build, continuation — G2)

Continued from `milestone: build-step-3-done` / `build-gate-g1-done`. Started the dev server and
`impeccable live-server` (`.impeccable/live/livesrv257.mjs` — was not actually running despite the
prior session's "stopped cleanly" note; started fresh, confirmed `:8400/detect.js` returns 200),
reseeded, ran `round296.mjs r0`, gated with `gate.mjs shots-r0 --residue-file residue.txt`.

First gate run: 1 real finding + 3 failing keyboard probes (all at 390). Investigated both with
throwaway Playwright inspection scripts (deleted after use):
- The 1 real finding (`body-text-viewport-edge` on the Rejected-status footer `<p>`) traced to
  `batch-queue.tsx:157`, shipped unmodified by #251 for every terminal status footer
  (Paid/Waiting/Rejected) — newly reachable via this ticket's Approved-origin reject flow, not a
  #296 regression. Recorded as residue (`body-text-viewport-edge.*57-char` added to
  `residue.txt`, alongside the standard app-wide four from `docs/agents/areas/finance.md`: no
  area primer exists yet for payments).
- The 3 failing probes traced to a round-script bug: the mobile pane itself renders with
  `role="dialog"` (a full-screen sheet), so `page.locator('[role="dialog"], [role="alertdialog"]')`
  matched 2 elements at 390 and Playwright's strict-mode `textContent()` threw, silently caught as
  `""`. Fixed in `round296.mjs`: `.last()` on the confirm-dialog text reads (states 03, 04), `<= 1`
  instead of `=== 0` on the post-Escape dialog count (state 02) — the full-screen pane is expected
  to remain after Escape closes only the nested confirm dialog.

Re-ran the round: `gate: CLEAN — real findings 0 (none); probes pass 14 fail 0 unjudged 0; errored
states 0; page errors 0` (both widths, all 5 states). No product code changed this session — both
fixes were to the residue list and the test harness, not `components/payments/*`.

Stopped both servers before ending (`ss -ltnp` confirmed empty). Marked G2 done on the hand-off,
wrote G3's exact next steps (`tsc --noEmit`, Part B contract checks, then `milestone: build-done`).
Committed as `wip(autopilot): #296 build gate G2` (012c7f7).

## Scores and counts (update)
scores: predicted-critique=33 first-critique= close-critique= first-evaluate= close-evaluate=

Detector gate (both widths, all 5 states): 0 real findings (after residue), 14/14 keyboard probes
pass, 0 errored states, 0 page errors. No critique/evaluate/include readers run yet — those are
the measure phase's job, per the build phase brief (gate steps don't run readers).
