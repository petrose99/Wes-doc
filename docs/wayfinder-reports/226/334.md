# Autopilot report — #334 Decide how coding learns and how the operator sees and steers it

- Map: #226 Map: Make the operator app work like Vic.ai (grammar, not skin)
- Type: wayfinder:grilling · Started: 2026-09-20 · Finished: 2026-09-20
- Outcome: resolved

## What I did
1. Claimed #334 (`gh issue edit 334 --add-assignee @me`).
2. Read the map (#226) body for Notes/standing constraints and the ledger-stance decisions (#292/#329) this ticket depends on.
3. Read the evidence named on the ticket: `field-row.tsx`, `rationale-popover.tsx` (`SOURCE_BADGE`, `RationalePopover`), `lib/rationale.ts`, `split-pane.tsx` (`canCreateRule` card, `FieldNavForm`), `models/automation-rules.ts` (`applyVendorHistoryCoding`, `updateAutomationRule`), `lib/automation/vendor-history.ts` (agreement/support thresholds), `models/coding-corrections.ts`, `models/category-account-mappings.ts`, and `app/(app)/workspaces/[workspaceId]/admin/suppliers/page.tsx`.
4. Checked `CONTEXT.md`'s "Avoid: Category" entries to confirm no existing user-facing violation (the coded field's label is workspace-configured, not hardcoded).
5. Answered the ticket's five questions with `➡️` recommendations grounded in the code already read (no separate grilling-round dialogue needed — the questions were concrete enough to answer directly against the evidence, per the standing delegation to take the recommended answer).
6. Posted the resolution as an [issue comment](https://github.com/petrose99/Wes-doc/issues/334#issuecomment-5751764128).
7. Closed #334 with the resolution and a link to the execution ticket.
8. Created [Build the in-place coding memory (#350)](https://github.com/petrose99/Wes-doc/issues/350), labelled `wayfinder:task`, and linked it as a sub-issue of #226 (`gh api -X POST .../issues/226/sub_issues`).
9. Appended one line to #226's Decisions so far, before `## Not yet specified`.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Remembered vs guessed — popover enough, or a visible mark, and what vocabulary? | Keep the existing always-visible `SOURCE_BADGE` pill (already more than a popover); rename `history`'s label "History" → "Remembered". `rule`/`ai`/`few_shot` unchanged. | The badge already renders outside the popover in `field-row.tsx:62-65` — the ticket's own framing ("we remembered this from your last five Acme invoices") names the missing word directly; `articulate` principle: name the outcome, not the mechanism. |
| 2 | What affordance makes a correction hold, and does it bypass the vendor-history threshold? | Inline line under the field: "Code {supplier} to {value} from now on? Keep · Just this once." Keep calls `createAutomationRule`/`updateAutomationRule` directly, bypassing `applyVendorHistoryCoding`'s 90%/3-row threshold. Just this once relies on the existing `recordCodingCorrection` path. No checkbox-on-Save, no silent auto-promotion. | Ticket's own honesty test ("no default that silently creates a rule"); an explicit human action is stronger evidence than a 3-row statistical prior, so it should supersede rather than wait for it. |
| 3 | What does the field show on a conflict, and how is a rule overridden/retired from here? | Override once = a normal edit+save (already works). Fix the badge to stop showing a stale Rule/Remembered mark once the input diverges from `rationale`'s stored value. Add a "Retire this rule" action inside `RationalePopover`'s rule branch, calling the already-existing `updateAutomationRule({ isActive: false })`. History-sourced values get no separate retire control — `getVendorCodingPrior` recomputes from confirmed rows every run, so an override is already one more self-correcting data point. | Reuses the exact action Admin's `AutomationRuleActiveToggle` already calls — no new rule-mutation path; avoids building a manual reset that would duplicate the existing statistics. |
| 4 | Field picks ledger account directly, or category stays primary? | Category stays primary/editable; the ledger account renders as a derived read-only line via `resolveCategoryAccount`/`CategoryAccountMapping` once a connection exists. Unchanged with no ledger connected. | #292/#329 already decided DocuBite posts through a translation layer, no primary built-in ledger; rules and vendor-history priors are keyed on category strings, which would break on reconnect to a different provider or company if the field itself switched to raw external account ids (#231/#291 multi-company). |
| 5 | Does Admin show what each vendor is coded to, for audit/retire — is that in scope? | Already built: Admin › Suppliers (`admin/suppliers/page.tsx`) shows trust, vendor coding history, rules, and payments per supplier today. Nothing to decide; the new Retire-from-pane action (Q3) is additive to this page, not a replacement. | Direct read of the existing page and its code comment (`#231 Q10 (#252)`). |

## Decisions recorded
Five decisions on the Detail pane's Details tab: (1) visible "Remembered" badge replacing "History"; (2) an inline Keep · Just this once line at the moment of correction, Keep bypassing the vendor-history agreement threshold; (3) a live (non-stale) badge plus a Retire-this-rule action inside the existing popover, with history-sourced values self-correcting rather than needing a reset control; (4) category stays the primary editable field with the ledger account rendered as a derived line once connected; (5) Admin › Suppliers already covers vendor-level review, unchanged. Full text: [resolution comment](https://github.com/petrose99/Wes-doc/issues/334#issuecomment-5751764128).

## Tickets created / changed
- Created [Build the in-place coding memory: field-level source badge, Keep/Just-this-once, and derived ledger account (#350)](https://github.com/petrose99/Wes-doc/issues/350) (`wayfinder:task`), linked as a sub-issue of #226.
- Closed #334 with the resolution comment and a pointer to #350.
- #226: appended one line to Decisions so far; no fog graduated or added (the ticket's questions were all answerable directly, none opened a new unresolved area) and nothing ruled out of scope.

## Scores and counts (rendered surfaces only)
Not applicable — this ticket is a decision/grilling ticket; no rendered surface was built or changed. Execution and its scores belong to #350.

## First-pass findings → lessons (rendered surfaces only)
Not applicable — see above.

## Needs the owner
Nothing deferred. No removal was decided (the existing bottom-of-tab "Create a rule from this document" card stays as-is, untouched, no sign-off needed) and no credential or ambiguous call was left open.
