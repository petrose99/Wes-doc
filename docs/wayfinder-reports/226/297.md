# Autopilot report — #297 Build the Direction row and Move to another queue: retire the Details-tab type chooser on typed queues

- Map: #226 Make the operator app work like Vic.ai (grammar, not skin)
- Type: wayfinder:task · Started: 2026-09-20 · Finished: 2026-09-20
- Outcome: partial (spec phase complete; build/measure/close remain)

## What I did
1. Claimed #297 (`gh issue edit 297 --add-assignee @me`).
2. Read the map (#226) and full ticket body; traced the existing code the
   ticket names: `split-pane.tsx`'s local `docType` state (actually the
   category value, not the queue `DocType`), `lib/doc-types.ts`'s
   `DOC_TYPE_SPECS`/`DocCategory`, `setDocumentTypeAction` and
   `reclassifyDocumentAction` (`app/(app)/workspaces/[workspaceId]/actions.ts`),
   the audit-event label system (`models/audit-events.ts`), the Admin
   field table (`lib/configuration/field-table.ts`,
   `components/admin/field-table-editor.tsx`), and the shared pane
   primitives (`PaneFrame`/`PaneMenu`/`PaneMenuItem`/`ConfirmDialog`,
   `document-actions-menu.tsx`) per the detail-pane area primer.
3. Wrote the full spec: `docs/wayfinder-reports/226/logs/scratch-297/spec.md`.
4. Filled the pre-flight (`docs/wayfinder-reports/226/logs/scratch-297/preflight.md`)
   — Parts A–F.
5. Ran the independent spec critic (fresh-context `Agent`, general-purpose,
   sonnet) against both rubrics; full analysis at
   `docs/wayfinder-reports/226/logs/scratch-297/critic.md`.
6. Reconciled the two P1-class findings into the spec (Part D):
   - The Move dialog's radiogroup left its arrow-key selection model
     ambiguous against the "no default pre-selected" rule — spec §3 now
     states the model explicitly (native ARIA radiogroup, arrow-key
     movement selects, ConfirmDialog's live consequence line is the
     feedback that makes it legible).
   - The audit-event label change ("no other event's label changes") was
     an unverified claim — spec now cites `auditEventLabel`'s three call
     sites (`models/audit-events.ts:111,138,190`) and confirms none needs
     a breaking signature change.
   Also tightened two citation gaps the critic flagged (Search pane is
   actually the **Library** route reusing the shared `DocumentDetailPage`,
   not a bespoke component; `ConfirmDialog`'s `children`/`confirmDisabled`
   props were confirmed against the real component, not assumed).
7. Wrote the build plan onto the hand-off (`docs/wayfinder-reports/226/297.handoff.md`):
   6 build steps + 3 gate steps, 6 named capture states (matching the
   ticket's own Bar section) — within the phase-sizing limits, no split
   needed.
8. Committed the spec artefacts and hand-off as WIP.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Should Move's radiogroup default to a target queue for convenience? | No default selection | Anti-pattern sweep: pre-selection risks nudging an unintended choice; no "expected" target exists here, so leaving it unset is the safer read, confirmed Clean by the spec critic |
| 2 | Is a dedicated "Undo" needed after a successful Move? | No — moving the document back is the same control run in reverse | Spec critic scored this H3=1 (cosmetic), not a P1/P0; consistent with #244's precedent that navigation-shape actions don't need a second reversal mechanism |

## Decisions recorded
None — this is an execution ticket building #283's decision, not a
decision ticket. No map/ticket resolution comment was posted; the ticket
stays open, assigned to this session's identity, with `milestone:
spec-done` on the hand-off for the next (build) session to pick up.

## Tickets created / changed
None created. #297 remains open (spec phase only; build/measure/close
phases still to run per the four-phase autopilot protocol).

## Scores and counts (rendered surfaces only)
scores: predicted-critique=32 first-critique=n/a close-critique=n/a first-evaluate=n/a close-evaluate=n/a
No code was built this session — no detector/critique/evaluate run yet.
Predicted (post-reconciliation) critique sum 32/40, no heuristic under 3,
two 4s (H1, H5); predicted evaluate heuristic-severity sum 2, 0 P0/P1
predicted, anti-pattern verdict Clean. These are pre-flight predictions
only; the first real pass happens at the measure phase.

## First-pass findings → lessons (rendered surfaces only)
N/A — no build/measure has run yet this ticket.

## Needs the owner
Nothing yet. The one owner-signature item on this decision chain (#283's
removal of the `sale` category / receivable ledger path) is already
tracked separately on #298, per the map's Decisions-so-far line; this
ticket's own scope (§1) explicitly unplugs dead code rather than removing
a feature, so it needs no fresh sign-off request.

## Session 2 (build, step 1)
Continuation session. Hand-off said step 1 ("Retire chooser + Direction
row on pane") was in progress with uncommitted, partial edits — checked
the working tree first: `lib/doc-types.ts` (the `hasDirectionField`/
`directionLockedFor`/`DIRECTION_FIELD_TYPES` helpers) and the import line
in `split-pane.tsx` were actually already committed (`c0f00f5`), so only
the remaining diff in `docs/wayfinder-reports/226/logs/scratch-297/step1-notes.md`
was outstanding.

Finished step 1 in `components/pipeline/document-detail/split-pane.tsx`:
removed `DOC_TYPE_LABELS`, the `docType`/`setDocType`/`savingDocType`
state, `selectDocType`, and the chooser JSX; the two `bank_statement`
checks now read the `documentType` prop directly. Added a `queueDocType:
DocType` prop to `SplitPane` (wired from `resolveDocType(document)` at
the one caller, `app/(app)/.../documents/[documentId]/page.tsx`), passed
to `FieldNavForm`, which now owns the Direction radiogroup
(Payable/Receivable, locked to Payable for Purchase Order with the
"Purchase orders are always payable" footnote, roving-tabindex keyboard
pattern copied from `filter-sheet.tsx`'s `onGroupKeyDown`) and the
Save-review gate (`disabled={showDirection && !directionLocked &&
!categoryConfirmed}`).

Verified: `npx tsc --noEmit` — zero new errors (pre-existing unrelated
errors elsewhere in the repo, none in the two touched files). No
dedicated test file exists for `SplitPane`/`FieldNavForm`, so no test
run was needed for this step.

Committed as `98060a1` (`wip(autopilot): #297 build step 1`). Hand-off
updated: `milestone: build-step-1-done`, plan's step 1 marked done, next
step pointed at step 2 (Admin › Configuration › Fields Direction row).
Session ends here per the build phase brief (one plan step per session).

## Session 2 (build step 2)

Direction row in Admin › Configuration › Fields (spec §2, second half):

- `lib/configuration/field-table.ts`: added `direction: boolean` to
  `FieldTableRow`; `resolveFieldTable` injects one synthetic
  `key: "__direction"` row (position `-1`, always sorts first) for
  `invoice`/`receipt`/`purchase_order`, using `hasDirectionField`/
  `directionLockedFor` from `lib/doc-types.ts` (already added in step 1).
  The row is `editable: true, required: true, requiredLocked: true`
  (reuses the existing locked-checkbox rendering, no new branch needed in
  the editor for those two columns) and carries the PO "always payable"
  line inline in its own hint when locked.
- `components/admin/field-table-editor.tsx`: `EditableRow` picks up
  `direction`; Width cell and the Move up/down buttons render empty
  (`{!row.direction && ...}`) for the Direction row; `move()` refuses to
  swap a Direction row or swap into its slot (pinned first); the up-arrow
  button one row below it is `aria-disabled` too, so the keyboard affordance
  matches; the locked-fields footer counts real `requiredLocked` rows only
  (excludes the Direction row, which isn't part of that sentence); the save
  payload filters out Direction rows (nothing owner-editable to persist).
- `lib/configuration/field-table.test.ts`: updated the two tests whose
  `rows[0]`/position assertions assumed no leading row (`invoice`,
  `purchase_order`); added a new test asserting the row is absent for
  `bank_statement`, present and locked+footnoted for `purchase_order`.

Verified: `vitest run lib/configuration/field-table.test.ts` — 10/10
green. `tsc --noEmit` — no new errors in either touched file.

Committed as `8c4080d` (`wip(autopilot): #297 build step 2`). Hand-off
updated: `milestone: build-step-2-done`, plan's step 2 marked done, next
step pointed at step 3 (`reclassifyDocumentAction` guards + audit event,
spec §4). Session ends here per the build phase brief.

## Session 3 (build)

Continued from `milestone: build-step-2-done`. Built plan step 3 —
`reclassifyDocumentAction` guards + audit event (spec §4):

- New `lib/reclassify.ts::describeMoveIneligibility(workspaceId,
  documentId, docType)` — one function, three checks in spec §4's order:
  1) pending approval, via `getActiveWorkflowStageState` (reused from
     `models/review-tasks.ts`, the same source `fieldsReadOnly` reads);
  2) Posted/Paid, via `getDocumentPaymentStatuses` +
     `derivePaidState` (reused from `models/ledger-payments.ts` /
     `lib/payments/paid-state.ts`, the same combination `models/bills.ts`
     assembles for the row-level pill);
  3) PO matched invoices (purchase orders only), via
     `summarizePoConsumption` (reused from `models/po-matching.ts`).
  Each returns the exact caption spec §4 specifies.
- `reclassifyDocumentAction`
  (`app/(app)/workspaces/[workspaceId]/actions.ts:209`) now selects
  `docType` on the lookup, calls `describeMoveIneligibility` before the
  update when the document has a `docType`, and on success writes
  `recordDocumentAudit({ type: "document_reclassified", detail: {
  fromType: document.docType, toType: docType } })`.
- `models/audit-events.ts::auditEventLabel` gains an optional `detail`
  param, special-cased only for `document_reclassified` →
  `"Moved to ${to} (was ${from})"` via `DOC_TYPE_SPECS[...].label`,
  falling through to the static `EVENT_LABELS` map for every other type
  (confirmed by reading all existing entries first, per spec). Grepped
  every call site (`listWorkspaceAuditEvents`,
  `listWorkspaceAuditEventsForExport`, `listDocumentAuditEvents`,
  `listWorkspaceAuditEventTypes`) and passed `event.detail` through the
  three that map individual rows; `listDocumentAuditEvents`'s `select`
  needed `detail` added (it didn't select it before). Left
  `listWorkspaceAuditEventTypes` (the distinct-types dropdown, no
  per-row detail available) unchanged.

Verified: `tsc --noEmit` clean for `lib/reclassify.ts`,
`app/(app)/workspaces/[workspaceId]/actions.ts`, `models/audit-events.ts`
(the one remaining error, in `dictation-actions.ts`, is pre-existing and
unrelated). `vitest run models/audit-events.test.ts
lib/configuration/field-table.test.ts` — 12/12 green. §7's dedicated
tests (three guard tests, the audit-write test) belong to step 6 per the
build plan — not added in this step.

Committed as `06f3cd4` (`wip(autopilot): #297 build step 3`). Hand-off
updated: `milestone: build-step-2-done` retained, plan's step 3 marked
done, next step pointed at step 4 (Move menu item + dialog, spec §3).
Session ends here per the build phase brief.

## Session 4 (build, step 4)

Continued from `milestone: build-step-2-done`. Built plan step 4 — Move
menu item + dialog (spec §3):

- `components/queue/document-actions-menu.tsx`: added
  `MoveDocumentMenuItem` (mirrors `DocumentMenuDeleteItem`'s
  open-after-menu-closes shape; renders `null` until `doc.docType` is set;
  disabled + hinted on `cancelled` or `moveDisabledReason`) and
  `MoveDocumentDialog` (mirrors `DeleteDocumentDialog` on the shared
  `ConfirmDialog`). Radiogroup: `TYPED_MOVE_TARGETS`
  (invoice/receipt/purchase_order minus `currentType`) as flat radios,
  then a plain "Other types" disclosure button (not itself a radio)
  revealing `OTHER_MOVE_TARGETS` (delivery_note/contract/payslip/tax_form/
  other), captioned "Found in Search only." Arrow-key handler copied from
  split-pane's Direction radiogroup (B4 reuse: moving focus selects).
  Consequence line and confirm label track `target` state live;
  `confirmDisabled={!target}`, no default selection (§3's explicit model).
  Confirm calls `reclassifyDocumentAction`; success toasts "Moved to
  ‹queue›" with an "Open" action using `documentDestinationPath` +
  `withOrigin` (#244's `origin.ts`) so the arrival surface gets the
  `from=` param, then `onMoved()` → `ctx.onMutated?.("removed")`; failure
  sets an inline error (B1: selection preserved), reading the server's
  guard caption or "Couldn't move ‹filename›. Try again." for other
  errors — same pattern as `DeleteDocumentDialog`.
  `RegisteredDocument` gains `docType?: DocType` and `moveDisabledReason?:
  string | null` — left optional since the only existing call site
  (`split-pane.tsx`) doesn't populate them yet; that wiring is step 5's.
- `components/queue/detail-pane.tsx`: `MoveDocumentMenuItem` wired into
  `PaneMenu` right after `DocumentMenuTopItems` and before the
  separator/`menu` slot (spec: "between `DocumentMenuTopItems` and the
  queue's own items"); `moveOpen` state + `MoveDocumentDialog` rendered as
  a sibling of `<Popover>`, same shape as `deleteOpen`/`DeleteDocumentDialog`.

Verified: `tsc --noEmit` clean for both touched files. `vitest run
lib/configuration/field-table.test.ts models/audit-events.test.ts` — 12/12
green (no new tests added — dialog/menu-item tests are step 6's per the
plan).

Committed as `wip(autopilot): #297 build step 4`. Hand-off updated: plan's
step 4 marked done, next step pointed at step 5 (`RegisteredDocument`
wiring at `split-pane.tsx`'s `useRegisterDocumentActions` call — populate
`docType` and `moveDisabledReason` via `describeMoveIneligibility`;
confirm/extend the Library Type line per §5). No servers were started this
session, so nothing to stop. Session ends here per the build phase brief.

## Session 4 (build, step 5)

- `components/pipeline/document-detail/split-pane.tsx`: `header` type gains
  `moveDisabledReason?: string | null` (doc comment explaining B1: computed
  server-side, never re-derived client-side); `SplitPane`'s param
  destructuring and the `useRegisterDocumentActions` call now pass
  `docType: queueDocType, moveDisabledReason: moveDisabledReason ?? null`.
- `app/(app)/workspaces/[workspaceId]/documents/[documentId]/page.tsx`:
  new `const moveDisabledReason = await describeMoveIneligibility(workspaceId,
  documentId, docType)` right after the existing PO-consumption branch
  (reuses the already-resolved `docType` from `resolveDocType`), passed to
  `<SplitPane moveDisabledReason={moveDisabledReason} .../>`.
- Library route (`library/documents/[documentId]/page.tsx`) re-exports this
  same `DocumentDetailPage` — confirmed no separate wiring needed.
  Re-checked spec §5's Type-line clause: it is conditional on a
  Library-only `docType` branch existing in `split-pane.tsx`
  ("if one does, this is where the new Type line is added"). `grep -n
  docType split-pane.tsx` shows no such branch — only the generic
  `queueDocType`/Direction-row usage from steps 1–2 — so per spec the
  condition doesn't hold and the Type line is **not** added this session.

Verified: `tsc --noEmit` clean for all four touched files (pre-existing
unrelated errors elsewhere on the repo, e.g. `dictation-actions.ts` and a
handful of `models/*`/`worker/job-worker.ts` errors, untouched by this
work). `vitest run lib/configuration/field-table.test.ts
models/audit-events.test.ts` — 12/12 green (no new tests added — the
guard/audit tests and any `MoveDocumentDialog` test are step 6's per the
plan).

Committed as `wip(autopilot): #297 build step 5`. Hand-off updated: plan's
step 5 marked done, next step pointed at step 6 (spec §3/§7: verify the
toast's Open-action `origin.ts` wiring end to end — implemented in step 4,
confirm at build time — plus the full test sweep: three
`describeMoveIneligibility` guard tests, the `document_reclassified`
audit-write test, and any dialog/component tests the spec's test table
names). No servers were started this session, so nothing to stop. Session
ends here per the build phase brief.

## Session 6 (build)

Verified the Move dialog's toast/Open-action wiring against spec §3 —
already correct from step 4 (`document-actions-menu.tsx:292-298`:
`reclassifyDocumentAction` → `documentDestinationPath` →
`toast.success("Moved to ‹label›", { action: Open → withOrigin(...) })`),
no code change needed.

Added spec §7's test sweep:
- `lib/reclassify.test.ts` (new): 4 tests for `describeMoveIneligibility`
  — pending-approval guard, Posted guard, PO-matched-invoices guard, and
  the eligible→null happy path. All three model calls
  (`getActiveWorkflowStageState`, `getDocumentPaymentStatuses`,
  `summarizePoConsumption`) mocked.
- `models/audit-events.test.ts`: added a case for
  `auditEventLabel("document_reclassified", {...})` — "Moved to Invoice
  (was Receipt)" and the no-`fromType` variant.

Scoped out of this session: a `reclassifyDocumentAction`-level integration
test and a `MoveDocumentDialog` component test. `actions.ts` has no
existing test file or mocking harness for its `getCurrentUser`/
`requireMember`/`prisma.update` chain, and §7 itself offers the
alternative explicitly ("or the live click-through the capture round
already does") — G1–G3's capture round with keyboard probes covers the
dialog live, cheaper and spec-sanctioned.

`npx vitest run lib/reclassify.test.ts models/audit-events.test.ts
lib/configuration/field-table.test.ts` — 17/17 green. `tsc --noEmit`
clean for all touched/added files.

Committed as `wip(autopilot): #297 build step 6`. Hand-off updated: all 6
build-plan steps now done; next step is G1 (write the capture round
script). No servers were started this session. Session ends here per the
build phase brief.

## Session (build step G1)

Continuation session. Read the hand-off, all 6 build-plan steps already
done; started G1 (round script).

- Started `docubite-web-1`'s dev server (already up, on
  `localhost:7331` via docker port mapping — not `:3000`, no host
  `next dev` was running this session) and confirmed devdb/workspace
  reachable (`af91555d-…`).
- Started the impeccable live-server (`execFileSync` +
  `IMPECCABLE_SKILL_DIR` workaround, port 8400).
- Wrote `docs/wayfinder-reports/226/logs/scratch-297/round-297.mjs`
  (imports the shared `capture-round.mjs`) covering the spec's 6 named
  states (Direction row invoice/PO-locked/absent, Move dialog, Admin
  Fields ×4 sub-states, Library pane) at 1440+390, using existing seeded
  documents (no new seed needed) — invoice `784e8f38-…`, PO
  `9ccd3d76-…`, bank statement `8e17b30a-…`, an untyped doc for Library
  `d67d4e1c-…`.
- Built `residue.txt` from the app-wide default + `docs/agents/areas/
  admin.md`'s fenced residue regexes (this ticket touches both the
  detail-pane and Admin surfaces).
- Ran the round once as `r0`: exit 0, 21 states captured — proves the
  script executes end to end, per the G1 step's scope. Two states
  (`04-move-dialog`, `06-library-pane`, both widths) errored on a
  "More actions" button click timeout, and one keyboard probe
  (`01-direction-row-invoice@390`'s arrow-key test) timed out on
  `.focus()`. Per the phase brief, G1 stops at "prove it executes" — these
  are recorded on the hand-off as G2's fix-batch starting point, not
  fixed here.
- Stopped the impeccable live-server; left the docker dev stack running
  (it was already up before this session, not started by it).
- Hand-off updated with the G1 done note, r0 findings, and G2's exact next
  step. Committed as `wip(autopilot): #297 build step G1` (317bcaf).

Session ends here per the build phase brief — one plan step per session.

## Session (build, G2 — partial)

- Claimed the ticket, read the hand-off, resumed at G2.
- Discovered the previous session's G1 round was captured against the
  wrong server: `docubite-web-1` (:7331) runs `.env.production` with
  `NODE_ENV=production` and no `DEV_AUTH_BYPASS`, so every request
  silently redirected to `/login` — the "captured" screenshots were all
  login pages, and r0's own `keyboard.json` shows `ok: false` on almost
  every check, which the earlier hand-off had not read closely enough to
  catch. Confirmed with a standalone Playwright probe
  (`scratch-297/probe297.mjs`) against :7331 (login page, 0 buttons) vs.
  a real dev server.
- Started the correct server per the `docubite_live_ui_verification`
  memory pattern: `scratch-297/devserver.mjs` — `next({dev:true})` on
  :3000, `DEV_AUTH_BYPASS=true`, `DATABASE_URL` → devdb :55433 (already
  seeded with the round's workspace and all 4 documents). Repointed
  `round-297.mjs`'s `HOST` to :3000.
- Re-ran states individually (`shots-r0b`, `shots-r0c`): 01/02/03/05/06
  now show real content. State 04 (Move dialog) still errored — root
  cause was the seeded `INVOICE` document (winelands-WC-0093.pdf) is
  actually Paid, so Move is correctly disabled per §4, not a script bug.
  Fixed by adding a second, move-eligible document constant
  (`MOVABLE_INVOICE`, bergrivier-BL-7702.pdf) for state 04 only.
- Found and fixed a real B4 defect surfaced by the probe: Esc on the Move
  dialog returned focus to `<body>`, not the "More actions" trigger.
  Root cause in the shared `ConfirmDialog` (`components/ui/
  confirm-dialog.tsx`): its focus-management effect re-captures
  `document.activeElement` as "the opener to restore focus to" every time
  it re-runs (its deps include `children`, which gets a new identity on
  every interaction inside the dialog), and for dialogs that mount
  right after their popover menu unmounts (Move, Delete — by design,
  #259) the very first capture can land on `<body>` because the popover's
  own focus-restore hasn't necessarily resolved yet. Fixed at the root
  (not per-caller): `ConfirmDialog` now captures the opener once per open
  (a ref guard) and accepts an optional `restoreFocusTo` override;
  `detail-pane.tsx`'s `PaneMenu` now keeps a ref to its "More actions"
  button and passes it through `DeleteDocumentDialog`/`MoveDocumentDialog`
  as that override. `tsc --noEmit` clean on all four touched files.
- **Not done**: re-verify the Esc fix live (one probe run), re-run the
  full round as `r0d` against the corrected :3000 server, run `gate.mjs`
  and fix what it lists, write final counts, commit `milestone:
  build-step-G2-done`. Left as the immediate next step on the hand-off,
  which is otherwise current (every finding and fix above is recorded
  there in full, including exact commands to resume).
- Stopped the dev server and impeccable live-server started this session
  before ending.

Session ends here at the context hand-off line, mid-G2 (a fix batch is
explicitly allowed two sessions per the phase brief).

## Session (build, G2 — continued)

- Claimed the ticket, read the hand-off (G2 in progress: Esc-focus root
  cause found, fix landed but not yet re-verified).
- Started `devserver.mjs` (dev server, :3000, DEV_AUTH_BYPASS) and
  `start-live.mjs` (impeccable live-server, :8400).
- Ran `probe297.mjs`: confirmed the Esc-focus fix from the prior session
  works live — active element after Esc is now the "More actions" button,
  not `BODY`.
- Ran the full round as `r0d` (25 states, both widths): 0 errored states,
  23 real (non-residue) findings, keyboard probes 25 pass / 2 fail (both
  the Library "no currentType" typedCount assertion).
- Triaged all 23 r0d findings by reading the flagged elements directly
  (grep + a scratch Playwright probe, not a reader agent):
  - 19 were pre-existing app shell chrome untouched by this ticket's diff:
    a `hidden lg:flex` responsive-utility cramped-padding false positive
    on the pane's layout-toggle radiogroup, the pane-header `<h2>` title
    truncate overflow, the workspace-switcher name truncate overflow, and
    the nav-rail "waiting" count badge's 10.5px text — all confirmed via
    `git log`/grep to predate this ticket and appear on every document
    detail pane regardless of the Direction row/Move feature. Added 4
    regex lines to `residue.txt`.
  - 1 was a real, ticket-introduced bug: the "Found in Search only."
    caption (`document-actions-menu.tsx`) had 0 vertical padding on
    12px text — added `py-1`.
  - The remaining 2 keyboard-probe failures traced to a real logic bug:
    `MoveDocumentDialog`'s `currentType` was always the document's
    *resolved* type (`resolveDocType`'s legacy-template fallback), so a
    Library document with a null raw `docType` that only resolves to
    e.g. "receipt" wrongly excluded "receipt" from the Move target list —
    spec §5 requires `currentType` unset for Library (no real "current
    queue"). Fixed by threading a new `moveCurrentType` (computed from
    the *raw* `document.docType`, only set if `isDocType(...)`) from
    `documents/[documentId]/page.tsx` through `SplitPane` into a new
    `currentType` field on `RegisteredDocument`
    (`document-actions-menu.tsx`), read by `detail-pane.tsx`'s
    `MoveDocumentDialog` instead of `doc.docType`. Confirmed via the
    code comment on `TYPED_MOVE_TARGETS` that the deliberate 3-target
    list (bank statement excluded) was correct as-is — the bug was the
    wrong exclusion, not a missing target.
  - `tsc --noEmit` clean for all touched files.
- Re-ran the full round as `r1`: real findings down from 23 to 4, all
  keyboard probes now 27/27 pass. Gate (`gate.mjs shots-r1
  --residue-file residue.txt`): **real findings: 4** (1440: 2, 390: 2),
  all the same `cramped-padding` "0px vertical padding (need ≥4.0px for
  12px text)" finding, only on `04-move-dialog-target-chosen` and
  `04-move-dialog-other-expanded` at both widths — a second 12px-text
  element with no vertical padding that the `py-1` fix didn't cover,
  not yet located (candidates and a probe technique are on the
  hand-off).
- Hit the context hand-off line; wrote the hand-off (kept all prior
  `milestone:` lines, no new one added — G2 is still not done), committed
  (`fefd791`), posted `Autopilot: continue —` on the ticket, unassigned.
- Stopped the dev server and impeccable live-server started this session
  before ending (best-effort — `pkill`/`kill` with a subshell were
  blocked by the sandbox; the driver's process-group kill at session end
  covers what a direct kill could not).

Session ends here at the context hand-off line, mid-G2 (a fix batch is
explicitly allowed two sessions per the phase brief; this is that second
session, still not closed — one more small CSS fix and a clean re-gate
remain).
