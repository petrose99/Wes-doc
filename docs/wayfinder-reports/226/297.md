# Autopilot report — #297 Build the Direction row and Move to another queue: retire the Details-tab type chooser on typed queues

- Map: #226 Vic.ai-style operator app
- Type: Build (execution) · Started: (continuation, prior sessions unrecorded here) · Finished: 2026-09-20 (this session)
- Outcome: resolved

## What I did (this session — close phase, session 4 of 4)

1. Claimed #297, read the hand-off (`docs/wayfinder-reports/226/297.handoff.md`) — build and measure
   phases already done (`milestone: build-done`, `milestone: measured`), scores on the hand-off.
2. Triaged the three readers' raw findings (`readers-r2/{critique,evaluate,include}.md`) into
   `docs/wayfinder-reports/226/logs/scratch-297/close.md`: 3 evaluate P1s = residue (already in
   `residue.txt`, confirmed pre-existing shell chrome outside this ticket's diff), include's F2/F8 =
   tooling/verified-by-source (sonner's built-in live region; radio rows measured at ~32px tall),
   F3 = documented spec-level UX note not a defect, F1 (consequence line has no confirmed
   live-region announcement, Serious) = **real**, fixed this session.
3. Fixed F1: added `aria-live="polite"` to `ConfirmDialog`'s description paragraph
   (`components/ui/confirm-dialog.tsx`) — verified live with a new Playwright probe
   (`probe_arialive.mjs`): the Move dialog's consequence line now carries `aria-live="polite"` and
   its text updates correctly on target selection.
4. While fixing F1 and re-running `tsc`/`eslint` for the close-phase checks, found and fixed a real
   defect the readers/gate/tsc could not see: `ConfirmDialog`'s `restoreFocusTo` prop was typed
   `HTMLElement | null` and its two callers (`detail-pane.tsx`) passed `triggerRef.current` directly
   as a prop, which `eslint`'s `react-hooks/refs` rule flags as **2 errors**
   ("Cannot access ref value during render") — a real regression from this ticket's own earlier
   Esc-focus fix (build phase). Changed the prop to a `RefObject<HTMLElement | null>`, dereferenced
   inside `ConfirmDialog`'s existing effect instead of at the caller's render time; updated both
   `detail-pane.tsx` call sites and `document-actions-menu.tsx`'s prop types to match.
5. Ran a confirm round: `shots-c1` (full 25-state capture) vs baseline `shots-r2` —
   `gate.mjs` **CLEAN**: real findings 0, new-since-baseline 0, probes 27/27 pass, 0 errored states,
   0 page errors. Did not re-run the three heavy readers on an unchanged-visually capture set (the
   fixes are DOM-attribute/type-level, invisible to the capture pipeline's own evidence sources per
   include.md's own note) — confirmed F1 instead with the targeted live probe.
6. Ran the close-phase checks once each, dev server stopped first: `vitest run` full suite
   (2797/2802 pass, 5 pre-existing failures in unrelated files — `lib/analytics.test.ts`,
   `lib/marketing-layout.test.ts`, both last touched by an unrelated commit `a0450ec`); `tsc --noEmit`
   (zero errors in this ticket's files, 20 pre-existing errors elsewhere unchanged from build phase);
   `eslint` on the ticket's changed files (0 errors, down from 2, after the ref-prop fix; 3
   pre-existing warnings); `next build` (compiles successfully, its own type-check step hits the
   same pre-existing `dictation-actions.ts` error `tsc` already reported).
7. Refreshed the area primer `docs/agents/areas/detail-pane.md`: documented the Move
   dialog/`currentType`/`MoveDocumentDialog` additions, the `restoreFocusTo` RefObject contract, and
   the confirmed-residue detector entries for this surface.
8. Wrote the lesson to `.claude/wayfinder-autopilot/lessons.md` (#297 entry): a `.current` read
   passed as a *prop* is still "during render" to `react-hooks/refs`, even though the callee only
   dereferences it inside an effect — a category `tsc`/the gate/the readers all miss.
9. Posted the resolution comment on #297 and closed it
   (https://github.com/petrose99/Wes-doc/issues/297#issuecomment-5747156830).
10. Appended a "Decisions so far" line to map #226 for #297.
11. Stopped the dev server and impeccable live-server (both were running for this session's
    confirm round and probe).

## Questions I answered on the owner's behalf

None — this session was fix/verify/close work on an already-grilled, already-spec'd ticket; no
open design questions remained.

## Decisions recorded

#297 closes shipped: Direction radiogroup (Payable/Receivable) replaces the retired Details-tab
type chooser; PO Direction is locked with a visible reason; pane ⋯ gains "Move to another queue"
with a live, now screen-reader-announced consequence line; Library's read-only Type line added per
spec §5.

## Tickets created / changed

None created this session. No fog graduated — #297's own fog note ("bulk Move from the bulk bar,
mis-typed-batch signal — only once #297's Move audit event shows usage") stands unchanged; it
depends on operational data accumulating after this ship, not on anything decided this session.

## Scores and counts (rendered surfaces only)

scores: predicted-critique=32 first-critique=32 close-critique=32 first-evaluate=88 close-evaluate=88

Note on scale: the pre-flight (Part A/D) and this map's other tickets score critique on a /40
rubric (8-10 heuristics). The measure-phase reader in an earlier session scored critique on a
narrower 5-heuristic /5-each rubric instead (H1-H5, sum 21/25, "rounds to 4/5") — a rubric mismatch
this close session did not re-derive (no re-measurement performed per the close brief's "do not
re-measure first"; the confirm round used only the gate + a targeted live probe, not a full
reader re-run). The `scores:` line above reports the pre-flight's own /40 figures
(critique gate: predicted ≥32, met — Part D's independent critic scored the spec 32/40) and the
measure session's evaluate health (88/100) unchanged by this session's fixes (no visual/detector
regression, confirmed by the clean confirm-round gate). Gate counts: real findings 0/0 (1440/390)
on both `shots-r2` (measure) and `shots-c1` (this session's confirm round); probes 27/27 pass both
widths; `include` verdict Warning → the one Serious item (F1) it flagged is now fixed and verified
live, F2/F8 dispositioned as tooling/verified-by-source, not a defect.

## First-pass findings → lessons (rendered surfaces only)

| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| F1: consequence line has no confirmed live-region announcement (include, Serious) | Pre-flight Part B5 (focus/keys/failure path) named focus-trap/Esc/return but not live-region announcement for in-place text updates | **unchecked** — B5's table has no live-region column, so a dynamically-updating description text had no check to catch it | Fixed (`aria-live="polite"`); recorded as a gap to add to B5's checklist scope generically (not written to a lessons file this session — single occurrence, not yet a pattern) |
| `react-hooks/refs` error via a prop-passed `.current` (not a reader finding — an `eslint` finding surfaced at close) | No existing contract named this specific "prop boundary" variant; #257's lesson (line 17) only covers a ref written inside a render-created closure | **new class** (a variant the existing lesson didn't cover) | Fixed; new lesson added to `.claude/wayfinder-autopilot/lessons.md` (#297 entry) with a grep-based check |
| 3 evaluate P1s (pane-header truncation, Library span overflow, sub-11px badge) | Pre-flight/primer already covers detector residue for this surface | **none needed** — correctly pre-existing and already in `residue.txt`; not a build-phase miss | Verified via residue.txt cross-check, no action |

## Needs the owner

Nothing deferred. No removal, no credentials, no open decision.
