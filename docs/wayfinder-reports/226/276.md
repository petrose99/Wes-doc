# Autopilot report — #276 Decide whether Bill Pay rows that cannot be batched are selectable at all (Scheduled · Needs bank details) or selectable-then-left-out

- Map: #226 Map: Make the operator app work like Vic.ai (grammar, not skin)
- Type: wayfinder:grilling · Started: 2026-09-17 (session start) · Finished: 2026-09-17
- Outcome: resolved

## What I did
1. Claimed #276; loaded the map, the ticket, CONTEXT.md (Bill Pay, Not eligible, Payer account, Partial outcome), the tour findings §6 (lines 69–71, 82–87, 157–169), the 34/40 critique and the incumbent code (`components/payments/bill-pay-queue.tsx`, `create-batch-dialog.tsx`, `lib/payments/eligibility.ts`, `models/bill-pay.ts`, `components/queue/approval-invoice-queue.tsx`).
2. Loaded `grilling`, `domain-modeling`, `intent journey`, `impeccable shape`. `impeccable context` was blocked by the permission sandbox; read PRODUCT.md/context directly (no DESIGN.md in the repo).
3. Two grilling rounds (5 questions), each answered with its `➡️` recommendation.
4. Resolution comment: https://github.com/petrose99/Wes-doc/issues/276#issuecomment-5707912506 · closed #276.
5. Created [Fold in #276 on Bill Pay (#294)](https://github.com/petrose99/Wes-doc/issues/294) (`wayfinder:task`), attached as a sub-issue of #226, linked from #276.
6. Appended one line to the map's Decisions so far.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| Q1 | Selection model: keep selectable-then-left-out · per-row predicate · predicate for Scheduled only | Keep selectable-then-left-out | Vic wins ties: tour §6 lines 69–71 ("Eligible (N of M)" strip, count on the button). Three bulk actions have three eligibility sets, so a checkbox predicate = every row. Approvals has no checkbox column (not a precedent). H4 consistency. |
| Q2 | Where "1 of 7" is learned | On the bar at selection: hint "k of n selected can't be batched — each row says why" when eligible < selected; dialog strip stays | H7 finding in the 34/40 critique; Intent "make intent visible" / feedback loops; H5 confirm stays. |
| Q3 | Bulk Set Pay From on Scheduled rows | Skip them with the `(n)` suffix, as Mark as paid does | Pane already hides it (`:157`); bulk writes under a batch that fixed the account — silent change (Intent "transparent consequences"); smallest reversible option. |
| Q4 | Per-row affordance for un-batchable rows | Subtitle only, no muted checkbox | Row *is* selectable for two of three actions; muted checkbox would misstate it (Intent cat. 1 visual misdirection); craft-floor: no decorative state. |
| Q5 | Execution ticket or fold-in | Small `wayfinder:task` #294 carries both fold-ins | Two-line edits, no schema/component; #277/#278 are grilling tickets, so they could not carry execution. |

## Decisions recorded
Keep every Bill Pay row selectable; each bulk action reports what it can do with the selection on the bar (count + hint) and in the confirm (Vic's N-of-M strip). No per-row `selectable` predicate. Two fold-ins → #294.

## Tickets created / changed
- #294 created (`wayfinder:task`), sub-issue of #226, unblocked. No fog graduated or added; no removals.

## Scores and counts (rendered surfaces only)
No score; decision ticket, surface unchanged. The 34/40 critique of #251's Bill Pay was used as evidence only.

## First-pass findings → lessons (rendered surfaces only)
Not applicable (no build).

## Needs the owner
Nothing. `impeccable context` needs a permission rule for `.claude/skills/impeccable/scripts/impeccable` in the autopilot sandbox so future sessions can run it instead of reading context by hand.
