# #253 — project lessons pending merge into `.claude/wayfinder-autopilot/lessons.md`

The autopilot session could not write `.claude/wayfinder-autopilot/lessons.md` (the driver's permission mode treats `.claude/` as sensitive). Owner: append these five lines to that file.

- (#253) A client component must not import from `models/*` that reach `lib/audit` (→ `next/headers`): the route 500s with a Pages-Router error. Shared strings/constants go under `lib/`.
- (#253) The box killed a dev server started outside `dev.mjs` (no heap cap, 5 GB RSS) mid-capture. → Always `node .impeccable/live/dev.mjs start`, warm routes with `probe252.mjs`, check `dev.mjs status` first; a `*:3000` listener at 5 GB is an orphan — kill it.
- (#253) `prisma migrate deploy` can find columns already present (a nested agent used `db push`) and mark the migration failed; `scripts/dev/db253.ts fix|resolve` finishes it idempotently. `db253.ts seed` adds two members and two flows to the dev workspace; `role member|owner` flips the dev user for read-only captures; `set-active-default|set-inactive-default|clear-default` set DB states.
- (#253) Admin capture recipe: `.impeccable/live/admin253.mjs` (copy to `/tmp/scratch229`; `when=default|inactive|readonly` for DB states). `models/review-tasks.test.ts` stubs Prisma per model — a new model read in `createReviewTask` needs a stub in its `beforeEach`.
- (#253) `Agent` transcripts cannot be resumed with SendMessage in the driven session ("No transcript found"); a critic that misreads a probe needs a fresh agent, or the reconciliation is written into Part G with the log lines.
