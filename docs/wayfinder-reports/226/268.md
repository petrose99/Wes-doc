# Autopilot report — #268 Build the Origin link: one strip and a from= URL for every cross-surface hop

- Map: #226 Post-login UX
- Type: wayfinder:task · Started: 2026-09-16 (session 2, SPEC phase) · Finished: 2026-09-16
- Outcome: partial — spec phase complete (`milestone: spec-done`); build, measure and close are later sessions

## What I did
1. Read the hand-off from session 1 (inspection facts, seven drafted decisions); claimed #268 (assignee petrose99 — the assignment had not persisted).
2. Loaded `intent` (specify → fortify → articulate → include) and `impeccable shape`; read `craft-floor.md`, both lessons files, the queue-shell primer, the ticket and its three comments.
3. Wrote `docs/wayfinder-reports/226/logs/scratch-268/spec.md`: context, Origin model + `readOrigin` validation, screens at 1440/390, Search variant, standalone page, generalised missing-row notice (3 cases), hop table (6 hops, all with `from=`), copy matrix, 16-state inventory, accessibility, tests, files.
4. Filled `preflight.md` Parts A–E (H1 + H5 targeted at 4; six contracts; coverage; evaluate prediction; walkthroughs; anti-pattern sweep).
5. Ran the Part D spec critic (fresh `Agent`, sonnet, text only). It found one P1: hops landing on a deleted document reached the legacy route's `notFound()` (raw 404), and the hand-off's claim that the legacy route forwards `from` was wrong (it forwards only `stage/page/bb` — verified by reading the route). Reconciled: spec §3.0 now contracts a `gone=` redirect to the origin (notice "That document was deleted.") and a shell `not-found.tsx` when there is no origin; PaneFrame back-link removal decided as an affordance swap; identifier suffix never truncates on the phone; label-map coverage and empty-queue focus tests added.
6. Gate passed after reconciliation: predicted critique 32/40 (H1, H5 = 4, none under 3), evaluate sum 6, P0/P1 0, Clean, health ≈ 88.
7. Hand-off rewritten as a build checklist with `milestone: spec-done`; committed `wip(autopilot): #268 spec`.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Where is the origin described — client fetch or server? | Server, `describeOrigin` on each host page | smallest reversible option; no client state (#244 §13) |
| 2 | Standalone page: keep PaneFrame's icon back link beside the strip? | No — the strip replaces it | B4 one control per act; same feature, visible name; not a removal under #237 |
| 3 | Hop lands on a deleted document — what renders? | Redirect to origin with `gone=` notice; shell not-found page without origin | critic P1 (link → raw error page class); H9 |
| 4 | What truncates at 390 on a long vendor name? | The vendor segment; label and identifier never | H6 (critic), #264 wrap-rule lesson |
| 5 | Unknown path in `from`? | Strip hidden, treated as invalid | H5 validation; no "previous page" fallback (B3) |
| 6 | Search hop when the row's type is unknown? | `documents/<id>` legacy route resolves it and forwards `from` | #245 comment; hop table H-d |

## Decisions recorded
No resolution comment yet (ticket stays open through build/measure/close). Spec decisions live in `scratch-268/spec.md`; the affordance swap (§2.4) is flagged for the owner at close.

## Tickets created / changed
None. #268 claimed; `Autopilot: continue —` comment posted with the phase pointer.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=32 first-critique=n/a close-critique=n/a first-evaluate=n/a close-evaluate=n/a
Surface not built yet; predicted evaluate health ≈ 88, P1 0, Clean.

## First-pass findings → lessons (rendered surfaces only)
Not applicable this phase. One pre-build lesson for the project file (the sandbox refused the edit to `.claude/wayfinder-autopilot/lessons.md`; the close session should append it):
`- (#268, spec phase) A hand-off carried "the legacy documents/[id] route forwards from" as a fact; the route forwards only stage/page/bb and calls notFound() on a missing id — the Part D critic caught it as a P1 (link → raw 404). → Any hand-off claim about an existing route or guard that a Part B row depends on is re-read from the file before the row is ticked. · check: grep -n "notFound\|redirect(" <route> on every route a new link targets; a notFound() with no shell not-found.tsx beside it is an open H9 row.`

## Needs the owner
- At close: confirm the PaneFrame full-mode ArrowLeft link → OriginStrip swap (same feature, one control).
