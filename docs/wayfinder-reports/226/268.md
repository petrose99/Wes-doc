# Autopilot report — #268 Build the Origin link

- Map: #226 Post-login UX
- Type: wayfinder:task (execution) · Started: 2026-09-16 (sessions 3–6, BUILD phase; session 6 22:20–22:55 UTC) · Finished: 2026-09-16T22:55Z
- Outcome: partial — BUILD phase complete (`milestone: build-done`); MEASURE and CLOSE phases follow in fresh sessions

## What I did
1. Read the hand-off, spec, pre-flight A–C, queue-shell primer, both lessons files, craft-floor; loaded `impeccable layout` (launcher can't run unattended — read project context directly) and `intent specify`.
2. Split the server half of `lib/navigation/origin.ts` into `lib/navigation/origin-server.ts` — the previous session had put `prisma` into a module that client components import (`withOrigin`), which would have broken the client bundle.
3. Added `queueArrival()` (origin + missing-row notice, `gone=` handling) and wired it through the seven queue pages and six `*-queue` components into `QueueScreen` (`scratch-268/wire.mjs`).
4. Widened `QueueScreen`'s missing-notice gate so `gone=` works without a selected row; the notice clears when another row is opened.
5. Built H-a/H-b: **Open on {queue}** button and ⋯ item on the Exceptions pane, `from=` = current URL. Added `library: "Archive"` to the label map (spec §2.4 claimed `documentDestinationPath` only returns typed queues; it also returns `library/documents/<id>`).
6. Hit the 150K hand-off line mid-H-c; wrote the hand-off, committed.

### Session 6 (build gate)
1. Servers + seed up; wrote `.impeccable/live/seed268.ts` (bigcapital IntegrationConnection, approved invoices, an exception on an invoice) and enabled Finance in the dev `.env` (`SECRETS_ENCRYPTION_KEY`, `BIGCAPITAL_ENABLED=true`) — without it `/finance` is "The ledger isn't connected" and H-c cannot be seen.
2. Wrote `scratch-268/round-268.mjs` on `capture-round.mjs` — 15 states × 1440/390, 20 keyboard probes for every Part B focus/keyboard line — and ran it as r0.
3. Fixed what r0 showed: (a) rows filtered on the server (Invoices aging, Exceptions status, PO consumed) were reported as "moved" — added `unfilteredRowIds` to `queueArrival` + `describeFilteredRow` so §2.5 case 1 wording and the query-less Show it apply; (b) an Exceptions origin (`/exceptions/<checkId>`) had no row segment because the id is a check id — `describeOrigin` resolves the check to its document; (c) the Finance `Open on Invoices` link wrapped — `whitespace-nowrap`. Re-ran r0: every probe passes; remaining detector findings are shell/adjacent residue (listed on the hand-off).
4. Affected tests (51) pass; `tsc --noEmit` clean; servers stopped; hand-off rewritten with r0 counts and `milestone: build-done`.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Exceptions row id is a check id, not a document id — what does the missing notice say? | "That exception is no longer open.", no Show it | Spec §2.5 case 3 shape; smallest reversible option |
| 2 | Origin label for a `library/documents/<id>` destination? | "Archive" | B3: the sidebar's own string (#245) |
| 3 | A server-filtered row (aging/status/consumed) — "moved" or "filtered" notice? | Filtered (case 1 wording, Show it drops the query) | Spec §2.5 case 1 + B3 one-term-per-concept: the row is still on this queue |
| 4 | Phone: the pane sheet covers the strip after a hop — redesign now? | No; record for the measure readers | Smallest reversible option; spec §2.2 silent on the sheet; a change is a decision → ticket if the reader scores it |
| 5 | Finance needs a live connection to render H-c — enable in dev env? | Yes, dev-only `.env` (git-ignored) | Build gate requires the state at both widths; no code change |

## Decisions recorded
None closed; ticket stays open with `Autopilot: continue —`.

## Tickets created / changed
None.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=32 first-critique=n/a close-critique=n/a first-evaluate=n/a close-evaluate=n/a
r0 (build gate, not a scored round): non-residue detector findings 1440 = 65 / 390 = 71 across the 31 captures, all pre-existing shell or adjacent-surface residue (rail avatar palette ×2 per state, pane `clipped-overflow-container`, Finance page's own nested-cards/contrast, PDF-fallback line-length); zero on the strip, notices, Open-on controls, not-found. 20/20 keyboard probes pass at both widths. `tsc` clean, 51 tests pass.

## First-pass findings → lessons (rendered surfaces only)
n/a — no measurement yet. Candidate for the project lessons file at close: a `lib/` module imported by client components must not import `prisma`; split server lookups into `*-server.ts` (check: `grep -l "@/lib/db" $(grep -rl "from \"@/lib/<module>\"" components)`).

## Needs the owner
Nothing yet. PaneFrame's full-mode back-link removal (spec §2.4) is an affordance swap, not a #237 removal — flagged for the close report.
