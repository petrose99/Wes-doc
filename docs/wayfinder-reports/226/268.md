# Autopilot report — #268 Build the Origin link: one strip and a from= URL for every cross-surface hop

- Map: [Map: Vic.ai-style operator app parity (#226)](https://github.com/petrose99/Wes-doc/issues/226)
- Type: wayfinder:task · Started: 2026-09-16 (session 1, prior) · Finished: 2026-09-16T23:15Z (this session, close phase)
- Outcome: resolved

## What I did

This was the close phase (4 of 4) of a multi-session continuation; sessions 1–6 wrote the spec, built the surface, and ran the build-gate round; session 7 measured. This session:

1. Read the hand-off file — bar already met on the first measurement (critique 32/40 no heuristic under 3, evaluate health 91/P0 0/P1 0/Clean, keyboard PASS both widths) — no fix batch or second capture round was needed.
2. Ran the close-phase checks once, dev server stopped: `npx vitest run` (279 files, 2727 tests, all pass), `npx tsc --noEmit` (clean), `npx eslint .` (76 pre-existing warnings/errors, all in files this ticket never touched — `lib/automation/rules.ts`, `lib/budget/resolve.ts`, `lib/gates/*`, `models/*`, `prisma/seed.ts`, etc. — none introduced), `npx next build` (succeeds).
3. Refreshed `docs/agents/areas/queue-shell.md` with a new "Origin / back navigation" section describing `lib/navigation/origin.ts`/`origin-server.ts`, the strip, the `queueArrival` wiring, the Exceptions check→document resolution, the phone two-step sheet pattern, and the two carried non-blocking gaps.
4. Posted the [resolution comment](https://github.com/petrose99/Wes-doc/issues/268#issuecomment-5705938299) and closed the ticket.
5. Appended a Decisions-so-far line for #268 on the map.
6. **Incident, caught and fixed in this session:** a `gh issue edit 226 --body-file -` piped from a heredoc with a quoted terminator (`<<'BODYEOF'`) did not expand the `$(gh issue view ...)` inside it, so the map issue's body was briefly overwritten with the literal placeholder string. Caught immediately by re-reading the issue; restored the full body (recovered verbatim from this session's own earlier `gh issue view` output, since it predates any tracked file) plus the new #268 line, verified by re-reading the issue. No data was actually lost — the full body was already in this session's context — but recorded as a project lesson (`.claude/wayfinder-autopilot/lessons.md`) so a future session doesn't repeat the pattern.

## Questions I answered on the owner's behalf

None — no open question remained by the close phase. The one substantive judgment call (whether the two reader-flagged gaps warranted a fix batch) was already made in the measure session and recorded on the hand-off; this session only verified the bar still held and did not need to revisit it.

## Decisions recorded

Origin link shipped and closed: `origin.ts`/`origin-server.ts` + the Origin strip on all seven queues and doc-detail pages, `not-found.tsx`, and the `gone=` redirect. critique 32/40 (no heuristic under 3), evaluate health 91 (P0 0, P1 0, Clean), keyboard PASS at both widths — bar met on the first pass, no fix batch required.

## Tickets created / changed

None created. No fog graduated — #244's execution scope (this ticket) is now fully closed; the two carried gaps (label-density mismatch on the strip, Finance "Open on Invoices" visual weight) are explicitly attributed to Finance's pre-existing #249 surface, not new tickets, per the reader detail in the hand-off.

## Scores and counts (rendered surfaces only)

scores: predicted-critique=32 first-critique=32 close-critique=32 first-evaluate=91 close-evaluate=91

In-page detector: zero findings on the strip, notices, Open-on links, or not-found page at both 1440 and 390, unchanged from the build-gate round (r0) through the measurement round (r1) — no fix batch, so no further round was run at close. Non-residue counts held steady at 65@1440 / 71@390, all pre-existing shell/#249 residue (workspace-avatar palette, detail-pane `clipped-overflow-container` primer, rail `text-overflow`, Finance's own `nested-cards`/`low-contrast`). `include` (keyboard): PASS at both widths, verified live in session 7 (strip/hop/Show-it controls all reachable with correct accessible names, Enter activates, arrival focus sane, no traps). No pre-build step was weak — the spec and pre-flight predicted the eventual score almost exactly (predicted critique 32/40 matched; predicted evaluate sum 6/health≈88 vs. actual sum 3/health 91, slightly better than predicted, different issue mix).

## First-pass findings → lessons (rendered surfaces only)

| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| H4 (critique)/H8 (evaluate), non-blocking: label-density mismatch between bare "Back to Invoices" and row-qualified strip variants | Spec §2.x named the label grammar per queue but not a cross-queue density-consistency contract | unchecked | Attributed to Finance's pre-existing #249 surface, not fixed here; recorded on the primer as a known gap, no new ticket (not a P1, no real decision pending) |
| H8 (evaluate), non-blocking: Finance's "Open on Invoices" link lacks distinct visual weight inside an already-dense row | Pre-existing #249 `nested-cards`/`low-contrast` residue on Finance, only touched by adding one link | new class (residue of an out-of-scope surface, not this ticket's build) | Left as #249 follow-up per hand-off; no fix in this ticket, no new ticket filed (pure execution/visual, no decision) |
| H3/H9 (critique), non-blocking: phone detail sheet covers the strip, two-step way back below 390 | Spec §2.2 did not address the sheet interaction with the strip | unchecked, but all three readers independently judged it acceptable (nested-context pattern, not asymmetric friction) | No action — accepted design, documented on the primer for future reference |

No P0/P1 findings occurred on either the build-gate or measurement round, so the close phase required no fix batch and no confirming capture round.

## Needs the owner

None. The two non-blocking gaps above are explicitly scoped to Finance's existing #249 surface (already tracked there), not new removals or decisions requiring sign-off.
