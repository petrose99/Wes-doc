# Autopilot report — #268 Build the Origin link: one strip and a from= URL for every cross-surface hop

- Map: #226 Map: Vic.ai-grade operator app — one shell, every surface
- Type: wayfinder:task · Started: 2026-09-16T18:10Z · Finished: 2026-09-16T18:40Z
- Outcome: partial (spec phase, handed off at the context line; no `milestone:` reached)

## What I did
1. Claimed #268; created the scratch folder `docs/wayfinder-reports/226/logs/scratch-268/`.
2. Loaded the map body, both lessons files, the queue-shell primer, the pre-flight template and #244's resolution (14 decisions + Action Summary).
3. Ran the pre-build skills: `intent` → `specify`, `fortify`, `articulate`, `include`; `impeccable shape` (launcher cannot run unattended — read PRODUCT.md directly); read `craft-floor.md`.
4. Inspected the incumbent hosts and seams (origin.ts, useOriginHere, standalone page Back, QueueScreen slots/notice/syncUrl/fullHref, Exceptions pane actions, Finance failed row, Search ResultRow `/pipeline` link, rail label sources) — all facts recorded on the hand-off so the next session writes the spec without re-reading code.
5. Context crossed 150K mid-inspection (the map body + two lessons files + four skill bodies are ~60K before any work); hook said HAND-OFF NOW. Wrote `268.handoff.md` with the facts and seven drafted decisions, committed, posted `Autopilot: continue —`.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Where does the strip get the row's name from a bare URL? | Server-side `describeOrigin` lookup by the trailing id on each host page | Smallest reversible; #244 §8 copy needs the name; no client fetch |
| 2 | Standalone page: strip *and* PaneFrame's Back arrow? | Strip always renders (origin or `documentDestinationPath`); full-mode arrow not rendered | Impeccable one-hierarchy / H8; #244 §10 — flagged to owner below |
| 3 | Search rows without a docType | Link `${base}/documents/${id}` with `from=`; the legacy route resolves type and forwards `from` (#245 comment) | Existing seam; removes the last `/pipeline` document hop |
| 4 | Exceptions/Finance link label when the doc is not an invoice | `Open on ‹queue›` from the doc type | B3 one term per concept (the rail label) |

## Decisions recorded
None on the ticket yet (spec not written). Seven drafted decisions are on the hand-off file.

## Tickets created / changed
None. #268 claimed and left open with an `Autopilot: continue —` comment.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=0 first-critique=0 close-critique=0 first-evaluate=0 close-evaluate=0
No surface built or measured this session (spec phase, unfinished).

## First-pass findings → lessons (rendered surfaces only)
None yet. Process observation for the lessons file at close: the spec phase's first-turn context (map body 44KB + two lessons files + four Intent skill bodies + impeccable) is ~90K before a single code read; reading skill bodies through the Skill tool is unavoidable, but the map body should be read by section (`## Notes` + the relevant Decisions lines), not whole.

## Needs the owner
- FYI, not blocking: decision 3 replaces the standalone page's ArrowLeft "Back to queue" icon with the Origin strip (same feature, one control). If the owner wants the arrow kept beside the strip, say so on the ticket before the build phase.
