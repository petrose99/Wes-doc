# Autopilot report — #268 Build the Origin link

- Map: #226 Post-login UX
- Type: wayfinder:task (execution) · Started: 2026-09-16 (session 4, BUILD phase) · Finished: 2026-09-16
- Outcome: partial (hand-off at the context line; build not yet at `build-done`)

## What I did
1. Read the hand-off, spec, pre-flight A–C, queue-shell primer, both lessons files, craft-floor; loaded `impeccable layout` (launcher can't run unattended — read project context directly) and `intent specify`.
2. Split the server half of `lib/navigation/origin.ts` into `lib/navigation/origin-server.ts` — the previous session had put `prisma` into a module that client components import (`withOrigin`), which would have broken the client bundle.
3. Added `queueArrival()` (origin + missing-row notice, `gone=` handling) and wired it through the seven queue pages and six `*-queue` components into `QueueScreen` (`scratch-268/wire.mjs`).
4. Widened `QueueScreen`'s missing-notice gate so `gone=` works without a selected row; the notice clears when another row is opened.
5. Built H-a/H-b: **Open on {queue}** button and ⋯ item on the Exceptions pane, `from=` = current URL. Added `library: "Archive"` to the label map (spec §2.4 claimed `documentDestinationPath` only returns typed queues; it also returns `library/documents/<id>`).
6. Hit the 150K hand-off line mid-H-c; wrote the hand-off, committed.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Exceptions row id is a check id, not a document id — what does the missing notice say? | "That exception is no longer open.", no Show it | Spec §2.5 case 3 shape; smallest reversible option |
| 2 | Origin label for a `library/documents/<id>` destination? | "Archive" | B3: the sidebar's own string (#245) |

## Decisions recorded
None closed; ticket stays open with `Autopilot: continue —`.

## Tickets created / changed
None.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=32 first-critique=n/a close-critique=n/a first-evaluate=n/a close-evaluate=n/a
No capture round yet (build phase, not code-complete). Nothing typechecked this session.

## First-pass findings → lessons (rendered surfaces only)
n/a — no measurement yet. Candidate for the project lessons file at close: a `lib/` module imported by client components must not import `prisma`; split server lookups into `*-server.ts` (check: `grep -l "@/lib/db" $(grep -rl "from \"@/lib/<module>\"" components)`).

## Needs the owner
Nothing yet. PaneFrame's full-mode back-link removal (spec §2.4) is an affordance swap, not a #237 removal — flagged for the close report.
