# Hand-off — #271 Build the Approval notice (map #226)

Phase: **SPEC** (1 of 4), not finished. Session 1 (2026-09-16) hit the 150K context line after the
pre-build reads + skill loads (intent/specify/articulate/fortify/include/impeccable shape all loaded;
craft-floor, both lessons files, preflight.md, area primer `docs/agents/areas/approvals.md` read).
No spec, no pre-flight, no code written yet. Ticket claimed (assignee petrose99).

## Next step (exact)
1. Do NOT re-load the skill bodies one by one — the Skill calls alone cost ~90K. Call `intent` and
   `impeccable` once each (CLAUDE.md rule), then write from the digest below.
2. Write `docs/wayfinder-reports/226/logs/scratch-271/spec.md` and `preflight.md` (copy of
   `scripts/wayfinder-autopilot/preflight.md`, Parts A–E filled). Run the Part D critic as one
   foreground `Agent` with `model: "opus"` (ticket touches approval + schema). Gate: 0 P0/P1, none
   under 3, two 4s (H1, H5), Clean.
3. Write this file with `milestone: spec-done`, commit `wip(autopilot): #271 spec`, stop.

## Research digest (verified this session — do not re-read these files to learn this)
- **Incumbent sweep** `models/reminders.ts` (106 lines): `sendDueReminders(now)` → `sendReviewTaskReminders`
  (48h stale / 72h repeat via `lib/reminders/engine.ts` `isReminderDue`; recipients = assignee else all
  owners; link `/review/<id>` (308); stamps `ReviewTask.lastReminderAt`) + `sendExpenseClaimReminders`
  (removal → #272 owner sign-off; do not delete, unplug behind a flag only if the owner signs).
  Called from `app/api/internal/jobs/process/route.ts` on every drain hit (cron = empty body POST,
  auth `Bearer config.aws.internalWorkerSecret`). Kick pattern: `lib/document-processing.ts:886-899`
  (`kickEmbedJob`, fetch with 5s AbortSignal, swallowed).
- **Email**: `lib/email.ts` (`resend`, `isEmailConfigured()`, `sendReminderEmail` → `components/emails/
  reminder-email.tsx` on `email-layout.tsx` (600px container, centred h2 #047857, button, break-all
  link)). `@react-email/render` ^2.1 is a dep (use `render(..., {plainText:true})` for the text part).
  Resend: pass `text:` alongside `react:`; open/click tracking is off unless domain-level — state on spec.
- **Approver rule**: `lib/approvals/engine.ts:48` `canDecideStage` (approverIds else requireOwner/any
  member — glossary wins: named approverIds else owners, never every member). Structural check
  `lib/approvals/row-eligibility.ts` `stageHasEligibleApprover`, `computeApprovalEligibility`.
- **Schema** `prisma/schema.prisma`: `ReviewTask` (line 1584; has `lastReminderAt`, `currentStageIndex`,
  `createdById`, `workflowId`) — add `stageReachedAt DateTime?`; `ApprovalWorkflowStage` (1750:
  `approverIds String[]`, `requireOwner`). `User` (line 16) has no prefs column — add
  `approvalNoticeEmails Boolean @default(true)`; per-person floor → `WorkspaceMember.noticeLastSentAt
  DateTime?` (one email per workspace, floor per person per workspace). Model tests mock prisma as a
  bare object — add tables to `beforeEach` (`models/reminders.test.ts`).
- **Stage moves**: `models/review-tasks.ts` — `createReviewTask` (:87, sets stage 0), `decideReviewTaskStage`
  (:208, advances `currentStageIndex` at :239), `sendReviewTaskBackForReview` (:253, actor + reason →
  emails the starter `createdById`), `cancelApprovalOnDocument` (:277). `models/approval-workflows.ts:88`
  `startApprovalOnInvoice` / `:117 startWorkflowOnReviewTask` — fire-and-forget kick goes here.
  "Started by" comes from `task.createdById` (see `models/approvals.ts:304-324`).
- **Deep link**: `/workspaces/<ws>/approvals/invoices?doc=<id>` already works — `lib/navigation/
  origin-server.ts` `queueArrival` reads `searchParams.doc` (:119) and shows `decidedText` when the row
  is gone. Add `via=notice` → write a `DocumentAuditEvent` type `notice.opened` (helper
  `lib/audit.ts` `auditEventData`) once per arrival, server-side in the approvals page.
- **Sign-in return**: `proxy.ts:42` redirects protected paths to `/login` with **no** `next` param;
  `app/(auth)/login/page.tsx` passes `redirectTo` to `LoginForm` → `postSignInDestination` (mfa
  challenge already carries `?next=`). Spec: proxy appends `?next=<path+query>` (same-origin relative
  only; validate like `parseInviteToken`), login honours it. Google button also takes `redirectTo`.
- **Switch home**: desktop `components/shell/account-menu.tsx` (menu items; the Keyboard shortcuts row
  shows an "Off" suffix — mirror that: row "Approval emails" + "Off" suffix opening a small dialog with
  `components/ui/switch.tsx` `Switch`); phone `app/(app)/workspaces/[workspaceId]/account/page.tsx`
  (`AdminPage` + `Panel` from `components/automation/automation-ui.tsx`) — new Panel "Approval emails".
  Server-backed (User column), not localStorage (#262's switch is localStorage; this one must reach
  the sender). Stop link: `/notices/stop?t=<hmac>` public route, HMAC of userId with
  `INTERNAL_WORKER_SECRET` (`lib/webhook-signature.ts` `computeSignature` pattern), no login, one
  confirm-free landing "You'll stop getting approval emails" + link to turn back on (sign-in).
- **Admin sentence** when `!isEmailConfigured()`: Admin › Configuration page is
  `app/(app)/workspaces/[workspaceId]/admin/configuration/page.tsx` (title "Fields", `AdminPage`,
  `ReadOnlyBand`) — one sentence band, no new page.
- **Formats**: `lib/money.ts` `formatMoney(amount, currency)` → "12.50 USD"; `Workspace.baseCurrency`,
  `Workspace.timezone` (schema :117-118) for the date.
- **Preview route**: none exists; spec a dev-only `app/dev/emails/approval-notice/route.ts` (404 unless
  `NODE_ENV=development`) rendering singular / plural / nudge / send-back / long-names fixtures; capture
  at **600 and 390** (ticket), detector via the scratch Playwright (`/tmp/scratch229` recipe in
  project lessons).
- Nearest stored critique: `.impeccable/critique/2026-09-16T03-50-00Z__components-payments-bill-pay-queue-tsx.md`;
  #257's Approvals close on the ticket. Residue list is in the area primer.

## Artefacts
- Spec: (not yet) `docs/wayfinder-reports/226/logs/scratch-271/spec.md`
- Pre-flight: (not yet) `docs/wayfinder-reports/226/logs/scratch-271/preflight.md`
- Report: `docs/wayfinder-reports/226/271.md`
