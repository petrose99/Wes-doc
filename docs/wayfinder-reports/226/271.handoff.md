# Hand-off — #271 Build the Approval notice (map #226)

milestone: spec-done
milestone: build-in-progress
milestone: build-done
milestone: measured

Phase MEASURE finished in session 7 (r1: `scratch-271/shots-r1/`, 33 states, `sheet.png`, `detector.json`, `keyboard.json`). Next phase: **CLOSE** — extend `round-271.mjs` with the four missing 390 keyboard probes below, re-run as `r2` (only the affected states, then a full run to refresh `detector.json`/`keyboard.json`), fix anything real they surface, confirm, checks, report, close the ticket, refresh `docs/agents/areas/approvals.md`.

## Measured scores (r1)

**critique: 32/40 — PASS (bar ≥32, no heuristic <3, ≥2 at 4).** H1=4, H5=4 (both confirmed live via keyboard.json, not just asserted); H2/H3/H4/H6–H10 all at the floor of 3 — an exact-bar pass, not a comfortable one. Design specificity: not generic (product copy/glossary/colour all present).

**evaluate: health 87/100 — PASS (bar ≥85, zero P0/P1, Clean).** Zero P0/P1. Anti-pattern verdict Clean (stop-flow specifically checked for roach-motel/hard-to-cancel: one-click unauthenticated opt-out, `List-Unsubscribe-Post: One-Click`, no confirmshaming; re-enable requiring sign-in is disclosed, justified, not a dark pattern). P2s (execution-level, hand to `/articulate`+`/fortify` polish, no ticket needed): (1) desktop footer sentence "under Account › Approval emails" isn't a link — phone variant already links; (2) menu-row `Off` suffix freshness depends on `router.refresh()` on menu open, no test pins it; (3) nudge copy is day-count only, no timestamp, confirm intentional per spec.

**include (keyboard, both widths): FAIL.** 1440 fully verified per spec/B5. **390 is not**: `round-271.mjs`'s keyboard probes never captured the panel's "Saving…" pending state, the save-failure/alert state, or the offline-disabled state at 390 — only off→on toggle and arrival were probed there. Also flagged: `account-panel-switch`'s `status` array is `[""]` (empty) at **both** widths where the spec's describedby sentence ("You'll still see them under Approvals…") should populate it — needs a look at whether that's a probe-script omission (most likely, since 1440 dialog's copy renders fine) or an actual missing wire-up on the panel variant. Minor secondary gap: `stop-done-url` probes capture the URL but not where focus lands post-redirect (spec says h1 first) at either width.

## Detector (r1) — clean once app-wide residue is discounted
56 raw findings, all within the documented app-wide residue set (ai-color-palette rail avatars, overused-font Inter, dev-overlay layout-transition/dark-glow, rail workspace-name text-overflow, rail-hover text-occlusion in menu-open, the Approvals-table flush-padding div) plus one `skipped-offline` harness tag. **Zero new real findings** on this ticket's surfaces — matches r0.

## Close-phase first action
Not a product fix yet — a measurement gap. Add to `round-271.mjs`: (a) 390 pending/"Saving…" probe on the panel switch, (b) 390 save-failure/alert probe, (c) 390 offline-disabled probe, (d) focus-target assertion on `stop-done` at both widths, (e) confirm what `account-panel-switch.status` should read and why it's empty. Run as `r2`, only then decide whether (e) is a real defect requiring a code fix (if so, that's the ticket's one allowed fix batch) or a probe-script gap (fix the script, no code change, close on r1's product scores since nothing in the app changed).

## What is built (sessions 3–6; git log `c71deef`…)
- Backbone: schema + migration on devdb; `lib/notices/{stop-token,kick,fixtures,stop-urls,stop-state}.ts`; `models/approval-notices.ts` (sender, coalesce, nudge, sent-back); `components/emails/approval-notice-email.tsx`; preview `/dev/emails/approval-notice?case=single|plural|nudge|mixed|sent-back|long-names` (`&text=1` for plain text); kicks in `models/review-tasks.ts` + `models/approval-workflows.ts`; `next` param on sign-in; Approvals arrival audit (`?doc=&via=notice`).
- The switch: `components/shell/approval-emails.tsx` (`ApprovalEmailsControl` mounted in the account-menu `ApprovalEmailsDialog` on desktop and in the phone Account panel `#approval-emails-switch-panel`); `components/ui/switch.tsx` gained `busy` (aria-busy, stays focusable while saving).
- Stop pages: `app/notices/stop/page.tsx` (confirm / done / already-off / invalid), `app/notices/stop/confirm/route.ts` (POST only writes; GET → 303 to the page).
- Admin › Configuration band via `ReadOnlyBand children` (now `max-w-[60ch]`); `components/ui/dialog.tsx` title `text-lg` (was `text-base`: flat step vs 14px body).

## Verified
- Tests: `models/approval-notices.test.ts`, `lib/notices/*.test.ts` — 23/23; `npx tsc --noEmit` clean (session 6).
- Part B checks: `scratch-271/part-b-checks.mjs` → `part-b-results.txt`, all PASS (session 5).
- Round tooling: `scratch-271/round-271.mjs` (33 states; account-menu + switch-dialog are 1440-only, the 390 contract is the Account panel), `run-round.mjs <r> [only]` (**`only` overwrites `detector.json`/`keyboard.json` with the subset — always finish with a full run**), `sheet.mjs <r>`, `warm.mjs`, `facts.json`. Recipe: `node .impeccable/live/dev.mjs start`; `node .impeccable/live/livesrv257.mjs` (background, :8400; kill by pid from `ss -ltnp`); `node .../warm.mjs`; `node .../run-round.mjs r1`; `node .../sheet.mjs r1`.

## r0 (build gate) — `scratch-271/shots-r0/` (33 PNGs, `sheet.png`, `detector.json`, `keyboard.json`)
Real findings after the build's fixes: **0 at 1440 and 0 at 390 in every state.** Fixed in session 6: `Switch` disabled-while-saving dropped focus to body (probes failed → `busy`); stop card `gpt-thin-border-wide-shadow` (auth-card 70px shadow → `shadow-sm`); Admin band `line-length` 126 → capped; email footer `line-length` 92 → `maxWidth 460px`; dialog `flat-type-hierarchy`; round script asked for `sent_back` (fixture is `sent-back`) → 404 captured.
Residue (total/real): emails 0/0; account-menu-open 9/0; switch-dialog idle/off/error 5/0, offline 1/0 (`skipped-offline`); account-panel(+off) 6/0 · 5/0; stop-* 0/0; admin-band 5/0 · 5/0; arrival 6/0 · 5/0. Residue = app-wide five (rail avatar palette ×2, Inter, dev-overlay `layout-transition`+`dark-glow`) + shell `text-overflow` on the rail's truncated workspace name + rail-hover `text-occlusion` in the menu-open state + `hidden lg:table` "children flush" on Approvals (approvals primer) + the detector's own legend span occluded by the stop page's full-viewport wrapper.
Keyboard probes (all as expected): open-focus = switch, checked true · toggle off/on keeps focus on the switch, "Saving…" status, help text swaps · Esc returns to `#account-menu-trigger` ("Account menu"), dialog gone · save failure → alert "Couldn't save this…", value reverted, focus kept · offline → disabled + reason · panel switch 390 off→on with focus kept · stop page Tab order home → Stop button → Privacy → Terms · POST lands on `?done=1&t=…` · `done=1` without token → "This link isn't valid." · after stop the Account panel shows off; restored on · arrival opens the detail pane, focus on the row h2.

## Known simplifications to flag at close
- Eligibility logic in `models/approval-notices.ts` duplicates `models/approvals.ts` inline.
- `email-layout.tsx` got no `align` prop; spec §3 literal wording differs slightly.
- `ReadOnlyBand` reused for the Admin email band; `Dialog` title size changed for every dialog (2px).
- Arrival audit writes `documentId` only when the doc is a current row (FK).
- Stop POST route is `/notices/stop/confirm` (Next forbids page+route in one segment); spec said `/notices/stop`.
- Stop page background is `bg-slate-50` (auth layout's), not a `bg-canvas` token (none exists).

## Area primer
`docs/agents/areas/approvals.md` refresh (Approval notice, stop routes, preview route, cron cadence, the `busy` Switch) is CLOSE-phase work.
