# Hand-off — #271 Build the Approval notice (map #226)

milestone: spec-done

Phase SPEC finished in session 2 (2026-09-16). Ticket claimed (assignee petrose99). No product code yet.

## Artefacts
- Spec: `docs/wayfinder-reports/226/logs/scratch-271/spec.md` (§1 sender/schema, §2 deep link + `next`, §3 email copy matrix + preview route, §4 switch + stop link, §5 states, §6 a11y, §7 tests)
- Pre-flight (A–E filled, critic reconciled): `docs/wayfinder-reports/226/logs/scratch-271/preflight.md`
- Critic fix script (already applied): `scratch-271/fix-spec-after-critic.mjs`
- Report: `docs/wayfinder-reports/226/271.md` · Area primer: `docs/agents/areas/approvals.md` (refresh at close: add Approval notice, stop route, preview route, cron cadence)

## Gate (Part D, opus critic)
Before fixes 30/40 · sum 17 · 2 P1 (stop link was a mutating GET → scanners switch people off; send-back restart never re-notified). After fixes: predicted **critique 32/40** (H1 4, H5 4, rest 3) · evaluate sum ≈ 8 · P0/P1 0 · health ≈ 85 · Clean. Gate met.

## Next step (BUILD phase, fresh session)
Build from the pre-flight tables row by row; read the area primer first, then only the spec sections you are coding. No captures. Order:
1. Schema: `ReviewTask.stageReachedAt`, `ApprovalNotice` table, `WorkspaceMember.noticeLastSentAt`, `User.approvalNoticeEmails`; migration; add tables to `beforeEach` in `models/reminders.test.ts` (bare-object prisma mock).
2. `lib/notices/stop-token.ts` + test; `models/approval-notices.ts` (`sendApprovalNotices`, `sendSentBackNotice`) + test; re-point `sendReviewTaskReminders`; kicks in `models/approval-workflows.ts:88/117`, `models/review-tasks.ts:239/253`; drain route calls the sender.
3. `components/emails/approval-notice-email.tsx` (+ `align` prop on `email-layout.tsx`), fixtures `lib/notices/fixtures.ts`, preview `app/dev/emails/approval-notice/route.ts` (`?case=…&text=1`).
4. `proxy.ts` `next` param + `LoginForm`/Google `redirectTo`; approvals page `via=notice` → `notice.opened` audit event.
5. Switch: `setApprovalNoticeEmailsAction`; `account-menu.tsx` row + `ApprovalEmailsDialog`; phone account `Panel`; Admin › Configuration `ReadOnlyBand` when `!isEmailConfigured()`.
6. `app/notices/stop/page.tsx` (GET confirm/done/invalid) + `route.ts` (POST write, 303).
7. Run Part B checks (B1 greps, B3 string extraction, B4 diff, focus probe), affected vitest, `tsc --noEmit` once; write `milestone: build-done`; commit `wip(autopilot): #271 build`.
Measure phase captures: email cases × 600/390 via the preview route, switch dialog 1440, account panel 390, stop page 390/1440, Admin band 1440 — on `capture-round.mjs` (copy beside `/tmp/scratch229` Playwright).

## Research digest (verified in session 1 — do not re-read to learn this)
- Sweep `models/reminders.ts` (`sendDueReminders` → `sendReviewTaskReminders` 48h/72h via `lib/reminders/engine.ts`; `sendExpenseClaimReminders` untouched, #272). Drain: `app/api/internal/jobs/process/route.ts` (Bearer `config.aws.internalWorkerSecret`); kick pattern `lib/document-processing.ts:886-899`.
- Email: `lib/email.ts` (`resend`, `isEmailConfigured`, `sendReminderEmail`), `components/emails/email-layout.tsx` (600px, centred h2 #047857), `@react-email/render` (`render(..., {plainText:true})`).
- Approver rule `lib/approvals/engine.ts:48` `canDecideStage`; eligibility `lib/approvals/row-eligibility.ts` `computeApprovalEligibility`.
- Schema: `ReviewTask` :1584, `ApprovalWorkflowStage` :1750, `User` :16, `Workspace.baseCurrency/timezone` :117-118.
- Stage moves `models/review-tasks.ts` `createReviewTask` :87, `decideReviewTaskStage` :208 (advance :239), `sendReviewTaskBackForReview` :253; start `models/approval-workflows.ts:88/:117`; started-by `models/approvals.ts:304-324`.
- Arrival: `lib/navigation/origin-server.ts` `queueArrival` reads `searchParams.doc` (:119), `decidedText`. Audit helper `lib/audit.ts` `auditEventData`.
- Sign-in: `proxy.ts:42` redirect (no `next` today); `app/(auth)/login/page.tsx` → `LoginForm` `redirectTo` → `postSignInDestination`.
- Switch homes: `components/shell/account-menu.tsx` (Keyboard shortcuts row :74-78 is the pattern), phone `app/(app)/workspaces/[workspaceId]/account/page.tsx` (`AdminPage` + `Panel`). `components/ui/switch.tsx`. Admin band page `admin/configuration/page.tsx` (`ReadOnlyBand`). HMAC pattern `lib/webhook-signature.ts`.
- `lib/money.ts` `formatMoney`. Nearest critique `.impeccable/critique/2026-09-16T03-50-00Z__components-payments-bill-pay-queue-tsx.md`.

## Lessons to write at close (from the critic's P1s)
- generic: an emailed link that changes state must be GET-confirm + POST-write (scanners GET) · check: grep route handlers for `export async function GET` that call `prisma.*.update`.
- generic: "new since" keyed on a stage *index* misses a restart of the same stage · check: fortify B6 row for every "re-entry" transition.
