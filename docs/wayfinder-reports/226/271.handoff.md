# Hand-off — #271 Build the Approval notice (map #226)

milestone: spec-done
milestone: build-in-progress

Phase SPEC finished session 2. Phase BUILD (session 3) got through the backbone (schema, stop-token, sender, email component, drain wiring) but ran out of context before the UI/route pieces and before `build-done`'s gate (Part B checks, tests, tsc, r0 capture). **Not build-done** — next session continues BUILD, does not restart it.

## What is built and verified this session
- Schema (`prisma/schema.prisma`): `User.approvalNoticeEmails`, `WorkspaceMember.noticeLastSentAt`, `ReviewTask.stageReachedAt`, new `ApprovalNotice` model + relations. Migration written by hand at `prisma/migrations/20260916090000_add_approval_notices/migration.sql` (additive, includes a backfill of `stage_reached_at` from `created_at` for open workflow tasks). **Not yet run** — no `prisma migrate deploy`/`db push` executed against devdb this session; do that first next session (`npx prisma generate` too, since the client needs regenerating for the new model/columns before anything using them will typecheck against a real client — I worked against the schema types only, not a regenerated client).
- `lib/notices/stop-token.ts` + `lib/notices/stop-token.test.ts` — **passing** (ran `npx vitest run lib/notices/stop-token.test.ts`, 5/5 green). HMAC sign/verify, tamper/expiry rejection, domain separation from `lib/webhook-signature.ts`.
- `lib/notices/kick.ts` — `kickApprovalNoticeDrain()`, fire-and-forget POST `{drainApprovalNotices:true}`, mirrors `kickWebhookDrain`. **Not yet called from anywhere** (see next steps).
- `models/reminders.ts` — `sendReviewTaskReminders` candidates now exclude `workflowId: null`-having tasks... actually excludes tasks WITH a workflow (`where: {..., workflowId: null}`), so a workflow-attached task is never double-reminded by both the old path and the new sender. Not covered by a new test; existing `models/reminders.test.ts` still passes conceptually (mocks don't assert `where`) but **was not re-run this session** — run it next session before trusting it.
- `models/approval-notices.ts` — `sendApprovalNotices(now)` and `sendSentBackNotice(...)`. Implements: candidate load across all workspaces (workflow-attached, in_review, `stageReachedAt` set, invoice template only), eligibility via the same `computeApprovalEligibility`/gates/exceptions/payment logic `models/approvals.ts` uses (duplicated inline, not imported, to get the extra fields — `createdById`, `approverIds`, `stageReachedAt` — the Approvals-page row type doesn't carry), decider resolution (named approvers else workspace owners, never every member), reached-vs-nudge bucketing keyed on `stageReachedAt` (so a send-back restart is new again), the 60-min floor, the nudge cap (2) and 72h repeat gate, exclusions (starter, switch off, unverified email, no-longer-member). **Not yet unit-tested** — `models/approval-notices.test.ts` from spec §7 does not exist yet. **Not yet typechecked** — written against the not-yet-generated Prisma client, so `tsc --noEmit` will likely surface `approvalNotice`/`stageReachedAt`/`noticeLastSentAt`/`approvalNoticeEmails` type errors until `prisma generate` runs.
- `components/emails/approval-notice-email.tsx` — the email component (heading/lead/rows/button/footer per spec §3). Simplification from the spec: I did **not** add an `align` prop to `email-layout.tsx` — that layout has no centred-h2 default to override (unlike `ReminderEmail`, which centres inline on its own), so left-alignment is just the new component's own inline styles. Note this simplification when reviewing against spec §3's literal wording.
- `lib/notices/fixtures.ts` — six of the seven copy-matrix cases (`single`, `plural`, `nudge`, `mixed`, `sent-back`, `long-names`); `claim` omitted since expense claims aren't rows yet (#273, correctly out of scope per spec §8).
- `app/dev/emails/approval-notice/route.ts` — preview route, `?case=&text=1`, 404 outside development. **Not yet hit/verified in a browser** (no dev server started this session).
- `app/api/internal/jobs/process/route.ts` — wired `sendApprovalNotices()` into the drain's `Promise.all`, added `drainApprovalNotices` to the body type (accepted but not yet special-cased the way `drainWebhooks`/`drainIntegrationPushes` skip other work — check whether it should skip `processNextQueuedDocumentJob` the same way before calling this done), response now includes `approvalNotices`.

## Not started — next session's work, in order
1. **Run the migration + regenerate the client** (`npx prisma migrate deploy` or the project's dev-db push script — check `docs/agents/areas/` or `package.json` scripts first; then `npx prisma generate`). Nothing above typechecks for real until this happens.
2. **Kicks** — I was mid-edit on `models/approval-workflows.ts` (`startApprovalOnInvoice`/`startWorkflowOnReviewTask`, lines ~88/117) when the context line hit; no edit was made there, it's untouched. Still to do:
   - `models/review-tasks.ts`: `createReviewTask` — set `stageReachedAt: now()` alongside the existing `currentStageIndex: 0, status: "in_review"` when a workflow is attached (line ~110 area, the `...(workflowId ? {...} : {})` spread). `decideReviewTaskStage` — on `outcome === "advance"`, also set `stageReachedAt: new Date()`; after the transaction, call `kickApprovalNoticeDrain()` (fire-and-forget, don't await-block the response). `sendReviewTaskBackForReview` — clear `stageReachedAt: null` alongside the existing `workflowId: null, currentStageIndex: null`; after the transaction, call `sendSentBackNotice(...)` (needs supplier/invoiceNumber/amount/currency off the document's `reviewedData` — same `asString`/`asNumber` extraction as `models/approvals.ts`/`models/approval-notices.ts`; needs `actorName` — look up the actor's `User.name`) and `kickApprovalNoticeDrain()`.
   - `models/approval-workflows.ts`: `startApprovalOnInvoice`/`startWorkflowOnReviewTask` — set `stageReachedAt: now()` when starting at stage 0 (mirrors `createReviewTask`'s workflow branch), kick the drain after.
3. **Proxy `next` param** (spec §2): `proxy.ts` — append `?next=<pathname+search>` when redirecting to `/login`. `app/(auth)/login/page.tsx` → `LoginForm` — accept and validate `next` (regex `^\/(?![\/\\])`, not starting with `/login`), use as post-sign-in redirect for both the credentials form and the Google button. Test: `proxy.test`/login — `next` accepted only for `/…` relative, `//evil` rejected.
4. **Approvals page arrival audit event** (spec §2): in the Approvals › Invoices page (`app/(app)/workspaces/[workspaceId]/(queue)/approvals/invoices/page.tsx`), when `searchParams.via === "notice"`, write a `DocumentAuditEvent` type `notice.opened` once per arrival (actor = viewer, doc = `doc` param if present). Don't echo `via` into any link the page emits.
5. **The switch** (spec §4): `setApprovalNoticeEmailsAction` in `app/(app)/workspaces/[workspaceId]/actions.ts`; `components/shell/account-menu.tsx` new menu row + `ApprovalEmailsDialog`; phone `Panel` in `app/(app)/workspaces/[workspaceId]/account/page.tsx`. Follow the "Keyboard shortcuts" row pattern already in `account-menu.tsx` (lines ~74-78 per the research digest) for the menu-row shape.
6. **Stop page + route** (spec §4): `app/notices/stop/page.tsx` (GET, four states: confirm / done / already-off / invalid) + `app/notices/stop/route.ts` (POST, verifies token, flips `approvalNoticeEmails`, 303). Uses `verifyStopToken` from `lib/notices/stop-token.ts`, already built.
7. **Admin band** (spec §4): `admin/configuration/page.tsx` — `ReadOnlyBand` when `!isEmailConfigured()`.
8. **Tests** (spec §7): `models/approval-notices.test.ts` (coalescing, floor, exclusions, nudge cap, restart-after-send-back, not-configured skip — mirror `models/reminders.test.ts`'s bare-object prisma mock pattern, but note `sendApprovalNotices` touches many more prisma models than `reminders.ts` did, so the mock's `beforeEach` needs `reviewTask`, `approvalNotice`, `user`, `workspaceMember`, `documentCheckResult`, `integrationPush`, and whatever `lib/gates/list.ts`'s `listOpenGatesForDocuments` queries — check that file for its prisma calls before mocking). `app/notices/stop` tests (GET doesn't write, POST does, bad token doesn't write). Email render snapshot per fixture.
9. **Part B contract checks** (preflight.md, already filled) — run the greps and the string-extraction check named there.
10. `tsc --noEmit` once, stop any dev server, then write `milestone: build-done` and hand off to MEASURE.

## Known simplifications to flag at close (not necessarily wrong, but decided under time pressure, not by the spec/critic)
- Eligibility computation in `models/approval-notices.ts` duplicates `models/approvals.ts`'s `loadSubmittedApprovals` logic inline rather than importing it — kept self-contained (matches `models/reminders.ts`'s standalone style) but is now two places that must stay in sync if eligibility rules change. Worth a comment cross-reference at minimum; consider whether it should actually be extracted to a shared helper.
- `email-layout.tsx` was not given an `align` prop (see above) — the spec's literal instruction ("pass `align=\"left\"`") wasn't followed because there was nothing to override; confirm this reading holds before the critic/close review.

## Research digest (still valid, from session 1 — do not re-read to learn this)
See prior hand-off revision's "Research digest" section, preserved in git history at commit `93a5a99` and earlier if needed — file paths and line numbers for reminders.ts, email.ts, approvals engine/row-eligibility, schema locations, stage-move call sites, arrival/audit helpers, sign-in flow, switch homes, HMAC pattern. Nothing there has changed.

## Area primer
`docs/agents/areas/approvals.md` still needs its close-time refresh (Approval notice, stop route, preview route, cron cadence) — not done yet, do it in the CLOSE phase per the original plan.
