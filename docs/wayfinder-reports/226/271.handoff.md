# Hand-off — #271 Build the Approval notice (map #226)

milestone: spec-done
milestone: build-in-progress

Phase BUILD, session 4 (continuation). Ran out of context mid-build. **Not build-done** — the next session continues BUILD from the "Not started" list below; nothing below is to be redone. Spec: `docs/wayfinder-reports/226/logs/scratch-271/spec.md`; pre-flight: `.../scratch-271/preflight.md`; edit scripts used: `.../scratch-271/edit-*.mjs` (pattern: write a `.mjs` patcher, `node` it — the bash sandbox rejects heredocs/braces/quotes).

## Done and verified (sessions 3+4)
- Migration `prisma/migrations/20260916090000_add_approval_notices/` **applied to devdb** and client regenerated (`scratch-271/migrate.mjs`). Schema: `User.approvalNoticeEmails`, `WorkspaceMember.noticeLastSentAt`, `ReviewTask.stageReachedAt`, `ApprovalNotice`.
- `lib/notices/stop-token.ts` (+ test, 5/5 green), `lib/notices/kick.ts`, `models/approval-notices.ts` (`sendApprovalNotices`, `sendSentBackNotice`, new `notifySentBack` wrapper that loads ws name / actor name / doc facts), `components/emails/approval-notice-email.tsx`, `lib/notices/fixtures.ts`, `app/dev/emails/approval-notice/route.ts` (preview, unverified in browser), drain wiring in `app/api/internal/jobs/process/route.ts`.
- **Kicks wired** (session 4): `models/review-tasks.ts` — `createReviewTask` sets `stageReachedAt` + kicks when a workflow attaches; `decideReviewTaskStage` sets `stageReachedAt` on advance + kicks; `sendReviewTaskBackForReview` clears `stageReachedAt`, selects `createdById`, calls `notifySentBack` (fire-and-forget). `models/approval-workflows.ts` — `startWorkflowOnReviewTask` sets `stageReachedAt` + kicks.
- **`next` param** (spec §2): `lib/auth-post-sign-in.ts` `safeNextPath` (+4 tests, 9/9 green); `proxy.ts` appends `?next=`; `app/(auth)/login/page.tsx` passes the validated `next` as `redirectTo` (invite still wins).
- **Arrival audit** (spec §2): Approvals › Invoices page accepts `?doc=&via=notice`, opens the row via `initialSelectedId` when the doc is in the rows, writes `notice.opened` audit once per arrival (documentId only when the doc is a row — FK).
- **The switch** (spec §4): `setApprovalNoticeEmailsAction` / `getApprovalNoticeEmailsAction` in `app/(app)/workspaces/[workspaceId]/actions.ts`; `components/shell/approval-emails.tsx` (`ApprovalEmailsControl` = label + Switch + describedby sentences + `role=status` Saving…/offline + `role=alert` revert; `ApprovalEmailsDialog` host, event-opened, reads value on open, `initialFocus="#approval-emails-switch"`, Done button; `openApprovalEmailsDialog()`); account-menu row (Mail icon, `Off` suffix, `aria-label` folded), trigger now has `id="account-menu-trigger"`; `Sidebar` mounts the dialog and takes `user.approvalEmails`; layout passes `user.approvalNoticeEmails`; phone Account page `Panel "Approval emails"` with `switchId="approval-emails-switch-panel"`.
- **Admin band** (spec §4): `ReadOnlyBand` gained optional `children`; `admin/configuration/page.tsx` shows the not-configured sentence when `!isEmailConfigured()`.
- None of the session-4 UI has been typechecked or rendered yet (no dev server started; no `tsc`).

## Not started — next session, in order
1. **Stop page + POST route** (spec §4). Next forbids `page.tsx` + `route.ts` in one segment, so: `app/notices/stop/page.tsx` (GET: confirm / done / already-off / invalid; public; card markup copied from `app/(auth)/layout.tsx` lines 38–50; `?t=` verified with `verifyStopToken`, `?done=1` re-verified) and `app/notices/stop/confirm/route.ts` (POST: verify `t`, `prisma.user.update({approvalNoticeEmails:false})`, 303 → `/notices/stop?done=1&t=…`; GET → 303 to the page). Then in `models/approval-notices.ts` lines 179/187 and 297/305 make `List-Unsubscribe` point at `/notices/stop/confirm?t=` (POST route) while the footer link stays `/notices/stop?t=` (GET page) — add `stopPageUrl`/`stopPostUrl` helpers in `lib/notices/` and use them in both send sites. Tests: GET leaves the column, POST flips it, bad token no write.
2. **Tests** (spec §7): `models/approval-notices.test.ts` (mirror `models/reminders.test.ts`'s bare-object prisma mock; add every table `sendApprovalNotices` touches: `reviewTask`, `approvalNotice`, `user`, `workspaceMember`, `workspace`, `document`, `documentCheckResult`, `integrationPush`, plus whatever `lib/gates/list.ts` queries). Re-run `models/reminders.test.ts` (candidate `where` now has `workflowId: null`). Email render snapshot per fixture.
3. `npx tsc --noEmit` (no dev server up) — expect first real type errors from session 3/4 code (written against the schema, never compiled). Fix.
4. **Part B contract checks** from `preflight.md` (reachability greps, string extraction for "Reminder/digest/alert/notification", primitive diff = no new file under `components/ui`, `window.confirm|alert(` grep).
5. **Build gate:** seed + servers (`docs/agents/areas/approvals.md` §Seed, dev server, capture — `node .impeccable/live/dev.mjs start`, livesrv on :8400), round script on `scripts/wayfinder-autopilot/capture-round.mjs` in `scratch-271/pw/round.mjs`: states = email preview × 6 cases (600/390 via the `/dev/emails/approval-notice?case=` route), account-menu open, Approval-emails dialog (idle/saving/error/offline), phone Account panel (390), stop page (confirm/done/already-off/invalid), Admin band (set `RESEND_API_KEY` empty or mock), Approvals arrival `?doc=&via=notice`; keyboard probes per preflight B5 (`switch-dialog-open-focus-1440`, `switch-dialog-esc-return-1440` → `#account-menu-trigger`, `account-panel-switch-390`). Run as `shots-r0`, fix real findings, write r0 counts here, stop servers, `milestone: build-done`, commit `wip(autopilot): #271 build`.

## Known simplifications to flag at close
- Eligibility logic in `models/approval-notices.ts` duplicates `models/approvals.ts` inline (comment cross-reference at least).
- `email-layout.tsx` got no `align` prop (nothing to override); spec §3 literal wording differs.
- `ReadOnlyBand` reused for the Admin email band via `children` (amber band; spec said "ReadOnlyBand sentence" — same primitive, fine).
- Arrival audit uses `docParam` (doc must be a current row) for `documentId` to respect the FK; a decided doc's arrival is audited with `documentId: null`.

## Area primer
`docs/agents/areas/approvals.md` refresh (Approval notice, stop route, preview route, cron cadence, tracking-off note) is CLOSE-phase work.
