# Hand-off — #271 Build the Approval notice (map #226)

milestone: spec-done
milestone: build-in-progress

Phase BUILD, session 5 (continuation). Hit the context line before the r0 capture round. **Not build-done** — the next session continues BUILD at step 1 of "Next" below. Spec: `docs/wayfinder-reports/226/logs/scratch-271/spec.md`; pre-flight: `.../scratch-271/preflight.md`.

## Done and verified (sessions 3–5)
- Sessions 3–4 (see git log `c71deef`, `0ec0ad8`): schema + migration applied to devdb; `lib/notices/{stop-token,kick,fixtures}.ts`; `models/approval-notices.ts` (sender, sent-back, `notifySentBack`); `components/emails/approval-notice-email.tsx`; `/dev/emails/approval-notice?case=` preview; drain wiring; kicks in `models/review-tasks.ts` + `models/approval-workflows.ts`; `next` param (`lib/auth-post-sign-in.ts`, `proxy.ts`, login page); Approvals arrival audit (`?doc=&via=notice`); the switch (`components/shell/approval-emails.tsx` `ApprovalEmailsControl`/`ApprovalEmailsDialog`, account-menu row, `Sidebar`, phone Account `Panel`); Admin band via `ReadOnlyBand children`.
- Session 5: **Stop page + POST route** — `lib/notices/stop-urls.ts` (`stopPageUrl`/`stopPostUrl`/`stopHeaders`; both send sites use them: footer link → GET page, `List-Unsubscribe` → `/notices/stop/confirm`), `lib/notices/stop-state.ts` (`resolveStopPageState` read-only, `applyStop` the one write), `app/notices/stop/page.tsx` (confirm/done/already-off/invalid; auth-layout card; `<main>`, h1 first, plain `<form method=POST>`, `error=1` alert), `app/notices/stop/confirm/route.ts` (POST verifies token from body or query, 303 back; GET → 303 to page, no write).
- **Tests green**: `models/approval-notices.test.ts` (12: floor, coalesce, exclusions, owner fallback, not-eligible wait, nudge cap/72h, restart keyed on stageReachedAt, failure no stamp, sent-back no stamp/left/off), `lib/notices/stop-state.test.ts` (6), `lib/notices/stop-token.test.ts`, `models/reminders.test.ts` — 25/25 + 23/23.
- **`npx tsc --noEmit` clean** (two errors fixed: `errorMessage(error, fallback)` in `setApprovalNoticeEmailsAction`; `subjectAndHeading` row kind type).
- **Part B checks all PASS**: `scratch-271/part-b-checks.mjs` → `part-b-results.txt` (B1 check reworded: one shared `ApprovalEmailsControl` mounted on 2 surfaces).
- **Round tooling written, not yet run**: `scratch-271/round-271.mjs` (states: email×5 cases, account-menu open, switch-dialog idle/off/error/offline + probes `switch-dialog-open-focus`, `switch-toggle-off/on`, `switch-dialog-esc-return`, `switch-save-failure` (aborts `next-action` requests), `switch-offline`; account-panel + `account-panel-switch`; stop-confirm → done (real POST) → already-off → invalid → `done=1` without token → restore the switch via Account panel; admin-band; arrival), `run-round.mjs r0 [only]` (copies beside `/tmp/scratch229`), `sheet.mjs r0`, `warm.mjs` (curl-warms every route), `round-facts.ts` → `facts.json` (owner id, docId `28a0bb04…`, signed stop token; regenerate if the token ages >90d).
- Servers were started and stopped again this session (dev :3000 via `node .impeccable/live/dev.mjs start|stop`; detector :8400 via `node .impeccable/live/livesrv257.mjs` in background — kill by pid from `ss -ltnp`).

## Next — build gate, in order
1. `node .impeccable/live/dev.mjs start`; `node .impeccable/live/livesrv257.mjs` (background); `node docs/wayfinder-reports/226/logs/scratch-271/warm.mjs` (expect 200s; first hits 50 s+).
2. `node docs/wayfinder-reports/226/logs/scratch-271/run-round.mjs r0` → `shots-r0/detector.json`, `keyboard.json`. Then `node .../sheet.mjs r0`, read `shots-r0/sheet.png` once.
3. Fix real detector findings and failing probes in place (no readers). Expected probe values: open-focus = `button[switch]:Approval emails` checked=true; esc-return focused = `button:…` the `#account-menu-trigger` (name "Dev User"/"Account menu"); save-failure alert = the "Couldn't save this…" sentence, checked reverted to true; offline disabled=true; stop-done-without-token h1 = "This link isn't valid."; stop-restored checked=true.
4. Write r0 counts per width per state here; stop both servers; `npx tsc --noEmit` if code changed; `milestone: build-done`; commit `wip(autopilot): #271 build`.

## Known simplifications to flag at close
- Eligibility logic in `models/approval-notices.ts` duplicates `models/approvals.ts` inline.
- `email-layout.tsx` got no `align` prop; spec §3 literal wording differs slightly.
- `ReadOnlyBand` reused for the Admin email band via `children`.
- Arrival audit writes `documentId` only when the doc is a current row (FK).
- Stop POST route is `/notices/stop/confirm` (Next forbids page+route in one segment); spec said `/notices/stop`.
- Stop page background is `bg-slate-50` (auth layout's), not a `bg-canvas` token (none exists).

## Area primer
`docs/agents/areas/approvals.md` refresh (Approval notice, stop routes, preview route, cron cadence) is CLOSE-phase work.
