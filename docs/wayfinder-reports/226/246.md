# Autopilot report — #246 Decide how the Approver learns there is work: channel and cadence

- Map: #226 Make the operator app work like Vic.ai (grammar, not skin)
- Type: wayfinder:grilling · Started: 2026-09-16T00:40Z · Finished: 2026-09-16T01:05Z
- Outcome: resolved

## What I did

1. Loaded the map and ticket; claimed #246 (assigned).
2. Called the Skill tool for `grilling`, `domain-modeling`, `intent` (journey) and `impeccable` (clarify). `impeccable context` could not run (the launcher command was blocked by the session's permission mode); I read `PRODUCT.md` and the `clarify` playbook directly, as the skill instructs when the launcher is unavailable. No `craft-floor.md` was read: no UI edit was made.
3. Gathered facts from the repo instead of asking: `models/reminders.ts` / `lib/reminders/engine.ts` (48h/72h assignee-or-owners nudge, expense-claim sweep still live, `/review` link), `lib/email.ts` (Resend, `isEmailConfigured`), `lib/approvals/engine.ts` (`canDecideStage`'s any-member fallback), `public/sw.js` (no push), `lib/document-processing.ts:892` (drain kick pattern), the two HANDOFF docs (drain cron cadence unrecorded), the tour findings (no channel recorded), the journey map §4 and seam H8, CONTEXT.md's Approver / Ready to Approve / Not eligible terms, and #227 / #232 resolutions (badge rules).
4. Ran three grilling rounds (16 questions), answering each with its `➡️` recommendation.
5. Added **Approval notice** to `CONTEXT.md`; updated `docs/journeys/operator-working-day.md` §4.
6. Created [#271 Build the Approval notice](https://github.com/petrose99/Wes-doc/issues/271) (task) and [#272 Owner sign-off: silence the expense-claim reminder sweep](https://github.com/petrose99/Wes-doc/issues/272); attached both as sub-issues of #226.
7. Posted the [resolution comment](https://github.com/petrose99/Wes-doc/issues/246#issuecomment-5690313901), closed #246, appended the Decisions-so-far line and one fog line (web push) to the map.
8. Committed the docs on `codex/post-login-ux-implementation` (not pushed).

## Questions I answered on the owner's behalf

| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Trigger | Event-driven, coalesced per person, 60-min floor | Intent real conditions; Category 5 Notification Spam at 25/day; daily digest breaks the H8 hand-off |
| 2 | Channel | Email + existing badges; no push | No push infra in `sw.js`; Permission Harassment; smallest reversible |
| 3 | Recipients | Named approvers, else owners; never every member | CONTEXT.md Approver term over `canDecideStage` fallback; conflict handed to #253 |
| 4 | Self-exclusion | No notice for your own run or a decided one | Feedback is on screen already |
| 5 | Not eligible rows | Wait until eligible | #227 Not eligible; Dead End (cat. 9) |
| 6 | Stale reminders | Keep, re-pointed, capped at two nudges | Nagging (cat. 5); existing engine = smallest change |
| 7 | Reviewer outcomes | Only Send back for review emails the starter | #239 lanes; #233 Status line carries the rest |
| 8 | Deep link | Row when one, Ready to Approve when several; `via=notice`; no pixel | #236 deep links, #232 phone pane; privacy by default |
| 9 | Per-user switch | On by default, one-click off + signed footer link | Ethical defaults / opt-out burden; GDPR symmetry |
| 10 | Latency | ≤15 min; Start kicks the drain | Existing pattern `document-processing.ts:892` |
| 11 | Measure | Stage-reached → decided; counter-metrics notices/week, switch-off rate, opened-without-deciding | `measure`'s Goodhart guard |
| 12 | Expense-claim sweep | Silence while unplugged — pending owner sign-off | Removal protocol #237 |
| 13 | Copy | Supplier · amount subject; one **Open in DocuBite** button | `clarify` hierarchy; #227 no acting from outside the app |
| 14 | State inventory | Fourteen states as listed on the ticket | `fortify` |
| 15 | In-app beyond badge | Nothing | Operate mode; Attention Bait; smallest reversible |
| 16 | Verifying an email | Dev preview route + critique + detector at 600/390 | CLAUDE.md verify rule, closest possible form |

## Decisions recorded

One **Approval notice** email per person, event-driven and coalesced (≤1/hour, none when nothing is new, never for your own run), to the stage's named approvers else owners, Not eligible rows waiting until they flip; the existing reminder re-pointed and capped at two nudges; Send back for review is the only outcome that emails the starter; deep link to the row or Ready to Approve with `via=notice` as an audit fact, no tracking; email + existing badges, no push/toast/tab count; per-user switch on by default with a one-click signed stop link; Start kicks the drain; success and counter-metrics named; copy and state inventory fixed. Action Summary: Intent `articulate` → `fortify` → `specify`; Impeccable `clarify` → `harden` → `polish` on #271.

## Tickets created / changed

- Created #271 (execution, `wayfinder:task`, sub-issue of #226, unblocked — #236 is closed).
- Created #272 (owner sign-off, `wayfinder:task`, sub-issue of #226, not blocking #271).
- #246 closed; map #226 gained the Decisions line and a fog entry for web push.
- Input for #253 (Approval Flows): the glossary/code disagreement on who may decide a stage that names nobody with `requireOwner` off.

## Scores and counts (rendered surfaces only)

No score: no surface was built or changed. Incumbent findings that shaped the recommendations are listed in the resolution comment.

## Needs the owner

- #272: sign to silence the expense-claim reminder sweep while Expenses is unplugged.
- `impeccable context` did not run in this session (launcher blocked by permission mode); the next execution session should allow `.claude/skills/impeccable/scripts/impeccable` or run it interactively.
- The #236 build commit `b137c01` is not reachable on this branch (`models/approvals.ts` absent); #271 assumes the #236 decisions, not its code, and should confirm where that build lives before starting.
