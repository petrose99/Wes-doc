# Autopilot report — #281 Post to ledger (map #226 — Post to ledger / Post-login UX)

- Map: #226 Post-login UX implementation
- Type: wayfinder:task (execution ticket, phased build) · Started: continuation session, spec phase · Outcome: partial (spec phase complete, build not started)

## What I did
1. Claimed #281 (`gh issue edit 281 --add-assignee @me`), read the hand-off
   from the prior session (recon already done, no code touched).
2. Re-read #248 (the decision ticket this executes) and CONTEXT.md's
   Post/Posted/Ledger mark/Finance/Bill Pay/Payee glossary entries verbatim.
3. Wrote the full spec: `docs/wayfinder-reports/226/logs/scratch-281/spec.md`
   — 11 sections covering bulk Post (3 queues), row Ledger mark, ledger
   facet, connection-failure band, pane ⋯ Post + Checks-tab Retry, Finance
   page rewrite, states, accessibility, and explicit out-of-scope items.
4. Filled the pre-flight: `.../scratch-281/preflight.md` (Parts A-E).
5. Ran the spec critic as a fresh-context `Agent` (opus, since the ticket
   touches money/ledger writes): `.../scratch-281/spec-critic.md`.
6. Reconciled the critic's findings directly into spec.md (§1b lists all
   five fixes) and updated preflight Part D/E and the gate check.
7. Wrote the build plan (`.../scratch-281/build-plan.md`, 6 build steps +
   3 gate steps — within the 6-step/10-state sizing limit, no split needed)
   and mirrored its status lines onto the hand-off.
8. Updated and committed the hand-off with `milestone: spec-done`.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Bulk Post button enablement: gated on client-known eligibility or always enabled on non-empty selection? | Always enabled on non-empty selection; eligibility is only ever shown/gated inside the dialog | H9/H5 — hiding a selection's problem behind a disabled button is worse than naming it inside the dialog; matches existing `pushAllReadyDocumentsAction` pattern of resolving server-side |
| 2 | Does a server-ineligible-at-confirm row get a silent count reduction or a visible failure? | Visible failure — same Ledger-mark/open-check/toast-breakdown path as any other failed post | Spec critic flagged the silent version as a P1 (H9); CLAUDE.md's error-recovery bar requires errors named at their source |
| 3 | Is the "Nothing un-posts" consequence line required on the bulk dialog, or only the single-doc one? | Both | Spec critic flagged this as an H5 gap; the bulk action is the higher-consequence one, so requiring it there and not the single-doc path would have been backwards |
| 4 | `PushToAccountingCard` — delete now or leave for a sign-off ticket? | Delete as housekeeping (zero importers, confirmed by recon) | Map #226's removal protocol (#237) only applies to removing a *live* feature; dead code with no importers isn't a removal decision |

## Decisions recorded
This session did not close #281 — it is a phased build ticket and only the
spec phase ran. No resolution comment/close yet; that happens at the close
phase per `phases/close.md`.

## Tickets created / changed
None. The plan fit within the 6-step/10-state sizing limit, so no split
into a child ticket was needed (spec step 6 of the phase brief).

## Scores and counts (rendered surfaces only)
scores: predicted-critique=34 first-critique=n/a close-critique=n/a first-evaluate=n/a close-evaluate=n/a

No code built yet — first-pass/close numbers land at the measure/close
phases. Predicted numbers are the spec-critic-reconciled ones (preflight
Part D): critique 34/40 (three 4s: H1, H5, H8, zero heuristic under 3),
evaluate sum 4 (P0 0, P1 0, P2 1), verdict Clean.

## First-pass findings → lessons (rendered surfaces only)
Not applicable yet (no build). One process lesson worth recording now:
self-scored Part D/E in the pre-flight template can be materially optimistic
even when filled honestly — this session's self-score (critique 32, evaluate
sum 4, zero P1) missed 3 real P1s (H1, H9, H5) that the independent opus
critic found on the same spec text. Recorded in project lessons.

## Needs the owner
Nothing yet. One item to flag for the owner's awareness at close, not a
blocker: `PushToAccountingCard` is being deleted as housekeeping (dead code,
zero importers) rather than routed through the removal sign-off protocol —
if the owner disagrees with that classification they can flag it before the
close phase's commit lands.

## Session 2 (build, step 1)
Built build-plan.md's step 1: the selection-driven bulk Post server action.

- `lib/integration-push-selection.ts` — new pure module: `resolveSelectionEligibility`
  (re-checks status/pushable-type/currency-conversion/category-confirmed, plus
  cancelled and already-posted which `pushDocumentToConnection` doesn't check
  itself, per spec.md §2) and `filterOverridesToEligible` (spec.md §3.1's
  "account override only applied to server-confirmed eligible ids"). Kept out
  of the `"use server"` action file so it's unit-testable without a DB, same
  split `lib/integration-push-policy.ts` already uses.
- `lib/integration-push-selection.test.ts` — 5 tests: every ineligibility
  reason returns a named reason (never a silent drop, check (a) from the
  build plan), currency gate is skipped when already in base currency, and
  `filterOverridesToEligible` drops an override for a non-eligible id (check
  (b)).
- `app/(app)/workspaces/[workspaceId]/post-selected-documents-actions.ts` —
  new `postSelectedDocumentsAction(workspaceId, connectionId, documentIds,
  accountOverrides?)`: fetches the selected docs + already-succeeded pushes
  server-side, re-resolves eligibility per id via the pure module (every id
  in the selection gets exactly one outcome row — ineligible ids never
  silently vanish), then pushes only the eligible set through
  `pushDocumentToConnection` with overrides filtered to eligible ids only.
- `app/(app)/workspaces/[workspaceId]/integration-push-actions.ts` — exported
  the previously-internal `pushDocumentToConnection` so the new action can
  reuse it (no duplicated push logic).

No UI wiring in this step (per the build plan) — that's step 2.

Checks: `npx tsc --noEmit -p .` clean; `npx vitest run
lib/integration-push-selection.test.ts` — 5/5 passed. Did not run the full
test suite (phase brief: affected tests only).

Commit: `wip(autopilot): #281 build step 1`. Hand-off updated with
`milestone: build-step-1-done`, build-plan.md step 1 marked done, "Next
session" pointed at step 2.
