# Autopilot report — #293 Decide whether the Override-mode and module-pill toggles adopt the shared Switch primitive (B4, from #262)

- Map: #226 Make the operator app work like Vic.ai (grammar, not skin)
- Type: wayfinder:grilling · Started: 2026-09-20 · Finished: 2026-09-20
- Outcome: resolved

## What I did
1. Claimed #293 (`gh issue edit 293 --add-assignee @me`).
2. Read the two existing toggle markups: `components/queue/queue-screen.tsx:524-531` (Override Mode, a Popover menu item) and `components/workspace/module-row.tsx:64-67` (module pill, Admin › Configuration › What's on — the ticket named `components/admin/module-row.tsx`, which doesn't exist; the component lives at `components/workspace/module-row.tsx` and is used from the Admin Configuration route, confirmed by grep).
3. Ran a grilling round (Impeccable `clarify`/`layout`, Intent `organize`) against the shared `Switch` primitive (`components/ui/switch.tsx`, #262) — see the resolution comment for the three questions and answers.
4. Created the execution ticket [Swap the module-pill toggle to the shared Switch primitive (#330)](https://github.com/petrose99/Wes-doc/issues/330), wired as a GitHub sub-issue of #226.
5. Posted the resolution comment on #293 and closed it.
6. Appended one line to #226's Decisions so far.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Is the module pill (`module-row.tsx`) the same concept as the Switch primitive? | Yes — adopt `Switch` | Both are a plain label + binary control row; no other affordance bundled in (matches the Keyboard dialog's off switch, `Switch`'s first user). |
| 2 | Is the Override Mode menu item the same concept? | No — stays its own markup | It bundles a stateful icon, a two-line state-dependent title, and a description sentence, and the whole row is a menu-closing click target — a compound menu action, not a label+control settings row; forcing it into `Switch`'s shape would drop the icon/description or require new layout work not asked for. |
| 3 | Does leaving Override Mode alone reopen the second toggle vocabulary B4 was meant to close? | No, if scoped correctly | `Switch` becomes the primitive for *label + control settings rows*; a *stateful menu action* is a distinct, smaller concept with its own affordance. Intent's one-term-per-concept rule applied at the right grain treats these as two concepts, not two spellings of one. |

## Decisions recorded
Module pill (`components/workspace/module-row.tsx`) adopts the shared `Switch` primitive — execution ticket #330. The Override Mode menu item (`components/queue/queue-screen.tsx`) stays its own compound-menu-action markup — documented as a distinct concept, not a second toggle vocabulary.

## Tickets created / changed
- Created [Swap the module-pill toggle to the shared Switch primitive (Admin › Configuration) (#330)](https://github.com/petrose99/Wes-doc/issues/330), label `wayfinder:task`, sub-issue of #226, parent-linked to #293's resolution.
- Closed #293 with the resolution comment above.
- No fog graduated or added — this ticket's question was already sharp (from #262); no new fog surfaced.

## Scores and counts (rendered surfaces only)
Not applicable — decision ticket, no rendered surface built or scored this session. #330 (execution) will carry the detector before/after counts.

## First-pass findings → lessons (rendered surfaces only)
Not applicable.

## Needs the owner
Nothing deferred. No removal involved (Override Mode's markup is kept, not removed); the module-pill swap is a straightforward primitive adoption, not a removal.
