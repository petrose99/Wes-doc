# Autopilot report — #270 Build Search on the Queue-screen shell

- Map: #226 (post-login UX)
- Type: Build · Started: spec phase (earlier session) · Finished: 2026-09-20
- Outcome: resolved

## What I did
1. Spec phase: wrote the Search surface spec (reuses `QueueScreen`/`QueueCard`/`QueueColumn`, new
   Status-facet primitive shared with #344, Popover-based `?` syntax help). Trimmed the five typed
   queues' Closed-facet retrofit + Archive verb + signed removals to #344 (blocked by #270).
   Commit `c5ff167`.
2. Build phase (multiple sessions, steps G1–G3 + earlier numbered steps): built
   `app/(app)/workspaces/[workspaceId]/search/page.tsx` + `search-client.tsx`, wired the shared
   queue primitives, chip-syntax parsing, empty/loading/gone states. Commits `03054af`…`ec0f62a`.
3. Measure phase: capture round r1 (1440/390, all states), three fresh readers (evaluate, critique,
   include). Commit `239ba71`.
4. Close phase (this and a prior session):
   - Triaged r1 findings in `docs/wayfinder-reports/226/logs/scratch-270/close.md`: 2 real (result-gone
     param not wired; zero-results default-copy leak), 2 residue (shell-wide truncate + palette/glow,
     matches `queue-shell.md`/`detail-pane.md`), 1 false positive (archived mark misread by reader,
     verified against screenshot), 1 tooling call (dev-overlay route-transition spinner misread as a
     missing skeleton, verified against code), 1 recorded gap (phone keyboard-probe coverage,
     not a defect).
   - Fix batch 1: wired `?gone=<id>` → `initialGone` → `initialMissing` banner
     (`search/page.tsx`, `search-client.tsx`); `filteredBody: ""` on Search's empty-state prop
     (`search-client.tsx`, `components/queue/queue-empty.tsx`).
   - Gate c1: one reverted wait-bump experiment (see close.md), then clean — 0 real findings, 0 new
     vs. baseline, 21/21 keyboard probes pass.
   - Confirm round c1: fresh evaluate/critique/include readers on the same capture set.
   - Checks: full vitest suite (2803 tests), `tsc --noEmit`, `eslint` on the 3 changed files,
     `next build` — all clean.
   - This session: wrote lessons (generic + project), added a Search section to
     `docs/agents/areas/queue-shell.md`, wrote this report, closing the ticket. Commit `ca53014`
     carries the fix batch; this session's commit carries lessons/primer/report.

## Questions I answered on the owner's behalf
None this session — all remaining work was bookkeeping (lessons, primer, report) on a ticket that
had already cleared the bar.

## Decisions recorded
Search ships as the one cross-type destination, reusing the queue shell's primitives with a new
shared Status-facet component (wired into the five typed queues by #344) and a persistent syntax-help
popover; result-gone and zero-results copy match the shell's existing patterns exactly.

## Tickets created / changed
- No new tickets. #344 (Closed-facet retrofit + Archive verb + signed removals on the five typed
  queues) remains blocked on #270 and can now be unblocked — #270 is closing.
- Map #226 Decisions so far: append — "#270 Search on the Queue-screen shell — cross-type search
  reusing queue-shell primitives; Closed-facet retrofit split to #344."

## Scores and counts (rendered surfaces only)
scores: predicted-critique=34 first-critique=29 close-critique=37 first-evaluate=78 close-evaluate=88

Gate counts: r1 baseline → c1 clean (0 real findings outside residue at both 1440 and 390; 21/21
keyboard probes pass; 0 errored/page-error states). include: Pass on the confirm round (r1's P1 was
confirmed pre-existing #297 shell-chrome residue). Weak pre-build step: Part B6 (time-axis edge)
named the `result-gone` state in the copy table but didn't check the query param actually crossed
the server→client boundary — see lessons entry below.

## First-pass findings → lessons (rendered surfaces only)
| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| `result-gone` renders as empty-query reset, not the "no longer available" notice (critique P1, H1/H9) | Part B6 named the state in the copy table, not the param wire-up | unchecked | Wired `?gone=<id>` → `initialGone` → `initialMissing`; wrote lessons entry with a `check:` (any spec state with a query param needs a matching `searchParams.<name>` read in `page.tsx`) |
| Zero-results shows an extra sentence not in spec copy (critique P2/H9) | `queue-empty.tsx`'s default `filteredBody` was assumed to no-op when omitted | wrong | Passed `filteredBody: ""` explicitly; wrote lessons entry (every prop a shared empty-state component defaults gets an explicit value from a new caller) |
| Archived rows show no distinct mark (evaluate P1) | none — reader misread a correctly-rendered second pill | new class (reader error, not a spec/contract gap) | Verified against `archived-row-1440.png`; re-scored on confirm, no code change |
| Phone pane spinner-in-box instead of skeleton (evaluate P1) | none — capture-wait artifact (dev route-transition spinner), not app code | new class (measurement gap) | Verified against code (`detail-pane.tsx:267`); documented in close.md; no fix needed |

## Needs the owner
None. #344 is unblocked now that #270 is closing; no removal or credential decision was required
this session.
