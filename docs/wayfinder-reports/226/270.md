# Autopilot report — #270 Build Search on the queue shell and the Closed facet on every typed queue; retire the Archive destination

- Map: #226 Make the operator app work like Vic.ai (grammar, not skin)
- Type: wayfinder:task · Started: 2026-09-20 · Finished: 2026-09-20
- Outcome: partial (spec phase complete, build not started this session)

## What I did
1. Claimed #270 (`gh issue edit 270 --add-assignee @me`).
2. Read #270, map #226, #245's resolution comment (Archive's fate) and #269's
   owner sign-off (approved 2026-09-20, all lines signed).
3. Found the ticket's combined scope (new Search surface + Closed-facet
   retrofit on five typed queues + Archive verb + five signed removals)
   exceeds the phase budget (>6 build steps, >10 captured states). Split at
   the surface-family boundary: created
   [#344](https://github.com/petrose99/Wes-doc/issues/344) for the
   Closed-facet retrofit + Archive verb + signed removals, blocked by #270;
   trimmed #270's body to the Search surface only; posted the split
   rationale as a comment on #270.
4. Read the queue-shell area primer and the existing code: `/search` route
   and `search-client.tsx` already exist from #262 as a bare placeholder
   (five legacy scopes + dead "Ask AI" mode, not on the Queue-screen shell,
   wrong predicate reused from `models/library-facets.ts`/`LIBRARY_WHERE`
   which excludes non-reviewed and archived documents — the exact defect
   #245's evidence named).
5. Wrote the screen-by-screen spec (`docs/wayfinder-reports/226/logs/
   scratch-270/spec.md`): desktop list, phone entry, six states (`fortify`),
   copy table (`articulate`), keyboard/focus plan (`include`), and the B4
   primitive-reuse list.
6. Ran an independent spec critic (fresh-context Agent, sonnet): first pass
   31/40, P1×2 on H6/H10 (one-shot syntax hint with no way to recall it),
   H5=2 (malformed chip syntax unspecified). Fixed the spec (§2a): a
   persistent `?` syntax popover (reusing the shared Popover primitive) and
   an explicit statement that malformed chip syntax degrades safely to
   plain-text search (true by construction in `lib/global-search.ts`).
   Reconciled prediction: critique ≥34/40, no heuristic <3, evaluate 0
   P0/P1, health ≥88, verdict Clean.
7. Filled the pre-flight (`.../scratch-270/preflight.md`) — Parts A–E, gate
   passed on the reconciled prediction.
8. Wrote the hand-off (`docs/wayfinder-reports/226/270.handoff.md`) with a
   6-step + 3-gate build plan naming files, checks and spec sections per
   step.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Does the pane's Archive action survive (#269's open question)? | Yes, kept as the narrowly-scoped verb #245 specified (deferred to #344, which builds it) | #269's sign-off comment ("approved as described") signs the removal lines; the Archive-verb *survival* question reads as answered by #245's own decision (kept as verb only), not reopened by the sign-off comment's brevity |
| 2 | Split the ticket now, at spec, or attempt it whole? | Split now, before code | Spec-phase sizing rule: >6 build steps / >10 states must split at a surface-family boundary the map would recognise |

## Decisions recorded
Map #226 Decisions so far — no new line yet (ticket not closed; will append
"Build Search…" line at close, not spec).

## Tickets created / changed
- Created [#344](https://github.com/petrose99/Wes-doc/issues/344) "Fold the
  Closed facet and reversible Archive verb into the five typed queues;
  retire the Archive destination (#245, #269)", `wayfinder:task`, blocked by
  #270.
- Edited #270's body to trim scope to the Search surface; posted a comment
  recording the split.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=34 first-critique=n/a close-critique=n/a first-evaluate=n/a close-evaluate=n/a
No code changed this session (spec phase only) — no first-pass/close counts
yet; the reconciled prediction (34/40, 0 P0/P1, health ≥88, Clean) is the
gate the build phase must hit.

## First-pass findings → lessons (rendered surfaces only)
N/A — no build yet this session.

## Needs the owner
Nothing new. #269 (owner sign-off) is closed and approved; #344 (created
this session) needs no sign-off of its own — it only executes lines #269
already signed, plus the Archive-verb build #245 already decided.

## Session 2 (build, step 1)
- Read hand-off (spec-done), spec.md §1/§2a/§5.
- `components/shell/sidebar.tsx`: added the **Search** rail item (top of
  spine, above the typed groups, in both immersive and non-immersive
  layouts); no `g` shortcut added (`/` already reserved, #262).
- `app/(app)/workspaces/[workspaceId]/search/search-client.tsx`: rewritten
  onto `QueueScreen` — header search box (`id="search-input"`) in the
  `views` slot (replacing Views/Sort), `Search` rail's `/` focus-handoff
  kept unchanged, Ask AI/Sparkles/`AssistantPanel` removed. Columns/predicate
  are a step-1 skeleton (still `globalSearchAction`) — step 2 replaces them.
- `app/(app)/workspaces/[workspaceId]/search/page.tsx`: dropped the `ask`
  param/redirect now that Ask mode has no UI.
- `components/shell/global-search.tsx`: removed the command-palette's
  "Ask AI" shortcut into `?ask=1` (dead now that the page ignores it) and
  its now-unused `Sparkles` import.
- Checks: `tsc --noEmit` clean repo-wide; `eslint` on the four changed files
  (one pre-existing warning, inherited from the prior implementation, not
  introduced here); no `AssistantPanel` import left in `search-client.tsx`.
  No test suite covers these components (no `*.test.*` for search/sidebar).
- Commit: `03054af` "wip(autopilot): #270 build step 1".
- Hand-off updated: step 1 marked `done`, `## Next step` points at step 2
  (predicate/columns/facets).
