milestone: spec-done

Sizing: split at spec time. Kept on #331: Bill Pay queue claim rows + claim-surface copy.
Spawned #347 (blocked by #331): Payment batches Reimbursements group + mixed-selection
batching (createPaymentBatches doesn't accept claim rows yet). Comment on #331 explains it.

## Artifacts
- docs/wayfinder-reports/226/logs/scratch-331/spec.md
- docs/wayfinder-reports/226/logs/scratch-331/preflight.md (gate: predicted critique 34/40,
  evaluate ≈87, 0 P0/P1, Clean — spec-critic reconciled)
- docs/wayfinder-reports/226/logs/scratch-331/batches-ticket-body.md (#347's body, filed)

## Build plan (one step per session; each ticks the preflight's Part B/C rows as it lands)
- step: 1 — models: `lib/claims/facts.ts` gains `paidState`/`paidAt`/`paidBy` on
  `DocumentClaimFacts` (one shared derivation with `models/bill-pay.ts`'s `listClaimRows`, not
  a second copy); `CLAIM_ELIGIBILITY_COPY` map beside `ELIGIBILITY_COPY` — done
- step: 2 — `components/payments/bill-pay-queue.tsx`: widen to the `BillPayRow` union via
  accessor helpers (rowLabel/rowDue/rowAmount/rowId); Payee header; claim row per-column
  rendering incl. read-only amount, Claim mark, row-level "Batching claims ships next" line
  (spec §2, critic fix); Payee-kind facet — todo
- step: 3 — detail pane: claim branch in the loader, renders `<ClaimSection readOnly>`; Paid
  fact line; pane actions/menu claim branches (mark-as-paid/remove-records action wrappers) —
  todo
- step: 4 — claim-surface copy: claim-card Status line 3-way branch on `paidState` (spec §4,
  critic fix — single derivation shared with the row); `ApprovalTimeline` "Paid" entry;
  approve-confirm sentence clause — todo
- step: 5 — fortify pass: null-currency render guard, left-workspace pane suppression,
  scheduled-claim batch link, remove-records reversal — todo
- step: 6 — CONTEXT.md glossary entry for "Payee"; docs/agents/areas/payments.md gets a short
  note pointing at #331's claim-row additions (full rewrite rides on #347 once both surfaces
  exist) — todo
- step: G1 — round script (reuse scratch-296/scratch-297 harness shape) capturing the 8 states
  named in preflight Part C/E at 1440+390 — todo
- step: G2 — gate.mjs against baseline + residue (docs/agents/areas/payments.md's app-wide
  four), fix round — todo
- step: G3 — tsc --noEmit clean on changed files, affected vitest (models/bill-pay.test.ts,
  facts test if any), build-done — todo

## Open findings
None yet — pointer will be docs/wayfinder-reports/226/logs/scratch-331/build-notes.md once
build starts.

## Next step
Build session: run step 2 (bill-pay-queue.tsx widen to BillPayRow union, Payee header, claim
row rendering, Payee-kind facet).
