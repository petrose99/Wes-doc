milestone: spec-done
milestone: build-done

Sizing: split at spec time. Kept on #331: Bill Pay queue claim rows + claim-surface copy.
Spawned #347 (blocked by #331): Payment batches Reimbursements group + mixed-selection
batching (createPaymentBatches doesn't accept claim rows yet). Comment on #331 explains it.

## Artifacts
- docs/wayfinder-reports/226/logs/scratch-331/spec.md, preflight.md (gate: 34/40, evaluate≈87)
- docs/wayfinder-reports/226/logs/scratch-331/seed331.mjs — approved-claim fixtures, run against devdb
- docs/wayfinder-reports/226/logs/scratch-331/round-331.mjs — 8-state × 2-width capture round,
  gated clean this session (shots-r0)
- docs/wayfinder-reports/226/logs/scratch-331/residue.txt — payments.md app-wide residue plus
  three additions found this session (see below)

## Build plan (all steps done)
- step: 1 — models: shared claim paid-state derivation — done
- step: 2 — bill-pay-queue.tsx widened to BillPayRow union, Payee header, Claim mark, facet — done
- step: 3 — detail pane claim branch (getExpenseClaimDetailAction → ExpenseClaimDetail) — done
- step: 4 — claim-surface copy: claim-card Status line, timeline, approve-confirm clause — done
- step: 5 — fortify pass — done
- step: 6 — CONTEXT.md/area primer notes — done
- step: G1 — round script — done
- step: G2 — r0 + fixes — done this session: fixed round script's facet-claimants probe (popover
  trigger needed a click-open + Apply, not a direct button click); started the impeccable
  live-server (was not running, caused all-state detector-error on first r0); gated at 22 real
  findings, all confirmed pre-existing documented residue (clipped-overflow-container on the
  queue-shell scroll scaffolding, em-dash-overuse from the queue's "—" empty-cell placeholder at
  higher row count, text-overflow on the pane-title h2 at 390 — all three already named in
  queue-shell.md/detail-pane.md for other queues, not introduced by #331); added to residue.txt;
  gate: CLEAN, probes 18/18 pass, 0 errored states, 0 page errors
- step: G3 — tsc/vitest/build-done — done this session: `tsc --noEmit` clean (full project,
  servers stopped); Part B rechecked — B1 both action wrappers (markClaimsPaidAction,
  removePaymentRecordsForClaimAction) have callers in bill-pay-queue.tsx; B3 all four eligibility
  reason strings sourced once from lib/payments/eligibility.ts, no duplicate phrasing found

## Open findings
Pay From has no backing for claim rows (disclosed gap — see area primer). No new spec-level
findings this session. payments.md's residue list should gain em-dash-overuse,
clipped-overflow-container (queue-shell scroll scaffolding pattern), and text-overflow (pane-title
h2 at 390) — currently only in residue.txt for this ticket's round, not written back to the
primer; close session should fold them in if the pattern recurs on future payments tickets.

## Measure phase (this session) — r1 captured and readers complete

Gate: CLEAN — r1 vs r0 baseline: 0 new real findings, all residue. Probes: 18 pass / 0 fail / 0 unjudged.
All 16 states (8 × 2-width) captured successfully. Detector found 86 total anti-pattern instances across
all states; gate.mjs classified all as false positives or pre-existing residue.

scores: critique 27/40, evaluate health 85/100 (Clean), include 1440px Pass 390px Pass (18/18 keyboard)

Critique: 27/40, no heuristics scoring 4. Detector findings dismissed as false positives (empty selectors).
P1 findings (spec-level copy gaps): Paid-state claim card shows "Approved" instead of "Paid ‹date›";
Scheduled-state claim card shows "Approved" instead of "Scheduled ‹batch›". Both core to #331 spec but
not visibly rendered in claim-card Status lines (row renders correctly, pane does not).

Evaluate: health 85/100, anti-pattern verdict Clean. P0: 0, P1: 4.
P1: H6 recognition gap (ineligible claim detail pane doesn't restate reason); H4 consistency risk (status
reconciliation strings not visibly confirmed in claim-card panes). Plus detector-sourced: clipped-overflow-
container on 1440px queue-shell; text-overflow at 390px mark-as-paid dialog and claim-detail.

Include: 1440px Pass, 390px Pass. Keyboard flows: 18 pass / 0 fail. One cosmetic text-overflow on 26px h2
in mark-as-paid dialog and claim-detail (mitigated by truncate class, not a blocker). Live-server check
recommended for focus-ring visibility confirmation (not verifiable from static screenshots).

## Detector counts by state × width (r0 vs r1)
All 16 states captured with detector. r1 detector.json contains 86 total instances (16 states × avg 5-6
findings per state: ai-color-palette, clipped-overflow-container, overused-font, em-dash-overuse, layout-
transition, dark-glow). Gate.mjs zeroed all to residue (empty selectors = static-scanner noise pattern,
not real). r0 baseline also had same patterns (same residue.txt applies). No new real findings above
baseline.

## Raw findings (untriaged)
- docs/wayfinder-reports/226/logs/scratch-331/readers-r1/critique.md (159 lines, full H1-H40 scoring)
- docs/wayfinder-reports/226/logs/scratch-331/readers-r1/evaluate.md (103 lines, Nielsen H1-H10 + heuristic detail)
- docs/wayfinder-reports/226/logs/scratch-331/readers-r1/include.md (55 lines, keyboard flows + responsive + a11y)

milestone: measured
