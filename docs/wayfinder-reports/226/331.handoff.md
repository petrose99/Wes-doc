milestone: spec-done
milestone: build-done
milestone: measured

Sizing: split at spec time. Kept on #331: Bill Pay queue claim rows + claim-surface copy.
Spawned #347 (blocked by #331): Payment batches Reimbursements group + mixed-selection batching.

## Close phase (this session)

Triage + close state lives in `docs/wayfinder-reports/226/logs/scratch-331/close.md` — read it
first, it is the source of truth for what's been tried.

Fix batch 1 (seed-only): `seed331.mjs` was missing the `expense_claim_status_changed` audit event
real approvals always write, so `facts.approval` was null in fixtures and the (already-correct)
claim-card paid/scheduled confirmation lines never rendered. Added `ensureApprovalEvent()`. Gate
CLEAN vs r1 baseline (gate-c1.json). Confirm-round readers on shots-c1 scored: critique 35/40 (no
heuristic <3, five 4s — readers-c1/critique.md), include Pass/Pass 18/18 keyboard
(readers-c1/include.md), evaluate health 88/100 but **1 P1 still open**
(readers-c1/evaluate.md): the ineligible-claim pane (08, Sipho Dlamini/needs bank details) — once
the fix made the confirmation line actually render — shows "Ready to pay" while the CTA is "Add
bank details", a self-contradiction (H6 gap, worse than before because the line is now visible).

Fix batch 2 (in progress, NOT yet captured/gated/confirmed): added `paymentEligibility` to
`DocumentClaimFacts` (`lib/claims/facts.ts`) — same `claimPaymentEligibility` check
`BillPayClaimRow.eligibility` runs (exported from `models/bill-pay.ts`, was private
`claimEligibility`, renamed to avoid clashing with the unrelated `lib/claims/eligibility.ts`
export of the same name already imported in `models/expense-claims.ts`). Wired into
`buildClaimFacts` (`models/expense-claims.ts`: fetches the submitter's `WorkspaceMember` bank
fields in parallel with `loadClaimPaidFacts`, only for approved claims). `claim-card.tsx`'s
"unpaid" status line now branches: if `paymentEligibility.eligible === false`, prints
`CLAIM_ELIGIBILITY_COPY[reason]` (e.g. "Needs bank details") instead of "Ready to pay", amber not
emerald.

**Not yet done — next step, in order:**
1. `tsc --noEmit` on the whole project (this diff touches 4 files: `lib/claims/facts.ts`,
   `models/bill-pay.ts`, `models/expense-claims.ts`, `components/queue/claim-card.tsx`) — verify
   the new field/import compile cleanly before anything else.
2. Dev server + live-server (`.impeccable/live/dev.mjs status` — likely still running from this
   session, started via `dev.mjs start` + `livesrv257.mjs`) — reuse if up, else restart per
   `docs/agents/areas/payments.md`.
3. Re-run `node docs/wayfinder-reports/226/logs/scratch-331/round-331.mjs c2` (seed already has
   the approval events from batch 1, no reseed needed unless devdb was reset).
4. Gate: `node scripts/wayfinder-autopilot/gate.mjs shots-c2 --baseline shots-c1 --residue-file
   residue.txt --json gate-c2.json`.
5. If gate clean: contact sheet + 3 fresh sonnet reader agents on shots-c2 (same prompts as
   readers-c1, updated to say the ineligible-pane fix is what's being re-checked) →
   readers-c2/*.md. This is confirm round 2 of at most 3 total gate rounds (1 used).
6. If bar met (critique ≥32/40 no heuristic <3, evaluate ≥85 Clean/no P0-P1, include Pass/Pass):
   run checks-once (affected tests → full suite → `tsc --noEmit` → `eslint` on changed files →
   `next build`, dev server stopped first), write lessons back (generic +
   `.claude/wayfinder-autopilot/lessons.md`), update `docs/agents/areas/payments.md` (§#331, plus
   fold the em-dash-overuse/clipped-overflow-container/text-overflow residue additions noted in
   the prior hand-off — still not folded in), write the report at
   `docs/wayfinder-reports/226/331.md`, commit, close #331.
7. If a P1 remains after round 2: one more batch allowed (round 3 max), else hand off again with
   the P1 named as a gap or spun into a ticket.

## Artifacts
- `docs/wayfinder-reports/226/logs/scratch-331/close.md` — triage + fix batch log, keep current
- `.../spec.md`, `preflight.md`, `seed331.mjs` (now includes approval events), `round-331.mjs`
- `.../residue.txt`, `.../gate-c1.json`, `.../contact-c1.png`
- `.../readers-r1/*.md` (measure baseline), `.../readers-c1/*.md` (confirm round 1)
- Diff not yet committed: `lib/claims/facts.ts`, `models/bill-pay.ts`, `models/expense-claims.ts`,
  `components/queue/claim-card.tsx`, `docs/wayfinder-reports/226/logs/scratch-331/seed331.mjs`

## Next step
Run `tsc --noEmit`, then round c2 + gate + confirm readers per the numbered plan above; do not
re-derive the triage, it's in close.md.
