# Autopilot report — #244 Decide the way back: one Origin-context restore for Exceptions, Archive, Finance failed rows and the standalone document page

- Map: #226 Make the operator app work like Vic.ai (grammar, not skin)
- Type: wayfinder:grilling · Started: 2026-09-16T00:27Z · Finished: 2026-09-16T00:41Z
- Outcome: resolved

## What I did
1. Loaded the map and the ticket; claimed #244 (assigned myself).
2. Called the Skill tool for `grilling`, `domain-modeling`, `intent` (routed to `journey`) and `impeccable` (`shape`). `impeccable context` did not run — the launcher is denied by the sandbox — so I read `PRODUCT.md` and `shape.md` directly (no `DESIGN.md` exists).
3. Read the evidence: the three #239 hand-off critiques (`.impeccable/critique/2026-09-15T22-25-51Z__*`), the journey map's hand-off table (H3/H6/H7/H11/H13/H14), the tour findings (no back affordance recorded in Vic), `CONTEXT.md` "Origin context", and the code: `queue-screen.tsx` `syncUrl` (replaceState, keeps the query), `exception-queue.tsx:123`, `documents/[documentId]/page.tsx:167,308-313`, `accounting-dashboard.tsx:355-361`, the four `[documentId]/page.tsx` deep-link routes, `sidebar.tsx` bare rail links.
4. Ran two grilling rounds (14 questions) and took every `➡️` recommendation.
5. Sharpened **Origin context** and added **Origin link** in `CONTEXT.md`.
6. Posted the resolution comment (https://github.com/petrose99/Wes-doc/issues/244#issuecomment-5690208818), closed #244, appended the decision to the map's Decisions so far, added one fog line.
7. Created the execution ticket [#268](https://github.com/petrose99/Wes-doc/issues/268) as a sub-issue of #226; posted inputs to #249 and #245.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | What "back" is | Browser Back and one visible Origin link, agreeing | Intent "make intent visible"/wayfinding; Nielsen H3; tour records no Vic affordance |
| 2 | What counts as a hop | The four ticket cases + any cross-surface row link; rail is not a hop | Journey map H6/H7/H11/H13/H14 |
| 3 | What is carried | The origin URL verbatim (row + query) | URL already is the state (`syncUrl`, facets in URL); CONTEXT.md Origin context |
| 4 | Transport | `from` query param, same-workspace validated | Only option that survives "Open in a new tab" and refresh; smallest reversible |
| 5 | Lifetime | Navigation only | Vic has no persistent breadcrumb; hidden memory = Assumption of Context |
| 6 | Fix H3 (rail drops filters) by memory? | No; rail stays bare, saved View is durable | Vic's rail gives the default View; browser Back restores the URL |
| 7 | Placement | 32px queue-level strip above header row 1 | Hop is queue-level; #234's pane budget ≤96px; one component, three hosts |
| 8 | Copy | `← Back to Invoices · Acme Corp · INV-1042` | Articulate: name destination + what it restores, no narration of filters |
| 9 | Row left the origin list | Keep filters, inline notice + *Show it* | Anti-pattern cat. 9 Dead Ends; honest |
| 10 | Missing/invalid origin | Strip hidden; standalone Back = typed queue, never `/pipeline` | #243 closes `/pipeline`; #249 |
| 11 | × / Esc = back? | No, pane-local only | Consistency with #234; Inconsistent Patterns |
| 12 | Exceptions/Finance link | In-tab **Open on Invoices** with `from`; new-tab stays in ⋯ | Not a removal; Vic-neutral |
| 13 | Keyboard/phone | No new key; first focusable; ≥44px | #240 no key acts on data; #232 sheet pushState |
| 14 | Rendering | Slate link, not emerald, hairline | Map Q2 emerald = primary action; craft floor |

## Decisions recorded
One Origin strip + one `from=` URL for every cross-surface hop; browser Back always agrees; navigation lifetime; rail bare; filtered-out notice instead of a dropped pane; × never leaves the queue; Exceptions and Finance gain "Open on Invoices"; no `/pipeline` fallback.

## Tickets created / changed
- Created [#268 Build the Origin link: one strip and a from= URL for every cross-surface hop](https://github.com/petrose99/Wes-doc/issues/268) (task), sub-issue of #226, unblocked.
- #249 and #245: input comments (fallback rule, `from` on Archive results).
- Map: #244 appended to Decisions so far; fog added: per-queue filter memory only on operator demand.

## Scores and counts (rendered surfaces only)
No score; the affordance is not built yet. Incumbent evidence cited: Exceptions H3 = 2, Archive H3 = 1, Finance H9 = 0 (`.impeccable/critique/2026-09-15T22-25-51Z__*`), not re-run.

## Needs the owner
Nothing. No removal was performed or proposed. `impeccable context` could not run in this sandbox (permission denied) — context was read from `PRODUCT.md` directly.
