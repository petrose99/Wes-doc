# Autopilot report — #299 Polish: give the shared Dialog title its own type step

- Map: #226 (post-login UX implementation)
- Type: wayfinder:task · Started: 2026-09-20 04:50 UTC · Finished: 2026-09-20 05:05 UTC
- Outcome: resolved

## What I did
1. Claimed #299 (`gh issue edit 299 --add-assignee @me`).
2. Read `components/ui/dialog.tsx`: the shared `Dialog`'s `<h2>` title used
   `text-lg font-semibold` — identical to `QueueScreen`'s page `h1#queue-title`
   (`text-lg font-semibold`) — so the in-page detector reads `flat-type-hierarchy`
   on every dialog-open state, per the ticket's own diagnosis.
3. Bumped the title to `text-xl font-semibold` (`components/ui/dialog.tsx:118`),
   one step above the page h1's `text-lg` and above the body's `text-sm`/`text-base`;
   left description and footer untouched, per scope. Searched all callers of
   `Dialog`/`confirm-dialog` for local title-style overrides that would now
   double up — none found (`companies-queue.tsx`'s `text-lg` hits are on page
   `h1`s, not dialog titles). `ConfirmDialog` (a separate primitive, `text-base`)
   is out of this ticket's scope, untouched.
4. Checked `.claude/wayfinder-autopilot/lessons.md` and `docs/agents/areas/*.md`
   for a dedicated "dialog flat-type-hierarchy" residue entry to remove per the
   ticket's scope — found none; `docs/agents/areas/admin.md`'s only
   `flat-type-hierarchy` line is a distinct, still-valid residue (queue-shell
   `h1`/`h2` size clash, unrelated to the Dialog title), left as is. The
   historical mentions in `docs/wayfinder-reports/226/{264,285}.md` are past
   session reports, not lessons files, and are not edited.
5. Verified live: started `next dev` (port 3000, `docs/wayfinder-reports/226/logs/scratch-299/dev.log`)
   and `impeccable live-server --background` (port 8400), opened the
   "How DocuBite works" dialog (`components/shell/how-it-works.tsx`, reused by
   #264/#285's captures) via the account menu at 1440 and via its phone button
   at 390, and ran the in-page detector both widths
   (`docs/wayfinder-reports/226/logs/scratch-299/dialog-check.mjs`,
   screenshots `dialog-1440.png`/`dialog-390.png`).
6. Ran a keyboard check (`keyboard-check.mjs`): initial focus lands on the
   dialog's close button on open, Escape returns focus to `#account-menu-trigger`
   — unchanged from before the CSS-only edit.
7. `tsc --noEmit` shows only pre-existing unrelated errors (Prisma/model files
   untouched by this change).
8. Stopped the dev server and the live-server before ending the session.
9. Committed the fix, closed #299 with a resolution comment, appended one line
   to map #226's Decisions so far.

## Questions I answered on the owner's behalf
None — this was a task ticket (already-specified fix), not a grilling round.

## Decisions recorded
Dialog title bumped from `text-lg` to `text-xl` (still `font-semibold`), giving
it a type step distinct from the page `h1` (`text-lg`) and body copy
(`text-sm`/`text-base`). Clears `flat-type-hierarchy` on every dialog-open
state; no local title overrides existed to remove.

## Tickets created / changed
None created. No fog graduated — this was a pre-specified fix, not a decision
that opens new questions.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=n/a first-critique=n/a close-critique=n/a first-evaluate=n/a close-evaluate=n/a
Detector, "How DocuBite works" dialog-open state (reused from #264/#285's
capture set):
- @1440: 6 findings, all app-wide/shell residue (`ai-color-palette` ×2 avatar
  gradient, `overused-font` inter/body, `layout-transition` height/body,
  `dark-glow` body, `text-overflow` sidebar workspace-name truncation) — **no
  `flat-type-hierarchy`**.
- @390: 5 findings, same residue set minus the sidebar `text-overflow` (rail
  collapsed at that width) — **no `flat-type-hierarchy`**.
`include`: PASS both widths — initial focus on the dialog close button, Escape
returns to the opener (`#account-menu-trigger`), unchanged from before.
No full `critique`/`evaluate` re-run this session (small unphased CSS-scope
ticket, no reader agents spent) — the deterministic detector + keyboard check
directly answer the ticket's own bar (`flat-type-hierarchy` gone, focus
unchanged); "n/a" above is intentional, not skipped.

## First-pass findings → lessons (rendered surfaces only)
None — no new detector class introduced.

## Needs the owner
Nothing. No removal, no credentials, no ambiguous call.
