milestone: spec-done

step: 1 — model: `updateApprovalWorkflowFields` — todo — spec.md §Server; models/approval-workflows.ts; check: id-set mismatch throws, valid patch updates name+stages, stageIndex untouched (vitest or a quick tsx script)
step: 2 — action: `updateApprovalWorkflowAction` + id-keyed stage-row parser — todo — spec.md §Server; app/(app)/workspaces/[workspaceId]/approval-actions.ts; check: `grep -rn updateApprovalWorkflowAction components app` shows the new caller after step 3
step: 3 — `StageEditor` gains `locked` prop; `EditFlowForm` added to approval-workflow-form.tsx — todo — spec.md §Design approach; components/workspace/approval-workflow-form.tsx; check: `tsc` clean, create-flow path unchanged (locked omitted/false)
step: 4 — Edit toggle wired into approval-flows-editor.tsx row (single-open-at-a-time), Panel note copy replaced — todo — spec.md §Design approach + §Copy change; components/workspace/approval-flows-editor.tsx; check: `impeccable detect --json` static scan + tsc
step: 5 — live verify: seed a flow with 2+ stages, edit name/approvers/threshold, save, confirm row updates same tick; owner-gating (non-owner sees no Edit) — todo — check: manual dev-server pass, screenshot read+edit view at 1440/390
step: G1 — round script (capture states from Part C table at 1440/390, detector JSON per state) — todo
step: G2 — r0 capture + fix batch against Part B contracts — todo
step: G3 — build-done gate: critique ≥32 no heuristic <3, evaluate ≥85 0 P0/P1 Clean, detector clean to admin.md residue, include PASS both widths — todo

## Artifacts
- docs/wayfinder-reports/226/logs/scratch-328/spec.md
- docs/wayfinder-reports/226/logs/scratch-328/preflight.md (gate: PASS, predicted critique 33, evaluate 87)

## Open findings
(none yet — spec phase only)

## Next step
Build session: do step 1 (model function `updateApprovalWorkflowFields` in models/approval-workflows.ts per spec.md §Server), mirroring `replaceApprovalWorkflowStages`'s existing structure for the transaction and id-scoping.
