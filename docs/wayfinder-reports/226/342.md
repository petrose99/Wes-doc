# Autopilot report — #342 Build the reorganized rail (execution of #291)

- Map: #226 Post-login UX (reorganized rail, Admin surfaces)
- Type: wayfinder execution ticket · Started: 2026-09-18 (spec) · Finished: 2026-09-20 (close)
- Outcome: resolved

## What I did

1. **Spec** (`3c2da4e`, `92bf361`): specified §1–§5 (Today group + Archive move + Accounting rail
   item, Admin always-labelled row + Activity/Health moved into Admin nav, three-state rail-width
   persistence, centralized `isActive`, Help-dialog copy) against `articulate`/`impeccable shape`;
   spec critic predicted 33/40 critique, evaluate ≈90+, 0 P0/P1, Clean.
2. **Build**, five steps (`4fc6662`, `da3b45f`, `f0a76b6`, `02c59af`, `bb717dd`): Today
   group/Archive/Accounting item + `g c` shortcut; Admin always-labelled row + Activity/Health into
   `admin/layout.tsx`; `User.railWidth` schema + actions; `RailWidthControl`/`RailWidthMenuSelect`
   UI replacing the old `pinned`/`RAIL_PIN_KEY` localStorage toggle; centralized
   `RailNavItem.matchPrefixes` `isActive` + Help-dialog copy + `CONTEXT.md` rename. `tsc --noEmit`
   clean after every step.
3. **Gate** (`23562cb`, `9af8933`, `6654e2f`): round script (30 states × 2 widths); G2 found and
   fixed a real RSC bug (client-only helpers imported by server components — moved to
   `lib/rail-width.ts`) and widened the `text-overflow.*truncate` detector residue; G3 re-verified
   Part B contracts against shipped code and fixed a missed `router.refresh()` after a rail-width
   save. `gate.mjs`: clean, 0 real findings.
4. **Measure** (`bd714ec`, `0082bfd`): r1 capture (30 states, 0 errors), gate clean vs r0 baseline.
   Readers: critique 37/45 (P1: recurring `text-overflow` on `.truncate`), evaluate 92/100 Clean
   (P1: offline rail-width state `skipped-offline` by the detector harness), include Pass/Pass
   (P2/P3 verification gaps).
5. **Close** (this session): triaged both r1 P1s in
   `docs/wayfinder-reports/226/logs/scratch-342/close.md` — the `text-overflow` finding is
   **residue** (already in `residue.txt`, gate already excludes it, source-confirmed as
   by-design ellipsis truncation); the `skipped-offline` finding is **tooling** (the detector has
   no rule for that state, not a broken capture — confirmed by viewing
   `rail-width-control-offline-1440.png` directly: disabled select, `aria-live` offline copy, and
   a `role="alert"` error line, exactly matching `rail-width-control.tsx`'s spec'd state machine).
   No fix batch needed. Re-ran `critique` and `evaluate` as fresh `sonnet` agents on the same r1
   capture set with the triage reasoning: critique 39/45 (no heuristic <4, 0 P0/P1), evaluate
   96/100 Clean (0 P0/P1). Ran the deterministic checks once (dev server stopped): `tsc --noEmit`
   clean, `eslint` on the 9 changed/added files caught 3 real unused-import errors in
   `rail-width-control.tsx` (dead since build step 4) — fixed; full `vitest run` (288 files / 2803
   tests) all pass; `next build` succeeds. Updated the area primer
   (`docs/agents/areas/admin.md`) with a `#342` section and widened residue note, and recorded a
   project lesson (`scripts/wayfinder-autopilot/lessons.md` — the project lessons file at
   `.claude/wayfinder-autopilot/lessons.md` refused all writes this session as a sandboxed
   "sensitive file", so the lesson is recorded in the generic file with a note to move it).

## Questions I answered on the owner's behalf

None this session — no `➡️`-recommendation grilling round ran in the close phase; earlier spec/build
sessions' decisions are recorded on the ticket's spec.md and prior hand-offs.

## Decisions recorded

Map #226 Decisions so far, one line added: "#342 Build the reorganized rail — Today/Archive nav
groups, Accounting rail item, Admin always-labelled row, three-state rail-width persistence
(icons/labels/auto), centralized isActive."

## Tickets created / changed

None created. No finding this session carried a real decision — both triaged P1s were residue/
tooling, not product defects, and closed within this ticket per the "findings split two ways" rule.

## Scores and counts (rendered surfaces only)

scores: predicted-critique=33 first-critique=37 close-critique=39 first-evaluate=92 close-evaluate=96

- Gate: r0 and r1 both clean, 0 real findings outside residue, 0 new-since-baseline, probes 3/3
  pass, 0 errored, 0 page errors, at 1440 and 390.
- include: 1440 Pass, 390 Pass, 0 P0/P1 (unchanged r1→close, not re-run — no P1 there to dismiss).
- Pre-build step that was weakest: none — predicted (33/≈90) undershot both first-pass reader
  scores (37/92), i.e. the pre-flight was conservative rather than optimistic; the two findings the
  first pass raised (residue + a detector-coverage gap) were not genuine misses of a Part B check.

## First-pass findings → lessons (rendered surfaces only)

| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| H8 P1: recurring `text-overflow` on `.truncate` spans | Admin primer's residue list (`text-overflow.*truncate`) | none needed — already covered; reader flagged it as "needs source verification" per protocol, correctly | Verified against source, confirmed residue, no code change |
| evaluate P1: `skipped-offline` on rail-width-control-offline | No existing lesson/check for "detector has no rule for this state" vs "state is broken" | new class | Verified against screenshot + source; recorded the distinction as a Recommended Action in the c1 evaluate re-score for future rounds |
| eslint: 3 unused imports in `rail-width-control.tsx` (build step 4→5, only surfaced at close) | Build steps checked `tsc --noEmit clean`, not `eslint`, per file | wrong (contract existed but was too narrow) | Fixed the imports; added lesson: a build step adding a new file should also run one `eslint` pass on it |

## Needs the owner

Nothing outstanding. No feature removal, no credential need, no unresolved P0/P1. Ticket closed at
the bar: critique 39/45 (no heuristic <3), evaluate 96/100 Clean, include Pass/Pass, gate clean,
tsc/eslint/tests/build all clean.

## Sessions

- `3c2da4e`, `92bf361` — spec phase
- `4fc6662`, `da3b45f`, `f0a76b6`, `02c59af`, `bb717dd` — build steps 1–5
- `23562cb`, `9af8933`, `6654e2f` — gate steps G1–G3
- `bd714ec`, `0082bfd` — measure phase (r1 capture + readers)
- this session — close phase (triage, confirm re-score, checks, lessons, primer, report)
