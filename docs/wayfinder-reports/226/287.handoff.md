# Hand-off — #287 Build Admin Dashboard rollups and the grouped company switcher

milestone: spec-done

## Spec-done summary

Spec finished and hardened by an independent-critic pass (four real findings, all fixed in
`spec.md`): Ready to Approve / Pending PO mismatch approvals are now disjoint columns (gate-filter
split, invariant stated), Dashboard grouping contradiction resolved (flat tables, no section
headers — grouping is switcher/picker only), the nav-placement either/or resolved to one
deterministic redirect, and `Promise.all` → `Promise.allSettled` with per-cell "—" fallback so one
company's failing query can't blank the page. Pre-flight gate met: predicted critique ≥ 32 (two 4s:
H1, H5), zero P0/P1, evaluate walkthroughs all pass.

## Artefacts
- Spec: `docs/wayfinder-reports/226/logs/scratch-287/spec.md`
- Pre-flight (Parts A-E, critic reconciliation): `docs/wayfinder-reports/226/logs/scratch-287/preflight.md`

## Build plan (one step per build session; run in order)

step: 1 — org fields on getWorkspacesForUser — done
  Files: `models/workspaces.ts:137-141` (add `organizationId` + `organization: { select: { id, name } }` to the `include`).
  From: spec.md "Data" (org fields), B4.
  Check: `npx vitest run models/workspaces.test.ts`; grep every caller (`layout.tsx`, `switcher.tsx`, `/workspaces/page.tsx`, account page, marketing layout, `workspaces/new`) still compiles — additive field, no destructuring elsewhere breaks.

step: 2 — shared org-grouping helper + switcher grouping/role/search — todo
  Files: new `lib/workspace-groups.ts` (`groupWorkspacesByOrg()`, per spec.md B4); `components/workspace/switcher.tsx` (section headers, per-entry role text, search `<input>` gated on `workspaces.length > 6`).
  From: spec.md "Primitives to reuse", "States" (switcher rows), B4, B5 (search input `aria-label`).
  Check: existing switcher behavior (current-company check, "New workspace" link) unchanged; keyboard probe on the popover with search focused.

step: 3 — rollup count functions — todo
  Files: `models/approvals.ts` — add `countInvoiceApprovalsReadyToApprove(workspaceId, actor)` and `countPoMismatchesReadyToApprove(workspaceId, actor)` beside `countReadyToApprove` (line 258), both filtering on the same `match-variance` gate check `listPoMismatchRows` uses (lines 221-224), opposite polarity.
  From: spec.md "Ready to Approve / Pending PO mismatch approvals must be disjoint" section.
  Check: a script asserting, per seeded company, `countInvoiceApprovalsReadyToApprove + countPoMismatchesReadyToApprove + <ready expense-claim count> === countReadyToApprove` (the stated invariant); `npx vitest run models/approvals.test.ts`.

step: 4 — Admin Dashboard route — todo
  Files: new `app/(app)/workspaces/[workspaceId]/admin/dashboard/page.tsx`; `lib/admin/paths.ts` (add `dashboard` entry); rail nav (wherever Companies/Users are listed) — add Dashboard first, only rendered when viewer has ≥2 companies; direct hit with <2 companies redirects to `admin.companies`.
  From: spec.md "Dashboard nav placement", "Data" (flat tables, `Promise.allSettled`), Impeccable section (AdminPage, no Card, two-column tables), States table.
  Check: `Promise.allSettled` per metric verified live (kill one company's query, confirm others still render, failing cell shows "—"); in-page detector on the Dashboard state at 1440 and 390.

step: 5 — /workspaces picker page — todo
  Files: `app/(app)/workspaces/page.tsx` (branch: `memberships.length <= 1` keeps redirecting; `>1` renders the picker); picker uses `lib/workspace-groups.ts` from step 2, `AdminPage` furniture.
  From: spec.md "`/workspaces` picker shape", States table.
  Check: manual nav to `/workspaces` with the seeded dev-bypass user (5 memberships) renders the grouped picker, not a redirect.

step: G1 — round script — todo
  Capture states: Dashboard (1), switcher (1, seed's 5 memberships — search path not exercised, seed doesn't reach >6, note as "reviewed not captured"), picker (1) — 3 states × 2 widths = 6 captures. Detector JSON + keyboard probe per state.

step: G2 — r0 and fixes — todo
  Run `critique` + `evaluate` against the captures; fix anything under the bar (32+/two-4s, zero P0/P1) before re-measuring.

step: G3 — build-done — todo
  Full `npx vitest run`, `tsc --noEmit`, `eslint` on changed files; close-phase report.

## Sizing
5 build steps + 3 gate steps, 6 captured states — within the ≤6 build-step / ≤10-state guard (see preflight.md "Sizing check"). No split needed.

## Next step (exact)
A build session starts at step 2 (shared org-grouping helper + switcher grouping/role/search):
new `lib/workspace-groups.ts` (`groupWorkspacesByOrg()`), `components/workspace/switcher.tsx`
(section headers, per-entry role text, search `<input>` gated on `workspaces.length > 6`). Read
spec.md's "Primitives to reuse" and "States" (switcher rows) sections and preflight.md's B4/B5
rows by line range.
