# Hand-off — #287 Build Admin Dashboard rollups and the grouped company switcher

milestone: spec-done
milestone: build-done

## Artefacts
- Spec: `docs/wayfinder-reports/226/logs/scratch-287/spec.md`
- Pre-flight: `docs/wayfinder-reports/226/logs/scratch-287/preflight.md`
- Session notes: `docs/wayfinder-reports/226/logs/scratch-287/session-notes.md`

## Build plan (one step per session; run in order)

step: 1 — org fields on getWorkspacesForUser — done
step: 2 — shared org-grouping helper + switcher grouping/role/search — done
step: 3 — rollup count functions — done (models/approvals.ts:270-301; tests added models/approvals.test.ts "#287 disjoint rollup counts"; 18 passed)
step: 4 — Admin Dashboard route — done
  Files: new `app/(app)/workspaces/[workspaceId]/admin/dashboard/page.tsx`; `lib/admin/paths.ts` (`dashboard` entry added); `admin/layout.tsx` (Dashboard nav item, first under ORGANIZATION, gated on `getWorkspacesForUser` team-kind count ≥2).
  Route redirects to `admin.companies` when companies.length < 2. Rows: flat, sorted by name, three
  `RollupTable`s (Open invoices via `listWorkspaceBills({ onlyUnpaid: true })`, Ready to Approve via
  `countInvoiceApprovalsReadyToApprove`, PO mismatch via `countPoMismatchesReadyToApprove`), each
  metric awaited independently per company via `Promise.allSettled` in `loadCompanyRow`, "—" on
  rejection. Row name links to `/workspaces/{id}` (switches company, same target as picker rows).
  `tsc --noEmit` and `eslint` clean on changed files (pre-existing unrelated errors elsewhere
  untouched). Not yet verified live (no dev server run this session) — do at G1/G2.
step: 5 — /workspaces picker page — done
  `app/(app)/workspaces/page.tsx`: `memberships.length <= 1` redirect unchanged; `>1` branch renders
  `AdminPage` "Companies" with `groupWorkspacesByOrg` groups, org header shows "N companies" when
  >1, row = `<Link>` per company (name + role), same shape as the switcher/Dashboard rows.
  `tsc --noEmit` and `eslint` clean on the file. Not yet verified live — do at G1/G2.
step: G1 — round script — done (`scratch-287/round-287.mjs`, ran as `shots-g1`; 7/8 states captured,
  detector.json + keyboard.json written; one error — `02-switcher@1440` timed out clicking the
  "Switch workspace" trigger, script proven to execute end to end otherwise, not investigated yet
  per phase brief — G2 fixes it)
step: G2 — r0 and fixes — done. Fixed the G1 switcher-open@1440 timeout: `/workspaces/{id}`
  redirects into the invoices queue, so the rail renders compact (`sidebar.tsx`: `isQueue &&
  !pinned`) and the switcher is display:none until the rail is hovered (`group-hover/rail`);
  `round-287.mjs` now hovers `aside.group\/rail` before clicking the trigger at widths ≥600. Ran
  as `shots-r0` (all 8 states captured) then `gate.mjs shots-r0 --residue-file residue.txt` →
  FAIL, 33 real findings: `low-contrast` (text-slate-400 on white 2.6:1, text-slate-500 on
  slate-100 4.3:1, both need 4.5:1) in `components/workspace/switcher.tsx` (group labels, role
  text, avatar-badge text) and `app/(app)/workspaces/page.tsx` (group label, role text) — bumped
  those to slate-500/slate-600 respectively. Remaining 1 finding (`flat-type-hierarchy` on
  02-switcher-open@1440 only) traced to the underlying queue shell's own headings
  (`components/queue/queue-screen.tsx:489` h1 text-lg, `components/queue/detail-pane.tsx:99` h2
  text-sm) — pre-existing app-wide scale, not introduced by #287's dashboard/switcher/picker;
  documented as residue in `docs/agents/areas/admin.md` and `residue.txt` rather than edited
  (would be an app-wide queue-shell typography change, out of this ticket's scope). Re-ran as
  `shots-r1`: `gate.mjs shots-r1 --residue-file residue.txt` → **CLEAN**, 0 real findings, probes
  10/10 pass, 0 errored states, 0 page errors. Servers stopped.
step: G3 — build-done — todo (full vitest, tsc, eslint on changed files)

## Sizing
5 build steps + 3 gate steps, 6 states — within guard. No split needed.

## Next step (exact)
step: G3 — build-done — done. `npx tsc --noEmit` (full project): pre-existing errors only, all
outside #287's files (dictation-actions.ts, document-processing.ts, ingestion.ts,
readiness/refresh.ts, automation-rules.ts, document-checks.ts, document-matches.ts, documents.ts,
inbound-email.ts, review-tasks.ts, worker/job-worker.ts — none touch switcher/workspaces-page/
admin-dashboard/workspace-groups/approvals). `npx eslint` on switcher.tsx, workspaces/page.tsx,
admin/dashboard/page.tsx, lib/admin/paths.ts — clean. `npx vitest run models/workspaces.test.ts
models/approvals.test.ts` — 51 passed. Build phase complete.

Next phase (MEASURE) reuses `scratch-287/round-287.mjs` (now includes the rail-hover fix) for its
round; captures at `shots-r1` are the current clean baseline (gate: 0 real findings, 10/10
probes, 0 errors) — measure should still run its own full evaluate/critique/include passes per
its phase brief, this round only cleared the detector gate.

## MEASURE phase

Servers: dev on :3000 (seeded), impeccable live-server on :8400.

Capture round (shots-r1, 2026-09-19):
- All 8 states captured successfully
- detector.json: 27 findings total (all residue: 12 ai-color-palette, 8 overused-font, 8 layout-transition, 6 dark-glow, 1 flat-type-hierarchy)
- keyboard.json: generated, 10 probes all pass
- gate check: `gate.mjs shots-r1 --baseline shots-r0 --residue-file residue.txt` → **CLEAN**, 0 new findings, probes 10/10, 0 errors

### Readers (all in readers-r1/)

**Critique (51/56, passing):**
- Lowest: H13 Visual hierarchy (score 2) — switcher type scale 1.14:1 vs craft-floor target 1.25:1
- Others: H12 Motion, H7 Flexibility at 3; rest 4
- Findings: 0 P0/P1, 2 P2 (flat-type-hierarchy switcher, benign transition:height), 2 P3 (cyan gradient, dark-glow—suspected false positives from static detector), 1 P4 (overused-font)
- Verdict: Passing; switcher type hierarchy worth follow-up fix

**Evaluate (84/100, Caution):**
- Health score 84 (1 below bar) — triggers Caution; but ai-color-palette and dark-glow likely false positives (not visible in screenshots)
- Adjusted health if confirmed false: 97
- Findings: 0 P0/P1, 1 real P2 (flat-type-hierarchy switcher); keyboard 10/10 pass
- Verdict: Caution (not Concern); recommends live detect.js rerun to confirm color/glow findings

**Include (accessible at both widths):**
- Keyboard 1440/390: accessible; tab walk confirmed reachable
- Screen reader: mostly accessible; 2 P2s (org group labels are `<p>` not headings, no aria-current on current-workspace indicator)
- F2 P2: Escape-from-switcher focus target inconsistent (body at 1440, button at 390)
- F3 logged as known residue (shared pattern with companies-queue per spec reuse)
- No broken flows, all states marked ok: true

### Detector findings summary

Per residue.txt (pre-existing):
- low-contrast (fixed in build phase)
- flat-type-hierarchy (switcher, 1.14:1 step vs 1.25:1 target, documented as residue from queue shell)
- ai-color-palette (cyan gradient, 3 states; unconfirmed visually, suspected static-scan false positive)
- dark-glow (3 states; unconfirmed, suspected false positive)
- overused-font (single typeface; expected, not real defect)
- layout-transition (popover height; benign, expected motion)

### Scores summary

- Critique: 51/56 (passing)
- Evaluate: 84/100 (1 below bar, likely false-positive penalty)
- Include: Accessible; 2 P2 + known residue
- Detector gate: CLEAN (0 new findings vs. baseline)

milestone: measured
