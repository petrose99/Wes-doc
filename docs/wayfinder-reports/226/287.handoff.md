# Hand-off — #287 Build Admin Dashboard rollups and the grouped company switcher

Claimed, spec phase (session 1). Not spec-done yet — hit the context guard mid-draft.

## Done this session
- Read map #226 (Decisions so far, Notes), #254/#300/#301 for the org model and vocabulary this
  ticket inherits, `docs/agents/areas/admin.md` (Companies #285 / Users #286 patterns to reuse),
  Vic tour §2/§6 (Dashboard is two plain per-entity tables, no chart/color — explicitly not to be
  over-designed, §4b).
- Traced the data: `models/workspaces.ts` `getWorkspacesForUser` (no org fields yet — needs
  `organizationId`/`organization.name` in its `include`), `models/approvals.ts`
  `countReadyToApprove` (reusable as-is per company), `listApprovalInvoiceRows` vs
  `listPoMismatchRows` (queues already split invoice vs PO-mismatch rows — the per-kind Ready
  count should come from here, not a re-derivation). No existing "open invoices" count function —
  first build step must read the Invoices queue's own default Open facet before writing one.
- Traced the switcher (`components/workspace/switcher.tsx`, 51 lines, flat list, no grouping/role/
  filter) and `/workspaces` (`app/(app)/workspaces/page.tsx`, always redirects to
  `memberships[0]`, needs a >1-membership picker branch).
- Wrote a draft spec: `docs/wayfinder-reports/226/logs/scratch-287/spec.md` — scope, Vic reference,
  vocabulary (per #300: "company" not "workspace"; "Ready to Approve" not Vic's "Pending
  Approvals"), data plan, primitives to reuse, open questions.

## Not done — next steps in order
1. **Finish the spec**: resolve the "Open questions" section in `spec.md` (open-invoices
   predicate, Dashboard nav placement, `/workspaces` picker shape, seed check) by reading the
   Invoices queue's default facet and `prisma/seed.ts` `seedDevBypassOrganization` /
   `scripts/dev/db253.ts` for whether dev-bypass already has ≥2 memberships.
2. Run `fortify` (state inventory: 0/1/2+ companies, org with 1 vs many companies, >6 switcher
   entries, all-zero rollup counts, a company the viewer lost access to mid-session) and `include`
   (switcher keyboard/search, Dashboard table link focus) into the spec.
3. Call `impeccable shape`/`layout`/`clarify` for the Dashboard's plain-table layout and the
   switcher's grouped popover; re-read `craft-floor.md` findings already loaded this session
   (contrast, spacing, states) — don't reload the routers, sub-skills are already named here.
4. Fill the pre-flight (`preflight.md` per `phases/spec.md` Parts A–E: two excellence targets H1/
   H5, six B-contracts — especially B1 open-every-callee for `countReadyToApprove`/
   `listPoMismatchRows` reuse and B4 primitive reuse — coverage table, independent spec critic via
   a foreground `Agent`, an `evaluate` prediction).
5. Write the ordered build-plan `step:` lines onto this file (seed check first, then org-field
   plumbing, switcher grouping, Dashboard route + rollups, `/workspaces` picker, gate steps last).
6. Size against the phase limits (≤6 build steps, ≤10 captured states) — split into a follow-on
   ticket at a scope boundary (e.g. Dashboard vs switcher/picker) if the plan exceeds either.

## Artefacts
- Draft spec: `docs/wayfinder-reports/226/logs/scratch-287/spec.md`
- Area primer (read, not yet updated): `docs/agents/areas/admin.md`

## Next step (exact)
Open `docs/wayfinder-reports/226/logs/scratch-287/spec.md`, resolve its "Open questions" section,
then continue with fortify/include/impeccable and the pre-flight as listed above. No code yet.
