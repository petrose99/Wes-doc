# Hand-off — #287 Build Admin Dashboard rollups and the grouped company switcher

milestone: spec-done

## Artefacts
- Spec: `docs/wayfinder-reports/226/logs/scratch-287/spec.md`
- Pre-flight: `docs/wayfinder-reports/226/logs/scratch-287/preflight.md`
- Session notes: `docs/wayfinder-reports/226/logs/scratch-287/session-notes.md`

## Build plan (one step per session; run in order)

step: 1 — org fields on getWorkspacesForUser — done
step: 2 — shared org-grouping helper + switcher grouping/role/search — done
step: 3 — rollup count functions — done (models/approvals.ts:270-301; tests added models/approvals.test.ts "#287 disjoint rollup counts"; 18 passed)
step: 4 — Admin Dashboard route — todo
  Files: new `app/(app)/workspaces/[workspaceId]/admin/dashboard/page.tsx`; `lib/admin/paths.ts` (add `dashboard` entry); rail nav — add Dashboard first, only when viewer has ≥2 companies; direct hit with <2 redirects to `admin.companies`.
  From: spec.md "Dashboard nav placement", "Data" (flat tables, `Promise.allSettled`), Impeccable section, States table.
  Check: `Promise.allSettled` per metric verified live (kill one company's query, others still render, "—" fallback); in-page detector at 1440/390.
step: 5 — /workspaces picker page — todo
  Files: `app/(app)/workspaces/page.tsx` (branch `memberships.length <= 1` redirect vs `>1` picker, uses `lib/workspace-groups.ts`, `AdminPage` furniture).
  Check: dev-bypass user (5 memberships) sees grouped picker, not redirect.
step: G1 — round script — todo (Dashboard, switcher, picker × 2 widths = 6 captures; detector + keyboard probes)
step: G2 — r0 and fixes — todo
step: G3 — build-done — todo (full vitest, tsc, eslint on changed files)

## Sizing
5 build steps + 3 gate steps, 6 states — within guard. No split needed.

## Next step (exact)
Build session starts at step 4 (Admin Dashboard route). Read spec.md's "Dashboard nav placement"
and "Data" sections, preflight.md's matching rows, and the area primer for admin pages before
editing. Reuse `countInvoiceApprovalsReadyToApprove`/`countPoMismatchesReadyToApprove` (step 3) and
`groupWorkspacesByOrg` (step 2, `lib/workspace-groups.ts`).
