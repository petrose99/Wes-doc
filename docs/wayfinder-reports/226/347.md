# Autopilot report — #347 Mixed bill/claim batching on Payment Batches

- Map: #226 DocuBite post-login UX
- Type: `wayfinder:build` · Started: 2026-09-17 (spec) · Finished: 2026-09-21 (close)
- Outcome: resolved

## What I did

1. **Spec** (61f8721): specified mixed bill/claim batching — `createPaymentBatches` takes the
   full `BillPayRow` union, `getPaymentBatch` groups lines into `suppliers` (bill-only) and a
   trailing `reimbursements` group, one `describeBatchCounts` string for every "n bills · m
   claims" surface. Spec critic reconciled critique 33/40, predicted evaluate ≈88, both clearing
   the gate.
2. **Build** (4193a09..0b00a70..fd29248..5ba4f0a, six steps + a G1/G2 gate-fix pair): shared
   accessors (`models/bill-pay.ts`), `lib/payments/batch-counts.ts`, the Reimbursements pane
   group, the create-batch dialog's mixed selection and receipt, claim-only batch handling.
3. **Measure** (63949e8, ce4b2ba): first capture round (`round347.mjs`, 14 states × 1440/390),
   fresh critique/evaluate/include readers — critique 28/40 (H8=2, H9-adjacent coverage-gap
   notes, no heuristic <3 besides H8 itself flagged as scored-conservatively), evaluate 88/100
   Clean, include flagged one wrong assumption ("static detector") and one real gap ("Escape
   not verified for dialog variants").
4. **Close, fix batch** (afb13d3): triaged all readers-r1 findings (full triage in
   `docs/wayfinder-reports/226/logs/scratch-347/close.md`). Fixed H8 (zero-count noun in
   `describeBatchCounts`) and H10 (native `title` tooltip on the Reimbursements heading, no new
   component). Added a live Escape-close probe for the claim-row dialog to `round347.mjs`;
   investigated a gate failure on it (r2/r3) and found it was a probe bug, not an app bug — the
   probe matched any `[role=dialog]` including the mobile Detail pane, which correctly stays
   open behind the innermost dialog; scoped the probe to `[data-inner] [role=dialog]`. Gate
   round r4/`gate-c1.json`: clean, 18/18 probes pass, 0 new findings vs baseline.
5. **Close, confirm round** (this session): dev server was down; regenerated a contact sheet
   from the existing clean `shots-shots-r4` capture (no re-capture needed) and ran fresh
   sonnet critique/evaluate/include readers. Critique came back 29/40 with H7 (Flexibility, 2)
   and H9 (Error Recovery, 2) under 3 — both explicitly "absence of evidence in the 14
   captures," not a named defect. Verified against source: `bill-pay-queue.tsx`'s pre-existing
   (#251) `bulkActions` multi-select toolbar and `create-batch-dialog.tsx`'s `role="alert"`
   error banner both exist and work; #347's capture states never exercised either path. Re-ran
   the critique reader on the same captures with those source citations (per the "dismissed
   finding is re-scored, not subtracted by hand" rule) — H7 and H9 both moved to 4/4, total
   33/40.
6. **Checks** (dev server never came up this session, so no stop/restart needed): `vitest run
   lib/payments/` 26/26, full suite `vitest run` 2803/2803, `tsc --noEmit` clean, `eslint` on
   the fix-batch's changed files clean, `next build` succeeds.
7. **Lessons**: `.claude/wayfinder-autopilot/lessons.md` append was refused by the sandbox as a
   "sensitive file" (same block hit on #342) — lesson text recorded in
   `docs/wayfinder-reports/226/logs/scratch-347/close.md` under "Lesson (blocked write)" for a
   future session with write access to move.
8. **Area primer**: `docs/agents/areas/payments.md` `#347` section extended with the close-phase
   fixes (zero-count noun, `title` tooltip, the Escape-probe scoping convention, the bulk-bar
   note).
9. Closing this report and the ticket.

## Questions I answered on the owner's behalf

None this session — the fix batch, triage and re-verify all had source-grounded answers (code
either does or doesn't do what a reader assumed); no open taste call.

## Decisions recorded

Map #226 Decisions so far, appended: "Mixed bill/claim batching on Payment Batches (#347) —
`createPaymentBatches` takes bills and claims together; a trailing Reimbursements group, one
shared bill/claim count string, no per-row Pay From override for claims."

## Tickets created / changed

None created. #347 closed.

## Scores and counts (rendered surfaces only)

scores: predicted-critique=33 first-critique=28 close-critique=33 first-evaluate=88 close-evaluate=90

Gate rounds: r2/r3 failed on the Escape probe (probe bug, not app bug — see above), r4/`gate-c1`
clean (18/18 probes, 0 real findings, 0 new vs baseline `shots-r0`). Include: no P0/P1 either
pass, 18/18 keyboard probes passing including the new claim-row Escape probe. Detector residue:
one new entry this ticket (`text-overflow` on `connection-band.tsx`'s truncated status label,
pre-existing since #281, out of `components/payments`), already in `residue.txt` — see
`docs/agents/areas/payments.md`.

## First-pass findings → lessons (rendered surfaces only)

| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| H8, "0 bills · 1 claim" on claim-only batches | `describeBatchCounts` spec'd one string for both counts, but the zero-count case wasn't in the spec's copy table | unchecked | Fixed: omits a zero-count noun. |
| H10, no inline help for claim vocab | Nothing in the spec named an explanation for "Reimbursements" | none | Fixed: native `title` tooltip, matching the surface's existing convention. |
| include P1, "Escape/focus not verified for dialog variants" | B5 (focus contract) named the dialog but the round script had no live probe for the claim-row variant | unchecked | Added a live Escape probe; found and fixed a probe-scoping bug along the way (not an app bug). |
| close-confirm H7/H9, "no evidence in captures" | round347.mjs never captured the pre-existing bulk-select toolbar or an induced error state | new class (capture-set gap, not a spec/code gap) | Re-verified against source (no code change needed); lesson recorded for future round scripts to include a multi-select and an induced-failure state when those branches exist. |

## Needs the owner

None. The `.claude/wayfinder-autopilot/lessons.md` append is blocked by the sandbox (see
"What I did" #7) — not an owner decision, just a write a future session should carry over.
