# Hand-off — #297 (Build the Direction row and Move to another queue)

milestone: spec-done

## Artifacts
- Spec: `docs/wayfinder-reports/226/logs/scratch-297/spec.md`
- Pre-flight (filled, gate met): `docs/wayfinder-reports/226/logs/scratch-297/preflight.md`
- Spec critic: `docs/wayfinder-reports/226/logs/scratch-297/critic.md` (32/40 critique, 2 P1s found and closed in spec via reconciliation — see preflight Part D)

## Sizing
6 build steps (at the phase limit, not over), 6 named capture states
(matches the ticket's own Bar section) — fits build/measure/close in one
session each. No split needed.

## Build plan (one step per session; check the spec section named)

- step: 1 — Retire chooser + Direction row on the pane — todo — spec §1, §2 (pane half) — files: `components/pipeline/document-detail/split-pane.tsx` (remove docType/setDocType/selectDocType/savingDocType/DOC_TYPE_LABELS; add Direction radiogroup in `FieldNavForm`; remove Save-review `disabled={!docType}` gate), `lib/doc-types.ts` (mark category-bearing/locked types) — check: chooser JSX gone, Direction radio renders and calls `setDocumentTypeAction`, Save review enabled per new rule; `npx vitest run` on split-pane's own tests if any + saveReview tests.
- step: 2 — Direction row in Admin › Configuration › Fields — todo — spec §2 (admin half) — files: `lib/configuration/field-table.ts` (`FieldTableRow.direction`, synthetic row in `resolveFieldTable`), `components/admin/field-table-editor.tsx` (no ✕/no Width cells when `direction`, PO footnote) — check: `npx vitest run` on field-table tests if present; manual/live: Invoice+Receipt show it editable/required-locked-on, PO shows locked+footnote, Bank Statement has none.
- step: 3 — `reclassifyDocumentAction` guards + audit event — todo — spec §4, "Audit event" — files: `app/(app)/workspaces/[workspaceId]/actions.ts`, `models/audit-events.ts` (`auditEventLabel(type, detail?)`, verify its 3 call sites at `models/audit-events.ts:111,138,190`), new tiny `describeMoveIneligibility` helper — check: 3 new guard tests + audit-event test pass; `grep -n "auditEventLabel(" -r models app` shows all 3 callers updated.
- step: 4 — Move menu item + dialog — todo — spec §3 — files: `components/queue/document-actions-menu.tsx` (`MoveDocumentMenuItem`, `MoveDocumentDialog` using `ConfirmDialog`'s `children`/`confirmDisabled`), `components/queue/detail-pane.tsx` (`PaneMenu`: `moveOpen` state, render between `DocumentMenuTopItems` and `menu` slot, dialog outside `PopoverContent`) — check: component/live test that dialog's own text renders after the ⋯ item click + menu close (primer contract); radiogroup selection model matches spec §3's explicit statement.
- step: 5 — Wire `RegisteredDocument.docType`/`moveDisabledReason`, Library pane inherits — todo — spec §3 (RegisteredDocument), §5, §6 — files: wherever `useRegisterDocumentActions`/`setDoc` is called from `SplitPane` (pass the new fields from server-loaded data), confirm Library route (`app/(app)/workspaces/[workspaceId]/library/documents/[documentId]/page.tsx` → shared `DocumentDetailPage`) needs no bespoke code, add read-only Type line if the Details tab doesn't already show one generically — check: grep confirms no second, client-only eligibility computation (B1); live check on Library.
- step: 6 — Toast/origin wiring + full test sweep — todo — spec §3 (toast/Open/from=), §7 — files: toast calls in `MoveDocumentDialog`, `origin.ts` usage — check: full `npx vitest run` (not just affected files, per project lesson on stale mocks), `git diff --name-only` reviewed for any Prisma-backed function whose *callers'* tests weren't re-run.
- step: G1 — round script (capture-round.mjs) covering the 6 Bar states at 1440+390 — todo
- step: G2 — r0 detector/critique/evaluate read, fix batch — todo
- step: G3 — build-done gate (bar met, hand-off updated) — todo

## Next step
Fresh session starts at build step 1.
