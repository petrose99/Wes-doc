# Hand-off — #297 (Build the Direction row and Move to another queue)

milestone: spec-done
milestone: build-step-1-partial
milestone: build-step-1-done
milestone: build-step-2-done
milestone: build-step-5-done
milestone: build-step-6-done
milestone: build-step-G1-done
milestone: build-step-G2-partial

## Artifacts
- Spec: `docs/wayfinder-reports/226/logs/scratch-297/spec.md`
- Pre-flight (gate met): `docs/wayfinder-reports/226/logs/scratch-297/preflight.md`
- Spec critic: `docs/wayfinder-reports/226/logs/scratch-297/critic.md`
- Step 1 in-progress diff notes: `docs/wayfinder-reports/226/logs/scratch-297/step1-notes.md`

## Sizing
6 build steps (at the phase limit), 6 named capture states. No split needed.

## Build plan
- step: 1 — Retire chooser + Direction row on pane — **done**, see
  step1-notes.md for what landed.
- step: 2 — Direction row in Admin Configuration Fields — **done**. Added
  `direction: boolean` to `FieldTableRow`; `resolveFieldTable` injects a
  synthetic `__direction` row (position -1, sorts first) for
  invoice/receipt/purchase_order via `hasDirectionField`/`directionLockedFor`
  (lib/doc-types.ts, from step 1). Editable+Required render locked
  (existing requiredLocked styling, no new branch needed). Width/Order
  cells render empty for `row.direction`; move() blocks swapping the
  Direction row; save payload filters it out (not owner-editable, nothing
  to persist). PO's row hint includes "always payable" inline. Updated
  `lib/configuration/field-table.test.ts` for the new row-0 shift + added
  a direction-row test. `vitest run lib/configuration/field-table.test.ts`
  green (10/10); `tsc --noEmit` clean for both touched files.
- step: 3 — reclassifyDocumentAction guards + audit event — **done**. New
  `lib/reclassify.ts::describeMoveIneligibility(workspaceId, documentId,
  docType)` — one function, three checks in spec §4 order: pending
  approval (`getActiveWorkflowStageState`, reused from
  `models/review-tasks.ts`), Posted/Paid (`getDocumentPaymentStatuses` +
  `derivePaidState`, reused from `models/ledger-payments.ts` /
  `lib/payments/paid-state.ts`), PO matched invoices
  (`summarizePoConsumption`, reused from `models/po-matching.ts`). Called
  from `reclassifyDocumentAction`
  (`app/(app)/workspaces/[workspaceId]/actions.ts:209`) right after the
  document lookup, before the update; on success writes
  `recordDocumentAudit({ type: "document_reclassified", detail: {
  fromType, toType } })`. `models/audit-events.ts::auditEventLabel` gains
  an optional `detail` param, special-cased for `document_reclassified` →
  "Moved to X (was Y)"; all three row-mapping call sites
  (`listWorkspaceAuditEvents`, `listWorkspaceAuditEventsForExport`,
  `listDocumentAuditEvents` — the last needed `detail` added to its
  `select`) now pass `event.detail` through; `listWorkspaceAuditEventTypes`
  (distinct-types dropdown, no per-row detail) left as-is. `tsc --noEmit`
  clean for all four touched files (`lib/reclassify.ts`,
  `app/(app)/workspaces/[workspaceId]/actions.ts`, `models/audit-events.ts`
  — pre-existing unrelated `dictation-actions.ts` error not touched here);
  `vitest run models/audit-events.test.ts
  lib/configuration/field-table.test.ts` green (12/12). §7's dedicated
  tests (three guard tests, the audit-write test) are step 6's per the
  build plan — not added here.
- step: 4 — Move menu item + dialog — **done**. `components/queue/
  document-actions-menu.tsx` gains `MoveDocumentMenuItem` (mirrors
  `DocumentMenuDeleteItem`'s open-after-menu-closes shape; hidden until
  `doc.docType` is set — step 5's job; disabled on `cancelled` or
  `moveDisabledReason`, hint always visible) and `MoveDocumentDialog`
  (mirrors `DeleteDocumentDialog` on the shared `ConfirmDialog`). Radiogroup:
  `TYPED_MOVE_TARGETS` (invoice/receipt/purchase_order, minus
  `currentType`) as flat radios, then an "Other types" disclosure button
  (not itself a radio) revealing `OTHER_MOVE_TARGETS` (delivery_note/
  contract/payslip/tax_form/other) captioned "Found in Search only.".
  Arrow-key handler copied from split-pane's Direction radiogroup pattern
  (B4 reuse) — moving focus selects. Consequence line and confirm label
  update live off `target` state; `confirmDisabled={!target}`, no default
  selection. On confirm: `reclassifyDocumentAction`, success toasts
  "Moved to ‹queue› — Open" using `documentDestinationPath` +
  `withOrigin` (origin.ts, #244) for the Open action, `onMoved()` →
  `ctx.onMutated?.("removed")`; failure sets an inline error (mirrors
  Delete's B1 "input preserved on error") reading "Couldn't move
  ‹filename›. Try again." for network errors, or the server's own guard
  caption otherwise. `RegisteredDocument` gains `docType?: DocType` and
  `moveDisabledReason?: string | null` — **optional** for now (only
  `split-pane.tsx`'s one call site exists and doesn't populate them yet;
  step 5 wires that and can tighten to required then if it still reads
  cleaner). Wired into `PaneMenu` in detail-pane.tsx: `MoveDocumentMenuItem`
  right after `DocumentMenuTopItems`/before the separator+`menu` slot (per
  spec "between DocumentMenuTopItems and the queue's own items");
  `moveOpen` state + `MoveDocumentDialog` sibling of `<Popover>`, same
  shape as `deleteOpen`. `tsc --noEmit` clean; existing
  `field-table.test.ts`/`audit-events.test.ts` still green (12/12) — no
  new tests here, step 6's per the plan.
- step: 5 — RegisteredDocument wiring, Library inherits — **done**.
  `split-pane.tsx`'s `header` type gains `moveDisabledReason?: string | null`
  (doc comment); `SplitPane`'s destructuring + `useRegisterDocumentActions`
  call now pass `docType: queueDocType, moveDisabledReason: moveDisabledReason
  ?? null`. Page-level wiring at
  `app/(app)/workspaces/[workspaceId]/documents/[documentId]/page.tsx`: new
  `const moveDisabledReason = await describeMoveIneligibility(workspaceId,
  documentId, docType)` right after the existing PO-consumption branch
  (reuses the already-resolved `docType`), passed to `<SplitPane
  moveDisabledReason={moveDisabledReason} .../>`. Library route
  (`library/documents/[documentId]/page.tsx`) re-exports this same
  `DocumentDetailPage` — confirmed no separate wiring needed. §5's Type-line
  clause is conditional ("if one does [exist], this is where the new Type
  line is added"): `grep -n docType split-pane.tsx` shows no Library-only
  branch (only the generic `queueDocType`/Direction-row usage already there
  from step 1/2), so per spec the Type line is **not** added — the
  condition for adding it doesn't hold. `tsc --noEmit` clean for all four
  touched files (pre-existing unrelated errors elsewhere on the repo
  untouched); `vitest run lib/configuration/field-table.test.ts
  models/audit-events.test.ts` green (12/12) — no new tests here, step 6's
  per the plan.
- step: 6 — Toast/origin wiring + full test sweep — **done**. Verified (no
  code change needed, already correct from step 4):
  `MoveDocumentDialog`'s success path in `document-actions-menu.tsx:292-298`
  calls `reclassifyDocumentAction`, builds the destination with
  `documentDestinationPath`, and toasts `Moved to ‹label›` with an Open
  action that does `router.push(withOrigin(destination, origin))` — matches
  spec §3 exactly. Added the test sweep §7 asks for: new
  `lib/reclassify.test.ts` (4 tests — pending-approval guard, Posted guard,
  PO-matched-invoices guard, eligible→null happy path, all three model
  calls mocked); `models/audit-events.test.ts` gained a test for
  `auditEventLabel("document_reclassified", {...})` ("Moved to Invoice (was
  Receipt)" / "Moved to Invoice" with no fromType). Scoped out: a
  `reclassifyDocumentAction`-level integration test and a
  `MoveDocumentDialog` component test — `actions.ts` has no existing test
  file/mocking harness (getCurrentUser/requireMember/prisma.update chain),
  and the spec's own §7 line offers the alternative explicitly ("or the
  live click-through the capture round already does"); G1-G3's capture
  round + keyboard probes cover the dialog live, which is cheaper and the
  spec-sanctioned path. `npx vitest run lib/reclassify.test.ts
  models/audit-events.test.ts lib/configuration/field-table.test.ts` green
  (17/17); `tsc --noEmit` clean for both touched/added files.
- step: G1 — round script, 6 Bar states at 1440+390 — **done**. Round
  script at `docs/wayfinder-reports/226/logs/scratch-297/round-297.mjs`
  (imports the shared `capture-round.mjs`), residue at
  `docs/wayfinder-reports/226/logs/scratch-297/residue.txt` (app-wide +
  `docs/agents/areas/admin.md`'s fenced regexes — this ticket touches both
  the detail-pane and Admin surfaces). Base `http://localhost:7331`
  (docker `docubite-web-1`'s mapped port — not :3000, no host `next dev`
  was running this session), workspace
  `af91555d-7450-4b21-a8ac-73db092617c8`. Uses existing seeded documents,
  no new seed needed: invoice `784e8f38-…` (`/invoices/…`), PO
  `9ccd3d76-…` (`/purchase-orders/…`), bank statement `8e17b30a-…`
  (`/bank-statements/…`), a doc-type-blank row `d67d4e1c-…` for the
  Library pane (`/library/documents/…`). 6 named states: 01 direction row
  invoice (+ 390 arrow-key probe), 02 direction row PO locked, 03
  direction row absent (bank statement), 04 Move dialog (no-target →
  target-chosen → Other types expanded, + open-focus/Esc probes), 05
  Admin Fields × 4 sub-states (invoice/receipt/po-locked/bank-absent), 06
  Library pane (type line + Move dialog with no currentType). Ran once
  end-to-end as round `r0`: exit 0, 21 states captured. **Known from r0,
  deferred to G2 as instructed** (G1 is "prove it executes", not fix):
  `04-move-dialog` and `06-library-pane` both errored (`locator.click:
  Timeout 30000ms exceeded` on the "More actions" button — selector or
  timing bug in the round script itself, not yet diagnosed) at both
  widths, and the 390 arrow-key probe in `01-direction-row-invoice`
  timed out on `.focus()`. r0's `by type` counts (`gpt-thin-border-wide-
  shadow`, `dark-glow`) are not yet filtered by the residue file — the r0
  run predates the `residue: RESIDUE` option added to the script after r0
  finished (gate.mjs applies `--residue-file residue.txt` independently
  regardless, so the gate's own counts are correct; this only affects the
  round script's own printed "non-residue" tally). Impeccable live-server
  was started this session (port 8400, pid recorded in scratch — restart
  with the `execFileSync`+`IMPECCABLE_SKILL_DIR` workaround in
  `docubite-live-ui-verification` memory if it's down for G2).
- step: G2 — r0 read + fix batch — **in progress, root cause found and
  fixed, gate not yet run**. **Critical G1 correction**: the "More actions"
  click timeouts in r0 were not a selector bug — `docubite-web-1` (port
  7331) is a docker container running `.env.production` with
  `NODE_ENV=production` and no `DEV_AUTH_BYPASS`, so every request in r0
  silently redirected to `/login` (confirmed with a probe script,
  `probe297.mjs`) and r0's "captured" screenshots are login-page shots —
  its keyboard.json `ok: false` on nearly every check (01/02/05) proves it,
  the earlier hand-off's "known from r0" note was wrong about the cause.
  **Fixed for this session**: started the real dev server per the
  `docubite_live_ui_verification` memory pattern —
  `scratch-297/devserver.mjs` (DATABASE_URL → devdb :55433,
  DEV_AUTH_BYPASS=true, NODE_ENV=development, `next({dev:true})` on :3000)
  — devdb already had the workspace + all 4 round documents seeded, no
  reseed needed. `round-297.mjs`'s `HOST` is now `http://localhost:3000`
  (was :7331 — **do not revert this**). Re-ran states individually against
  :3000 (`shots-r0b`, `shots-r0c`): 01/02/03/05/06 now show real
  content/findings; 04-move-dialog still errored because the seeded
  invoice used for it (`winelands-WC-0093.pdf`, the `INVOICE` constant) is
  actually Paid, so Move is correctly `aria-disabled` per §4 — not a bug.
  Fixed the round script: added `MOVABLE_INVOICE =
  "07e3fc18-130c-4a4e-87c2-3906a8cc8235"` (bergrivier-BL-7702.pdf, reviewed,
  no payment_status) and pointed state 04 at it instead of `INVOICE`
  (`round-297.mjs`, the `INVOICE` constant is untouched, still used by
  state 01). **Real bug found and fixed** via the same probe: the Move
  dialog's Esc-closes-and-returns-focus contract (B4) landed focus on
  `<body>`, not the "More actions" trigger. Root cause in
  `components/ui/confirm-dialog.tsx`: `ConfirmDialog`'s focus-management
  effect depends on `[open, children, destructive]`, and both
  `MoveDocumentDialog`/`DeleteDocumentDialog` mount with `open` hardcoded
  `true` right after their popover menu unmounts (by design, #259) — so at
  mount, `document.activeElement` is still unsettled (the popover's own
  focus-restore to its trigger hasn't necessarily resolved yet), and the
  effect can capture `body` as the "opener" to restore to on close. Fixed
  with two changes, both `tsc --noEmit` clean: (1) `confirm-dialog.tsx`
  gained an `openerCapturedRef` guard so the opener is captured only once
  per open (not re-captured every time `children` gets a new identity —
  was going to be a second bug once a target/disclosure interaction
  happened first) and a new optional `restoreFocusTo?: HTMLElement | null`
  prop that overrides the `document.activeElement` guess when the caller
  can name the real trigger explicitly; (2) `detail-pane.tsx`'s `PaneMenu`
  gained `triggerRef` on the "More actions" button (ref forwarded through
  `PopoverTrigger asChild`) and passes `restoreFocusTo={triggerRef.current}`
  to both `DeleteDocumentDialog` and `MoveDocumentDialog`; (3)
  `document-actions-menu.tsx`: both dialogs gained a `restoreFocusTo` prop
  forwarded straight to their `ConfirmDialog`. Verified live with
  `scratch-297/probe297.mjs` before the fix (focus landed on body after
  Esc) — **not yet re-verified after the fix** (this is the immediate next
  step, one probe run). Not yet done: re-run the probe to confirm the Esc
  fix; re-run the full round as a fresh `r0d` against :3000 with the fixed
  `round-297.mjs`; run `gate.mjs` against it and fix whatever real findings
  it lists (detector findings not yet triaged — r0b/r0c's `by type` counts
  are real now but unreviewed); write final counts here; commit.
- step: G3 — build-done gate — todo

## Next step
G2 is mid-fix-batch, not done. In order:
1. Start the servers this session's `scratch-297/devserver.mjs` used
   (`node docs/wayfinder-reports/226/logs/scratch-297/devserver.mjs`,
   background — DEV_AUTH_BYPASS dev server on :3000 against devdb :55433,
   already seeded) and the impeccable live-server (`node
   docs/wayfinder-reports/226/logs/scratch-297/start-live.mjs`, prints a
   port — should be 8400, matches `round-297.mjs`'s default `detectUrl`).
   **`docubite-web-1` on :7331 is the wrong server for this ticket's
   round — it has no DEV_AUTH_BYPASS and every page redirects to
   `/login`.** Do not point the round back at it.
2. `node docs/wayfinder-reports/226/logs/scratch-297/probe297.mjs` (still
   pointed at the eligible `MOVABLE_INVOICE`, opens Move dialog, presses
   Esc) — confirm "active after esc" now reports the More-actions button,
   not `BODY`. If still body, the fix needs more work before the round.
3. `cd docs/wayfinder-reports/226/logs/scratch-297 && node round-297.mjs
   r0d` (fresh round, full 21 states, both widths).
4. `node /home/ubuntu/Dev/Wes-doc/scripts/wayfinder-autopilot/gate.mjs
   shots-r0d --residue-file residue.txt` — fix whatever it lists for real
   (detector findings, failing keyboard probes, errored states), re-run
   only the touched states, gate again.
5. Write the gate's final per-width counts here, `milestone:
   build-step-G2-done`, commit. Then G3 (tsc + Part B sweep once more,
   `milestone: build-done`).
Stop both servers (`pkill -f scratch-297/devserver.mjs`; the live-server
pid is printed by `start-live.mjs`) before ending any future session.
