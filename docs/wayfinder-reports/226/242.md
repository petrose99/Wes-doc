# Autopilot report — #242 Decide the flagged Dashboard's fate at 20/40: retire it now, or bring it to the bar — and whether its ranked next-best-action moves to the Invoices home

- Map: #226 Map: Make the operator app work like Vic.ai (grammar, not skin)
- Type: wayfinder:grilling · Started: 2026-09-16T00:16Z · Finished: 2026-09-16T00:40Z
- Outcome: resolved

## What I did

1. Claimed #242 (assigned to petrose99).
2. Loaded `grilling`, `domain-modeling`, `intent` (routed to `organize`, then `journey` in-session), `impeccable` (`distill`; `shape` not needed — nothing lands on the queue). `impeccable context` could not run (the launcher needs an approval nobody was present to give); read `PRODUCT.md` and the `distill` playbook directly, as the skill instructs. No `DESIGN.md` exists.
3. Read the evidence: the incumbent critique `2026-09-15T20-28-33Z` (20/40, three P1s, 10/8 in-page), #237's resolution (Dashboard blocks dropped with the owner's signature, page kept one release behind `DASHBOARD_LANDING`), #233 (no default view/sort decided), #239's journey map (Reviewer order, empty queue is done), #241 (Empty queue states), tour §6 (Vic's header band; its dashboard is its weakest screen), `dashboard/page.tsx`, `invoice-queue.tsx` (default sort *Newest first*), `lib/config.ts` (flag defaults to `queue`). Verified with `grep` that `components/dashboard/*`, `stat-cards`, `spend-by-category-chart`, `vendor-spend-chart` have no importer but the Dashboard page, and that `workspace-analytics.ts` is shared with Health and Automation. Skipped a fresh critique — the surface is leaving, not refined (autopilot brief).
4. Ran three grilling rounds (flagged page → the ranking → rendering), 12 questions, each answered with its ➡️ recommendation.
5. Sharpened **Unplugged surface** in `CONTEXT.md` (the old Dashboard joins the list; unplugged surfaces are never measured or polished, only deleted on signature). Added one sentence to `docs/journeys/operator-working-day.md` §1.
6. Created #265 (removal sign-off, `wayfinder:task`, HITL) and parented it to #226.
7. Posted the resolution comment: https://github.com/petrose99/Wes-doc/issues/242#issuecomment-5690069806 and closed #242.
8. Appended the Decisions-so-far line and one fog line (per-queue default sort) on #226.

## Questions I answered on the owner's behalf

| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Does the flagged page have a user? | No; the flag is a rollback lever | #239's three lanes never touch it; tour §6 line 124 / lesson 10 |
| 2 | Retire or bring to the bar? | Unplugged today, deleted on signature; no bar work | Bar applies to what renders; flag default is not-found; CONTEXT.md *Unplugged surface*; #237 removal protocol; `distill` |
| 3 | What goes on the sign-off list? | Route, flag, `stat-cards`, two spend charts; `aging-summary` held for #251; `workspace-analytics.ts` stays | `grep` importers; reporting out of scope on the map; smallest reversible |
| 4 | Where does `WelcomeTour` go? | Rides on #263 | One removal, one signature (#237) |
| 5 | What was the ranking ranking? | Destinations, all with a rail home | #239 Q11 order; `organize`: dashboards show state, nav with counts guides |
| 6 | A ranked element on the queue? | None | Vic §6 line 159 header band; CONTEXT.md *Workspace home* _Avoid_; #237 line 2; #233's two-counts P0 |
| 7 | Ranking as default sort? | Keep *Newest first*; fog | Tour records Sort as a user control; rows carry the urgency signal; smallest reversible |
| 8 | "All caught up"? | Already the Empty queue's done state | #241, #239 Q6 |
| 9 | To the journey map? | One sentence, no new artefact | Already in §2 / Q11; evidence over duplication |
| 10 | Success / counter-metric | `evaluate` H6, H7 ≥ 3 on the landing; "where do I start?" reopens Q7 | `measure`: name the exit |
| 11 | Anything to `shape`? | No | No surface built |
| 12 | Bookmarked `/dashboard`? | #238's not-found boundary already answers | Intent Cat. 9 Dead Ends avoided |

## Decisions recorded

Retire: the old Dashboard is an unplugged surface now (renders nowhere at the flag's default, so no bar work is spent) and is deleted line by line on the owner's signature in #265. The ranked next-best-action ranked destinations, which the rail already orders; nothing returns to the Invoices queue (no header line, no "needs me" view, no card, default sort unchanged); "All caught up" lives on as the Empty queue's done state. Glossary: Unplugged surface sharpened.

## Tickets created / changed

- #265 Sign off deleting the unplugged Dashboard: its route, the `DASHBOARD_LANDING` flag and the dashboard-only components — `wayfinder:task`, child of #226, HITL (owner signs each line by name). Notes the ordering with #263.
- #242 closed; #226 Decisions so far appended; fog added: per-queue default sort (recency vs. the Reviewer's urgency), on operator feedback.

## Scores and counts (rendered surfaces only)

No score; no surface built or changed. Incumbent evidence that shaped the recommendations: critique 20/40 (P1 contradictory money, P1 two-thirds empty chrome, P1 two panel systems; one authored element, the ranked card), in-page 10 @1440 · 8 @390. Not re-run: the surface is leaving.

## Needs the owner

- #265: sign each of the six lines yes / no. Deletion waits for the signature and for the next release to ship (#237's "one release").
- `impeccable context` launcher still needs a standing approval for unattended sessions (`.claude/skills/impeccable/scripts/impeccable context`); third session to fall back to reading PRODUCT.md directly.
