# Hand-off — #264 Three-way empty queue on Invoices + How DocuBite works

Phase **build** (2 of 4), in progress, cut at the context line. Claimed by petrose99.
Branch `codex/post-login-ux-implementation`.

milestone: spec-done

## Build progress so far (not yet `build-done` — do not skip any of "Not done" below)

New files:
- `lib/queue/empty-state.ts` — `emptyQueueState({ workspaceDocumentCount, rowCount, filtered, hasFirstUse })`, matches spec §2.
- `models/queue-outcome.ts` — `startOfTodayIn(tz)` + `getTodayOutcome(workspaceId)` (workspace-wide `reviewTask`/`integrationPush` counts).
- `components/queue/queue-empty.tsx` — `QueueEmpty` (three states, one renderer), `id="queue-empty-title" tabIndex={-1}` in every branch.
- `components/intake/inbound-address-line.tsx` — `InboundAddressLine({ address })`, Copy → Copied (1.5s), `toast.error("Could not copy the address")` on failure.

Edited:
- `models/documents.ts` — added `countWorkspaceDocuments` (cached count, all types/statuses) + `cache` import from "react".
- `components/queue/queue-screen.tsx` — `empty` prop is now `{ firstUse?, done?, filteredTitle/Body/Action }` (old flat `{title,body,action}` shape is **gone**); new optional `workspaceDocumentCount` prop (**defaults to 0 if the caller omits it**); render swapped to `<QueueEmpty .../>`; `focusRow(null)` now prefers `#queue-empty-title` over `#queue-title`.
- `components/queue/exception-queue.tsx` — `empty` migrated to `{ firstUse: {title, body} }` (mechanical). **`workspaceDocumentCount` NOT yet threaded through `ExceptionQueue`'s own props/caller** — it's silently defaulting to 0 inside `QueueScreen`, which means Exceptions will wrongly show the *first-use* text (not "Nothing needs you.") whenever it has zero rows, even in a non-empty workspace. **Fix this before anything else next session** (same pattern as `ReceiptQueue`/`InvoiceQueue` below: add `workspaceDocumentCount: number` prop, wire from `document-queue-page.tsx`'s exceptions caller — check the real file name/route for Exceptions' server page).
- `components/queue/receipt-queue.tsx` — `ReceiptQueue` takes new required `workspaceDocumentCount: number` and `todayOutcome: {approvedToday,postedToday}` props; `empty` migrated with `done.body` counts sentence. **Caller (`app/(app)/workspaces/[workspaceId]/(queue)/receipts/page.tsx`) NOT yet updated to pass them** — will fail to typecheck until fixed.
- `components/queue/invoice-queue.tsx` — `InvoiceQueue` takes new required `workspaceDocumentCount`, `todayOutcome`, `inboundAddress: string | null`. `empty.firstUse` has the full §3.1 copy + **Add invoices** as `<Link href="/workspaces/{id}/pipeline?from=invoices">` (fallback — #266 hasn't shipped, confirmed via `grep -rn openAddDocumentsDialog components app` → no hits) with `TODO(#266)` comment; `phoneAction` is the address line or "Add invoices from a computer." per §3.2. **Caller (`app/(app)/workspaces/[workspaceId]/(queue)/invoices/page.tsx`) NOT yet updated** — must compute `workspaceDocumentCount` (`countWorkspaceDocuments`), `todayOutcome` (`getTodayOutcome`), and `inboundAddress` (`try { token = await ensureInboundEmailToken(workspaceId) } catch { token = null }`, then `config.inboundEmail.enabled && token ? `${token}@${config.inboundEmail.domain}` : null` — see the existing pattern in `app/(app)/workspaces/[workspaceId]/admin/configuration/intake/page.tsx`).
- `components/queue/document-queue.tsx` — `DocumentQueue` takes new required `workspaceDocumentCount: number` and optional `todayOutcome?` (Bank Statements passes it, Purchase Orders doesn't — non-postable, default `done` body applies). `empty` migrated. **Caller `document-queue-page.tsx` and its two server pages (`purchase-orders/page.tsx`, `bank-statements/page.tsx`) NOT yet updated.**

## Not done (pick up here, in this order)

1. **Fix the `document-queue-page.tsx` chain**: add `workspaceDocumentCount` + `todayOutcome?` params, compute `countWorkspaceDocuments`/`getTodayOutcome` in `DocumentQueuePage`, thread to `bank-statements/page.tsx` (pass `todayOutcome`) and `purchase-orders/page.tsx` (omit `todayOutcome`).
2. **Fix `receipts/page.tsx`**: compute + pass `workspaceDocumentCount`, `todayOutcome`.
3. **Fix `invoices/page.tsx`**: compute + pass `workspaceDocumentCount`, `todayOutcome`, `inboundAddress` (see pattern above; `config` import from `@/lib/config`).
4. **Fix `exceptions/page.tsx`** (find its actual path under `(queue)/exceptions/page.tsx`): add `workspaceDocumentCount` prop to `ExceptionQueue`'s signature + JSX (empty is already migrated), compute + pass from the page.
5. **`po-mismatch-queue.tsx` and `approval-invoice-queue.tsx`** (Approvals — PO mismatches / Invoices): NOT touched yet at all. Migrate their `empty={{ title, body, action, filteredTitle }}` → `empty={{ done: { title: "Nothing needs you.", body: "Anything you can decide will show here[...]", action: <the existing waiting-on-others/Go-to-Invoices links> }, filteredTitle }}` (spec §3.3: Approvals keeps its `action` under `done`). Add `workspaceDocumentCount` prop + wire from their two server pages under `approvals/`.
6. **`components/payments/batch-queue.tsx` and `bill-pay-queue.tsx`**: out of scope for behaviour change, but their `empty={{title,body,filteredBody}}` no longer type-checks against the new `QueueScreen` prop shape — mechanically migrate to `empty={{ firstUse: {title, body}, filteredBody }}`, no `workspaceDocumentCount` needed (Payments not in scope, defaults to 0 which is fine since these two never show first-use copy with an action — same look as before). Two-line fix each.
7. **`components/shell/how-it-works.tsx`** (new) — `openHowItWorksDialog()` + `HowItWorksDialog`, pattern-copy `components/shell/keyboard-shortcuts.tsx`'s module-level-listener/dialog approach; mount in `Sidebar` beside `KeyboardShortcutsDialog`. Content per spec §3.5 (4-step `<ol>`, footer link to shortcuts).
8. **`components/shell/account-menu.tsx`** — replace the *Show welcome tour again* item (`RotateCcw`, `resetOnboardingAction`) with **How DocuBite works** (`CircleHelp`, `openHowItWorksDialog()`), same focus-return ordering as the shortcuts item (`close()` then open — see spec §3.5).
9. **Pipeline fallback**: `app/(app)/workspaces/[workspaceId]/pipeline/page.tsx` — when `searchParams.from === "invoices"`, render `<Link href="/workspaces/{id}/invoices">← Back to Invoices</Link>` under the h1 (`text-sm text-emerald-700`, `h-9`).
10. **Seed**: add a **second, empty workspace** (project lesson #261 — `seed257b` never has zero documents) so first-use is reachable in the capture round. Check `.impeccable/live/seed257b.ts`.
11. Run `npx vitest run components/queue lib/queue components/typed-destinations` — fix whatever the prop-shape migration breaks (existing queue tests likely reference the old `empty={{title,body}}` shape or assert on old empty-state markup/classes).
12. Write `scratch-264/part-b-checks.mjs` (B1–B6 greps from `preflight.md`) and run it.
13. Write `scratch-264/round-264.mjs` (per-state capture, both widths, on `scripts/wayfinder-autopilot/capture-round.mjs`), run once as `shots-r0`, fix every real detector/probe finding in place, record r0 counts here, `tsc --noEmit` once, stop servers, write `milestone: build-done`, commit.

## Reference

- Spec: `docs/wayfinder-reports/226/logs/scratch-264/spec.md` — full state tables §6, copy matrix §7, a11y §8.
- Pre-flight: `docs/wayfinder-reports/226/logs/scratch-264/preflight.md` — Part B contracts are the checklist for step 12.
- Primer: `docs/agents/areas/queue-shell.md` (seed/dev-server/capture recipe, detector residue to ignore).
- `#266` has not shipped as of this session (`grep -rn openAddDocumentsDialog components app` → no hits) — Invoices uses the `/pipeline?from=invoices` fallback throughout.

## Decisions taken on the owner's behalf (carried from spec phase, unchanged)
See the previous hand-off revision (still true): first-use outranks filtered; cross-type count kept (#241 d.3); `inbound-address-line.tsx` created here (first of #264/#266 to ship); done sentence uses "posted"; tour code untouched (#263).
