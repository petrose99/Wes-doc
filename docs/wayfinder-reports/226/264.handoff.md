# Hand-off — #264 Three-way empty queue on Invoices + How DocuBite works

Phase **spec** (1 of 4) complete. Claimed by petrose99. Branch `codex/post-login-ux-implementation`.

milestone: spec-done

## Milestones
- spec written: `docs/wayfinder-reports/226/logs/scratch-264/spec.md` (§0 brief · §1 vocabulary · §2 state function ·
  §3 screens · §4 `QueueScreen` API · §5 rendering · §6 states 1–23 · §7 copy matrix · §8 a11y)
- pre-flight A–E filled: `docs/wayfinder-reports/226/logs/scratch-264/preflight.md`; Part D ran twice (sonnet critic):
  round 1 FAIL (3 P1) → spec fixed → round 2 **PASS** — critique 33/40 (4s: H1, H5, H8), evaluate health 86, P1 0, Clean.
- `impeccable context` could not run (needs approval in the unattended session); context taken from the primer
  `docs/agents/areas/queue-shell.md` and the spec itself.

## Decisions taken on the owner's behalf (rules cited)
- #266's Add documents dialog not built → **Add invoices** = `openAddDocumentsDialog` if #266 has shipped at build
  time, else `<Link>` to `/pipeline?from=invoices` with `TODO(#266)`; the pipeline page shows **← Back to Invoices**
  when `from=invoices` (smallest reversible; never a dead button). No blocking edge on #266.
- First-use outranks filtered in `emptyQueueState` (a zero-document workspace with URL params has nothing to clear).
- Cross-type document count kept (#241 d.3 closed decision); receipts-only workspace sees done 0/0 on Invoices (state 19).
- `components/intake/inbound-address-line.tsx` created by whichever of #264/#266 ships first; Admin block fold-in owned by #266.
- Done sentence uses **posted** (#248); "today" = midnight in `workspace.timezone`.
- Tour code untouched (#263); only the account-menu item changes to **How DocuBite works**.

## Next phase: BUILD (fresh session)
1. Read the primer `docs/agents/areas/queue-shell.md`, then `spec.md` + `preflight.md` (Part F is the checklist).
   Read both lessons files (`scripts/wayfinder-autopilot/lessons.md`, `.claude/wayfinder-autopilot/lessons.md`) and
   `craft-floor.md`.
2. Check whether #266 shipped (`grep -rn openAddDocumentsDialog components`) → choose the Add invoices variant (§3.1).
3. Build from the tables: `lib/queue/empty-state.ts`, `models/queue-outcome.ts` (+ `startOfTodayIn(tz)`),
   `countWorkspaceDocuments` in `models/documents.ts`, `components/queue/queue-empty.tsx`, `QueueScreen` `empty`
   API (§4) + six callers, `components/intake/inbound-address-line.tsx`, `components/shell/how-it-works.tsx`,
   account-menu item, pipeline Back link (fallback only), #262 pending-focus consumer → `#queue-empty-title`.
4. Seed: first-use needs a **second, empty workspace** (project lesson #261 — `seed257b` never has zero documents);
   add it to the seed for the measure phase.
5. Write `scratch-264/part-b-checks.mjs` (B1–B6 greps) and run it; affected vitest; `tsc --noEmit` once at the end.
6. Write the hand-off with `milestone: build-done`, commit `wip(autopilot): #264 build`, stop. No capture.

## Not done
No code, no dev server, no captures, no lessons written (close phase writes them; candidate generic lesson: "a
state-selection function orders 'nothing exists' above 'filtered'; every fallback link specifies its success/return
path" — from critic round 1).
