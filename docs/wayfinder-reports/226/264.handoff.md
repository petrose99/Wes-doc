# Hand-off — #264 Three-way empty queue on Invoices + How DocuBite works

Phase **build** (2 of 4), in progress — second build session cut at the context line. Claimed by petrose99.
Branch `codex/post-login-ux-implementation`.

milestone: spec-done

## Done (verified: `tsc --noEmit` clean; `vitest run components/queue lib/queue components/typed-destinations components/shell models/documents` 64/64; new unit tests 5/5)

Surface is fully wired — every "Not done" item 1–9 from the previous hand-off is complete:
- `lib/queue/empty-state.ts` (+ `empty-state.test.ts`), `models/queue-outcome.ts` (+ `queue-outcome.test.ts`), `components/queue/queue-empty.tsx`, `components/intake/inbound-address-line.tsx` — unchanged from session 1.
- `components/queue/queue-screen.tsx` three-way `empty` prop + `workspaceDocumentCount`; `focusRow(null)` → `#queue-empty-title`.
- All seven queue components migrated and their server pages pass `workspaceDocumentCount` (Invoices/Receipts/Bank Statements also `todayOutcome`; Invoices also `inboundAddress`): `invoices`, `receipts`, `exceptions`, `document-queue-page.tsx` (`showTodayOutcome` flag, on for Bank Statements), `approvals/invoices`, `approvals/po-mismatches`. Approvals keep their links under `empty.done.action` (spec §3.3). Payments queues migrated to `done: {…}` shape only.
- `components/shell/how-it-works.tsx` (`openHowItWorksDialog` + `HowItWorksDialog`, mounted in `Sidebar` beside `KeyboardShortcuts`); `account-menu.tsx` item **How DocuBite works** (`CircleHelp`) replaces *Show welcome tour again*; `resetOnboardingAction`/`useTransition`/`RotateCcw` imports removed (action file untouched, #263).
- Pipeline fallback: `pipeline/page.tsx` reads `from`; `PipelineShell` gets `backToInvoices` → **← Back to Invoices** link under the h1.
- Invoices first-use title fixed to spec: "No invoices yet" (no period).
- Edit script used: `docs/wayfinder-reports/226/logs/scratch-264/edit-wire.mjs` (already applied — do not re-run).
- Design-detector hook produced no output on any Write/Edit this session (may not be firing) — note in the report; in-page detector in the round is the source of truth.

## Not done (build gate — pick up here, in order)

1. **Seed the empty workspaces.** `.impeccable/live/seed264.ts` is written (two workspaces: `264e0000-…0001` Empty Co finance → address line; `…0002` Empty Clinic healthcare → line omitted). It failed because the dev-bypass user id `00000000-0000-4000-8000-00000000d0de` does not exist in the devdb — `prisma.user.findMany` lists `priya@example.test` (2f5448d3-…), `tomas@example.test` (65dc837c-…) and others; `seed257b.ts` uses actorId `59887621-86bf-4886-8d6c-b1307bd538a1`. **Find which user the dev bypass actually resolves to** (`lib/supabase/dev-bypass.ts` + `resolveOrProvisionUser`; or start the dev server, hit `/`, then re-query users) and set `DEV_USER` in seed264.ts to it; run `npx tsx --env-file .env .impeccable/live/seed264.ts` (devdb: `docker start docubite-devdb`).
2. **Intake on/off:** `.env` has no `EMAIL_INBOUND_SECRET`, so `config.inboundEmail.enabled` is false. Start the dev server with it set for the round: `EMAIL_INBOUND_SECRET=dev EMAIL_INBOUND_DOMAIN=in.docubite.local node .impeccable/live/dev.mjs start` (dev.mjs passes process.env through; `.env` does not override these). Empty Co then covers states 1/3 (address line + Copy), Empty Clinic covers 2/4/5 (no line; phone shows "Add invoices from a computer.").
3. Write `scratch-264/part-b-checks.mjs` (B1–B6 greps from `preflight.md` Part B) and run it; fix what it shows.
4. Write `scratch-264/round-264.mjs` on `scripts/wayfinder-autopilot/capture-round.mjs` (copy beside the scratch Playwright in `/tmp/scratch229` with `node -e fs.copyFileSync`; pattern: `docs/wayfinder-reports/226/logs/scratch-257/pw/round261-r2.mjs`). States × 1440/390: Invoices first-use (Empty Co), Invoices first-use (Empty Clinic), Invoices done (seed workspace `af91555d-…` with a zero-row query — `aging=90%2B` is *filtered*; for **done** use a queue with zero rows and no params, e.g. Exceptions/Approvals in the seed workspace), Invoices filtered (`aging=90%2B`), Receipts done/filtered, Approvals done, account menu open, How DocuBite works dialog open, pipeline `?from=invoices`. Keyboard probes per preflight B5: `firstuse-tab-order-1440/390`, `firstuse-arrival-focus-1440` (sessionStorage `docubite.pendingFocus=rows` → `activeElement.id==="queue-empty-title"`), `help-open-focus-1440/390`, `help-esc-focus-return-1440/390` (chip `button[aria-haspopup=menu]`, never body), copy-click → label "Copied". Warm routes first (`.impeccable/live/probe252.mjs`). Run once as `shots-r0`; fix every real detector finding / failing probe in place; record r0 counts per width here.
5. Stop the dev server, `tsc --noEmit` once more, write `milestone: build-done`, commit `wip(autopilot): #264 build`.

## Reference
- Spec: `docs/wayfinder-reports/226/logs/scratch-264/spec.md` (§6 states, §7 copy, §8 a11y). Pre-flight: `…/scratch-264/preflight.md` (Part B = checklist for step 3).
- Primer: `docs/agents/areas/queue-shell.md`. `#266` not shipped → `/pipeline?from=invoices` fallback with `TODO(#266)` in `invoice-queue.tsx`.
- Decisions taken on the owner's behalf (spec phase, unchanged): first-use outranks filtered; cross-type count (#241 d.3); `inbound-address-line.tsx` created here; done sentence uses "posted"; tour code untouched (#263). Build phase: Exceptions' done body "Anything a check escalates will show here. Resolved exceptions drop off this list." (spec §3.3 lets non-postable queues pass one sentence).
