# Hand-off — #264 Three-way empty queue on Invoices + How DocuBite works

Phase **measure** done (3 of 4). Claimed by petrose99. Branch `codex/post-login-ux-implementation`.

milestone: spec-done
milestone: build-done

## Built and verified (`tsc --noEmit` clean; `vitest run components/queue lib/queue components/shell models` 481/481; Part B checks 26/26)

- `lib/queue/empty-state.ts`, `models/queue-outcome.ts`, `components/queue/queue-empty.tsx` (first-use h2 now `text-sm font-semibold` — detector flat-type-hierarchy), `components/intake/inbound-address-line.tsx` (`code` text slate-700 — contrast).
- `QueueScreen` three-way `empty` + `workspaceDocumentCount`; every queue page passes it (Payments queues `done` shape only, no firstUse). `focusRow(null)` → `#queue-empty-title` (probe-verified).
- `components/shell/how-it-works.tsx`: `openHowItWorksDialog`, `HowItWorksDialog` (mounted in `Sidebar`, portals so it works below md), **new `HowItWorksButton`** — phone opener on `app/(app)/workspaces/[workspaceId]/account/page.tsx` panel "How DocuBite works" (below md the account menu is the Account page, #252; spec §6 row 15). The Account page's "Welcome tour" panel is **kept** below it: it is the tour's last mount; removing it is #263 (owner sign-off).
- Account menu item **How DocuBite works** replaces *Show welcome tour again*; pipeline `?from=invoices` → **← Back to Invoices**.
- Design-detector hook produced no output on any Edit/Write in either build session (not firing) — say so in the report; the in-page round is the source of truth.

## Seed + servers (recipe for measure/close)
- devdb: `docker start docubite-devdb`. Seed: `npx tsx --env-file .env .impeccable/live/seed264.ts` (idempotent). Workspaces: Empty Co `264e…0001` (finance, 0 docs → address line), Empty Clinic `264e…0002` (healthcare, 0 docs → no line), Done Co `264e…0003` (1 bank statement + 1 approval today → every other queue "Nothing needs you. / 1 approved today, 0 posted."). Filtered states use the seed ws `af91555d-…` (`invoices?aging=90%2B`, `receipts?status=cancelled`).
- Start: `node .impeccable/live/start264.mjs` (dev server with `EMAIL_INBOUND_SECRET`/`DOMAIN` set + impeccable live-server :8400). Warm: `node docs/wayfinder-reports/226/logs/scratch-264/warm.mjs`. Stop: `node .impeccable/live/dev.mjs stop` + kill the :8400 pid.
- Round: `node docs/wayfinder-reports/226/logs/scratch-264/run-round.mjs <rN> [only]` (copies `round-264.mjs` beside `/tmp/scratch229` Playwright). Summary: `node …/scratch-264/summarize.mjs <rN>`. Contact sheet: `shots-r0/sheet.png` (copy `scripts/wayfinder-autopilot/contact-sheet.mjs` into `/tmp/scratch229` first).

## r0 (`scratch-264/shots-r0/`, 27 states, 0 errored)
States: inv-firstuse-co (+copied), inv-firstuse-clinic, inv-firstuse-arrival (1440), inv-filtered, inv-done, receipts-done/filtered, exceptions-done, approvals-done, help-menu (menu-open 1440 / account-page 390, dialog, closed), pipeline-from-invoices — each at 1440 and 390.
- App-wide residue on every state: 5 (avatar palette ×2, Inter, dev-overlay layout-transition + dark-glow). **Ticket surface after residue: 0 at 1440, 0 at 390** on all queue empty states, the dialog@390 and the Account page.
- Remaining non-residue, all pre-existing/shell: `help-menu-menu-open@1440` + `closed@1440` — rail focus-expansion occlusions/overflow (9 + 2; rail residue, #250/#258); `help-menu-dialog@1440` flat-type-hierarchy (shared `Dialog` title 16px — primitive, not this ticket); `pipeline-from-invoices` low-contrast ×5 at 1440 / ×4 at 390 (existing pipeline page; the back link is emerald-700 on white).
- Keyboard probes (all pass): tab order 1440 … Queue options → **Add invoices** → **Copy**; 390 … Filters → Copy (no Add on phone, spec); arrival `pendingFocus=rows` → `queue-empty-title`; Copy click → "Copied" both widths (clipboard permission granted in the round); help open → focus on dialog Close; Esc → "Account menu" chip (1440) / "Open How DocuBite works" button (390), never body; back link present both widths.

## r1 — first measurement (`scratch-264/shots-r1/`, 27 states, 0 errored; sheet `shots-r1/sheet.png`, `sheet.mjs r1` builds it)
Detector identical to r0: residue 5/state; ticket surface **0 non-residue at 1440 and 390** on every queue empty state, dialog@390, Account page. Non-residue only on shell/pipeline (rail occlusions ×9 + overflow ×2 on `help-menu-menu-open/closed@1440`; `help-menu-dialog@1440` flat-type-hierarchy = shared `Dialog` title 16px; pipeline low-contrast ×5/×4). Keyboard probes all pass (same as r0).

Readers (sonnet, on sheet + JSON + ≤6 PNGs each):
- **critique 34/40** — H1 4 · H2 4 · H3 3 · H4 3 · H5 4 · H6 3 · H7 3 · H8 4 · H9 3 · H10 4. Predicted 33 → measured 34 (H2, H10 +1; all others as predicted). Specificity: generic by design (spec §0, Operate).
- **evaluate health 91**, P0 0 · P1 0 · P2 2 · P3 1, anti-patterns **Clean**; heuristic sum 4 (H7 1, H8 2, H10 1). Predicted 88/sum 9/P2 0 → measured 91 with two P2s the pre-flight did not predict (below). Walkthrough (a)(c)(d) pass all four questions; (b) not re-walked (unchanged since #261).
- **include PASS at 1440 and 390** — all checklist rows ✓; reduced-motion on the shared `Dialog` unverified (`?`).

**Bar status: met on the first pass** (0 P0/P1, health ≥ 85, Clean, 34 ≥ 32, no heuristic < 3, include PASS).

Triage (real findings, for the close phase — one batch, then confirm round r2):
1. P2 · `inv-firstuse-co@1440` (also copied state) · H8/H3 — the inbound-address `<code>` wraps mid-token at 1440 (`…@in.docubite.l / ocal`) although the column has room; only the <md break-all case was specced (§3.2 / state 14). Fix in `inbound-address-line.tsx`: no `break-all` ≥ md, let the pill be `max-w-full`/`whitespace-nowrap` on desktop, break at `@` only below md. Lesson candidate (Part B time-axis/long-content row filled for 390 only).
2. P2 · `help-menu-dialog@1440` · H8 — detector flat-type-hierarchy 18/16/14 (shared `Dialog` title). Pre-existing primitive, not this ticket; **do not fix here** — record on the close as shell residue (same as Keyboard-shortcuts dialog), or ticket if the owner wants the primitive changed.
3. P3 · dialog footer "…or press ?" is inert text (H7/H10) — spec said one link, no button; make "Keyboard shortcuts" a real link/button that opens the shortcuts dialog, or drop "or press ?". Execution, no decision.
4. P3 · include — confirm `components/ui/dialog.tsx` honours `prefers-reduced-motion`; note on ticket.
5. Shell/pipeline items (rail occlusions #250/#258, pipeline low-contrast, menu-item prominence P3 from critique) — record, not this ticket.

## Next (close phase)
Servers up (recipe above) → fix items 1, 3, 4 (one batch; no product fix for 2) → `run-round.mjs r2` → `sheet.mjs r2` → re-run the three readers on r2 → tsc, eslint, full vitest, `next build` with the dev server stopped → Part G table in the report (`docs/wayfinder-reports/226/264.md`) → lessons (generic + project) → primer `docs/agents/areas/queue-shell.md` refresh → resolution comment, close #264, Decisions-so-far line, fog graduation. Detector hook still not firing on Edit/Write — say so in the report.

milestone: measured

## Reference
- Spec `…/scratch-264/spec.md`; pre-flight `…/scratch-264/preflight.md`; Part B script `…/scratch-264/part-b-checks.mjs`; primer `docs/agents/areas/queue-shell.md`. `#266` not shipped → pipeline fallback with `TODO(#266)` in `invoice-queue.tsx`.
- Decisions on the owner's behalf: first-use outranks filtered; cross-type count (#241 d.3); "posted"; tour code untouched (#263); Exceptions done body one sentence; **phone help entry = Account page panel** (spec row 15 assumed a <md menu that does not exist); Welcome-tour panel kept pending #263.
