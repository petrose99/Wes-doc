# Hand-off — #264 Three-way empty queue on Invoices + How DocuBite works

Phase: **spec** (1 of 4), in progress — spec and pre-flight A–C, E written; Part D (spec critic) **not yet run**.
Claimed by petrose99. Branch `codex/post-login-ux-implementation`.

## Milestones
- spec written: `docs/wayfinder-reports/226/logs/scratch-264/spec.md`
- pre-flight A–C, E filled: `docs/wayfinder-reports/226/logs/scratch-264/preflight.md` (Part D empty, Part G empty)
- (no `milestone: spec-done` yet — set it after Part D's gate is met)

## Decisions taken this session (owner's side, rules cited)
- #266's Add documents dialog is not built. **Add invoices** targets `openAddDocumentsDialog` when #266 has shipped at
  build time; otherwise a `<Link>` to `/pipeline?from=invoices` (the one working upload) with `TODO(#266)` — smallest
  reversible option, never a dead button. No blocking edge added: #266 is blocked by #267 (owner sign-off) and an edge
  would stall #264 unattended.
- The shared address line `components/intake/inbound-address-line.tsx` is created by whichever of #264/#266 ships first.
- Done sentence uses **posted** (#248); "today" = midnight in `workspace.timezone`; posted = `integrationPush.status
  "succeeded"` + `completedAt`; approved = `reviewTask.status "approved"` + `resolvedAt`.
- Other queues keep their existing "No ‹noun›s yet." copy as their first-use text; done/filtered come from the
  three-way `empty` prop (spec §4). Tour code untouched (#263); only the account-menu item changes.

## Exact next step (a fresh spec session)
1. Read `spec.md` + `preflight.md` (both under ~250 lines; no codebase re-read needed — primer is
   `docs/agents/areas/queue-shell.md`).
2. Run Part D: one foreground `Agent` (`model: "sonnet"` — no money/approval/schema/auth), fresh context, given the two
   files + the critique rubric's ten 4-rows and evaluate's severity scale; score the spec as built. Reconcile into
   Part D; gate: zero P0/P1, none < 3, two 4s, Clean.
3. Set `milestone: spec-done`, commit `wip(autopilot): #264 spec`, stop.

## Not done
No code, no dev server, no captures. Report at `docs/wayfinder-reports/226/264.md` is for this partial session.
