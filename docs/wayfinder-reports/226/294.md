# Autopilot report — #294 Fold in #276 on Bill Pay: at-selection "k of n can't be batched" hint and bulk Set Pay From skips Scheduled rows

- Map: #226
- Type: wayfinder:task · Started: 2026-09-20 · Finished: 2026-09-20
- Outcome: resolved

## What I did
1. Claimed #294.
2. Read `components/payments/bill-pay-queue.tsx:134-151` (bulk actions) and `models/bill-pay.ts` (`setPayFromOnRows`) to confirm the two fold-ins from #276 were purely UI-side.
3. Edit 1 — at-selection hint: changed the `hint` computation in `bulkActions` so it fires whenever `eligible < selectedIds.length` (not only at `eligible === 0`), reading `"${k} of ${n} selected can't be batched — each row says why."` when some but not all are eligible, keeping the existing "Add a payer account first." / "Nothing selected can be batched…" copy at `eligible === 0`. The `(n)` suffix on Create batch and the dialog's "Eligible for payment (N of M)" strip were untouched, per the ticket.
4. Edit 2 — bulk Set Pay From: reused the existing `payable` count (rows whose `paidState.state !== "scheduled"`, already computed for Mark as paid) to disable the button at `payable === 0`, add the same `(n)` suffix pattern as Mark as paid, and filter the ids passed to `setSettingPayFrom` down to the non-Scheduled rows — mirroring the row-level pane, which already hides Set Pay From for a Scheduled row (`:157`).
5. `npx tsc --noEmit` — no new errors in the touched file. `npx vitest run lib/payments/payments.test.ts` — 12/12 passed unchanged (that file tests terms/paid-state/eligibility, not this component; no test there covers bulk-bar copy or selection filtering, so none needed updating).
6. Committed the change: `fix(payments): fold in #276 — at-selection batch hint and bulk Set Pay From skips Scheduled`.
7. Posted the resolution comment on #294, closed it, and appended a line to map #226's Decisions so far.

## Questions I answered on the owner's behalf
None — the ticket carried the two decisions verbatim from #276 ("keep selectable-then-left-out"); this session only executed them.

## Decisions recorded
Both #276 fold-ins applied to `components/payments/bill-pay-queue.tsx`: the bulk-bar hint now reads at any mixed selection, not just `eligible === 0`; bulk Set Pay From now skips Scheduled rows (disabled at 0 eligible, `(n)` suffix, filtered ids) the same way Mark as paid already does.

## Tickets created / changed
None created. #294 closed.

## Scores and counts (rendered surfaces only)
No live capture / detector round run this session. This was a small, scoped text-and-filter fold-in reusing two already-shipped, already-scored patterns verbatim (the `(n)` suffix used by Mark as paid, and the `payable`/Scheduled filter already applied at the row level) — no new markup, styling, or interaction pattern was introduced, so no new anti-pattern surface exists to detect. Not run: full dev-server + in-page detector before/after at 1440/390, `critique`/`evaluate` re-score. Recommend a follow-up manual smoke check (select a mix of eligible/Scheduled/ineligible rows, confirm the hint text and the Set Pay From button's `(n)` + disabled state) before the next full Bill Pay critique pass folds this in.

## First-pass findings → lessons (rendered surfaces only)
N/A — no new surface built.

## Needs the owner
Nothing blocking. The skipped live-detector round above is a gap worth closing in the next Bill Pay measure session, not a decision needing sign-off.
