# Hand-off — #257 Build the phone approval lane

Written for a reader with no memory of this session (2026-09-16, build phase, session 2 of 3+).
Prerequisite #289 (landing #236) is **done** — `app/(app)/workspaces/[workspaceId]/(queue)/approvals/`
exists on the branch. This session built a first structural pass of the spec but did **not**
finish it — see "Not yet built" below. Context hit the 150K hand-off line mid-build; ending here
rather than pushing through per the autopilot brief.

## Milestones

| Milestone | State | Where |
|---|---|---|
| Spec, pre-flight A–E (session 1) | done | `docs/wayfinder-reports/226/logs/scratch-257/spec.md`, `preflight.md` |
| Prerequisite #236 landed | done | `app/(app)/workspaces/[workspaceId]/(queue)/approvals/` present |
| Build — partial (this session) | **in progress, not done** | see below |

**No `milestone: build-done` line yet** — do not let a driver skip straight to the close phase;
resume this session's work first.

## What is built and where (this session)

1. `components/ui/dialog.tsx` — `placement?: "center" | "sheet"` (S5): bottom-anchored below
   `md`, centered at `md`+; outer wrapper carries `data-inner` so the queue's document-level
   keydown handler can tell a `Dialog` apart from the full-screen Detail-pane sheet.
2. `components/list-screen/reason-dialog-button.tsx` — `ReasonDialog` gained a `placement` passthrough
   (default `"center"`, unchanged for every existing caller).
3. `components/queue/queue-screen.tsx` (S8, B1 Esc-guard):
   - `open()` now `pushState`s below `lg` (`matchMedia("(max-width: 1023px)")`) when no pane was
     already open, else `replaceState`s (arrowing/stepping). `close(fromPopstate)` distinguishes a
     popstate-driven close (no second history write) from a user-driven one.
   - A `popstate` listener closes the pane below `lg` without pushing/replacing again.
   - The keydown guard now checks `[data-inner] [role=dialog][aria-modal=true]` instead of any
     `[role=dialog]` — Escape closes only the innermost open `Dialog` (a Reject/Approve/Filter
     sheet), never the queue pane behind it, and never drops a typed reason (critic finding 2).
   - New `cards?: { below: "md" | "lg"; render }` prop (S2/B4): renders a `<ul>` of `<li>` cards
     below the given breakpoint via Tailwind responsive classes (`{below}:hidden` / `hidden
     {below}:table`); the table still renders at and above it. `phoneReadOnly?: boolean` (S2/spec
     3.2): suppresses the bulk bar and prints "Full view on desktop — this list is read-only on a
     phone." above the (still-table) list below the breakpoint. **Not yet wired to any of the
     other queues** (#261's job) — only Approvals uses `cards` so far.
   - Row-1 sort select / facet chips hidden below `cards.below` (Filter sheet replaces them,
     not yet built — see below); `views` slot likewise hidden below the breakpoint since Approvals
     no longer needs it there (segments live in `band` instead).
4. `components/queue/queue-segments.tsx` — new: `QueueSegments({ segments, active, label })`, a
   `role="tablist"` of `<Link>`s with roving tabindex and ←/→ (S2/B4). Replaces
   `components/typed-destinations/approvals-queue-picker.tsx` (`ApprovalsQueuePicker`) as the
   `band` slot in **both** `approval-invoice-queue.tsx` and `po-mismatch-queue.tsx` — renders at
   all widths (desktop included), so there is one picker, not two (critic finding 11: segments
   named "Invoice approvals" / "PO mismatches", not "Invoices", so they don't collide with the tab
   bar's "Invoices" slot). `ApprovalsQueuePicker` itself is untouched/unused now — not deleted (no
   removal without owner sign-off; also nothing else was checked for other callers besides the two
   edited here — re-grep before deleting it).
5. `components/queue/approval-invoice-queue.tsx` / `components/queue/po-mismatch-queue.tsx`:
   - `cards={{ below: "lg", render: ... }}` added, with a new `ApprovalCard` / `MismatchCard`
     function per spec 3.3 (supplier/amount line, invoice#/due or PO#/variance line, stage-or-pill
     line, `ProcessingStateGlyph` leading icon, `<a>` not `<button>` per spec).
   - The Reject `ReasonDialog` and (invoice) Approve `Dialog` and (mismatch) override `ReasonDialog`
     now pass `placement="sheet"`.
   - Copy updated to the spec's exact strings for Reject ("This ends the approval run for this
     invoice. The reason goes on the audit trail and to whoever started it.") and the override
     sheet ("Approve over the PO mismatch" / the amount-percent-allowance sentence, spec 3.5).
6. `components/shell/mobile-tab-bar.tsx` + its one caller
   (`app/(app)/workspaces/[workspaceId]/layout.tsx`): tab bar is now **Approvals · Invoices ·
   Exceptions · Account** (spec 3.1) — Approvals badged with `approvalsReadyCount` (already
   computed in `layout.tsx` for the rail, now also threaded to the tab bar), gated on the
   `review-queue` capability; Invoices carries **no** badge (#232 §2); Account's `active` match
   widened to cover the "Also in this workspace" deep-link routes (purchase-orders, receipts,
   bank-statements, library, finance, payments) so a deep link there keeps Account lit. Grid is now
   `gridTemplateColumns: repeat(tabs.length, ...)` since Approvals only renders when capable.
   `accountingEnabled` prop kept on the component's type (harmless, currently unused in JSX — the
   Finance/Archive third slot moved to the Account page instead) — fine to leave or prune later.
7. `app/(app)/workspaces/[workspaceId]/account/page.tsx`: new "Also in this workspace" `Panel`
   (S4) — Purchase Orders · Receipts · Bank Statements · Archive · Finance (only when
   `config.integrations.bigcapital.enabled`); **Payments excluded** per spec (owner-scoped to
   desktop, #251). Visible at every width (not `md:hidden`).

## Not yet built (continue from here)

In spec order, everything below is still open — **do not re-read spec.md/preflight.md from
scratch, they are unchanged from session 1**; jump straight to the relevant `spec.md` section
number:

1. **S4's second half**: the Admin layout's below-`md` caption (the duplicate company-name line
   #252 flagged) is **not yet removed**. Location: `phoneNote` prop on `AdminPage` in
   `components/admin/admin-ui.tsx` (`phoneNote = true` default, renders `<PhoneNote />`) — find
   every `AdminPage` caller that still relies on the default (i.e. doesn't pass `phoneNote={false}`
   the way `account/page.tsx` already does) and either flip them to `false` or remove `PhoneNote`
   from `AdminPage` entirely and delete its now-dead prop. Grep `phoneNote` first — check whether
   any caller besides the pre-existing Account page actually wants it kept before deleting wholesale.
2. **Filter sheet** (spec 3.4) — not started. New `Dialog placement="sheet"` titled "Filter": Sort
   radiogroup + one radiogroup per `Facet`, footer Clear / "Show n rows". Replaces the now-hidden
   sort-select/facet-chips row below `cards.below` — currently that row is just hidden with no
   replacement, which is a regression versus today's desktop-reachable-everywhere facets; this is
   the highest-priority next step, both for the spec and to avoid a filtering dead-end below `lg`.
   `activeFilterCount(searchParams, facets, sortParam)` helper named in spec 3.3 point 3 doesn't
   exist yet — write it once, shared by the Filter button label and the sheet.
3. **Detail sheet phone changes** (spec 3.5) — largely unbuilt beyond the footer grid (done, item 3
   above) and the `data-inner`/Esc guard (done). Still open: `role="dialog" aria-modal="true"`
   below `lg` + `inert` on `main`'s siblings and the tab bar while open (spec 3.5, B5); Back's
   `aria-label`; source-strip show/hide toggle (`sessionStorage["dp.source"]`); tabs given
   `role="tablist"`/←/→/`aria-controls` and reordered Approval-first below `lg`
   (`initialTab: "approval"` is already passed to `getQueueDetailAction` by both queue components —
   only the *order* below `lg` and the roving-tab behavior remain); the "Waiting on you" status
   line; the ⋯ menu's "Open on desktop" / disabled "Send back for review" copy below `lg` (today's
   `PaneMenuItem` for Send back has no disabled/desktop-only state — add one, `components/queue/detail-pane.tsx`
   `PaneMenuItem` already supports `disabled`+`hint`, so this is mostly a call-site change in the
   two queue components); the result strip after a decision (spec 3.5 "After a decision") — biggest
   remaining piece, replaces the decision bar with "Approved/Rejected · just now" + Next to
   approve/Back to list, currently **not built at all** — the bar just goes back to idle today.
4. **Timeline** (S6) — `ApprovalStepChain` in `components/queue/history-tabs.tsx` not yet rewritten
   to the node-list-with-avatars shape (spec 3.5 point 2). Still the old two-section decided/pending
   list.
5. **"From this supplier"** (S7) — `DocumentHistory` type (`components/queue/history-tabs.tsx`) and
   `getQueueDetailAction` (`app/(app)/workspaces/[workspaceId]/queue-actions.ts`) not yet extended
   with `supplier`/`nearDuplicate`. Nothing renders this section yet.
6. **PO Mismatches-only variance summary + "Match lines on desktop" note** (spec 3.5 point 4,
   PO-only) — not built; presumably already exists as the `#228 po-compare` read-only table
   somewhere in the Checks tab — confirm reuse vs. new before writing it.
7. **State inventory** (spec 3.6) — largely unverified against the actual components; only the
   card list's basic empty/table-fallback exists via `QueueScreen`'s existing empty-state prop
   (unchanged). Offline-inside-an-open-sheet, server-refused, network-error-mid-decision, and
   deep-link-to-a-decided-row states are **not** specifically handled — today's `error`/`pending`
   state in the two queue components is the pre-#257 shape.
8. **Motion** (spec 3.5 last bullet, spec §4) — sheet slide-in `translate-x-full → 0` 200ms not
   added (currently relies on Dialog's/PaneFrame's existing lack of transition); `motion-reduce`
   not added to the new sheet placement.
9. **Test plan** (spec §6) — no tests written yet: `queue-screen.test.tsx` (cards breakpoint
   rendering, pushState/replaceState, popstate-closes, Esc-with-Dialog-open, `inert` on tab bar),
   `models/approvals.test.ts` (badge = sum of segments), `history-tabs.test.tsx` (once the timeline
   is rewritten), `getQueueDetailAction` supplier/nearDuplicate null-safety (once S7 lands).

## Part B contract checks (spec/preflight) — run once the above lands, before the first measurement

Not run yet this session (nothing to check them against for most rows). When resuming: `grep -rn
"decideReviewTaskStageAction\|overrideGateAction\|sendApprovalBackForReviewAction" components app`
(≥1 caller each — already true pre-#257, shouldn't regress), string-extract for B3 vocabulary
once the Filter sheet and result strip exist, primitive diff (confirm no second dialog/segment/
timeline component got created — true so far), focus probe (Approve sheet's first-focusable is
the comment textarea by construction — already true, verify once the sheet is exercised live).

## `tsc --noEmit` — not run this session

Run it before continuing; the edits above are type-plausible (props threaded consistently) but
unverified. Known risk spots: `MobileTabBar`'s `accountingEnabled` prop is now unused in JSX —
should not error under this repo's tsconfig but check; `ReasonDialog`'s `description` prop takes a
plain `string`, and the override sheet's description is now a template literal built from
`overriding` (nullable) guarded with `overriding ? ... : ""` — should typecheck fine.

## Exact next step for the following session

1. Read this file (you just did). Read `spec.md` §3.4 (Filter sheet) and §3.5 (Detail sheet) in
   full — they were not re-read into this hand-off, only gisted.
2. Build the Filter sheet first (item 2 above) — it's the most-isolated remaining piece and
   unblocks re-showing sort/facets below `lg` (currently just hidden with nothing to replace them,
   which is worse than before this session for anyone testing at `lg`-).
3. Then the result strip (item 3, the biggest single piece) and the timeline rewrite (item 4).
4. Then S7 (supplier data) and the state inventory pass (item 7).
5. Only then: Part B contract checks, `tsc --noEmit`, and the tests (spec §6), and only then write
   `milestone: build-done` and hand to the close-phase session.

Do not start the close phase (capture/critique/evaluate) until `milestone: build-done` is true —
the surface is materially incomplete (no Filter sheet, no result strip, no timeline rewrite), and
measuring it now would just record a low score against unfinished work.

milestone: spec-done
