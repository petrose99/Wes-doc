# Hand-off — #257 Build the phone approval lane

Written for a reader with no memory of the spec session (2026-09-16). **Blocked on #289** (owner lands #236 on the sandbox branch); once #289 is closed the driver resumes here at the **build** phase.

## Milestones

| Milestone | State | Where |
|---|---|---|
| Intent context · `specify` · `fortify` state inventory · `articulate` copy matrix · `include` plan | done | `docs/wayfinder-reports/226/logs/scratch-257/spec.md` (§1–§3.8) |
| Impeccable `shape` → `adapt` · `layout` · `typeset` · `clarify` | done | `spec.md` §4 |
| Pre-flight A–C, E | done | `docs/wayfinder-reports/226/logs/scratch-257/preflight.md` |
| Pre-flight D (opus spec critic) | done — first verdict 30/40 · 3 P1 · FAIL; 15 findings applied via `apply-critic.mjs`; re-predicted 32/40 (H1 4, H5 4) · 0 P1 · health ≈ 85 · Clean → PASS | `preflight.md` Part D |
| Prerequisite: #236 landed on the local branch | **not done** — owner task #289 | `spec.md` §0 |
| Build · first measurement · fix batch · confirm | not started | — |

Note: `logs/` is gitignored, so `spec.md`, `preflight.md` and `apply-critic.mjs` live only on this machine. If they are missing, the ticket comment of 2026-09-16 (spec session) and `257.md` (report) carry the decisions; the spec would have to be rewritten from #232's resolution.

## Decisions taken in the spec (rule in `spec.md` §2 and Part D)

S1 #236 not merged by the autopilot (9-file conflict; ticket reserves landing for the owner) · S2 cards are a `QueueScreen` `cards` rendering mode shared with #261 · S3 segmented control is data-driven (Claims arrives with #273) · S4 phone header owns the company name; Admin's below-`md` caption removed (`organize`, per #252's comment) · S5 bottom sheets = `Dialog placement="sheet"` · S6 timeline replaces `ApprovalStepChain` in place · S7 supplier facts on `DocumentHistory` · S8 pushState on open below `lg`, popstate closes · S9 decision bar in `DetailPane`'s footer slot. Critic-driven: segments renamed **Invoice approvals · PO mismatches**; Send back for review is desktop-only below `lg`; Approve sheet focus never on the primary; queue Escape ignored while a Dialog is open; `phoneReadOnly` queues keep the table with a visible read-only line until #261.

## Exact next step (build phase)

1. Confirm `app/(app)/workspaces/[workspaceId]/(queue)/approvals/` exists (else stop: still blocked on #289).
2. Read `spec.md` §3 and `preflight.md` Parts B–C as the row-by-row checklist; read `craft-floor.md`; read both lessons files.
3. Build in this order: `Dialog placement="sheet"` → `QueueScreen` `cards` + history model + Escape guard → `queue-segments.tsx` → Filter sheet → `DetailPane` phone changes (role, inert, source toggle, tablist, decision bar, result strip) → timeline + From this supplier (`history-tabs.tsx`, `getQueueDetailAction`) → tab bar + Account list + Admin caption removal → state inventory §3.6 → tests §6.
4. Run the Part B checks (grep for the three actions, string extraction for B3, primitive diff, focus probe), affected vitest, `tsc --noEmit` once; write `milestone: build-done`.

milestone: spec-done
