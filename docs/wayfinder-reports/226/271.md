# Autopilot report — #271 Build the Approval notice

- Map: #226 Post-login UX
- Type: wayfinder:task (execution) · Started: 2026-09-17T00:00Z · Finished: 2026-09-17T00:30Z
- Outcome: partial — BUILD phase done (`milestone: build-done`); MEASURE is the next session

## What I did
1. Read the hand-off (session 5 ended before the r0 round); started dev :3000 and detector :8400, warmed every route.
2. Ran the build-gate round `r0` (33 states × 1440/390, detector JSON, 20 keyboard probes). First pass: 73 non-residue findings, 4 errored states, 4 failing probes.
3. Fixed in place (`scratch-271/fix-r0.mjs` + two sed edits): `Switch` disabled-while-saving dropped focus to body (→ `busy`/aria-busy, click ignored while saving; every downstream probe then passed); stop card 70px shadow (→ `shadow-sm`); Admin band and email footer line length; dialog title `text-lg`; round script — chip by `#account-menu-trigger`, account-menu/dialog states 1440-only, switch restored to on after each state, email case `sent-back` (was `sent_back` → 404 captured), `long-names` case added.
4. Re-ran the full round: **0 real findings in every state at both widths**, all probes as expected. Contact sheet read once.
5. Affected tests 23/23, `tsc --noEmit` clean, servers stopped, hand-off written with the r0 counts.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Change the shared `Dialog` title size (all dialogs) to clear `flat-type-hierarchy`? | Yes, `text-base` → `text-lg` | Smallest reversible fix; detector evidence (step 1.14 < 1.25); craft-floor type scale |
| 2 | Keep the auth card's 70px shadow on the stop page for consistency with login? | No — `shadow-sm` on the stop page only | Detector `gpt-thin-border-wide-shadow` is a real finding on the ticket's surface; login untouched (not in scope) |

## Decisions recorded
None on the ticket yet (build phase). Hand-off carries the simplifications to flag at close.

## Tickets created / changed
None.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=32 first-critique=n/a close-critique=n/a first-evaluate=n/a close-evaluate=n/a
r0 in-page detector (build gate, not a measurement): 0 real findings per state at 1440 and 390 after the build's fixes (residue named on the hand-off). Keyboard probes: 20/20 as expected. Readers not run — measure phase.

## First-pass findings → lessons (rendered surfaces only)
Deferred to the measure/close sessions. Candidate already visible: a save that disables its own control drops keyboard focus to `body` (B5) — caught only by the toggle probe, not by the spec text.

## Needs the owner
Nothing this session.
