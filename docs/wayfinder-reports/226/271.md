# Autopilot report — #271 Build the Approval notice

- Map: #226 Post-login UX
- Type: wayfinder:task (execution) · Started: 2026-09-16T23:40Z · Finished: 2026-09-16T23:58Z
- Outcome: partial (BUILD phase continued; hand-off at the 150K context line, not build-done)

## What I did
1. Read the hand-off, spec, pre-flight, area primer and both lessons files.
2. Applied the #271 migration to devdb and regenerated the Prisma client (`scratch-271/migrate.mjs`).
3. Wired the drain kicks and `stageReachedAt` writes into `models/review-tasks.ts` and `models/approval-workflows.ts`; added `notifySentBack` to `models/approval-notices.ts` and called it from `sendReviewTaskBackForReview`.
4. Added `safeNextPath` (+ 4 tests, 9/9 green in `lib/auth-post-sign-in.test.ts`); `proxy.ts` now carries `?next=`; login page honours it.
5. Approvals › Invoices page: `?doc=&via=notice` opens the row and writes a `notice.opened` audit event.
6. Built the Approval-emails switch: server actions, `components/shell/approval-emails.tsx` (control + dialog host), account-menu row with `Off` suffix, Sidebar/layout plumbing, phone Account panel.
7. Admin › Configuration not-configured band via `ReadOnlyBand` `children`.
8. Hit the context line; wrote the hand-off with the exact remaining list (stop page + POST route, tests, tsc, Part B checks, r0 capture round).

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Next forbids `page.tsx` + `route.ts` in one segment — where does the stop POST live? | `/notices/stop/confirm` (POST route), page stays at `/notices/stop`; `List-Unsubscribe` header points at the POST route | Spec §4 "GET never mutates" + RFC 8058 clients POST the header URL; smallest reversible option |
| 2 | Audit `documentId` for an arrival whose doc is no longer a row? | `null` (FK to Document) | `DocumentAuditEvent.documentId` is a nullable FK; a bogus id would fail the insert |
| 3 | Admin band primitive | `ReadOnlyBand` with a `children` override | Pre-flight B4 names `ReadOnlyBand`; extend, not fork |

## Decisions recorded
None on the ticket yet (build phase).

## Tickets created / changed
None.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=32 first-critique= close-critique= first-evaluate= close-evaluate=
No capture round this session (build not finished); measure phase not started.

## First-pass findings → lessons (rendered surfaces only)
Not yet measured.

## Needs the owner
Nothing new. The remainder of the build is listed in `271.handoff.md`.
