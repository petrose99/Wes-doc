# Autopilot report — #271 Build the Approval notice: coalesced email when an Approval reaches its Approver, re-pointed reminders, per-user switch

- Map: #226 Post-login UX
- Type: wayfinder:task (execution) · Started: 2026-09-16 (session 1) · Finished: 2026-09-17
- Outcome: resolved

## What I did
This was the CLOSE phase (4 of 4) of a continued ticket; sessions 1–7 (spec/build/measure) are recorded in the hand-off and earlier commits (`ef80bb9`…`58b33e5`). This session:
1. Read the hand-off — r1's measured scores (critique 32/40 pass, evaluate 87 pass) and the one open gap: `include` FAIL at 390 because `round-271.mjs`'s keyboard probes never captured the panel's pending/save-failure/offline states, and `account-panel-switch.status` came back empty at both widths.
2. Extended `docs/wayfinder-reports/226/logs/scratch-271/round-271.mjs`: added pending/"Saving…", save-failure, and offline probes to the `account-panel` state (mirroring the existing `switch-dialog` probes), and a focus-target + h1 assertion on `stop-done-url`.
3. Started the dev server (`node .impeccable/live/dev.mjs start`) and the in-page detector live-server (`.impeccable/live/livesrv257.mjs`), ran round r2 (targeted `account-panel` subset first, then a full 39-state run).
4. Confirmed the diagnosis: the `status` field was empty because the probe read it after a `settle()` long enough for the async save to finish and the "Saving…" text to clear — a probe-script gap, not a product defect. Fixed by reading `[role="status"]` immediately after the keypress. r2 shows `"Saving…"` at both 1440 and 390, focus kept, alert text and offline reason all correct.
5. Confirmed `stop-done-url` focus lands on `BODY` at both widths after the POST→redirect — this is a full document navigation (works without JS by design), not a client-side route change, so default browser focus-reset + AT re-announcing the new document is standard behaviour, not a defect; no code change made.
6. Detector r2: 76 raw findings across 39 states, all within the documented app-wide/queue residue set (rail avatar palette, Inter, dev-overlay `layout-transition`/`dark-glow`, rail `text-overflow`, one `cramped-padding` = the already-documented Approvals "children flush" div, `skipped-offline` harness tags). Zero new real findings — matches r0/r1.
7. Ran the close-phase checks: `npx vitest run` found 4 pre-existing failures in `models/review-tasks.test.ts`/`models/approval-workflows.test.ts` — stale `toHaveBeenCalledWith` literals from this ticket's own `stageReachedAt` field that were never updated when the field was added in an earlier session. Fixed all four assertions. Full suite: 282/282 files, 2754/2754 tests passing.
8. `npx eslint .`: one error in this ticket's surface (`models/approval-notices.ts` unused `Deciders` type) — removed. Remaining 27 errors/50 warnings are pre-existing, outside this ticket's files (unrelated `lib/`/`models/` modules).
9. `npx tsc --noEmit`: clean.
10. Stopped the dev server and live-server, ran `npx next build`: succeeded.
11. Refreshed `docs/agents/areas/approvals.md` with a new "Approval notice (#271)" section: sender/coalesce/nudge model, the switch and its `busy` Switch primitive, the stop-link route split, the `stageReachedAt` clock and which functions touch it, and the capture recipe.
12. Recorded two lessons (with `check:`) in both the project (`.claude/wayfinder-autopilot/lessons.md`) and generic (`scripts/wayfinder-autopilot/lessons.md`) files.
13. Closed #271 with a resolution comment and appended to map #226's Decisions so far.

## Questions I answered on the owner's behalf
None this session — the CLOSE phase had no open design decisions, only a measurement gap and stale tests to fix per the hand-off's own diagnosis.

## Decisions recorded
Approval notice (#271) built end-to-end and closed at the bar: schema/backbone, sender (coalesce/nudge/sent-back), the per-user switch (desktop account-menu dialog + phone Account panel, `busy` Switch primitive), the signed one-click stop link (GET-render/POST-write split), Admin configuration band, Approvals arrival audit. `include` failure from r1 resolved as a probe-script timing gap, not a product defect; fixed and reconfirmed at both widths.

## Tickets created / changed
None created. No fog graduated beyond this ticket's scope; no removals encountered.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=32 first-critique=32 close-critique=32 first-evaluate=87 close-evaluate=87
in-page detector: r0 0 real findings (1440+390, every state) · r1 0 new real findings (matches r0) · r2 (close confirm) 0 new real findings across 39 states at 1440 and 390 — all within the documented app-wide/queue residue set. `include`: r1 FAIL at 390 (missing keyboard probes) → r2 PASS at both widths (pending/save-failure/offline probes added and verified; `stop-done-url` focus-on-body confirmed as expected full-navigation behaviour, not a defect). Pre-build step that was weak: the pre-flight's B5 (keyboard/focus per interactive surface) named the 1440 switch-dialog contract in detail but under-specified the 390 panel contract's pending/failure/offline sub-states — it listed only the toggle round-trip.

## First-pass findings → lessons (rendered surfaces only)
| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status: none · unchecked · wrong · new class | Action taken |
|---|---|---|---|
| `include` FAIL: 390 panel pending/failure/offline states never keyboard-probed | Pre-flight B5 (focus/keys/failure path per interactive surface) | unchecked — B5 named the contract for the 1440 dialog only; the 390 panel's parallel sub-states weren't enumerated as their own rows | Round script extended with the three missing probes; both lessons files updated with a `check:` for transient-status probe timing |
| `account-panel-switch.status` empty in r1 | none — no existing contract covers probe-timing correctness itself | new class — the probe script, not the spec, was the defect | Fixed the probe's read timing (before `settle`, not after); confirmed real value ("Saving…") in r2 |
| 4 stale test assertions after `stageReachedAt` was added | none — no contract requires re-running a model's own tests after editing its mutation payload | new class (process gap, not a spec/heuristic gap) | Fixed all four assertions; added generic + project lessons with a `check:` (`vitest run <model>.test.ts` immediately after editing an existing update/create call) |

## Needs the owner
Nothing. No removals, no credentials needed, no unresolved decisions. Six pre-existing simplifications from the build phase remain documented on the hand-off (eligibility-logic duplication, `email-layout` `align` prop, `ReadOnlyBand` reuse, arrival-audit FK-only `documentId`, stop route path `/notices/stop/confirm` vs spec's `/notices/stop`, stop page background token) — all execution-level, no ticket needed.
