# Autopilot report — #271 Build the Approval notice: coalesced email when an Approval reaches its Approver, re-pointed reminders, per-user switch

- Map: #226 Vic.ai-grammar operator app
- Type: wayfinder:task · Started: 2026-09-16 (session 1, SPEC phase) · Finished: 2026-09-16T23:40Z (session 2, SPEC phase done)
- Outcome: partial — phase SPEC complete (`milestone: spec-done`); build, measure and close are later sessions

## What I did
Session 1: claimed #271; read the map, #246's resolution, both lessons files, `preflight.md`, the Approvals area primer and the incumbent code (reminders sweep, email layer, approver rule, stage moves, drain route, login redirect, account menu, schema); loaded the intent/impeccable skills; hit the context line before the spec; wrote the research digest into the hand-off and committed (`ef80bb9`).

Session 2 (this one):
1. Read the hand-off; called `intent` (specify) and `impeccable` (shape) once each; read the glossary entry, craft-floor head and the account-menu row pattern only.
2. Wrote `docs/wayfinder-reports/226/logs/scratch-271/spec.md` — context, sender + schema (§1), deep link + sign-in return (§2), email copy matrix and preview route (§3), Approval-emails switch + stop link + Admin band (§4), state inventory (§5), a11y (§6), tests (§7).
3. Filled `scratch-271/preflight.md` Parts A–E (H1 + H5 targets, B1–B6 contracts, C coverage, E prediction).
4. Ran the Part D spec critic (fresh `Agent`, `opus` — ticket touches approval + schema). It found 2 P1s and 3 structural P2s; applied every fix to spec + pre-flight via `scratch-271/fix-spec-after-critic.mjs` and recorded the reconciliation in Part D.
5. Rewrote the hand-off with `milestone: spec-done`, exact build order, capture list and the lessons to write at close; committed `wip(autopilot): #271 spec`.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Stop link: one-click GET (ticket wording) or confirm page + POST? | GET renders a confirm page; POST writes | Critic P1: mail scanners/Safe Links GET every URL, so a mutating GET silently switches people off. One human click on the page keeps the ticket's "one click, no login" (Category 2 symmetry); RFC 8058 one-click unsubscribe POSTs anyway. |
| 2 | "New since last notice" keyed on stage index or on `stageReachedAt`? | `stageReachedAt` copied onto the `ApprovalNotice` row | Critic P1: a send-back restart re-enters the same stage index and would never re-notify. |
| 3 | Where does the nudge/reached record live? | New `ApprovalNotice` table (task × person × kind) | Ticket asks for a per-person nudge cap of two; `ReviewTask.lastReminderAt` is per task. Smallest reversible schema. |
| 4 | Does a send-back mail obey the hourly floor? | No, and it does not stamp the floor | It is one mail per human act to one person; stamping would delay the starter's next reached notice (critic P2). |
| 5 | Switch save grammar | Autosave on change with `Saving…`/alert, `Done` footer; not the Admin save bar | Single boolean, immediate feedback, no draft to lose; lesson #252 says list it explicitly as the row's save model — done in spec §4. |
| 6 | Cron cadence for the floor bound | Assumed ≤ 15 min, stated as assumption | Not recorded in the infra hand-off; recorded for the close phase to verify in the area primer. |

## Decisions recorded
None on the tracker yet (execution ticket, still open). Spec-level decisions above are in `spec.md` and the hand-off.

## Tickets created / changed
None. #272 (expense-claim sweep removal) and #273 (claims as rows) remain the owner's; the claim copy row is in the spec so #273 needs no re-spec.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=32 first-critique= close-critique= first-evaluate= close-evaluate=
Predicted evaluate: heuristic sum ≈ 8, P0/P1 0, health ≈ 85, Clean (critic's reconciled numbers; before fixes it was 30/40, sum 17, 2 P1). No surface built yet — no detector counts, no include check.

## First-pass findings → lessons (rendered surfaces only)
Not applicable yet. Two critic findings are queued as generic lessons in the hand-off (mutating GET from an emailed link; "new since" keyed on a stage index misses a restart) — written to `lessons.md` at close with their `check:` lines.

## Needs the owner
- Cron-job.org drain interval (spec assumes ≤ 15 min) — confirm at close.
- #272 sign-off (expense-claim sweep) unchanged.
