# Autopilot report — #271 Build the Approval notice: coalesced email when an Approval reaches its Approver, re-pointed reminders, per-user switch

- Map: #226 Vic.ai-grammar operator app
- Type: wayfinder:task · Started: 2026-09-16 (session 1, SPEC phase) · Finished: same session, at the context line
- Outcome: partial — spec phase not finished; hand-off written

## What I did
1. Claimed #271; loaded the map, #246's resolution, both lessons files, `preflight.md`, the Approvals area primer.
2. Read the incumbent: `models/reminders.ts`, `lib/email.ts`, `components/emails/*`, `lib/approvals/engine.ts`, `models/review-tasks.ts`, the drain route, `proxy.ts` login redirect, account menu / Account page, schema for ReviewTask / User / WorkspaceMember / ApprovalWorkflowStage, `queueArrival` (`?doc=` already supported).
3. Loaded `intent` (specify, articulate, fortify, include) and `impeccable shape` + craft-floor as CLAUDE.md requires. The skill bodies plus the reads pushed context past the 150K line before the spec was written.
4. Wrote the hand-off (`271.handoff.md`) with a research digest so the next session writes the spec without re-reading, and committed.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Where does the per-user switch persist? | A `User` column, not localStorage | The sender (job drain) must read it; #262's localStorage switch is client-only |
| 2 | Where does the 60-minute floor live? | `WorkspaceMember.noticeLastSentAt` | Ticket: one email per workspace; floor is per person |

## Decisions recorded
None on the ticket yet (spec phase incomplete).

## Tickets created / changed
None.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=n/a first-critique=n/a close-critique=n/a first-evaluate=n/a close-evaluate=n/a
No surface built yet.

## First-pass findings → lessons (rendered surfaces only)
None yet. Process observation for the lessons file at close: on a spec phase, loading five Intent skill bodies plus `impeccable` cost ~90K of context before any output — load `intent` + `impeccable` once and read the sub-skill references with `sed -n` ranges instead.

## Needs the owner
- #272: expense-claim sweep removal (pending sign-off, unchanged).
