# Hand-off — #258 Build the one status vocabulary

milestone: spec-done

## Where it stands (2026-09-16, spec phase, one session)
- Ticket claimed (assignee petrose99). Area primer read: `docs/agents/areas/detail-pane.md`.
- Spec: `docs/wayfinder-reports/226/logs/scratch-258/spec.md` (copy matrix §1, components §2, rendering §3, tests §4).
- Pre-flight filled A–E + D reconciled: `docs/wayfinder-reports/226/logs/scratch-258/preflight.md`.
- Spec critic (opus, read the code) found 4 P1s in the *first* spec — all false codebase assumptions, all fixed in the spec: (1) `saveDocumentReviewAction` redirects to `/review` so the pane's Save review navigates away and `onMutated` never fires → §2.3 `stay` option + `useActionState`; (2) `?full=1` routes pass no `history` → §1.4 the four full routes load `getSelectionAuditPanelDataAction`; (3) held-back set folded into the pill but not the glyph → `heldBack` input on `ProcessingStateInput`; (4) Save review failure is silent → `toast.error`, inputs preserved.
- Gate after reconciliation: predicted critique 32 (H1 4, H5 4, none < 3), P0/P1 0, Clean, evaluate ≈ 88.

## Decisions taken on the owner's behalf (rule: map Notes / Decisions so far / smallest reversible)
- Ledger word is *Posted* now (#248 decision wins over the ticket text's "Synced"); source stays `paymentStatus` until #281 rewires to `IntegrationPush`. `LEDGER_FACT_LABELS` is the one place.
- PO line-status words (#250 comment) stay as they are — Check vocabulary, not processing states.
- Save-review redirect fix and full-mode history are in scope (H1's premise depends on them); no removal involved.
- `aria-live` region = the Status line `<p role="status">` itself.

## Next phase: BUILD (fresh session)
Build from the pre-flight tables row by row, in this order: `processing-state.ts` labels + `heldBack` → `processing-fact.ts` (+ `daysAgo`) → `StatePills` reshape + callers (invoice-queue, receipt-queue; `receivedAt` on both row types) → `StatusLine` + `RegisteredDocument.decision` + hook deps → `saveDocumentReviewAction { stay }` + `FieldNavForm useActionState` + `onMutated` → page.tsx stepper details + `getProcessingStateInput` loader + full-route history → `ApprovalTimeline` cases + `DocumentHistory.reviewed/touchless` → facets + `statusFilter` → tests §4 (incl. retired-words grep test). Run affected vitest per edit; `tsc --noEmit` once at the end (dev server stopped). End with `milestone: build-done`.
Capture states for measure (list + pane-open at 1440/390): Approved-with-actor, In review, Needs attention (2 checks), Touchless, Cancelled, Save review success + failure, Approval tab ×3 cases, Status chip open, `?full=1`.
