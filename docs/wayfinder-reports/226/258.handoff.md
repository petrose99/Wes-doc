# Hand-off — #258 Build the one status vocabulary

milestone: spec-done
milestone: build-in-progress
milestone: build-done

Spec: `docs/wayfinder-reports/226/logs/scratch-258/spec.md`
Pre-flight: `docs/wayfinder-reports/226/logs/scratch-258/preflight.md`
Build checklist: `docs/wayfinder-reports/226/logs/scratch-258/build-notes.md` (steps 1-15, all done).
Edit scripts (applied, reference only): `scratch-258/edit-*.py`, `edit-*.mjs`.
Part B check script: `scratch-258/part-b-checks.mjs` (re-run at close).

## What is built (BUILD phase, sessions 2–4)

- Vocabulary: `lib/documents/processing-state.ts` (`PROCESSING_STATES`, `PROCESSING_STATE_LABELS`,
  `LEDGER_FACT_LABELS`, `heldBack` input) · `lib/documents/processing-fact.ts` (`processingFact`, `daysAgo`).
- Loader: `models/processing-state.ts` `getProcessingStateInput` → `getSelectionAuditPanelDataAction`
  (`DocumentHistory.reviewed` / `.touchless`) and `documents/[documentId]/page.tsx`.
- Surfaces: `StatePills` reshaped (`row-cells.tsx`) · new `StatusLine` (`components/queue/status-line.tsx`,
  pane status slot; actor upgrade from `PaneDocumentContext.doc.decision`) · stepper labels · `ApprovalTimeline`
  approved/touchless/in-review/cancelled no-flow cases (`history-tabs.tsx`) · facets built from the map
  (`invoice-queue.tsx`, `receipt-queue.tsx`) · `row-signals.tsx` uses the map · report "Posted untouched".
- Save review: `saveDocumentReviewAction({ stay })` → `FieldNavForm` `useActionState`, toasts, `onMutated("changed")`.
- Full mode (`?full=1`) loads history + server-rendered `StatusLine`; `queueTitle` threaded.

## Verified this session

- `npx tsc --noEmit`: clean.
- Tests (91 pass): `lib/documents/processing-fact.test.ts` (every §1.2 row + degrade), `processing-state.test.ts`
  (keys/labels/order), `retired-status-words.test.ts` (grep; allow-list = close-period "signed off" ×2 files,
  accounting "Synced" toast — different concepts), `components/queue/{status-line,row-cells,history-tabs}.test.tsx`
  (react-dom/server markup; `vitest.config.ts` now includes `components/queue/**/*.test.tsx`),
  `models/bills.test.ts` / `receipts.test.ts` five `statusFilter` keys (receipts: cancelled matches nothing).
- Part B: B1 every export has callers; B3 no retired word in rendered code, no hand-written state label in the
  queue shell; B4 `StatePills` non-interactive; B2 `stay` / `useActionState` / `onMutated` present.
- Date format: repo convention `Intl "en"` → "Sep 12, 2026" (row-cells, history-tabs); spec wrote "12 Sep 2026".
  Kept the shell's format (B4 primitive reuse). Tests assert the en form.
- Not done here (belongs to measure/close): live capture, critique, evaluate, include, eslint, full suite.
- Known out-of-scope residue: `lib/section-copy.ts` and `lib/documents/stages.ts` still say "Synced" for the
  pipeline stage name (spec §5 — stages.ts outside scope; #281 owns the ledger word's source).

## Next step — MEASURE phase (fresh session)

1. Read `docs/agents/areas/` primer for the queue shell / Detail pane; seed (`memory: seed --reset` recipe in
   `.claude/wayfinder-autopilot/lessons.md`), dev server on :3000 with `DEV_AUTH_BYPASS`.
2. State list for `scripts/wayfinder-autopilot/capture-round.mjs`: Invoices queue with Status chip open; pane on an
   Approved (no flow), In review (opened N days), Needs attention (2 open checks / rejected), Touchless, Cancelled
   invoice; Approval tab for each; `?full=1` for one; Receipts pane In review ("Approve from the pane" copy);
   Save review from the pane (toast + Status line change, keyboard probe: `activeElement !== body`). 1440 + 390.
3. Contact sheet → three readers (`sonnet`) → scores + triage here → `milestone: measured`. No fixes.
