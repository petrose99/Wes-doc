# Hand-off — #258 Build the one status vocabulary

milestone: spec-done
milestone: build-in-progress
milestone: build-done

Spec: `docs/wayfinder-reports/226/logs/scratch-258/spec.md`
Pre-flight: `docs/wayfinder-reports/226/logs/scratch-258/preflight.md`
Build checklist: `docs/wayfinder-reports/226/logs/scratch-258/build-notes.md` (steps 1-15, all done).
Edit scripts (applied, reference only): `scratch-258/edit-*.py`, `edit-*.mjs`.
Part B check script: `scratch-258/part-b-checks.mjs` (re-run at close).

## What is built (BUILD phase, sessions 2–4)

- Vocabulary: `lib/documents/processing-state.ts` (`PROCESSING_STATES`, `PROCESSING_STATE_LABELS`,
  `LEDGER_FACT_LABELS`, `heldBack` input) · `lib/documents/processing-fact.ts` (`processingFact`, `daysAgo`).
- Loader: `models/processing-state.ts` `getProcessingStateInput` → `getSelectionAuditPanelDataAction`
  (`DocumentHistory.reviewed` / `.touchless`) and `documents/[documentId]/page.tsx`.
- Surfaces: `StatePills` reshaped (`row-cells.tsx`) · new `StatusLine` (`components/queue/status-line.tsx`,
  pane status slot; actor upgrade from `PaneDocumentContext.doc.decision`) · stepper labels · `ApprovalTimeline`
  approved/touchless/in-review/cancelled no-flow cases (`history-tabs.tsx`) · facets built from the map
  (`invoice-queue.tsx`, `receipt-queue.tsx`) · `row-signals.tsx` uses the map · report "Posted untouched".
- Save review: `saveDocumentReviewAction({ stay })` → `FieldNavForm` `useActionState`, toasts, `onMutated("changed")`.
- Full mode (`?full=1`) loads history + server-rendered `StatusLine`; `queueTitle` threaded.

## Verified this session

- `npx tsc --noEmit`: clean.
- Tests (91 pass): `lib/documents/processing-fact.test.ts` (every §1.2 row + degrade), `processing-state.test.ts`
  (keys/labels/order), `retired-status-words.test.ts` (grep; allow-list = close-period "signed off" ×2 files,
  accounting "Synced" toast — different concepts), `components/queue/{status-line,row-cells,history-tabs}.test.tsx`
  (react-dom/server markup; `vitest.config.ts` now includes `components/queue/**/*.test.tsx`),
  `models/bills.test.ts` / `receipts.test.ts` five `statusFilter` keys (receipts: cancelled matches nothing).
- Part B: B1 every export has callers; B3 no retired word in rendered code, no hand-written state label in the
  queue shell; B4 `StatePills` non-interactive; B2 `stay` / `useActionState` / `onMutated` present.
- Date format: repo convention `Intl "en"` → "Sep 12, 2026" (row-cells, history-tabs); spec wrote "12 Sep 2026".
  Kept the shell's format (B4 primitive reuse). Tests assert the en form.
- Not done here (belongs to measure/close): live capture, critique, evaluate, include, eslint, full suite.
- Known out-of-scope residue: `lib/section-copy.ts` and `lib/documents/stages.ts` still say "Synced" for the
  pipeline stage name (spec §5 — stages.ts outside scope; #281 owns the ledger word's source).

milestone: measured

## MEASURE phase results (session 5)

Seed: dev workspace `af91555d-7450-4b21-a8ac-73db092617c8` had no touchless/cancelled/2-open-checks/
rejected/days-old-in-review rows — extended it with
`docs/wayfinder-reports/226/logs/scratch-258/{seed258,seed258b,seed258c}.ts` (run via
`npx tsx --env-file .env`). `state258.ts` queries `getProcessingStateInput`/`processingFact` per
document to find/verify candidates — reuse it before re-seeding. Resulting document IDs used for
capture are listed in `scratch-258/pw/round.mjs` `IDS`.

Capture: dev server + `livesrv257.mjs` (detector :8400) started via `.impeccable/live/dev.mjs start`.
Round script `scratch-258/pw/round.mjs` (playwright via symlinked `scratch-257/pw/node_modules`) on
top of the shared `capture-round.mjs`. **Pane URL is a path segment, not a query param** —
`/invoices/<documentId>` (and `/receipts/<documentId>`), not `?doc=`; r1 used the wrong shape and
found no Approval tab anywhere (false negative) — fixed in r2/r3. Final clean round: `shots-r3/`
(36 states, 0 errors, `detector.json`, `keyboard.json`). Contact sheets: `shots-r3/sheet-1440.png`,
`shots-r3/sheet-390.png` (`pw/make-sheets.mjs`).

States covered: invoices list + status facet open; pane on Approved (no flow) / In review (opened 3
days ago) / Needs attention (2 open checks) / Needs attention (rejected) / Touchless / Cancelled;
Approval tab for each of those 6; `?full=1` full-mode route; Receipts pane In review; Save review
click (before/after), keyboard focus check at 390.

### Scores
scores: predicted-critique=32 first-critique=32 close-critique=n/a predicted-evaluate=88 first-evaluate=72 close-evaluate=n/a

- **Critique: PASS, 32/40**, no heuristic under 3, H1=4, H5=4. Every pane state's row pill/Status
  line/stepper/Approval tab agree; no retired words found across all 36 captures. Residue noted as
  pre-existing shell (list-row `text-occlusion` on supplier/title, 390 pane-title `text-overflow`) —
  not attributed to this ticket.
- **Evaluate: SHORT, health 72/100** (bar 85), anti-pattern verdict **Clean**, but **1 P0 + 1 P1**:
  - **P0 (H1/H4)** — `?full=1` full-mode route, both widths: the Status line pill/date and the
    stepper's Checks label are occluded (`text-occlusion`, confirmed on the contact sheet) — the
    left app-nav sidebar overlaps the header/stepper area in full mode. This is the surface §1.4
    added for #258, and the one word the ticket exists to show is unreadable there.
  - **P1 (H9)** — Save review: keyboard probe `save-review-focus` shows `activeElement` after
    clicking Save review lands on `DIV.mx-auto.space-y-4.p-4` (a generic pane wrapper), not the
    Save review button — violates preflight.md §B5's explicit "focus stays on Save review" contract.
    Confirmed independently by the `include` reader (FAIL) reading the same JSON.
- **Include (keyboard): FAIL** for the same P1 above; tab order through row/pane/stepper/Approval
  tab controls is otherwise sane at both widths (no traps, nothing lands on `body` mid-walk).

### Detector residue (real, non app-wide-four)
`text-occlusion` (list rows, all states — pre-existing, not this ticket's scope beyond the full-mode
P0 above), `text-overflow` (390 pane title, shell), `tiny-text`/`cramped-padding` (in-review pane,
minor, not vocabulary). `clipped-overflow-container` on pane scaffolding at 390 — structural per the
Detail-pane area primer, no screenshot shows actually-clipped content, excluded.

## Next step — CLOSE phase (fresh session)

One fix batch (a P1 was found, so a second measurement round is allowed/expected per the ticket's
"second only if the first removed a P1" rule):
1. **Fix the P0**: full-mode (`?full=1`) route's Status line + stepper Checks label are hidden
   behind the left nav — likely a z-index/overflow or missing content-width class on the full-mode
   container introduced/exposed by wiring `StatusLine` into that route (build-notes step 9). Check
   `documents/[documentId]/page.tsx` layout around the header/stepper for a missing `lg:pl-*` or
   sidebar-offset class other full-mode content already has.
2. **Fix the P1**: `saveDocumentReviewAction`/`FieldNavForm`'s `useActionState` success path needs
   an explicit `.focus()` call back onto the Save review button after the async update settles —
   focus is currently falling through to the pane's outer wrapper div. `split-pane.tsx` ~L434-486.
3. Re-run `scratch-258/pw/round.mjs` as a confirm round (new round name, e.g. `r4`) — same states,
   same `IDS` — regenerate contact sheets, re-run the three `sonnet` readers.
4. If confirm clears (health ≥85, 0 P0/P1, include PASS): run the Part B contract checks
   (`part-b-checks.mjs`), `npx tsc --noEmit`, affected vitest files, full suite once, `eslint`,
   `next build` (dev server stopped first per lessons). Write/refresh the Detail-pane area primer
   (`docs/agents/areas/detail-pane.md`) with anything the fix touched. Close #258, post the
   resolution comment with before/after scores, append one line to map #226's Decisions so far.
5. Fill preflight.md Part G (predicted vs measured) — H1/H9 predicted 4/0 issues, measured found a
   P0 on H1-adjacent (H4 full-mode) and a P1 on H9 that Part B5's contract table should have caught
   as a build-time check, not left to the confirm round; attribute in the close report's lessons
   table (unchecked vs wrong vs new class) and write back to
   `.claude/wayfinder-autopilot/lessons.md` (project) and/or
   `scripts/wayfinder-autopilot/lessons.md` (generic) per the close-phase instructions.
