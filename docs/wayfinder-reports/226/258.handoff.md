# Hand-off — #258 Build the one status vocabulary

milestone: spec-done
milestone: build-in-progress

Spec: `docs/wayfinder-reports/226/logs/scratch-258/spec.md`
Pre-flight: `docs/wayfinder-reports/226/logs/scratch-258/preflight.md`
Build checklist: `docs/wayfinder-reports/226/logs/scratch-258/build-notes.md` (steps 1-15).
Edit scripts from this session (already applied, keep for reference): `scratch-258/edit-*.py`.

## Status per build-notes step (BUILD phase, session 3, cut at the context line)

- 1 tests for processing-state/fact: **not done** (`processing-state.test.ts` exists from earlier; no `processing-fact.test.ts`).
- 2 `StatePills` reshape: done. 3 queue wiring: done. 4 models/facets: done. 5 `StatusLine`: done and
  **revised this session** — upgrade re-runs `processingFact` with `approvedBy`/`rejectedBy` from
  `PaneDocumentContext.doc.decision`; `· ` separator before non-"by" details; 24ch actor / 80ch reason
  truncation with `title`. `ProcessingFact` now carries `input` (every return in `processing-fact.ts`).
- 6 decision on `RegisteredDocument`: **done** — `split-pane.tsx` computes `decision` from
  `history.stageDecisions` (last) else `history.reviewed` and passes it to `useRegisterDocumentActions`.
  Duplicate `export { PaneDocumentContext }` in `document-actions-menu.tsx` removed (was the only tsc error).
- 7 `saveDocumentReviewAction` `{ stay }`: **done** (`actions.ts`) — returns `ActionState<SaveReviewResult>`
  (`{ approved, missingLabel }`), redirect only without `stay`. `page.tsx` passes `stay: true` (every
  render is pane or `?full=1`).
- 8 `FieldNavForm` → `useActionState`: **done** — toasts "Approved" / "Saved — still In review: ‹label›" /
  `toast.error`; `PaneDocumentContext.onMutated("changed")` else `router.refresh()`; button shows
  "Saving…" with `aria-busy`.
- 9 full-mode history: **done** — `page.tsx` loads `getSelectionAuditPanelDataAction` when `!embedded`,
  passes `history` + a server-rendered `<StatusLine>` to `PaneFrame mode="full"`. `queueTitle` threaded:
  invoices/receipts `?full=1` routes → `DocumentDetailPage`; `getQueueDetailAction({ queueTitle })` from
  `invoice-queue.tsx` / `receipt-queue.tsx`.
- 10 loader: **done** — new `models/processing-state.ts` `getProcessingStateInput` (cached) returning
  `ProcessingFactInput` + `reviewed` + `touchlessThresholdPercent`; wired into
  `getSelectionAuditPanelDataAction` (populates `DocumentHistory.reviewed`/`.touchless`) and `page.tsx`.
- 11 stepper labels: **done** — Approval detail = `PROCESSING_STATE_LABELS[state]`; Checks "All clear";
  Sync "Posted × n" / "Post failed"; Pay "Paid" / "Paid in ledger" via `LEDGER_FACT_LABELS`.
- 12 `ApprovalTimeline` cases: **done** — "No approval steps yet" fallback removed (→ "No approval started.");
  approved case no longer requires `reviewed`; touchless footer degrades without a threshold; Receipts
  wording "No approval started. Approve from the pane."; `SplitPane` passes `state`/`queueTitle`/
  `cancelledReason` to `ApprovalTab`.
- `components/typed-destinations/row-signals.tsx`: its private label map replaced by `PROCESSING_STATE_LABELS`.
- 13 retired-words grep test, 14 statusFilter tests, 15 tsc: **not done**.

**Nothing has been typechecked or tested since this session's edits** (tsc was clean before them
except the duplicate export). Likely tsc fallout to check first: `processing-fact.ts` `input` on every
return (13 present — verify a `return { state, detail: ... }` isn't missed); `page.tsx` `state` may be
`null` where `StageStep` literals expect a string; `split-pane.tsx` `useActionState` generic order;
`history-tabs.tsx` unused `touchless` narrowing; `SaveReviewResult` import in `split-pane.tsx`.

## Next step (resume here — still BUILD phase)

1. `npx tsc --noEmit` (no dev server running); fix.
2. Write tests: `lib/documents/processing-fact.test.ts` (spec §4 table 1.2 rows + degrade), extend
   `processing-state.test.ts` (labels/keys/order), `components/queue/status-line.test.tsx` (row-derived
   vs context-upgraded via real `PaneDocumentProvider`; `role=status`), `history-tabs.test.tsx` cases,
   `lib/documents/retired-status-words.test.ts` (grep `components/**`, `app/**` non-test for
   `Unreviewed|signed off|awaiting approval|No approval steps yet|Synced` — note `lib/documents/stages.ts`
   is outside the scope), `models/bills.test.ts`/`receipts.test.ts` for the five `statusFilter` keys
   (project lesson: add new `prisma.<table>` mocks — `getProcessingStateInput` is not called from the
   list models, so those tests should be unaffected).
3. Run affected vitest files; run Part B checks (reachability grep from preflight B1; retired-words
   grep B3); `tsc --noEmit` once more; then write `milestone: build-done` here and commit
   `wip(autopilot): #258 build`.
