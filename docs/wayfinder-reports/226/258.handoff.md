# Hand-off — #258 Build the one status vocabulary

milestone: spec-done
milestone: build-in-progress

Spec: `docs/wayfinder-reports/226/logs/scratch-258/spec.md`
Pre-flight: `docs/wayfinder-reports/226/logs/scratch-258/preflight.md`
Build checklist: `docs/wayfinder-reports/226/logs/scratch-258/build-notes.md` (steps 1-15,
source of truth for what's left — this file gives the current status per step, not a
restatement).

## Status — this session (context hand-off, not a stopping point by choice)

Built, NOT YET typechecked or tested (no vitest/tsc run this session):

- Step 1 (tests): **not done** — no test files written yet for processing-state.ts /
  processing-fact.ts (both already existed from the prior session).
- Step 2 (StatePills reshape): **done** — `components/queue/row-cells.tsx` `StatePills` now
  takes `{ state, ledger, openCheckCodes, cancelledReason, trailing }`, no fallback branch,
  renders `PROCESSING_STATE_LABELS`/`LEDGER_FACT_LABELS`.
- Step 3 (queue wiring): **done** — `invoice-queue.tsx`/`receipt-queue.tsx` each compute one
  `billState(bill)`/`receiptState(receipt)` (folds `heldBack`) shared by `leading` glyph and the
  State column; `paneStatus` now renders the new `StatusLine` (step 5) built from
  `processingFact(...)`.
- Step 4 (models + facets): **done** — `models/bills.ts`/`models/receipts.ts` gained
  `receivedAt` on the row types (selected + pushed), `statusFilter` now accepts the
  `ProcessingState` keys (+ `synced`/`paid` on bills) filtered via `processingState(row)`;
  `invoices/page.tsx`/`receipts/page.tsx` param parsing updated to the new keys;
  `INVOICE_FACETS`/`RECEIPT_FACETS` regrouped into the two-section (Processing state / Ledger)
  shape.
- Step 5 (`StatusLine`): **done** — new `components/queue/status-line.tsx`. Reads
  `PaneDocumentContext.doc.decision` for the actor upgrade (see step 6 below for what feeds that
  field — **nothing sets it yet**, so the upgrade path is wired but dark until step 6/8 land).
- Step 6 (decision on `RegisteredDocument`): **partially done** — `document-actions-menu.tsx`:
  `RegisteredDocument` gained `decision`, `PaneDocumentContext` is now exported, the
  `useRegisterDocumentActions` dep array has the three `decision.*` deps. **NOT done**: nothing
  in `SplitPane` actually sets `decision` on the object it registers (still needs a `reviewed`/
  last-stage-decision prop threaded in, per spec §2.2).
- Step 7 (`saveDocumentReviewAction` `stay`): **not started.**
- Step 8 (`FieldNavForm` → `useActionState`): **not started.**
- Step 9 (full-mode history routes): **not started.**
- Step 10 (`getProcessingStateInput` loader): **not started.**
- Step 11 (stepper labels via `PROCESSING_STATE_LABELS`): **not started** — page.tsx
  `approvalDetail`/`approvalState` (≈L235-239) still say "signed off"/"awaiting approval" (a
  **retired word** per spec §1.1 — step 13's grep test will fail on this until fixed).
- Step 12 (`ApprovalTimeline`/`ApprovalTab` new cases): **done in `history-tabs.tsx`** — added
  `state`, `reviewed`, `touchless`, `queueTitle`, `cancelledReason` props and the
  approved-no-flow / touchless / in-review-no-flow / cancelled-no-rows cases (spec §1.4).
  **NOT done**: `DocumentHistory.reviewed`/`.touchless` are declared but nothing populates them
  (`getSelectionAuditPanelDataAction` in `actions.ts:233` untouched), and `SplitPane`'s call to
  `<ApprovalTab>` (`split-pane.tsx:380`) does not yet pass `state`/`queueTitle`/`cancelledReason`
  — `SplitPane` has no `state`/`queueTitle` prop yet, so this needs threading from
  `documents/[documentId]/page.tsx` (which is also step 9/10/11's file) down through
  `getQueueDetailAction` and the two queue components' `loadDetail` calls.
- Steps 13, 14, 15: **not started** (retired-words grep test, bills/receipts statusFilter test
  additions, final `tsc --noEmit`).

**I have not run vitest or tsc at all this session** — do that FIRST on resuming, before writing
any more code, to see what step 2-6/12's changes already broke (most likely: `StatePills` old
call sites if any were missed — grep confirmed only the two queue files call it; the `ledger`
type cast in `row-cells.tsx` (`LEDGER_FACT_LABELS[ledger as "synced"|"paid"]`) is a likely `tsc`
noise source worth checking first).

## Files touched this session

- `lib/documents/processing-state.ts`, `lib/documents/processing-fact.ts` — from prior session,
  untouched this session, still untested.
- `components/queue/row-cells.tsx` — `StatePills` reshaped.
- `components/queue/invoice-queue.tsx`, `components/queue/receipt-queue.tsx` — shared state,
  `paneStatus` → `StatusLine`, facets regrouped.
- `components/queue/status-line.tsx` — new.
- `components/queue/document-actions-menu.tsx` — `decision` field, exported context, dep array.
- `components/queue/history-tabs.tsx` — `ApprovalTimeline`/`ApprovalTab` new props/cases.
- `models/bills.ts`, `models/receipts.ts` — `receivedAt`, `statusFilter` reshaped.
- `app/(app)/workspaces/[workspaceId]/(queue)/invoices/page.tsx`,
  `.../receipts/page.tsx` — `statusFilter` parsing updated.

**Not yet touched**: `documents/[documentId]/page.tsx` (stepper labels, `getProcessingStateInput`,
`state`/`queueTitle` threading into `SplitPane`), `split-pane.tsx` (decision wiring,
`FieldNavForm`, `ApprovalTab` call), `actions.ts` (`saveDocumentReviewAction` `stay`,
`getSelectionAuditPanelDataAction` reviewed/touchless), the four `?full=1` routes, all test files.

## Next step (resume here)

1. Run `npx vitest run lib/documents models/bills.test.ts models/receipts.test.ts
   components/queue` (whatever exists) and `npx tsc --noEmit` to see what's already broken from
   this session's edits before writing more code.
2. Continue build-notes.md from step 1 (tests for what exists) through step 15 in order. Steps
   6/12 are half-wired (see above) — finish threading `decision` and `state`/`reviewed`/
   `touchless`/`queueTitle` through `page.tsx` → `SplitPane` → `ApprovalTab` before moving to
   step 7.
3. Do not re-spec or re-measure. Write `milestone: build-done` only once all 15 steps are
   complete and `tsc --noEmit` is clean.
