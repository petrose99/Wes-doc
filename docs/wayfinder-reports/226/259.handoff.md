# Hand-off — #259 Build the folded Detail pane header

milestone: spec-written
phase: spec (1 of 3) — NOT complete; the spec critic (pre-flight Part D) has not run

## Done this session (2026-09-16)
- Ticket claimed (assignee petrose99). Map #226, #234 resolution, #258/#260 bodies, baseline critique `.impeccable/critique/2026-09-15T19-30-56Z__…invoices…md` (25/40, H3=3, H8=2), both lessons files, `craft-floor.md`, PRODUCT.md read.
- Codebase facts gathered (no primer exists for the Detail pane area — the close phase writes `docs/agents/areas/detail-pane.md`): `components/queue/detail-pane.tsx` (DetailPane + PaneMenuItem, 149 lines), `components/pipeline/document-detail/split-pane.tsx` (Top bar L223–283, archive/flag L148–176, layout toggle L199–205, panels L313–345), `stage-indicator.tsx` (56 lines), `delete-document-button.tsx` (ConfirmDialog copy L33–39), `queue-screen.tsx` L307–318 (DetailPane wiring; `paneMenu`, `helpers.refresh`), `documents/[documentId]/page.tsx` (L45 `DocumentDetailPage({embedded})`, stepper L199–262, SplitPane props L263–329), typed `[documentId]/page.tsx` routes read `?full=1|stage|page` → `DocumentDetailPage`, `queue-actions.ts` L17 `getQueueDetailAction` (embedded: true), `pipeline-actions.ts` archive/flag/delete actions (revalidatePipeline). Probe scripts: `logs/scratch-259/probe.mjs`, `probe2.mjs` (re-run for output; do not re-read files by hand).
- **Spec written:** `docs/wayfinder-reports/226/logs/scratch-259/spec.md` (architecture: PaneFrame split, PaneDocumentContext registration from embedded SplitPane, ⋯ order/copy, source strip, stepper one-liner, full mode, 14-state inventory, keyboard plan).
- **Pre-flight A–C, E filled:** `logs/scratch-259/preflight.md`. Predicted critique 32/40 (H1 4, H5 4, rest 3), evaluate sum 9, 0 P1, Clean, health ≈ 86.

## Exact next step (spec phase continues)
1. Run pre-flight **Part D**: a foreground `Agent` (`model: "opus"` — the surface carries Delete; fresh context) that reads `spec.md` + `preflight.md` + the critique rubric (`.claude/skills/impeccable/reference/critique.md` ten 4-rows) + `evaluate`'s severity scale and scores the spec as if built; reconcile into Part D; every heuristic where critic < self is a spec change. Gate: zero P0/P1, none under 3, two 4s, Clean.
2. Open assumption to put to the critic and record on the ticket: **full mode (`?full=1`) has no Approve/Reject footer** — the decision stays on the queue (#227); the ⋯ carries everything else. #234 d.7 said "same ⋯ and footer"; spec §1 reads footer as the slot. If the critic scores this as a P1 (action unreachable), add a full-mode footer that renders the queue's decision via a server-passed `paneActions` — the alternative is documented in spec §1.
3. Then write `milestone: spec-done` here, commit `wip(autopilot): #259 spec`, and stop. The build phase is a fresh session.

## Not done
No dev server, no browser, no product code, no report scores (spec phase). Report skeleton at `docs/wayfinder-reports/226/259.md`.
