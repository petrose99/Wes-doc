# Hand-off — #259 Build the folded Detail pane header

milestone: spec-done
phase: build (2 of 3) — in progress. The spec phase is complete (spec.md + preflight.md A–E + partD.md critic gate, commit abf798a). Two build sessions have run; the tree **does not compile yet** — continue the build, do not re-spec.

> Driver note: the milestone line above is `spec-done` because that is the last *completed* phase the driver can parse. The next session is a **build** continuation: read "Exact next step" and go; do not redo the spec.

## Done (across sessions)
- Spec, pre-flight A–E, critic (partD.md) — done, committed.
- Route gates: the four typed `[documentId]/page.tsx` (invoices, receipts, purchase-orders, bank-statements) gate full mode on `query.full === "1"` only.
- `components/queue/document-actions-menu.tsx` — `PaneDocumentContext`/`PaneDocumentProvider`, `useRegisterDocumentActions`, `DocumentMenuTopItems({ closeMenu })` (Open review task as a real link via `PaneMenuItem href`, Archive/Unarchive + Flag with `busy` prop; menu closes after the action resolves), `DocumentMenuDeleteItem`, `DeleteDocumentDialog` (S13 branches), `OpenInNewTabMenuItem`. Cancelled hint reads "Cancelled documents are already closed."
- **`components/queue/detail-pane.tsx` rewritten (this session):** `PaneFrame` (mode pane|full; `name: {title, suffix}`, `status` slot, `fullHref`, `backHref`, `menu`, `actions`, `onMutated`, `archivedToast`, `headingRef`, `contentKey`) wrapping `PaneDocumentProvider`; inner `PaneMenu` (role=menu, roving ArrowUp/Down/Home/End, `onOpenAutoFocus` → first item, renders nothing when no fullHref/menu/doc; separator rule); `DetailPane` = loader rendering `PaneFrame mode="pane"` with S1 (stepper-bar skeleton) / S2 (missing + **Refresh queue** → `onMutated("removed")`) / S3 states; `PaneMenuItem` now `role=menuitem tabIndex=-1`, `aria-disabled` (not `disabled`), `aria-describedby` hint, `busy`, `href` variant. `PaneErrorBoundary` unchanged. Exports: `DETAIL_PANE_ID`, `PaneName`, `PaneMutation`, `PaneFrame`, `DetailPane`, `PaneMenuItem`.
- **`components/pipeline/document-detail/stage-indicator.tsx` (this session):** `h-9` band with its own `border-b`; `<p class="lg:hidden">` one-liner with sr-only "Step n of N, " + `<ol class="hidden lg:flex">`. It now carries its own border — the wrapper `<div className="border-b">` in split-pane was removed accordingly.
- **`components/pipeline/document-detail/split-pane.tsx` (this session, via `logs/scratch-259/edit-split-pane.mjs`, applied):** top bar and the `embedded === false` branch deleted; props `prevHref`/`nextHref`/`position`/`stage`/`afterActionHref`/`embedded` removed; `header` gains `archived: boolean; cancelled: boolean; cancelledReason: string | null`; `useRegisterDocumentActions(...)` called; source strip added (filename · **Open file** link to `/api/documents/<id>/source` · `role=radiogroup aria-label="Pane layout"` Split/Details/Source, `lg+`, ←/→ keys, `sessionStorage["pane-layout"]`); stepper stays inside `SplitPane` (deliberate simplification, name it on the ticket at close); `history` decides Approval/Audit/Checks tabs vs Activity. `DeleteDocumentButton`, `archive`/`flag`/`moveToReady`, `router`, `Link` removed from this file. **Not yet type-checked.** Remaining matches for `prevHref|afterActionHref|embedded` in the file are in comments only — clean them up when you read the file.

## Known simplifications to keep (write into code/ticket, not just here)
- Stepper rendered by `SplitPane`, not lifted into `PaneFrame`.
- `archivedToast={{ archived: "Archived — now under Closed", unarchived: "Unarchived — back in Open" }}` only on Invoices and `document-queue.tsx` (Closed facet); default plain elsewhere.
- Payment Batches: no `status`, no `fullHref`; the ⋯ renders nothing unless `menu` exists.
- Exceptions: `fullHref={(row) => \`${documentBasePath}/${row.documentId}?full=1\`}` override.
- `DocumentDetailPage`'s `embedded` *param* stays (page-level decision to wrap in `PaneFrame mode="full"`); only `SplitPane`'s `embedded` prop is gone.
- Design detector hook is **not configured** in `.claude/settings.json` (only the context-guard hook) — per-edit findings do not arrive; the close phase's in-page detector is the check. Say so in the report.
- `impeccable context` launcher needs interactive approval — read PRODUCT.md directly (project lesson).

## Exact next step (build continuation)
1. `queue-screen.tsx`: replace `rowTitle`/`rowSubtitle` props with `rowName: (row) => PaneName` (row `aria-label` = `[title, suffix]` joined; checkbox `Select ${title}`); add `paneStatus?: (row) => ReactNode`, `fullHref?: (row) => string` (default `` `${basePath}/${(detailIdFor ?? rowId)(row)}?full=1` ``), `archivedToast?`; pass to `DetailPane` as `name`, `status`, `fullHref`, `archivedToast`, and `onMutated={(kind) => kind === "removed" ? (close(), router.refresh()) : refresh()}`; add `[role=radiogroup]` to the keydown-ignore `closest(...)` selector (~L158).
2. Six callers — `components/queue/{invoice,document,receipt,exception}-queue.tsx`, `components/payments/{batch,bill-pay}-queue.tsx`: `rowName={(row) => ({ title, suffix })}` (today's rowTitle/rowSubtitle strings); remove the hand-rolled "Open in a new tab"/"Open document in a new tab" `PaneMenuItem` (+ unused `ExternalLink` imports); Invoices/Receipts: extract one `statePills(row)` function used by both the State column and `paneStatus`; Invoices/Documents: `archivedToast`; Exceptions: `fullHref` override. Bill Pay keeps "Open on Invoices" as a surface item (it is not the pane's full mode).
3. `app/(app)/workspaces/[workspaceId]/documents/[documentId]/page.tsx`: stop passing `prevHref/nextHref/position/stage/afterActionHref/embedded`; add `archived: document.archivedAt !== null, cancelled: document.cancelledAt !== null, cancelledReason: document.cancelledReason ?? null` to `header`; when `!embedded` wrap in `<PaneFrame mode="full" name={{ title: <supplier/filename>, suffix }} backHref={from ?? queuePath}>` where `queuePath = documentDestinationPath(base, document)` minus the trailing `/${id}`; read `from` from searchParams (type widen). Delete now-dead neighbour/stage computations if nothing else uses them.
4. Delete `components/documents/delete-document-button.tsx` (grep first: expect zero importers).
5. Part B checks: `grep -rn "DeleteDocumentButton\|window.confirm\|alert(" components app` → empty; `grep -rn "Open full page\|Expand details\|Show source only\|Split view\|document header\|detail sheet" components` → empty; `grep -rn "archiveDocumentsAction\|flagDocumentsAction\|deleteDocumentsAction" components` → callers only in `document-actions-menu.tsx` + bulk bar (`components/queue/document-actions.tsx`).
6. Stop any dev server, then `npx tsc --noEmit` once; fix all errors. Run vitest only for files touching `components/queue` / `split-pane` if any exist.
7. Set `milestone: build-done` here, commit `wip(autopilot): #259 build`, stop. Next phase: **close** (capture, critique, evaluate, fix batch, area primer `docs/agents/areas/detail-pane.md`, report).

## Artefacts
- Spec: `docs/wayfinder-reports/226/logs/scratch-259/spec.md` · Pre-flight: `.../preflight.md` · Critic: `.../partD.md`
- Edit scripts applied: `.../edit-pane-item.mjs`, `.../edit-split-pane.mjs`
- Report so far: `docs/wayfinder-reports/226/259.md`

## Not done
No capture, no dev server, no scores (build phase). `tsc` not run — the tree will not compile until steps 1–3 are done (`DetailPane` props changed; `SplitPane` props changed).
