# Hand-off — #259 Build the folded Detail pane header

milestone: build-done
phase: close (3 of 3) is next — capture, critique, evaluate, fix batch, area primer, report. The spec and build phases are complete; **do not re-spec or rebuild**. `npx tsc --noEmit` is clean on the committed tree (2026-09-16).

## Done (across sessions)
- Spec, pre-flight A–E, critic (partD.md) — commit abf798a.
- Route gates: the typed `[documentId]/page.tsx` (invoices, receipts, purchase-orders, bank-statements, approvals/invoices, approvals/po-mismatches) render full mode on `query.full === "1"` only; `?stage=`/`?page=` land on the queue.
- `components/queue/document-actions-menu.tsx` — `PaneDocumentContext`/`PaneDocumentProvider`, `useRegisterDocumentActions`, `DocumentMenuTopItems` (Open review task link · Archive/Unarchive · Flag, `busy` labels, closes after resolve), `DocumentMenuDeleteItem`, `DeleteDocumentDialog` (S13 two branches), `OpenInNewTabMenuItem`.
- `components/queue/detail-pane.tsx` — `PaneFrame` (mode pane|full, `name {title,suffix}`, `status` slot, `fullHref`, `backHref`, `menu`, `actions`, `onMutated`, `archivedToast`), `PaneMenu` (role=menu, roving keys, first-item focus, renders nothing when empty), `DetailPane` loader (S1 skeleton / S2 missing + **Refresh queue** / S3 retry), `PaneMenuItem` (`role=menuitem`, `aria-disabled` + `aria-describedby` hint, `busy`, `href`).
- `components/pipeline/document-detail/stage-indicator.tsx` — `h-9` band with own `border-b`; `lg+` `<ol>`, below `lg` one-liner with sr-only "Step n of N, ".
- `components/pipeline/document-detail/split-pane.tsx` — top bar + `embedded === false` branch gone; props `prevHref/nextHref/position/stage/afterActionHref/backHref/embedded` removed; `header` carries `archived/cancelled/cancelledReason`; `useRegisterDocumentActions`; source strip (filename · **Open file** → `/api/documents/<id>/source` · `role=radiogroup aria-label="Pane layout"` Split/Details/Source at `lg+`, ←/→, `sessionStorage["pane-layout"]`, `motion-safe:transition-[flex-basis]`); `fieldsReadOnly = !!history && pendingStages.length > 0` (full mode never locks); stepper stays inside `SplitPane`.
- **This session:** `queue-screen.tsx` — `rowName: (row) => PaneName` replaces `rowTitle`/`rowSubtitle` (row `aria-label` = title · suffix; checkbox `Select ${title}`), `paneStatus?`, `fullHref?` (default `${basePath}/${detailId}?full=1`), `archivedToast?`, `onMutated` (`removed` → `close()` + `router.refresh()`, else `refresh()`), `[role=radiogroup]` added to the keydown-ignore selector. **Eight callers** migrated (invoice, document, receipt, exception, approval-invoice, po-mismatch, payments/batch, payments/bill-pay): hand-rolled *Open in a new tab* items and `ExternalLink` imports removed; Invoices/Receipts share one `statePills(row)` for the State column and `paneStatus`; Invoices/Documents pass the Closed-facet `archivedToast`; Exceptions override `fullHref` to the document route; Bill Pay keeps *Open on Invoices* as a surface item. `app/(app)/workspaces/[workspaceId]/documents/[documentId]/page.tsx` — neighbour/stage computation deleted, `header` gains the three flags, `!embedded` wraps `SplitPane` in `<PaneFrame mode="full" name backHref={readOrigin(query) ?? typed queue path}>`; `searchParams` type widened with `from`/`full`. `components/documents/delete-document-button.tsx` deleted (zero importers). Edit scripts: `logs/scratch-259/edit-{queue-screen,callers,split-pane-2,page}.mjs`.

## Verified this session
- `npx tsc --noEmit` — clean (dev server stopped first).
- Part B greps: `DeleteDocumentButton|window.confirm|alert(` → only comments; B3 banned terms → none (one #225 comment mentions "batch-detail sheet", not UI copy); action callers → `document-actions-menu.tsx` + bulk bar + legacy `components/extract/extract-panel.tsx` (pre-existing bulk delete, unchanged).
- `logs/scratch-259/state-check.mjs` — all 40 spec states/strings present in code (S1–S14, menu semantics, copy).
- No vitest file covers the changed components (`components/**` is outside the vitest include set); `use-field-nav.test.ts` untouched.

## Known simplifications to name on the ticket at close
- Stepper rendered by `SplitPane`, not lifted into `PaneFrame`.
- Full mode = read + secondary actions: no decision footer (`paneActions` are per-queue client code); Approve/Reject stays on the queue (#227).
- Full-mode name: `supplier` (vendor/merchant) with `invoice_number` or filename as suffix; filename as title when no supplier.
- Back to queue in full mode = `from` (#244) else typed destination minus the row id (lands on the list, not the pane).
- Design detector hook is **not configured** in `.claude/settings.json`; the close phase's in-page detector is the check. Say so in the report.
- `impeccable context` launcher needs interactive approval — PRODUCT.md read directly.

## Exact next step (close phase)
1. Start the dev server (`node .impeccable/live/dev.mjs start` or the recipe in memory `docubite_live_ui_verification`), seed if needed, warm routes.
2. One capture round: states from spec §4 (S1, S2 via a bogus id if reachable, S4 cancelled row, S5 archived row, S6, S7 long name, S9 `?full=1`, S11 at 390, S12 Payment Batches pane, ⋯ open, Delete dialog open) × 1440 and 390 on Invoices (+ Payment Batches, Exceptions, `?full=1`), detector JSON per state, keyboard probe (h2 → ↑ → ↓ → ⋯ → ×; menu roving; radiogroup ←/→; Esc). Contact sheet, not per-image reads.
3. Header budget measurement: pane chrome ≤ 96px, first field y ≤ 290 at 1440, ≤ 100px above the strip at 390.
4. `critique` + `evaluate` + `include` readers (sonnet, contact sheet + detector JSON); Part G reconciliation against `preflight.md` predictions.
5. One fix batch, confirm, close at the bar; write `docs/agents/areas/detail-pane.md` (no primer exists yet); lessons → both files; report at `docs/wayfinder-reports/226/259.md`; stop the dev server; `eslint`, full vitest, `next build` once.

## Artefacts
- Spec: `docs/wayfinder-reports/226/logs/scratch-259/spec.md` · Pre-flight: `.../preflight.md` · Critic: `.../partD.md` · State check: `.../state-check.mjs`
- Report so far: `docs/wayfinder-reports/226/259.md`
