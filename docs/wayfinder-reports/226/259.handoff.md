# Hand-off — #259 Build the folded Detail pane header

milestone: build-done
milestone: measured
phase: close (4 of 4) is next — fix batch, confirm round, checks, area primer, report. Spec/build/measure done; **do not re-spec, rebuild, or re-run the first capture round** — recapture only what the fix batch touches plus the two coverage gaps below.

## Done (spec + build, prior sessions — see git log on this branch for detail)
- Spec/pre-flight/critic: `logs/scratch-259/{spec,preflight,partD}.md`, commit abf798a.
- Build: `PaneFrame`/`PaneMenu`/`DetailPane` (`components/queue/detail-pane.tsx`), `document-actions-menu.tsx`, `stage-indicator.tsx` band, `split-pane.tsx` (source strip, radiogroup layout control, `useRegisterDocumentActions`), `queue-screen.tsx` (`rowName`/`paneStatus`/`fullHref`/`onMutated`) + eight callers migrated, full-mode route wrapped in `PaneFrame`. `npx tsc --noEmit` clean. Full detail in the prior hand-off revision (`git log -p -- docs/wayfinder-reports/226/259.handoff.md`).

## Measured this session (round r1)
- Environment: seeded `af91555d-…` workspace, dev server + `impeccable live-server` (via node execFileSync workaround, `.impeccable/bin/0.1.5`), scratch Playwright in `logs/scratch-259` (fresh installs — reboot/session wiped `/tmp`).
- Capture: `logs/scratch-259/round.mjs` on the shared `capture-round.mjs` runner → `shots-r1/` (20 states × 1440/390, `detector.json`, `keyboard.json`). Contact sheets: `sheet-1440.png`, `sheet-390.png`. Full findings: `detector-summary.txt`.
- Detector: 97 real (non-residue) findings across 20 states; recurring types (`ai-color-palette` 34, `overused-font` 17, `dark-glow` 17, `layout-transition` 17) read as the named app-wide residue, not new. `clipped-overflow-container` (20) skews toward this surface's pane-open/menu-open states — **unconfirmed, needs a closer look in the fix batch**, not yet counted as a defect.
- **Scores** (three fresh-context sonnet readers, contact sheets + detector JSON only):
  - `critique`: **27/40** (bar 32/40, no heuristic <3 — floor met, total short). No heuristic below 3; H1/H5 (the two contract-earned 4s) unverified by the capture — no state shows the Delete dialog open or an "Archiving…" busy label.
  - `evaluate`: **9 issues, all P2 or below, 0 P0/P1, anti-pattern Clean, health ≈85/100** (bar: 0 P0/P1 ✅, health ≥85 borderline ✅, Clean ✅ — reader called it "met pending confirmation" because two items below are unresolved).
  - `include`: **pass-with-notes**. Header focus order (h2→↑→↓→⋯→×) confirmed correct for the row-1 boundary (↑ has nothing to reach, so Tab#1 landing on ↓ is right) and for full-mode at 390; **not yet confirmed**: a mid-list row (↑ actually reachable), the ⋯ menu's own keyboard round-trip (arrow nav/Esc-return), and touch-target sizing at 390.

## Open items for the fix batch (close phase) — recapture only these, don't redo r1
1. **Possible S11 violation**: `01-invoices-list-pane-open-390.png` and `05-invoices-closed-pane-open-390.png` appear to show both `←` and `×` in pane mode at 390 (spec: `←` replaces `×` below `lg`, pane mode only — full mode already correct). Confirm via DOM query, not the screenshot; fix if real.
2. Capture gaps to fill before re-scoring: Delete-confirm-dialog-open state, an "Archiving…"/busy-label state (for H1/H5), a mid-list row's ↑ Tab stop, the ⋯ menu's arrow-key/Esc round-trip, and a correct exceptions-queue route (`03-exceptions` 404'd — `/documents?stage=exception` is wrong; use `approvals/invoices` or `approvals/po-mismatches`). Also retry `04-invoices-full@1440` (page.goto timeout, transient — 390 succeeded).
3. `clipped-overflow-container` (20 hits, concentrated on this surface) — inspect at least 2-3 flagged elements directly; fix if real, otherwise note as residue with evidence.
4. Critique heuristics to push toward two 4s once the above are confirmed clean: accessibility-adjacent craft (blocked on item 1 & the menu round-trip) and spacing/rhythm (blocked on item 3).
5. Known simplifications already agreed, not findings: stepper stays in `SplitPane` (not lifted to `PaneFrame`); full mode has no decision footer (#227 owns Approve/Reject); back-to-queue precedence is `from` (#244) then typed path.

## Exact next step (close phase)
1. Fix batch: resolve items 1–4 above (DOM-confirm #1 first — cheapest, most likely real).
2. One confirm capture round (recapture only the states touched by fixes + the coverage-gap states listed in item 2), same `capture-round.mjs` runner, same contact-sheet tiling.
3. Re-run the three readers (critique/evaluate/include, sonnet) on the confirm round; Part G reconciliation against `preflight.md` predictions.
4. Close at the bar (critique ≥32/40 no heuristic <3, evaluate 0 P0/P1 health ≥85 Clean, include pass). Write `docs/agents/areas/detail-pane.md` (no primer exists yet). Lessons → both files (esp. the row-1 focus-order boundary case and the S11 pane-vs-full × question, if either is confirmed a real miss). Report at `docs/wayfinder-reports/226/259.md`. Stop the dev server and impeccable live-server (`kill` the pids from `logs/scratch-259/live-server.log` and the npm background task). Run `eslint`, full vitest, `next build` once, dev server stopped first.

## Artefacts
- Spec/preflight/critic: `logs/scratch-259/{spec,preflight,partD}.md` · State check: `logs/scratch-259/state-check.mjs`
- Round r1: `logs/scratch-259/{round.mjs,shots-r1/,sheet-1440.png,sheet-390.png,detector-summary.txt}`
- Report so far: `docs/wayfinder-reports/226/259.md`
