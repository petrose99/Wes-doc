# Hand-off — #259 Build the folded Detail pane header

milestone: build-done

## Close phase — done this session
- Bar met: critique 34/40 (no heuristic <3, bar ≥32) · evaluate 0 P0/P1, health 87/100, Clean (bar ≥85) · include pass.
- Root-caused and fixed a real P1: the Delete confirmation dialog never rendered — `DocumentMenuDeleteItem` held the dialog's open-state/JSX inside `PaneMenu`'s `PopoverContent`, which Radix unmounts on the same click that requests the dialog. Fix: state/JSX for `DeleteDocumentDialog` lifted to `PaneMenu` (above the `Popover`) in `components/queue/detail-pane.tsx`; `DocumentMenuDeleteItem` in `components/queue/document-actions-menu.tsx` now takes an `onRequestDelete` callback. Verified live (`retry-delete.mjs`, screenshot in `shots-r3/06-delete-confirm-dialog-open-*`).
- Confirmed false positives: S11 ←/× both-visible concern (mutually exclusive by breakpoint, read directly from code) and `clipped-overflow-container` (structural sheet-animation scaffolding, not clipped content) — both documented as such, not filed.
- Confirmed real: mid-list row ↑ Tab stop reachable, ⋯ menu Esc round-trip returns focus to trigger, exceptions route fixed to `/approvals/invoices`.
- Checks: `npx tsc --noEmit` clean · full vitest (268 files/2648 tests) pass · `eslint` no new errors on touched files (one pre-existing warning) · `next build` succeeds (dev server stopped first).
- Wrote `docs/agents/areas/detail-pane.md` (new primer). Appended 2 lessons to `scripts/wayfinder-autopilot/lessons.md` (generic — Radix-popover-unmount-before-dialog-render, and async-context-population capture pitfall).
- **Blocked**: could not append to `.claude/wayfinder-autopilot/lessons.md` (project lessons) — sandbox classifier refuses edits to this file without a `settings.local.json` rule (known constraint, see memory). The codebase-specific half of the lesson lives in the new area primer instead.
- Report fully written at `docs/wayfinder-reports/226/259.md` (What I did steps 16–27, scores, findings→lessons table, Needs the owner).
- Both dev servers stopped (dev :3000 via TaskStop, impeccable live-server :8400 via kill).

## Exact next step
1. Post the resolution comment on ticket #259 (content: the report's summary — bar met, P1 fixed, primer written) and **close the issue**.
2. Append one line to the map's (#226) Decisions-so-far: link to #259, gist "Detail pane header shipped; close-phase found and fixed a real Delete-confirm-dialog bug (Radix popover unmount race)."
3. Note on the map or a follow-up ticket: project lessons file (`.claude/wayfinder-autopilot/lessons.md`) needs a `settings.local.json` permission rule before an autopilot session can write to it — currently blocked every attempt.
4. Commit this hand-off + the report update (all code/doc changes from this session are already committed or ready to commit — check `git status`).

## Artefacts
- Fix commit: `components/queue/detail-pane.tsx`, `components/queue/document-actions-menu.tsx` (PaneMenu owns delete-dialog state).
- Primer: `docs/agents/areas/detail-pane.md`.
- Lessons: `scripts/wayfinder-autopilot/lessons.md` (updated).
- Captures: `logs/scratch-259/{shots-r1,shots-r2,shots-r3}/`, `sheet-1440.png`, `sheet-390.png`, `sheet-r2.png`, `sheet-r3.png`.
- Report: `docs/wayfinder-reports/226/259.md`.

## Rescore

Reopened by the rescore sweep: the ticket closed below the closing bar. The build is landed; measure the surface as it stands now, then close at the bar.
