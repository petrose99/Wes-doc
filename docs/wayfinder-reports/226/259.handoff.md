# Hand-off — #259 Build the folded Detail pane header

milestone: spec-done
phase: spec (1 of 3) complete — next session is BUILD

## Done
- Session 1 (2026-09-16): ticket claimed; map #226, #234 resolution, #258/#260, baseline critique `.impeccable/critique/2026-09-15T19-30-56Z__…invoices…md` (25/40), both lessons files, `craft-floor.md`, PRODUCT.md read. Spec + pre-flight A–C, E written.
- Session 2 (2026-09-16): pre-flight **Part D** run (`opus` critic, fresh context): 29/40, one P1 (legacy `?stage=`/`?page=` links landed in full mode with no Approve; review-task link orphaned). Eight spec changes applied via `logs/scratch-259/spec-edit.mjs` (already run; do not re-run). Gate met: predicted critique 32/40 (H1 4, H5 4), evaluate 0 P0/P1, Clean. Ruling posted on the ticket (comment 5696927206).

## Artefacts (read these first in the build phase)
- Spec: `docs/wayfinder-reports/226/logs/scratch-259/spec.md` — §1 architecture table is the file-by-file plan; §2 ⋯ table (items 1, 1b, 2, 3, sep, 4, 5) and strip copy; §3 stepper; §4 S1–S14; §5 keyboard.
- Pre-flight: `docs/wayfinder-reports/226/logs/scratch-259/preflight.md` — Part B tables are the build checklist; Part D lists the eight changes and the checks the critic found optimistic.
- Codebase pointers: `components/queue/detail-pane.tsx` (split into PaneFrame + DetailPane), `components/pipeline/document-detail/split-pane.tsx` (delete Top bar L223–283 and the `embedded===false` branch; keep `CreateReviewTaskButton` ~L408; `header.reviewLink` → ⋯ item 1b), `stage-indicator.tsx`, `delete-document-button.tsx` (extract `DeleteDocumentDialog`), `queue-screen.tsx` L158 keydown ignore list (+ `[role=radiogroup]`), L307–318 DetailPane wiring, six typed `[documentId]/page.tsx` gates (drop `|| query.stage || query.page`), `documents/[documentId]/page.tsx` L45 `DocumentDetailPage`.
- No area primer exists for the Detail pane; the close phase writes `docs/agents/areas/detail-pane.md`.
- Probe scripts `logs/scratch-259/probe.mjs`, `probe2.mjs` re-runnable for codebase facts.

## Exact next step (build phase)
1. Read this file, spec.md, preflight.md Part B; read `craft-floor.md` and both lessons files; `impeccable context` (launcher needed approval in session 2 — if blocked again, read PRODUCT.md/DESIGN.md directly).
2. Build from the tables: PaneFrame/DetailPane split → PaneDocumentContext + `document-actions-menu.tsx` → SplitPane top bar removal + source strip + layout radiogroup → StageIndicator one-liner → queue-screen wiring (`rowName`, `paneStatus`, `onMutated`, keydown ignore) → route gates → full-mode `DocumentDetailPage` wrap.
3. Run Part B contract checks (B1 grep, B3 string extraction, B4 grep, affected vitest), `tsc --noEmit` once, then write `milestone: build-done` here, commit `wip(autopilot): #259 build`, stop. No capture in the build phase.

## Not done
No dev server, no browser, no product code, no scores (spec phase). Report at `docs/wayfinder-reports/226/259.md`.
