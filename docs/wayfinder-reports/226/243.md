# Autopilot report — #243 Decide where intake lives on the distilled app: upload from the queue, the inbound address, phone upload

- Map: #226 Map: Make the operator app work like Vic.ai (grammar, not skin)
- Type: wayfinder:grilling · Started: 2026-09-16T00:22Z · Finished: 2026-09-16T00:40Z
- Outcome: resolved

## What I did

1. Claimed #243 (assigned to petrose99).
2. Loaded `grilling`, `domain-modeling`, `intent` (journey → articulate), `impeccable` (shape). `impeccable context` did not run — the launcher needs an interactive approval — so I read PRODUCT.md, CONTEXT.md and the stored critiques directly.
3. Gathered evidence: tour §6 and `invoices-queue-filter-panel.png` (Vic's green ⊕ at the far right of the header band), the journey map's intake seam, `components/extract/*`, `components/files/file-hub-upload-button.tsx`, `components/shell/mobile-upload-buttons.tsx`, `app/(app)/workspaces/[workspaceId]/pipeline/page.tsx`, `lib/config.ts` (`inboundEmail.enabled` is an env flag), Settings › Email, the queue shell's header row.
4. Ran three grilling rounds (15 questions), answering each with its ➡️ recommendation.
5. Added **Intake** and **Inbound address** to CONTEXT.md.
6. Created [#266](https://github.com/petrose99/Wes-doc/issues/266) (execution) and [#267](https://github.com/petrose99/Wes-doc/issues/267) (owner sign-off on removing phone upload); parented both to #226; #266 blocked by #243 and #267.
7. Posted the [resolution comment](https://github.com/petrose99/Wes-doc/issues/243#issuecomment-5690159691), closed #243, posted the email-intake correction to #264, appended the decision to the map's Decisions so far and added the camera-capture fog line.

## Questions I answered on the owner's behalf

| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Which queues get intake? | The four typed intake destinations only | CONTEXT.md "typed intake destinations"; derived queues can't be added to |
| 2 | Header action, drop zone, both? | Both; drop frame only while dragging | Vic's ⊕ in the header band (screenshot); one-band header budget (#225) |
| 3 | Does the queue assert the type? | Yes, select preselected and changeable | CONTEXT.md Document: user's assertion at upload is authoritative; autonomy |
| 4 | Where after adding? | Stay on the queue; rows arrive at top | #242 Newest first; #223 glyph; tour §6 #11 scoped receipt |
| 5 | Learning the inbound address | Inside the Add dialog + first-use empty state; omitted when intake is off | Visible intent at the point of need; `inboundEmail.enabled` is an env var, no in-product toggle |
| 6 | Phone upload? | No; email is the phone's intake; removal pending sign-off | #189, #232, #235; removal protocol #237 |
| 7 | Reuse the Extract overlay? | New lean dialog reusing its internals | Overlay speaks unplugged Worksheets vocabulary (#237) |
| 8 | State inventory | idle · staged · uploading (close locked) · partial failure · refused over cap · done | `fortify`; Hidden Costs; the overlay's own poller-unmount comment |
| 9 | Words | Add ‹type› / Drop to add ‹type› / Or email them to / cap and error strings | #241 fixed "Add invoices"; `articulate` — user's verb, not the system's |
| 10 | Reversibility | Existing Delete on the row / bulk bar | Smallest reversible option |
| 11 | Button form/position | Emerald filled, icon+label, band height, Vic's slot, hidden `<md` | Craft floor: one focal action per band |
| 12 | Drop-over feedback | Inset dashed frame over the queue area, 120ms, reduced-motion aware | Feedback not chrome |
| 13 | Dialog composition | Modal ≤560px, ordered sections | Operate mode, native expectations |
| 14 | Accessibility | Focus trap/return, real Browse button, one live region, ≥44px, no new key | `include`; #240 no key acts on data |
| 15 | Measure | Time to first document; intake share by channel; type-changed counter-metric | `measure` |

## Decisions recorded

Intake lives on the four typed queues as a header **Add ‹type›** button plus drop-to-add; the queue asserts the type; the operator stays on the queue; a new Add documents dialog (no worksheet vocabulary) discloses the Storage cap, locks close while uploading, ends on a scoped receipt and carries the inbound address (omitted when email intake is off); no phone upload; `/pipeline` closes. Action Summary: Intent `specify` → `fortify` → `include` → `measure`; Impeccable `onboard` → `clarify` → `harden` → `adapt` → `audit` → `polish`.

## Tickets created / changed

- #266 Build intake on the typed queues (task) — child of #226, blocked by #243 and #267.
- #267 Owner sign-off: remove phone upload (task) — child of #226.
- #264: correction comment (no "turn email intake on" link; shared address block).
- #243: resolution comment, closed.
- #226: Decisions so far appended; fog line added (phone camera capture).

## Scores and counts (rendered surfaces only)

No score; surface not built yet. Incumbent findings that shaped the recommendations are listed at the top of the resolution comment.

## Needs the owner

- #267: sign the removal of `MobileUploadButtons` and its camera-capture path by name.
- `impeccable context` could not run unattended (launcher needs approval); a `settings.local.json` allow rule for `.claude/skills/impeccable/scripts/impeccable` would let future sessions run it.
