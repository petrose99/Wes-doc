# Autopilot report — #285 Build Companies (Admin, Queue-screen shell)

- Map: #226 DocuBite's authenticated app works the way Vic.ai's does (Vic's grammar, DocuBite's skin)
- Type: wayfinder:task (execution) · Started: 2026-09-17 (session 1, spec phase) · Finished: 2026-09-17
- Outcome: partial — spec phase handed off at the context line (reading pass complete, spec/pre-flight not yet written)

## What I did
1. Claimed #285; loaded map #226, the Admin and Queue-shell primers, both lessons files, the pre-flight template, #231's 27 decisions and #254's resolution.
2. Called `intent` (specify) and `impeccable` (shape); the `impeccable` launcher is denied under the driver, so PRODUCT.md, `shape.md` and `craft-floor.md` were read directly.
3. Read the code the spec needs via three one-shot readers in `logs/scratch-285/` (QueueScreen props, `models/organizations.ts`, Workspace/Organization schema, existing forms and actions, Admin layout/nav, danger zone, dialog primitives, Payment Batches as the non-document-queue precedent).
4. Crossed the 150K context line during reading; wrote the hand-off with every spec decision reached so far and committed.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Does Add a company require an organization? | In an org: yes, creates inside it. From a personal workspace it creates an ungrouped team workspace and lands on its Companies page, which asks "Name your organization". | #231 d15 (explicit org creation on first visit, no auto-grouping); smallest reversible option. |
| 2 | Where is the reversal of "Move into ‹org›"? | Pane ⋯ "Remove from ‹org›" (owner), new additive detach model function. | Pre-flight B1 (every action names a reversal); ADR 0002 additive model. |
| 3 | Does the rename form stay on Companies? | Yes, as ⋯ Rename… (one-field Dialog) on the current company's pane. | Removal protocol (#237): nothing removed without sign-off; primer's one-save-grammar rule is for page forms. |

## Decisions recorded
None on the ticket yet; the working decisions are in `285.handoff.md` §"Decisions taken for the spec".

## Tickets created / changed
None.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=0 first-critique=0 close-critique=0 first-evaluate=0 close-evaluate=0
No surface built; no captures.

## First-pass findings → lessons (rendered surfaces only)
None yet.

## Needs the owner
Nothing yet.
