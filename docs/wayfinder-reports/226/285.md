# Autopilot report — #285 Build Companies (Admin, Queue-screen shell)

- Map: #226 Map: Make the operator app work like Vic.ai (grammar, not skin)
- Type: wayfinder:task · Started: 2026-09-17T05:41Z · Finished: 2026-09-17T05:50Z
- Outcome: partial — build phase, **step 7 of 8 done** (seed extension); step 8 (build gate) is the next session's

## What I did
1. Read the hand-off; resumed at step 7 without re-spec. Read `prisma/seed.ts` by range (`seedDevBypassOrganization`, `main`), the `createWorkspaceForUser` / `createOrganization` / `addCompanyToOrganization` signatures, and the page's state resolution lines.
2. Rewrote `seedDevBypassOrganization` to be per-item idempotent (each row found by name, no "≥2 memberships" early return): Acme Advisory → Riverside Bakery Co. + Harbor Lights Cafe (owner) + Northwind Traders (member only; owner is a Prisma-only user "Priya Naidoo"); Pine Street Consulting as an ungrouped owned team workspace (state b + Move candidate); the personal workspace (state c).
3. First seed run failed: the sandbox has no Supabase env, and the demo-account half of `main()` calls the Supabase admin API unconditionally. Gated that loop on `NEXT_PUBLIC_SUPABASE_URL` with a log line; the dev-bypass organization seeds regardless.
4. Ran the seed twice; `scratch-285/check-seed.mts` shows counts stay at 1 per named row and lists the workspace ids (recorded on the hand-off for the capture round). `eslint prisma/seed.ts` clean (dropped the pre-existing unused `catch (error)` binding — lesson #258's residue in this file); `npx tsc --noEmit -p .` 0 errors.
5. Marked step 7 `done` on the hand-off with the capture-round target ids; committed as `wip(autopilot): #285 build step 7 — seed extension`.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | The seed cannot run here (no Supabase env) — block, or make the dev-bypass half runnable standalone? | Gate the demo-account loop on `NEXT_PUBLIC_SUPABASE_URL`; keep everything else | Smallest reversible option; the demo accounts are unchanged where Supabase exists, and the capture rounds only use the dev-bypass user |
| 2 | Who owns the member-only row (Northwind)? | A Prisma-only user, not `demo@docubite.local` | The demo user does not exist without Supabase; nobody signs in as Northwind's owner, so no identity is needed |

## Decisions recorded
None on the ticket (build WIP).

## Tickets created / changed
None.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=32 first-critique= close-critique= first-evaluate= close-evaluate=
No rendered surface captured this session (seed only). Detector counts come at the build gate (step 8).

## First-pass findings → lessons (rendered surfaces only)
| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| — | — | — | — |

## Needs the owner
Nothing. Note for the close phase: `docs/agents/areas/admin.md` should record the seed recipe (states a/b/c, the non-owner row, the Move candidates).
