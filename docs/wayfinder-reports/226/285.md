# Autopilot report — #285 Build Companies (Admin, Queue-screen shell)

- Map: #226 Post-login UX — every screen through the Queue-screen shell
- Type: wayfinder:build · Started: 2026-09-17 ~08:00 · Finished: 2026-09-17 (session 10, cut at the context line)
- Outcome: partial — build phase, step 8 (build gate) in progress; r0 captured at 1440 and 390, every real finding and failing probe fixed in place; one re-capture and the tsc read left for the next session.

## What I did
1. Read the hand-off; step 8 was the first `todo`. Wrote `scratch-285/servers.mjs` (seed + check, next dev on :3000 via `devserver.mjs`, impeccable live-server on :8400, `down` by port pid) and brought both up with the idempotent seed (states a/b/c confirmed by `check-seed.mts`).
2. Before the round: wired spec §5.4 (deep link to an id outside the viewer's rows → nameless notice, h1 focused) and fixed `afterMutation` to push the shell's `…/companies/<id>` path (it pushed `?company=`, which nothing consumed).
3. Wrote `scratch-285/round-285.mjs` on `capture-round.mjs` — 24 states per width (owner list + Add/Move dialogs, filtered, owned/current/member panes with ⋯ menus, Rename/Remove/Delete/Leave confirms, member view, deep links present/missing, ungrouped (b) + Name dialog + name_taken, personal (c) + Create dialog) with a B5 keyboard probe on every dialog; `--mutate` adds rename-success and remove-success. Ran it as `shots-r0`.
4. Fixed the r0 findings in place (list on the hand-off): focus return for menu-opened dialogs (Move/Rename/Remove → their triggers; Remove success → h1; Rename success → pane ⋯ after the refreshed rows), Rename dialog mounting empty (keyed by row), org `name_taken` copy, duplicate row count, currency option em-dashes, Move dialog 12px text (bisected to the long submit label with `probe-bisect.mjs`), pane/danger paragraphs at 95 chars. Re-ran r0: 48 states, 0 errored, all keyboard probes green at both widths; cramped-padding fix applied after the last capture (one state to re-capture).
5. Restored the seed after the mutation probes (`restore.mts`; the combined run had also let the 390 Add probe create a second Harbor — lesson recorded on the hand-off: run `--mutate` as its own round, last).
6. Started `npx tsc --noEmit -p .` and stopped the servers; the context line was crossed before tsc returned — result to be read next session.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Keep `stat` "3 companies" beside the shell h1's own count? | Dropped `stat` | Spec §3.1 names the count as the h1's trailing figure; two counts is H8 noise |
| 2 | Currency options "USD — US Dollar" (163 em-dashes flagged)? | "USD · US Dollar" | Detector finding is real on 160+ options; the app's suffix separator is `·` (row suffixes, pane heading) |
| 3 | `flat-type-hierarchy` on dialog states (Dialog title 18px vs h1)? | Report as residue, not fixed here | Shell `Dialog`/`QueueScreen` primitives — a shell ticket, per generic lesson "name residue once" |

## Decisions recorded
None closed — build phase in progress.

## Tickets created / changed
None.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=32 first-critique= close-critique= first-evaluate= close-evaluate=
In-page detector r0 (after fixes) at 1440 and 390: every state = app-wide residue only (ai-color-palette ×2, overused-font, layout-transition, dark-glow) + shell `flat-type-hierarchy` on dialog-open states and the filtered-empty state; `cramped-padding` ×2 on the Move dialog fixed after the last capture (re-capture pending). Keyboard: 56 probes, all pass (initial focus, Esc return-to-trigger, disabled Remove-on-current with reason, Delete typed-name gate, deep-link notice focus, Remove success → h1). Include check: measure phase.

## First-pass findings → lessons (rendered surfaces only)
| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| Move/Rename/Remove Esc → focus `body` (H3/include) | B5 named the return targets, but the shell `Dialog` restores to `document.activeElement` at open, which for a menu-opened dialog is the unmounted menu item | new class | `focusAfterClose(selector)` per dialog; candidate generic lesson: "a dialog opened from a popover menu item must name its return target — the opener is gone" |
| Rename dialog opened with an empty field (H1) | B5 "input, selected" — probe found `selected: 0` | unchecked | keyed dialog by row id |
| `?company=` deep link ignored; §5.4 notice unwired (H1) | Step 5 note said so; caught by the deep-link probes | none | wired `initialMissing` + shell path |
| Duplicate count in header (H8) | Spec §3.1 said h1 figure only | unchecked | dropped `stat` |
| Org `name_taken` said "A company called this company…" (H9) | B3 string extraction was never run against §7 | unchecked | mapped in place |

## Needs the owner
Nothing.
