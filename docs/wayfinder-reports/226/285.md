# Autopilot report — #285 Build Companies (Admin, Queue-screen shell)

- Map: #226 Post-login UX (Admin / Organization surfaces)
- Type: wayfinder:task · Started: 2026-09-17 (session 2, spec phase) · Finished: same session
- Outcome: partial — **spec phase done** (`milestone: spec-done`); build, measure, close are the next sessions'

## What I did
1. Read the hand-off from session 1, claimed the ticket (assignee), loaded `intent` (specify) and `impeccable` (shape; launcher denied, read PRODUCT.md directly), the preflight template, both lessons files, the Admin and Queue-shell primers, the Bill Pay critique baseline, CONTEXT.md glossary entries (Company / Organization / Admin).
2. Verified the remaining shell facts by grep (QueueScreen `menu`/`stat`/`empty`/`PaneHelpers`, Override Mode item unconditional, Bill Pay's primary lives in the bulk bar → two additive shell props needed).
3. Wrote `logs/scratch-285/spec.md` (nine sections, all states, copy matrix, a11y) and `preflight.md` Parts A–E.
4. Ran the Part D spec critic (`Agent`, opus, fresh context, text only). First pass: critique 28/40, evaluate health ≈ 72, 4 P1, Not clean. Reconciled every finding into spec changes (see below); predicted after reconciliation 32/40 · health ≈ 88 · P1 0 · Clean. A confirm re-score was not run (agent could not be resumed; context line reached).
5. Wrote the hand-off with `milestone: spec-done`, this report, committed `wip(autopilot): #285 spec`, posted the continue comment.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Where do "Name your organization" and "Move into ‹org›" live — a band (session-1 draft) or shell slots? | Shell first-use empty state + Dialog; Move in the header ⋯ menu + Dialog | #225 "no band" (Bill Pay aging is the one documented exception); smallest reversible |
| 2 | Header primary "Add a company" — misuse `stat` slot or add a prop? | Additive `primaryAction` prop on `QueueScreen`; also `overrideMode={false}` | B4: shell-level additive props, not a bespoke header; Override Mode is document-only |
| 3 | Org-owner visibility of a moved company (critic) | Keep ADR 0002 (access per company); reword the consequence copy | ADR wins; visibility grant would be a schema/auth decision for #287 |
| 4 | Create organization: confirm dialog or disclosure? | Disclosure sentence "can't be changed here yet" in body + dialog description | Intent honesty / visible consequence; a confirm on a non-destructive create adds friction |
| 5 | Delete of the org's last company | Name "‹Org› will be left with no companies" in the Delete body; dissolve stays #287 | Hidden-consequence anti-pattern removed; removal-type decisions are not mine |
| 6 | State (c) creates "a company" or "a team workspace"? | "Create a team workspace"; toast "‹Name› created — name its organization next" | CONTEXT.md glossary: a company is a team workspace *in an organization* |

## Decisions recorded
On the hand-off and spec (ticket not yet resolved): two gates (header = current-workspace owner, pane = row owner); success selection clears filters; nameless deep-link notice; `last_owner_required` at submit → disabled-with-reason; only-company Remove reason; `/workspaces?notice=` toast carrier; vocabulary rule for "team workspace".

## Tickets created / changed
None. Notes for #287 recorded on the hand-off (org rename/dissolve, empty org after last delete).

## Scores and counts (rendered surfaces only)
scores: predicted-critique=32 first-critique=0 close-critique=0 first-evaluate=0 close-evaluate=0
No surface built yet; predicted evaluate health ≈ 88, P1 0. Detector/include counts come with the measure phase.

## First-pass findings → lessons (rendered surfaces only)
Not applicable yet (spec phase). Spec-critic findings that the pre-flight template had no row for, worth a generic lesson at close if they recur: (1) a success-selection after create/move must clear active facets or it lands on a filtered-out row (B2 row "success selection"); (2) role gating stated once for "the owner" hides a per-row vs per-route gate difference (B1 row "which entity's owner").

## Needs the owner
Nothing blocking. Optional: a 5-minute confirm re-score of the reconciled spec at the start of the build session.

---

## Build session — step 1 (2026-09-17)

Phase: BUILD (2 of 4), one plan step per session. No build plan existed on the hand-off yet, so this session wrote one (8 steps, from spec §2–§9 and preflight B4/B1) and built step 1 only.

### What I did
1. Read the hand-off, `spec.md` and `preflight.md` whole (283 lines combined) — no re-spec.
2. Read the actual `models/organizations.ts` and `models/organizations.test.ts` to verify the hand-off's guessed function signatures against real code — found the hand-off's `listOrganizationCompanies(orgId)` should read `(organizationId, userId)`, and the reused workspace actions live at `app/(app)/workspaces/[workspaceId]/workspace-actions.ts`, not a top-level file. Corrected both in the hand-off's progress notes.
3. Wrote the 8-step build plan to the hand-off (`milestone: build-plan-written`).
4. Built step 1: `models/organizations.ts` gained `_count.members` on `listOrganizationCompanies`, `organizationCompanyCount` (org-total for the "k more" line, mirroring the `hiddenCompanyCount` pattern already used in `models/admin-users.ts`), `listOwnedUngroupedTeamWorkspaces` (Move dialog's eligible list), `removeWorkspaceFromOrganization` (mirrors `moveWorkspaceIntoOrganization`'s owner check). Added matching tests to `models/organizations.test.ts`.
5. `npx vitest run models/organizations.test.ts` — 14/14 pass. Marked step 1 `done`, committed (`6c6d47c`), stopped without running the full suite/tsc (reserved for the build gate, step 8).

### Note on the commit
The working tree had unrelated files already staged by a concurrent session (#286: `admin-save-bar.tsx`, `user-confirm.tsx`, `detail-pane.tsx`, `confirm-dialog.tsx`) when this session started. `git commit` (no pathspec) committed those alongside this session's `git add` of the #285 files, so commit `6c6d47c`'s message describes only the #285 change but its diff also carries #286's pre-staged work. No content was lost or altered — only the commit boundary is inexact. Flagging for the owner; not something I could safely unwind mid-session on a shared branch.

### Scores and counts
Not applicable this session (no rendered surface touched; model-layer step only).

### Needs the owner
- The commit-boundary note above (informational, not blocking).
- Everything else per the spec-phase report above.
