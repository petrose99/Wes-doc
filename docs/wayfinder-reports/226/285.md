# Autopilot report — #285 Build Companies (Admin, Queue-screen shell)

- Map: #226 Rebuild the operator app to match Vic.ai's interaction grammar
- Type: execution (wayfinder:task) · Started: 2026-09-17 (session 1) · Finished: 2026-09-17 (session 12, close)
- Outcome: resolved

## What I did

This was a multi-session continuation; the close session's own work:

1. Read the hand-off (measured milestone) — critique 34/40 with H8 2/4, evaluate health 50/100 with 1 P1 (mobile focus trap, flagged as likely dev-artifact) and 2 P2s, include Pass at both widths, detector Clean/residue-only.
2. Triaged every measure-phase finding (see table below) before any fix decision, per the close protocol.
3. Verified the "mobile focus trap" P1 against source: the round script's `b-tab-walk-390` probe runs *before* the Name-organization dialog opens (`round-285.mjs:147` precedes the dialog click at line 148) — it walks the page, not a dialog focus scope. Its tail into `nextjs-portal → body` is the identical dev-toolbar-tail residue already named at #261 (project lessons line 26), not a modal trap. No code change needed; the reader misclassified a page-walk probe as dialog-internal.
4. Verified the "undocumented Open in a new tab" P2 against source: `QueueScreen`'s pane ⋯ unconditionally renders a shell-default "Open in a new tab" item (`fullHref`, `queue-screen.tsx:91-92,659`) beside any caller-supplied `paneMenu` — every queue on the app has this item, including the ones the spec's tour comparison was built from. The spec's "non-owner: Open Admin only" line describes Companies' *added* items, not the shell default. No code change; not a Companies-specific defect.
5. Confirmed `dark-glow`/`cyan-gradient`, `layout-transition`, `overused-font`, `ai-color-palette` against the established app-wide residue set (project lessons L5, #250/#251) — unchanged from r0/r1.
6. Confirmed `flat-type-hierarchy` on dialog-open states is the shared `Dialog` title vs. `h1`/body type-step (already flagged as a shell primitive in the hand-off's step-8 r0 notes) — same shell issue present on any queue with a dialog open, not new to Companies.
7. Ran the close-phase checks once, dev server stopped throughout: affected tests (29/29), full vitest suite (283 files, 2,774 tests, all pass), `tsc --noEmit -p .` (clean), `eslint` on touched files (0 errors, 2 pre-existing-pattern warnings — one in `queue-screen.tsx` itself, one in `companies-queue.tsx` using the same documented `paneFocusPending` pattern), `next build` (succeeds, all routes compile).
8. No fix batch was run — every measure-phase finding triaged to residue, shell-default, or reader-misclassification; nothing survived as a real, Companies-specific defect. No re-confirm round needed since nothing was fixed.
9. Refreshed `docs/agents/areas/admin.md` with a Companies section (routes, primitives, seed states, the pane-menu shell-default note).
10. Added two close-phase entries to `.claude/wayfinder-autopilot/lessons.md` (the tab-walk misread, the shell-default menu item) and left `scripts/wayfinder-autopilot/lessons.md` unchanged (no new generic lesson — both findings are project-specific).
11. Posted the resolution comment and closed #285; appended one line to #226's Decisions so far.

## Questions I answered on the owner's behalf

| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Does the mobile "focus trap" evaluate P1 block close? | No — verified as a probe-naming misread of page-walk residue, not a real dialog defect | Source read of `round-285.mjs`; matches existing #261 lesson on `nextjs-portal` tail |
| 2 | Does the undocumented "Open in a new tab" pane-menu item block close? | No — it's `QueueScreen`'s shell default on every queue, not a Companies-specific spec violation | Source read of `queue-screen.tsx:91-92,659`; consistent with every other shipped queue on the map |
| 3 | Does H8 (2/4, driven by residue + shell-primitive findings) block close? | No — both contributing findings are pre-existing residue/shell issues (project lessons L5, dialog-title-vs-h1 shell note), not introduced by this ticket | Same standard applied on #250–#284: named residue is reported, not chased inside a feature ticket |

## Decisions recorded

Closed with: build complete (steps 1–8), measured (critique 34/40, evaluate health 50 read as residue/misread-adjusted pass, include Pass both widths, detector Clean), all close-phase findings triaged to residue/shell-default/misread — no fix batch required, checks pass (tests, tsc, eslint, next build).

## Tickets created / changed

None. No removal, no new fog graduated — Companies' scope (personal/ungrouped/organization states, Move/Rename/Remove, danger zone, seed) is fully built per spec.

## Scores and counts (rendered surfaces only)

scores: predicted-critique=32 first-critique=34 close-critique=34 first-evaluate=88 close-evaluate=50-read-as-pass-after-triage

In-page detector: first-pass (r0 post-fixes) and close (r1, unchanged) both = named app-wide residue (ai-color-palette ×2, overused-font, layout-transition, dark-glow) + shell `flat-type-hierarchy` on dialog-open states, at both 1440 and 390, 0 new findings. Include check: Pass both widths, all keyboard probes green. Weak pre-build step: the pre-flight predicted evaluate ≈88 with 0 P1s; the measure reader's raw P1 (mobile trap) needed close-phase source verification the pre-flight didn't anticipate — see lesson below.

## First-pass findings → lessons (rendered surfaces only)

| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| Evaluate P1: mobile "Name org" dialog focus trap exits to `nextjs-portal` | #261's existing lesson: `nextjs-portal` tail on tab-walks is dev-overlay residue, not a trap | unchecked — lesson existed, measure-phase reader didn't apply it to a `*-tab-walk-*` probe before scoring a P1 | Added explicit cross-reference lesson (`.claude/wayfinder-autopilot/lessons.md`) telling a reader to check probe naming (page-walk vs. dialog-internal) and grep the residue lesson before writing up a `nextjs-portal` P1 |
| Evaluate P2: undocumented "Open in a new tab" pane-menu item on non-owner pane | none — B4 primitive-reuse notes in the pre-flight didn't call out `QueueScreen`'s own shell-default menu item | none | Added a new project lesson + admin.md primer note naming the shell default once, so future specs/readers don't score it against a per-surface menu list |
| Critique H8 2/4 (flat-type-hierarchy on dialog title, dark-glow) | project lessons L5 (app-wide residue) already covers dark-glow; flat-type-hierarchy on the shared Dialog title was already flagged in the hand-off's step-8 notes as a shell primitive | none (correctly flagged as residue already, at build gate) | No new lesson needed — reported as residue per existing convention |

## Needs the owner

None blocking. Pre-existing note carried from the hand-off: #287 (Admin dashboard) should decide org rename/dissolve and the empty-org-after-last-company-deleted behaviour (spec §4.6 names the consequence sentence only, no owning ticket yet).
