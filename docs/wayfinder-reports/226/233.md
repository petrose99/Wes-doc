# Autopilot report — #233 Decide one status vocabulary for the Detail pane (row pill · pane pill · stepper · Approval tab)

- Map: #226 Make the operator app work like Vic.ai (grammar, not skin)
- Type: wayfinder:grilling · Started: 2026-09-15T23:30Z · Finished: 2026-09-15T23:50Z
- Outcome: resolved

## What I did

1. Loaded the map and the ticket; claimed #233 (assigned self).
2. Called the Skill tool for `grilling`, `domain-modeling`, `intent` (→ `articulate`, `fortify`), `impeccable` (→ `clarify`). `impeccable context` could not run: the launcher needs an approval the unattended session cannot give, so I read `PRODUCT.md`, the `clarify` reference and the stored critique directly (no `DESIGN.md` exists). `craft-floor.md` was not loaded — planning-only ticket, no UI edit.
3. Gathered the evidence: critique `.impeccable/critique/2026-09-15T19-30-56Z__paces-workspaceid-queue-invoices-page-tsx-a507843a.md` (25/40, H1 = 2, H4 = 2, P0), CONTEXT.md → Processing state / Touchless / Approval, tour §6 (batch-detail summary strip; Approval tab = STEP n + named user + INITIATED footer), and the code paths that print each of the four words (`components/queue/row-cells.tsx:32`, `components/pipeline/document-detail/split-pane.tsx:236`, `app/(app)/workspaces/[workspaceId]/documents/[documentId]/page.tsx:211`, `components/queue/history-tabs.tsx:25`, `lib/documents/processing-state.ts`, `models/audit-events.ts`). Found the root cause: four derivations from four different inputs, only the #223 glyph reads `processingState()`.
4. Read neighbouring tickets #223 (closed), #234 (blocked by this one), #235 to avoid collisions.
5. Ran three grilling rounds (12 questions), answering each with its `➡️`.
6. Updated CONTEXT.md: Processing state sharpened (one vocabulary, untouched = In review, retired synonyms under _Avoid_); new term **Status line**.
7. Created execution ticket [#258](https://github.com/petrose99/Wes-doc/issues/258), wired as a child of #226 and blocked by #234.
8. Posted the [resolution comment](https://github.com/petrose99/Wes-doc/issues/233#issuecomment-5689749685), closed #233, appended the decision to the map's Decisions so far, and left #233's input as a [comment on #234](https://github.com/petrose99/Wes-doc/issues/234#issuecomment-5689753674).
9. Committed CONTEXT.md and this report.

## Questions I answered on the owner's behalf

| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Is CONTEXT.md's five-word Processing state the single vocabulary, no sixth word? | Yes; *Unreviewed*, *Reviewed*, *signed off*, *awaiting approval*, raw status strings retired; untouched document = In review | #177 baseline (#222/#223); critique H1/H4; Intent Cat. 9 Inconsistent Patterns; `clarify` "same noun for the same concept" |
| 2 | One derivation (`processingState()`) for all four surfaces? | Yes, plus one `PROCESSING_STATE_LABELS` map | Pill and glyph already disagree on the same row (escalated); `stages.ts` comment asks for "one place" |
| 3 | Ledger facts stay a second axis? | Yes — Synced/Paid/Reconciled as their own mark | CONTEXT.md Processing state; Vic keeps Status and Invoice Approval as separate facets; ADR 0001 |
| 4 | Facet and legacy labels follow? | Status facet → Processing state + Ledger groups; `STAGE_LABELS` API-only | Tour §6 hierarchical facet; map fog "facet-panel parity" |
| 5 | Does the pane pill survive? | No — replaced by a Status line (StatePills + fact sentence); placement is #234's | Vic batch-detail summary strip (§6); critique H8 "introduces itself four times"; `clarify` "say each idea once" |
| 6 | Does the stepper survive; Approval node words? | Survives (structure is #234's); node details use the five words + ledger facts | Documented exception (combined pane); Q1 |
| 7 | Approval tab on a document approved without stage decisions? | From the audit log: *Reviewed and approved by ‹actor› · ‹date›* + footer; Touchless fact; honest empty state in review | Tour §1 "real audit trail, not a status enum"; `document_reviewed` event exists with actor/time; `clarify` "don't promise what the system can't know" |
| 8 | Rename the tab? | No, stays Approval | #227/#232 fixed the tab set; smallest reversible option |
| 9 | Missing actor / old documents? | Degrade to date (`reviewedAt`), then the word alone; never invent a name | `fortify` partial state |
| 10 | Accessible name / announcement? | Text-first `<p>`, existing live region, no extra role | `include`, WCAG 1.4.1; critique Sam persona |
| 11 | Capitalisation and tense? | Exact CONTEXT.md strings, sentence case, from one map | `clarify`: inconsistency is the defect, not tense; Vic mixes tenses too |
| 12 | Other queues? | Same words everywhere; bank statements keep four stepper nodes | #223 already runs `processingState()` on Receipts |

## Decisions recorded

Five Processing-state words are the only status vocabulary, derived once; the pane pill becomes a Status line carrying the state and the fact behind it; the stepper keeps its shape but speaks the same words; the Approval tab reads the workflow-less sign-off and Touchless from the audit log and is never empty on an Approved document; facets regroup into Processing state + Ledger. Glossary: Processing state sharpened, **Status line** added.

## Tickets created / changed

- Created [#258 Build the one status vocabulary…](https://github.com/petrose99/Wes-doc/issues/258) (`wayfinder:task`), child of #226, **blocked by #234** so the status line is built once into the decided header stack.
- Closed #233; #234 is now unblocked (its only blocker was #233) and carries #233's input as a comment.
- No fog graduated or added; the "facet-panel parity" fog item is partly sharpened by decision 5 on #258 (Invoices and Receipts) and stays for POs/Bank Statements.

## Scores and counts (rendered surfaces only)

No score; no surface built. Incumbent evidence that shaped the recommendations: critique 2026-09-15T19-30 Invoices Queue screen 25/40 — H1 Visibility of status 2, H4 Consistency 2, H8 Minimalist 2; in-page detector 6/5 list · 6/5 pane-open (1440/390). #258 closes on H1 ≥ 3 and H4 ≥ 3, evaluate ≥ 80, detector before/after.

## Needs the owner

- Nothing blocking. No removals: the pane pill's retirement is a replacement of one element by another on the same surface (the Status line still shows the state), not a feature removal; if the owner reads it as one, #258 is where to say so.
- `impeccable context` needs a one-time approval to run in autopilot sessions; until then each session reads the context files by hand.
