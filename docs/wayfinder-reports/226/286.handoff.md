# Hand-off — #286 Build Users (Admin, org-level Queue-screen shell)

milestone: spec-done
milestone: build-done
milestone: measured
milestone: close-partial

Full history of this file (spec, build steps, G2/G3 records, raw r1 findings, session-13 triage) is in
git at `470b4c8`. Artefacts: `docs/wayfinder-reports/226/logs/scratch-286/` — `spec.md`, `preflight.md`,
`round-286.mjs` + `run-round.mjs <rN> [only]`, `servers.mjs up|down`, `shots-r1/` (measure), `shots-r2/`
(close confirm, 46 states × 1440/390, `detector.json`, `keyboard.json`, `contact-r2.png`),
`verify-p0.mjs` (list renders + Tab walk at 1440), `verify-close.mjs` (owner-phone bank line, Ctrl+S,
"Co.." period, Invite "Open their row" link), `check-personal.mts` (personal workspace = owner only).

## What is built (all committed; see git history for the per-step record)
- Data: migration `add_workspace_member_bank_details`; `models/workspaces.ts`, `models/organizations.ts`,
  `models/admin-users.ts` (`loadUsersPage`, `loadUserRow`; `bank` only for owner-of-current or self).
- Shell props (additive): `QueueScreen.primaryAction`, `overrideMode`, `search`; `AdminSaveBar` `inPane` + `status`.
- `lib/admin/users.ts` (client-safe; `actionErrorText`, `endSentence`), `admin/users/actions.ts` (8 owner-gated actions).
- Surface: `components/admin/users-queue.tsx`, `user-detail.tsx`, `user-confirm.tsx`; `admin/users/page.tsx` +
  `[userKey]/page.tsx`. Area primer: `docs/agents/areas/admin.md` § Users.

## Close (session 14) — triage of the r1 raw findings
| Finding | Label | Evidence |
|---|---|---|
| `a-list-owner-1440` blank page (P0, all readers) | tooling | Uncaptured: dev server stuck at `Compiling /` on a stale 4.9 GB `.next/dev/cache/turbopack`; cleared, `/` compiles in 25 s; `verify-p0.mjs` → 6 rows, 118 KB; r2 captured it cleanly |
| `list-tab-walk-1440` never leaves `body` (P0) | tooling | Same uncaptured state; r2 walk: skip links → rail → Account menu (20 presses, all reachable) |
| Personal workspace shows 3 rows (P0, D1) | tooling | Seed contamination by `scripts/dev/db253.ts`; no app path adds members to a personal workspace; cleaned; r2 `c-controls: rows 2` (owner + the pane's own row-as-link) |
| Esc/pane-✕ discards unsaved edits (P0/P1, H3/D3/include) | decision → **#327** | Shell-wide: `queue-screen.tsx:415` `close()` unconditional, `AdminLeaveGuard` covers links/Back only; r2 `pane-esc-unsaved-guard-1440: dialog 0` stands |
| Leave absent on own non-owned row (P1, D1) | real → fixed (session 13) | r2 `self-leave-northwind-present-1440: count 1` |
| "Ask an owner: an owner can…" to an owner on phone (P1, D1) | real → fixed (session 13) | `verify-close.mjs` `phoneOwnerBank.askOwner: 0` |
| Per-company Invite error (P1, H5/D1/D3) | misread + real gap → fixed | Spec §4.1 is dialog-level text; the missing "(link opens that row)" now links `u:<userId>` (`verify-close` `inviteExisting.link 1`) |
| `a-pane-invited-390` click timeout (P1, H3) | tooling | Probe asks the ⋯ menu for Resend at 390; `user-detail.tsx:107` `canManage = … && !phone` — the phone lane is read-only by design (d18) |
| `invite-send-disabled-none-ticked: false` | tooling | Probe never unticks; the dialog pre-ticks the current company |
| `.truncate` text-overflow ×13 at 390 (P2, H8) | residue | G2 triage: ellipsis false positive, visually clean; same on #285 |
| `dark-glow`, `ai-color-palette`, `overused-font`, `layout-transition` (45×) | residue | App-wide dev overlay/switcher set (project lessons line 5) |
| `line-length` ~93 ch, `em-dash-overuse`, `flat-type-hierarchy`, `body-text-viewport-edge` | residue | Shell/copy residue named at #285's close (band `<p>` is `max-w-[70ch]` at `text-xs`) |
| Ctrl+S untested (P2, D4/include) | coverage → verified | `verify-close`: Ctrl+S submits the pending role; two-phase last-reviewer confirm opens; Cancel keeps the draft |
| "Riverside Bakery Co.." (P3, D3) | real → fixed | `endSentence()` in `lib/admin/users.ts`, used at every name-terminated sentence |
| Deep-link-missing focus on `body` | residue | Shell-wide (G2 record), notice renders |
| Bank error grouping, generic self-demotion consequence, heavy Resend confirm, card role label (P2/P3) | residue / spec-as-designed | Spec §4.5 / §3.4 / §4.1 as written; no decision behind them |

## Close scores (session 14, readers on `shots-r2` with the triage above, fresh sonnet agents)
- critique **36/40**, lowest 3 (H1 3 copy-link status unread by probe · H2 4 · H3 3 (#327) · H4 4 · H5 4 · H6 4 · H7 4 · H8 4 · H9 3 · H10 3). P2: copy-link no visible confirmation (probe read `status:[""]` — check `user-detail.tsx` copy handler for a transient status before calling it real, lesson #271); P2 #327.
- evaluate **health 88**, P0 0, P1 0, anti-patterns Clean (D1 4, D2 4, D3 3, D4 4, D5 4, D6 3, D7 4).
- include **PASS 1440 / PASS 390**; P2 #327; P3 deep-link-miss focus on body (shell residue).
- Detector r2: 204 findings, all residue (see table); 0 real at 1440 and 390. **Bar met.**

## Checks so far (servers stopped)
- Full `vitest`: 281/283 files, 2769/2774 tests. 5 failures in `lib/analytics.test.ts` (4) and `lib/marketing-layout.test.ts` (1) — **not #286 files**; they belong to #316's uncommitted PostHog WIP (`8973d8d`). Verify with `git stash`-free check: `git log --oneline -1 -- lib/analytics.ts`.
- `eslint` clean on every file #286 changed. `tsc --noEmit` and `next build` **not yet run this session** (G3 ran tsc clean; this session added `endSentence`, `errorRow`, the `:<userId>` suffix).

## Next steps (the remainder of close, in order)
1. `npx tsc --noEmit` (~7 min) then `npx next build`; fix anything in #286 files only.
2. Copy-link P2: read `user-detail.tsx` `copy` handler; if a status/toast exists it is a probe-timing artefact (record), else add a "Copied" status via the existing `sonner` toast.
3. Report at `docs/wayfinder-reports/226/286.md` (append a session-14 section; `scores:` line: predicted-critique=32 first-critique=27 close-critique=36 first-evaluate=70 close-evaluate=88); Part G lessons table (first-pass P0s → lesson "uncaptured ≠ finding" + "seed contamination", both written to `scripts/wayfinder-autopilot/lessons.md`; the project lessons file `.claude/wayfinder-autopilot/lessons.md` is **permission-locked** unattended — owner to copy the two lines).
4. Resolution comment on #286 (scores, before/after detector counts 204→204 all residue, #327 named with its cost H3=3), close it, one-line entry in map #226 Decisions so far, graduate fog if any.
5. Commit `feat(admin): Users queue close (#286)`; leave servers down.

milestone: close-scored
milestone: closed
