# Hand-off — #286 Build Users (Admin, org-level Queue-screen shell)

milestone: spec-done

Phase **build** in progress — one plan step per session (see `step:` lines). Read this file, then `spec.md` + `preflight.md` in
`docs/wayfinder-reports/226/logs/scratch-286/` (132 + 152 lines — read whole, they are the build
tables), then the steps below. **Do not** re-read primers/lessons whole (`grep` them); the
`intent`/`impeccable` skill loads cost ~60K — call them, but read nothing else large after.
`impeccable context` launcher is denied unattended: say "Context loading did not run" and move on.

## Done (sessions 3–6)
- Schema + migration `add_workspace_member_bank_details` deployed (devdb `127.0.0.1:55433`).
- `models/workspaces.ts`: `addExistingUserToWorkspace`, `setWorkspaceMemberBankDetails`,
  `getPendingInvitationWithGrants`. `models/organizations.ts`: `ownedWorkspaceIds`,
  `listOrganizationInvitations`. Model tests 42/42 (session 5).
- Shell props landed + committed: `QueueScreen.primaryAction`, `overrideMode`, `search`
  (`#queue-search`, Esc clears, 200 ms → `router.replace`); `lib/queue/filters.ts` `extraParams`.
  `npx vitest run lib/queue components/queue` → 38/38 green (session 6).
- **Session 6 (this commit):**
  - `lib/admin/users.ts` — client-safe `UserRow`/`UsersPageData` types, `ROLE_LABELS`,
    `ROLE_CONSEQUENCE`, `displayName`, `statusLabel`, `normalizeSearch` (NFD strip),
    `filterUserRows` (params `role`/`status`/`company`/`q`), `daysUntil`, `actionErrorText(code, ctx)`
    (every §2 code → §4 sentence).
  - `models/admin-users.ts` — `loadUsersPage(current, viewer)` → (a)/(b)/(c) rows with
    `viewerOwns`, `ownersByCompany`, `ownedCompanies`/`unownedCompanies`, `hiddenCompanyCount`,
    `bank` only for owner-of-current or self; `loadUserRow(current, viewer, key)`.
  - `admin/users/actions.ts` ("use server") — `loadUserDetailAction`, `inviteUserAction`
    (owner + no-existing-member per grant), `resendInvitationAction` (re-issues with grants),
    `revokeInvitationAction`, `addUserToCompanyAction`, `setMemberRoleAction` (returns
    `previousRole`), `removeMemberAction` (self → `leaveWorkspace`; returns `{role, bank}` snapshot
    for Undo), `saveMemberBankDetailsAction`. All take `(workspaceId, input)`; `grep -c
    owner_required` = 9. **Not yet type-checked** (`tsc` not run; no test yet).

## Shell facts (save re-reading)
- Selected row = `${basePath}/${id}` via replaceState: add `admin/users/[userKey]/page.tsx`
  rendering the same page with `initialSelectedId` (pattern:
  `approvals/expense-claims/[claimId]/page.tsx` re-exports the queue page fn with `selectedClaimId`).
- Pane pattern: `components/queue/expense-claim-queue.tsx` 127–131 — `loadDetail` is a client
  `useCallback` awaiting a server action and returning `<UserDetail …/>` (client component); no
  `CLIENT_MANIFEST_ANCHORS` needed. Tabs markup to copy: same file lines 209–235
  (`role=tablist`, arrow keys, `tabButton`/`panelProps`). `paneActions(row, helpers)` = sticky bar
  slot; `paneMenu` = ⋯ items via `PaneMenuItem` (`detail-pane.tsx:285`).
- `PaneHelpers = { close, refresh, next }`; `refresh` bumps `reloadKey` (content remounts). Add
  `"user-pane-save"` to the pending-focus id list at `detail-pane.tsx:252` (lesson #258).
- `AdminSaveBar` (`components/admin/admin-save-bar.tsx`, 85 lines) has **no** `inPane`/`status`
  props yet — add: `inPane` swaps the `sticky bottom-[72px] -mx-5 md:-mx-8` classes for the pane's
  slot, `status?: string` renders beside the Saved/Unsaved span ("Saved 2 of 3 — X still unsaved").
- Primitives: `Pill` is in `components/automation/automation-ui.tsx` (`state: auto|waiting|blocked|idle`);
  `ReadOnlyBand`/`Consequence`/`PhoneNote` in `components/admin/admin-ui.tsx`; `Dialog`
  (`initialFocus` selector, `placement`), `ConfirmDialog` (`destructive`, `busy`, children);
  `NativeSelect`; `Input`/`Label`; toasts via `sonner` (`toast.success(msg, { action: {label,onClick} })`).
- `getAdminContext(wsId)` → `{ user, membership, workspace{id,name,kind,organizationId}, owner, owners, members }`.
  Incumbent `admin/users/page.tsx` (AdminPage + MembersTable + InvitePanel) — replace entirely.
- Client components never import `models/*` (Prisma in the bundle) — only `lib/admin/users.ts`.

## Build plan (one step per session)
step: 1 — Users queue (`components/admin/users-queue.tsx`, spec §3–§4.1) — done
step: 2 — Users pane (`components/admin/user-detail.tsx`, spec §4.2–§5.4) — done
step: 3 — Page rewrite + `[userKey]` route (spec §2, §5.4 deep link) — todo
step: 4 — Build gate: Part B checks, round script `shots-r0` both widths, detector, keyboard probes, `tsc`, `milestone: build-done` — todo

### Step 2 record (session 8)
- `components/admin/user-detail.tsx` (≈600 lines): `UserDetail` → `PaneHeader` (email + `Pill`s You /
  Invited / Invitation expired / ‹Role› here) + `InvitedBody` (§5.3 dl, Resend/Revoke/Copy link bar
  with clipboard fallback input, "Ask an owner of ‹primary›" when not `viewerOwnsPrimary`) or
  `MemberBody` (tablist Information · "Companies, n"; Information = dl + Bank details fieldset §4.5
  with draft inputs/validation; Companies = sections current-first with `NativeSelect` role,
  `Consequence`, "Now:", only-owner disabled options + reason, ✕ Remove/Leave, "Ask an owner",
  Add-to-company button or "in every company you own"; `AdminSaveBar inPane saveButtonId="user-pane-save"`
  when the viewer can edit anything). Save = per dirty section `setMemberRoleAction` (Give-up-ownership
  confirm first; last-reviewer confirm mid-way; stop at first failure with `role=alert` at the section;
  "Saved n of m — X still unsaved" status) then bank. Remove/Leave via `useAsyncConfirm`
  (`components/admin/user-confirm.tsx`, already committed): `escalate` two-phase, `refuse` on
  last-owner race, Undo toast (8 s) re-adds + restores bank snapshot. Leave current → hard nav
  `/workspaces?notice=left&name=`. `usePhoneLane()` → read-only below lg (no editors/bar/✕).
- Props: `{workspaceId,row,currentWorkspaceId,currentCompanyName,currentCompanyKind,ownersByCompany,
  ownedCompanies,helpers:{close,refresh},register}`. `users-queue.tsx` carries the shell `PaneHelpers`
  to the content via `helpersRef` (set inside `paneMenu`) and the pane's Resend/Revoke/Leave back to
  the ⋯ menu via `register` → `paneApi` (`PaneMenuItem`s: Copy email · Resend · Revoke invitation ·
  Leave ‹current›).
- Checks: `tsc --noEmit` clean; `eslint` clean on both files; `vitest run lib/queue components/queue`
  38/38; Part B lint `logs/scratch-286/partb-step2.mjs` — every action has a caller, no
  `window.confirm`, no member-as-noun (fixed "not a member here" → "not in this company"), no
  "please", focus ids present. Static `impeccable detect` launcher denied unattended — in-page
  detector at step 4.
- Not yet verified live (step 4): AdminSaveBar `inPane` sticks inside the pane scroll area (bar is
  rendered inside the content, not the `paneActions` slot); `Pill` tones for the header; card ✕ text
  hidden below md.

## Next steps, in order
3. Rewrite `app/(app)/workspaces/[workspaceId]/admin/users/page.tsx`: `getAdminContext(wsId)` →
   `loadUsersPage(workspace, user)` (`models/admin-users.ts`) → `<UsersQueue workspaceId data
   initialSelectedId initialMissing>` (see `UsersQueueProps` in users-queue.tsx; `initialMissing` is
   the §5.4 "not in a company you're a member of" notice); add `admin/users/[userKey]/page.tsx`
   re-exporting the page fn with `initialSelectedId` (pattern `approvals/expense-claims/[claimId]/page.tsx`);
   `[userKey]` may be `inv:<id>`. `AdminSaveBar` props and the `user-pane-save` focus id are already in.
   Check: `tsc --noEmit`, route renders under `DEV_AUTH_BYPASS` (memory: docubite_live_ui_verification).
4. Build gate as in the phase brief (round script `scripts/wayfinder-autopilot/capture-round.mjs`,
   `shots-r0` in scratch-286, both widths, detector JSON, keyboard probes for B5: activeElement after
   each dialog close and after Save; fix findings; r0 counts here; stop servers; `milestone: build-done`).

## Open / needs owner
None blocking. Org-role editing and org rename deliberately out (#287).
