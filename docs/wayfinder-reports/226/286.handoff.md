# Hand-off — #286 Build Users (Admin, org-level Queue-screen shell)

milestone: spec-done

Phase **build** in progress (session 4 cut at the context line; models done, no UI yet). Read
`spec.md` + `preflight.md` in `docs/wayfinder-reports/226/logs/scratch-286/` (already absorbed),
`docs/agents/areas/admin.md` + `queue-shell.md` + `detail-pane.md`, then the steps below.

## Done (sessions 3–4)
- Schema + migration `20260917000000_add_workspace_member_bank_details` **deployed and client
  generated** (devdb `127.0.0.1:55433`; a shell `DATABASE_URL` shadows `.env`, so run
  `node docs/wayfinder-reports/226/logs/scratch-286/migrate.mjs` for prisma commands).
- `models/workspaces.ts`: added `addExistingUserToWorkspace` (create, refuses existing; reviewer/
  mode audit; audit `workspace_member_added`; Bigcapital provision), `setWorkspaceMemberBankDetails`
  (validates 6–20 digits / branch 3–10 / bank name; audit without values; `details: null` clears),
  `MemberBankDetails` type, `getPendingInvitationWithGrants(invitationId)` (unscoped, with
  `additionalGrants`, `workspace`, `sentBy`). Import of `unscoped` added.
- `models/organizations.ts`: added `ownedWorkspaceIds(orgId, userId)`, `OrganizationInvitationRow`,
  `listOrganizationInvitations(orgId, userId)` (owned companies, pending, grants resolved to names,
  primary grant first; **not** deduped by email — each invitation is its own record/Revoke).
- Not yet run: `npx vitest run models/workspaces.test.ts models/organizations.test.ts` (bare-object
  prisma mocks — lesson #257; the new fns add no new tables but check anyway).

## Shell facts learned (save re-reading)
- `QueueScreen` (`components/queue/queue-screen.tsx`, 638 lines): selected row is reflected as
  `${basePath}/${id}` via replaceState (not `?user=`) — **deviation from spec §3.2**: follow the
  shell; add `admin/users/[userKey]/page.tsx` that renders the same page with `initialSelectedId`
  (copy how `(queue)/invoices/[documentId]` does it). Row keys are `u:<userId>` / `inv:<invitationId>`.
- Props to add (all absent): `primaryAction?: ReactNode` (render in the `ml-auto` group at line
  ~475 before `stat`), `overrideMode?: boolean` (default true; false → skip the `role=switch` menu
  item at ~484 and the banner at ~518; `QueueScreen` wraps `OverrideModeProvider` at line 73 —
  keep the provider, just hide UI), `search?: { param: string; label: string }` (text input
  after `#queue-facets` at ~474 and inside `FilterSheet` — `components/queue/filter-sheet.tsx`
  keeps a `draft` URLSearchParams, `clear` at line 51; `lib/queue/filters.ts` `clearFilterParams`
  and `activeFilterCount` take `facets` — add an optional `extraParams: string[]` so `q` is cleared
  and counted; `filtered` at line 286 must also consider `q`). `filterRows(rows, params)` is where
  the page does the name/email match (trim + toLocaleLowerCase + NFD strip).
- `PaneHelpers = { close, refresh, next }`; `refresh` bumps `reloadKey` (pane content remounts
  through the skeleton — lesson #258: pending-focus ref for `#user-pane-save`) + `router.refresh()`.
- Actions pattern: `app/(app)/workspaces/[workspaceId]/workspace-actions.ts` (`getCurrentUser`,
  `requireMember(workspaceId, user.id, ["owner"])` from `./action-helpers`, `ActionState` from
  `lib/actions`, `errorMessage`, `deliverInvitation` + `inviteUrlFor` for the copyable link).
- `getAdminContext(workspaceId)` (`lib/admin/context.ts`) → `{ user, membership, workspace, owner,
  owners, members }`. Incumbent page: `admin/users/page.tsx` (AdminPage + MembersTable + InvitePanel).

## Next steps, in order
1. Run the two model test files; fix mocks if needed.
2. New `admin/users/actions.ts` ("use server"): load/invite/addToCompany/setRole/remove(+leave)/
   saveBankDetails/resend/revoke per spec §2, owner check inside each (`grep -c owner_required` ≥ 6),
   codes mapped in the client.
3. `queue-screen.tsx` + `filter-sheet.tsx` + `lib/queue/filters.ts`: the three props above.
4. `components/admin/users-queue.tsx` (client, spec §3) and `components/admin/user-detail.tsx`
   (pane, spec §5 — biggest file: tabs, AdminSaveBar `inPane`/`status` props, Add-to-company
   dialog, Remove/Leave/Revoke/Resend/Remove-bank confirms, bank group, invited rows).
5. Rewrite `admin/users/page.tsx` (rows a/b/c, `bank` only for owner-of-current or self) +
   `admin/users/[userKey]/page.tsx`; `admin/layout.tsx` `CLIENT_MANIFEST_ANCHORS = [UserDetail]`.
6. Part B checks (preflight B1–B5), round script on `capture-round.mjs` (both widths, detector,
   keyboard probes) as `shots-r0`, fix findings, r0 counts here, `tsc --noEmit`, stop servers,
   `milestone: build-done`, commit `wip(autopilot): #286 build`.

## Open / needs owner
None blocking. Org-role editing and org rename deliberately out (#287).
