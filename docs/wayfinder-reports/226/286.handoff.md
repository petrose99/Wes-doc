# Hand-off — #286 Build Users (Admin, org-level Queue-screen shell)

milestone: spec-done
milestone: build-done

Build is **done** (all `step:` lines below). Next phase: **measure** — see "Next steps". Read this file,
then `spec.md` + `preflight.md` in `docs/wayfinder-reports/226/logs/scratch-286/` (132 + 152 lines,
the build tables) only by the sections the readers need. Do not re-read primers/lessons whole.
`impeccable context` launcher is denied unattended: say "Context loading did not run" and move on.

## What is built (sessions 3–9, all committed)
- Data: migration `add_workspace_member_bank_details`; `models/workspaces.ts` (`addExistingUserToWorkspace`,
  `setWorkspaceMemberBankDetails`, `getPendingInvitationWithGrants`), `models/organizations.ts`
  (`ownedWorkspaceIds`, `listOrganizationInvitations`), `models/admin-users.ts` (`loadUsersPage`,
  `loadUserRow`; `bank` only for owner-of-current or self). Model tests 42/42.
- Shell props (additive): `QueueScreen.primaryAction`, `overrideMode`, `search` (`#queue-search`, Esc
  clears, 200 ms → `router.replace`); `lib/queue/filters.ts` `extraParams`; `AdminSaveBar` `inPane` +
  `status`; `"user-pane-save"` in `detail-pane.tsx` pending-focus list. `vitest lib/queue components/queue` 38/38.
- `lib/admin/users.ts` (client-safe types, `ROLE_LABELS`, `filterUserRows`, `actionErrorText`);
  `admin/users/actions.ts` (8 server actions, every mutation owner-gated, `owner_required` ×9).
- Surface: `components/admin/users-queue.tsx`, `user-detail.tsx`, `user-confirm.tsx`; pages below.
- Primitives used (B4): `QueueScreen`, `AdminSaveBar`, `Dialog`/`ConfirmDialog`, `NativeSelect`, `Pill`
  (`components/automation/automation-ui.tsx`), `ReadOnlyBand`/`Consequence`/`PhoneNote`
  (`components/admin/admin-ui.tsx`), `PaneMenuItem`, `sonner` toasts. Client code never imports `models/*`.

## Build plan (one step per session)
step: 1 — Users queue (`components/admin/users-queue.tsx`, spec §3–§4.1) — done
step: 2 — Users pane (`components/admin/user-detail.tsx`, spec §4.2–§5.4) — done
step: 3 — Page rewrite + `[userKey]` route (spec §2, §5.4 deep link) — done
step: G1 — round script (`docs/wayfinder-reports/226/logs/scratch-286/round-286.mjs` + `run-round.mjs`, both widths, detector, B5 keyboard probes) — done
step: G2 — r0 and fixes — done
step: G3 — `tsc --noEmit`, Part B contract checks over the whole surface, `milestone: build-done` — done

### Step G3 record (session 11)
- `npx tsc --noEmit` whole project, servers stopped: exit 0, no diagnostics (~7 min). Ran before
  the two copy edits below (string literals only; `eslint` clean on both files after).
- Part B whole-surface lint `logs/scratch-286/partb-g3.mjs` (B1 reachability + no native
  confirm/alert + 9 owner gates; B3 string extraction; B4 primitive diff — no raw
  select/dialog/table, one `AdminSaveBar`, no clickable `Pill`; B5 focus ids): **ALL PASS** after
  two B3 fixes — "you're not a member of it/them" → "you don't belong to it/them"
  (`users-queue.tsx:126` band line) and the §5.4 notice "a company you're a member of" →
  "a company you belong to" (`users/page.tsx:27`). Both sentences came verbatim from spec §3.1/§5.4;
  B3's "no member as a noun for a person" check won (Member is a role name). Note for close: spec
  lines 29 and 88 should be updated to the shipped wording.
- `round-286.mjs`: `a-pane-invited` now uses `paneAction(page, w, name)` — inline button at ≥1024,
  ⋯ menu item below (phone lane is read-only, Resend/Revoke live only in the menu). Syntax-checked,
  not re-run (no servers this session; measure reuses it).
- Servers were never started; nothing to stop.

### Step G2 record (session 10) — r0 triage the measure phase inherits
- Fresh r0 (`node run-round.mjs r0`, 46 states × 1440/390): 204 detector findings, all triaged
  residue / false positive / out-of-scope; **no code change**. (Earlier on-disk shots were stale,
  pre-dating the `phoneRender` companies-column fix in 316dd0d.)
- Residue classes: `text-overflow` (13×, `.truncate` ellipsis false positive, visually clean at 390);
  `body-text-viewport-edge` (3×, the mode band `<p>` — carries `px-4`, inset in shots);
  `em-dash-overuse`, `flat-type-hierarchy` (dialog-title sizing), `line-length` (~93 ch band
  sentence at 1440) — same shell/copy residue as #285's close. `nested-cards` 0×.
- Not bugs: `invite-present-390: 0` (Invite is `hidden md:inline-flex`, d18 desktop-only Admin);
  `b-invite-locked` shows `lockedReason:1, disabled:true` live.
- Probes: `invite-self-error-*` logs `focused:"body"` in the round but a direct live repro focuses
  `#invite-email` — timing flake. `leave-esc-1440` correct (focus → pane `h2`).
- **Real, shell-wide, not #286's**: `pane-esc-unsaved-guard-1440: dialog:0` — `queue-screen.tsx`'s
  Escape handler (~line 415) and the pane ✕ never consult `hasUnsaved()` before `close()`;
  `AdminLeaveGuard` only guards `<a>` clicks and Back. Affects every editable pane on any queue.
  Needs its own ticket at close (see "Open / needs owner").

### Steps 2–3 gist (sessions 8–9; full records in git history of this file at 72137f1)
- `components/admin/user-detail.tsx` (~600 lines): `PaneHeader` (Pills You/Invited/Expired/‹Role› here) +
  `InvitedBody` (§5.3 dl, Resend/Revoke/Copy link with clipboard fallback) or `MemberBody` (tabs
  Information · Companies; bank fieldset §4.5; per-company `NativeSelect` role + `Consequence`; ✕
  Remove/Leave via `useAsyncConfirm` in `user-confirm.tsx`, Undo toast 8 s; `AdminSaveBar inPane
  saveButtonId="user-pane-save"`). `usePhoneLane()` → read-only below lg. Pane ⋯ menu items via
  `register` → `paneApi` (Copy email · Resend · Revoke invitation · Leave ‹current›).
- `admin/users/page.tsx` → `UsersScreen({params, selectedId})`; `[userKey]/page.tsx` re-exports
  (keys `u:<userId>` / `inv:<invitationId>`); §5.4 nameless notice on a miss; `admin/layout.tsx`
  `CLIENT_MANIFEST_ANCHORS = [CompanyDetail, UserDetail]`. Personal mode = one-row `QueueScreen`.
- Seeds (check-seed.mts): Riverside `9cdcdf3f…` (org, owner), Northwind
  `c5315ed3-053f-4dba-9821-ab1d085d405a` (org, 2 rows), Harbor Lights `a74a45c2…`, Pine `60a2b427…`
  (team), personal `af91555d…`; dev user `59887621-86bf-4886-8d6c-b1307bd538a1`.
- Machine: 2 cores / 7.8 GB — one heavy process at a time (dev server *or* tsc); first route
  compile ~70 s (`curl` before Playwright). Deep-link miss leaves focus on body (shell-wide, triage
  in measure).

## Next steps, in order
MEASURE. `node servers.mjs up` (scratch-286), `node run-round.mjs r1` (46 states × 1440/390,
    detector JSON + keyboard probes), then the three readers (`critique`, `evaluate`, `include`)
    on the captures; write scores per heuristic + triage to this file, `milestone: measured`.
    No fixes in that phase. Known residue to expect: `text-overflow` on `.truncate`,
    `body-text-viewport-edge` on the band `<p>`, `em-dash-overuse`, `flat-type-hierarchy`,
    `line-length` (see G2 record). Shell-wide Esc/✕ unsaved-guard gap → its own ticket, not a fix.

## Measure round r1 (session 12)
Servers up, r1 captured (46 states × 1440/390, detector JSON, keyboard probes); contact sheet generated;
three readers (`critique`, `evaluate`, `include`) launched in parallel. Raw findings below, untriaged.

### Scores per reader

**critique (Nielsen H1–H10):**
- H1 (Visibility of system status): 1 — P0: `a-list-owner-1440` renders blank; no loading skeleton, no error boundary, script injection fails, form fill times out. Elsewhere status well-handled (pending-state text, save bar states, field alerts).
- H2 (Match between system and real world): 3 — roles/vocabulary consistent (Owner/Reviewer/Member kept distinct from org roles per ADR 0002), "Users" vs "people" dual vocabulary is spec-intentional.
- H3 (User control & freedom): 2 — P1: `a-list-owner-1440` keyboard Tab never advances past `body` (20/20 presses, list non-operable by keyboard). P1: `a-pane-invited-390` click interaction times out (30s). Esc/focus-return correct elsewhere.
- H4 (Consistency and standards): 3 — primitives consistent across states, P2: `dark-glow` box-shadow recurs on every state (30/30, likely shell-wide not per-ticket).
- H5 (Error prevention): 4 — guards are pre-disabled with visible reasons before Save; field validation present; confirms on destructive actions.
- H6 (Recognition rather than recall): 3 — pending state surfaces "Now: ‹previous›", tab counts visible, "This company" label present; card rows lack persistent "Role here" label.
- H7 (Flexibility & efficiency): 3 — Sort, facets, search present; metric (≤6 interactions) met by Invite flow; Ctrl+S unverified.
- H8 (Aesthetic & minimalist design): 2 — P1: `.truncate` overflow (86px) on 9 mobile states (`a-list-owner-390`, `a-pane-owned-info-390`, `a-pane-owned-companies-390`, `a-pane-self-companies-390`, `a-pane-self-pane-menu-390`, `a-list-member-390`, `a-list-member-pane-390`, `a-deeplink-present-390`, `a-deeplink-missing-390`); P2: pane `h2` overflow for long emails (+33px, +20px); P3: line-length ~93 chars (spec'd sentences run long), em-dash-overuse (9 on `a-list-member-1440`).
- H9 (Help users recognize, diagnose, recover from errors): 3 — field-adjacent errors with inputs kept; `a-list-owner-1440` has no error state at all (blank page).
- H10 (Help and documentation): 3 — "Ask an owner: ‹names›", PhoneNote, Consequence sentences provide at-control help; spec-appropriate.
**Health: 80** (one heuristic ≤1).

**evaluate (D1–D7 correctness/quality):**
- D1 (Specification Adherence): 3 — P0: Personal workspace (c) renders 3 rows directly contradicting "just you" copy and spec §3.3(c); P1: Leave control absent for non-owner companies on own row (Northwind Traders section shows no ✕, `keyboard.json` `self-leave-northwind-present-1440.count = 0`); P1: "Ask an owner: an owner can change bank details…" copy placeholder not interpolated with real owner name on phone (`a-pane-owned-info-390.png`); edge-case states (last-reviewer/last-owner confirms, Undo flows) not captured so not verified.
- D2 (Data Integrity & Accuracy): 4 — row data, bank-details masking, invitation data correct; Undo bank-restore and RSC `lastFour` security check not captured (assumed correct if models/actions are spec-correct).
- D3 (User Feedback & Error Handling): 2 — P0: unsaved-changes guard on Esc not signalling (see critique P0 / include P1); P1: per-company error detail missing in Invite dialog (spec requires row-level feedback, not dialog-level); P2: bank-field-error grouping unclear (alert inline without visual container); consequence line for self-demotion generic.
- D4 (Accessibility — WCAG 2.2): 3 — P0: Tab order broken at 1440 (list-tab-walk never advances past `body`); P2: Ctrl+S shortcut untested (coverage gap); possible empty accessible name on `b-invite-locked-1440` input; unsaved-changes guard Esc not signalled violates H3.3.4.
- D5 (Visual Consistency & Fit): 4 — component use consistent, pane layout uniform, phone adaptations clear (read-only sections, no Invite on personal, masked bank details).
- D6 (Performance & Responsiveness): 4 — no visual regressions or layout shifts observed; pane loading, dialogs, actions show expected patterns; filter latency unmeasured.
- D7 (Completeness of Implementation): 3 — core flows present (Invite, role-change, Add-to-company, Remove/Leave, bank, pane tabs, deep-link); edge cases (last-X confirms, per-company errors in Invite, Undo, Metric) not captured or possibly missing.
**Health: 60–80** (pending P0 verification of Tab order and Esc guard).

**include (keyboard accessibility):**
- 390 (mobile/narrow): PASS — full Tab walk succeeds cleanly with correct skip links, nav collapse, row-as-link exposure; all probed interactions (tabs, dialogs, deep links) match spec.
- 1440 (desktop): FAIL — `list-tab-walk-1440` shows 20 Tab presses, zero focus movement off `body`; P0: nothing in header toolbar or row list is Tab-reachable. Since 390's equivalent walk works, reads as genuine desktop-width regression.
- P0: 1440 Tab walk does not move focus past `body`; re-verify live (could be probe-timing or hydration artifact).
- P1: possible missing unsaved-changes Esc guard (`pane-esc-unsaved-guard-1440: dialog: 0` shows no confirm appeared); bank details could be silently discarded. Re-verify live with unsaved role/bank changes.
- P2: Ctrl+S save shortcut (spec §4.3) has no keyboard probe; add test before closing.
- P2: no virtual-keyboard probes at 390 for numeric-input or search-field interference.
- P3: `b-invite-locked-1440` shows empty focused label; confirm input has accessible name in live DOM.

### Findings (P0–P3, untriaged)

**P0 (critical, violates core spec/Nielsen, blocks use):**
1. Critique H1 / Evaluate D1: `a-list-owner-1440` renders blank white page (5.8 KB), no loading skeleton, no error boundary, script injection fails ("Failed to load script at http://localhost:8400/detect.js"), form-fill timeout (30s). This is the primary "owner viewing their org's Users list" state and is unrecoverable by user (no error message, no status, no control).
2. Critique H3 / Include: `list-tab-walk-1440` shows 20 consecutive Tab presses with focus never leaving `body` — desktop header controls and row list not Tab-reachable. Keyboard navigation blocked at 1440.
3. Evaluate D1: Personal workspace renders 3 rows ("Priya Natarajan", "Tomás Ferreira", "Dev User · You · Owner") directly under "Your personal workspace is just you." — contradicts spec §3.3(c) and spec §1's core product truth that personal = viewer only. Data-scoping bug.
4. Critique H9 / Evaluate D3 / Include P1: Unsaved-changes guard on Esc possibly missing — `pane-esc-unsaved-guard-1440: dialog: 0` shows no confirm dialog appeared when pane with pending role/bank changes was closed with Esc. Re-verify live.

**P1 (major, violates spec, impedes workflow):**
1. Evaluate D1: Leave control absent for non-owner companies on own row — Northwind Traders section in `a-pane-self-companies-1440.png` shows only "Ask an owner: Priya Naidoo can change this." (read-only text), no ✕ / Leave button; `keyboard.json` `self-leave-northwind-present-1440.count = 0` confirms. Spec §4.4: "Leave" should be present on every own-row company, not gated by ownership.
2. Evaluate D1: "Ask an owner: an owner can change bank details, or Thabo Mokoena can under their Account." — literal placeholder on phone bank band, not interpolated with real owner name(s). Appears on `a-pane-owned-info-390.png` and likely other phone info panes. Copy matrix violation (§7 pattern: "Ask an owner: ‹names›").
3. Critique H5 / Evaluate D1 / D3: Per-company error rendering missing in Invite dialog — spec §4.1 requires `member_already_exists` errors to appear at company row level ("‹email› already has access to ‹company›. Open their row…"), not dialog-level. Captured Invite states do not show multi-company invite with per-row error feedback; unclear if feature is implemented.
4. Critique H3: `a-pane-invited-390` click interaction times out (30s); a second broken control path on mobile (separate from 1440 Tab-walk issue or related).

**P2 (minor, deviates from spec polish or coverage):**
1. Critique H8: `.truncate` name/email spans overflow their box by 86px across 9 mobile states — systemic CSS issue, not nine separate bugs. Also pane `h2` overflow for long invited/expired emails (+33px, +20px, `a-pane-invited-pane-390`, `a-pane-expired-pane-390`).
2. Evaluate D3: Bank-details error state (account-number validation) has alert text inline without clear visual container, reducing error scannability.
3. Evaluate D3: Consequence line for viewer's own role demotion is generic ("Reviewer 'Signs off on close…'") when field is changed; confirm dialog is specific ("Give up ownership?"). Field-level guidance split across two moments; H5 feedback weaker than ideal.
4. Critique H1 / H4: `dark-glow` box-shadow signature recurs on every single state (30/30), including unmodified shell chrome — likely shell-wide default, not per-ticket defect. Needs live in-page detector confirm; `craft-floor.md` bans glow effects outright.
5. Evaluate D4 / Include P2: Ctrl+S keyboard shortcut (spec §4.3) untested — no keyboard probe; may fail silently or trigger browser save-as.
6. Evaluate D2: `a-list-owner-1440` never captured successfully (detector-error, then timeout) — most-used state unverified by this round.
7. Evaluate D4: `list-tab-walk-1440` stays on `body` for all 20 Tab presses; likely a capture defect, but should be re-run to prove §8 focus order at desktop width. (Same root as Critique H3 P1 / Include P0.)

**P3 (cosmetic, low impact):**
1. Critique H8: Line-length ~93 chars on `b-ungrouped-1440`, `b-ungrouped-invite-dialog-1440`, `c-personal-1440` (spec'd copy runs long, aim <80 chars per Nielsen).
2. Critique H8: `em-dash-overuse` (9 instances) on `a-list-member-1440` — copy style uses em-dash liberally per spec Consequence sentences.
3. Evaluate D3: Guard-reason sentence has doubled period ("Riverside Bakery Co.." — company name ends in "Co." + period).
4. Evaluate D4: Deep-link-missing notice text ("isn't in a company you belong to") differs slightly from spec's exact wording ("isn't in a company you're a member of"); same meaning, minor drift.
5. Evaluate D6 / Critique H6: Invite/Resend confirm copy for low-risk, reversible actions reads as heavy relative to actual risk.
6. Critique H6: Mobile card rows show role but lack persistent "Role here" column-label equivalent on cards (role present but without desktop header context).
7. Include P3: `b-invite-locked-1440` focused input shows empty label in probe; confirm accessible name in live DOM.
8. Evaluate D6: No Undo/Undo-bank-restore states captured, so those flows unverified (spec'd per §4.4).

**Out of scope for #286 (shell-wide, every state including unrelated pages):**
- `ai-color-palette: Cyan gradient background` ×2 on all states.
- `overused-font: inter (98–99%)` — design system single-typeface by design.
- `layout-transition: transition: height` — all states, can't confirm jank from static.
- `flat-type-hierarchy` — pre-existing shell type scale.
- `body-text-viewport-edge` (Critique H8 P3) — exceeds scope, left for shell/foundation pass.

**Patterns:**
1. Single root-cause P0, multiple symptoms: `a-list-owner-1440`'s blank render, detector script failure, form-fill timeout, and Tab-walk stuck-on-`body` are one defect, not four. Most critical finding — metric path from spec §1.
2. `.truncate` overflow is systemic at 390, hitting 9 of ~14 mobile states, plus related pane-heading overflow — fix once at shared CSS, not per-screen.
3. (c) personal-workspace and (Northwind) missing-Leave are same problem class — own-row logic diverged from ownership logic in both cases. Recommend single focused review of row-scoping/own-row gating.
4. Five "universal" detector findings (cyan, Inter, glow, etc.) on all 44 states indicate shell-level, not ticket-level; should file once not per-state.
5. Two separate probe timeouts (`a-list-owner-1440` fill, `a-pane-invited-390` click) suggest possible build/hydration instability; worth rerun of just those states before concluding deterministic bugs vs. environment flakiness.
6. Desktop visual/copy/confirm elements render faithfully where spec'd; defects found are data-scoping (personal workspace) and control-wiring (Leave) at the pane layer, not visual craft.
7. Coverage gaps: primary state `a-list-owner-1440` never captured; last-X confirms and per-company errors not exercised; Undo flows and Ctrl+S not tested.

**Detector/keyboard repeatability check:**
- `text-overflow` on truncated names (86px excess) and `body-text-viewport-edge` on pane headers flagged by detector but visuals show correct ellipsis/truncation — recommend live in-page detector confirm for these before accepting as real (static measurement likely incorrect).
- `list-tab-walk-1440` stuck on `body` for all 20 presses vs. `list-tab-walk-390` clean success suggests real 1440 regression or probe-timing artifact; prioritize rerun of this probe at both widths live on dev server.

milestone: measured

## Close session (session 13) — triage + partial fix batch, handed off before confirm round

**Machine is memory-starved right now** (`free -h`: ~345Mi free, 1.9Gi swap in use) — the dev
server hung compiling `/` for 10+ minutes and never answered a plain `curl` at either 30s or 100s
timeout. Killed both via `servers.mjs down` before ending this session. **Next session: check
`free -h` before `servers.mjs up`; if swap is still this full, something else on the box needs to
free memory first — this is environment, not the app.**

### P0/P1 triage (of the raw findings recorded above)

1. **Personal workspace showing 3 rows (P0, Evaluate D1)** — **tooling, fixed.** Verified via
   `docs/wayfinder-reports/226/logs/scratch-286/check-personal.mts`: the personal workspace
   `af91555d…` had two extra `WorkspaceMember` rows (Priya Natarajan, Tomás Ferreira) planted by an
   unrelated dev script, `scripts/dev/db253.ts` (a different ticket's local script, not #286 code —
   confirmed by `grep -rl` for those names; only hit was that script). No app code path can add a
   member to a personal workspace (only `models/workspaces.ts:264` even checks
   `kind === "personal"`, for Leave). Cleaned with `fix-personal-contamination.mts` (removed the 2
   non-owner rows); `seed-users.mts` never touches that workspace, so a reseed won't reintroduce it.
   **Data-only, no code change.**
2. **Leave control absent for non-owner companies on own row (P1, Evaluate D1)** — **real #286 bug,
   fixed.** `components/admin/user-detail.tsx` (Companies tab, ~line 484–501): the ✕ button was
   gated by `editable = section.viewerOwns && !phone`, which also gates the role select — but per
   spec §4.4 ("✕ on the own row is Leave ‹company›") Leave needs no ownership of the company being
   left, only Remove (someone else's row) does. Split into `showRemove` (own row: only the
   personal-workspace exclusion applies; other rows: still `section.viewerOwns`) from the
   role-select `editable` gate, unchanged. **Not yet re-verified live** (server hung before the
   confirm round) — re-run `round-286.mjs` state `a-pane-self` and check `keyboard.json` →
   `self-leave-northwind-present-1440.count` is now 1, not 0.
3. **"Ask an owner: an owner can change bank details…" placeholder (P1, Evaluate D1)** — **real
   #286 bug, fixed.** Not a missing interpolation — `ownerNames()` correctly excludes the viewer by
   design (`models/admin-users.ts:14`, "Never the viewer"), so when the viewer is the company's only
   owner the list is empty and the sentence degrades to "an owner" nonsensically. Real bug:
   `user-detail.tsx:475`'s bank-details fallback rendered even when `canEditBank` was false *only*
   because of the phone-width gate — telling the owner to "ask an owner" about their own company.
   Fixed: when `viewerOwnsCurrent || row.isViewer`, render nothing there (the pane's `PhoneNote` at
   line 436 already covers it); the "Ask an owner: ‹names›" sentence now only renders for a true
   non-owner. **Not yet re-verified live** — re-check `a-pane-owned-info-390.png` shows no line
   under Bank details for the owner-on-phone case.
4. **`a-list-owner-1440` blank / `list-tab-walk-1440` stuck on `body` (P0×2, all three readers)** —
   **unresolved, suspect environment not code.** Every live repro attempt this session
   (`verify-p0.mjs`, `waitUntil: "load"` at 30s then 90s) timed out with the dev server's own log
   frozen at `○ Compiling / ...` for 10+ minutes — the server itself never responded, while the box
   was swapping heavily (see machine note above). Matches r1's own two probe timeouts flagged as
   possible hydration/build instability. **Do not accept this as a real P0 without a clean
   re-repro**: next session, confirm `free -h` shows headroom, `servers.mjs up` (no `--seed`, DB is
   already seeded+cleaned), wait for "ready" AND a successful plain `curl` before Playwright, then
   hit the Riverside admin/users URL directly (`verify-p0.mjs` does this) and redo the 20-press Tab
   walk. If it reproduces clean, it's a real P0 (390's tab-walk passed in r1, so 1440-only would be
   genuine) and needs investigation before any fix.
5. **Unsaved-changes guard missing on Esc/pane-✕ (P0/P1, all three readers)** — **confirmed real,
   shell-wide, not #286's to fix.** `components/admin/admin-leave-guard.tsx`'s `AdminLeaveGuard`
   only intercepts `<a>` link clicks and Back (`popstate` + history sentinel) — never `Escape` or
   the pane ✕. The Escape handler lives in the shared shell, `components/queue/queue-screen.tsx`
   (~line 415), and calls `close()` unconditionally. Affects every editable pane on every admin
   queue (#253, #285, #286…), not just Users. **File as its own ticket against map #226**, scoped to
   `queue-screen.tsx` + `AdminLeaveGuard`; named at #286's close. **Not yet filed** — do it before
   closing #286.
6. **Per-company error in Invite dialog (P1, Critique H5/Evaluate D1/D3)** — **misread, mostly
   already correct, one real gap.** Spec §4.1 line 55: `member_already_exists` is dialog-level text
   naming the company ("‹email› already has access to ‹company›. Open their row to add companies or
   change roles." **(link opens that row)**), not a per-row error. `lib/admin/users.ts:112` already
   renders the exact wording. Real gap: the dialog renders it as plain text with no link to the row.
   **Not fixed this session** — small; leave as a P3 follow-up note at close rather than block it.
7. **Ctrl+S untested, Undo flows uncaptured, remaining P2/P3 items** — not investigated this
   session; re-triage at the confirm round once the environment is stable.

### State of the tree at hand-off
- Data-only fix already applied directly to the dev DB (not code, no migration): personal workspace
  now has exactly its owner.
- `components/admin/user-detail.tsx` edited: `showRemove`/`canRemove` split (item 2) and bank
  fallback suppression (item 3). **Not yet run through `vitest`, `tsc`, or `eslint` this session** —
  do that first next session, before starting the dev server again.
- New scratch scripts under `logs/scratch-286/`: `check-personal.mts`,
  `fix-personal-contamination.mts`, `verify-p0.mjs` — keep, needed to re-verify items 2–4.

## Next steps, in order
1. `free -h` / `uptime` — do not start the dev server until there is real headroom.
2. `npx vitest run` the existing admin/queue globs + `npx tsc --noEmit` — cheap, no server, do this
   before anything else.
3. `servers.mjs up` (no `--seed`), confirm a plain `curl` succeeds before Playwright touches it.
4. Re-run `round-286.mjs` (or scoped states `a-pane-self`, `a-pane-owned`, `a-list-owner`) to
   confirm items 2 and 3, and get a clean read on item 4.
5. File the shell-wide Esc/✕ unsaved-guard ticket (item 5) against map #226 — does not need the
   server.
6. Once 2–4 confirm clean and 5 is filed: full checks (`vitest`, `tsc --noEmit`, `eslint`,
   `next build`), triaged findings written up, lessons + area primer, report, close at the bar
   (critique ≥ 32/40 no heuristic < 3, evaluate health ≥ 85 zero P0/P1, detector clear, include PASS
   both widths) or another `Autopilot: continue —` if the environment keeps failing.

milestone: close-partial
