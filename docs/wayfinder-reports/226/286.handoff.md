# Hand-off — #286 Build Users (Admin, org-level Queue-screen shell)

milestone: spec-done

Phase **build** in progress (session 3 cut at context line before most code landed). Read
`spec.md` + `preflight.md` in `docs/wayfinder-reports/226/logs/scratch-286/` (already absorbed —
don't re-read unless a table needs re-checking), then go straight to the steps below.

## Done this session
- `prisma/schema.prisma`: added `bankName`/`bankAccountNumber`/`bankBranchCode` (String?) to
  `WorkspaceMember`. New migration `prisma/migrations/20260917000000_add_workspace_member_bank_details/`
  — **not yet deployed/generated** (needs `npx prisma migrate deploy` + `npx prisma generate`,
  devdb is up at `postgresql://docubite:docubite@127.0.0.1:55433/document_inbox`, matches `.env`).
- Read and confirmed: `queue-screen.tsx` (`QueueScreenProps` has no `primaryAction`/`overrideMode`/
  `search` yet — add all three, additively, this session — #285 hasn't landed them either);
  `detail-pane.tsx` PaneFrame/DetailPane; `admin-save-bar.tsx`, `admin-leave-guard.tsx`,
  `confirm-dialog.tsx`, `native-select.tsx`, `admin-ui.tsx`; `models/workspaces.ts` full (role/
  remove/leave/transfer/invite/accept — authorise nothing themselves, confirmed); `models/
  organizations.ts` (`listOrganizationUsers`, `accessibleWorkspaceIds`); `workspace-actions.ts` +
  `action-helpers.ts` (the `ActionState`/`errorMessage`/`requireMember` pattern to follow, NOT
  reuse — spec §2 wants new actions); `admin/layout.tsx` has no `CLIENT_MANIFEST_ANCHORS` yet,
  copy the pattern from `(queue)/layout.tsx:11`; `WorkspaceInvitation.additionalGrants` already
  exists (#254).

## Next steps, in order
1. `npx prisma migrate deploy && npx prisma generate` from repo root (picks up `.env`).
2. `models/workspaces.ts`: add `addExistingUserToWorkspace` (mirror `acceptWorkspaceInvitation`'s
   per-grant transaction incl. reviewer/mode accounting, audit `workspace_member_added`);
   `setWorkspaceMemberBankDetails` (plain update or clear, audit with no values); a helper to read
   a pending invitation + its grants for resend.
3. `models/organizations.ts`: add `listOrganizationInvitations(organizationId, viewerId)` —
   unscoped + workspaceId IN (ids the viewer **owns**), acceptedAt null, include additionalGrants,
   dedupe by email.
4. New `app/(app)/workspaces/[workspaceId]/admin/users/actions.ts` (`"use server"`): every action
   in spec §2 (load/invite/addToCompany/setRole/remove/saveBankDetails/resend/revoke), owner check
   **inside each action** (models authorise nothing). Check at build-done: `grep -c owner_required
   admin/users/actions.ts` ≥ 6.
5. `queue-screen.tsx`: add `primaryAction?: ReactNode`, `overrideMode?: boolean` (default true; off
   skips OverrideModeProvider/menu toggle/banner), `search?: { param; label }` (input in facet row
   + Filter sheet, wire through `filterRows`/`clearFilterParams`).
6. New `components/admin/users-queue.tsx` (client `QueueScreen<UserRow>` per spec §3) and
   `components/admin/user-detail.tsx` (pane per spec §5 — biggest file, budget most of next
   session here: tabs, AdminSaveBar, Add-to-company dialog, Remove/Leave confirms, bank details,
   invited-row Resend/Revoke).
7. Rewrite `admin/users/page.tsx` per spec §2 (three row shapes a/b/c, `bank`/`viewerOwns`
   visibility — check RSC payload as a reviewer has no `lastFour`).
8. `admin/layout.tsx`: add `const CLIENT_MANIFEST_ANCHORS = [UserDetail]; void
   CLIENT_MANIFEST_ANCHORS`.
9. Run Part B checks (preflight.md B1–B4), then write+run the round script
   (`scripts/wayfinder-autopilot/capture-round.mjs`, both widths, detector + keyboard probes),
   fix findings, record r0 counts here, `tsc --noEmit` once, stop servers, write
   `milestone: build-done`.

## Open / needs owner
None blocking. Org-role editing and org rename deliberately out (#287).
