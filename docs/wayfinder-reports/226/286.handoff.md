# Hand-off — #286 Build Users (Admin, org-level Queue-screen shell)

milestone: spec-done

Phase **build** in progress (session 5 cut at the context line after the shell props; no Users UI
yet). Read `spec.md` + `preflight.md` in `docs/wayfinder-reports/226/logs/scratch-286/`,
`docs/agents/areas/admin.md` + `queue-shell.md` + `detail-pane.md`, then the steps below.
**Context budget warning:** loading `intent` + `impeccable` skills costs ~60K; with the primers and
spec this session hit the 150K line after 20 tool calls. Next session: read the hand-off, spec,
preflight, and craft-floor only; do not re-read the primers/lessons whole (`grep` them).

## Done (sessions 3–5)
- Schema + migration `20260917000000_add_workspace_member_bank_details` deployed, client generated
  (devdb `127.0.0.1:55433`; a shell `DATABASE_URL` shadows `.env` — use `scratch-286/migrate.mjs`).
- `models/workspaces.ts`: `addExistingUserToWorkspace`, `setWorkspaceMemberBankDetails`,
  `MemberBankDetails`, `getPendingInvitationWithGrants`. `models/organizations.ts`:
  `ownedWorkspaceIds`, `OrganizationInvitationRow`, `listOrganizationInvitations` (grants resolved,
  primary first, one row per invitation). **Model tests pass** (42/42, session 5).
- **Shell props landed (session 5, uncommitted → committed with this hand-off), via
  `scratch-286/edit-shell.mjs`:** `QueueScreen.primaryAction` (before `stat`), `overrideMode`
  (default true; false hides menu switch + banner), `search?: {param,label}` (input `#queue-search`
  after `#queue-facets` ≥ md, Esc clears when non-empty, 200 ms debounce → `router.replace`;
  in `FilterSheet` as `#filter-sheet-search`); `lib/queue/filters.ts` `activeFilterCount`/
  `clearFilterParams` take `extraParams`; `FacetFilters`/`FilterButton` take `extraParams`;
  `filtered` in QueueScreen counts `q`. Not yet run: `npx vitest run lib/queue components/queue`.

## Shell facts (save re-reading)
- Selected row = `${basePath}/${id}` via replaceState (not `?user=`): add
  `admin/users/[userKey]/page.tsx` rendering the same page with `initialSelectedId`. Row keys
  `u:<userId>` / `inv:<invitationId>`.
- **Pane content pattern to copy:** `components/queue/expense-claim-queue.tsx` lines 127–131 —
  `loadDetail` is a client `useCallback` that awaits a server action and returns `<UserDetail …/>`
  (a client component imported by the queue), so **no `CLIENT_MANIFEST_ANCHORS` line is needed**
  in `admin/layout.tsx`. `paneActions(row, helpers)` renders the pane's sticky bar slot;
  `paneMenu(row, helpers)` the ⋯ items (dialogs must live above the Popover — detail-pane.md).
- `PaneHelpers = { close, refresh, next }`; `refresh` bumps `reloadKey` (content remounts —
  pending-focus ref for `#user-pane-save`, lesson #258) + `router.refresh()`.
- Actions pattern: `workspace-actions.ts` (`getCurrentUser`, `requireMember(wsId, user.id,
  ["owner"])`, `ActionState`, `errorMessage`, `deliverInvitation`, `inviteUrlFor`, `revalidatePath`).
  `models/organizations.ts` is *not* "use server" (imports `prisma`, `unscoped`, `cache`).
- `listOrganizationUsers` lacks `createdAt`/bank/org role → write a new server-only loader
  `models/admin-users.ts` (`unscoped` + `workspaceId IN accessible ids`) producing spec §2's
  `UserRow` for cases (a)/(b)/(c), `bank` only for owner-of-current or self; reuse
  `listOrganizationInvitations` for invited rows.
- `getAdminContext(wsId)` → `{ user, membership, workspace, owner, owners, members }`.
  Incumbent page: `admin/users/page.tsx` (AdminPage + MembersTable + InvitePanel) — replace.

## Next steps, in order
1. `npx vitest run lib/queue components/queue` (shell change regression, ~11 s).
2. `models/admin-users.ts` loader (above) + `admin/users/actions.ts` ("use server"): load/invite/
   addToCompany/setRole/remove(+leave)/saveBankDetails/resend/revoke per spec §2, owner check in
   each (`grep -c owner_required` ≥ 6).
3. `components/admin/users-queue.tsx` (client, spec §3, on `QueueScreen` with the new props) and
   `components/admin/user-detail.tsx` (pane, spec §5: tabs, AdminSaveBar `inPane`/`status`,
   Add-to-company dialog, confirms, bank group, invited rows).
4. Rewrite `admin/users/page.tsx` + add `admin/users/[userKey]/page.tsx`.
5. Part B checks (preflight B1–B5), round script on `capture-round.mjs` (both widths, detector,
   keyboard probes) as `shots-r0`, fix findings, r0 counts here, `tsc --noEmit` (dev server
   stopped), `milestone: build-done`, commit `wip(autopilot): #286 build`.

## Open / needs owner
None blocking. Org-role editing and org rename deliberately out (#287).
