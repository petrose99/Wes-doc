# Hand-off — #286 Build Users (Admin, org-level Queue-screen shell)

Phase: **spec** (1 of 4), session 1 ended at the context line. No `milestone:` line yet — the Part D critic has not run, so the spec gate is not met. Next session: finish the spec phase (steps below), then write `milestone: spec-done`.

## Done this session
- Ticket claimed (assignee petrose99). Blocker #254 is closed (schema shipped `a186c97`).
- Read: admin primer, queue-shell primer, both lessons files, #285 spec + hand-off (sibling — the Users surface mirrors its shell/gating grammar), preflight template, craft-floor, PRODUCT.md, incumbent Admin critique (23/40), #231 decisions d2/d3/d6/d14/d19/d23/d24/d27, #254 resolution, #277/ADR 0003 bank-details comment on the ticket.
- `intent` (specify) and `impeccable` (shape) loaded; `impeccable context` launcher not attempted (project lesson).
- Written: `docs/wayfinder-reports/226/logs/scratch-286/spec.md` (§1 job + metric, §2 route/data/actions/schema/shell props, §3 header/table/three page states/gates, §4 flows 4.1–4.6, §5 pane, §6 state inventory, §7 copy matrix, §8 a11y, §9 anti-goals) and `…/scratch-286/preflight.md` (Parts A–C, E filled; D empty). Predicted critique 32/40 (H1 4, H5 4), evaluate sum ≈ 9, P1 0, Clean.
- Code reader: `…/scratch-286/read.mjs` (prints users page, org/workspace model exports, invitation sig, member actions, schema, guards in one call).

## Facts verified in code (do not re-read)
- Incumbent `admin/users/page.tsx`: `AdminPage` + `Panel` Members (`components/workspace/members-table.tsx`, 160 lines: intents role/remove/transfer/leave with two-phase `last_reviewer_removal_requires_confirmation`, `ConfirmDialog`, `NativeSelect`, sonner toasts) + `Panel` Invitations (`invite-panel.tsx` 25 lines + `member-invite.tsx` 47 lines, one copyable-link slot).
- `models/organizations.ts` `listOrganizationUsers(orgId,userId)` → deduped users with `companies[{workspaceId,workspaceName,role}]` over accessible workspaces. No org-level invitations read exists (spec adds `listOrganizationInvitations`).
- `models/workspaces.ts`: `createWorkspaceInvitation({workspaceId,ownerId,email,role,additionalGrants[]})` checks owner of the **primary only** (spec: action checks every grant); `updateWorkspaceMemberRole`, `removeWorkspaceMember`, `leaveWorkspace` (`cannot_leave_personal_workspace`), `transferWorkspaceOwnership`, `revokeWorkspaceInvitation`, `listWorkspaceInvitations`; guards throw `last_owner_required` / `last_reviewer_removal_requires_confirmation`. No "add existing user directly" fn (spec adds `addExistingUserToWorkspace`).
- Actions in `workspace-actions.ts`: change role, remove, transfer, leave, invite (FormData), resend, revoke.
- `WorkspaceMember` has **no** bank columns yet; #295 (open) owns the same three — check the schema before adding the migration.
- `QueueScreen` props `primaryAction`/`overrideMode` are #285's spec (build not landed); `search` prop is new in this spec.

## Exact next step (spec phase, continue)
1. Read `spec.md` + `preflight.md` whole (≈ 250 lines together) — do not re-derive.
2. Part D: one foreground `Agent` (`model: "opus"` — the ticket touches auth/roles) scoring spec + preflight as if built, with the critique rubric 4-rows and evaluate severities; list P1s, contracts and primitive gaps. Reconcile every heuristic where critic < self as a spec change; fill Part D table.
3. Gate: predicted zero P0/P1, no heuristic under 3, two 4s (≥ 32), Clean. Then rewrite this file with `milestone: spec-done`, commit `wip(autopilot): #286 spec`, write the report `docs/wayfinder-reports/226/286.md`, stop.

## Open / needs owner
- None blocking. Org-role editing and org rename deliberately out (#287).
