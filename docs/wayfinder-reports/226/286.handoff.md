# Hand-off — #286 Build Users (Admin, org-level Queue-screen shell)

milestone: spec-done

Phase **spec** complete (sessions 1–2). Next session is **build**: read this file, then `spec.md` + `preflight.md` whole (≈ 300 lines), the Admin and queue-shell primers, craft-floor, both lessons files; build every row of the Part B/C tables; no capture, no measurement; end with `milestone: build-done`.

## Artefacts
- Spec: `docs/wayfinder-reports/226/logs/scratch-286/spec.md` (§1 job/metric · §2 route/data/actions/schema/shell · §3 header/table/page states/gates · §4 flows 4.1–4.6 · §5 pane · §6 state inventory · §7 copy matrix · §8 a11y · §9 anti-goals).
- Pre-flight: `…/scratch-286/preflight.md` — Parts A–E filled, **Part D run (opus critic) and reconciled**. Gate met: predicted critique 32/40 (H1 4, H5 4, none under 3), evaluate P0/P1 0, health ≈ 85, sweep Clean. Critic's first pass was 29/40 with 5 P1s — all removed by spec changes (see Part D table).
- Code reader: `…/scratch-286/read.mjs` (users page, org/workspace model exports, invitation sig, member actions, schema, guards in one call).
- Reports: `docs/wayfinder-reports/226/286.md` (session log, per-session sections).

## Facts verified in code (do not re-read)
- Incumbent `admin/users/page.tsx`: `AdminPage` + `Panel` Members (`components/workspace/members-table.tsx`, 160 lines: role/remove/transfer/leave with two-phase `last_reviewer_removal_requires_confirmation`, `ConfirmDialog`, `NativeSelect`, sonner) + `Panel` Invitations (`invite-panel.tsx` + `member-invite.tsx`). Replaced on this page per spec §2.
- `models/organizations.ts` `listOrganizationUsers(orgId,userId)`; no org-level invitations read (spec adds `listOrganizationInvitations`).
- `models/workspaces.ts`: `createWorkspaceInvitation` checks owner of the **primary only**; `updateWorkspaceMemberRole` / `removeWorkspaceMember` **authorise nothing** — owner checks go in the actions (spec §2); `leaveWorkspace` throws `cannot_leave_personal_workspace`, `transfer_ownership_before_leaving`, `delete_workspace_instead`; `acceptWorkspaceInvitation` overwrites an existing member's role on a secondary grant (lines ≈ 431–435) — spec closes it with per-grant `member_already_exists:<name>` at invite time. No `addExistingUserToWorkspace` / `setWorkspaceMemberBankDetails` yet (spec adds both).
- `workspace-actions.ts` `resendWorkspaceInvitationAction` (≈ 147–151) **drops `additionalGrants`** — do not reuse; spec §2 defines a new `resendInvitationAction({invitationId})`.
- `WorkspaceMember` has **no** bank columns; #295 (open) owns the same three — check `prisma/schema.prisma` for `bankAccountNumber` before writing the migration.
- `QueueScreen` props `primaryAction`/`overrideMode` are #285's spec (check whether #285's build landed; else add here); `search` prop is new in this spec.

## Build-phase pointers
- New files: `components/admin/users-queue.tsx`, `components/admin/user-detail.tsx` (+ invite / add-to-company dialogs), `app/(app)/workspaces/[workspaceId]/admin/users/actions.ts`; model additions in `models/workspaces.ts` / `models/organizations.ts`; `CLIENT_MANIFEST_ANCHORS` line in `admin/layout.tsx` (RSC client-manifest gotcha, #225).
- Part B checks to run before build-done: `owner_required` count ≥ 6 in actions; reviewer RSC payload has no `lastFour`; no `window.confirm`; no new file under `components/ui`; string extraction for "member"-as-noun.
- `tsc --noEmit` once at the end of the build phase; affected vitest only.

## Pending for the close phase
- Project lessons file `.claude/wayfinder-autopilot/lessons.md` could not be written by this session (permission classifier blocks `.claude/` edits in autopilot). Line to append at close, from the report's lessons section: models authorise nothing / resend drops grants / four leave-owner codes.

## Open / needs owner
- None blocking. Org-role editing and org rename deliberately out (#287).
