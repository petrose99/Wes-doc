milestone: spec-done

## Steps
step: 1 — extend IntegrationsManager + trim page + gate + CONTEXT.md — done
step: G1 — round script — todo
step: G2 — r0 and fixes — todo
step: G3 — build-done — todo

## Artifacts
- spec: docs/wayfinder-reports/226/logs/scratch-329/spec.md
- pre-flight (filled, reconciled with critic): docs/wayfinder-reports/226/logs/scratch-329/preflight.md
- independent spec critique: docs/wayfinder-reports/226/logs/scratch-329/spec-critic.md

## Open findings
None yet — checks run so far: `tsc --noEmit` (clean on touched files, pre-existing unrelated
errors elsewhere), `vitest run models/workspaces.test.ts` (33 passed), `vitest run
lib/documents/retired-status-words.test.ts` (1 passed), `grep -rn Provisioning components/integrations
app/.../admin/integrations` (only a code comment, no user-facing string),
`grep -rn repairBigcapitalConnectionAction|getBigcapitalStatusAction components app` (new
IntegrationsManager callers alongside existing accounting-dashboard.tsx ones, per B1 check).

## Next step
Build session: G1 — write scripts/wayfinder-autopilot/capture-round.mjs (Admin › Integrations,
1440/390, 3 provider states + Bigcapital's Authorising… seeded as a pending job, detector JSON,
keyboard probe for tab-order/Enter-Space on Connect/Reconnect/Disconnect), reusing an existing
admin round script as the base (scratch-286/scratch-295). Dev workspace
af91555d-7450-4b21-a8ac-73db092617c8.
