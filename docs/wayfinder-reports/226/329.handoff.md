milestone: spec-done

## Steps
step: 1 — extend IntegrationsManager + trim page + gate + CONTEXT.md — todo
  - spec: spec.md §1 (provider list, 4 states, copy), §2 (Panel removal), §3 (auto-provision
    removal), §4 (gate OR), §5 (CONTEXT.md)
  - files: components/integrations/integrations-manager.tsx (provider list →
    quickbooks/xero/bigcapital, PROVIDER_LABELS de-fork against lib/finance/actions.ts:7,
    CardDescription rewrite, AccountingConnectionCard extended with Bigcapital's
    Authorising…/connect+poll via repairBigcapitalConnectionAction +
    getBigcapitalStatusAction, "Open in Bigcapital" link only);
    app/(app)/workspaces/[workspaceId]/admin/integrations/page.tsx (drop Ledger connection
    Panel block, widen accountingProviders prop, intro sentence);
    models/workspaces.ts (delete auto-provision block ~119-127);
    app/(app)/workspaces/[workspaceId]/layout.tsx:91,105 (OR the three enabled flags, via a
    new small helper — e.g. lib/config.ts or lib/finance/provider-flags.ts);
    CONTEXT.md (line ~262 glossary widen, ~265 delete Built-in ledger, ~306 Integrations & API
    line reword)
  - check: gate.mjs clean at 1440/390 on Admin › Integrations (3 provider states captured:
    not-connected, connected, needs-reconnect — Bigcapital's Authorising… captured via a
    slow-motion seed/mock of the job as pending); `grep -rn Provisioning components/integrations
    components/accounting app` returns nothing user-facing; `npx vitest run models/workspaces.test.ts`
    passes; retired-status-words.test.ts still passes (unrelated word list, not touched)
step: G1 — round script — todo
step: G2 — r0 and fixes — todo
step: G3 — build-done — todo

## Artifacts
- spec: docs/wayfinder-reports/226/logs/scratch-329/spec.md
- pre-flight (filled, reconciled with critic): docs/wayfinder-reports/226/logs/scratch-329/preflight.md
- independent spec critique: docs/wayfinder-reports/226/logs/scratch-329/spec-critic.md

## Open findings
None yet — build not started. Pre-flight gate: critique 32/40 predicted, no heuristic <3,
evaluate ≈88, 0 P0/P1, Clean.

## Next step
Build session: implement step 1 (the one build step) per spec.md + preflight.md, then run the
capture round (G1-G3) at 1440/390 on Admin › Integrations, dev workspace
af91555d-7450-4b21-a8ac-73db092617c8, per docs/agents/areas/admin.md's seed/dev-server/capture
recipe (reuse an existing admin round script as the base, e.g. scratch-286's or scratch-295's).
