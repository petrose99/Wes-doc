# Autopilot report — #266 Build intake on the typed queues: Add ‹type› header action, drop-to-add, the Add documents dialog with the inbound address

- Map: #226 DocuBite's authenticated app works the way Vic.ai's does, end to end
- Type: wayfinder:task · Started: 2026-09-20 08:2x UTC · Finished: 2026-09-20 08:4x UTC
- Outcome: partial (spec phase complete; build not started this session)

## What I did
1. Claimed #266 (`gh issue edit 266 --add-assignee @me`).
2. Read #266's body and its blocking decision ticket #243 (closed, decisions 1–12) and its Action Summary in full.
3. Confirmed #267 (owner sign-off, phone-upload removal) is closed — no blocker.
4. Read the queue-shell area primer, `QueueScreen`'s header-row markup, `InvoiceQueue`/`DocumentQueue`, the Extract overlay's internals (`ExtractOverlay`, `ExtractPanel`, `useExtractionProgress`), `FileHubUploadButton` (the closest existing analog), `ensurePipelineFile`, the `/pipeline` page/actions, `lib/domains/finance.ts`'s four document-type codes, and `InboundAddressLine` (already anticipates #266 as a second consumer).
5. Wrote the spec (`docs/wayfinder-reports/226/logs/scratch-266/spec.md`): screen-by-screen for the header Add button, the queue drop-zone, the new Add documents dialog (full #243 decision-8 state inventory), the `/pipeline` redirect, and extending intake to all four typed queues (not just Invoices, which #264 already covers).
6. Filled the pre-flight (`docs/wayfinder-reports/226/logs/scratch-266/preflight.md`) Parts A–C, then ran the independent spec critic (fresh-context `Agent`, sonnet) for Part D.
7. Critic's first pass: 31/40, two real P1s (format-rejection path undefined for drag-and-drop; mobile operators had no way to relearn the inbound address after first use). Fixed both in the spec (state-table row now covers cap + format symmetrically and explicitly covers drag-and-drop bypassing `accept`; added a "Mobile inbound-address rediscovery" section reusing the existing How DocuBite works surface) rather than arguing with the critic. Reconciled: 34/40, three 4s (H1, H5, H9), evaluate worst-issue sum 7, zero P0/P1, verdict Clean.
8. Wrote the build plan onto the hand-off (`docs/wayfinder-reports/226/266.handoff.md`): 6 substantive steps + 3 gate steps, 9 capture states — within the phase-sizing limits, no split needed.
9. Committed spec/preflight/hand-off as WIP.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Is the 200 MB cap a real server-side storage limit or new UI-only arithmetic? | Client-side-only per-batch cap for this ticket; no existing byte-based field on `WorkspaceUsage` | Smallest reversible option; `WorkspaceUsage` only has `documentsLimit`/`aiLimit`, not bytes — inventing a server field is out of scope for an intake-UI ticket |
| 2 | How does a mobile operator relearn the inbound address after first-use, given #243 decision 5 rules out a banner/header line? | Add the line to the existing opt-in **How DocuBite works** surface (Account page on phone) | #243 decision 5 forbids a persistent/banner surface, not an opt-in lookup one; reuses an existing primitive rather than inventing a new always-on one |
| 3 | Delete `PipelineShell`/the old `/pipeline` UI now that the route redirects? | No — leave it unreached, flag as a follow-up cleanup | Ticket scope is the redirect (#243 decision 4's literal words); deleting a whole shell component is a materially larger diff than the intake surface itself, and risks breaking other things the session didn't audit |
| 4 | Dashboard/Reports/ControlsSpine links pointing at `/pipeline?stage=X` losing their stage-specific destination once `/pipeline` redirects unconditionally to Invoices — fix now or flag? | Flag as a known, accepted regression from #243 decision 4, not blocking | The decision (closed, not re-opened here) is "redirect to the Invoices queue"; the stage-to-type mapping this would need doesn't exist 1:1 and re-deciding it is outside this ticket |

## Decisions recorded
None — this is a build ticket, not a decision ticket. No tracker comment/close: the ticket stays open and assigned, spec phase done.

## Tickets created / changed
None created. #266 remains open, assigned, spec phase complete (hand-off `milestone: spec-done`).

## Scores and counts (rendered surfaces only)
scores: predicted-critique=34 first-critique=n/a close-critique=n/a first-evaluate=n/a close-evaluate=n/a
No build yet — nothing measured. Pre-flight's reconciled prediction: 34/40 (H1, H5, H9 at 4), evaluate worst-issue sum 7, zero P0/P1, verdict Clean.

## First-pass findings → lessons (rendered surfaces only)
Not applicable yet — no first pass built.

## Needs the owner
Nothing blocking. Two non-blocking flags for awareness, both already resolved in the spec rather than left open:
- The `/pipeline?stage=X` deep-link regression (Dashboard/Reports/ControlsSpine) is an accepted consequence of #243 decision 4, not a new decision — no action needed unless the owner wants those links redesigned later (would be a new ticket, not this one).
- `PipelineShell` and its stage-tab chrome become dead code once `/pipeline` redirects; left in place this session as a scope boundary, worth a small follow-up cleanup ticket at some point.

## Session 2 (build)

Continuation session picking up build step 1, which a prior session had written but left unverified (hit the hand-off line before running checks).

1. Read the hand-off; step 1's code (`components/queue/queue-screen.tsx` `addAction`/`dropZone` slots, drag-over overlay) was already committed by a prior session (`0130517`).
2. Ran `npx tsc --noEmit -p .`: remaining errors are all pre-existing and unrelated to queue-screen.tsx — confirmed via `git stash`/re-run on the same commit before any #266 edits, identical error list. queue-screen.tsx itself compiles clean.
3. Ran `npx vitest run components/queue`: 5 files, 26/26 passing.
4. Marked step 1 `done` on the hand-off, updated Open findings/Next step, committed (`8848e08`).
5. Did not start step 2 (Add documents dialog) — one plan step per build session per the phase brief.

Outcome: partial (build step 1 of 6 substantive steps + 3 gate steps confirmed done; ticket stays open, assigned, for the next build session to take step 2).

## Session 3 (build)

Continuation session, build step 2 (Add documents dialog).

1. Read the hand-off (step 1 done); read spec Screen 3 (lines 43-75) and preflight B1/B2/B3/B4/B5 in full.
2. Read the reuse targets by range: `components/extract/use-extraction-progress.ts` (whole, 126 lines), `components/extract/extract-panel.tsx` `uploadRows`/`addEntries`/`handleDrop` (lines 150-410), `components/ui/dialog.tsx` (whole), `components/intake/inbound-address-line.tsx` (whole), `components/extract/types.ts` (SheetTemplate), `lib/domains/finance.ts` (FINANCE_TEMPLATES codes), and `app/(app)/workspaces/[workspaceId]/pipeline/page.tsx` for how `ensurePipelineFile`/`getFileTemplates`/`uploadTemplates` are already wired (this ticket's step 3/4 will reuse the same shape).
3. Exported `DOCUMENT_TYPES` from `extract-panel.tsx` (one-word reuse rather than duplicating the accepted-mime literal its own comment says must stay in sync).
4. Wrote `components/intake/add-documents-dialog.tsx`: type select (from a `templates: SheetTemplate[]` prop), drop/browse area sharing `DOCUMENT_TYPES`, staged file list with per-row staged/uploading/done/failed + Retry + remove ×, client-side 200MB cap and PDF/image check applied identically in `addFiles` (called from both `input.onChange` and `onDrop`, so drag-drop can't bypass the OS-picker-only `accept` attribute), `InboundAddressLine` reused verbatim, footer Cancel/Add ‹n›/Done. Locked (Esc/×/backdrop no-op) while `sending`. Calls `uploadDocumentsAction` directly per-file (mirrors `uploadRows`'s per-file try/catch so one failure doesn't abort the batch) and hands settled document ids to the caller's `onUploaded` callback so the caller's own `useExtractionProgress` instance keeps the existing toast/refresh behavior working after this dialog closes (B4 — dialog owns no poller itself; step 3's header button will).
5. `npx tsc --noEmit -p .`: no new errors from either touched file.
6. `npx vitest run components/queue components/extract`: 5 files, 26/26 passing (no new test file added — the component has no branchy pure logic split out yet; `acceptFile`'s cap/mime check is exercised by the manual state walk step G2 will run against a seeded file).
7. Marked step 2 `done` on the hand-off, updated Next step to step 2b, committed (`6430ca9`).
8. Did not start step 2b — one plan step per build session.

Outcome: partial (build steps 1-2 of 6 substantive steps + 3 gate steps done; ticket stays open, assigned, for the next build session to take step 2b).

## Session 4 (build)

Continuation session, build step 2b (Mobile inbound-address rediscovery).

1. Read the hand-off (steps 1-2 done); read spec §"Mobile inbound-address rediscovery" (lines 88-89) and `components/shell/how-it-works.tsx` whole.
2. Traced how the dialog gets mounted and where `inboundAddress` is already computed elsewhere: `components/shell/sidebar.tsx` mounts `<HowItWorksDialog />` once; `app/(app)/workspaces/[workspaceId]/layout.tsx` renders `Sidebar` and already computes the equivalent counts server-side; the `(queue)/invoices/page.tsx` pattern (`config.inboundEmail.enabled ? ensureInboundEmailToken(workspaceId) : null`) is the one to mirror.
3. Added the same `ensureInboundEmailToken`/`config.inboundEmail` computation to `WorkspaceLayout`, threaded `inboundAddress` through `Sidebar`'s props to `HowItWorksDialog`, and had `HowItWorksDialog` render `<InboundAddressLine address={inboundAddress} />` (third consumer, reused verbatim) in a new bordered block only when `inboundAddress` is truthy — nothing rendered when `inboundEmail.enabled` is false (UC5).
4. `npx tsc --noEmit -p .`: no new errors from the three touched files.
5. Marked step 2b `done` on the hand-off, updated Next step to step 3, committed (`wip(autopilot): #266 build step 2b`).
6. Did not start step 3 — one plan step per build session.

Outcome: partial (build steps 1-2b of 6 substantive steps + 3 gate steps done; ticket stays open, assigned, for the next build session to take step 3).
