milestone: spec-done

## Ticket
#270 (map #226) — Build Search on the Queue-screen shell. Trimmed at spec
time: the Closed-facet retrofit on the five typed queues + Archive verb +
signed removals split to #344 (blocked by this ticket). #270 is the Search
surface only.

## Step plan (build phase runs one per session)

- step: 1 — Rail item + route rewrite skeleton — todo
  Add "Search" rail item (top of spine, `components/shell/sidebar.tsx`);
  rewrite `search-client.tsx` onto `QueueScreen` shell shape (header search
  box replacing Views/Sort row, remove Ask AI/Sparkles/AssistantPanel per
  spec §1/§2a). Check: page renders on `QueueScreen`, no `AssistantPanel`
  import left.
- step: 2 — Predicate, columns, facets — todo
  New predicate (every doc, every state, archived included — do not reuse
  `LIBRARY_WHERE`/`models/library-facets.ts`, spec §1); columns (mark, Type,
  Supplier, Number, Date, Amount, Ledger mark); Type/Status/Supplier/Date
  facets via `lib/queue/filters.ts`, generic Status facet component (Open/
  Closed groups) built for #344 to reuse. Check: `npx vitest run lib/queue`
  passes; no-query/no-facet request returns zero rows server-side.
- step: 3 — Row → pane, Origin hop, persistent syntax help — todo
  Row/pane wiring (`?doc=`), "Open on ‹Queue›" via `withOrigin` with Search's
  full URL, confirm `documents/[documentId]/page.tsx:308-313` forwards
  `from`; add the `?` syntax popover (spec §2a) reusing the shared Popover
  primitive. Check: `grep -rn "Popover" components/ui` shows the reused
  import, not a new component file.
- step: 4 — States — todo
  Empty-query guidance + one-time inline hint, zero-results, slow skeleton
  (reuse `QueueLoading` row skeleton), result-gone notice, archived-row mark,
  embeddings-off silent (spec §3). Check: each state reachable via a scripted
  probe (empty q, q with no matches, q matching an archived doc).
- step: 5 — Phone — todo
  Search entry on Account tab list (#232 structure); `QueueCard` rows; full
  screen read-only pane, no bulk, no ⋯ beyond "Open on ‹Queue›". Check:
  `/` is inert below `md` (already gated, verify not broken).
- step: G1 — round script (capture 1440 + 390, list + pane-open + phone,
  ~10 states) — todo
- step: G2 — r0 read + fixes to close real detector/critique/evaluate gaps — todo
- step: G3 — build-done (tests green, gate clean, hand-off updated) — todo

## Artifacts
- docs/wayfinder-reports/226/logs/scratch-270/spec.md
- docs/wayfinder-reports/226/logs/scratch-270/preflight.md
- docs/wayfinder-reports/226/logs/scratch-270/split-body.md
- docs/wayfinder-reports/226/logs/scratch-270/trimmed-body.md

## Open findings
None yet — preflight Part D critic pass is recorded in preflight.md (31/40 →
reconciled 34/40 after the §2a fix). No scratch findings file needed yet.

## Next step
Build session 1: implement step 1 (rail item + shell skeleton) per spec.md §1/§2a.
