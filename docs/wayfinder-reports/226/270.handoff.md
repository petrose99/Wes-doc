milestone: spec-done
milestone: build-step-G1
milestone: build-step-G2

## Ticket
#270 (map #226) — Build Search on the Queue-screen shell. Trimmed at spec
time: the Closed-facet retrofit on the five typed queues + Archive verb +
signed removals split to #344 (blocked by this ticket). #270 is the Search
surface only.

## Step plan (build phase runs one per session)

- step: 1 — Rail item + route rewrite skeleton — done
- step: 2 — Predicate, columns, facets — done
- step: 3 — Row → pane, Origin hop, persistent syntax help — done
- step: 4 — States — done
- step: 5 — Phone — done
- step: G1 — round script — done
- step: G2 — r0 read + fixes — done
- step: G3 — build-done — todo

## Artifacts
- docs/wayfinder-reports/226/logs/scratch-270/{spec,preflight,split-body,trimmed-body}.md
- docs/wayfinder-reports/226/logs/scratch-270/round-270.mjs (gate round script)
- docs/wayfinder-reports/226/logs/scratch-270/shots-r1/ (clean gate round)
- docs/wayfinder-reports/226/logs/scratch-270/residue.txt (final regex list)
- docs/wayfinder-reports/226/logs/scratch-270/g2-notes.md (session history)
- app/(app)/workspaces/[workspaceId]/search/search-client.tsx (this session:
  hint row text-slate-600, code chips whitespace-nowrap, empty done.body="")
- components/queue/queue-empty.tsx (this session: done body only renders
  when truthy, so an explicit empty string suppresses it)
- models/search.ts, search-actions.ts (search surface, prior sessions)
- components/queue/queue-screen.tsx (prior session: `selectedIdParam` prop
  + `rowUrl` helper — fixes `?doc=<id>` deep link)
- scripts/wayfinder-autopilot/capture-round.mjs (prior session: `focusedOf`
  includes element `id`)

## Open findings
None open. `node gate.mjs shots-r1 --residue-file residue.txt` → CLEAN,
0 real findings, 21/21 probes pass, 0 errored states, 0 page errors, both
widths.

## Next step
Build step G3: `tsc --noEmit` full project (servers stopped), Part B
contract checks (reachability grep, string extraction, primitive diff)
over the whole surface, write `milestone: build-done`, commit.
