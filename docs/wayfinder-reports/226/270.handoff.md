milestone: spec-done

## Ticket
#270 (map #226) — Build Search on the Queue-screen shell. Trimmed at spec
time: the Closed-facet retrofit on the five typed queues + Archive verb +
signed removals split to #344 (blocked by this ticket). #270 is the Search
surface only.

## Step plan (build phase runs one per session)

- step: 1 — Rail item + route rewrite skeleton — done
- step: 2 — Predicate, columns, facets — done
  `search-client.tsx` rewritten onto `searchListAction`/`SearchRow`: real
  columns (State via `StatePills`, Type via `Badge`, Supplier, Number, Date
  via `formatDate`, Amount via `formatMoney`, Ledger via a `Check` icon keyed
  off `row.ledgerMark`); `facets` wired (Type — all nine `DOC_TYPES`; Status
  — `statusFacet()`); Supplier uses the shell's own `search` prop (own URL
  param/header box) since `Facet` has no free-text kind, per spec §1. Date
  range UI not built this step (server filter already accepts
  `dateFrom`/`dateTo`, no date-picker chip yet — next step or a follow-up,
  not blocking the check below). `statusFacet()` test added to
  `lib/queue/filters.test.ts`. Fixed a pre-existing break: kept
  `globalSearchAction`/`GlobalSearchResult` in `search-actions.ts` alongside
  the new `searchListAction` — `components/shell/global-search.tsx`'s cmd+k
  widget (a separate surface) still imports the old export.
  Check: `npx vitest run lib/queue` — 13 passed. `npx tsc --noEmit` — clean,
  whole project.
- step: 3 — Row → pane, Origin hop, persistent syntax help — done
  Row → pane already worked via `QueueScreen`'s `?doc=` mechanic (no change
  needed). Added `paneMenu` to `search-client.tsx`: a `PaneMenuItem`
  "Open on ‹Queue›" for the four typed doc types only (via
  `TYPED_DESTINATIONS`/`documentDestinationPath` + `withOrigin(target,
  here)`, `here` = pathname + full current search params including
  `doc=`) — secondary types get no item (spec §1). Confirmed by grep:
  `documents/[documentId]/page.tsx`'s legacy-route redirect already
  forwards `from` generically for every origin (#268's contract, no
  rewrite needed). Added the persistent syntax-help popover beside the
  search box (spec §2a): `CircleHelp` icon button, `aria-label="Search
  syntax"`, opens `components/ui/popover`'s `Popover` listing the same
  four (five) examples every time, not one-shot.
  Check: `npx tsc --noEmit` — clean. `npx eslint` on the file — clean.
- step: 4 — States — done
  All six spec §3 states: empty-query/zero-results via new `extraFilterParams`/`loadingRows` props
  on `QueueScreen` + `q` synced to the URL; `ArchivedMark()` (row-cells.tsx) via `StatePills`
  `trailing`; result-gone and embeddings-off needed no new code (already met). Detail in
  `wip(autopilot): #270 build step 4` commit. Check: `tsc --noEmit` clean; `eslint` clean on every
  touched file (queue-screen.tsx's 2 findings pre-existing, confirmed via `git stash`);
  `vitest run lib/queue` 13 passed.
- step: 5 — Phone — todo
- step: G1 — round script — todo
- step: G2 — r0 read + fixes — todo
- step: G3 — build-done — todo

## Artifacts
- docs/wayfinder-reports/226/logs/scratch-270/spec.md
- docs/wayfinder-reports/226/logs/scratch-270/preflight.md
- docs/wayfinder-reports/226/logs/scratch-270/split-body.md
- docs/wayfinder-reports/226/logs/scratch-270/trimmed-body.md
- models/search.ts (new)
- app/(app)/workspaces/[workspaceId]/search-actions.ts (rewritten, kept old export)
- app/(app)/workspaces/[workspaceId]/search/search-client.tsx (rewritten)
- lib/doc-types.ts (SEARCH_FIELD_KEYS added)
- lib/queue/filters.ts (statusFacet() added) + filters.test.ts (case added)
- components/queue/queue-screen.tsx (extraFilterParams, loadingRows props)
- components/queue/queue-loading.tsx (QueueRowSkeleton extracted)
- components/queue/row-cells.tsx (ArchivedMark added)

## Open findings
None yet — preflight Part D critic pass recorded in preflight.md.

## Next step
Build step 5 in the next session: Phone (spec, check the phone section for
Search specifically — search box/facets on the Filter sheet, card list,
full-screen pane sheet Back-first-focusable per spec §5).
