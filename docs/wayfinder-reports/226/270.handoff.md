milestone: spec-done

## Ticket
#270 (map #226) — Build Search on the Queue-screen shell. Trimmed at spec
time: the Closed-facet retrofit on the five typed queues + Archive verb +
signed removals split to #344 (blocked by this ticket). #270 is the Search
surface only.

## Step plan (build phase runs one per session)

- step: 1 — Rail item + route rewrite skeleton — done
- step: 2 — Predicate, columns, facets — done (search-actions.ts,
  search-client.tsx, statusFacet(); kept globalSearchAction for cmd+k)
- step: 3 — Row → pane, Origin hop, persistent syntax help — done
  (paneMenu "Open on ‹Queue›", CircleHelp popover per spec §2a)
- step: 4 — States — done (empty/zero-results/slow/archived-mark via
  QueueScreen's extraFilterParams/loadingRows + ArchivedMark())
- step: 5 — Phone — done
  Phone list/card/pane come free from `QueueScreen` (no `selectable`/bulk
  prop passed → no bulk bar); `/` already matchMedia-gated desktop-only.
  Only gap: entry point — added a "Search" row (first item) to Account
  tab's "Also in this company" list (account/page.tsx).
  Check: tsc --noEmit clean, eslint clean on touched file.
- step: G1 — round script — todo
- step: G2 — r0 read + fixes — todo
- step: G3 — build-done — todo

## Artifacts
- docs/wayfinder-reports/226/logs/scratch-270/{spec,preflight,split-body,trimmed-body}.md
- models/search.ts (new)
- app/(app)/workspaces/[workspaceId]/search-actions.ts (rewritten, kept old export)
- app/(app)/workspaces/[workspaceId]/search/search-client.tsx (rewritten)
- app/(app)/workspaces/[workspaceId]/account/page.tsx (Search row added)
- lib/doc-types.ts (SEARCH_FIELD_KEYS added)
- lib/queue/filters.ts (statusFacet() added) + filters.test.ts (case added)
- components/queue/queue-screen.tsx (extraFilterParams, loadingRows props)
- components/queue/queue-loading.tsx (QueueRowSkeleton extracted)
- components/queue/row-cells.tsx (ArchivedMark added)

## Open findings
None yet — preflight Part D critic pass recorded in preflight.md.

## Next step
Build step G1 in the next session: write the round script
(scripts/wayfinder-autopilot/capture-round.mjs) per the phase brief's gate
steps — every Search state at 1440/390, detector JSON, keyboard probes for
spec §5's contracts.
