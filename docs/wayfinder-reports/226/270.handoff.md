milestone: spec-done

## Ticket
#270 (map #226) — Build Search on the Queue-screen shell. Trimmed at spec
time: the Closed-facet retrofit on the five typed queues + Archive verb +
signed removals split to #344 (blocked by this ticket). #270 is the Search
surface only.

## Step plan (build phase runs one per session)

- step: 1 — Rail item + route rewrite skeleton — done
- step: 2 — Predicate, columns, facets — in progress (this session, not done)
  Backend done: `models/search.ts` (new `searchWorkspaceDocuments` — every
  Document, every `processingState()`, `archivedAt` included, no
  `LIBRARY_WHERE`); `SEARCH_FIELD_KEYS` map added to `lib/doc-types.ts`;
  `statusFacet()` (Open/Closed sections) added to `lib/queue/filters.ts`;
  `search-actions.ts` rewritten as `searchListAction` (chip `type:`/`status:`
  folds into facets when no other query text, per spec §2a).
  Still to do before `done`: rewrite `search-client.tsx` to call
  `searchListAction` with `q` + URL facet params (`type`, `status`,
  `supplier`, `dateFrom`, `dateTo`), build the real `QueueColumn` set (mark
  via `StatePills`, Type chip from `row.typeLabel`, Supplier, Number, Date
  via `formatDate`, Amount via `formatMoney`, Ledger mark — plain check icon
  keyed off `row.ledgerMark`), and wire the `facets` prop (Type — all nine
  `DOC_TYPES` via `DOC_TYPE_SPECS[...].label`; `statusFacet()`; Supplier —
  free-text facet if the existing `Facet` type supports one, else a
  `supplier` extraParam; Date range). No `filterRows` needed — the server
  action already returns the filtered set, `facets` here is UI-only (writes
  URL params `queue-screen.tsx` reads back out for the next `searchListAction`
  call).
  Also still to do: a small `lib/queue/filters.test.ts` case for
  `statusFacet()` (ponytail: non-trivial logic needs one check) — no test
  written yet, `npx vitest run lib/queue` not yet run this session.
  Check (unchanged from spec): `npx vitest run lib/queue` passes; no-query/
  no-facet request returns zero rows server-side (`searchWorkspaceDocuments`
  returns early in that case — already written, not yet exercised by a test
  or the live client).
- step: 3 — Row → pane, Origin hop, persistent syntax help — todo
- step: 4 — States — todo
- step: 5 — Phone — todo
- step: G1 — round script — todo
- step: G2 — r0 read + fixes — todo
- step: G3 — build-done — todo

## Artifacts
- docs/wayfinder-reports/226/logs/scratch-270/spec.md
- docs/wayfinder-reports/226/logs/scratch-270/preflight.md
- docs/wayfinder-reports/226/logs/scratch-270/split-body.md
- docs/wayfinder-reports/226/logs/scratch-270/trimmed-body.md
- models/search.ts (new)
- app/(app)/workspaces/[workspaceId]/search-actions.ts (rewritten)
- lib/doc-types.ts (SEARCH_FIELD_KEYS added)
- lib/queue/filters.ts (statusFacet() added)

## Open findings
None yet — preflight Part D critic pass recorded in preflight.md.

## Next step
Finish build step 2 in the next session: rewrite
`app/(app)/workspaces/[workspaceId]/search/search-client.tsx` to call the
new `searchListAction` (import from `search-actions.ts`, drop the old
`globalSearchAction`/`GlobalSearchResult`/`SearchResultItem` imports), build
the real column set and wire the `facets` prop per spec.md §1; add the
`statusFacet()` test case; run `npx vitest run lib/queue` and `tsc --noEmit`
on the touched files before marking step 2 `done` and committing
`wip(autopilot): #270 build step 2`.
