# Autopilot report — #333 Decide: first-run Bigcapital framing before Connect (H10 gap from #329)

- Map: #226 Make the operator app work like Vic.ai (grammar, not skin)
- Type: wayfinder:grilling · Started: 2026-09-20 · Finished: 2026-09-20
- Outcome: resolved

## What I did
1. Claimed the ticket (`gh issue edit 333 --add-assignee @me`).
2. Read the map (#226) body and the parent ticket #329's close-critique context (H10 at 2/4) that surfaced this ticket.
3. Read `components/integrations/integrations-manager.tsx` — `IntegrationsManager`'s not-connected card description (already names all three providers and the ledger-agnostic stance) and `BigcapitalRow` (the not-connected/needs-reconnect row: label + Connect/Reconnect button only, no caption, no link — unlike QuickBooks/Xero rows which at least carry a recognizable brand name).
4. Ran a single grilling round (Intent + Impeccable agendas per `docs/agents/design-tickets.md` §"Grilling a design ticket"), taking the recommended answer on each question per the autopilot's standing delegation. Recorded in the resolution comment: [docs/wayfinder-reports/226/logs/scratch-333/resolution.md](logs/scratch-333/resolution.md).
5. Posted the resolution comment and closed #333: https://github.com/petrose99/Wes-doc/issues/333#issuecomment-5751736091
6. Created the execution ticket [Build the Bigcapital first-run caption and link (execution of #333) (#349)](https://github.com/petrose99/Wes-doc/issues/349), parented to map #226.
7. Appended one line to map #226's Decisions so far (restored the full map body after an accidental placeholder overwrite mid-session — verified the restored body matches the original with only the new line added).

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Admin screen or defer to marketing/onboarding? | Fix in place, in the admin row | Visible intent at the point of decision — the recognition gap is at the Connect button; sending the operator away mid-task fails the "constraints they act under" test. Cheapest fix is also correct. |
| 2 | Tooltip, dialog, or inline caption/link? | Inline caption + external link, no dialog | Craft floor for Operate mode: no modal for a one-line fact; matches the existing plain-row markup other providers use. |
| 3 | Copy? | "Bigcapital · open-source accounting" caption + `bigcapital.app` link | Factual register matching the existing card description; no sales pitch. |
| 4 | Applies to connected-state card too? | No — not-connected/needs-reconnect row (`BigcapitalRow`) only | Recognition gap is pre-commitment; `AccountingConnectionCard` (shared, connected state) stays untouched, consistent with #332's approach. |

## Decisions recorded
The admin screen does need a one-line "what is this" affordance, scoped to `BigcapitalRow`'s not-connected/needs-reconnect state: an inline caption ("open-source accounting") and an external link to bigcapital.app, no dialog, no marketing-site dependency. Full resolution and Action Summary (articulate → impeccable clarify → critique/detect → include) on #333's closing comment.

## Tickets created / changed
- Closed #333 (this ticket), resolution comment posted.
- Created [Build the Bigcapital first-run caption and link (execution of #333) (#349)](https://github.com/petrose99/Wes-doc/issues/349) — `wayfinder:task`, blocked on nothing, parented to #226.
- Map #226: appended one Decisions-so-far line for #333; no fog graduated (this was a narrow named gap from #329's close, not a fog patch); no tickets ruled out of scope.

## Scores and counts (rendered surfaces only)
Not applicable — this was a decision ticket; no rendered surface was changed in this session. Per the phase brief: "no score; surface not built yet" (execution happens on #349).

## First-pass findings → lessons (rendered surfaces only)
Not applicable (decision ticket, no build).

## Needs the owner
Nothing. No removal, no credential, no ambiguous call — the recommendation was cheap, reversible, and consistent with #332's sibling decision on the same parent ticket.

## Process note
Mid-session, a `gh issue edit 226 --body-file -` heredoc call was mistyped and briefly overwrote the map's body with a placeholder string. Caught immediately (before any other session could read it) and restored from the full body captured earlier in this session's context, with only the new #333 line added — verified by diffing the tail of the restored body against the intended addition. No data was lost; flagging here per the report's factual-record rule.
