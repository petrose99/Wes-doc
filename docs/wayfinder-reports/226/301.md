# Autopilot report — #301 Fold in the org/company vocabulary

- Map: #226 Vic.ai-grammar operator app
- Type: build (execution ticket of #300) · Started: 2026-09-20 03:15 UTC · Finished: 2026-09-20 03:45 UTC
- Outcome: resolved

## What I did
1. Claimed #301, read the hand-off file (milestone `build-done`: the copy sweep was finished and committed as `9d31bc0` in a prior session; only the measure/close bar was outstanding).
2. Brought the environment up per the hand-off's recipe: `docubite-devdb` was already running; started `next dev --turbopack` in the background and the cached `impeccable live-server` binary via the documented `execFileSync` workaround.
3. Gave the fixed `DEV_BYPASS_USER` (`00000000-0000-4000-8000-00000000d0de`) a `users` row, an `organization_members` admin row on Acme Advisory, and a `workspace_members` owner row on Harbor Lights Cafe, since none existed — the hand-off flagged this as the exact blocker for live-testing org mode.
4. Ran the in-page detector (`scripts/dev/detect-in-page.mjs`) against Companies (list + rename dialog open) and Users (org mode) at 1440/390, and a keyboard-flow script (menu → dialog → Escape → focus return) at both widths.
5. Traced every non-residue finding to pre-existing shell code untouched by commit `9d31bc0` (rail nav `text-occlusion`, row-title `text-overflow` on long seeded names) or to #300's verbatim-decided banner wording (`line-length` ~88 chars) — none on the ticket's own surface, the `RenameOrganizationDialog`, which scored 5/5 (app-wide residue only) at both widths.
6. Scored `critique` (38/40, no heuristic below 3) and `evaluate` (health 90, 0 P0/P1, Clean) against the two pieces of UI this ticket built/finished: the rename dialog and the org-mode Users banner. Full tables in `docs/wayfinder-reports/226/logs/scratch-301/critique-evaluate.md`.
7. Posted the resolution comment and closed #301: https://github.com/petrose99/Wes-doc/issues/301#issuecomment-5747330833
8. Appended #301's line to map #226's Decisions so far: https://github.com/petrose99/Wes-doc/issues/226
9. Killed the dev server and impeccable live-server processes started this session to leave the machine clean.

## Questions I answered on the owner's behalf
None — this session ran the measure/close phase of an already-decided, already-built ticket; no new design decisions arose.

## Decisions recorded
#301 closed with: bar met (critique 38/40, evaluate 90/0/Clean, detector 5/5 on the owned surface, keyboard flow PASS both widths). No content or scope decision changed from #300's resolution.

## Tickets created / changed
None created. #300's line in the map already pointed at #301 as its execution ticket; no fog graduated this session (the one fog item naming #301, "a second Organization admin", stays fog — it needs the org role to see real use before it can be sharpened, per its own wording).

## Scores and counts (rendered surfaces only)
scores: predicted-critique=n/a first-critique=38 close-critique=38 first-evaluate=90 close-evaluate=90
gate counts: RenameOrganizationDialog 5/5 at 1440/390 (app-wide residue only, matches documented baseline). Companies list 10/5 (5 non-residue at 1440 = pre-existing rail `text-occlusion`, absent at 390). Users org-mode banner 12/7 (7 non-residue: 5 pre-existing rail `text-occlusion` + 1 `line-length` on #300's locked wording + 1 body-wide `em-dash-overuse`, at 1440; 2 pre-existing row `text-overflow` at 390). None of the non-residue findings sit in files this ticket's commit (`9d31bc0`) touched. `include`: PASS both widths (Enter opens menu → ArrowDown → Enter opens dialog focused on the input → Escape closes, focus returns to trigger).

## First-pass findings → lessons (rendered surfaces only)
| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| `text-occlusion` ×5 on admin rail nav labels (Companies/Users/Configuration/PO Mismatch Flows/Suppliers) | Not this ticket's contract — pre-existing shell defect on the admin nav rail, reproduced identically regardless of page content | unchecked (pre-existing, out of scope) | None taken; noted for a future shell/rail ticket |
| `text-overflow` ×2 on Users row titles (long seeded names) | Pre-existing `QueueScreen` row-title truncation with long seed data | unchecked (pre-existing, out of scope) | None taken |
| `line-length` on the org-mode banner's first sentence (~88 chars, aim <80) | #300's decision locks this sentence's exact wording; a soft layout heuristic doesn't override decided content | wrong (not a defect — locked content) | None taken; documented as precedent-consistent (matches #268/#273 tolerating line-length on specced content) |

## Needs the owner
Nothing. No removal, no credential gap, no unresolved decision.
