# Autopilot report — #295 Build claim reimbursement through Payments

- Map: #226 Map: Make the operator app work like Vic.ai (grammar, not skin)
- Type: wayfinder:task · Started: 2026-09-20 · Finished: 2026-09-20
- Outcome: partial (spec phase only — ticket continues across sessions)

## What I did
1. Claimed #295 (`gh issue edit 295 --add-assignee @me`).
2. Read the map (#226) low-res view and ticket #295's full scope.
3. Checked what earlier tickets already shipped: WorkspaceMember bank fields and the
   admin-editable Bank details panel (`components/admin/user-detail.tsx`,
   `app/.../admin/users/actions.ts`) already exist from #286; the `needs_bank_details`
   Bill Pay facet already exists from #294. This trims #295's scope significantly.
4. Read `models/bill-pay.ts`, `prisma/schema.prisma` (WorkspaceMember, PaymentRun,
   PaymentRunItem, InvoicePayment, PayerAccount, BillPayPreference, ExpenseClaim/Item),
   `lib/payments/za-eft-csv.ts`/`remittance.ts`, and `app/(app)/workspaces/[workspaceId]/account/page.tsx`
   to ground the spec in the real code (not the ticket's description alone) — found that
   `AccountPage` already is both the desktop profile panel and the phone Account tab, one
   component.
5. Sized the ticket (spec protocol step 6): the full scope as chartered exceeds 6 build steps
   / 10 states. Split at the schema/model-vs-rendered-surface boundary:
   - Kept on #295: schema (`ExpenseClaimPayment`, `PaymentRunItem.expenseClaimId`),
     `listBillPay` claim eligibility, `PaymentInstruction` for a claim, payment-write branching,
     and the one rendered surface with no queue/batch dependency — the self-service Bank
     details Panel on `AccountPage`.
   - Spawned [Build claim rows in Bill Pay, the Reimbursements group in Payment batches, and
     claim-surface copy (#331)](https://github.com/petrose99/Wes-doc/issues/331), blocked by
     #295, carrying the Bill Pay queue UI, batch pane, claim-surface copy, fortify states, the
     Payments primer doc, and the full bar.
6. Wired #331 as a sub-issue of map #226 and set its native blocking dependency on #295.
7. Posted a comment on #295 explaining the split and pointing at the scratch spec/preflight.
8. Wrote and filled `spec.md` and `preflight.md` (gate: PASS, predicted critique ≈34/40,
   evaluate ≈88, 0 P0/P1, Clean) for #295's trimmed scope.
9. Wrote the hand-off file with a 4-step + 3-gate build plan for future sessions.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Where does the trimmed ticket's scope boundary fall? | Model/schema layer stays on #295; rendered Bill Pay/batch/claim-surface UI spawns as #331 | Spec-phase sizing rule (protocol step 6): split at a boundary the map would recognise, never mid-surface |
| 2 | Does the self-service bank-details panel need a new page or reuse `AccountPage`? | Reuse `AccountPage` — it already serves both desktop profile panel and phone Account tab per its own doc-comment | Read the file directly; avoids a second surface for the same data |

## Decisions recorded
None closed this session — #295 is mid-ticket (spec phase of 4). No resolution comment yet;
the ticket stays open, assigned, with `milestone: spec-done`.

## Tickets created / changed
- Created [Build claim rows in Bill Pay, the Reimbursements group in Payment batches, and
  claim-surface copy (#331)](https://github.com/petrose99/Wes-doc/issues/331), `wayfinder:task`,
  sub-issue of map #226, blocked by #295.
- #295: claimed, commented with the sizing rationale; not closed (build/measure/close phases
  remain).

## Scores and counts (rendered surfaces only)
scores: predicted-critique=34 first-critique= close-critique= first-evaluate= close-evaluate=
Not yet built — pre-flight prediction only (preflight.md Part D/E). No captures taken this
session (spec phase does no browser work per the phase brief).

## First-pass findings → lessons (rendered surfaces only)
None yet — build has not started.

## Needs the owner
Nothing yet. No removal, no credential, no HITL blocker hit in this phase. Flagging for a
later phase: the pre-flight's B6 time-axis row notes the payment-file-export snapshot
("details captured at export time") must be verified against the real export code path at
build time, not assumed.

## Session 2 (build step 1 — schema migration)
- Claimed #295 (already assigned), read the hand-off, confirmed step 1 was `todo`.
- Added `ExpenseClaimPayment` model to `prisma/schema.prisma` (mirrors `InvoicePayment`) and
  `PaymentRunItem.expenseClaimId` (nullable, SetNull, unique `(workspaceId, expenseClaimId,
  active)`), plus the reverse relations on `ExpenseClaim`, `User`, `Workspace`, `PaymentRun`.
- `prisma migrate dev` refuses to run non-interactively in this environment; hand-wrote the
  migration SQL at `prisma/migrations/20260920010000_add_expense_claim_payments/migration.sql`
  (naming conventions copied from `20260916020000_add_payment_batches_payer_accounts_payment_records`)
  and applied it with `prisma migrate deploy` — applied clean on devdb.
- `prisma generate` fails on a pre-existing, unrelated issue: the `next-admin-generator-prisma`
  binary is not resolved by that generator's subprocess invocation on this box (`not found`).
  Worked around for this session by commenting out the `nextAdmin` generator block, running
  `generate`, then restoring the block — schema.prisma's generator list is unchanged on disk.
  Confirmed the client picked up the new model (`prisma/client/models/ExpenseClaimPayment.ts`).
- `tsc --noEmit`: 20 pre-existing errors, none touching prisma/schema.prisma, bill-pay, or
  payments files (dictation-actions.ts, document-processing.ts, automation-rules.ts,
  job-worker.ts, etc.) — present before this session's edits, not introduced by this change.
- Marked build step 1 `done` on the hand-off with the workarounds above noted for future
  sessions (the `next-admin-generator-prisma` PATH issue will recur on every `prisma generate`
  in this environment until fixed at the tooling level — out of scope for this ticket).
- Committed as `wip(autopilot): #295 build step 1` (f9dcad9). Ticket left open; steps 2–4 and
  the build-gate steps remain, each a fresh session per the phase brief.

## Session 3 (build step 2 — listBillPay claim rows)
- Read the hand-off, confirmed step 2 was `todo`, read spec.md §2 and `models/bill-pay.ts` in full.
- `models/bill-pay.ts`: discriminated `BillPayRow = BillPayBillRow | BillPayClaimRow` on `kind`.
  Renamed the previous flat row shape to `BillPayBillRow` (field-for-field, `BillRow` was already
  taken by `models/bills`'s export) and added `BillPayClaimRow` + `ClaimEligibility` per spec.
- Added `listClaimRows` (approved claims with a frozen total, member bank-details lookup,
  `ExpenseClaimPayment`/`PaymentRunItem.expenseClaimId` reads) and `claimEligibility`
  (`needs_bank_details` / `left_workspace` / `needs_currency`, all folding into the existing
  `needs_bank_details` `BillPayFacet` value per spec). Derived `paidState` (paid/scheduled/unpaid)
  mirrors the bill-row three-state shape, no partial state for claims.
- Updated the three existing consumers that assumed the old flat row shape so nothing already
  shipped breaks: `app/.../payments/bill-pay/page.tsx` (filters to `kind === "bill"` before the
  `.bill`/`.discount` reads), `components/payments/bill-pay-queue.tsx` and
  `components/payments/create-batch-dialog.tsx` (import `BillPayBillRow` aliased to `BillPayRow`),
  `models/payment-batches.ts` (`createBatches`/`markInvoicesPaid` filter to bill rows before
  `.bill.documentId`). The Bill Pay queue renders bills only until #331 adds claim-row UI.
- New test `models/bill-pay.claims.test.ts` (6 cases: eligible, needs_bank_details, left_workspace,
  needs_currency, paidState paid, paidState scheduled) — `npx vitest run
  models/bill-pay.claims.test.ts` → 6 passed.
- `tsc --noEmit`: same 20 pre-existing errors as step 1, none in files this step touched.
- Moved the per-step narrative off the hand-off (hook flagged it over the 80-line budget) into
  `docs/wayfinder-reports/226/logs/scratch-295/build-notes.md`; hand-off now points at it.
- Marked build step 2 `done`. Committed as `wip(autopilot): #295 build step 2` (e3a204d). Ticket
  left open; steps 3–4 and the build-gate steps remain, each a fresh session.

## Session 4 (build step 3 — PaymentInstruction + payment writes)
- Read the hand-off, confirmed step 3 was `todo`, read spec.md §3–4, `lib/payments/za-eft-csv.ts`,
  `lib/payments/remittance.ts`, and `models/payment-batches.ts`'s BATCH_SELECT/loadForDecision/
  markPaymentBatchPaid/markInvoicesPaid/removePaymentRecord/removePaymentRecordsForDocument/
  unmarkPaymentBatchPaid/listPaymentRecords by range.
- `lib/payments/za-eft-csv.ts`: `PaymentInstruction.documentId` made optional, added
  `expenseClaimId?` (mirrors `PaymentRunItem`); new `buildClaimPaymentInstruction()` per spec §3
  (beneficiary = claimant name/email, bank fields pass through unchanged, reference falls back
  title → submittedAt date → claim id prefix). `remittance.ts` needed no change — already generic
  over `PaymentInstruction.supplier`/currency.
- `models/payment-batches.ts`: `BATCH_SELECT`/`loadForDecision`/`unmarkPaymentBatchPaid` selects
  now fetch `expenseClaimId` too. `markPaymentBatchPaid` splits batch items bill/claim, writes
  `InvoicePayment`/`ExpenseClaimPayment` in the same transaction, audits both kinds separately.
  `unmarkPaymentBatchPaid` reverts both tables by `batchId`. Added claim counterparts of the manual
  functions (not requested by name in the hand-off's "touches" list but required by spec §4's "same
  call sites, branch on row kind" and needed for #331 to have something to call): `markClaimsPaid`,
  `removeClaimPaymentRecord`, `removePaymentRecordsForClaim`, `listClaimPaymentRecords`.
  `createPaymentBatch`/`instructionsFor` (bill-only batch creation/selection) intentionally left
  untouched — selecting claims into a batch is #331's UI work.
- New test `lib/payments/za-eft-csv.claims.test.ts` (3 cases: valid claim instruction builds and
  validates, reference fallback chain title→date→id, `validatePaymentInstructions` catches a claim
  with no bank account) — `npx vitest run lib/payments/za-eft-csv.claims.test.ts
  lib/payments/za-eft-csv.test.ts` → 11 passed. `npx vitest run lib/payments/
  models/bill-pay.claims.test.ts` → 32 passed, no regressions.
- `tsc --noEmit`: same 20 pre-existing errors as steps 1–2, none in files this step touched.
- Updated `build-notes.md` with the detail, marked build step 3 `done` and updated the hand-off's
  "Next step" to step 4 (self-service Bank details Panel). Committed as
  `wip(autopilot): #295 build step 3` (e02bba1). Ticket left open, still assigned; step 4 and the
  three build-gate steps remain, each a fresh session per the phase brief.

## Session 5 (build step 4 — self-service Bank details Panel)
- Read the hand-off, confirmed step 4 was `todo`, read spec.md §5, preflight.md Parts A–C,
  `app/(app)/workspaces/[workspaceId]/account/page.tsx`, `components/admin/user-detail.tsx`'s
  `BankField`/bank-draft/save logic, and `saveMemberBankDetailsAction`
  (`app/.../admin/users/actions.ts:184`)'s authorisation check.
- Confirmed `saveMemberBankDetailsAction` already authorises a caller acting on their own
  `userId` (`input.userId !== user.id && !isOwner`) and that the audit event
  (`workspace_member_bank_details_changed`) already fires in `setWorkspaceMemberBankDetails`
  (`models/workspaces.ts:518`) — no wrapper server action needed, per spec §5's "reuse the action
  if it already authorizes self" branch.
- New `components/account/bank-details-panel.tsx` (`BankDetailsPanel`, client component):
  view/edit/saving state machine copying the admin panel's `BankField` input, validation and
  error-copy verbatim; Remove uses `ConfirmDialog` directly (not `useAsyncConfirm`, whose focus
  fallback targets the admin Users pane's detail heading, not present here) with the B1
  consequence line naming that claim rows drop out of Bill Pay until re-added.
- Wired into `app/(app)/workspaces/[workspaceId]/account/page.tsx`'s new "Bank details" Panel,
  fed by `getWorkspaceMembership(workspaceId, user.id)`.
- `tsc --noEmit`: same 20 pre-existing errors as prior steps, none in the two touched files. No
  new test — UI wiring over the model layer's already-tested validation.
- Updated `build-notes.md` with the detail, marked build step 4 `done`, updated the hand-off's
  "Next step" to step G1 (round script). Committed as `wip(autopilot): #295 build step 4`
  (6006fe5). Ticket left open, still assigned; the three build-gate steps (G1–G3) remain, each a
  fresh session per the phase brief.
