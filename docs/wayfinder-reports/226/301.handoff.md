# Hand-off — Fold in the org/company vocabulary (#301)

milestone: build-in-progress

Full spec is #300's resolution comment (copy deck, Action Summary, Q&A) — read that, not this file, for wording rationale. This file is pointers only.

## Done (verified by reading the diffs, not yet by detector/critique)

1. **Rename organization** (item 1) — built:
   - `models/organizations.ts`: `renameOrganization(organizationId, name)`.
   - `app/(app)/workspaces/[workspaceId]/admin/companies/actions.ts`: `renameOrganizationAction`, gated on `getOrganizationMembership(...).role === "admin"`.
   - `app/(app)/workspaces/[workspaceId]/admin/companies/page.tsx`: fetches `getOrganizationMembership`, passes `viewerIsOrgAdmin` into `CompaniesPageState`.
   - `components/admin/companies-queue.tsx`: `OrganizationState` header `menu` now renders "Rename organization…" beside "Move a company into…" when `viewerIsOrgAdmin`; new `RenameOrganizationDialog` (Rename-company pattern: title `Rename ‹org›`, description consequence line, submit "Rename"); wired with toast `Renamed to ‹new› (was ‹old›)`.
   - Deleted "The organization's name can't be changed here yet." from the ungrouped-state disclosure.
   - Fortify not yet run as a skill pass — empty name blocked by `required`+trim+disabled submit; unchanged name is a harmless no-op write; concurrent rename is last-write-wins (documented in the action's comment). Revisit with `fortify` before close.

2. **Users (org mode)** (item 3) — done:
   - `components/admin/users-queue.tsx`: org column label "Org admin" → "Organization admin"; org-mode `bandLine` gained the standing sentence above the existing company-count line.
   - `components/admin/user-detail.tsx`: `Organization` Fact now shows "Organization admin"/"—" plus the same standing sentence as a sub-line.

3. **No second remove-from-organization surface** (item 4) — verified by grep: "Remove from" + organization text exists only in `companies-queue.tsx:271`; `user-detail.tsx`'s two "Remove from" hits are company-scoped, not organization-scoped. No code change needed.

4. **Partial "workspace" sweep** (item 2) — done so far:
   - `components/admin/companies-queue.tsx`: "Create a team workspace" → "Create a company" (button, dialog title, submit); "move other team workspaces you own into it" → "move other companies you own into it"; "Team workspaces you own that aren't in an organization yet." → "Companies you own that aren't in an organization yet." (dialog description + legend); "It becomes an ungrouped team workspace." → "It becomes a company on its own."
   - `components/admin/company-name-form.tsx:35`: personal-branch title "Workspace name" → "Name".
   - `components/workspace/module-row.tsx`: "Request sent to the workspace owner" → "Request sent to the owner".
   - Confirmed `components/workspace/members-table.tsx`, `danger-zone.tsx` are **not routed** anywhere (grep for their import paths under `app/` returns nothing) — leave them for the #286 unplug list, per the ticket's own carve-out. `module-row.tsx` **is** routed (`admin/configuration/whats-on/page.tsx`) so it was swept.

## Not yet done — item 2's remaining explicit list (from #300's resolution comment)

Still to change (verify each is still current at the given path before editing — line numbers in #300's comment may have drifted; the full list is on #300):
- `components/admin/company-danger-actions.tsx` "drops this workspace to SMB mode" → "drops this company to SMB mode"
- `components/auth/signup-form.tsx` "Create your workspace" → "Create your account"
- `app/(auth)/signup/page.tsx` "Join the workspace" → "Join ‹company›" (name from invitation) or "Accept your invitation"
- `app/invite/[token]/page.tsx` "You have been invited to a workspace" → "You've been invited to ‹company›"; "Ask the workspace owner to send a new one." → "Ask an owner of ‹company› to send a new one."
- Queue/pane strings ("this workspace" → "this company" where company-only: Admin/Payments/Close/Integrations; else drop the noun; "workspace owner" → "owner"): `payments/actions.tsx`, `settings/payments/actions.ts`, `action-helpers.ts` (x2), `close/lock-confirm-button.tsx`, `document-actions.tsx`, `extract-panel.tsx`, `line-items-section.tsx`, `automation-config-form.tsx`, `admin/configuration/page.tsx`, `admin/configuration/intake/page.tsx` (x2), `history-tabs.tsx` ("Workspace check" → "Company check"), `workspace-pulse.tsx`, `how-it-works.tsx`, `document-list.tsx`, `sheet-actions.ts` (x4), `actions.ts` (x2), `account/page.tsx` (x2), `account/security/page.tsx`, `workspace-actions.ts` (x4 error strings), `workspaces/new/actions.ts`, `api/integrations/xero|quickbooks/*`.
- Re-run `docs/wayfinder-reports/226/logs/scratch-300/ws-scan.mjs` at close: residue must be marketing prose (`components/marketing/**`), code comments, and the unrouted `members-table.tsx`/`danger-zone.tsx` only.

## Not started at all
- Close bar: `articulate`/`organize`/`fortify`/`include` (Intent) and `clarify`/`layout`/`harden`/`polish` (Impeccable) skill passes.
- In-page detector before/after at 1440 and 390 on Companies (all three states, pane open) and Users (org mode, pane open).
- `critique` ≥ 32/40 no heuristic < 3; `evaluate` health ≥ 85, 0 P0/P1, Clean.
- Keyboard flow of the new "Rename organization…" menu item + dialog, verified live at both widths.
- `npm run typecheck`/`tsc` and any existing companies-actions tests have **not** been run since these edits — run before continuing (`lib/admin/companies-actions.test.ts` exists and may need a case for `renameOrganizationAction`, or at least confirm it still passes).

## Exact next step
1. Run `npx tsc --noEmit` (or the project's typecheck script) to confirm the new prop threading (`viewerIsOrgAdmin`) and dialog compile cleanly.
2. Finish item 2's remaining string list above (mechanical greps + edits), then re-run `ws-scan.mjs` and confirm only the allowed residue remains.
3. Start the dev server + seed (recipe on #225's hand-off / `docs/agents/design-tickets.md`), verify Rename organization live (empty name, unchanged name), then run the close-bar skills and detector/critique/evaluate passes named above.
4. Close #301 with the resolution comment once every item in its "Scope" list and the close bar are met; append one line to map #226's Decisions so far.
