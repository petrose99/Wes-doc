# Autopilot report — #262 Build the keyboard shortcut set: roving rows, g-sequences between queues, f / ? and the Keyboard shortcuts dialog

- Map: #226 DocuBite's authenticated app works the way Vic.ai's does
- Type: wayfinder:task (execution) · Started: 2026-09-16 (spec phase) · Finished: 2026-09-16 (spec phase)
- Outcome: partial — phase 1 of 4 (spec) done; build, measure, close follow in fresh sessions

## What I did
1. Claimed #262; loaded the map, #240's resolution, the #245 and #250 inputs on the ticket.
2. Read both lessons files, the queue-shell and detail-pane primers, `preflight.md`, craft-floor; `intent` (specify,
   fortify, articulate, include) and `impeccable` (shape, clarify; `impeccable context` launcher not run — needs an
   interactive approval, project lesson).
3. Inspected the incumbent: `queue-screen.tsx` (document-wide ↑/↓/Esc, row buttons, guard selector), `sidebar.tsx`
   (rail item list, compact `title`), `account-menu.tsx`, `ui/dialog.tsx` (`data-inner`, openerRef), the workspace layout
   skip link, `search/page.tsx` (exists, off-rail), `po-compare.tsx` (≠ glyph buttons).
4. Wrote `logs/scratch-262/spec.md` and filled `preflight.md` Parts A–E.
5. Ran the Part D spec critic (sonnet, fresh context): 2 P1s found, both fixed in the spec; gate met.
6. Wrote the hand-off (`262.handoff.md`, `milestone: spec-done`), this report; committed `wip(autopilot): #262 spec`.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Register `/` search now, though Search is not on the rail until #270? | Yes → `/search` (route exists); #270 re-points | Ticket comment from #245 says `/` joins the set; smallest reversible option |
| 2 | Add the #250 next/prev-mismatch key? | Yes, `]`/`[`, focus only, scoped to invoice panes with a View PO row | #250 input on this ticket; H7 is the ticket's heuristic; no key acts on data |
| 3 | Where does the off switch live vs. the "mirrored menu item"? | One switch in the dialog; the menu item opens the dialog and shows "Off" | CONTEXT.md glossary: "turned off per person in one switch" |
| 4 | How to make ≤ 3 Tabs to the first row? | Second skip link "Skip to the list" (hidden off-queue) | Ticket DoD 4; rail has 14 stops before `#main` |
| 5 | Which queue does `g y` / `g a` land on? | The rail item's own href (Bill Pay, Approvals › Invoices), named in the dialog | Critic P1 (H4); "register from the rail's item list" |
| 6 | Row checkboxes in the tab sequence? | `tabIndex=-1`, reached with `←` from the row button | One tab stop per table (roving tabindex practice); ticket keeps Space-on-checkbox |
| 7 | Spec critic model? | sonnet | Brief: opus only for money/approval/schema/auth |

## Decisions recorded
Spec phase only — no resolution comment yet; the ticket stays open and assigned. Decisions above are in the hand-off.

## Tickets created / changed
None. No fog graduated (execution ticket).

## Scores and counts (rendered surfaces only)
scores: predicted-critique=32 first-critique= close-critique= first-evaluate= close-evaluate=
Predicted evaluate ≈ 90, 0 P1, Clean (pre-flight Part D reconciled). Not measured — surface not built.

## First-pass findings → lessons (rendered surfaces only)
Not applicable yet (no first pass). Critic P1s before code: `g y` landing ambiguous (H4) · `]`/`[` listed as universal
(H2/H9) — both were spec-text gaps of the class "one key, one named landing; every scoped key says its scope".

## Needs the owner
Nothing in this phase. No removals.
