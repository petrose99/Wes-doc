# Autopilot report — #262 Build the keyboard shortcut set: roving rows, g-sequences between queues, f / ? and the Keyboard shortcuts dialog

- Map: #226 Map: Make the operator app work like Vic.ai (grammar, not skin)
- Type: wayfinder:task (execution) · Started: 2026-09-16 ~18:10 UTC (spec session) · Finished: 2026-09-16 (close-out session, 5 of 5)
- Outcome: resolved

## What I did

The ticket ran over five sessions (spec → build → measure → close fix-batch → close-out). This report covers the whole ticket; each session's detail is on the ticket's `Autopilot: continue —` comments and in `262.handoff.md`.

1. **Spec** (`fce2b44`): `intent specify` + `fortify` + `articulate` + `include`; `impeccable shape/layout/typeset/clarify`; both lessons files read; spec at `logs/scratch-262/spec.md`, pre-flight A–E at `logs/scratch-262/preflight.md`. Spec critic (sonnet) found 2 P1s in the spec — `g y` landing ambiguous, `]`/`[` listed as universal — both fixed in the spec before code. Gate met: predicted critique 32/40, evaluate ≈90, 0 P1, Clean.
2. **Build** (`5207891`): store, listener host + dialog, `Switch` primitive, account-menu item, rail tooltips, skip-to-list link, roving-tabindex rows, search arrival focus. `tsc` clean. The build-phase r0 capture gate was not reached inside the context line and was handed to measure.
3. **Measure** (`95e06b7`): host-side `next dev` + impeccable live-server; round script `logs/scratch-257/pw/round262.mjs` on `capture-round.mjs`; `shots-r1` with detector + keyboard probes; three sonnet readers. First pass: critique 36/40, evaluate 84 / Clean / 2 P1, include FAIL.
4. **Close fix batch** (`3afcdc5`): the two P1s (Admin `g d` focus → `#main`; dialog Esc focus-return `body` → `#main`), the real P2 (`/search` redirected empty `q` home), and the round-script probe gap (skip-to-list probe). Confirm round `shots-r5`: critique 38/40, evaluate 88 / Clean / 0 P1, include PASS. vitest 2693/2693, `tsc`, eslint (0 errors in touched files), `next build` green with the dev server stopped. Primer `docs/agents/areas/queue-shell.md` refreshed; lessons appended.
5. **Close-out** (this session): re-ran `tsc --noEmit` (clean, no code change since `3afcdc5`); posted the [resolution comment](https://github.com/petrose99/Wes-doc/issues/262#issuecomment-5704146193); closed #262; appended the Decisions-so-far line to #226; created and wired [#293](https://github.com/petrose99/Wes-doc/issues/293) for the B4 Switch-reuse decision; wrote this report.

## Questions I answered on the owner's behalf

| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Is the B4 Switch-reuse gap (Override-mode item, module pill) execution to finish here or a decision? | Decision → ticket #293, not fixed under #262 | CLAUDE.md "findings split two ways": it changes how an existing row renders; #262's score does not depend on it |
| 2 | Are the 9 extra detector findings on `mismatch-nav-pane-open` #262's to fix? | No — pre-existing `po-compare.tsx` (#250) properties; `]`/`[` adds no DOM | Detector diff against the #250 baseline; polish item, no decision behind it |
| 3 | Was the `/` search failure a timing flake (as the measure round suspected)? | Real bug — fixed (`search/page.tsx` redirect on empty `q`) | Route file read; the shortcut deliberately arrives with no query |
| 4 | Re-run the full suite/lint/build again in this close-out session? | Only `tsc` re-run | HEAD is unchanged since the close session ran all five checks green; a repeat would be cost without information |

## Decisions recorded

Shipped the #240 shortcut set on the shell, desktop only: `g` + letter to the eight rail destinations with arrival focus on a row (or `#main` on Admin), `/` `f` `?` `]` `[`, roving-tabindex rows, a "Skip to the list" link that cuts the Tab count to the first row from 28 to 3, a per-user off switch in localStorage, no key acts on data. Dialog focus-return gained a `#main` fallback for `body` openers. Search no longer bounces an empty query home.

## Tickets created / changed

- #262 closed with resolution comment.
- #226 Decisions so far: one line appended for #262.
- **New:** [#293 Decide whether the Override-mode and module-pill toggles adopt the shared Switch primitive (B4, from #262)](https://github.com/petrose99/Wes-doc/issues/293) — `wayfinder:grilling`, sub-issue of #226, unblocked (frontier). No fog graduated; no removals.

## Scores and counts (rendered surfaces only)

scores: predicted-critique=32 first-critique=36 close-critique=38 first-evaluate=84 close-evaluate=88

- evaluate: first pass health 84, Clean, P0 0 · P1 2 · P2 2 · P3 2 → close health 88, Clean, P0 0 · P1 0 · P2 1 · P3 1.
- critique first pass 36/40 (H1 3, H2 4, H3 3, H4 4, H5 4, H6 4, H7 3, H8 4, H9 3, H10 4 — reader used a 10×4 table) → close 38/40, no heuristic under 3.
- include: first FAIL (3/5 behaviours failed or unverified) → close PASS, 0 failures; keyboard flow verified live at 1440 (`keyboard.json`, 20+ probe entries: skip → skip → Enter lands on a row; every `g` jump lands with `activeElement !== body`; `?`/Esc round-trip returns to `#main`; off-switch no-ops letters and keeps arrows; `/` and `f` land; `]`/`[` cycle and wrap) and at 390 (listener not attached, cards untouched).
- In-page detector (r5, close) per state: `invoices-list` 1440 5 · 390 5; `g-p/b/e/a/y/d-jump` 5 each; `g-r-jump` 6 (+1 `em-dash-overuse` in Receipts seed copy, pre-existing); `dialog-open` 5; `mismatch-nav-pane-open` 14 (5 residue + 9 pre-existing `po-compare` clipped-overflow/tiny-text/cramped-padding from #250). Residue = the app-wide four (`ai-color-palette`, `overused-font`, `layout-transition`, `dark-glow`) named in the queue-shell primer. First pass (r1) counts were identical — #262 adds no DOM to any captured state except the dialog, which sits at residue.
- Weak pre-build step: **Part B row 5 (focus/keys/failure path per interactive surface)** was filled optimistically for two destinations — Admin (no row-list consumer) and the dialog opened from `body` — and the `/` route's existing server guard was never read (Part B row 1, reachability). Both P1s and the P2 trace to those rows.

## First-pass findings → lessons (rendered surfaces only)

| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| `g d` Admin leaves focus on `body` (H1/H7, P1) | Part B row 5 — focus per destination; spec named "Admin → `#main`" but no consumer existed | unchecked | Fixed; project lesson with `check:` (keyboard probe per destination asserts `activeElement !== body`) |
| Dialog Esc returns focus to `body` (H3/H7, P1) | Part B row 4 — shell-primitive reuse; the shared `Dialog` had no `body` fallback | none | Fixed in `dialog.tsx` centrally; project lesson with `check:` (`*-esc-focus-return` resolving to `"body"`) |
| `/` lands on `body`, URL empty (H4, P2) | Part B row 1 — reachability; target route's `redirect()` guard never audited against the shortcut's arrival shape | none | Fixed in `search/page.tsx`; project lesson with `check:` (target `page.tsx` has no `redirect(` on the param state the caller sends) |
| Tab-count-to-first-row not demonstrated (H7, P2) | Spec §10 live-probe line existed; the round script probed a raw Tab walk instead | unchecked | Probe rewritten (Tab·Tab·Enter); no product change |
| Six `g` jumps read as broken on the cold-server pass (probe, not product) | none — capture recipe | new class | Generic + project lesson with `check:` (round script with no pre-warm on a just-started server) |

Lessons committed in `3afcdc5`: four lines in `.claude/wayfinder-autopilot/lessons.md`, one in `scripts/wayfinder-autopilot/lessons.md`; both files under the ~80-line cap.

## Needs the owner

- Nothing blocking. #293 (Switch primitive on the two existing toggles) is a frontier decision, not a sign-off.
- Polish, no ticket: the 9 pre-existing `po-compare.tsx` detector findings visible when an invoice pane with a View PO row is open (#250 residue) — pure execution, hand to a polish pass.
- Unit-test gap, accepted: `lib/shell/keyboard-shortcuts.test.ts` covers only the SSR path (no jsdom in the repo); client behaviour is verified by the live probes.
