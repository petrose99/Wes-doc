milestone: spec-done

#347 (map #226): mixed bill/claim batching on Payment Batches. Spec + reconciled preflight (critic
33/40, evaluate ~88, Clean) at `docs/wayfinder-reports/226/logs/scratch-347/spec.md` and
`preflight.md`. Blocked-by #331 is closed; unblocked.

## Artifacts
- `docs/wayfinder-reports/226/logs/scratch-347/spec.md` — full spec
- `docs/wayfinder-reports/226/logs/scratch-347/preflight.md` — Parts A–F, critic reconciliation

## In progress — plan step 1 (shared primitives), NOT committed as its own wip yet
Code written this session, uncommitted at hand-off (`git status`):
- `models/bill-pay.ts` — added exported `rowId`/`rowPayeeName`/`rowDue`/`rowAmount`/`rowScheduled`
  (moved from `bill-pay-queue.tsx`, was private) + new `rowCurrency`/`rowPayFrom`.
- `components/payments/bill-pay-queue.tsx` — imports the moved accessors instead of defining them
  locally (only the 5 already-used ones; `rowCurrency`/`rowPayFrom` are for step 5's dialog wiring,
  not imported here yet).
- `lib/payments/batch-counts.ts` (new) — `describeBatchCounts(billCount, claimCount)`.
- `lib/payments/remittance.ts` — `RemittanceAdvice`/`renderRemittanceText` gained a `kind: "bill" |
  "claim"` field/param (derived from `PaymentInstruction.expenseClaimId`), swapping "invoices" for
  "reimbursement claim" in the claim branch (spec §2 finding, patched at spec time).

**Not yet done / not verified:** `models/payment-runs.ts` and `lib/payments/remittance.test.ts`
also reference `buildRemittanceAdvices`/`RemittanceAdvice` (found via grep, not yet read) — the
new `kind` field may need a call-site update there or in a test literal. `tsc --noEmit` and the
affected vitest files have NOT been run this session — do that first in the next session before
writing more code, to catch this before it compounds.

## step: build plan (one per build session)
step: 1 — Shared primitives (bill-pay.ts accessors, batch-counts.ts, remittance.ts kind param) — spec §4, §2 — in-progress, resume here: verify models/payment-runs.ts + remittance.test.ts against the new `kind` field, then `tsc --noEmit` + `npx vitest run lib/payments/remittance.test.ts models/bill-pay.test.ts` (if it exists) before moving on
step: 2 — models/payment-batches.ts createPaymentBatches mixed selection + PaymentRunItem + leftOut + audit — spec §1a — todo
step: 3 — models/payment-batches.ts toRow/claimCount + BatchLine/getPaymentBatch claim facts + instructionsFor claimant bank details — spec §1b–1d — todo
step: 4 — components/payments/batch-detail.tsx Reimbursements group + header count — spec §2 — todo
step: 5 — components/payments/create-batch-dialog.tsx claim branch + bill-pay-queue.tsx wiring (button, dialog call, stale comments) — spec §3, §5 — todo
step: 6 — components/payments/batch-queue.tsx Lines column/claimCount/confirm strings + docs/agents/areas/payments.md — spec §6, §7 — todo
step: G1 — round script (extend round296.mjs states, capture 1440/390) — todo
step: G2 — r0 gate + fix batch — todo
step: G3 — build-done (tsc, affected vitest, eslint changed files) — todo

## Next step
Finish plan step 1: check `models/payment-runs.ts` and `lib/payments/remittance.test.ts` for
`RemittanceAdvice`/`buildRemittanceAdvices` call sites broken by the new `kind` field, fix, run
`tsc --noEmit` and the affected tests, then commit `wip(autopilot): #347 build step 1` before
starting step 2.
