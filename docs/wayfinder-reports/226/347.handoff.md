milestone: spec-done
milestone: build-step-1-3
milestone: build-step-4
milestone: build-step-5

#347 (map #226): mixed bill/claim batching on Payment Batches. Spec + reconciled preflight (critic
33/40, evaluate ~88, Clean) at `docs/wayfinder-reports/226/logs/scratch-347/spec.md` and
`preflight.md`.

## Artifacts
- `docs/wayfinder-reports/226/logs/scratch-347/spec.md` — full spec
- `docs/wayfinder-reports/226/logs/scratch-347/preflight.md` — Parts A–F, critic reconciliation

## Done this session (step 4)
- `components/payments/batch-detail.tsx` — header count uses `describeBatchCounts`; Lines tab
  renders a `<section aria-label="Reimbursements">` after the supplier sections (never
  interleaved) when `detail.reimbursements` is non-null: Claimant (linking to
  `/workspaces/{id}/approvals/expense-claims?claim={claimId}` via `withOrigin`, same pattern as
  `bill-pay-queue.tsx:210`) · Approved (`formatPaymentDate(line.approvedAt)`) · Amount; claimant
  name from `line.supplier`, secondary label from `line.reference`. Empty-state check now
  `suppliers.length === 0 && !reimbursements`. `tsc --noEmit` clean (no vitest for this file).

## Done this session (step 5)
- `create-batch-dialog.tsx` — `rows: BillPayRow[]`, new `defaultPayerAccount` prop; every
  `row.bill.*`/`.payFrom`/`.amountToPay` swapped for `rowId`/`rowPayeeName`/`rowAmount`/
  `rowCurrency`/`rowPayFrom` off `models/bill-pay.ts`. `bySupplier` built from `billRows` only
  (unchanged table); new claim `Reimbursements` group (Claimant/Approved/Amount/Remove) appended
  after it. Ineligible list and footer (`describeBatchCounts`) kind-agnostic.
- `bill-pay-queue.tsx` — dropped the two stale "batching claim rows isn't wired" comments; bulk
  bar `eligible` count no longer bill-only; claim pane gets a `Create batch` button (unpaid +
  eligible, mirrors the bill button); `CreateBatchDialog` call passes the full `selectedRows`
  and `defaultPayerAccount`.
- `tsc --noEmit` clean; `vitest models/payment-batches.test.ts lib/payments` 31/31 pass.

## Done previous session (steps 2–3; step 1 was already committed prior session)
- `models/payment-batches.ts` — `createPaymentBatches` now takes the full `BillPayRow` union
  (bills + claims), builds a normalized `Candidate` list, writes `expenseClaimId`/claim supplier/
  reference/amount per line, `expense_claim.batched` audit, `claimCount` on the result.
  `PaymentBatchRow`/`toRow` gained `claimCount` (narrowed `billCount` to doc-only). `BatchLine`
  gained `claimId`/`approvedAt`. `getPaymentBatch` splits lines into `suppliers` (bill-only,
  unchanged) + new `reimbursements` group (claims, appended after, never interleaved) via a new
  `claimFactsFor` helper (mirrors `dueDatesFor`). `instructionsFor` resolves claimant bank details
  via `workspaceMember` lookup (same fields `claimPaymentEligibility` reads) alongside supplier
  lookup.
- `lib/payments/batch-split.ts` — `BatchLineInput` dropped the unused `documentId` field.
- `models/payment-batches.test.ts` — passes unchanged (5/5); `tsc --noEmit` clean.

## step: build plan (one per build session)
step: 1 — Shared primitives (bill-pay.ts accessors, batch-counts.ts, remittance.ts kind param) — spec §4, §2 — done
step: 2 — models/payment-batches.ts createPaymentBatches mixed selection + PaymentRunItem + leftOut + audit — spec §1a — done
step: 3 — models/payment-batches.ts toRow/claimCount + BatchLine/getPaymentBatch claim facts + instructionsFor claimant bank details — spec §1b–1d — done
step: 4 — components/payments/batch-detail.tsx Reimbursements group + header count — spec §2 — done
step: 5 — components/payments/create-batch-dialog.tsx claim branch + bill-pay-queue.tsx wiring (button, dialog call, stale comments) — spec §3, §5 — done
step: 6 — components/payments/batch-queue.tsx Lines column/claimCount/confirm strings + docs/agents/areas/payments.md — spec §6, §7 — todo
step: G1 — round script (extend round296.mjs states, capture 1440/390) — todo
step: G2 — r0 gate + fix batch — todo
step: G3 — build-done (tsc, affected vitest, eslint changed files) — todo

## Next step
Build step 6: `components/payments/batch-queue.tsx` "Bills" header → "Lines", cell/rowName/confirm
strings → `describeBatchCounts` + `docs/agents/areas/payments.md` — spec §6, §7.
