milestone: spec-done
milestone: build-step-1-3
milestone: build-step-4
milestone: build-step-5
milestone: build-step-6

#347 (map #226): mixed bill/claim batching on Payment Batches. Spec + reconciled preflight (critic
33/40, evaluate ~88, Clean) at `docs/wayfinder-reports/226/logs/scratch-347/spec.md` and
`preflight.md`. Build steps 1-6 (all model/component code) are committed
(0b00a70..4193a09..61f8721) — see `git log --format='%h %s%n%b' -- components/payments
models/payment-batches.ts models/bill-pay.ts lib/payments`.

## Artifacts
- `docs/wayfinder-reports/226/logs/scratch-347/spec.md` — full spec
- `docs/wayfinder-reports/226/logs/scratch-347/preflight.md` — Parts A–F, critic reconciliation
- `docs/wayfinder-reports/226/logs/scratch-347/round347.mjs` — G1 round script (written this
  session, NOT yet run clean end-to-end — see Open findings)
- `docs/wayfinder-reports/226/logs/scratch-347/seed347.mjs` — seeds one more eligible/unpaid claim
  (00000000-0000-0000-0000-000000034701, Bongani Nkosi, ZAR 620) for the claim-only state; run it
  once (`npx tsx seed347.mjs`) — idempotent upsert, safe to re-run.
- `docs/wayfinder-reports/226/logs/scratch-347/residue.txt` — copied verbatim from
  `docs/agents/areas/payments.md`'s `## Detector residue` block.

## step: build plan (one per build session)
step: 1 — Shared primitives (bill-pay.ts accessors, batch-counts.ts, remittance.ts kind param) — spec §4, §2 — done
step: 2 — models/payment-batches.ts createPaymentBatches mixed selection + PaymentRunItem + leftOut + audit — spec §1a — done
step: 3 — models/payment-batches.ts toRow/claimCount + BatchLine/getPaymentBatch claim facts + instructionsFor claimant bank details — spec §1b–1d — done
step: 4 — components/payments/batch-detail.tsx Reimbursements group + header count — spec §2 — done
step: 5 — components/payments/create-batch-dialog.tsx claim branch + bill-pay-queue.tsx wiring (button, dialog call, stale comments) — spec §3, §5 — done
step: 6 — components/payments/batch-queue.tsx Lines column/claimCount/confirm strings + docs/agents/areas/payments.md — spec §6, §7 — done
step: G1 — round script (extend round296.mjs states, capture 1440/390) — todo
step: G2 — r0 gate + fix batch — todo
step: G3 — build-done (tsc, affected vitest, eslint changed files) — todo

## Open findings
`round347.mjs` state 1 (`01-billpay-claim-row-button`, `/payments/bill-pay`) errors: the dev
server 500s on `/workspaces/{ws}/payments/bill-pay` with `ENOENT ... .next/dev/server/app/(app)/
workspaces/[workspaceId]/(queue)/payments/bill-pay/page/build-manifest.json` — a Turbopack
build-manifest race, not a code error (confirmed: `/payments/batches` on the same server compiles
and loads clean via `debug2.mjs`; `tsc` was not yet run this session to rule out a real type
error in the bill-pay page tree touched by steps 4-6). Reproduced across two full `rm -rf .next`
+ `dev.mjs start` cycles. Next session: run `npx tsc --noEmit` first to rule out a real error in
`bill-pay-queue.tsx`/`create-batch-dialog.tsx` before treating this as a Turbopack flake; if tsc
is clean, try a longer settle after first hit (Turbopack sometimes needs ~45s on a cold first
compile of a page with this many dependents) or `next dev` (webpack) as a one-off diagnostic.
Query scripts in this scratch folder (`query.mjs`..`query7.mjs`) already establish the fixture
data: mixed-batch pair is claim `00000000-0000-0000-0000-000000033101` (Thandiwe Mokoena) + bill
`fe267644-c4e9-417f-91ce-c6474c8869a4` (Cape Office Supply); claim-only fixture is `...034701`
(Bongani Nkosi, needs `seed347.mjs` run once per fresh devdb).

## Next step
Stop/restart the dev server, run `npx tsc --noEmit` to rule out a real compile error touching
`components/payments/bill-pay-queue.tsx` or `create-batch-dialog.tsx`; once the page loads clean,
run `seed347.mjs`, then finish building step G1 (`node round347.mjs r0 <only-state>` per state,
then a full `node round347.mjs r0` pass) before moving to G2.
