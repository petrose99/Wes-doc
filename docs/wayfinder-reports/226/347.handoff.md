milestone: spec-done
milestone: build-step-1-3
milestone: build-step-4
milestone: build-step-5
milestone: build-step-6

#347 (map #226): mixed bill/claim batching on Payment Batches. Spec + reconciled preflight (critic
33/40, evaluate ~88, Clean) at `docs/wayfinder-reports/226/logs/scratch-347/spec.md` and
`preflight.md`. Build steps 1-6 (all model/component code) are committed
(0b00a70..4193a09..61f8721) — see `git log --format='%h %s%n%b' -- components/payments
models/payment-batches.ts models/bill-pay.ts lib/payments`.

## Artifacts
- `docs/wayfinder-reports/226/logs/scratch-347/spec.md` — full spec
- `docs/wayfinder-reports/226/logs/scratch-347/preflight.md` — Parts A–F, critic reconciliation
- `docs/wayfinder-reports/226/logs/scratch-347/round347.mjs` — G1 round script, now runs clean
  end-to-end (14 states × 1440/390, no errors) after this session's fixes: rail hover intercepts
  the leftmost checkbox column at desktop widths (added `mouseOff` before every check, per
  round-273/281 precedent); the dialog's submit button reads "Create batch (N)" not "Create batch"
  (scoped the click to `[role=dialog] >> getByRole('button', /^create batch/i)`); "Bongani Nkosi"
  collides with an unrelated pre-existing fixture claim ("Client gift", 033106, already batched) —
  every claim-only selector now scopes to `tr:has-text("Stationery run")`; the receipt→pane link
  needs `page.waitForURL(/\/payments\/batches\//)` (first hit compiles the route, was flaking on a
  fixed settle).
- `docs/wayfinder-reports/226/logs/scratch-347/seed347.mjs` — seeds one more eligible/unpaid claim
  (00000000-0000-0000-0000-000000034701, Bongani Nkosi, "Stationery run (#347 gate)", ZAR 620) for
  the claim-only state; run it once (`npx tsx seed347.mjs`) — idempotent upsert, safe to re-run.
- `docs/wayfinder-reports/226/logs/scratch-347/residue.txt` — copied verbatim from
  `docs/agents/areas/payments.md`'s `## Detector residue` block.
- `docs/wayfinder-reports/226/logs/scratch-347/shots-r0/` — current capture (detector.json,
  keyboard.json, PNGs) from the round script above.

## step: build plan (one per build session)
step: 1 — Shared primitives (bill-pay.ts accessors, batch-counts.ts, remittance.ts kind param) — spec §4, §2 — done
step: 2 — models/payment-batches.ts createPaymentBatches mixed selection + PaymentRunItem + leftOut + audit — spec §1a — done
step: 3 — models/payment-batches.ts toRow/claimCount + BatchLine/getPaymentBatch claim facts + instructionsFor claimant bank details — spec §1b–1d — done
step: 4 — components/payments/batch-detail.tsx Reimbursements group + header count — spec §2 — done
step: 5 — components/payments/create-batch-dialog.tsx claim branch + bill-pay-queue.tsx wiring (button, dialog call, stale comments) — spec §3, §5 — done
step: 6 — components/payments/batch-queue.tsx Lines column/claimCount/confirm strings + docs/agents/areas/payments.md — spec §6, §7 — done
step: G1 — round script (extend round296.mjs states, capture 1440/390) — done
step: G2 — r0 gate + fix batch — todo
step: G3 — build-done (tsc, affected vitest, eslint changed files) — todo

## Open findings
`node scripts/wayfinder-autopilot/gate.mjs docs/wayfinder-reports/226/logs/scratch-347/shots-r0
--residue-file residue.txt` on the current `shots-r0` (this session's, clean capture, 0 errored
states): FAIL — 7 real findings, all `text-overflow — span.truncate overflows its box by 38px`,
only at 1440, on **every** state including the untouched Bill Pay list (state 01) — this is
`components/queue/connection-band.tsx` (the "OPEN INVOICES BY AGE" bar), not `components/payments`,
and the same finding is present in `scratch-342/shots-r0` and `shots-r1` (pre-existing, unrelated
to #347's diff). G2 should confirm this with a quick `git blame`/before-shot check and, if
confirmed pre-existing and out of components/payments, add it to this ticket's residue rather than
fixing an unowned shared component — or fix it in place if G2 finds it's small and self-contained.

4 keyboard-probe failures, all `pane-*-sections`: `pane-mixed-sections-{1440,390}` and
`pane-claim-only-{1440,390}`. Visually inspected `shots-r0/03-receipt-and-pane-mixed-pane-1440.png`
and `04-claim-only-dialog-and-pane-pane-{1440,390}.png` by eye — **all render correctly** (Cape
Office Supply bill group, then Reimbursements section with Thandiwe Mokoena / Bongani Nkosi,
exactly as spec'd; no stray "no lines" message). The probe itself is wrong: it reads
`page.locator("main").textContent()`, but the pane's content is not inside a `<main>` element (grep
`components/payments/batch-detail.tsx` and the batches page shell for the real container — likely
needs the pane's own root selector, e.g. `[data-testid]` or the panel's aria-label/role). Fix the
probe's selector in round347.mjs (not the app — the app is correct), re-run states 03 and 04 only,
confirm probes pass, then re-run the full round once clean.

## Next step
G2, continued: (1) fix the `pane-*-sections` probe's `main` selector in round347.mjs states 03/04,
re-run just those two states, confirm the 4 probes pass; (2) resolve the text-overflow finding
(residue carve-out with a one-line justification, or a small fix, per the note above); (3) re-run
the full round as `shots-r0` (or a fresh tag) and gate again until clean; (4) proceed to G3
(`tsc --noEmit`, affected vitest, eslint on changed files) once G2 is clean. Servers were left
running this session (dev on :3000, live-server on :8400) — stop them
(`node .impeccable/live/dev.mjs stop`; the live-server PID is in its own JSON output) before
starting new work if they're still up, or reuse them if still healthy.
