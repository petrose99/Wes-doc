# Hand-off — #296 Reject on Approved batches + tightened self-approval note

milestone: spec-done
milestone: build-step-3-done
milestone: build-gate-g1-done
milestone: build-gate-g2-done
milestone: build-done

Map #226. Ground truth: #278 resolution comment. Spec + filled preflight (gate met, no
split needed — 3 build steps, ≤5 states, well under the 6-step/10-state sizing limit):

- Spec: `docs/wayfinder-reports/226/logs/scratch-296/spec.md`
- Pre-flight (Parts A–E, critic reconciled): `docs/wayfinder-reports/226/logs/scratch-296/preflight.md`
- Critic full write-up: `docs/wayfinder-reports/226/logs/scratch-296/critic.md`

Gate: critique reconciled 33/40 (H1=4, H5=4, rest 3), evaluate predicted 0 P0/P1 sum 0,
verdict Clean. No split — ships as one ticket.

## Build plan (one step per session; each gate step per `phases/build.md`)

step: 1 — model: rejectPaymentBatch accepts approved + fromStatus/exportedAt audit detail — done
  - spec §1, §6 (fromStatus consumer decided: `describeAuditDetail` appends "(from Approved)")
  - files: models/payment-batches.ts (loadForDecision select, rejectPaymentBatch status check + detail), models/payment-batches.test.ts (new file: 5 cases — approved→rejected + fromStatus, pending→rejected + fromStatus, exportedAt carried, paid→throws no writes, reason required)
  - check: `npx vitest run models/payment-batches.test.ts` green (5/5) — done

step: 2 — batch-queue.tsx: Reject… on Approved bar, dynamic dialog description, tightened Approve-confirm copy — done
  - spec §2, §3, §5 (Approve-confirm sentence)
  - files: components/payments/batch-queue.tsx
  - check: `grep -c "you can still reject it until it is marked paid"` = 1 in this file; reject button present in owner-Approved branch only — done; `rejectPaymentBatchAction` has exactly one call site (shared by both origins) — confirmed

step: 3 — batch-detail.tsx: self-approval note, "(from Approved)" audit line — done
  - spec §5 (pane note), §6 (describeAuditDetail)
  - files: components/payments/batch-detail.tsx
  - check: `grep -c "you can still reject it until it is marked paid"` = 1 in this file (2 total across both files, byte-identical string) — done; `fromStatus === "approved"` branch present in describeAuditDetail — done

Lint (`npx eslint` on the 4 touched files) clean. `tsc --noEmit -p .` run in background this
session (repo-wide, ~2 min); check `docs/wayfinder-reports/226/logs/scratch-296/tsc296.out`
for pre-existing vs. new errors before the next session's first edit if not already confirmed
clean by the time this hand-off is read.

step: G1 — round script (5 states × 1440/390) — done
step: G2 — r0 capture + fix batch (detector, critique, evaluate; bar: 0 P0/P1, health ≥85, Clean, critique ≥32/40 no heuristic <3) — done
step: G3 — build-done (include keyboard flow live both widths, close-phase handoff) — done
step: M1 — r1 capture + gate + contact sheet — done
step: M2 — three readers (critique, evaluate, include) — done

## G1 notes

Full narrative, dev-server gotcha, seed IDs: `docs/wayfinder-reports/226/logs/scratch-296/g1-notes.md`.
Script: `docs/wayfinder-reports/226/logs/scratch-296/round296.mjs`. Seed:
`docs/wayfinder-reports/226/logs/scratch-296/seed296.mjs`.

**Bug found and fixed this session:** state 05 fires a real reject on the NO_DOWNLOAD batch
(`...0296`), mutating it in the DB. `round()` runs all 5 states once per width (1440 then 390),
so the second width's states 02/05 hit an already-rejected batch and timed out waiting for the
Reject… button. Fixed by having state 02 reset that batch back to `approved` (raw `pg` query,
table `payment_runs`) + `page.reload()` at its start, every run. Confirmed clean:
`node round296.mjs r0-smoke3` → 10/10 states, 0 errored; "by type": all `detector-error` (the
detector's own finding-type label — not yet triaged, that's G2's job).
Dev server and impeccable live-server stopped before this session ended (port 3000 confirmed
free).

## G2 (done this session)

Started dev server + `impeccable live-server` (`node .impeccable/live/livesrv257.mjs` — was not
actually running at session start despite the G1 note claiming it was stopped cleanly; start it
fresh every session and confirm `curl -s -o /dev/null -w '%{http_code}' localhost:8400/detect.js`
returns 200 before running the round). Reseeded (idempotent). Ran `round296.mjs r0`, gated with
`gate.mjs shots-r0 --residue-file residue.txt` — no area primer exists for payments; wrote
`residue.txt` with the standard app-wide four from `docs/agents/areas/finance.md`
(`ai-color-palette`, `overused-font`, `layout-transition`, `dark-glow`) plus one payments-local
line, see below.

Two things fixed:
1. **Real but pre-existing detector finding**: `body-text-viewport-edge` on the Rejected-status
   footer `<p>` in `batch-queue.tsx:157` (`Rejected {date}. Its invoices are back on Bill Pay.`) —
   shipped by #251 for every terminal status footer (Paid/Waiting/Rejected), unmodified by #296,
   just newly *reachable* via the Approved-origin reject flow this ticket adds. Confirmed with a
   one-off DOM inspection (deleted after use, gitignored scratch dir anyway) that it's the page
   footer, not a toast or new #296 line, before adding `body-text-viewport-edge.*57-char` to
   `residue.txt` rather than touching #251's shell code.
2. **Round-script bug, not a product bug**: 3 keyboard probes failing only at 390
   (`reject-no-download-esc`, `reject-with-download-copy`, `approve-confirm-copy`) — the mobile
   pane itself renders with `role="dialog"` (`fixed inset-0 z-50 …` full-screen sheet), so
   `page.locator('[role="dialog"], [role="alertdialog"]')` matched 2 elements at 390 (pane +
   confirm dialog); Playwright's strict-mode `textContent()` threw and was swallowed by
   `.catch(() => "")`. Fixed in `round296.mjs`: `.last()` on the two text-content reads (states 03,
   04) and `<= 1` instead of `=== 0` on the post-Escape dialog count (state 02) — the full-screen
   pane itself is expected to remain after Escape closes only the confirm dialog.

Final gate: `gate: CLEAN — real findings 0 (none); probes pass 14 fail 0 unjudged 0; errored
states 0; page errors 0` (both widths, all 5 states).

Both servers stopped before this session ended (`ss -ltnp | grep ':3000\|:8400'` confirmed
empty).

## G3 (done this session)

- `npx tsc --noEmit -p .` (repo-wide, servers stopped): same pre-existing repo-wide error set as
  `tsc296.out` (dictation-actions, document-processing, ingestion, readiness/refresh,
  automation-rules, document-checks, document-matches, documents, inbound-email, review-tasks,
  job-worker — all unrelated TS2554/TS2305/TS7006, none in the 4 touched files). Output:
  `docs/wayfinder-reports/226/logs/scratch-296/tsc296-g3.out`. Zero errors in
  `models/payment-batches.ts`, `batch-queue.tsx`, `batch-detail.tsx` — confirmed clean.
- Part B contract checks (grep, no readers):
  - Reachability: `Reject…` button present in the owner-only Approved branch,
    `batch-queue.tsx:146` (`{isOwner && <Button …>Reject…</Button>}`), reusing the same handler as
    the Pending-origin button at line 139.
  - String extraction: self-approval note is the same 130-char string in both files
    (`batch-queue.tsx:168`, `batch-detail.tsx:51`), single source of truth, no drift.
  - Primitive diff: `ReasonDialog` and `ConfirmDialog` imported and reused as-is in
    `batch-queue.tsx`; no new dialog primitive forked.

No fixes needed — build-done confirmed, no rework since G2's clean gate.

## Measure phase (r1 — this session)

**Gate (r1):** CLEAN — real findings 0 (none), new since baseline 0; probes pass 14 fail 0 unjudged 0; errored states 0; page errors 0.

**Scores:**
- `critique: 37/40` (H1=4, H2=4, H3=4, H4=4, H5=4, H6=4, H7=3, H8=3, H9=4, H10=3) — no P0/P1 findings.
- `evaluate: 88/100 health, Clean verdict, P0: 0 P1: 0` — no P0/P1 findings.
- `include: 1440px PASS, 390px PASS, no P0/P1 findings` — WCAG 2.2 AA compliant at both widths.

**Detector summary (r1):** 10 states, all residue:
- 1440px (5 states): 4 residue findings per state (ai-color-palette, overused-font, layout-transition, dark-glow)
- 390px (5 states): 4 residue findings per state + 1 body-text-viewport-edge flag on state 05 (resolved as pre-existing, added to residue pattern)

All reader full reports written to `/home/ubuntu/Dev/Wes-doc/docs/wayfinder-reports/226/logs/scratch-296/readers-r1/` (critique.md, evaluate.md, include.md).

## Close phase (done this session)

Bar already met from measure (no fix batch needed): critique 37/40 no heuristic <3, evaluate
88/Clean 0 P0/P1, include PASS both widths, gate CLEAN. Ran the checks once: model tests 5/5,
full vitest suite (2 unrelated pre-existing failures — `lib/marketing-layout.test.ts`,
`lib/analytics.test.ts` — not in touched files), `tsc --noEmit` (20 pre-existing repo-wide errors,
none in the 3 touched files, matches G3's set), `eslint` on the 4 touched files clean, `next build`
fails only on a pre-existing repo-wide TS2554 in `dictation-actions.ts` (unrelated). Wrote area
primer `docs/agents/areas/payments.md` (none existed). No lessons entries needed — measured ≥
predicted on every score, no fix-loop corrections this session. Posted resolution comment, closed
#296, appended to map #226's Decisions so far. Ticket done.

## Rescore

Reopened by the rescore sweep: the ticket closed below the closing bar. The build is landed; measure the surface as it stands now, then close at the bar.
