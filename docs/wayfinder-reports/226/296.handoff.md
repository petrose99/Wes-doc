# Hand-off — #296 Reject on Approved batches + tightened self-approval note

milestone: spec-done

Map #226. Ground truth: #278 resolution comment. Spec + filled preflight (gate met, no
split needed — 3 build steps, ≤5 states, well under the 6-step/10-state sizing limit):

- Spec: `docs/wayfinder-reports/226/logs/scratch-296/spec.md`
- Pre-flight (Parts A–E, critic reconciled): `docs/wayfinder-reports/226/logs/scratch-296/preflight.md`
- Critic full write-up: `docs/wayfinder-reports/226/logs/scratch-296/critic.md`

Gate: critique reconciled 33/40 (H1=4, H5=4, rest 3), evaluate predicted 0 P0/P1 sum 0,
verdict Clean. No split — ships as one ticket.

## Build plan (one step per session; each gate step per `phases/build.md`)

step: 1 — model: rejectPaymentBatch accepts approved + fromStatus/exportedAt audit detail — todo
  - spec §1, §6 (fromStatus consumer decided: `describeAuditDetail` appends "(from Approved)")
  - files: models/payment-batches.ts (loadForDecision select, rejectPaymentBatch status check + detail), models/payment-batches.test.ts (new: approved→rejected case, paid→reject-throws case)
  - check: `npx vitest run models/payment-batches.test.ts` green; grep confirms no other model file needs a mock addition (no new prisma table)

step: 2 — batch-queue.tsx: Reject… on Approved bar, dynamic dialog description, tightened Approve-confirm copy — todo
  - spec §2, §3, §5 (Approve-confirm sentence)
  - files: components/payments/batch-queue.tsx
  - check: `grep -c "you can still reject it until it is marked paid"` = 1 in this file; reject button present in owner-Approved branch only

step: 3 — batch-detail.tsx: self-approval note, "(from Approved)" audit line — todo
  - spec §5 (pane note), §6 (describeAuditDetail)
  - files: components/payments/batch-detail.tsx
  - check: `grep -c "you can still reject it until it is marked paid"` = 1 in this file (2 total across both files, byte-identical string); `fromStatus === "approved"` branch present in describeAuditDetail

step: G1 — round script (capture states: Approved-pane-owner, Reject-confirm-no-download, Reject-confirm-with-download, Approve-confirm-self-submitted, Rejected-from-Approved-pane; both widths 1440/390) — todo
step: G2 — r0 capture + fix batch (detector, critique, evaluate against the bar: zero P0/P1, health ≥85, Clean; critique ≥32/40 no heuristic <3) — todo
step: G3 — build-done (include keyboard flow live both widths, close-phase handoff) — todo

## Next step

Build session: implement step 1 (model layer). Read models/payment-batches.ts:319-345 by
range (already known from spec) and models/payment-runs.test.ts or a sibling model test file
for the vitest/prisma mock pattern before writing the two new test cases — don't re-read the
whole spec/preflight, they're summarized above.
