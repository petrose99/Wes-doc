# Hand-off — #285 Build Companies (Admin, Queue-screen shell)

milestone: spec-done
milestone: build-done
milestone: measured
milestone: closed

**Ticket closed.** Full history lives in the artefacts below; nothing left for a future session.

## Outcome
Companies shipped on the Queue-screen shell (personal/ungrouped/organization states, Move/Rename/Remove, card-less danger zone, deep link, `?notice=` toast chain). Close-phase triage found no real defects: the evaluate P1 (mobile "focus trap") was a probe-naming misread of the pre-existing `nextjs-portal` dev-overlay tail (#261 residue); the P2 "undocumented Open in a new tab" item is `QueueScreen`'s shell default on every queue; H8's 2/4 was driven by the same residue plus a shared-Dialog-title type-step already flagged as shell in the build gate. No fix batch, no re-confirm round needed. Checks (affected + full vitest, `tsc`, `eslint`, `next build`) all pass.

## Artefacts
- Report (full detail, triage table, scores): `docs/wayfinder-reports/226/285.md`
- Spec: `docs/wayfinder-reports/226/logs/scratch-285/spec.md`
- Pre-flight: `docs/wayfinder-reports/226/logs/scratch-285/preflight.md`
- Round/capture scripts, keyboard probes, detector JSON: `docs/wayfinder-reports/226/logs/scratch-285/` (gitignored scratch)
- Primer: `docs/agents/areas/admin.md` (Companies section)
- Lessons: `.claude/wayfinder-autopilot/lessons.md` (two close-phase entries)

## Scores
critique 34/40 (no P0/P1) · evaluate raw 50/100 read as pass after triage (both flagged findings non-real) · include Pass both widths · detector Clean, named residue only.
