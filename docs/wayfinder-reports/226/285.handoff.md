# Hand-off — #285 Build Companies (Admin, Queue-screen shell)

milestone: spec-done
milestone: build-plan-written

## Build plan (one step per session; check named per step; mark `done` and stop after each)

- step: 1 — model additions — todo|done: **done**
  Spec §2, preflight B4. Files: `models/organizations.ts` (`_count.members` on `listOrganizationCompanies`, `organizationCompanyCount`, `listOwnedUngroupedTeamWorkspaces`, `removeWorkspaceFromOrganization` — mirrors `moveWorkspaceIntoOrganization`'s owner check), `models/organizations.test.ts` (mocks for the three new functions, P15 shape). Check: `npx vitest run models/organizations.test.ts` — 14/14 pass. Landed this session.
- step: 2 — companies actions.ts — **done**
  Spec §2 gating rule. Landed: `app/(app)/workspaces/[workspaceId]/admin/companies/actions.ts` — **all actions take `(workspaceId, …)` first, like Users' actions**: `loadCompanyDetailAction(workspaceId, targetId)` → `ActionState<CompanyRow>`; `createOrganizationAction(workspaceId, {name})`; `addCompanyAction(workspaceId, {name,country?,baseCurrency?})` → `{workspaceId,name}`; `moveCompanyIntoOrganizationAction(workspaceId, targetId)`; `removeCompanyFromOrganizationAction(workspaceId, targetId)`. Codes: the spec's seven plus `name_required`, `personal_workspace`, `is_current` (remove on the current company), `failed`. `lib/admin/companies.ts` (client-safe): `CompanyRow`, `CompanyViewerRole`, `CompanyActionCode`, `companyActionErrorText(code, {name, org})` — step 4/5 use this mapper; spec §4/§7 sentences that need more context (Move's `not_owner` wording → use `owner_required`) are mapped there. Test: `lib/admin/companies-actions.test.ts` (vitest include does not cover `app/`) — 15/15 pass. Note `name_taken` is checked in the action (case-insensitive within the org; org names against the caller's orgs) — the schema has no unique constraint.
- step: 3 — layout anchor + page wiring — **done**
  Landed 2026-09-17: `admin/layout.tsx` has `CLIENT_MANIFEST_ANCHORS = [CompanyDetail]`; `admin/companies/page.tsx` rewritten (incumbent Panel list gone from this page; `CompanyNameForm`/`TeamWorkspaceForm`/`WorkspaceDangerZone` files untouched — `workspaces/create/page.tsx` still uses the form). Page builds `CompaniesPageState` (`personal` | `ungrouped{workspaceName}` | `organization{organizationName, rows: CompanyRow[], hiddenCount, movable[{id,name,memberCount}]}`) and renders `<CompaniesQueue workspaceId state viewerRole owners>`; `movable` is loaded only for owners. **Stubs created with the final prop contracts**: `components/admin/companies-queue.tsx` (exports `CompaniesPageState`, `CompaniesQueueProps`, `CompaniesQueue` — body is a placeholder h1) and `components/admin/company-detail.tsx` (`CompanyDetailProps {workspaceId,row,organizationName}`, placeholder dl). Steps 4/5 replace the bodies, keep the exports. tsc clean on touched files; 29/29 affected tests pass.
- step: 4 — companies-queue.tsx (list surface) — **done**
  Spec §3 (header, table/cards, states a/b/c), §7 copy matrix, §8 focus order for the list. `QueueScreen` already has `primaryAction`/`overrideMode` props (landed via #286, verified in `components/queue/queue-screen.tsx` — do not re-add). Replace the body of `components/admin/companies-queue.tsx` (keep `CompaniesPageState`/`CompaniesQueueProps` exports — the page depends on them): `QueueScreen<CompanyRow>` + column defs + `empty.firstUse` for b/c + dialogs: Add a company, Name your organization, Create a team workspace, Move a company into ‹org›. `loadDetail` → `loadCompanyDetailAction(workspaceId, id)` → `<CompanyDetail>` (stub exists). Check: Part B1 reachability grep for the 4 dialog-opening actions; B3 string extraction against spec §7.
- step: 5 — pane + danger zone — **done**
  Spec §5. Landed 2026-09-17 (session 7): `components/admin/company-detail.tsx` body filled — summary `dl` (Name+"This company" badge, Country, Currency, Tax jurisdiction with the Configuration › Tax link when null, Members "n · owners: A, B" via `ownersLine`, Your role, Created); card-less danger zone (`CompanyDangerActions`) when `row.isCurrent`, else the muted "open it and use its danger zone" line with an Open link. `lib/admin/companies.ts` gained `CompanyDetailRow = CompanyRow & { owners: string[] }` (detail-only, not on the list row); `loadCompanyDetailAction` (actions.ts) now selects `members: { where: { role: "owner" } }` and returns `owners: string[]`. Sticky bar + pane ⋯ are `paneActions`/`paneMenu` on `QueueScreen` in `companies-queue.tsx` (they only need the list row, not the loaded detail): Open (Link to `/workspaces/{row.id}`), pane ⋯ = Open Admin (`adminPaths(row.id).base`, always) + Rename…/Remove from ‹org› (owner of the row only; Remove disabled+reason when `row.isCurrent`). New `RenameCompanyDialog` (reuses `renameWorkspaceAction(row.id, name)`) and `RemoveCompanyConfirm` (`ConfirmDialog`, reuses `removeCompanyFromOrganizationAction(workspaceId, row.id)`, closes the pane via the `PaneHelpers.close` captured at menu-click time since the row disappears from `rows` on success) added to `companies-queue.tsx`. `components/admin/company-danger-actions.tsx` is new — card-less Delete (typed-name `Dialog`, owner only) / Leave (`ConfirmDialog` + last-reviewer two-phase `ConfirmDialog`, everyone), refactored from `components/workspace/danger-zone.tsx` (kept its hard-navigation-on-success reasoning and two-phase leave logic verbatim; dropped the `Card` wrapper and the personal-workspace branch, which cannot occur here).
  **Also fixed this session (found via `tsc --noEmit`, not in the original step list):** `app/(app)/workspaces/[workspaceId]/admin/companies/[companyId]/page.tsx` was created in step 3 anticipating a `CompaniesScreen` named export from `page.tsx` that step 3 never added — the deep-link route (spec §5.4) didn't build. `page.tsx` now exports `CompaniesScreen({ params, selectedId })` (the default `CompaniesPage` just calls it with `selectedId` undefined); `CompaniesQueue`/`CompaniesQueueProps` gained `initialSelectedId?: string | null`, threaded to `OrganizationState` → `QueueScreen`'s existing `initialSelectedId` prop. Not yet handled: the spec §5.4 "not in the viewer's unfiltered rows" notice and the "filtered but present → clear filters and select" branch — `QueueScreen`'s own `initialMissing`/`missingNotice` machinery (queue-screen.tsx:236-251) already covers the filtered-but-present case per its existing contract; the nameless "isn't in ‹org›, or you're not a member" notice for a truly absent id is **not yet wired** — next session (build gate, step 8) should check this against a real deep link during the capture round and add it if the existing `initialMissing` text doesn't already satisfy spec wording.
  Check run: `tsc --noEmit -p .` clean (was failing on the `[companyId]` route before the fix above — unrelated pre-existing breakage, not a new regression from this step). `eslint` clean on all touched/new files. B1 grep: `setRenaming`/`setRemoving` both only reachable from the two new `PaneMenuItem`s. `npx vitest run models/organizations.test.ts lib/admin/companies-actions.test.ts` — 29/29 pass (one test's mock updated for the new `members`/`owners` shape). B5 (focus-return per dialog) not yet probed live — that's the build gate's keyboard-probe job (step 8), not a per-step check this session ran.
- step: 6 — /workspaces notice consumer — todo
  Spec §4.6. Check first whether `/workspaces` already reads a `?notice=` param (grep before writing) — if not, add the toast consumer for `deleted`/`left`. Small, bundle with step 5 if both land in one session comfortably; otherwise its own step.
- step: 7 — seed extension — todo
  Hand-off "Facts verified" §Seed. Extend the dev seed so all three page states are reachable: state (a) already has one row (Acme Advisory → Riverside Bakery Co.) — add a second company in the same org, an ungrouped owned team workspace (state b path from a different workspace), and a personal workspace (state c). Check: seed runs clean, `psql`/prisma studio spot check row counts.
- step: 8 — build gate — todo
  Seed + servers up. Write `scripts/wayfinder-autopilot/capture-round.mjs`-based round script for Companies (states a/b/c × 1440/390, detector JSON, keyboard probes for every B5 row). Run once as `shots-r0`. Fix every real detector finding and failing probe in place (no readers this phase). Record r0 counts per width on this file. `tsc --noEmit` once, full project. Stop dev server. Write `milestone: build-done`, commit, stop.

## Progress notes
- Step 1 landed 2026-09-17: `models/organizations.ts` additions verified against the actual file (not the earlier hand-off's guessed signatures — `listOrganizationCompanies(organizationId, userId)`, not `(orgId)`; the reused actions live at `app/(app)/workspaces/[workspaceId]/workspace-actions.ts`, not a top-level `workspace-actions.ts`). Step 2 landed 2026-09-17 (see plan line). Step 3 landed 2026-09-17 (session 5). Next session starts at step 4 and should trust these corrected paths over the "Facts verified in code" section below where they conflict.
- Step 4 landed 2026-09-17 (session 6): `components/admin/companies-queue.tsx` body replaced (exports kept). Three states as separate small components (`PersonalState`, `UngroupedState`, `OrganizationState`); `OrganizationState` renders `QueueScreen<CompanyRow>` with columns per spec §3.2, `SORTS` (Name A–Z, Most members), the `role` facet + `filterByRole`, `primaryAction`/`menu` gated on `isOwner`, `loadDetail` → `loadCompanyDetailAction` → `<CompanyDetail>` (still the step-3 stub — step 5 fills its body). Four dialogs: `AddCompanyDialog`, `MoveCompanyDialog` (state a), `NameOrganizationDialog` (state b), `CreateTeamWorkspaceDialog` (state c) — each wired to its named action and `companyActionErrorText`, using `lib/geo/iso-lists.ts` (already on disk, untracked, for country/currency selects). `Badge` (components/ui/badge.tsx) used for the "This company" pill — no dedicated `Pill` component exists in this repo. `clearFilterParams` (lib/queue/filters.ts) takes `URLSearchParams`, not a path string — used a plain `router.push` to the bare companies path instead (no carried-over query = filters already cleared), noted inline. Not yet wired: `?company=` deep-link-not-in-rows handling (spec §5.4), the Move dialog's "Nothing left to move" live-empty case beyond initial-empty, Undo actions on Move/Remove toasts (Remove itself is step 5). B1 reachability grep passed (`setAdding/setMoving/setNaming/setCreating` all wired to visible triggers). `tsc --noEmit` clean on the file. No vitest for this file yet (client component under `app/` convention — see step 2's note that `app/` isn't covered by the vitest include; this file is under `components/` but has no test file — none planned per the spec's test plan, which relies on the build-gate capture round).

Phase spec (1 of 4) is complete (sessions 1–2). Next phase: **build**, fresh session, no re-spec.

## Artefacts (scratch folder is gitignored; regenerable readers there)
- Spec: `docs/wayfinder-reports/226/logs/scratch-285/spec.md` (§1–§9: route/shell/data, header, table, three page states, roles, flows 4.1–4.6, pane 5.1–5.4, state inventory §6, copy matrix §7, a11y §8, anti-goals §9). Lines marked "critic" are the Part D reconciliation changes.
- Pre-flight: `…/scratch-285/preflight.md` — Parts A–E filled, D reconciled. Predicted: critique **32/40** (H1 4, H5 4, rest 3) · evaluate sum 10, P1 0, health ≈ 88 · Clean. Critic's first pass on the pre-reconciliation spec was 28/40 · health 72 · 4 P1 (all fixed in the spec; the confirm re-score was not run — the agent could not be resumed and the context line was hit; optional 5-minute text-only re-score at the start of build if wanted, not required by the gate).
- Code readers from session 1: `…/scratch-285/read.mjs`, `read2.mjs`, `read3.mjs` (print the relevant code ranges in one call each).
- Report: `docs/wayfinder-reports/226/285.md` (session 2).

## Facts verified in code (do not re-read)
- Route `app/(app)/workspaces/[workspaceId]/admin/companies/page.tsx` (56 lines, incumbent Panel list + TeamWorkspaceForm + CompanyNameForm + WorkspaceDangerZone) → replaced by `QueueScreen<CompanyRow>` in `components/admin/companies-queue.tsx`, inside Admin layout's `min-w-0 flex-1` column (no `AdminPage`).
- `models/organizations.ts`: `listOrganizationCompanies(orgId,userId)` (rows + `members[0].role`, `orderBy name`), `listOrganizationsForUser`, `createOrganization(name, ownerId)`, `addCompanyToOrganization(orgId, ownerId, {name,country,baseCurrency})`, `moveWorkspaceIntoOrganization(wsId, orgId, actorId)`, `countAccessibleCompanies`. **To add (additive):** `removeWorkspaceFromOrganization`, `listOwnedUngroupedTeamWorkspaces(userId)`, `_count.members` on the companies query, an org-total count for the "k more in ‹org›" line.
- `workspace-actions.ts`: `createWorkspaceAction(name)`, `renameWorkspaceAction(id,name)`, `deleteWorkspaceAction(id)`, `leaveWorkspaceAction(id,{confirmLastReviewerRemoval})`.
- `QueueScreen` (queue-screen.tsx 76–186): `PaneHelpers{close,refresh,next}`, `empty{firstUse{title,body,action,phoneAction},done,filtered*}`, `menu` (renders after Override Mode + Export — Override item is unconditional today, hence the new `overrideMode` prop), `stat` is "one inline figure" (hence the new `primaryAction` prop, rendered at line ~476 beside `stat`), `initialMissing{text,showHref,name}`, `phoneReadOnly`, `cards.below:"md"`, `onControls`. `(queue)/layout.tsx` line 11 `CLIENT_MANIFEST_ANCHORS` trick → repeat in `admin/layout.tsx`.
- Workspace fields: `kind` ("personal"|"team"), `organizationId`, `country`, `baseCurrency`, `jurisdictionCode?`. Seed: dev user has org "Acme Advisory" → "Riverside Bakery Co." + a personal ws (state (a) with one row; need a second company + an ungrouped owned team ws + a member-only ws to capture (b), Move, and the non-owner pane).
- Primer: `docs/agents/areas/admin.md` (refresh at close); queue shell: `docs/agents/areas/queue-shell.md`.

## Exact next step (build phase)
1. Read spec.md + preflight.md whole (they are the checklist), `craft-floor.md`, both lessons files.
2. Model additions + `admin/companies/actions.ts` (gating rule in spec §2; named error codes) + `models/organizations.test.ts` mock shape (project lesson P15).
3. Shell: `primaryAction`, `overrideMode` props on `QueueScreen`; `/workspaces` `?notice=` consumer (check whether one exists first).
4. `components/admin/companies-queue.tsx`, `company-detail.tsx` (pane, server-action `loadDetail`), dialogs (Add, Move, Rename, Name organization, Create team workspace), `company-danger-actions.tsx` (card-less Delete/Leave from `WorkspaceDangerZone` logic), page + layout anchor.
5. Extend the seed for the states above; run Part B checks (B1 grep, B3 string extraction, B4 diff, B5 focus probe); affected vitest; `tsc --noEmit` once with the dev server stopped; commit; `milestone: build-done`.

## Open / needs owner
- None blocking. Notes for #287 (Admin dashboard): org rename/dissolve; empty-org after last company deleted (spec §4.6 names the consequence only).
