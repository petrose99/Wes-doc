# Hand-off — #285 Build Companies (Admin, Queue-screen shell)

Phase: **spec** (1 of 4). Session 1 ended at the context line after the reading pass; no spec file written yet.
Milestones done: claimed · map/primer/lessons/preflight template read · #231 decisions + #254 resolution read · code ranges read (scratch `read.mjs`, `read2.mjs`, `read3.mjs` print them in one call each — re-run those, do not re-read files).

## Next step (exact)
Write `docs/wayfinder-reports/226/logs/scratch-285/spec.md` and `preflight.md` (copy of `scripts/wayfinder-autopilot/preflight.md`, parts A–E filled) from the decisions below, run the Part D critic (`Agent`, `model: "opus"` — ticket touches schema/auth: org attach/detach, delete), gate it, then write `milestone: spec-done`, commit `wip(autopilot): #285 spec`.

## Facts the spec rests on (already verified in code)
- Route `app/(app)/workspaces/[workspaceId]/admin/companies/page.tsx` (56 lines, incumbent = Panel list + TeamWorkspaceForm + CompanyNameForm + WorkspaceDangerZone) → replace with `QueueScreen` inside the Admin layout's `min-w-0 flex-1` column (nav is 208px; do NOT use `AdminPage`'s 880px column — #231 d24 says Queue-screen shell).
- `models/organizations.ts`: `listOrganizationCompanies(orgId, userId)` (rows, each with `members[0].role`), `listOrganizationsForUser`, `createOrganization(name, ownerId)`, `addCompanyToOrganization(orgId, ownerId, {name,country,baseCurrency})`, `moveWorkspaceIntoOrganization(wsId, orgId, actorId)` (owner-checked). **Missing: a detach (`removeWorkspaceFromOrganization`) for B1 reversal of Move — add it (additive, owner-checked, sets organizationId null).** New "use server" actions go in a new `admin/companies/actions.ts` (models are not "use server" on purpose).
- Existing actions reused: `workspace-actions.ts` `createWorkspaceAction(name)` (ungrouped team ws), `renameWorkspaceAction`, `deleteWorkspaceAction`, `leaveWorkspaceAction` (two-phase last-reviewer confirm).
- `QueueScreen` props read (queue-screen.tsx 76–179): `rowName`, `loadDetail(id)→JSX via server action` (needs a client-manifest anchor in `admin/layout.tsx`, same trick as `(queue)/layout.tsx` `CLIENT_MANIFEST_ANCHORS`), `paneActions`, `paneMenu`, `empty{firstUse,done,filtered}`, `cards.below:"md"`, `phoneReadOnly`, `selectable=false`, `stat`, `band`, `menu`. Non-document precedent: `components/payments/batch-queue.tsx`.
- Workspace fields for columns: `name`, `country`, `baseCurrency`, `jurisdictionCode` (Tax; null → "—"), `taxProfile.region` alt, `members` count, viewer role. Seed: dev user has org "Acme Advisory" → "Riverside Bakery Co." + a personal ws.
- Dialog primitives: `components/ui/dialog.tsx` `Dialog{open,title,description,onClose,initialFocus,placement}`, `confirm-dialog.tsx` `ConfirmDialog{destructive,busy,children}`; danger zone uses `Card` from `admin/panel-card` — inside the pane render it card-less (nested-cards detector).
- Vic record: tour findings lines 11–14, 26, 36 (ORGANIZATION › Companies; per-entity roles; plain tables).

## Decisions taken for the spec (rule: map Notes → #231 d15/d24 → smallest reversible)
1. **Rows = team workspaces in the current workspace's organization** the viewer has `WorkspaceMember` on (ADR 0002). Columns: Name (title; "This company" pill on current) · Country · Currency · Tax · Members · Your role. Sort Name A–Z default, Most members. Facet: Your role. No bulk bar.
2. **Three top-level states**: (a) current ws in an org → list + Add a company + Move band; (b) current ws is an ungrouped team ws → one row (itself) + band **Name your organization** (input + "Create organization"), Add a company *disabled with visible reason* "Name your organization first" (#252 H6 lesson); (c) current ws personal → empty state "Your personal workspace isn't a company." body: companies are team workspaces inside an organization; action **Add a company** (creates an ungrouped team ws via `createWorkspaceAction`, lands on its Companies page = state b, per #231 d15 "first visit"); second action link "Switch company" → `/workspaces`.
3. **Add a company** (state a): shell `Dialog`; Name (required), Country + Currency selects defaulting to the current company's; consequence line "Starts empty inside ‹org›, with you as its owner. Nothing is shared between companies." Submit **Add company**; success → `router.refresh()` + pane opens on the new row + toast "‹Name› added"; failure keeps input, error at field.
4. **Move into ‹org›**: band above the list listing other ungrouped team workspaces the viewer *owns*; per item button "Move ‹name› into ‹org›" → `ConfirmDialog` (consequence: "Its members keep their roles. ‹Org› admins see it under Companies and Users. Nothing is shared between companies."). Reversal: pane ⋯ **Remove from ‹org›** (owner only, new detach action, same confirm grammar). Both refresh list + pane same tick.
5. **Pane** = summary (Name · Country · Currency · Tax · Members with owners named · Your role · Created) + sticky bar **Open** (→ `/workspaces/{id}`, the Invoices home). ⋯: Open Admin (→ configuration), Rename… (owner; one-field Dialog, replaces CompanyNameForm on this page), Remove from ‹org› (owner). Current company's pane ends with the **danger zone** (Delete for owner with typed-name confirm; Leave for others; personal never) — reuse `WorkspaceDangerZone` logic card-less; hard navigation to `/workspaces` after, as today.
6. **Vocabulary**: Company / Organization / Admin per CONTEXT.md glossary; "Add a company" (button/dialog title) · "Add company" (submit) · toast "‹Name› added"; "Move into ‹org›" / "Remove from ‹org›"; roles Owner · Reviewer · Member; never "workspace" in copy except "personal workspace".
7. **Below md**: `cards.below:"md"`, `phoneReadOnly`, `PhoneNote` (Admin is desktop-only, #231 d18); nav already hidden.
8. **Members-not-owner**: page renders, Add/Move/Rename/Delete absent, `ReadOnlyBand` "Ask an owner: …" (never 404).
9. Part A targets: H1 (every mutation → row + pane + count + toast via `router.refresh` + `helpers.refresh` same tick) and H5 (every ⚠ confirmed with consequence; Delete typed-name; Add inputs constrained by selects).
10. B6 edges: org renamed/deleted by another admin while open → refresh shows state b; company deleted while its pane is open → `initialMissing` notice "‹Name› was deleted."; last owner cannot Leave (server guard `last_owner_required` mirrored as disabled-with-reason).

## Not done / open
- No spec.md, no preflight.md, no critic run, no build. Predicted scores not yet recorded.
- Nothing committed for product code; only scratch readers + this file.
