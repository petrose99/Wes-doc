# Autopilot report — #232 Shape mobile approval against the tour's app screens (home cards, Ready-to-Approve cards, Approval Context, timeline)

- Map: #226 Make the operator app work like Vic.ai (grammar, not skin)
- Type: wayfinder:grilling · Started: 2026-09-15T23:36Z · Finished: 2026-09-15T23:42Z
- Outcome: resolved

## What I did

1. Claimed #232; loaded the map, the ticket and its two owner comments (#237 and #231 inputs), tour findings §2b "Approval workflows — mobile app" and §6, `approvals-mobile-context.png`, CONTEXT.md, PRODUCT.md, `docs/journeys/operator-working-day.md`, #189/#209/#235 and #227's resolution; read the incumbent sources `queue-screen.tsx`, `detail-pane.tsx`, `split-pane.tsx` (responsive rules), `mobile-tab-bar.tsx`, `mobile-header.tsx`, `review-inbox.tsx` (mobile cards + bar), `prisma/schema.prisma` (`Supplier`, `User.avatar`), `lib/checks/duplicates.ts`, `public/site.webmanifest`.
2. Called `intent` (routed `transpose`), `impeccable` (`adapt`), `grilling`, `domain-modeling`. `impeccable context` did not run — the launcher requires approval the driver does not grant (same as #229/#231); read PRODUCT.md directly, no DESIGN.md exists.
3. Did not re-run the incumbent critique: the mobile inbox is being replaced (#227 §16 deletes `review-inbox.tsx`), so per the brief its score is evidence only. Cited #227's critique (21/40, in-page 12/6 · 17/9, P0 bar-under-nav, P1 optimistic approval, P1 "—" amounts) and #225's Invoices sheet at 390 (6/5).
4. Grilled in two rounds (landing/IA/flow → behaviour/states/words/rendering/a11y), 19 questions, each answered with its ➡️ recommendation.
5. Amended CONTEXT.md's **Detail pane** entry (the phone opens the same pane full-screen; "Approval Context" and "detail sheet" under _Avoid_).
6. Created #257 (execution), attached it as a sub-issue of #226, wired blocked-by #236.
7. Posted the resolution comment (https://github.com/petrose99/Wes-doc/issues/232#issuecomment-5689697857), closed #232, appended to the map's Decisions so far, graduated the "More From This Vendor" fog into #257's scope.
8. Posted the phone-card input on #235 (https://github.com/petrose99/Wes-doc/issues/235#issuecomment-5689700005).
9. Committed the glossary change and this report.

## Questions I answered on the owner's behalf

| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Phone landing: two-card home, Invoices, or Ready to Approve | Invoices on every width; Approvals is the first tab | CONTEXT.md "Workspace home — no separate overview surface" (#237) is a documented decision, so Vic does not win the tie; viewport redirect is the least reversible option |
| 2 | Tab bar | Approvals · Invoices · Exceptions · Account; other areas as a read-only list on Account; Invoices badge dropped | #239 Q7 (phone = Approver's lane), #231 (Account), #239 (Finance off-route, Archive side-path); Attention Bait cat. 5 |
| 3 | Nothing to approve | "Done" empty state with "N waiting on others" + `Approver · Anyone` + link | #239 "empty queue is done"; #227 §10; Dead Ends cat. 9 |
| 4 | Two queues on a phone | One list, segmented Invoices (n) · PO Mismatches (n); Filter bottom sheet | Vic's always-present counts; #225's chips wrap to three rows at 390 |
| 5 | Row anatomy | ≥64px card, mark · Supplier · Amount / # · Due / Stage-or-Not-eligible; no checkboxes | Vic's card row; #223 mark; #233 vocabulary; no colour-only meaning; Destructive Defaults |
| 6 | Detail vs Approval Context | One sheet = the below-`lg` Detail pane, source strip over tabs, opens on Approval | Documented exception to Vic (#224 combined pane); #227 §6; `split-pane.tsx` already stacks source 38vh |
| 7 | Timeline | One component both widths, avatar or initials, INITIATED first | Vic wins the tie on form; #227 §6 list is the same data; `User.avatar` exists; #209 built it |
| 8 | More From This Vendor | "From this supplier" from `Supplier.documentCount/lastSeenAt/paymentTermsDays` + near-duplicate check; no model | Evidence-in-view principle (#228); fog condition met by checking the model |
| 9 | PO Mismatches on phone | Variance summary read-only, override-with-reason, "Match lines on desktop" note | #189 line matching desktop-only; #227 §3; #228 breakdown |
| 10 | Decision bar | Sticky footer Reject (outline) · Approve (filled), 48px, above the tab bar; reason/modal as bottom sheets; no swipe | #227 P0 fixed by construction (`z-50` over `z-40`); #185; Destructive Defaults |
| 11 | After a decision | Stay on the row, Next to approve · Back to list, no auto-advance | #239; autonomy under interruption; Server-confirmed (#227 §10) |
| 12 | History | pushState on open below `lg` so system Back closes; desktop keeps replaceState | Real conditions: the phone's Escape is Back |
| 13 | Offline / refused / Not eligible / loading | #227 §10 inventory with phone rendering | `fortify`; #227 §5, §10 |
| 14 | Words | Tab "Approvals", title "Ready to Approve", "Waiting on you", plain reasons | `articulate`; #233 |
| 15 | Type/spacing/colour at 390 | 15/13px, tabular, 16px inputs, hairlines, emerald/red reserved for action and state | Impeccable craft floor (file missing in repo — skill reference used); iOS zoom |
| 16 | Motion | Slide-in 200ms, reduced-motion instant, result crossfade | Intent: feedback must exist; Impeccable `animate` |
| 17 | Accessibility | dialog/aria-modal, focus to heading, tablist, aria-live result, 48px targets | `include`; PRODUCT.md accessibility constraint |
| 18 | Tablet band | Unchanged: rail carries Approvals, sheet full-screen | Smallest reversible; already the shell's behaviour |
| 19 | Measure | Share of decisions below `lg`, Start→decision time, mis-tap and rubber-stamp counter-metrics | `measure`; Goodhart guard |

## Decisions recorded

Landing stays Invoices; Vic's card home rejected. Tab bar Approvals · Invoices · Exceptions · Account. Ready to Approve on the phone is one card list with a segmented two-queue control and a Filter sheet, no bulk. The below-`lg` Detail pane is the phone screen (no separate Approval Context), one timeline component, "From this supplier" from existing data. Sticky Reject · Approve above the tab bar, Server-confirmed, stay on the row, pushState for system Back, full phone state inventory. Full text: the resolution comment on #232.

## Tickets created / changed

- #257 task — Build the phone approval lane (sub-issue of #226; blocked by #236, which is closed, so it is on the frontier).
- #235 — received the phone-card input as a comment; still open.
- Map: #232 appended to Decisions so far; "More From This Vendor" fog graduated into #257.
- CONTEXT.md: Detail pane entry amended; "Approval Context", "detail sheet" added under _Avoid_.

## Scores and counts (rendered surfaces only)

No score; surface not built yet. Incumbent findings that shaped the recommendations: #227's critique of the mobile inbox 21/40, in-page 12/6 (list) · 17/9 (row selected) at 1440/390, P0 decision bar under the tab bar, P1 optimistic approval, P1 "—" amounts, P1 second grammar; #225's Invoices sheet at 390 6/5 pane-open; facet chips wrapping to three rows at 390 (#235's evidence).

## Needs the owner

- **#236's commit `b137c01` is not on `codex/post-login-ux-implementation` in this sandbox** — no `/approvals*` routes, `review-inbox.tsx` still present — though the map records it shipped. #257 tells its session to confirm the routes exist before starting; if they do not, #257 is blocked on landing #236's work.
- `impeccable context` cannot run under the driver (launcher needs approval); every autopilot session has read PRODUCT.md by hand instead. Allow-listing `.claude/skills/impeccable/scripts/impeccable` would fix it.
- `craft-floor.md` still does not exist in the repo (flagged since #236).
- No removals were decided. Dropping the Invoices tab badge and moving Finance/Archive off the phone tab bar are re-placements (still reachable from Account), not unplugs.
