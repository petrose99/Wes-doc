# Hand-off — #268 Build the Origin link (map #226)

milestone: spec-done
milestone: build-in-progress

## Phase state
- Spec phase done (sessions 1–2). Sessions 3–5: **BUILD**. Session 5 finished the code (every §8 file), `tsc --noEmit` is clean, the ticket's unit tests pass, Part B checks pass — cut at the context line **before the build gate's round r0**. Next session: still BUILD — run r0 only (below), fix what it shows, write `milestone: build-done`.
- Ticket claimed (assignee petrose99). Scratch: `docs/wayfinder-reports/226/logs/scratch-268/` — `spec.md`, `preflight.md` (A–E, gate passed: predicted critique 32/40, evaluate sum 6, P1 0, health ≈ 88, Clean), `part-b-checks.mjs` (run from repo root: all ✓; the two "FAIL" lines it prints are comment-text matches, not code), `tsc.txt` (empty = clean).

## Built and verified (sessions 3–5)
1. `lib/navigation/origin.ts` (+ `origin.test.ts`, 13 tests) — `withOrigin` (2048 cap, drops outermost chained from), `withParam`, `readOrigin(searchParams, workspaceId?)`, `originLabel` (10 paths incl. `library → Archive`), `labelForDestinationPath`, `Origin` type. No prisma import.
2. `lib/navigation/origin-server.ts` — `describeOrigin`, `describeMissingRow` (returns `name` for the Show-it aria-label), `queueArrival(workspaceId, {searchParams, queuePath, selectedId, rowIds, decidedText?, missingText?})`. `rowIds` are **documentIds** (fixed this session for invoices/receipts/approvals).
3. `components/queue/origin-strip.tsx` (+ `origin-strip.test.tsx`, 4 tests) — spec §2.1–2.3; not yet seen rendered.
4. `components/queue/queue-screen.tsx` — `origin` slot before `band`, `initialMissing { text; showHref?; name? }`, filtered notice (case 1), Show it `aria-label="Show {name}"`, `fullHref` carries `from=`.
5. Six `*-queue.tsx` + seven pages wired through `queueArrival` (Exceptions passes `missingText`, Approvals `decidedText`).
6. H-a/H-b Exceptions pane: `Open on {label}` button + ⋯ item. H-c `components/accounting/accounting-dashboard.tsx` `ReadyToPushList`: filename link `aria-label="Open {filename} on {label}"` + visible **Open on {label}** link in the action cell, both `withOrigin`. H-d `search/search-client.tsx` `ResultRow`: `withOrigin(`${base}/documents/${id}[?page=]`, here)`, aria "Open {filename} on {label}" / "Open {filename}"; no `/pipeline?doc` left in code.
7. `documents/[documentId]/page.tsx`: `OriginStrip` above `PaneFrame` in a `h-screen` flex wrapper (fallback origin = `documentDestinationPath(document)` with the row, label via `labelForDestinationPath`); `goneOrNotFound()` → missing doc + valid `from` → `redirect(withParam(from,"gone",id))`, else `notFound()`; `LegacyDocumentPage` forwards `from`. New `documents/[documentId]/not-found.tsx` (client, `useParams`): "That document was deleted." / "It isn't in this workspace any more." / **Back to Invoices**.
8. `components/queue/detail-pane.tsx`: `PaneFrame` full-mode ArrowLeft link removed (affordance swap, spec §2.4); full-mode class now `flex min-h-0 flex-1 flex-col` (the page wrapper owns `h-screen`). `backHref` still serves Delete's landing.
9. `CONTEXT.md` Origin context entry: `from=` / `gone=` sentence added.
- Tests run: `npx vitest run lib/navigation components/queue` → 51 pass. Not written (spec §7): queue-screen notice tests and the route test for `gone=`/not-found — verify those live in r0 instead (states below) and say so in the report.

## NOT yet done — the build gate
- Servers: `docker start docubite-devdb` if :55433 is down · `node .impeccable/live/dev.mjs start` · `node .impeccable/live/livesrv257.mjs` (run_in_background; port 8400) · seed `npx tsx --env-file .env .impeccable/live/seed257b.ts` · warm routes with `.impeccable/live/probe252.mjs`. Recipe in `docs/agents/areas/queue-shell.md`.
- Write `scratch-268/round-268.mjs` on `scripts/wayfinder-autopilot/capture-round.mjs`, copied and run from `/tmp/scratch229` via a `run-round.mjs` like `scratch-264/run-round.mjs` (copy with `fs.copyFileSync`, not `cp`). Find ids by SQL against devdb (memory: `docubite_local_db_access`): an exception check id + its invoice document id; an invoice id; a fake id for gone.
  States × 1440 + 390: `exceptions-pane-open` (click row; assert **Open on Invoices** in pane; probe `exceptions-open-on-invoices-focus` = Tab to it, Enter, settle, url contains `from=`) · `invoices-with-origin` (`/invoices/<docId>?from=<encoded /exceptions?doc=<checkId>>`; probes `strip-tab-first-focus` = first Tab inside `#main` lands on the strip `a[aria-label^="Back to"]`, `strip-enter-navigates` = Enter → url back to exceptions) · `search-origin` (`/invoices/<id>?from=<encoded /search?q=acme>`) · `standalone-with-from` (`/invoices/<id>?full=1&from=…`) · `standalone-no-from` (`?full=1`, strip says Back to Invoices, no ArrowLeft link in header) · `notice-filtered` (`/invoices/<id>?aging=90%2B`, case 1; probe `notice-show-it-focus`) · `notice-moved` (`/exceptions?doc=<invoiceDocId not an exception>`) · `notice-gone` (`/invoices?gone=x`) · `finance-failed-row` (`/finance`, Open on … link visible) · `gone-redirect` (`/documents/nonexistent?from=<encoded /invoices>` → lands on `/invoices?gone=nonexistent` with the notice) · `not-found` (`/documents/nonexistent`, h1 "That document was deleted.").
- Read `detector.json` + `keyboard.json`; fix real findings in place (no readers); write r0 counts per width here; `tsc --noEmit` once more only if code changed; stop servers (`dev.mjs stop`; kill the livesrv pid) → `milestone: build-done` → commit `wip(autopilot): #268 build`.

## Notes carried
- Typed deep links `invoices/<id>?full=1` on a missing doc with no `from` fall to the workspace-level `not-found.tsx` ("This page isn't here any more" / Go to Invoices) — inside the shell, so not the P1 class; note in the report as a B3 wording gap if the reader flags it.
- The Search page's own `from=` handling for a search hop: `LegacyDocumentPage` redirect carries `from` → typed queue page → `queueArrival` → strip. Verify in `search-origin`.
- Dev-server lessons: cold Turbopack compile 50–90 s per route; `tsc`/vitest only with the dev server stopped.
