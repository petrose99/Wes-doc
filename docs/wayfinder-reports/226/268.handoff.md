# Hand-off — #268 Build the Origin link (map #226)

milestone: spec-done
milestone: build-in-progress
milestone: build-done

## Phase state
- Spec done (sessions 1–2). Build done (sessions 3–6): every §8 file built, round r0 run at 1440 + 390, every Part B probe passes, `tsc --noEmit` clean (`scratch-268/tsc.txt` empty), `npx vitest run lib/navigation components/queue` → 51 pass. **Next session: MEASURE** — servers up, one round (`r1`, same script), the three readers on the contact sheet + `detector.json`, scores and triage here, `milestone: measured`. No fixes.
- Ticket claimed (assignee petrose99). Scratch: `docs/wayfinder-reports/226/logs/scratch-268/` — `spec.md`, `preflight.md` (A–E, gate passed: predicted critique 32/40, evaluate sum 6, P1 0, health ≈ 88, Clean), `part-b-checks.mjs`, `round-268.mjs` + `run-round.mjs` (`node run-round.mjs r1 [only]`), `summarize.mjs <round>` (per-state non-residue findings + keyboard log), `shots-r0/` (31 PNGs, `detector.json`, `keyboard.json`, `sheet.png`).

## Built (where)
1. `lib/navigation/origin.ts` (+13 tests) — `withOrigin`/`withParam`/`readOrigin`/`originLabel`/`labelForDestinationPath`.
2. `lib/navigation/origin-server.ts` — `describeOrigin` (Exceptions `/exceptions/<checkId>` origins resolve the check → document for the row segment, session 6), `describeMissingRow`, `describeFilteredRow` (session 6: spec §2.5 case 1 for queues that filter on the server — Invoices aging, Exceptions status, PO consumed — via `queueArrival`'s new `unfilteredRowIds`; text "… no longer matches these filters.", Show it drops the query).
3. `components/queue/origin-strip.tsx` (+4 tests); `queue-screen.tsx` `origin` slot + `initialMissing {text, showHref?, name?}` + Show it `aria-label="Show {name}"`.
4. Seven queue pages through `queueArrival`; Exceptions H-a/H-b (`Open on {label}` button + ⋯ item); Finance H-c (`accounting-dashboard.tsx` filename link + `Open on {label}` in the action cell, `whitespace-nowrap` since session 6); Search H-d.
5. `documents/[documentId]/page.tsx` strip above `PaneFrame`; `gone=` redirect; `not-found.tsx`; `PaneFrame` full-mode ArrowLeft link removed (§2.4). `CONTEXT.md` glossary line.

## r0 (build gate) — counts and probes
- States: exceptions-pane-open(+after-hop) · invoices-with-origin(+sheet-closed@390, +back) · search-origin · standalone-with-from · standalone-no-from · notice-filtered(+shown) · notice-moved · notice-moved-exceptions · notice-gone · finance-ready-row · gone-redirect · not-found.
- Non-residue detector counts (after the app-wide five): 1440 = 65, 390 = 71 across 15/16 states — **all pre-existing shell/adjacent-surface residue**: `ai-color-palette` "Cyan gradient" ×2 every state (rail avatar), `clipped-overflow-container` ×4–6 on every pane-open state (detail-pane primer: structural), `cramped-padding <div hidden>` on list states @1440, `text-overflow span.block.truncate` (rail workspace name), `line-length ~105` on standalone (PDF-fallback line), Finance page's own `nested-cards` ×4 / `low-contrast` emerald ×2 / `line-length` ×2 (#249 surface, untouched by this ticket beyond the link), `undersized-ui-text "19"` (rail Finance badge). **Zero findings on the strip, the notice, the Open-on controls, or not-found.**
- Probes (keyboard.json, both widths): Open on Invoices focus+Enter → `/invoices/<doc>?from=/exceptions/<check>` ✓ · strip label "Back to Exceptions, TechSupply Inc, TS-8834", first Tab in `#main` lands on it, Enter returns to `/exceptions/<check>` ✓ · Search strip `Back to Search, "acme"` ✓ · standalone with/without `from` → strip, one back link ✓ · filtered notice "… no longer matches these filters. Show it" → Show focus → `/invoices/<doc>` (no query) ✓ · moved "Acme Corp · PO-1042 is no longer on Invoices. Show it" ✓ · Exceptions moved "That exception is no longer open." ✓ · gone "That document was deleted." ✓ · Finance Open on Invoices href carries `from=…/finance` ✓ · gone-redirect → `/invoices?gone=<id>` + notice ✓ · not-found h1 + Tab → Back to Invoices ✓.
- Fixed in build from r0: server-filtered rows called "moved" (case 1 wording) · Exceptions origin had no row segment (check id ≠ doc id) · Finance link wrapped.

## For the measure/close readers (open, not fixed — decisions)
- 390 with a row selected: the phone pane sheet covers the strip; the sheet's own `Back to Invoices` closes it, then the strip is the first focusable (`strip-phone-sheet-back-390`). Two-step way back on phone; the list is scrolled to the row so the strip is above the fold. Spec §2.2 did not address the sheet. Reader to score; possible ticket (strip inside the phone sheet, or sticky).
- Typed deep link `invoices/<id>?full=1` on a missing doc with no `from` → workspace `not-found.tsx` (in shell) — B3 wording gap if flagged.
- Spec §7 unit tests for the queue-screen notice and the `gone=` route were not written; r0 probes cover them — say so in the report.

## Recipes
- Servers: `docker start docubite-devdb` · `node .impeccable/live/dev.mjs start` · `node .impeccable/live/livesrv257.mjs` (run_in_background, kill the printed pid at the end) · seed `npx tsx --env-file .env .impeccable/live/seed257b.ts` then `.impeccable/live/seed268.ts` (bigcapital connection + approved invoices + exception on an invoice; prints ids) · warm with `.impeccable/live/probe252.mjs <paths>` (cold compile 50–90 s). `.env` (git-ignored) now carries `SECRETS_ENCRYPTION_KEY` + `BIGCAPITAL_ENABLED=true` so `/finance` renders — without them the page is "The ledger isn't connected".
- Ids in `round-268.mjs` (EXC/EXC_DOC/INV/PO_DOC) match the seeded devdb. Contact sheet: copy `contact-sheet.mjs` to `/tmp/scratch229` via `fs.copyFileSync`, run there.
- `tsc`/vitest only with the dev server stopped.
