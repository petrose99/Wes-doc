# Hand-off — #268 Build the Origin link (map #226)

milestone: spec-done
milestone: build-in-progress

## Phase state
- Spec phase done (sessions 1–2). Sessions 3 and 4: **BUILD**, both cut at the context hand-off line before the build gate — not yet `build-done`. Next session: continue BUILD from the checklist below, then run the build gate (Part B checks, round r0 at 1440+390, `tsc --noEmit`) before writing `milestone: build-done`.
- Ticket claimed (assignee petrose99). Scratch: `docs/wayfinder-reports/226/logs/scratch-268/` — `spec.md` (screens §2, hops §2.6, §3.0 contract, copy §4, states §5, a11y §6, tests §7, files §8), `preflight.md` (A–E filled, gate passed: predicted critique 32/40, evaluate sum 6, P1 0, health ≈ 88, Clean), `inv.mjs` / `wire.mjs` (the edit scripts that did the wiring — already applied, don't rerun).
- **Not typechecked yet** — nothing below has been through `tsc` or vitest; session 4 was all mechanical edits. First thing: `npx tsc --noEmit -p . 2>&1 | grep -E "origin|queue|exception|accounting|search-client" ` (dev server stopped).

## Built so far (sessions 3–4)
1. `lib/navigation/origin.ts` — client-safe: `withOrigin` (2048 cap, drops outermost chained `from`), `withParam`, `readOrigin(searchParams, workspaceId?)`, `originLabel` (nine paths + `library: "Archive"` — `documentDestinationPath` can return `library/documents/<id>`, spec §2.4 was wrong to say only six), `Origin` type, `labelForDestinationPath`, re-export `documentDestinationPath`. **No prisma import** (client components import `withOrigin`).
2. `lib/navigation/origin-server.ts` — **new**, server half: `rowFromDocument`, `describeOrigin(workspaceId, from)`, `describeMissingRow(...)` (spec §2.5 cases 2/3), `queueArrival(workspaceId, { searchParams, queuePath, selectedId, rowIds, decidedText?, missingText? })` → `QueueArrival = { origin, initialMissing }`. Reads `gone=<id>` → "That document was deleted."
3. `components/queue/origin-strip.tsx` — new, per spec §2.1–2.3 (not yet seen rendered).
4. `components/queue/queue-screen.tsx` — `origin` prop → `<OriginStrip>` before `band`; `initialMissing?: { text; showHref? }`; missing-notice gate is now `initialMissing && (!initialSelectedId || !rows.some(...))` (so `gone=` works without a selected id); `filteredNotice` (case 1); `activeNotice` clears when `openId` moves off `initialSelectedId`; `fullHref` wrapped in `withOrigin(..., here)` (H-e).
5. Six `*-queue.tsx` components take `arrival?: QueueArrival` and pass `origin={arrival?.origin ?? null}` + `initialMissing={arrival?.initialMissing}` to `QueueScreen` (the Approvals hard-coded `initialMissing` moved to the page's `decidedText`).
6. Seven pages call `queueArrival`: `(queue)/{invoices,receipts,exceptions,approvals/invoices,approvals/po-mismatches}/page.tsx` and `(queue)/document-queue-page.tsx` (PO + bank statements). Exceptions uses `missingText: "That exception is no longer open."` (its row id is a check id, not a document id). Approvals `rowIds` = `row.documentId`.
7. H-a/H-b `components/queue/exception-queue.tsx`: `hop(row)` → `{ href: withOrigin(documentDestinationPath(...), here), label: "Open on {label}" }`; outline `Button asChild` `Link` in `paneActions` beside Start review, and `paneMenu={(row) => <PaneMenuItem href=…>}`.

## NOT yet done
- **H-c** `components/accounting/accounting-dashboard.tsx`: the failed-row filename `Link` is inside `ReadyToPushList` (function at :257, table row ~:393–444). Add `const here = useOriginHere()` (import from `@/components/documents/po-compare`) in `ReadyToPushList`, wrap the href in `withOrigin(...)`, add `aria-label={\`Open ${doc.filename} on ${originLabel(path) ?? "Archive"}\`}`, and a visible **Open on {label}** text link in the row's action cell (the `<td>` ending :443).
- **H-d** `app/(app)/workspaces/[workspaceId]/search/search-client.tsx` ~:101-104: replace `${base}/pipeline?doc=${item.documentId}` with `withOrigin(\`${base}/documents/${item.documentId}\`, here)`; `aria-label` "Open {title} on ‹queue›" when `item.docType` known (`originLabel(documentDestinationPath(base, {id, docType}))`), else "Open {title}".
- **`documents/[documentId]/page.tsx`**: strip at top of full mode (before `PaneFrame`) via `describeOrigin` when `from` valid, else `{ href: documentDestinationPath(document), label: originLabel(...) }`; pass `workspaceId` to the existing `readOrigin(query)` call (:213); §3.0: `LegacyDocumentPage` (bottom of file) forwards `from` on redirect; missing doc + valid `from` → `redirect(withParam(from, "gone", id))`; missing doc + no `from` → new `documents/[documentId]/not-found.tsx` (h1 "That document was deleted.", body "It isn't in this workspace any more.", link "Back to Invoices").
- `components/queue/detail-pane.tsx` `PaneFrame` full-mode `ArrowLeft` back link (~:99) → remove (affordance swap, spec §2.4).
- `CONTEXT.md` glossary line: **Origin** — the URL a cross-surface hop came from, carried as `from=`.
- Tests (spec §7): `lib/navigation/origin.test.ts`, `components/queue/origin-strip.test.tsx`, `queue-screen.test.tsx` additions, route test, `grep -rn "pipeline?doc" components app` → 0.
- Build gate: Part B checks → `npx vitest run components/queue lib/queue lib/navigation` → round r0 script on `scripts/wayfinder-autopilot/capture-round.mjs` (states: invoices-with-origin, exceptions pane with Open on Invoices, search origin, standalone with/without from, notice cases 1–3, 1440+390; probes `strip-tab-first-focus`, `strip-enter-navigates`, `notice-show-it-focus`, `exceptions-open-on-invoices-focus`) → fix → `tsc --noEmit` → stop servers → `milestone: build-done` → commit `wip(autopilot): #268 build`.

## Notes carried
- Spec §3.0 (gone= redirect + not-found.tsx) is the critic's only P1 — still open.
- PaneFrame full-mode back link removal is an affordance swap (spec §2.4), not a #237 removal.
- Dev server / seed / capture recipe: `docs/agents/areas/queue-shell.md`.
