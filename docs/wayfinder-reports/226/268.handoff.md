# Hand-off — #268 Build the Origin link (map #226)

milestone: spec-done
milestone: build-in-progress
milestone: build-done
milestone: measured

## Phase state
- Spec done (sessions 1–2). Build done (sessions 3–6): every §8 file built, round r0 run at 1440 + 390, every Part B probe passes, `tsc --noEmit` clean (`scratch-268/tsc.txt` empty), `npx vitest run lib/navigation components/queue` → 51 pass.
- **Measured (session 7).** Servers restarted clean (`.next` was corrupt from a prior Turbopack panic — `rm -rf .next` before `dev.mjs start` fixed it), devdb re-seeded (`seed257b.ts` then `seed268.ts` — ids in `round-268.mjs` already matched the fresh seed output, no edits needed), live-server restarted on :8400. Round `r1` run with the existing `run-round.mjs`/`round-268.mjs` unchanged — non-residue detector counts are **identical to r0** (65@1440, 71@390, same pre-existing shell/#249 residue types, zero findings on the strip/notice/Open-on/not-found controls) and all keyboard probes match r0's shape. Contact sheets built via `scripts/wayfinder-autopilot/contact-sheet.mjs` (copied into `/tmp/scratch229`, explicit file lists per width — the script has no `--filter` flag) → `shots-r1/sheet-1440.png`, `shots-r1/sheet-390.png`. Three fresh-context `sonnet` Agent readers ran against the r1 set (contact sheets + `detector.json` + `keyboard.json`), told to score independently rather than anchor to the preflight prediction.
- **Next session: CLOSE.** Bar is already met on this first pass (see Scores below) — no P1s, nothing under 3, health ≥85, keyboard PASS. Per the closing-bar rule ("Confirm under the bar with no P1s: close and record the gap" doesn't apply — this *met* the bar first pass) the close session should: do a **light polish pass only** on the two named non-blocking gaps below if cheap (H4 label-density normalization, H8 Finance link visual weight — flag as #249 follow-up per the residue note if not cheap to touch here), run `tsc`/`eslint`/full vitest/`next build` once with the dev server stopped, refresh/write the queue-shell area primer, write the ticket resolution + close, append Decisions-so-far on the map, and file the report at `docs/wayfinder-reports/226/268.md`.

## Scores (round r1, independent fresh-context reads)
- **critique: 32/40** — H1=4, H5=4, all others=3, **none below 3**. Bar (≥32/40, no heuristic <3) **met**. Same sum as the spec-time prediction, reasoned fresh from the capture (H4/H9 landed on 3 for different concrete reasons than predicted — see reader detail below).
- **evaluate: health 91/100**, issue sum 3 (H1:1, H7:1, H8:1), **P0 0, P1 0**, verdict **Clean** (anti-pattern sweep negative). Bar (zero P0/P1, health ≥85, Clean) **met**. Predicted was sum 6/health≈88 — actual is slightly better, different issue mix.
- **include (keyboard): PASS at both 1440 and 390.** Strip/hop/Show-it controls all keyboard-reachable with specific accessible names, Enter activates correctly, arrival focus sane, no traps. Bar (keyboard flow verified live both widths) **met**.
- scores: predicted-critique=32 first-critique=32 first-evaluate-health=91 first-evaluate-sum=3

### Reader detail (non-blocking gaps, not findings — carry to close as polish or #249 follow-up)
- **H3/H9 (critique) + H1 (evaluate), 390px:** the phone detail sheet covers the strip when a row is selected; user must close the sheet ("Back to Invoices") first, then the strip is the first focusable control for the second hop back. All three readers judged this **acceptable** (include: "nested-context pattern, not a finding"; evaluate: "friction, not asymmetric friction, scored as H1/H7 not a dark pattern"; critique: not flagged as a defect) — known/accepted since r0, spec §2.2 didn't address the sheet. No action required to close.
- **H3/H9 (critique):** "Show it" and the standalone not-found page both drop the `from` param, so the way back to origin after using them is browser-history-only, not a visible affordance. Spec §2.5/B1 discloses this as a deliberate design choice (Show it drops `from` by design). Not a P1; optional polish for close if cheap, otherwise a #249-adjacent follow-up ticket, not a blocker.
- **H4 (critique), H8 (evaluate):** "Back to Invoices" (bare) vs. "Back to Exceptions, Vendor, ID" (row-qualified) is an inconsistent label density between strip variants; and Finance's new "Open on Invoices" link sits inside an already-dense row (nested-cards/low-contrast from #249) without distinct visual weight. Both flagged as **Finance-page (#249) follow-up, not an #268 fix** — the residue there is pre-existing and only touched by adding one link.
- **H8 (evaluate) text-overflow:** `h2...truncate` overflow at 390 on search-origin, and a row-label overflow at 1440 on finance-ready-row — same detector findings present unchanged in r0, pre-existing (search header / Finance row), not introduced by this build.

## Detector triage (real, non-residue findings — all pre-existing, none attributable to this ticket)
Unchanged from r0's characterization: `ai-color-palette` rail avatar ×2/state, `clipped-overflow-container` on the detail-pane split (structural primer, ×4–6 on pane-open states), `cramped-padding` on list states @1440, `text-overflow` (rail workspace name @1440; search header @390), `line-length` on standalone (PDF-fallback line) and on Finance (#249), Finance's own `nested-cards` ×4 / `low-contrast` emerald ×2 (#249), `undersized-ui-text "19"` (rail Finance badge). **Zero findings on the strip, the notice, the Open-on controls, or not-found** — confirmed again in r1.
- Ticket claimed (assignee petrose99). Scratch: `docs/wayfinder-reports/226/logs/scratch-268/` — `spec.md`, `preflight.md` (A–E, gate passed: predicted critique 32/40, evaluate sum 6, P1 0, health ≈ 88, Clean), `part-b-checks.mjs`, `round-268.mjs` + `run-round.mjs` (`node run-round.mjs r1 [only]`), `summarize.mjs <round>` (per-state non-residue findings + keyboard log), `shots-r0/` (31 PNGs, `detector.json`, `keyboard.json`, `sheet.png`).

## Built (where)
1. `lib/navigation/origin.ts` (+13 tests) — `withOrigin`/`withParam`/`readOrigin`/`originLabel`/`labelForDestinationPath`.
2. `lib/navigation/origin-server.ts` — `describeOrigin` (Exceptions `/exceptions/<checkId>` origins resolve the check → document for the row segment, session 6), `describeMissingRow`, `describeFilteredRow` (session 6: spec §2.5 case 1 for queues that filter on the server — Invoices aging, Exceptions status, PO consumed — via `queueArrival`'s new `unfilteredRowIds`; text "… no longer matches these filters.", Show it drops the query).
3. `components/queue/origin-strip.tsx` (+4 tests); `queue-screen.tsx` `origin` slot + `initialMissing {text, showHref?, name?}` + Show it `aria-label="Show {name}"`.
4. Seven queue pages through `queueArrival`; Exceptions H-a/H-b (`Open on {label}` button + ⋯ item); Finance H-c (`accounting-dashboard.tsx` filename link + `Open on {label}` in the action cell, `whitespace-nowrap` since session 6); Search H-d.
5. `documents/[documentId]/page.tsx` strip above `PaneFrame`; `gone=` redirect; `not-found.tsx`; `PaneFrame` full-mode ArrowLeft link removed (§2.4). `CONTEXT.md` glossary line.

## r0 (build gate) — counts and probes
- States: exceptions-pane-open(+after-hop) · invoices-with-origin(+sheet-closed@390, +back) · search-origin · standalone-with-from · standalone-no-from · notice-filtered(+shown) · notice-moved · notice-moved-exceptions · notice-gone · finance-ready-row · gone-redirect · not-found.
- Non-residue detector counts (after the app-wide five): 1440 = 65, 390 = 71 across 15/16 states — **all pre-existing shell/adjacent-surface residue**: `ai-color-palette` "Cyan gradient" ×2 every state (rail avatar), `clipped-overflow-container` ×4–6 on every pane-open state (detail-pane primer: structural), `cramped-padding <div hidden>` on list states @1440, `text-overflow span.block.truncate` (rail workspace name), `line-length ~105` on standalone (PDF-fallback line), Finance page's own `nested-cards` ×4 / `low-contrast` emerald ×2 / `line-length` ×2 (#249 surface, untouched by this ticket beyond the link), `undersized-ui-text "19"` (rail Finance badge). **Zero findings on the strip, the notice, the Open-on controls, or not-found.**
- Probes (keyboard.json, both widths): Open on Invoices focus+Enter → `/invoices/<doc>?from=/exceptions/<check>` ✓ · strip label "Back to Exceptions, TechSupply Inc, TS-8834", first Tab in `#main` lands on it, Enter returns to `/exceptions/<check>` ✓ · Search strip `Back to Search, "acme"` ✓ · standalone with/without `from` → strip, one back link ✓ · filtered notice "… no longer matches these filters. Show it" → Show focus → `/invoices/<doc>` (no query) ✓ · moved "Acme Corp · PO-1042 is no longer on Invoices. Show it" ✓ · Exceptions moved "That exception is no longer open." ✓ · gone "That document was deleted." ✓ · Finance Open on Invoices href carries `from=…/finance` ✓ · gone-redirect → `/invoices?gone=<id>` + notice ✓ · not-found h1 + Tab → Back to Invoices ✓.
- Fixed in build from r0: server-filtered rows called "moved" (case 1 wording) · Exceptions origin had no row segment (check id ≠ doc id) · Finance link wrapped.

## For the measure/close readers (open, not fixed — decisions)
- 390 with a row selected: the phone pane sheet covers the strip; the sheet's own `Back to Invoices` closes it, then the strip is the first focusable (`strip-phone-sheet-back-390`). Two-step way back on phone; the list is scrolled to the row so the strip is above the fold. Spec §2.2 did not address the sheet. Reader to score; possible ticket (strip inside the phone sheet, or sticky).
- Typed deep link `invoices/<id>?full=1` on a missing doc with no `from` → workspace `not-found.tsx` (in shell) — B3 wording gap if flagged.
- Spec §7 unit tests for the queue-screen notice and the `gone=` route were not written; r0 probes cover them — say so in the report.

## Recipes
- Servers: `docker start docubite-devdb` · `node .impeccable/live/dev.mjs start` · `node .impeccable/live/livesrv257.mjs` (run_in_background, kill the printed pid at the end) · seed `npx tsx --env-file .env .impeccable/live/seed257b.ts` then `.impeccable/live/seed268.ts` (bigcapital connection + approved invoices + exception on an invoice; prints ids) · warm with `.impeccable/live/probe252.mjs <paths>` (cold compile 50–90 s). `.env` (git-ignored) now carries `SECRETS_ENCRYPTION_KEY` + `BIGCAPITAL_ENABLED=true` so `/finance` renders — without them the page is "The ledger isn't connected".
- Ids in `round-268.mjs` (EXC/EXC_DOC/INV/PO_DOC) match the seeded devdb. Contact sheet: copy `contact-sheet.mjs` to `/tmp/scratch229` via `fs.copyFileSync`, run there.
- `tsc`/vitest only with the dev server stopped.
