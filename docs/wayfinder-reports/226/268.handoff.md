# Hand-off — #268 Build the Origin link (map #226)

phase: spec (1 of 4) — NOT finished; hit the 150K context line during code inspection. No `milestone:` line yet.
Next session: SPEC phase again. Read this file, then write the spec + preflight from the facts below without re-reading the codebase (the inspection is done and recorded here).

## Done this session
- Claimed #268 (assignee petrose99). Scratch folder created: `docs/wayfinder-reports/226/logs/scratch-268/` (holds `read.mjs`, `read2.mjs` — one-shot readers; `rg` is NOT installed, use the Grep tool or `grep`).
- Skills loaded: intent → specify, fortify, articulate, include; impeccable shape (`impeccable context` cannot run unattended — read PRODUCT.md directly). craft-floor.md read. Both lessons files read. Queue-shell primer read (`docs/agents/areas/queue-shell.md`). #244 resolution read (14 decisions + Action Summary: specify → fortify → include; layout → clarify → polish).
- Not done: spec file, preflight.md A–E, spec critic (Part D), gate. Nothing built.

## Facts established (the spec is written from these)
- Seam exists: `lib/navigation/origin.ts` — `withOrigin(href, origin)`, `readOrigin(searchParams)` (validates only `/` prefix, not `//`; **must be tightened to same-workspace `/workspaces/<id>/…`**, cap 2048 chars). `useOriginHere()` in `components/documents/po-compare.tsx:57` = current path+query on the client. Hops already wired with `from=`: PO chip, line-items sibling invoices, PO consumption panel, Bill Pay / batch / create-batch links.
- Standalone page `app/(app)/workspaces/[workspaceId]/documents/[documentId]/page.tsx:209-213,367`: `queueHref = readOrigin(query) ?? typedDestination-without-row-id`; passed as `backHref` to `PaneFrame mode="full"` (`components/queue/detail-pane.tsx:99` renders an ArrowLeft icon link aria-label "Back to queue"). `/pipeline` fallback there is already gone (#249). Legacy route at :300-313 redirects and forwards `from`.
- Remaining `/pipeline` links to remove in this ticket: `app/(app)/workspaces/[workspaceId]/search/search-client.tsx:103-104` (`ResultRow` → `${base}/pipeline?doc=`). `SearchResultItem` (`lib/global-search.ts:65`) has no docType → link to `${base}/documents/${documentId}` (legacy route resolves type + forwards `from`) with `from=` the current Search URL. (Dashboard/report `/pipeline?stage=` links are out of this ticket's scope — not document hops.)
- `QueueScreen` (`components/queue/queue-screen.tsx`): root at :424; `band` slot :426 sits above header row 1 (:428 `div.flex.flex-wrap … border-b px-4 py-2`) — the strip is a new slot rendered before `band`. `#main` is `app/(app)/workspaces/[workspaceId]/layout.tsx:99` (`div#main[role=main] tabIndex=-1`); MobileTabBar precedes it in DOM. Existing missing-row notice: `initialMissingNotice` string prop (:158,:209-210, rendered :479 as `<p role="status">` under the header) — used by approval-invoice-queue.tsx:152 and po-mismatch-queue.tsx:129 ("This invoice was already decided — it's no longer in Ready to Approve."). Filtering is client-side (`filterRows`, :214) so "row filtered out" = row in `rows` but not in `visibleRows` (client knows its name); "row gone" = not in `rows` (server lookup for the name). `syncUrl` :265 keeps `window.location.search` → `from` survives row stepping. Pane `fullHref` default :588 `${basePath}/${id}?full=1` → must gain `from=` current queue URL. Escape/× :358 close pane only.
- Exceptions: `components/queue/exception-queue.tsx` paneActions :121-137 (status span · Start review · Resolve popover); no in-tab link yet (only ⋯ Open in a new tab via `fullHref` :104 `${documentBasePath}/${row.documentId}?full=1`). Row has `documentId`, `docTypeLabel`; label the link `Open on ‹queue›` from the doc type (Invoices/Receipts/…); href = `withOrigin(documentDestinationPath(...), useOriginHere())`.
- Finance failed row: `components/accounting/accounting-dashboard.tsx:399` filename `<Link>` → `documentDestinationPath(...)` bare; becomes *Open on ‹queue›* with `from=` (useOriginHere; the component is a client component with `useState`).
- Rail label source for the strip (B3, one map): `SHORTCUT_DESTINATIONS` in `components/shell/keyboard-shortcuts.tsx:14` + sidebar labels (`sidebar.tsx:131-167`: Invoices, Purchase Orders, Receipts, Bank Statements, Exceptions, Approvals, Finance, Admin). Search label "Search". Put `originLabel(path)` beside `readOrigin` in `lib/navigation/origin.ts`.
- Detector residue + capture recipe: queue-shell primer (dev server `.impeccable/live/dev.mjs`, seed257b, round on `capture-round.mjs`, contact-sheet). Tests: `npx vitest run components/queue lib/queue lib/navigation`.

## Decisions drafted (recommended answers; ground = #244 §n, primer, craft floor)
1. Strip = `components/queue/origin-strip.tsx`, props `{ href, label, row?: {title, suffix}, search?: {q, filterCount} }` → text `← Back to {label}[ · {title}[ · {suffix}]]` / `← Back to Search · "acme" · 2 filters`. One `<a>` (Next `Link`), whole strip is the target; `h-8` desktop, `min-h-11` `<md`; `text-sm text-slate-700`, hairline `border-b border-slate-200`, bg `white`; `←` is lucide `ArrowLeft` (craft floor: no unicode glyph as icon); visible text = accessible name (no aria-label). First focusable in `#main` = rendered before `band` in QueueScreen and before `PaneFrame` on the standalone page. (#244 §7,§13,§14)
2. Origin description resolved **server-side** on each host page: `describeOrigin(workspaceId, from)` → label from path map; row name by `prisma.document` lookup of the trailing id when the path is `<queue>/<id>` (deleted/foreign id → strip without the row segment); Search: `q` + count of facet params. Passed as `origin` prop to `*Queue` → `QueueScreen`. (smallest reversible: no client fetch)
3. Standalone page: strip always renders — with `from` (origin), else `documentDestinationPath(document)` labelled by the doc's own queue — and `PaneFrame` full mode no longer renders the ArrowLeft link (same feature, one control; flag to owner in report as an affordance swap, not a removal).
4. Missing-row notice generalised: `initialMissing?: { text, showHref? }` on QueueScreen; filtered-out (client) `Acme Corp · INV-1042 no longer matches these filters · Show it` (→ `${basePath}/${id}`, no query); gone from queue (server) `Acme Corp · INV-1042 is no longer on Exceptions · Show it` (→ documentDestinationPath); deleted `… was deleted.` no link. Approvals keeps its sentence + gains Show it. `role="status"`.
5. `from` validation: `/workspaces/${workspaceId}/` prefix, no `//`, no `..`, ≤ 2048 chars, else ignored (strip hidden). Verbatim chaining allowed (an origin URL may itself carry `from`).
6. No new key; Esc/× unchanged; rail links bare; nothing in session storage. (#244 §5,§11,§13)
7. Part A targets: H1 (notice + strip state always true to URL) and H5 (validation; Show it never drops the pane silently). Hops list for B1: Exceptions pane link+⋯, Finance failed row, Search row, pane *Open in a new tab* (`fullHref` + from), standalone Back.

## Next step (exact)
Write `logs/scratch-268/spec.md` (screens: queue strip 1440/390, standalone, notice ×3, copy matrix, state inventory: valid · none · foreign/tampered · filtered out · gone · deleted · refresh new tab · Back after Resolve · long vendor name → truncate with `title`, row segment `truncate`, never wrap the strip past one line) and `logs/scratch-268/preflight.md` A–E from decisions 1–7; run the Part D critic (`Agent`, sonnet, foreground); gate; then write `milestone: spec-done`, commit `wip(autopilot): #268 spec`.
