# Hand-off — #268 Build the Origin link (map #226)

milestone: spec-done
milestone: build-in-progress

## Phase state
- **Spec phase done** (sessions 1–2). This session (3): **BUILD**, cut short at the context hand-off line before the build gate (round r0) — not yet `build-done`. Next session: continue BUILD from the checklist below, then run the build gate (Part B checks, round r0 at 1440+390, `tsc --noEmit`) before writing `milestone: build-done`.
- Ticket claimed (assignee petrose99). Scratch: `docs/wayfinder-reports/226/logs/scratch-268/` — `spec.md` (full spec, screens §2, copy §4, state inventory §5, a11y §6, tests §7, files §8), `preflight.md` (A–E filled, gate passed: predicted critique 32/40, evaluate sum 6, P1 0, health ≈ 88, Clean).

## What is built and verified so far (this session)
1. `lib/navigation/origin.ts` — **rewritten**: `readOrigin(searchParams, workspaceId?)` tightened (same-workspace prefix when `workspaceId` passed, no `//`/`\`/`..`, ≤2048); `originLabel(path)` (nine paths → sidebar label, else null); `describeOrigin(workspaceId, from)` (async, server — Search q/filterCount, `<queue>/<id>` or `?doc=` document lookup via `prisma.document` for `{title, suffix}`, never throws); `withOrigin` now caps chained `from` at 2048 (drops outermost first); added `withParam` (for the §3.0 `gone=` redirect, not yet used); re-exports `documentDestinationPath`. **Not yet done: `origin.test.ts`** (spec §7) — no test file written yet, and the existing call sites of `readOrigin(query)` (documents/[documentId]/page.tsx:213) don't pass `workspaceId` yet — check whether that changes its validation behavior for that call (it still works, just doesn't enforce the same-workspace check there — spec wants it).
2. `components/queue/origin-strip.tsx` — **new**, built per spec §2.1–2.3 (Link, ArrowLeft, `min-h-11 md:h-8`, truncate row segment, shrink-0 label/suffix, aria-label comma-joined, no `sr-only`). Not yet visually verified (no dev server run this session).
3. `components/queue/queue-screen.tsx` — wired: new `origin?: Origin | null` prop, `<OriginStrip origin={origin} />` rendered first, before `band` (spec §2.1, before old line 425). `initialMissingNotice: string` → `initialMissing?: { text; showHref? }`; added `filteredNotice` (useMemo, spec §2.5 case 1: row in `rows` but not `visibleRows` → notice with `Show it` → `${basePath}/${id}`, no query); render block for the notice now shows `activeNotice.text` + conditional `Show it` link. `fullHref` on `DetailPane` now wrapped in `withOrigin(..., here)` where `here` = current pathname+search (H-e). **Not yet done:** none of the 7 queue pages/components actually pass an `origin` prop yet — the slot exists but nothing feeds it.
4. `components/queue/approval-invoice-queue.tsx`, `components/queue/po-mismatch-queue.tsx` — updated their `initialMissingNotice="…"` call to the new `initialMissing={{ text: "…" }}` shape. That's the only change in these two files so far.

## NOT yet done (the rest of the build checklist, spec §8)
- **Wire `origin` through the 7 `(queue)/*/page.tsx` pages**: each needs `describeOrigin(workspaceId, readOrigin(searchParams, workspaceId))` called server-side and the result passed down through its `*Queue` component to `QueueScreen`'s new `origin` prop. Files: `(queue)/invoices/page.tsx` → `InvoiceQueue` (`components/queue/invoice-queue.tsx`), `(queue)/purchase-orders/page.tsx` → likely `document-queue.tsx`, `(queue)/receipts/page.tsx` → `receipt-queue.tsx`, `(queue)/bank-statements/page.tsx` → `document-queue.tsx`, `(queue)/exceptions/page.tsx` → `exception-queue.tsx`, `(queue)/approvals/invoices/page.tsx` → `approval-invoice-queue.tsx`, `(queue)/approvals/po-mismatches/page.tsx` → `po-mismatch-queue.tsx`. Also server notice for `?doc` not in rows per spec §2.5 case 2/3 (moved/deleted) — not started; currently only the client-derived filtered case (1) and the existing Approvals "already decided" case exist.
- **`documents/[documentId]/page.tsx`**: add the strip at the top of full mode (before `PaneFrame`), build its `origin` via `describeOrigin` when `from` is valid, else fall back to `documentDestinationPath(document)` + `originLabel` (spec §2.4). **§3.0 contract not implemented**: `LegacyDocumentPage` (default export, bottom of file) still only forwards `stage/page/bb` on redirect and calls bare `notFound()` on a missing doc in both legacy and full branches — needs: forward `from` too; on missing doc with valid `from` → `redirect(withParam(from, "gone", id))`; on missing doc with no `from` → new `documents/[documentId]/not-found.tsx` (not created yet) instead of framework 404. The origin queue then needs to read `gone=<id>` and render notice case 3 ("That document was deleted.").
- **`components/queue/detail-pane.tsx`**: `PaneFrame` full-mode still renders its own `ArrowLeft` back `<Link href={backHref}>` (line ~99) — spec §2.4 decides this is removed once the OriginStrip is the one back affordance on the standalone page. Not touched yet.
- **Hops (spec §2.6)**: none of H-a through H-d implemented yet.
  - H-a/H-b `components/queue/exception-queue.tsx`: needs an **Open on {label}** control in `paneActions` (beside Start review/Resolve) and in the pane's ⋯ menu, using `withOrigin(documentDestinationPath(...), useOriginHere())`. `useOriginHere()` already exists at `components/documents/po-compare.tsx:57` — reuse it, don't reinvent.
  - H-c `components/accounting/accounting-dashboard.tsx` (~line 399): failed-row filename link exists already (`documentDestinationPath`) but has no `from=` and no visible "Open on Invoices" text link — needs `withOrigin` + `useOriginHere()` (file is already `"use client"`).
  - H-d `app/(app)/workspaces/[workspaceId]/search/search-client.tsx` (~line 101-104): `ResultRow` still links to the dead `${base}/pipeline?doc=${item.documentId}` — replace with `withOrigin(`${base}/documents/${id}`, here)`. Needs a `docType`-aware `aria-label` too (spec H-d). `item.docType` already exists on `SearchResultItem` (lib/global-search.ts).
- **CONTEXT.md glossary line** for "Origin" — not added.
- **`docs/agents/areas/queue-shell.md`** — refresh, close-phase only, not this session's job.
- **Tests (spec §7)** — none written: `lib/navigation/origin.test.ts`, `components/queue/origin-strip.test.tsx`, updates to `components/queue/queue-screen.test.tsx` (check if that file exists — grep first), the `documents/[documentId]` route test, the `grep -rn "pipeline?doc" components app` → 0 check.
- **Build gate** (Part B checks, `npx vitest run components/queue lib/queue lib/navigation`, round r0 at 1440+390, `tsc --noEmit`) — none run this session. Do this only after the above is code-complete.

## Next step for the continuing session
1. Read this file, then `spec.md` §2.6 (hops table) and §8 (file list) again — they're the checklist.
2. Wire the 7 queue pages (grep each `(queue)/*/page.tsx` for its `*Queue` component import, add `describeOrigin` call + `origin` prop threading — mechanical, same pattern each time).
3. Implement the 4 hops (H-a/b in exception-queue.tsx, H-c in accounting-dashboard.tsx, H-d in search-client.tsx).
4. `documents/[documentId]/page.tsx`: add the strip + `origin` in full mode; implement §3.0 (gone= redirect + not-found.tsx); drop `PaneFrame`'s full-mode back link in `detail-pane.tsx`.
5. Write the tests from spec §7.
6. Add the CONTEXT.md glossary line.
7. Run the Part B contract checks (reachability grep, string extraction, primitive diff, focus probe), then `npx vitest run components/queue lib/queue lib/navigation`, then write/run the round-r0 capture script per the queue-shell primer, fix findings, `tsc --noEmit` once, stop servers, write `milestone: build-done` to this file, commit `wip(autopilot): #268 build`.

## Hand-off notes carried from the spec phase (still true)
- The legacy route "forwards `from`" claim from an earlier session was **wrong** — it only forwards `stage/page/bb` and calls `notFound()` on a missing doc. Spec §3.0 is the contract that closes this (the critic's only P1) — see "NOT yet done" above, still open.
- PaneFrame full-mode back link removal is a decided **affordance swap** (spec §2.4), not a removal under #237 — say so in the close report.
