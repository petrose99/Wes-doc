# Hand-off — #268 Build the Origin link (map #226)

milestone: spec-done

## Phase state
- **Spec phase done** (sessions 1–2). Next: **BUILD** phase, fresh session, from the tables below. No dev server, no browser yet.
- Ticket claimed (assignee petrose99). Scratch: `docs/wayfinder-reports/226/logs/scratch-268/` — `spec.md` (screens, model, copy matrix, state inventory, a11y, tests, files), `preflight.md` (A–E filled, Part D critic reconciled, gate passed: predicted critique 32/40 with H1/H5 = 4, evaluate sum 6, P1 0, health ≈ 88, Clean).
- Skills: intent specify/fortify/articulate/include; impeccable shape (launcher can't run unattended; PRODUCT.md read directly). craft-floor read; both lessons files read; queue-shell primer read.

## Build checklist (spec §8 + preflight Part B; tick as they land)
1. `lib/navigation/origin.ts`: tighten `readOrigin` (same-workspace `/workspaces/<id>/` prefix, no `//`, no `..`, ≤ 2048); add `originLabel(path)` (nine paths, sidebar strings) and `describeOrigin(workspaceId, from)` (server; optional `prisma.document` lookup for `<queue>/<id>` / `?doc=`; Search: `q` + facet count). Tests in `origin.test.ts` incl. label-map covers every `documentDestinationPath` output.
2. `components/queue/origin-strip.tsx` (new): one `Link`, lucide `ArrowLeft`, `h-8 md` / `min-h-11 <md`, `text-sm text-slate-700`, `border-b border-slate-200 bg-white`, `whitespace-nowrap`; title segment `truncate`, label + identifier suffix `shrink-0`; `aria-label` = visible text comma-joined; separators `aria-hidden`.
3. `queue-screen.tsx`: `origin?` slot rendered **before `band`** (:426); `initialMissingNotice: string` → `initialMissing?: { text, showHref? }` with **Show it** link; client filtered-out notice from `rows` vs `visibleRows` (`… no longer matches these filters.` → `${basePath}/${id}`); `fullHref` (:588) gains `from=` current URL; `gone=<id>` param → notice `That document was deleted.`
4. Seven `(queue)/*/page.tsx`: call `describeOrigin`, pass `origin`; server notice for `?doc` not in rows (moved → `… is no longer on {label}.` + Show it → `documentDestinationPath`; deleted → case 3). Approvals callers switch to the new `initialMissing` shape.
5. `documents/[documentId]/page.tsx`: strip at top (with `from` → origin; else `documentDestinationPath(document)` labelled by `originLabel`); `PaneFrame` full mode drops its ArrowLeft link (`detail-pane.tsx:99`); legacy branch forwards `from`; missing doc + valid `from` → `redirect(from + gone=id)`; new `not-found.tsx` beside it (`That document was deleted.` / `It isn't in this workspace any more.` / link `Back to Invoices`).
6. Hops: `exception-queue.tsx` paneActions **Open on ‹queue›** (`docTypeLabel` → label) + ⋯ item, `from=` via `useOriginHere()`; `accounting-dashboard.tsx:399` failed-row link → `withOrigin` + visible **Open on Invoices**; `search/search-client.tsx:103` row → `${base}/documents/${id}` + `from=` (no `/pipeline?doc=` left — grep must be empty).
7. `CONTEXT.md` glossary: **Origin** — the URL a cross-surface hop came from, carried as `from=`.
8. Before build-done: Part B checks (reachability grep, string extraction for B3 — no "View"/"Go to"/"Return"/"previous page", primitive diff, no `sr-only` in buttons), `npx vitest run components/queue lib/queue lib/navigation`, `tsc --noEmit` once (dev server stopped).

## Measure phase pointers
- States (× 1440/390): Exceptions list + pane (Open on Invoices visible) · Invoices arrived from Exceptions (strip) · Invoices arrived from Search (`"acme" · 2 filters`) · standalone `?full=1` with and without `from` · notice filtered / moved / deleted · long vendor name · tampered `from` (no strip). Seed `seed257b`; dev-server/capture recipe in `docs/agents/areas/queue-shell.md`; round on `scripts/wayfinder-autopilot/capture-round.mjs`, reuse `scratch-257/pw/round261-r2.mjs` shape.
- Keyboard probes per width: `strip-tab-first-focus`, `strip-enter-navigates`, `notice-show-it-focus`, `exceptions-open-on-invoices-focus`.
- Detector residue: the queue-shell primer's five app-wide + `em-dash-overuse` on Receipts.

## Hand-off notes from the spec phase
- Hand-off session 1 claimed the legacy route "forwards `from`" — **wrong**: it forwards only `stage/page/bb` and calls `notFound()` on a missing doc (bare 404). Spec §3.0 is the contract that closes this (the critic's only P1).
- PaneFrame full-mode back link removal is decided in the spec as an affordance swap (same feature, one control) — say so in the close report so the owner sees it.
