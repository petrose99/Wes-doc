# #273 hand-off — Build expense claims in place

milestone: spec-done

Phase SPEC complete (sessions 1–2, 2026-09-17). Next phase: **BUILD** — fresh session, no capture, builds every surface from the pre-flight tables.

milestone: build-partial (session 3, 2026-09-17)

**Server layer (S7) only — no UI surface built yet.** This session spent its whole budget on
`lib/claims/*` + the `models/expense-claims.ts` rewrite, which every S1–S6 surface depends on;
none of S1–S6 exist in the tree yet. Do not mark `build-done` or run a capture round — there is
nothing to capture. The next BUILD session continues from here; it does not re-derive the server
contracts below, but does still need to build the UI before any capture.

### Done this session (committed)
- `lib/claims/labels.ts` — `CLAIM_STATUS_LABELS`, `claimName`, `ELIGIBILITY_REASON_TEXT`.
- `lib/claims/eligibility.ts` — `claimEligibility(facts)`, the single reachable/row/server function spec §2 demands.
- `lib/claims/totals.ts` — `sumReceiptTotals` (integer cents via `lib/money.ts`; per-currency + `mixed`).
- `lib/claims/refusals.ts` — `claimRefusalSentence(code, merchant)` for every server error code named in spec §2/§B1.
- `models/expense-claims.ts`: `validateClaimableDocuments` rewritten onto `claimEligibility` (was: template-code + naive "any ExpenseClaimItem exists" check, which wrongly blocked re-claiming after rejection — now reads the *latest* claim's status per document, and derives `processingState` via `lib/documents/processing-state.ts` + `documentCheckResult` for the escalation input); `submitExpenseClaim` now re-validates every item through the same eligibility (excluding this claim) and uses `sumReceiptTotals` — refuses `expense_claim_mixed_currency` / `expense_claim_no_amounts`; added `withdrawExpenseClaim` (submitted → draft, refuses once any stage has decided, resets the freeze fields); added `listExpenseClaimRows` (submitted claims, S4's row shape, `firstDocumentId`/`claimant`/`total` etc. — approval-stage filtering/`canDecide` is NOT yet done, that's `lib/approvals/filters.ts`'s `filterExpenseClaimRows`, still to write).
- `models/expense-claims.test.ts` updated for the new query shape (`doc()` fixture helper, `documentCheckResult.findMany` mock). All 30 tests pass. `tsc --noEmit` clean repo-wide.

### Not started (everything else in spec.md / preflight.md)
- `lib/claims` still needs: nothing else pure-server is blocking; `getExpenseClaimDetailAction` / claim facts projection for `DocumentHistory.facts.claim` (spec §2 the big object) is NOT written — S3 cannot render without it.
- `app/(app)/workspaces/[workspaceId]/expense-claim-actions.ts`: still the OLD action set (create/delete/add/remove/submit/decide only). Needs: `addToExpenseClaimAction` (dialog shape, target new|existing, `heldBack[]` result), `withdrawExpenseClaimAction`, `listMyDraftClaimsAction`, `getExpenseClaimDetailAction`, `decideExpenseClaimAction` reason param for reject (currently no reason arg), `listExpenseClaimRowsAction`/page loader. `countReadyToApprove` (models/approvals.ts) not yet touched to add submitted claims.
- S1 Receipts queue: Claim column, facet, bulk-bar button, pane ⋯ item — none built. `ReceiptRow` doesn't have `claim`/`claimEligibility` fields yet (models/receipts.ts untouched).
- S2 Add to expense claim dialog — not built (new component).
- S3 Approval tab claim section — not built (`components/queue/history-tabs.tsx` `ApprovalTab` untouched).
- S4 Approvals › Expense claims route/list/pane/decision bar — not built. `(queue)/approvals/expense-claims/` does not exist.
- S5 phone (segments `shortLabel`, sheets) — not built; `QueueSegments` untouched.
- S6 closures (`receipts?mode=claims`, `(chrome)/expenses` → in-shell `notFound()`) — not done.
- `PaneTabs` extraction from `split-pane.tsx` (B4) — not done.
- `ItemizedRecord` optional `note` field (B4) — not done.
- Part B contract checks script (`part-b-checks.mjs`) — not written; nothing to check yet since no UI exists.

### Facts still true from the spec session (re-read spec.md/preflight.md, not restated in full here)
Everything under "Facts the build must not re-derive" above this entry still holds, plus: the
`validateClaimableDocuments` signature changed to `(workspaceId, documentIds, targetCurrencyCode?, excludeClaimId?)` —
callers adding this UI must pass `excludeClaimId` when re-checking an existing claim's own items (submit does this; a future "add to existing draft" server call should too, for the `currency_mismatch` check against that draft specifically).

### Next step
Continue BUILD in a fresh session: write the new server actions in `expense-claim-actions.ts`
first (they're the seam every S1–S6 component calls), then `models/receipts.ts`'s claim
projection for `ReceiptRow`, then S1 → S2 → S3 → S4/S5 → S6, exactly per spec.md's surface order.
Only after all of S1–S6 exist does the r0 capture round make sense — do not capture a partial
surface.

## Milestones
- [x] Ticket claimed (assignee petrose99); scratch folder `docs/wayfinder-reports/226/logs/scratch-273/`.
- [x] Pre-build reads: #247 resolution (14 decisions), primers `docs/agents/areas/{approvals,queue-shell,detail-pane}.md`, both lessons files, `PRODUCT.md`, `craft-floor.md`, `operate.md`. `impeccable context` not run (needs interactive approval — project lesson).
- [x] Code recon scripts `scratch-273/recon{,2,3}.mjs` (re-runnable with `node`).
- [x] **Spec**: `scratch-273/spec.md` (§0 context · §1 S1–S7 · §2 data contracts · §3 Receipts · §4 dialog · §5 Approval tab · §6 Approvals › Expense claims · §7 phone · §8 layout/copy · §9 a11y · §10 pending). Critic changes 1–10 applied via `scratch-273/fix-spec-after-critic.mjs`.
- [x] **Pre-flight**: `scratch-273/preflight.md` Parts A–E filled; Part D critic (opus) before fixes 30/40 · sum 22 · 4 P1 · health ≈ 64; **after reconciliation 33/40 (H1 4, H5 4, rest 3) · sum 7 · P0 0 · P1 0 · health ≈ 86 · Clean. Gate met.**
- [ ] BUILD: surfaces S1–S7 from the Part B/C tables; Part B checks (`part-b-checks.mjs`, copy the #271 one) before build-done; `tsc --noEmit` once with the dev server stopped; `milestone: build-done`.
- [ ] MEASURE · [ ] CLOSE.

## Facts the build must not re-derive
- No schema change. `ExpenseClaimItem @@unique([claimId, documentId])` allows re-claiming after rejection; rejection reason lives in audit `detail.reason`.
- `validateClaimableDocuments` (models/expense-claims.ts:26) → rewrite onto `lib/claims/eligibility.ts` (four reasons incl. `currency_mismatch`); add-to-claim transactional; submit re-checks every item (spec §2).
- `sumReceiptTotals` returns `{ total, currencyCode, missing, mixed, byCurrency }`; mixed drafts cannot submit; missing amounts named in the Submit confirm (spec §2, §5.2–5.3).
- One creation breakpoint: `md` for every opener (bulk bar, ⋯ item, S3 sentence button, rejected button).
- Focus after remount: `#claim-card-title` → `#claim-empty-action` → `#claim-empty-text` via `pendingReloadFocus` (detail-pane primer); S4 decision → `DecisionResultStrip` takes focus.
- S4 pane renders the S3 claim card `readOnly`; Stage column reads `stageLabel`, never `CLAIM_STATUS_LABELS`.
- `ExpenseClaimsPage` imported by `receipts/page.tsx:10` (`?mode=claims`) and `(chrome)/expenses/page.tsx:99` redirect → both become in-shell `notFound()`; components unreferenced, not deleted (#274 sign-off).
- `whats-on/page.tsx` lists `expense-approvals` under `UNPLUGGED_MODULE_KEYS` — the seed must enable the capability for the dev workspace before capture (check `getWorkspaceCapabilities`).
- `QueueSegments` needs `shortLabel` (three segments at 390); `ItemizedRecord` gains optional `note`; `PaneTabs` may need extracting from `split-pane.tsx`.
- Capture harness to reuse: `scratch-257/pw/` + `scripts/wayfinder-autopilot/capture-round.mjs`; #271's `round-271.mjs`/`part-b-checks.mjs` are the nearest models.

## Needs the owner (carry to the report / close)
- May an owner decide their own claim (no flow)? Spec labels the case ("Your own claim", confirm prefix) and does not block it.
- **Reopen an approved claim** (owner-only, approved → submitted, audited): decision → Wayfinder ticket to create at close; it costs H3 its 4.
- Removal of `ExpenseClaimsPage`/`ExpenseClaimForm`/`ExpenseClaimRow`/workflow picker: #274 sign-off.
- Approval notice for claims (#271 sender keys on `ReviewTask`): follow-up ticket at close.

## Next step
Start the BUILD phase from `spec.md` + `preflight.md` tables (B1 row by row, then C states), server first (`lib/claims/*`, `models/expense-claims.ts`, actions), then S1 → S2 → S3 → S4/S5 → S6 closures.
