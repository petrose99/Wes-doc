# #273 hand-off — Build expense claims in place

milestone: spec-done

milestone: build-partial (sessions 3–5, 2026-09-17)

Phase BUILD is **in progress** — not `build-done`. Session 5 (2026-09-17) finished the server layer and wrote S2 + S3 components; S1/S4/S5/S6 wiring, Part B checks, seed, round script and r0 remain. Spec `logs/scratch-273/spec.md`, pre-flight `logs/scratch-273/preflight.md` (A–E filled, critic 33/40 · 0 P1 · Clean).

## Done (committed on `codex/post-login-ux-implementation`)
- **Server (S7) complete** — commit b4f5a18. `lib/claims/facts.ts` (client-safe types: `DocumentClaimFacts`, `DocumentClaimView`, `DraftClaimOption`, `AddToClaimResult`). `models/expense-claims.ts`: `classifyClaimableDocuments(db, …)` per-id (no throw) + `validateClaimableDocuments` throws `Object.assign(Error(code), {merchant})`; `addToExpenseClaim` (one `$transaction`, held-back reasons, currency of first receipt); `listMyDraftClaims`; `buildClaimFacts` → `getDocumentClaimFacts(ws, docId, actor)` / `getExpenseClaimDetail(ws, claimId, actor)`; `listExpenseClaimRows(ws, actor)` now has `stage/canDecide/nextStageName/hasWorkflow`; `countClaimsReadyToApprove`; `decideExpenseClaimStage`/`updateExpenseClaimStatus` take `reason` → audit `detail.reason`. `models/receipts.ts`: `ReceiptRow.claim` + `claimEligibility`, `claimFilter` widened to the 4 statuses + unclaimed. `models/approvals.ts` `countReadyToApprove` += claims (capability-gated). `lib/approvals/filters.ts`: `filterExpenseClaimRows`, `countClaimsWaitingOnOthers`. `expense-claim-actions.ts`: `addToExpenseClaimAction`, `withdrawExpenseClaimAction`, `listMyDraftClaimsAction`, `getExpenseClaimDetailAction`; `submitExpenseClaimAction(ws, claimId, submitterId)` resolves the default flow server-side; `decideExpenseClaimAction(…, reason?)`; every catch → `claimRefusalSentence`. `getSelectionAuditPanelDataAction` (`app/(app)/workspaces/[workspaceId]/actions.ts`) returns `claimsEnabled`, `claim`, `claimEligibility`; `DocumentHistory` type in `history-tabs.tsx` carries them. Model tests 30/30 pass; `tsc` clean for these files at the time.
- **Uncommitted at hand-off (committed with this file):** `components/queue/add-to-claim-dialog.tsx` (S2, `AddToClaimDialog({open, workspaceId, candidates: ClaimCandidate[], forceNew?, onClose, onAdded})`, exports `MAX_CLAIM_RECEIPTS`), `components/queue/claim-card.tsx` (S3: `ClaimSection({workspaceId, documentId?, facts, eligibility?, readOnly?, onOpenAddDialog?})`, `ClaimPill`, `useCanCreateClaims`, `DESKTOP_HINT`; all 4 confirms per spec §5.3), `EligibilityStrip label` prop + `ItemizedRecord.note` (`bulk-approve-receipt.tsx`), `detail-pane.tsx` reload-focus chain `save-review-submit → claim-card-title → claim-empty-action → claim-empty-text`. **None of these three is typechecked or wired yet** — run `tsc` first (note `claim-card.tsx` imports `ApprovalTimeline` from `history-tabs.tsx`, which will import `ClaimSection`: a cycle — if it bites, move `ApprovalTimeline` usage or import lazily).

## Next step (session 6 — still BUILD)
1. `tsc --noEmit` (servers stopped) → fix S2/S3 errors.
2. **S3 wire**: `history-tabs.tsx` `ApprovalTab` — when `history.claimsEnabled`, render `<ClaimSection workspaceId documentId facts={history.claim} eligibility={history.claimEligibility} onOpenAddDialog={…}>` above the timeline; when `history.claim` skip PO-match / "From this supplier" and the existing timeline (the card renders its own). `ApprovalTab` needs `documentId` + an `onOpenAddDialog` prop threaded from `ReceiptQueue` (through `SplitPane` — check how `history` reaches `ApprovalTab`; a small React context from `ReceiptQueue` is the least invasive: `AddToClaimContext` in `claim-card.tsx`).
3. **S1** `receipt-queue.tsx`: replace local `ClaimPill` with the one from `claim-card.tsx`; `RECEIPT_FACETS` claim options Unclaimed + 4 labels; Claim cell `—`/pill + subtitle (`claimName` if mine else `claimantName` — needs `me` = current user id, check what the page passes); `DocumentBulkActions` `extra` slot (check `document-actions.tsx`; add if absent) with **Add to claim** + visible "None of these can be claimed"; `PaneMenuItem` **Add to claim** (disabled+hint below md via `useCanCreateClaims`), hidden when `in_claim`; `AddToClaimDialog` rendered by `ReceiptQueue` with `useState<{ids, forceNew}|null>`; `onAdded` → toast `Added n receipts to ‹name›` (+ `and k held back`), `router.refresh()`, `onMutated("changed")`, selection = held-back ids. Candidates built from `ReceiptRow` (`merchant,total,currencyCode,purchaseDate,claimEligibility`). Remove old `Create expense claim…` item.
4. **S4** `components/queue/expense-claim-queue.tsx` modelled on `approval-invoice-queue.tsx` (spec §6) + routes `(queue)/approvals/expense-claims/{page,loading,[claimId]/page}.tsx` (copy `approvals/invoices/page.tsx`; `notFound()` unless `review-queue` and `expense-approvals`); pane = `ClaimSection readOnly` + Details list + audit; decision bar → `decideExpenseClaimAction(ws, claimId, hasWorkflow, decision, reason)`. Third segment in `approval-invoice-queue.tsx` + `po-mismatch-queue.tsx`; `QueueSegments` `shortLabel`.
5. **S6**: `receipts/page.tsx` `mode==="claims"` → `notFound()`; `(chrome)/expenses/page.tsx` redirect → `notFound()`.
6. Part B checks (`scratch-273/part-b-checks.mjs` from `scratch-271/`), seed (enable `expense-approvals`, a few `expense_receipt` docs, one submitted claim), `round-273.mjs` on `capture-round.mjs` at 1440/390 → `shots-r0`, fix detector/probe findings, `tsc`, stop servers, `milestone: build-done`, commit `wip(autopilot): #273 build`.

## Facts the build must not re-derive
- No schema change; `ExpenseClaimItem.createMany` needs `workspaceId`. Rejection reason = audit `detail.reason`.
- One creation breakpoint `md` (`useCanCreateClaims`). S4 Stage column reads `stageLabel`, never `CLAIM_STATUS_LABELS`.
- `ExpenseClaimsPage`/`ExpenseClaimForm`/`ExpenseClaimRow` unreferenced, not deleted (#274). `expense-claim-row.tsx` already updated to the 3-arg submit action.
- `whats-on/page.tsx` lists `expense-approvals` under `UNPLUGGED_MODULE_KEYS` — seed must enable it for dev workspace `af91555d-7450-4b21-a8ac-73db092617c8`.
- Capture: `scratch-257/pw/` + `scripts/wayfinder-autopilot/capture-round.mjs`; models `scratch-271/round-271.mjs`, `part-b-checks.mjs`. Dev recipe `docs/agents/areas/approvals.md` §Seed. Stop servers before `tsc`.

## Needs the owner (carry to close)
- May an owner decide their own claim (no flow)? Build labels it ("Your own claim"), does not block.
- **Reopen an approved claim** → Wayfinder ticket at close (costs H3 its 4). Legacy claims components removal: #274. Approval notice for claims: follow-up ticket at close.
