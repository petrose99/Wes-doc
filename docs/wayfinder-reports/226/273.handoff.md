# #273 hand-off — Build expense claims in place

milestone: spec-done

milestone: build-partial (sessions 3–9, 2026-09-17)

Phase BUILD is **in progress** — not `build-done`. Session 9 (2026-09-17) wrote the round script, ran `shots-r0` (34 states, 1 errored), fixed the findings below, then hit the context line. Remaining: re-run r0 clean, r0 counts here, `tsc`, `milestone: build-done`. Spec `logs/scratch-273/spec.md`, pre-flight `logs/scratch-273/preflight.md` (A–E filled, critic 33/40 · 0 P1 · Clean). Edit scripts already applied (don't re-run): `logs/scratch-273/{fix-s2,fix-page,wire-s3,wire-qs,wire-s1,wire-s4,wire-pages,fix-classify,fix-confirm-focus,fix-dialog-footer,fix-round}.mjs`.

## Done (committed on `codex/post-login-ux-implementation`)
- **Server (S7)** — b4f5a18 + session-8 fixes (`classifyClaimableDocuments` derives state from `reviewTask`; `asNumber` parses string totals). Tests `npx vitest run models/expense-claims.test.ts models/receipts.test.ts lib/claims lib/approvals` → 70/70.
- **S1–S6 UI**: S2 `components/queue/add-to-claim-dialog.tsx`; S3 `claim-card.tsx` in `history-tabs.tsx`; S1 `receipt-queue.tsx`; S4 `expense-claim-queue.tsx` + routes `(queue)/approvals/expense-claims/page.tsx`, `approvals/expense-claims/[claimId]/page.tsx`; S5 `queue-segments.tsx` `shortLabel`; S6 `receipts?mode=claims` and `(chrome)/expenses` → notFound.
- **Seed** `logs/scratch-273/seed273.ts` (idempotent; `npx tsx --env-file .env …`); **`facts.ts` → `facts.json`** (seed doc/claim ids by key; re-run after re-seeding). Owner = Dev User; Priya's submitted no-flow claim "Client visit September" is the one the owner decides.
- **Part B checks** `part-b-checks.mjs` → `part-b-results.txt` 0 FAIL.
- **Round script** `logs/scratch-273/round-273.mjs` on `capture-round.mjs`; run via `node logs/scratch-273/run-round.mjs r0 [only] [--decide]` (copies beside `/tmp/scratch229`). `run-scratch.mjs <file>` runs any probe there. `triage.mjs r0` → findings minus app-wide residue + keyboard log. `--decide` approves Priya's claim at 390 only (last width) and snaps the strip + empty me-list; re-seed + facts after.
- **r0 (before fixes)**: 34 states; 1440 = 17 states / 113 findings / 67 non-app-wide; 390 = 17 / 93 / 44. Keyboard: pane tabs ←/→ OK both widths; segments ←/→ OK; Reject sheet focus textarea, Esc → Reject button OK; 390 Back → list with focus on the row OK; **Approve confirm Esc → body (FAILED, fixed below)**; `bulk-add@1440` errored (fixed below); S2 probes (in `shots-r0b`): open focus = `#claim-target-new` radio, none checked, Esc → "Add to claim" button — PASS.
- **Fixes this session (uncommitted until this commit)**: `components/ui/confirm-dialog.tsx` — opener captured before any focus move (removed `autoFocus`; rAF focus path always) so Esc returns to the trigger (probe `probe-esc.mjs` confirms both mouse and keyboard open); `queue-segments.tsx` — `flex-auto whitespace-nowrap max-md:text-[13px] max-md:px-1.5` (PO mismatches wrapped at 390); `claim-card.tsx` + `add-to-claim-dialog.tsx` — dropped `bg-emerald-600` override (3.7:1 low-contrast), default primary button; dialog disabled-reason moved into the footer (`mr-auto`), it rendered as a stray line under the footer; round: mouse moved off the rail before checkbox clicks (rail hover-expand swallowed clicks — `.check()` "did not change state"), `paneLoaded()` waits for `[aria-busy=true]` to detach before tab clicks (hotel@1440 snapped the loading overlay → 24 false occlusions).

## r0 residue / not chased (carry to measure)
- App-wide five + `em-dash-overuse` (body text) + `cramped-padding` on `hidden lg:table` wrapper + `text-overflow` on truncated card subtitles (ellipsis renders correctly) + shell notice `<p>` `line-length` at 1440 (full-width `role=status` in queue-screen, same for every queue) + `expenses-gone` rail name overflow (404 page).
- `nested-cards` ×2 + `cramped-padding` "max-h-64" ×2 in the S2 dialog (shots-r0b) — **not yet looked at**; `max-h-64` is not in our files → shell `ItemizedRecapTable`/`Dialog` scroll container. Check before measure; fix only if it is ours.
- Pre-existing React key warning on every queue (`QueueScreenInner` child from the page — Invoices too), not #273.

## Known deviations from spec (decide at close)
- S4 pane has no source strip / Details "Show ‹merchant›"; Audit tab derived from facts. `/[claimId]` decided → shell notice, not read-only pane (§6.1). `PaneTabs` not extracted. New claim accepts mixed currencies (Submit then disabled with reason — per spec §98).

## Next step (session 10 — still BUILD)
1. `node .impeccable/live/dev.mjs start`; `node .impeccable/live/livesrv257.mjs`; `node logs/scratch-273/warm.mjs` (~4 min cold).
2. `node logs/scratch-273/run-round.mjs r0 --decide` (full, both widths; overwrite shots-r0) → `node logs/scratch-273/triage.mjs r0`. Expect: bulk-add clean, no occlusions on hotel@1440, no low-contrast, no segment overflow at 390, `approve-esc-return-*` = `button:Approve`, `approve-strip-focus-390` ≠ body. Check the two S2 dialog findings above. Fix anything real, re-run only the affected states (`run-round.mjs r0c <only>`).
3. Re-seed (`npx tsx --env-file .env logs/scratch-273/seed273.ts` then `facts.ts`) after `--decide`. Contact sheet: `node scripts/wayfinder-autopilot/contact-sheet.mjs logs/scratch-273/shots-r0 --out logs/scratch-273/shots-r0/sheet.png`.
4. Write r0 counts per width here; `node .impeccable/live/dev.mjs stop` (+ kill livesrv pid); `npx tsc --noEmit`; `milestone: build-done`; commit `wip(autopilot): #273 build`.

## Facts the build must not re-derive
- No schema change; `ExpenseClaimItem.createMany` needs `workspaceId`. Rejection reason = audit `detail.reason`. Extracted totals are **strings**. Document has no `approvalStatus`/`blockedByCheck` columns.
- One creation breakpoint `md` (`useCanCreateClaims`). S4 Stage column reads `stageLabel`. `?approver=anyone` = all; default = me.
- Legacy `ExpenseClaimsPage`/`ExpenseClaimForm`/`ExpenseClaimRow`/`addExpenseClaimItemsAction` unreferenced, not deleted (#274). `impeccable context` can't run unattended. Stop servers before `tsc`/lint.

## Needs the owner (carry to close)
- May an owner decide their own claim (no flow)? Build labels it, does not block. Reopen an approved claim → ticket at close. Legacy claims removal: #274.
