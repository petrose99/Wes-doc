# #273 hand-off — Build expense claims in place

Phase: **SPEC** (1 of 4), in progress. Session 1 (2026-09-17) ended at the context line (158K) after writing the spec; pre-flight A–E and the spec critic are still to do. No `milestone: spec-done` yet — the next session continues the spec phase from this file.

## Milestones
- [x] Ticket claimed (assignee petrose99); scratch folder created.
- [x] Pre-build reads done: #247 resolution (14 decisions), primers `docs/agents/areas/{approvals,queue-shell,detail-pane}.md`, both lessons files, `preflight.md` template, `PRODUCT.md`, `craft-floor.md`, `operate.md`. `impeccable context` not attempted (project lesson: needs interactive approval).
- [x] Code recon (scripts `logs/scratch-273/recon{,2,3}.mjs`, re-runnable with `node`): model exports, schema, `ReceiptRow`, `receipt-queue.tsx` structure, `DocumentBulkActions extra` slot, `ApprovalTab`, `QueueSegments`, Approvals page/queue pattern, `loadDetail` returns a ReactNode (Payments batches pattern), `ReasonDialog`/`ConfirmDialog`/`Dialog` props, capability key `expense-approvals`, `/expenses` page redirects to `receipts?mode=claims` (line 99).
- [x] **Spec written**: `docs/wayfinder-reports/226/logs/scratch-273/spec.md` (§0 context · §1 surfaces S1–S7 · §2 data contracts · §3 Receipts · §4 dialog · §5 Approval tab · §6 Approvals › Expense claims · §7 phone · §8 layout/copy · §9 a11y · §10 pending).
- [ ] `preflight.md` copied to `logs/scratch-273/preflight.md` and Parts A–E filled (A: H1 + H5 only; B1–B6 from spec §2–§6; C by component type; E worst-issue + 3–5 walkthroughs + anti-pattern sweep).
- [ ] Part D spec critic: fresh `Agent`, `model: "opus"` (touches money + approval), `run_in_background: false`, given spec.md + preflight.md, scores as if built; reconcile; gate = zero P0/P1, none under 3, two 4s, Clean, evaluate ≈ ≥85. Spec changes, not arguments.
- [ ] Hand-off rewritten with `milestone: spec-done`; commit `wip(autopilot): #273 spec`; stop.

## Facts the next session should not re-derive
- No schema change: `ExpenseClaimItem @@unique([claimId, documentId])` already allows re-claiming after rejection; rejection reason goes in the audit event `detail.reason`.
- `validateClaimableDocuments` (models/expense-claims.ts:26) today checks only template + "any item exists" — must be rewritten onto `lib/claims/eligibility.ts` (spec §2) so row and server share one function.
- `ExpenseClaimsPage` is imported by `receipts/page.tsx` (line 10) for `?mode=claims`; `(chrome)/expenses/page.tsx:99` redirects there. Both close as in-shell `notFound()`; the components stay unreferenced (sign-off #274).
- `whats-on/page.tsx` lists `expense-approvals` under `UNPLUGGED_MODULE_KEYS` — the dev workspace may not have the capability enabled; the build/measure phases must enable it in the seed before capture (check `getWorkspaceCapabilities` for the dev workspace first).
- `QueueSegments` needs a `shortLabel` for three segments to fit at 390 (spec §6.2).
- Open question for the owner (not a build decision): may an owner decide their own claim (no flow)? Spec assumes yes (#247 d6), flagged in §0.

## Next step
Fill `preflight.md` Parts A–E from spec.md (start by copying `scripts/wayfinder-autopilot/preflight.md` into the scratch folder), run the Part D critic, reconcile into spec.md, then write `milestone: spec-done`.
