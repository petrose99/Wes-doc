# #273 hand-off — Build expense claims in place

milestone: spec-done

milestone: build-partial (sessions 3–6, 2026-09-17)

Phase BUILD is **in progress** — not `build-done`. Session 6 (2026-09-17) typechecked and wired S1 + S3 + S6; **`tsc --noEmit` is clean** on the whole tree. S4/S5, Part B checks, seed, round script and r0 remain. Spec `logs/scratch-273/spec.md`, pre-flight `logs/scratch-273/preflight.md` (A–E filled, critic 33/40 · 0 P1 · Clean). Edit scripts used: `logs/scratch-273/{fix-s2,fix-page,wire-s3,wire-qs,wire-s1}.mjs` (already applied; don't re-run).

## Done (committed on `codex/post-login-ux-implementation`)
- **Server (S7) complete** — commit b4f5a18 (see git log for the inventory: `lib/claims/*`, `models/expense-claims.ts`, `models/receipts.ts` claim projection + 5-value `claimFilter`, `expense-claim-actions.ts` add/withdraw/decide-with-reason, `getSelectionAuditPanelDataAction` → `claimsEnabled/claim/claimEligibility`). Model tests 30/30.
- **S2** `components/queue/add-to-claim-dialog.tsx` — typechecks; "Held back" h3 is `text-sm font-semibold` (no eyebrow).
- **S3** `components/queue/claim-card.tsx` (`ClaimSection`, `ClaimPill`, `useCanCreateClaims`, `DESKTOP_HINT`, **`AddToClaimContext`**) — wired: `history-tabs.tsx` `ApprovalTab` takes `documentId`, renders `ClaimSection` (claimed → card only, invoice sections skipped; unclaimed → empty state above the timeline) when `history.claimsEnabled`; `split-pane.tsx` passes `documentId={header.documentId}`. Import cycle claim-card ↔ history-tabs exists and typechecks; verify at runtime on first render.
- **S1** `components/queue/receipt-queue.tsx` — props `claimsEnabled`, `currentUserId`; facet Unclaimed + `CLAIM_STATUS_LABELS`; Claim column (gated) `—`/pill + name-or-claimant subtitle; bulk `extra` **Add to claim** (+ visible "None of these can be claimed"); `PaneMenuItem` **Add to claim** (disabled+hint below md; hidden when in a non-rejected claim); `AddToClaimDialog` rendered by `ReceiptQueue`, `AddToClaimContext.Provider` around the screen; `onAdded` → toast, `controls.select(heldBack)`, `controls.refresh()`, `router.refresh()`. `queue-screen.tsx` gained `QueueControls` + `onControls` prop (effect hands `{refresh, select}` up).
- **S6** `receipts/page.tsx`: `?mode=claims` → `notFound()`, computes `claimsEnabled` via `getWorkspaceCapabilities`, passes `currentUserId`; `(chrome)/expenses/page.tsx` default export → `notFound()` (no redirect).

## Next step (session 7 — still BUILD)
1. **S4** `components/queue/expense-claim-queue.tsx` modelled on `approval-invoice-queue.tsx` (spec §6) + routes `(queue)/approvals/expense-claims/{page,loading,[claimId]/page}.tsx` (copy `approvals/invoices/page.tsx`; `notFound()` unless `review-queue` and `expense-approvals`); pane = `ClaimSection readOnly` + Details list + `AuditLog`; decision bar → `decideExpenseClaimAction(ws, claimId, hasWorkflow, decision, reason)` (check the action's exact signature in `expense-claim-actions.ts`). Third segment in `approval-invoice-queue.tsx` + `po-mismatch-queue.tsx`; `QueueSegments` `shortLabel` (S5).
2. Part B checks (`scratch-273/part-b-checks.mjs` from `scratch-271/`), seed (enable `expense-approvals` for the dev workspace, a few `expense_receipt` docs, one submitted claim), `round-273.mjs` on `scripts/wayfinder-autopilot/capture-round.mjs` at 1440/390 → `shots-r0`, fix detector/probe findings, `tsc`, stop servers, `milestone: build-done`, commit `wip(autopilot): #273 build`.
3. Affected tests not yet run this session: `npx vitest run models/receipts.test.ts models/expense-claims.test.ts components/queue` (whatever exists).

## Facts the build must not re-derive
- No schema change; `ExpenseClaimItem.createMany` needs `workspaceId`. Rejection reason = audit `detail.reason`.
- One creation breakpoint `md` (`useCanCreateClaims`). S4 Stage column reads `stageLabel`, never `CLAIM_STATUS_LABELS`.
- `ExpenseClaimsPage`/`ExpenseClaimForm`/`ExpenseClaimRow` unreferenced, not deleted (#274).
- `whats-on/page.tsx` lists `expense-approvals` under `UNPLUGGED_MODULE_KEYS` — seed must enable it for dev workspace `af91555d-7450-4b21-a8ac-73db092617c8`.
- Capture: `scratch-257/pw/` + `scripts/wayfinder-autopilot/capture-round.mjs`; models `scratch-271/round-271.mjs`, `part-b-checks.mjs`. Dev recipe `docs/agents/areas/approvals.md` §Seed. Stop servers before `tsc`. `impeccable context` launcher can't run unattended — skip it.

## Needs the owner (carry to close)
- May an owner decide their own claim (no flow)? Build labels it ("Your own claim"), does not block.
- **Reopen an approved claim** → Wayfinder ticket at close (costs H3 its 4). Legacy claims components removal: #274. Approval notice for claims: follow-up ticket at close.
