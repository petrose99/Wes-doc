# #273 hand-off — Build expense claims in place

milestone: spec-done

milestone: build-partial (sessions 3–8, 2026-09-17)

Phase BUILD is **in progress** — not `build-done`. Session 8 (2026-09-17) seeded, fixed two server bugs, ran the Part B checks (all pass) and the affected tests (70/70), then hit the context line while warming routes. Remaining: round script + `shots-r0`, fix findings/probes, r0 counts here, `tsc`, `milestone: build-done`. Spec `logs/scratch-273/spec.md`, pre-flight `logs/scratch-273/preflight.md` (A–E filled, critic 33/40 · 0 P1 · Clean). Edit scripts already applied (don't re-run): `logs/scratch-273/{fix-s2,fix-page,wire-s3,wire-qs,wire-s1,wire-s4,wire-pages,fix-classify}.mjs`.

## Done (committed on `codex/post-login-ux-implementation`)
- **Server (S7)** — b4f5a18 (`lib/claims/*`, `models/expense-claims.ts`, `models/receipts.ts` claim projection, `expense-claim-actions.ts`). Session 8 fixes: `classifyClaimableDocuments` no longer selects non-existent Document columns (`approvalStatus/blockedByCheck/touchless`) — it derives state from `reviewTask`/`documentCheckResult` like `receipts.ts` (`Db` type gained `reviewTask`); `asNumber` parses string totals ("24.50") like `receipts.ts` (every submit refused with `expense_claim_no_amounts` before). Tests: `models/expense-claims.test.ts` gained `db.reviewTask` mock, `models/receipts.test.ts` fixtures carry `claim.createdAt` and use `claimFilter: "draft"`. `npx vitest run models/expense-claims.test.ts models/receipts.test.ts lib/claims lib/approvals` → 70/70.
- **S1–S6 UI** as in earlier sessions: S2 `components/queue/add-to-claim-dialog.tsx`; S3 `claim-card.tsx` (`ClaimSection`, `AddToClaimContext`) in `history-tabs.tsx`; S1 `receipt-queue.tsx` (facet `?claim=`, column, bulk + ⋯ Add to claim); S4 `expense-claim-queue.tsx` + routes `(queue)/approvals/expense-claims/page.tsx`, `approvals/expense-claims/[claimId]/page.tsx`; S5 `queue-segments.tsx` `shortLabel`, both approval pages pass `expenseClaimCount`; S6 `receipts?mode=claims` and `(chrome)/expenses` → notFound. S4 toast/audit words now read `CLAIM_STATUS_LABELS`.
- **Seed** `logs/scratch-273/seed273.ts` (idempotent; `npx tsx --env-file .env …`): enables `expense-approvals`; 10 `expense_receipt` docs (sha256 `seed273-*`): lunch/uber ready unclaimed, parking needs_attention (open `check_failed` task), conference USD (currency hold-back), hotel in the **owner's draft** "seed273 Joburg trip"; claims: Priya's **submitted, no workflow** "seed273 Client visit September" `d832de4b-65bf-46dc-94d0-66e20bdd9f8e` (owner decides — S4 "me"); Tomás's submitted on workflow `78a88f51…` stage 0 = Priya "seed273 Office supplies" `6c76b3ce-f243-4cc2-b279-1676090da90f` (→ "waiting on other approvers"); rejected with reason `01146aa5-8c91-4b33-ab55-edff4eba4b0d`; approved `6d8a5cd9-17ed-46a2-81fc-bed84a9d8914`. Doc ids: hotel `61004791-9379-43a5-b159-c218f7f65b0e`, lunch `9181f814-7893-49fd-ba04-d3b735e33cfb`, coffee (rejected, released) `306992aa-d83e-4af2-8466-6db0cd9abdcb`. Re-running the seed re-creates everything with new ids (update `warm.mjs`/round).
- **Part B checks** `logs/scratch-273/part-b-checks.mjs` → `part-b-results.txt` 0 FAIL (exclusions in-script: `addExpenseClaimItemsAction` legacy/unreferenced → #274; invariant throw codes fall to the generic refusal; dialog refresh delegated to `onAdded`; "Submitted" column is the date column). Strings in `part-b-strings.txt`.
- Routes warm (`logs/scratch-273/warm.mjs`, all 200): receipts, approvals/expense-claims (+`?claim=`), `/[claimId]`, receipts/[documentId]. Servers were **stopped** at hand-off — restart + re-warm (~4 min cold).

## Known deviations from spec (carry to measure/close, decide there)
- S4 pane has **no source strip** / Details "Show ‹merchant›" toggle (`DocumentClaimFacts` has no `SourceDocument`). Audit tab derived from facts, not `DocumentAuditEvent` rows.
- `/[claimId]` decided/withdrawn renders a **notice**, not a read-only pane (spec §6.1).
- `PaneTabs` not extracted; `ExpenseClaimDetail` copies split-pane's tab markup (B4 row — do at close if cheap).

## Next step (session 9 — still BUILD)
1. `node .impeccable/live/dev.mjs start`; `node .impeccable/live/livesrv257.mjs` (daemonises, :8400); `node logs/scratch-273/warm.mjs`.
2. Write `logs/scratch-273/round-273.mjs` on `scripts/wayfinder-autopilot/capture-round.mjs` (model `scratch-271/round-271.mjs` + `run-round.mjs`; scratch Playwright `/tmp/scratch229`, copy with `fs.copyFileSync`). States ×1440/390: receipts list (`?claim=unclaimed` facet + Claim column), bulk bar with Add to claim (select lunch+uber+parking+conference), S2 dialog (new / existing draft "Joburg trip" / held-back rows / offline), S3 Approval tab on hotel (draft), a submitted receipt (flight), coffee (rejected → Add to a new claim), fuel (approved), S4 list (Approver me / anyone / empty via `?approver=me` after deciding — or use filters), S4 pane Approval/Details/Audit on Priya's claim, Approve confirm, Reject sheet, offline bar, result strip (decide Priya's claim last, `--decide` flag), deep link `/approvals/expense-claims/01146aa5…` (decided → notice), segments (390 short labels). Keyboard probes per preflight B5: S2 initial focus `#claim-target-new` + Esc returns to trigger; confirm initial focus; strip takes focus after decide (`activeElement` ≠ body); segments ←/→; pane tabs ←/→; 390 Back closes pane and URL returns. Run as `shots-r0`; contact sheet via `scripts/wayfinder-autopilot/contact-sheet.mjs`.
3. Fix real detector findings + failing probes (pure execution); write r0 counts per width here; stop servers; `npx tsc --noEmit`; `milestone: build-done`; commit `wip(autopilot): #273 build`.

## Facts the build must not re-derive
- No schema change; `ExpenseClaimItem.createMany` needs `workspaceId`. Rejection reason = audit `detail.reason`. `ExpenseClaim.resolvedAt`.
- Extracted totals are **strings**; templates are per-workspace (`expense_receipt` = `49a503e9…` in the dev ws). Document has no `approvalStatus`/`blockedByCheck` columns — needs_attention = open `check_failed` ReviewTask or escalated check.
- One creation breakpoint `md` (`useCanCreateClaims`). S4 Stage column reads `stageLabel`, never `CLAIM_STATUS_LABELS`.
- `ExpenseClaimsPage`/`ExpenseClaimForm`/`ExpenseClaimRow`/`addExpenseClaimItemsAction` unreferenced, not deleted (#274).
- `whats-on/page.tsx` lists `expense-approvals` under `UNPLUGGED_MODULE_KEYS`; seed enables it. `impeccable context` can't run unattended — skip. Stop servers before `tsc`.

## Needs the owner (carry to close)
- May an owner decide their own claim (no flow)? Build labels it, does not block.
- **Reopen an approved claim** → Wayfinder ticket at close. Legacy claims removal: #274. Approval notice for claims: follow-up ticket at close.
