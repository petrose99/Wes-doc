# #273 hand-off — Build expense claims in place

milestone: spec-done

Phase SPEC complete (sessions 1–2, 2026-09-17). Next phase: **BUILD** — fresh session, no capture, builds every surface from the pre-flight tables.

milestone: build-partial (session 3, 2026-09-17)

**Server layer (S7) only — no UI surface built yet** (`lib/claims/*`, `models/expense-claims.ts` rewrite; see "Done" below). Session 4 (2026-09-17) spent its context on code recon and hit the hand-off line before writing code; its recon is condensed under "Build plan" so session 5 writes code from line one — **do not re-read the files listed there, the facts are here.**

### Done (committed)
- `lib/claims/labels.ts` (`CLAIM_STATUS_LABELS`, `claimName`, `ELIGIBILITY_REASON_TEXT`), `eligibility.ts` (`claimEligibility(facts)`), `totals.ts` (`sumReceiptTotals`), `refusals.ts` (`claimRefusalSentence(code, merchant)` — covers every server code incl. `expense_claim_mixed_currency`/`_no_amounts`).
- `models/expense-claims.ts`: `validateClaimableDocuments(ws, ids, targetCurrencyCode?, excludeClaimId?)` on `claimEligibility` (throws code strings); `submitExpenseClaim` re-validates + `sumReceiptTotals`; `withdrawExpenseClaim`; `listExpenseClaimRows(ws)` (no stage/canDecide yet); `decideExpenseClaimStage`/`updateExpenseClaimStatus` take **no reason yet**. Tests: 30 pass (`models/expense-claims.test.ts` mocks prisma as bare object; `beforeEach` sets `$transaction` (array form only) + `documentCheckResult.findMany`).

### Build plan (session-4 recon — the facts, so nothing below needs re-reading)
Server:
- `models/expense-claims.ts` add: `getDocumentClaimFacts(ws, documentId, actor)` → `{ claim: DocumentClaimFacts|null, claimEligibility }` (latest claim = `expenseClaimItem.findFirst({ orderBy: { claim: { createdAt: "desc" } } })`); shared `buildClaimFacts(claim, actor)` also backs `getExpenseClaimDetail(ws, claimId, actor)` for S4. Facts per spec §2 + `approval: {by,at}|null`, `submitDisabledReason`, `submitGoesTo` ("Any owner" | "‹stage› approvers" from `getDefaultApprovalFlow` in `models/approval-defaults.ts` + its workflow stages), `receipts[]` (documentId, itemId, merchant, amount, currencyCode, date) for S4 Details, and a `timeline` in `ApprovalTimeline`'s shape (`decisions` = `expense_claim_stage_decided` events → `{id, stageIndex, stageName, decision, note, actorName, decidedAt}`; `pendingStages`; `approval` = `ApprovalDetailFacts["approval"]` shape: `startedBy/startedAt/stages/currentStageIndex/waitingOnYou/waitingOn`). Claim audit events: `documentAuditEvent.findMany({ where: { workspaceId, type: { startsWith: "expense_claim" }, detail: { path: ["claimId"], equals } } })` (fields: actorId, type, detail Json, createdAt); actor names via `prisma.user.findMany`. Rejection reason = `detail.reason` on the reject event. Stage naming copies `models/approvals.ts:313-321` (`toWorkflowStageInputs`, `findCurrentStage`, `canDecideStage({stage, actorRole: owner|member, actorId})`, named approvers else "an owner"/"any member"). `deletedReceiptCount = expense_claim_submitted.detail.itemCount − items.length`.
- `addToExpenseClaim(ws, actor, { documentIds, target: {new:{title}} | {claimId} })` in `prisma.$transaction(async tx => …)`: refactor the eligibility classifier to take a client and return per-id results (no throw) so it returns `{ claimId, name, added, heldBack:[{id, reason}] }`; throw `Object.assign(new Error(code), { merchant })` on the validate path so actions can name the merchant.
- `decideExpenseClaimStage`/`updateExpenseClaimStatus`: add `reason?: string` → audit `detail.reason`. `listExpenseClaimRows(ws, actor)`: add `stage: ApprovalStageInfo` (no flow → `{index:0,total:1,name:"Owner"}`), `canDecide` (flow → `canDecideStage`, else `role==="owner"`), `nextStageName`. `lib/approvals/filters.ts`: `filterExpenseClaimRows` (scopedToMe only; no eligibility facet). `models/approvals.ts:256 countReadyToApprove` += claims the actor can decide.
- `models/receipts.ts`: `ReceiptRow` += `claim {id,status,title,createdAt,claimantId,claimantName}|null` + `claimEligibility` (needs `template.code` (selected) + `processingState(...)` + `currency_code`); `claimFilter` widens to `unclaimed|draft|submitted|approved|rejected` (page `receipts/page.tsx:36` builds the union; old `claimed` ignored). Query the latest claim per document (order items by `claim.createdAt desc`, first wins).
- `app/(app)/workspaces/[workspaceId]/expense-claim-actions.ts` (auth helper `requireExpenseClaimsMember`, `errorMessage`, `NO_ACCESS`, `revalidateExpenseClaims`): add `addToExpenseClaimAction`, `withdrawExpenseClaimAction`, `listMyDraftClaimsAction` (`{id, name, receiptCount, total, currencyCode}[]`), `getExpenseClaimDetailAction`; `decideExpenseClaimAction(ws, claimId, hasWorkflow, decision, reason?)`; every catch → `claimRefusalSentence(error.message, error.merchant)`. `submitExpenseClaimAction` resolves `workflowId` from `getDefaultApprovalFlow` (active only) server-side — drop the client `workflowId` param.
- Claim facts into the pane: `app/(app)/workspaces/[workspaceId]/actions.ts:238 getSelectionAuditPanelDataAction` adds `claim` + `claimsEnabled` (`getWorkspaceCapabilities(ws).has("expense-approvals")`) to its return; `components/queue/history-tabs.tsx:17 DocumentHistory` gains `claim?: DocumentClaimFacts|null; claimsEnabled?: boolean` (top-level, not `facts.claim` — `facts` is `ApprovalDetailFacts`). `ApprovalTab` (`history-tabs.tsx:184`) renders `<ClaimSection>` above the timeline and skips PO-match/"From this supplier" when `history.claim`; mutation refresh = `useContext(PaneDocumentContext)?.onMutated("changed")` (`components/queue/document-actions-menu.tsx`) + `useRouter().refresh()`; reload focus = `components/queue/detail-pane.tsx:242-254` — extend the fallback chain `#save-review-submit` → `#claim-card-title` → `#claim-empty-action` → `#claim-empty-text` → heading.
UI:
- S1 `components/queue/receipt-queue.tsx`: `RECEIPT_FACETS` claim options → Unclaimed + `CLAIM_STATUS_LABELS`; `ClaimPill` reads the label map (currently `capitalize` on the raw status — replace); column `claim` cell `—` when null else pill + subtitle; `bulkActions` → `DocumentBulkActions` needs an `extra` slot (check `components/queue/document-actions.tsx` for one; add if absent) with **Add to claim** + the visible "None of these can be claimed" line; `paneMenu` → `PaneMenuItem` **Add to claim** (`disabled`+`hint` below `md` via `matchMedia("(min-width:768px)")`), dialog state `useState<{ids, opener}>` in `ReceiptQueue`; on success `router.refresh()` + `onMutated("changed")` (use the `paneActions` helpers' `refresh`) + selection scoped (`QueueScreen` bulk `clear` — check whether a `setSelection` exists; else clear all and note the gap). Remove the old `Create expense claim…` item.
- S2 new `components/queue/add-to-claim-dialog.tsx`: shell `Dialog` (`open,title,description,width,onClose,placement,initialFocus`), `EligibilityStrip` (its sentence is "Eligible for Approval (n of m)" — add a `label` prop → "Can be added"), `ItemizedRecapTable` + `ItemizedRecord.note?` (extra trailing column when any record has a note), native radios per spec §4; `OFFLINE_REASON` from `components/queue/decision-result.tsx`; `useOnlineStatus` from `lib/client/use-online-status`.
- S3 new `components/queue/claim-card.tsx` `ClaimSection({ facts, claimsEnabled, readOnly, onOpenAddDialog })` — states §5.2, confirms §5.3 through `ConfirmDialog({open,title,description,confirmLabel,destructive,busy,onConfirm,onCancel})` (`components/ui/confirm-dialog.tsx`); server refusal shown inside via `children` alert.
- S4 new `components/queue/expense-claim-queue.tsx` modelled line-for-line on `components/queue/approval-invoice-queue.tsx` (segments band, `pinned`/`decided`, `DecisionResultStrip`, `ReasonDialog placement="sheet"`, footer `disabledReason`), pane content = own `ClaimPane` (source strip optional — first cut: `ClaimSection readOnly` + Details list + `AuditLog`); routes `(queue)/approvals/expense-claims/{page,loading,[claimId]/page}.tsx` copied from `approvals/invoices/page.tsx` (`notFound()` unless `review-queue` **and** `expense-approvals`). Add the third segment to `approval-invoice-queue.tsx` and the po-mismatches queue; `QueueSegments` gets `shortLabel` (`md:hidden`/`hidden md:inline` spans, `aria-label` full).
- S6: `receipts/page.tsx:31` `mode==="claims"` → `notFound()`; `(chrome)/expenses/page.tsx:99` redirect → `notFound()`.
- Then `scratch-273/part-b-checks.mjs` (copy `scratch-271/part-b-checks.mjs`), `round-273.mjs` on `capture-round.mjs`, seed (enable `expense-approvals` capability + a few `expense_receipt` docs and one submitted claim), r0 at 1440/390, `tsc --noEmit` with servers stopped, `milestone: build-done`.

## Milestones
- [x] Claimed; scratch `docs/wayfinder-reports/226/logs/scratch-273/` (spec.md, preflight.md, recon*.mjs).
- [x] Spec + pre-flight A–E (critic opus reconciled 33/40 · P1 0 · health ≈ 86 · Clean).
- [x] Server layer part 1 (lib/claims, model rewrite) — session 3.
- [ ] BUILD remainder per "Build plan" → Part B checks → seed/servers → r0 → `tsc` → `milestone: build-done`.
- [ ] MEASURE · [ ] CLOSE.

## Facts the build must not re-derive
- No schema change. `@@unique([claimId, documentId])`; rejection reason in audit `detail.reason`.
- One creation breakpoint `md` for every opener. Focus chain after remount: `#claim-card-title` → `#claim-empty-action` → `#claim-empty-text`; S4 decision → `DecisionResultStrip` takes focus.
- S4 pane renders the claim card `readOnly`; Stage column reads `stageLabel` (`"n of N · name"`), never `CLAIM_STATUS_LABELS`.
- `ExpenseClaimsPage`/`ExpenseClaimForm`/`ExpenseClaimRow` unreferenced, not deleted (#274 sign-off).
- `whats-on/page.tsx` lists `expense-approvals` under `UNPLUGGED_MODULE_KEYS` — the seed must enable it for the dev workspace before capture.
- Capture harness: `scratch-257/pw/` + `scripts/wayfinder-autopilot/capture-round.mjs`; nearest models `scratch-271/round-271.mjs`, `part-b-checks.mjs`. Dev recipe in `docs/agents/areas/approvals.md` §Seed.

## Needs the owner (carry to the report / close)
- May an owner decide their own claim (no flow)? Build labels it ("Your own claim"), does not block.
- **Reopen an approved claim** → Wayfinder ticket at close (costs H3 its 4).
- Removal of the legacy claims components: #274. Approval notice for claims: follow-up ticket at close.

## Next step
Session 5 = BUILD: start at "Build plan › Server" and write code immediately; the only reads needed are the exact line ranges named above.
