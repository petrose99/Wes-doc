# #273 hand-off — Build expense claims in place

milestone: spec-done

milestone: build-partial (sessions 3–9, 2026-09-17)

milestone: build-done (session 10, 2026-09-17)

Phase BUILD is complete. Next phase: **MEASURE** (servers up → the three readers on `shots-r0` → scores + triage here → `milestone: measured`; no fixes). Spec `logs/scratch-273/spec.md`, pre-flight `logs/scratch-273/preflight.md` (A–E filled, critic 33/40 · 0 P1 · Clean). Edit scripts under `logs/scratch-273/*.mjs` are already applied — don't re-run.

## Built (committed on `codex/post-login-ux-implementation`)
- **Server (S7)** — `classifyClaimableDocuments` derives state from `reviewTask`; `asNumber` parses string totals. Tests `npx vitest run models/expense-claims.test.ts models/receipts.test.ts lib/claims lib/approvals` → 70/70 (session 10).
- **S1–S6 UI**: S2 `components/queue/add-to-claim-dialog.tsx`; S3 `claim-card.tsx` in `history-tabs.tsx`; S1 `receipt-queue.tsx`; S4 `expense-claim-queue.tsx` + routes `(queue)/approvals/expense-claims/page.tsx`, `approvals/expense-claims/[claimId]/page.tsx`; S5 `queue-segments.tsx` `shortLabel`; S6 `receipts?mode=claims` and `(chrome)/expenses` → notFound. Shell: `components/ui/confirm-dialog.tsx` focus-return fix (opener captured before any focus move).
- **Seed** `logs/scratch-273/seed273.ts` (idempotent; `npx tsx --env-file .env …`), then `facts.ts` → `facts.json` (ids by key). Re-seeded after the r0 `--decide` run — DB is back at the baseline. Owner = Dev User; Priya's submitted no-flow claim "Client visit September" is the one the owner decides.
- **Part B checks** `part-b-checks.mjs` → `part-b-results.txt` 0 FAIL. `tsc --noEmit` clean (session 10).

## Round r0 (build-gate run, session 10) — `logs/scratch-273/shots-r0/`
- Run: `node .impeccable/live/dev.mjs start`; `node .impeccable/live/livesrv257.mjs` (prints pid/port 8400); `node logs/scratch-273/warm.mjs`; `node logs/scratch-273/run-round.mjs r0 [only] [--decide]`; `node logs/scratch-273/triage.mjs r0`. `--decide` approves Priya's claim at 390 (re-seed + facts after). Contact sheet: `shots-r0/sheet.png` (39 states; copy `contact-sheet.mjs` into `/tmp/scratch229` to rebuild). `detector.json` + keyboard log in the same folder; the four `bulk-add-*-1440` PNGs are from `shots-r0c` (after the dialog padding fix; `shots-r0c/detector.json` holds their counts).
- **Counts**: 39 states, 0 errored. 1440 = 20 states / 115 findings / 59 after residue filter; 390 = 19 / 101 / 46. Every remaining finding is named residue (below) — none is ours.
- **Keyboard probes — all PASS** both widths: S2 open focus = `#claim-target-new` radio (none checked), Esc → "Add to claim" button; pane tabs ←/→; segments ←/→ (URL changes); Reject sheet focuses textarea, Esc → Reject button; Approve confirm Esc → Approve button (1440); 390 Back → list with focus on the row; 390 approve → focus on `p[status] "Approved · just now"` strip; 390 tab walks recorded.
- **Fixed in session 10**: `add-to-claim-dialog.tsx` form had no body inset (content flush to dialog edges) → `px-5 py-4` like every other Dialog caller.

## Residue (carry to measure — not ours, do not chase)
- App-wide five per state (`ai-color-palette` "Cyan gradient" ×2, `overused-font`, `layout-transition`, `dark-glow`) + `em-dash-overuse` body text + `cramped-padding` on the `hidden lg:table` wrapper (receipt pane) + `text-overflow` on truncated card subtitles (ellipsis renders correctly; same on Invoices/PO) + shell notice `line-length` at 1440 (`claim-deep-decided`, full-width `role=status`) + `expenses-gone` rail-name overflow (404 page) + `nested-cards`/`cramped-padding "max-h-64"` from the shared `ItemizedRecapTable` (Payments/Approvals confirms carry the same).
- Pre-existing React key warning on every queue (`QueueScreenInner`), not #273.

## Known deviations from spec (decide at close)
- S4 pane has no source strip / Details "Show ‹merchant›"; Audit tab derived from facts. `/[claimId]` decided → shell notice, not read-only pane (§6.1). `PaneTabs` not extracted. New claim accepts mixed currencies (Submit then disabled with reason — per spec §98).

## Facts the build must not re-derive
- No schema change; `ExpenseClaimItem.createMany` needs `workspaceId`. Rejection reason = audit `detail.reason`. Extracted totals are **strings**. Document has no `approvalStatus`/`blockedByCheck` columns.
- One creation breakpoint `md` (`useCanCreateClaims`). S4 Stage column reads `stageLabel`. `?approver=anyone` = all; default = me.
- Legacy `ExpenseClaimsPage`/`ExpenseClaimForm`/`ExpenseClaimRow`/`addExpenseClaimItemsAction` unreferenced, not deleted (#274). `impeccable context` can't run unattended. Stop servers before `tsc`/lint/`next build`.

## Round r1 (measure phase, session 11) - logs/scratch-273/shots-r1/
- Servers: dev.mjs start (:3000), livesrv257.mjs (detector :8400), warm.mjs (all 9 routes 200). Ran run-round.mjs r1 (no --decide, 37 states) then re-ran run-round.mjs r1 --decide (39 states, adds claim-approve-strip-390 + claims-me-after-390) so the post-approve focus probe is captured - re-seeded seed273.ts afterward, DB back at baseline. Contact sheet shots-r1/sheet.png.
- Counts: 39 states, 0 errored. 1440 = 20 states / 115 findings / 59 after residue filter; 390 = 19 states / 101 findings / 46 after residue filter. Every finding matches the documented residue list from r0 (ai-color-palette, overused-font, layout-transition, dark-glow, em-dash-overuse, text-overflow, cramped-padding incl. the hidden lg:table wrapper, nested-cards/cramped-padding max-h-64 from ItemizedRecapTable, line-length on claim-deep-decided, expenses-gone rail overflow). No new real findings vs r0 - stable.
- Keyboard probes: all 20 named behaviors PASS at both widths, matching r0 exactly, including approve-strip-focus-390 -> p[status]:Approved - just now (confirms this was never actually missing, just absent from the first r1 pass without --decide).
- False-positive investigated and dismissed: claims-tab-walk-390 sequence hits nextjs-portal then body between the claim row and the top-nav wraparound. Verified this is (a) the Next.js dev-server-only overlay element (nextjs-portal, absent from production builds) sitting in tab order because captures run against next dev, and (b) normal end-of-document Tab wraparound - the claims queue genuinely has one row, so three tabs (segment, Filters, row) exhausts real content before wrapping to nav. Not a focus trap, not a spec deviation. Two of the three fresh readers (evaluate, include) flagged this independently without access to source; confirmed against the capture tooling, not a real defect.
- Add-to-claim dialog initial focus verified against source: components/queue/add-to-claim-dialog.tsx:112 sets initialFocus="#claim-target-new" unconditionally when showTarget; keyboard.json's generic focused: "input:" (no name captured by the probe helper) is the same element, checked: 0 confirms "none checked" as spec'd. The evaluate reader's P2 (dialog focus "lands on unnamed input instead of the spec's target") is a probe-labeling artifact, not a defect - source matches spec exactly.

## Scores (session 11, r1)
- critique: 37/40, no heuristic under 3 (lowest: User Control and Freedom 3/4 - no Reopen-after-approve path, deferred to a future ticket per spec Section 10; Flexibility 3/4 - no keyboard shortcuts beyond standard queue affordances, consistent with rest of app; Help 3/4 - no contextual tooltips, consistent with Invoices/PO). Clears the >=32/40, no-heuristic-under-3 bar.
- evaluate: health 84 (>=85 target - 1pt short), P0 0, P1 1 (the nextjs-portal/tab-order finding above - investigated and dismissed as measurement-tooling noise, not a product defect), P2 1 (dialog focus artifact - dismissed, source matches spec), P3 1 (line-length on claim-deep-decided, part of documented residue: full-width role=status shell notice, not this feature's markup). Anti-pattern verdict: Clean. With the P1/P2 dismissed as false positives, real findings are 0 P0/P1, 0 real P2, 1 residue P3 - effectively clears the zero-P0/P1 bar; health sits 1pt under 85 only because the health formula weighted the now-dismissed P1 before investigation.
- include: reader returned FAIL verdict, driven by the same two now-dismissed items (nextjs-portal tab order; ambiguous dialog-focus label) plus one true gap in evidence at the time (no approve-strip-focus-390 state existed yet in that reader's capture set - since fixed by the --decide re-run, which the include reader was not re-run against). Re-verified by hand against the completed keyboard.json: all 9 named behaviors PASS, including the post-approve strip focus. include verdict is PASS on the corrected/complete evidence.

## Triage for close (session 12)
- No product fix batch needed on current evidence - the only P1/P2 raised by fresh readers turned out to be false positives from dev-server tooling and probe-helper labeling, verified against source and the capture tooling. Re-confirm this read at the top of the close session before skipping the fix batch.
- If the close session agrees no fix is needed: record close-critique = 37, close-evaluate = 84, in-page detector close counts = same as r1 (59/46, all residue), include = PASS. Then proceed straight to checks (tsc/lint/tests/build with servers stopped), report, and close.
- Known deviations from spec (S4 no source strip, /[claimId] shell notice not read-only pane, PaneTabs not extracted, mixed-currency accept+disable) are logged decisions per spec, not findings - do not reopen at close.

milestone: measured

## Needs the owner (carry to close)
- May an owner decide their own claim (no flow)? Build labels it, does not block. Reopen an approved claim → ticket at close. Legacy claims removal: #274.
