# #273 hand-off — Build expense claims in place

milestone: spec-done

milestone: build-partial (sessions 3–7, 2026-09-17)

Phase BUILD is **in progress** — not `build-done`. Session 7 (2026-09-17) built S4 + S5 and wired the third segment; **`tsc --noEmit` is clean** on the whole tree (servers were down). Remaining: seed, Part B checks, affected tests, round script + `shots-r0`, fix findings, `milestone: build-done`. Spec `logs/scratch-273/spec.md`, pre-flight `logs/scratch-273/preflight.md` (A–E filled, critic 33/40 · 0 P1 · Clean). Edit scripts already applied (don't re-run): `logs/scratch-273/{fix-s2,fix-page,wire-s3,wire-qs,wire-s1,wire-s4,wire-pages}.mjs`.

## Done (committed on `codex/post-login-ux-implementation`)
- **Server (S7)** — b4f5a18 (`lib/claims/*`, `models/expense-claims.ts` incl. `ExpenseClaimRow`/`listExpenseClaimRows`/`getExpenseClaimDetail`, `models/receipts.ts` claim projection, `expense-claim-actions.ts` add/withdraw/decide(ws, claimId, hasWorkflow, decision, reason)/`getExpenseClaimDetailAction`). Model tests 30/30 at that commit.
- **S2** `components/queue/add-to-claim-dialog.tsx`. **S3** `components/queue/claim-card.tsx` (`ClaimSection` readOnly-capable, `AddToClaimContext`) wired into `history-tabs.tsx` `ApprovalTab`. **S1** `receipt-queue.tsx` (facet, column, bulk + ⋯ Add to claim, dialog). **S6** `receipts/page.tsx` `?mode=claims` → notFound; `(chrome)/expenses/page.tsx` → notFound. (Session 6.)
- **S4** (session 7) `components/queue/expense-claim-queue.tsx`: `ExpenseClaimQueue` (columns Claimant/Claim/Receipts/Amount/Stage/Submitted with phone slots, Approver facet only, sorts Submitted/Amount, three-segment band, cards "Ready to Approve", empty done + "N waiting on other approvers" + Go to Receipts, decision bar Reject/Approve with `disabledReason`/pending/`DecisionResultStrip`, Approve = `ConfirmDialog` with the §6.4 consequence copy incl. own-claim prefix, Reject = `ReasonDialog` sheet) + `ExpenseClaimDetail` pane (roving tablist Approval · Details · Audit; Approval = `ClaimSection readOnly` + "Withdrawn — not waiting for approval." for drafts; Details = receipts list with "Open on Receipts" links; Audit = `ClaimEvents` derived from facts). Routes: `(queue)/approvals/expense-claims/page.tsx` (`notFound` unless `review-queue` **and** `expense-approvals`; deep-link/`?claim=` to a decided/withdrawn claim → `queueArrival` `missingText` notice; unknown id → notFound), `approvals/expense-claims/[claimId]/page.tsx`. `(queue)/approvals/loading.tsx` already covers the segment.
- **S5** `queue-segments.tsx` `shortLabel` (visible below `md`, `aria-label` = full label + count); `approval-invoice-queue.tsx` / `po-mismatch-queue.tsx` take `expenseClaimCount` (null hides the segment) and both pages compute it when `expense-approvals` is on. `lib/navigation/origin.ts` + `origin-server.ts` know `approvals/expense-claims`.

## Known deviations from spec (carry to measure/close, decide there)
- S4 pane has **no source strip** and Details has no "Show ‹merchant›" toggle: `DocumentClaimFacts` carries no `SourceDocument`; would need a server addition. Audit tab is derived from facts (submitted/decisions/outcome), not `DocumentAuditEvent` rows.
- `/[claimId]` decided/withdrawn renders a **notice**, not a read-only pane (spec §6.1) — `QueueScreen` can only open panes on rows.
- `PaneTabs` not extracted; `ExpenseClaimDetail` copies split-pane's tab markup (B4 row says extract — do at close if cheap).

## Next step (session 8 — still BUILD)
1. Seed (`logs/scratch-273/db-recon.ts` written, **not yet run** — run first: `npx tsx --env-file .env docs/wayfinder-reports/226/logs/scratch-273/db-recon.ts` to learn templates/members/claims/modules). Then a seed script: upsert `workspaceModule {workspaceId, moduleKey:"expense-approvals", status:"enabled"}` for ws `af91555d-…`; ensure ≥4 `expense_receipt` documents (2 ready unclaimed, 1 needs_attention, 1 in a draft of the dev owner), 1 supplier receipt; one **submitted** claim by a non-owner member (use `createExpenseClaim` + `submitExpenseClaim` so the audit itemCount exists) so the owner can decide; one rejected claim with `detail.reason`.
2. Part B checks `scratch-273/part-b-checks.mjs` (model on `scratch-271/part-b-checks.mjs`): reachability grep, string extraction (one term per concept), primitive diff, focus probe list.
3. Affected tests: `npx vitest run models/expense-claims.test.ts models/receipts.test.ts lib/approvals lib/claims` (servers down).
4. Servers up (`node .impeccable/live/dev.mjs start`, `node .impeccable/live/livesrv257.mjs`), warm routes with curl, write `round-273.mjs` on `scripts/wayfinder-autopilot/capture-round.mjs` (model `scratch-271/round-271.mjs` + `run-round.mjs`, scratch Playwright in `/tmp/scratch229`). States ×1440/390: receipts list (column/facet Unclaimed), bulk bar Add to claim, S2 dialog (new/existing/held-back/offline), S3 tab (unclaimed/draft/submitted/rejected/approved), S4 list (me/anyone/empty), S4 pane (Approval/Details/Audit), Approve confirm, Reject sheet, offline bar, result strip, deep link decided, segments. Keyboard probes per preflight B5 (dialog focus/Esc return, confirm focus, strip takes focus, segments ←/→, tabs ←/→, 390 Back). Run as `shots-r0`; fix real detector findings + failing probes; write r0 counts per width here; `tsc`; stop servers; `milestone: build-done`; commit `wip(autopilot): #273 build`.

## Facts the build must not re-derive
- No schema change; `ExpenseClaimItem.createMany` needs `workspaceId`. Rejection reason = audit `detail.reason`. `ExpenseClaim.resolvedAt` (not `decidedAt`).
- One creation breakpoint `md` (`useCanCreateClaims`). S4 Stage column reads `stageLabel`, never `CLAIM_STATUS_LABELS`.
- `ExpenseClaimsPage`/`ExpenseClaimForm`/`ExpenseClaimRow` unreferenced, not deleted (#274).
- `whats-on/page.tsx` lists `expense-approvals` under `UNPLUGGED_MODULE_KEYS` — seed must enable it for the dev workspace.
- Capture: `scratch-257/pw/` + `scripts/wayfinder-autopilot/capture-round.mjs`; models `scratch-271/round-271.mjs`, `part-b-checks.mjs`. Dev recipe `docs/agents/areas/approvals.md` §Seed. Stop servers before `tsc`. `impeccable context` launcher can't run unattended — skip it.

## Needs the owner (carry to close)
- May an owner decide their own claim (no flow)? Build labels it ("Your own claim" / "You are approving your own claim."), does not block.
- **Reopen an approved claim** → Wayfinder ticket at close (costs H3 its 4). Legacy claims components removal: #274. Approval notice for claims: follow-up ticket at close.
