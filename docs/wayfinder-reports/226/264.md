# Autopilot report — #264 Build the three-way empty queue on Invoices: first-use guidance, done, filtered — and How DocuBite works on the account menu

- Map: #226 (autopilot map)
- Type: wayfinder:task · Started: earlier sessions (spec/build/measure) · this session (close): 2026-09-16
- Outcome: resolved

## What I did
This is the CLOSE phase (4 of 4) of a ticket continued across sessions; spec, build and first measurement (r1) were done in prior sessions and recorded on the hand-off. This session:
1. Read the hand-off (`264.handoff.md`) — bar was already met on the first pass (critique 34/40, evaluate health 91/Clean, include PASS), with a small triage list for one batch before close.
2. Fixed the batch: (1) `components/intake/inbound-address-line.tsx` — the inbound-address `<code>` pill no longer wraps mid-token at desktop (`max-md:break-all md:whitespace-nowrap`, was bare `break-all`); (3) `components/shell/how-it-works.tsx` — "Keyboard shortcuts" in the dialog footer is now a real button (`openKeyboardShortcutsDialog()`, closing this dialog first) instead of inert text; (4) confirmed `components/ui/dialog.tsx` has no CSS transitions/animations at all, so the include checklist's reduced-motion row is N/A-pass, not "unverified". Item 2 (shared `Dialog` title flat-type-hierarchy) was left as pre-existing shell residue per the triage, not fixed on this ticket.
3. Ran `tsc --noEmit` (clean) and the affected test scope (481/481) before the confirm round.
4. Seeded, started the dev server + in-page detector live-server, warmed routes, ran the confirm round `r2` (27 states × 1440/390, 0 errored) with `run-round.mjs`/`summarize.mjs`, built the contact sheet.
5. Re-ran the three readers (critique, evaluate, include) as fresh sonnet agents against the r2 sheet + detector JSON + keyboard probes.
6. Stopped the dev server and detector live-server, then ran the full suite once: `vitest run` (2698/2698 passed), `eslint .` (pre-existing 27 errors/49 warnings, none in files this ticket touched), `next build` (succeeded).
7. Wrote lessons back to both files with `check:` lines, refreshed the queue-shell area primer, wrote this report.

## Questions I answered on the owner's behalf
None this session — no open decision surfaced during the close; the fix batch was pure execution against the r1 triage (items already scoped as fixes, not decisions).

## Decisions recorded
Resolution comment on #264: three-way empty queue (first-use/filtered/done) shipped across all six queues via `QueueScreen`'s `empty` prop + `workspaceDocumentCount`, Invoices carrying first-use copy and the shared `InboundAddressLine`; account-menu "How DocuBite works" dialog replaces "Show welcome tour again" with a phone entry point on the Account page (spec assumption, since the account menu doesn't exist below `md`, #252). Welcome-tour code is untouched, per the ticket (#263 owns its removal, pending owner sign-off).

## Tickets created / changed
None created. #263 (welcome-tour removal, owner sign-off) and #266 (Add-documents dialog, second `InboundAddressLine` consumer) referenced but not touched — both already exist and are named in the hand-off/lessons.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=33 first-critique=34 close-critique=37 first-evaluate=88 close-evaluate=96 first-evaluate-health=91 close-evaluate-health=96

In-page detector: first-pass (r1) and close (r2) both show **0 non-residue findings** at 1440 and 390 on every ticket-owned state (three-way empty queue states across Invoices/Receipts/Exceptions/Approvals, the How-DocuBite-works dialog at 390, and the Account page). App-wide residue (ai-color-palette, overused-font, layout-transition, dark-glow) is identical both rounds (5/state), tracked separately. Non-ticket findings unchanged both rounds and left alone: rail focus-expansion occlusions/overflow on `help-menu-menu-open/closed@1440` (#250/#258 shell), `help-menu-dialog@1440` flat-type-hierarchy (shared `Dialog` title primitive, pre-existing), `pipeline-from-invoices` low-contrast (pre-existing pipeline page). `include`: PASS at both 1440 and 390 in both rounds; r2 additionally confirms the new "Keyboard shortcuts" button is keyboard-reachable/operable and reduced-motion is N/A-pass (no motion on the `Dialog` primitive). Pre-build step that was weakest: Part C's long-content coverage table specced the `<md` wrap rule for the address pill but not the `≥md` rule (see lessons below) — a P2 caught only at the first measurement, not pre-flight.

Design-detector hook: did not fire on any Edit/Write in this or prior sessions for this ticket — noted again; the in-page round remained the source of truth throughout.

## First-pass findings → lessons (rendered surfaces only)
| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status: none · unchecked · wrong · new class | Action taken |
|---|---|---|---|
| `inv-firstuse-co@1440` code pill wraps mid-token despite column room (H8/H3, P2) | Part C long-content row — only the `<md` break-all rule was written, the `≥md` nowrap rule was never stated | unchecked → converted to a check | Fixed: `max-md:break-all md:whitespace-nowrap`; lesson added to both lessons files with a detector `check:` (desktop-width text-overflow finding on a long-content state) |
| Dialog footer "Keyboard shortcuts are under the account menu, or press ?" is inert text naming another feature (H7/H10, P3) | No existing Part B contract covered "prose that names an actionable destination becomes a link" | new class | Fixed: made "Keyboard shortcuts" a real button; lesson added (generic file) so `articulate`/`evaluate` treat this pattern as P3-minimum, not a style nit |
| Reduced-motion on shared `Dialog` unverified (include, informational) | No check existed for "primitive has zero motion, so the row is N/A" vs. leaving it open | none → added | Confirmed by reading `dialog.tsx` (no transition/animate classes); lesson added so future include passes close this as N/A-pass rather than a lingering `?` |
| `help-menu-dialog@1440` flat-type-hierarchy on shared `Dialog` title (H8, P2) | Pre-existing shell primitive, out of this ticket's scope | no_change_needed | Not fixed here — recorded as shell residue, same disposition as the Keyboard-shortcuts dialog; no ticket needed (execution-only if/when the primitive itself is revisited) |

## Needs the owner
Nothing new. Pre-existing open items surfaced but not owned by this ticket: rail focus-expansion occlusions on the account menu (#250/#258), the shared `Dialog` title's flat type hierarchy (primitive-wide, would need its own ticket if the owner wants it changed), the pipeline page's pre-existing low-contrast text. The welcome-tour removal remains pending owner sign-off on #263, untouched by this ticket as instructed.
