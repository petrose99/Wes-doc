# Autopilot report — #264 Build the three-way empty queue on Invoices: first-use guidance, done, filtered — and How DocuBite works on the account menu

- Map: #226 Vic.ai-grammar operator app
- Type: wayfinder:task (execution) · Started: 2026-09-16 (spec phase) · Finished: same session, at the context line
- Outcome: partial — spec phase, hand-off at 150K before the spec critic (Part D)

## What I did
1. Claimed #264; loaded the map, the #241 resolution, #243's correction (no "turn on intake" link), #248's word (posted), #266's status (open, unbuilt).
2. Read the queue-shell primer, both lessons files, craft-floor, `QueueScreen`'s `empty` prop, the account menu, the inbound-email model/config, the schema fields for the done counts.
3. Called `intent` (specify → fortify → articulate → include) and `impeccable` (onboard → clarify → adapt; launcher not run — known unattended failure, PRODUCT.md read directly).
4. Wrote `logs/scratch-264/spec.md` (brief, vocabulary, the state function, screens at 1440/390, `QueueScreen` API, rendering table, 18-row state inventory, copy matrix, accessibility).
5. Wrote `logs/scratch-264/preflight.md` Parts A–C and E (two 4-targets H1/H5; six contracts with checks; predicted evaluate sum 9, health ≈ 88, Clean).
6. Context line reached → hand-off file written; Part D not run.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why |
|---|---|---|---|
| 1 | #266's dialog is unbuilt — what does Add invoices open? | #266's opener if shipped at build time, else a Link to `/pipeline?from=invoices` with TODO(#266) | smallest reversible option; a dead button is a Dead End (catalog); no blocking edge because #266 waits on owner sign-off #267 |
| 2 | Who builds the shared address block? | whichever of #264/#266 ships first; the other imports it | ticket comment from #243 + build order is the driver's |
| 3 | "today" for the done counts | midnight in `workspace.timezone` | the only timezone the schema holds |
| 4 | Other queues' first-use copy | keep their existing "No ‹noun›s yet." as first-use; done/filtered from the shared prop | #241 d.2: guidance lives on Invoices only |

## Decisions recorded
None on the ticket yet (spec phase; resolution comes at close).

## Tickets created / changed
None.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=32 first-critique=0 close-critique=0 first-evaluate=0 close-evaluate=0
No surface built; prediction is pre-critic (Part D pending).

## First-pass findings → lessons
Not applicable yet.

## Needs the owner
Nothing new. Sign-offs already pending elsewhere: #263 (tour removal), #267 (phone upload removal, blocks #266).
