# Hand-off — #262 Build the keyboard shortcut set

Map #226 · ticket #262 (`wayfinder:task`, execution) · decided on #240 · inputs #245 (`/`), #250 (`]`/`[`).
Owner delegation: recommended answers taken; no removals involved.

milestone: spec-done
milestone: build-done (code written and typechecked; the round-script + r0 capture step below was
NOT reached — hand off before it, per the context-line rule, rather than starting it thin)

## Done (spec phase, 2026-09-16)
See prior entry — spec at `docs/wayfinder-reports/226/logs/scratch-262/spec.md`, filled pre-flight
at `.../scratch-262/preflight.md` (Parts A–E), gate met (predicted critique 32/40, evaluate ≈90, 0 P1, Clean).

## Done (build phase, this session)
All files from spec §2 written/edited and `tsc --noEmit` passes clean at repo root:
- `lib/shell/keyboard-shortcuts.ts` — new. `localStorage["docubite.keyboardShortcuts"]` store,
  `useSyncExternalStore`, `docubite:keyboard-shortcuts-changed` custom event for same-tab mirror,
  in-memory fallback when `localStorage` throws. Test: `lib/shell/keyboard-shortcuts.test.ts`
  (2 cases) — **note**: this repo's vitest has no `jsdom` package installed, so only the
  SSR/no-window path is tested here; the client toggle/storage-event/fallback behaviour described
  in spec §10 is unverified by a unit test and needs the live keyboard probe in the measure phase
  to actually exercise it (off→letters no-op, other-tab mirror, etc.) — flag this as a real gap,
  not a skip.
- `components/ui/switch.tsx` — new track/thumb `role=switch` primitive. Used by the shortcuts
  dialog's foot control. **Not yet wired into** `queue-screen.tsx`'s Override toggle or
  `module-row.tsx`'s pill toggle — spec's B4 said "third use, queue-screen re-imports it"; this
  session left both existing switches on their own markup to limit blast radius under the context
  budget. This is a B4 gap to close in the confirm/close phase or note as accepted scope-narrowing.
- `components/shell/keyboard-shortcuts.tsx` — new. `KeyboardShortcuts` host (single `document`
  keydown listener, `md`+ gated via `matchMedia`, `g`-sequence with 1.5s timer, `/`, `f`, `]`/`[`
  mismatch-nav scoped to `#queue-detail-pane`, `?` dialog toggle reachable even when shortcuts are
  off) + `KeyboardShortcutsDialog` (three groups, `Kbd`/`Sequence` helpers, foot `Switch`) +
  `openKeyboardShortcutsDialog()` exported for the account menu. `SHORTCUT_DESTINATIONS()` builds
  the 8 rail destinations (all currently unconditional — sidebar never actually hides any of the
  eight today, so the spec's "unbuilt destination has no key" filtering was not implemented; if a
  destination ever becomes conditional this needs revisiting).
- `components/shell/sidebar.tsx` — renders `<KeyboardShortcuts destinations={...} />` once, inside
  the `<aside>`; `SHORTCUT_KEY_BY_LABEL` map feeds the collapsed-rail `title` tooltip suffix
  (`Invoices · g i`).
- `components/shell/account-menu.tsx` — new "Keyboard shortcuts" menuitem (Keyboard icon),
  dispatches `openKeyboardShortcutsDialog()`; trailing "Off" text + `aria-label` suffix via
  `useKeyboardShortcutsEnabled()`.
- `components/shell/skip-to-list-link.tsx` — new client component (layout.tsx is a Server
  Component; an inline `onClick` on a bare `<a>` there is a Next.js RSC build error, so this had
  to be extracted rather than inlined as the spec implied). "Skip to the list" — visible only when
  `document.body.dataset.queueList` is set (via `[[data-queue-list]_&]:block`), writes
  `sessionStorage["docubite.pendingFocus"]="rows"` and dispatches `docubite:focus-rows`.
- `app/(app)/workspaces/[workspaceId]/layout.tsx` — renders `<SkipToListLink />` after the existing
  "Skip to content" skip link, before `<Sidebar>`.
- `components/queue/queue-screen.tsx` — roving tabindex on desktop row buttons (`rovingId` =
  open row ?? last-focused ?? first row; `tabIndex={id === rovingId ? 0 : -1}`), row `onKeyDown`
  (↓/↑ only when `openId === null`, so the existing document-level pane handler isn't double-fired;
  Home/End always; → checkbox when selectable), checkbox `tabIndex={-1}` + its own ↓/↑ (adjacent
  checkbox) and ←/→ (back to row button) handlers, `data-row-id` added to `<tr>`, `id="queue-list"`
  on the scroll container, `document.body.dataset.queueList` set/cleared via `useLayoutEffect`,
  `sessionStorage["docubite.pendingFocus"]` consumed in a `useEffect` keyed on `[ids, openId]`
  (falls back to `#queue-title` focus when `ids` is empty), and a `docubite:focus-rows` listener
  for the same-route `g` jump case.
- `app/(app)/workspaces/[workspaceId]/search/search-client.tsx` — consumes the `"search"` pending-
  focus flag (belt-and-braces beside the existing `autoFocus` on the input).

## NOT done — build gate incomplete, this is the real next step
The build-phase gate (per the brief) requires: seed + servers up, write the ticket's round script
on `scripts/wayfinder-autopilot/capture-round.mjs`, run it once as `shots-r0` (every state at 1440
**and** 390, detector JSON, keyboard probes for every Part B focus/keyboard contract), then fix
every real detector finding and every failing probe **in this same build phase**, before writing
`build-done`. **This session did not reach that step** — code was written and typechecked but
never run against a live dev server or exercised by an actual keyboard probe. Do not treat
"build-done" above as "verified"; treat it as "written, typechecks, unverified live."

### Exact next step for the continuing session
1. Bring up devdb + dev server (see `docubite_live_ui_verification` memory: :3000 with
   `DEV_AUTH_BYPASS`, devdb :55433, seed script).
2. Write the round script (model on `scratch-257/pw/round261-r2.mjs` per the spec pointer below).
   States to capture: invoices list (closed pane) · invoices list with pane open (an invoice with
   a View PO row, so `]`/`[` has something to move across) · dialog open. Each at 1440 **and** 390
   (dialog itself only needs 1440 per the original pointer, but capture 390 for the listener-off
   check too). Keyboard probes, one per spec §10 "Live probes" line:
   - Tab count from load to first row focus at 1440 (expect ≤ 3: skip → skip → Enter)
   - `g i`, `g p`, `g r`, `g b`, `g e`, `g a`, `g y`, `g d` each land + `document.activeElement` is
     a row button (or `#main` for Admin)
   - `g` then an unrecognized key clears pending without acting
   - `?` opens the dialog; `Esc` returns focus to the opener
   - toggling the switch off: subsequent `g i` no-ops, arrows/Home/End/Enter still work; menu shows
     "Off"; toggle back on
   - `/` focuses the search input on the search route
   - `f` focuses the first facet chip
   - `]`/`[` cycle mismatch buttons inside an open invoice pane with a View PO row; no-op with no
     such pane
   - 390: confirm the listener is not attached (`g i` does nothing), table replaced by cards,
     cards untouched by any of the above
3. Fix every real detector finding and every failing probe *in place* — this is lint, not a design
   pass; no readers (critique/evaluate/include) run in this phase, those are the measure session's.
4. Decide/resolve the two flagged gaps above (Switch B4 reuse in queue-screen + module-row; the
   `keyboard-shortcuts.test.ts` DOM-behaviour gap) — at minimum note them explicitly in the r0
   write-up if left as accepted scope-narrowing rather than fixed.
5. Write r0 detector/probe counts per width to this file, then the line `milestone: build-done`
   (replacing the tentative one above with a verified one), commit `wip(autopilot): #262 build`.

## Decisions taken in the spec (why)
See prior entry — unchanged.

## Measure phase pointers (later)
- Round script on `scripts/wayfinder-autopilot/capture-round.mjs`, modelled on `scratch-257/pw/round261-r2.mjs`; states:
  invoices list · pane-open · dialog-open (1440 + 390; dialog only at 1440) + keyboard entries for every §10 probe.
- Baseline detector (incumbent, #240): 1440 list/pane 3/4 · 390 3/3; residue per primer.
