# Hand-off — #262 Build the keyboard shortcut set

Map #226 · ticket #262 (`wayfinder:task`, execution) · decided on #240 · inputs #245 (`/`), #250 (`]`/`[`).
Owner delegation: recommended answers taken; no removals involved.

milestone: spec-done
milestone: build-done
milestone: measured
milestone: close-fix-batch-done (this session — bar met, report/close not yet written)

## State at end of this session
All 4 P1/P2 findings from the measure phase fixed and confirmed live. Bar met:
critique 38/40 (no heuristic <3) · evaluate health 88, Clean, P0=0 P1=0 P2=1 P3=1 ·
include PASS, failures=0. Full checks green: vitest (2693/2693), `tsc --noEmit` clean,
`eslint .` — 0 errors in touched files (27 pre-existing errors elsewhere, unrelated),
`next build` succeeds. Dev server + impeccable live-server both stopped cleanly.

Fixes made this session (all committed on this branch, not yet as a final ticket-close commit):
1. `components/shell/keyboard-shortcuts.tsx` — Admin (`g d`) now writes pendingFocus="main"
   and a new pathname-keyed effect consumes it (focuses `#main`) since Admin has no row list.
2. `components/ui/dialog.tsx` — Esc/close focus-return now falls back to `#main` when the
   captured opener was `body` (was stranding focus on the document root).
3. `app/(app)/workspaces/[workspaceId]/search/page.tsx` — no longer redirects to the
   workspace root when `q` is empty (only bare `ask=1` with no `q` still redirects) — this
   was a REAL bug (not the timing flake the measure phase suspected): the `/` shortcut
   deliberately arrives with no query and was being bounced home every time.
4. Round script `docs/wayfinder-reports/226/logs/scratch-257/pw/round262.mjs` strengthened:
   longer slash wait, a real skip-to-list probe (was testing raw Tab-walk instead), real
   evidence for `shortcuts-back-on` (was a bare "done" marker), `button-count` evidence on
   the mismatch-nav probe (single-button pane wrap is correct, not a failure).

Confirm captures: `docs/wayfinder-reports/226/logs/scratch-262/shots-r5/` (detector.json,
keyboard.json, PNGs, contact-sheet.png) — this is the authoritative round.

Primer refreshed: `docs/agents/areas/queue-shell.md` (new "Keyboard shortcuts (#262)" section).
Lessons appended: `.claude/wayfinder-autopilot/lessons.md` (4 lines) and
`scripts/wayfinder-autopilot/lessons.md` (1 line) — both under the ~80-line cap.

## Known accepted scope-narrowing (not fixed, noted not silently dropped)
- B4 Switch-reuse gap: `components/ui/switch.tsx` not wired into `queue-screen.tsx`'s
  Override-mode menu item or `module-row.tsx`'s pill toggle — both are visual changes to an
  existing menu row, not pure execution; left open, documented in the primer.
- `lib/shell/keyboard-shortcuts.test.ts` still only covers the SSR/no-window path (no jsdom
  package installed in this repo) — the client toggle/storage-event behaviour is verified
  live (keyboard.json shortcuts-off/-back-on entries), not by a unit test.

## Exact next step for the closing session
1. Post the resolution comment on ticket #262 (gh issue comment), close it.
2. Append one line to map #226's Decisions so far.
3. Write the final report at `docs/wayfinder-reports/226/262.md` per the brief's format,
   including the scores line: `predicted-critique=32 first-critique=36 close-critique=38
   first-evaluate=84(health) close-evaluate=88(health) ...` and the first-pass-findings →
   lessons table (4 rows: Admin focus, dialog body-fallback, search-redirect, skip-link
   probe-gap — all mapped to the lessons appended above).
4. `git add -A && git commit` the report + any final state, referencing #262.
5. Nothing else pending — the surface, tests, lint, build and both readers are already green.
