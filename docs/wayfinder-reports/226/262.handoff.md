# Hand-off — #262 Build the keyboard shortcut set

Map #226 · ticket #262 (`wayfinder:task`, execution) · decided on #240 · inputs #245 (`/`), #250 (`]`/`[`).
Owner delegation: recommended answers taken; no removals involved.

milestone: spec-done

## Done (spec phase, 2026-09-16)
- Claimed (assignee petrose99). Read map, #240 resolution, both lessons files, `docs/agents/areas/queue-shell.md`,
  `detail-pane.md`, craft-floor, `preflight.md` template.
- Spec: `docs/wayfinder-reports/226/logs/scratch-262/spec.md` — §0 brief (shape), §1 vocabulary, §2 files/components,
  §3 behaviour (roving rows · arrival focus · listener · off switch · dialog content), §4 dialog layout/type, §5 tooltips,
  §6 state inventory (fortify), §7 every string (articulate/clarify), §8 include, §9 from=, §10 tests + live probes.
- Pre-flight: `.../scratch-262/preflight.md` Parts A–E filled; Part D critic (sonnet) found 2 P1s (`g y` landing
  ambiguous; `]`/`[` listed as universal) — both fixed in the spec text. Gate met: predicted critique **32/40**
  (H1, H5 = 4), evaluate ≈ 90, 0 P1, Clean.

## Decisions taken in the spec (why)
- `/` registered now → `/search` (route exists; #245 input); #270 re-points it. `]`/`[` next/prev `≠` glyph, scoped to
  invoice panes with a View PO row (#250 input; focus only — no key acts on data).
- One switch, in the dialog foot (CONTEXT.md glossary: "one switch"); the account-menu item "Keyboard shortcuts" opens the
  dialog and shows trailing "Off" as the mirror.
- Second skip link "Skip to the list" in the workspace layout (hidden unless a queue is mounted) → Tab, Tab, Enter reaches
  the first row (DoD ≤ 3).
- Roving group = desktop table row buttons; checkboxes `tabIndex=-1`, reached with `←`; cards (<md) untouched.
- Arrival focus via `sessionStorage["docubite.pendingFocus"]` consumed by `QueueScreen` after rows/empty render.
- Host `KeyboardShortcuts` rendered by `Sidebar` (owns the rail list); destinations derived from rail items → unbuilt/hidden
  items have no key. Desktop gate `matchMedia(min-width:768px)`.
- `Switch` extracted to `components/ui/switch.tsx` from the Override toggle (B4: same control, third use).

## Next phase: BUILD (fresh session)
1. Read `docs/agents/areas/queue-shell.md`, then `spec.md` §2–§4 and `preflight.md` Parts B–C (build checklist rows).
2. Files: `lib/shell/keyboard-shortcuts.ts` (store), `components/shell/keyboard-shortcuts.tsx` (hook + dialog + Kbd),
   `components/ui/switch.tsx`, edits to `components/shell/sidebar.tsx` (host + tooltips), `account-menu.tsx` (item),
   `components/queue/queue-screen.tsx` (roving rows, `data-queue-list`, pendingFocus), workspace `layout.tsx` (skip link),
   `search-client.tsx` (pendingFocus → input). Tests per spec §10 (vitest on touched dirs only).
3. Detector hook per edit; no capture; `tsc --noEmit` once at the end (dev server stopped). Then write
   `milestone: build-done` here with what is built/unverified, commit `wip(autopilot): #262 build`.

## Measure phase pointers (later)
- Round script on `scripts/wayfinder-autopilot/capture-round.mjs`, modelled on `scratch-257/pw/round261-r2.mjs`; states:
  invoices list · pane-open · dialog-open (1440 + 390; dialog only at 1440) + keyboard entries for every §10 probe.
- Baseline detector (incumbent, #240): 1440 list/pane 3/4 · 390 3/3; residue per primer.
