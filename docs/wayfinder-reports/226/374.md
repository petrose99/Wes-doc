# Autopilot report — #374 Build WhatsApp intake

- Map: #226 · Type: build · Started: (spec session) · Finished: 2026-09-22
- Outcome: resolved

## What I did

1. Spec: WhatsApp channel mirrors inbound-email intake into WP13, per #372's decision on #226
   (schema/model/webhook, Admin › Configuration › Intake panels, add-sender dialog, queue +
   detail-pane surfacing, fortify states, copy pass). Hand-off milestone `spec-done`.
2. Build (5 steps, commits `dcf7d9c`, `f8cc562`, `6413e7b`, `a2d8a24` + G1 round script,
   `695ffe6`): schema + `models/inbound-whatsapp.ts` + webhook route; Admin Intake panel set
   (`whatsapp-settings.tsx`); add-sender dialog, queue sender chips/facet, detail-pane audit line;
   fortify (offline/duplicate/no-document/jurisdiction-missing outcomes); copy pass. Milestone
   `build-done` (`d833920`, gate clean at r0 — 0 real findings, 9/9 probes, both widths).
3. Measure (r1, `b34e4f9`): fresh critique/evaluate/include readers on 13 states × 1440/390.
   Findings: evaluate found 1 P1 (H9 — recovery states existed in code but weren't captured, and
   two validation error codes fell through to the raw-code fallback); critique's 9/40 turned out
   to use the wrong scale (see triage). Milestone `measured`.
4. Close: triaged every r1 finding in `<scratch>/close.md` — detector findings all matched
   `docs/agents/areas/admin.md`'s documented residue; the critique-reader score was invalid
   (wrong scale, verified against `critique.md:392`); the H9 P1 and the low-contrast toast
   (`#e60000`/`#fff0f0`, same shortfall class #251 already fixed for `success`) were real and
   fixed. One fix batch (c1): `components/ui/sonner.tsx` `error` classNames override,
   `action-helpers.ts` `BILLING_MESSAGES` entries for `phone_number_invalid`/`label_required`,
   round-script state `05-add-sender-error`. Gate on `shots-c1` vs. `shots-r1` baseline: clean,
   0 real findings, 0 new-since-baseline. Confirm round (fresh sonnet readers, scale stated
   explicitly this time): critique 30/40, evaluate health 94 (P0 0, P1 0, Clean), include
   Pass/Pass. Checks run once, dev server stopped: vitest full suite 2826 pass, `tsc --noEmit`
   clean, `eslint` clean on changed files, `next build` succeeded. Milestone
   `close-fix-confirmed` (`5291c58`).
5. This session: wrote this report, the lessons (written to a pending file — see below), confirmed
   the area primer (`docs/agents/areas/admin.md`) was already updated by the prior close session,
   appended the map decision line, and closed the ticket.

## Questions I answered on the owner's behalf

None this session — no open questions; the ticket's scope was fixed at spec from #372's decision.

## Decisions recorded

WhatsApp intake ships mirroring inbound-email's model end to end (schema, model, webhook, Admin
config panel, add-sender dialog, queue/detail surfacing, fortify states, copy). Closed with one
named gap: H10 (help/documentation) at 2/4 — no dedicated help affordance beyond inline body copy,
P2/cosmetic, not a defect.

## Tickets created / changed

None created. No P0/P1/decision-class findings remained open after the fix batch.

## Scores and counts (rendered surfaces only)

scores: predicted-critique=n/a first-critique=9 close-critique=30 first-evaluate=85 close-evaluate=94

Note on `first-critique=9`: the r1 critique reader used evaluate's descending scale instead of
critique's ascending 0-4 scale (verified against `impeccable/reference/critique.md:392`) — an
invalid reading, not a real first-pass score; recorded as-produced per the scoreboard's
integers-only rule, corrected by the confirm round's explicit-scale re-run (30/40).

Gate counts: r0 (build-done baseline) — 0 real findings, 9/9 probes pass, both widths, 11 states.
r1 (measure) — 0 real detector findings (all residue), 1 P1 (evaluate H9). c1 (close, 13 states
incl. new `05-add-sender-error`) — 0 real findings, 0 new-since-baseline, 11/11 probes pass, both
widths. Include: r1 no P0/P1 (one P3 coverage-gap note); c1 Pass/Pass. Weak pre-build step: none
identified — the P1 was a capture-coverage gap (states existed in code, weren't in round374.mjs's
r1 state list) rather than a missed spec/Part-B line.

## First-pass findings → lessons (rendered surfaces only)

| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| evaluate H9 P1 — recovery states existed in code, uncaptured; two error codes fell through to raw-code fallback | None named the fallback as a risk for new model error codes | none — new lesson written | Added `phone_number_invalid`/`label_required` to `BILLING_MESSAGES`; added round state `05-add-sender-error`; lesson drafted (see below, pending write) |
| low-contrast `#e60000`/`#fff0f0` on sonner `error` toast | #251's fix covered only the `success` variant; no contract says "every `richColors` variant" | unchecked — existed as a precedent (#251), not generalized into a check | Added matching `error` override in `sonner.tsx`, same fix shape as #251 |
| critique r1 = 9/40 (invalid scale) | No contract enforces stating the critique reader's scale explicitly per run | none — new lesson written | Confirm round re-ran with the scale stated explicitly in-prompt; lesson drafted (pending write) |
| evaluate H3 "no remove-sender path" | Capture-coverage gap (no state showed an existing sender row), not a code defect | tooling — verified against `whatsapp-settings.tsx:76` (`RemoveWhatsAppSenderButton` exists) | No code change; named as a coverage note, not chased into a 4th round (no P1 depended on it) |

Both lessons above were drafted for `.claude/wayfinder-autopilot/lessons.md` (project lessons) but
the sandbox refuses editing that file directly in this worktree (same restriction hit on #342).
Written instead to `docs/wayfinder-reports/226/374.project-lessons-pending.md` for a session with
write access to append verbatim.

## Needs the owner

None. No removal, credential, or ambiguous-decision item arose on this ticket.
