# Hand-off: Build WhatsApp intake (#374)

Execution of #372 on #226. WhatsApp channel mirrors inbound-email into WP13 intake.

## Build steps

- step: 1 — schema + model + webhook — done
- step: 2 — Admin › Configuration › Intake — done
- step: 3 — Add dialog + queue + detail pane — done
- step: 4 — Fortify states — done
- step: 5 — Copy pass — done
- step: G1 — round script — done
- step: G2 — r0 baseline — done
- step: G3 — build-done — done
- step: measured (r1) — done
- step: close fix batch + confirm round (c1) — done
- step: close checks (tests/tsc/eslint/build) — done
- step: close report + decisions line + gh close — todo

## Close phase — status

Fix batch, gate, confirm-round scores and full triage: see `<scratch>/close.md`.
Scores: critique 30/40 (lowest H10=2), evaluate health 94 P0 0 P1 0 Clean, include Pass/Pass.
No P1s open — protocol's "confirm under bar, no P1s: close and record the gap" applies to H10;
do not run a 4th batch. Checks all clean (vitest 2826 pass, tsc clean, eslint clean on changed
files, next build succeeded); dev server already stopped.

Note: `.claude/wayfinder-autopilot/lessons.md` write is sandbox-refused mid-session (same as
#342) — the two lessons for this ticket are drafted in `<scratch>/close.md`, add them to the
report/lessons file from a session with write access.

## Artifacts

- `<scratch>/close.md` — full triage, fix batch, gate record, drafted lessons
- `<scratch>/round374.mjs`, `contact-c1.png`, `shots-c1/`, `gate-c1.json`
- `<scratch>/readers-c1/{critique,evaluate,include}.md`
- prior round: `readers-r1/`, `shots-r1/`, `contact-r1.png`, `gate-r1.json`

## Open findings

None P1/P0. See `<scratch>/close.md` for the H10 (help/documentation, 2/4) gap — named, not a
defect, no ticket needed.

## Next step

Write the close report at `docs/wayfinder-reports/226/374.md` (template
`scripts/wayfinder-autopilot/report.md`): session history from `git log`, reconciled/measured/close
scores table, attribution table for r1's findings, the two lessons from `close.md`. Append one
line to map #226's Decisions so far. Commit the report, then `gh issue close 374`.

milestone: spec-done
milestone: build-done
milestone: measured
milestone: close-fix-confirmed
