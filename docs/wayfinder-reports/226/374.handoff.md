# Hand-off: Build WhatsApp intake (#374)

Execution of [Decide WhatsApp intake (#372)](https://github.com/petrose99/Wes-doc/issues/372) on [Map #226](https://github.com/petrose99/Wes-doc/issues/226). Mirrors the existing inbound-email intake channel (WP13) as a second channel into the same ingestion path.

## Recon (read-only pass, this session)

- `app/api/inbound-email/route.ts` (84 lines): `config.inboundEmail.enabled` gate → bearer-secret auth (not provider HMAC) → parse Postmark-shaped body → `resolveWorkspaceByInboundToken` → healthcare block → jurisdiction gate → `processInboundEmail`.
- `models/inbound-email.ts` (305 lines): `ensureInboundEmailToken`, `resolveWorkspaceByInboundToken`, `matchesAllowPattern`/`isSenderAllowed`, `listAllowedSenders`/`listRecentIntakes` (cached), `addAllowedSender`/`removeAllowedSender`, `processInboundEmail` (records `InboundEmailIntake` row always, checks allowlist, classifies intent, ingests every attachment via `createIngestionItem`).
- Schema: `InboundEmailAllowedSender` (`prisma/schema.prisma:2038`, `{id, workspaceId, pattern, createdById?, createdAt}`, `@@unique([workspaceId, pattern])`), `InboundEmailIntake` (`:2758`, `{id, workspaceId, fromAddress, subject?, bodyPreview?, attachmentCount, acceptedCount, rejectedCount, outcome, createdAt}`, indexed on `[workspaceId, createdAt]` and `[workspaceId, outcome]`). `Workspace.inboundEmailToken` + back-relations (~223, 257).
- Admin UI: `app/(app)/workspaces/[workspaceId]/admin/configuration/intake/page.tsx` (91 lines, server component) → `components/settings/inbound-email-settings.tsx` (client: `InboundEmailAddress`, `AddAllowedSenderForm`, `RemoveAllowedSenderButton`) + server actions in `app/(app)/workspaces/[workspaceId]/inbound-email-actions.ts`. No bespoke field-table component — generic `Panel`/`Empty` from `components/automation/automation-ui` and `AdminPage`/`ReadOnlyBand` from `components/admin/admin-ui`.
- `components/intake/add-documents-dialog.tsx`: renders `<InboundAddressLine address={inboundAddress} />` (from `components/intake/inbound-address-line.tsx`, 26 lines) at line 179 when `inboundAddress` prop is set. Only usage site found this pass; Receipts empty-state usage not yet located — check before step 3.
- `lib/ingestion.ts`, `createIngestionItem` (line 34): the shared funnel every channel uses (`source: "upload"|"camera"|"email"|"zip"|"api"`, line 12's `IngestionSource` union). Jurisdiction gate + `documentHash` dedup, wraps `models/documents.ts::createDocumentFromBuffer` unchanged. WhatsApp adds `source: "whatsapp"` + a `sourceWhatsapp`-shaped field analogous to `sourceEmail`.
- Detail-pane "how did this arrive" provenance UI: **does not exist today** — only per-field extraction-rationale badges (`rule`/`ai`/`few_shot`) in `field-row.tsx`/`rationale-popover.tsx`. This is new UI, not an extension, for both the email and WhatsApp channel — step 3 builds it from scratch (channel + sender chip), not by finding an existing pattern to copy.
- Feature flag pattern (`lib/config.ts:409`): `inboundEmail: {enabled: Boolean(env.EMAIL_INBOUND_SECRET), secret, domain}` — presence of the secret env var is the on/off switch, deployment-wide; per-workspace routing via `Workspace.inboundEmailToken`. WhatsApp mirrors this: `whatsapp: {enabled: Boolean(env.WHATSAPP_APP_SECRET && env.WHATSAPP_VERIFY_TOKEN), appSecret, verifyToken, phoneNumberId, businessAccountId}`.

Platform facts from #373 that shape step 1: media URLs expire 5 min after issue (fetch synchronously on webhook receipt, no deferred queue); webhook payloads must be HMAC-SHA256 verified via `X-Hub-Signature-256` over the *raw* request body (hash before JSON parsing).

## Build plan

- step: 1 — schema + model + webhook route — done
  - Spec lines: #374 item 1 ("Webhook + routing"), item 2's routing logic (unknown/ambiguous number, Receipts default).
  - Files built: `prisma/schema.prisma` (`WhatsAppAllowedSender` — routed by `phoneNumber` directly, no per-workspace token, since one number serves every workspace on the deployment; `WhatsAppIntake` with nullable `workspaceId` for unknown/ambiguous sends; `Document.sourceWhatsapp`), `prisma/migrations/20260921220000_add_whatsapp_intake/` (hand-authored — see note below), `models/inbound-whatsapp.ts` (new: `normalizePhoneNumber`, `resolveWorkspacesByPhoneNumber` returns *every* matching workspace so the caller decides unknown/single/ambiguous rather than the model picking one, `listAllowedSenders`/`listRecentIntakes`, `addAllowedSender`/`removeAllowedSender`, `processInboundWhatsApp` — always targets the Receipts template per #372 item 2, no intent classification), `lib/whatsapp/client.ts` (new: `verifyWebhookSignature` HMAC-SHA256 over raw body, `downloadWhatsAppMedia` synchronous fetch within the 5-min TTL, `sendWhatsAppText`), `app/api/inbound-whatsapp/route.ts` (new: GET verification challenge, POST with signature check → per-message unknown/ambiguous/no-media/ingest branching → ack reply), `lib/config.ts` (`whatsapp` block, same "unset secret = feature dark" convention as `inboundEmail`), `lib/ingestion.ts` + `lib/analytics.ts` + `models/documents.ts` (extended to carry `"whatsapp"`/`sourceWhatsapp` alongside the email equivalents).
  - Deliberately deferred to step 4 (fortify), not built here: the ambiguous-number *keyword-reply* flow (today it refuses cleanly with one message and logs `sender_ambiguous` rather than storing a pending "reply 1 for X" choice); duplicate/burst/storage-cap handling; the unreadable-photo follow-up message (today the ack is based on whether ingestion accepted anything at intake time, not on a later extraction-failure signal).
  - Check, as run: `npx tsc --noEmit` passes clean (repo-wide, not just changed files); `npx eslint` clean on every new/touched file (one pre-existing unrelated warning in `lib/ingestion.ts` confirmed via `git stash` to predate this session); `verifyWebhookSignature` unit-checked directly with `tsx` — valid signature accepted, invalid and missing signatures rejected. The migration was applied to `docubite-devdb` (port 55433) via `docker exec ... psql` and marked applied with `prisma migrate resolve --applied`; `npx prisma generate` succeeded against the updated schema.
  - **Not done this step, and why:** a live end-to-end curl against a running dev server was not completed. `npx prisma migrate diff` against devdb's live state showed large *pre-existing, unrelated* drift (dropped columns with live data, renamed indexes) that has nothing to do with this change — I did not apply that generated script; I hand-wrote a minimal, additive-only migration instead (schema/model correctness confirmed by `tsc`/`prisma generate`, not by a live diff). Separately, an existing `next dev --turbopack` process (PID 1959325, port 3002) was already running — Turbopack refuses a second instance, and it isn't safe to assume that server belongs to this session or to kill it. A future step (or the step 6 gate) should curl `/api/inbound-whatsapp` against a dev server that has the `WHATSAPP_*` env vars set, once one is available to this session without colliding with another session's server.
- step: 2 — Admin › Configuration › Intake WhatsApp section — todo
  - Spec lines: #374 item 3.
  - Files: extend `app/(app)/workspaces/[workspaceId]/admin/configuration/intake/page.tsx`, new `components/settings/whatsapp-settings.tsx` (mirrors `inbound-email-settings.tsx`), new server actions file mirroring `inbound-email-actions.ts`.
- step: 3 — Add dialog / Empty queue / Receipts row / Detail pane — todo
  - Spec lines: #374 item 4.
  - Files: `components/intake/add-documents-dialog.tsx` (add WhatsApp line beside `InboundAddressLine`), Receipts empty-state (locate first), Receipts queue row component (attribution chip, filter chip), Detail-pane audit section (new provenance UI — none exists yet, build channel+sender chip from scratch per recon item 7).
- step: 4 — Fortify states — todo
  - Spec lines: #374 item 5, #372's states table.
  - Files: `models/inbound-whatsapp.ts` (reply-line branches), webhook route (media-type checks, burst cap, duplicate hash check reusing `documentHash`).
- step: 5 — Copy pass (`clarify`) — todo
  - Spec lines: #374 item 6's copy half, #372's exact reply lines.
- step: 6 — Gate: `shape` review, `impeccable detect --json`, in-page overlay, critique/evaluate per CLAUDE.md closing bar — todo (build gate, last step)

## Status

milestone: build-done (step 1 of 6)

Step 1 built, checked, and committed. Next session/step: build step 2 (Admin › Configuration › Intake WhatsApp section) — mirror `components/settings/inbound-email-settings.tsx` and `app/(app)/workspaces/[workspaceId]/inbound-email-actions.ts` for `whatsapp-settings.tsx` / a new whatsapp-actions file, wired into the same `admin/configuration/intake/page.tsx`. Read this hand-off and the step-1 diff (not the whole repo) before starting; the recon above already covers the Admin UI's file locations and shared primitives.
