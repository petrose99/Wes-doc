# Autopilot report — #252 Build the Admin area shell and Configuration: rail Admin item, Controls folded in, field table, Suppliers, Tax, Integrations

- Map: #226 Make the operator app work like Vic.ai (grammar, not skin)
- Type: wayfinder:task · Started: 2026-09-16 07:24 UTC (this attempt; third session on the ticket) · Finished: 2026-09-16 09:35 UTC
- Outcome: resolved

## What I did

This attempt picked up the hand-off from the two earlier sessions (WIP `bd6233d`, cap `21d4615`): the surface was built and critiqued (fresh-reviewer 34/40, all ≥ 3), but `evaluate`, `include`, the report, lessons and the close had not been done. Per the hand-off: no rebuild, no re-critique.

1. Claimed the ticket (assigned to petrose99). Loaded `evaluate`, `include`, `impeccable polish`; ran `impeccable context` through the engine wrapper (`.impeccable/live/imp.mjs`), read `craft-floor.md`. Its `AUTONOMY_DIRECTIVE_CHECK` asks for a structured question probe; the owner's standing delegation for this run forbids `AskUserQuestion`, so the substitution is stated here instead of probed.
2. Started devdb/dev server/live-server, warmed every Admin route (`probe252.mjs`), ran **one capture pass** (`admin252.mjs all verbose` → `logs/scratch-252/capture-round2.log`, PNGs in `.impeccable/live/shots252/`): 21 states × 1440/390, in-page detector per state, keyboard probes (Tab trails, tablist arrows, Space toggling, save-bar reachability, leave guard on nav click and Back, Ctrl+S, phone save-bar position, landmarks).
3. **First `evaluate`** (fresh-context agent reading that capture set + code): **76/100, one P1, Clean**. The P1: three save grammars inside Configuration — Tax › Jurisdiction had its own inline Save, Tax › Deferred VAT scheme *autosaved on change* with a toast, Users › Workspace name had an inline Save — contradicting #231's "explicit Save on a sticky bar" and bypassing the leave guard.
4. **Fix batch 1** (every P1 and every P2/P3 it named):
   - `components/admin/tax-form.tsx` — jurisdiction, VAT scheme and per-row goods/services nature edits behind one `AdminSaveBar`; `category-nature-table.tsx` reports nature edits to the enclosing form (`onNatureChange`), Add/Remove stay row actions; the two legacy pickers deleted.
   - `components/admin/company-name-form.tsx` (sticky-bar save) on **Companies**, beside Delete/Leave, both moved off Users (`danger-zone.tsx` lost the inline rename).
   - `field-table-editor.tsx` — visible footnote naming the locked fields and why, `title` on the cell; phone table now fits 390 (measured `tableW 350 = wrapW 350`), header "Move" below `md`.
   - `admin/loading.tsx`, `account/loading.tsx` — page-frame skeletons in the same 880px column.
   - `admin/layout.tsx` renders the company caption below `md` on every Admin page (`AdminPage` lost the per-page `company` prop).
   - `account-menu.tsx` — focus to first item on open, ↑/↓/Home/End, Escape returns focus to the chip, Tab closes.
   - `admin-leave-guard.tsx` — the app's `ConfirmDialog` ("Leave without saving?", Cancel focused) for link clicks and Back; `unsaved-changes.ts` gained `hasUnsaved`/`unsavedReason`/`clearUnsaved`; `beforeunload` keeps the native prompt.
   - `confirm-dialog.tsx` footer lost its grey fill (detector `nested-cards` in every dialog-open state, app-wide).
   - `admin-save-bar.tsx` collapses its margin while hidden (a 40px hole showed under the name form).
   - `next.config.ts` — merged the duplicate `experimental` key from `a3d15cd` (it broke `tsc` and would have broken `next build`).
5. **Confirm capture** (`capture-round3.log`, `-3b`, `-3c`): every state at the app-wide residue; all keyboard probes pass with the new dialog.
6. **Confirming `evaluate`**: **87/100, zero P0/P1, Clean, every heuristic ≤ 1.** Three of its P2/P3 items were pure execution and were fixed in the same batch window (partial-save refresh in `tax-form.tsx`, Companies intro names the company instead of "the left nav", Tab closes the account menu); the one with a decision went to #257 (below).
7. Close checks with the dev server stopped (the box cannot run `tsc` beside `next dev`): `tsc` clean, `eslint` clean on touched files, full `vitest`, `next build` — see "Scores and counts".
8. Lessons appended (4 generic, 4 project), resolution comment posted, ticket closed, map's Decisions so far appended, fog graduated, committed on `codex/post-login-ux-implementation`.

## Questions I answered on the owner's behalf

| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | `impeccable context` asks to probe the user with a structured question before inferring | Inferred from the brief; substitution stated | Owner's standing delegation: never `AskUserQuestion` |
| 2 | Is the category-nature table's per-row nature select a "setting" (sticky bar) or a "row action" (immediate)? | Setting — routed through the Tax form's Save; Add/Remove stay immediate | #231 Q23 explicit Save for policy; Suppliers/members are the row-list precedent for Add/Remove |
| 3 | Where do Company name and Delete/Leave live — Users or Companies? | Companies | #231 IA: Users = this company's people; a company's own facts belong to Companies (evaluate H8) |
| 4 | Fix the pre-existing `next.config.ts` duplicate key (owner's chore commit) inside this ticket? | Yes, merged the two `experimental` blocks | Smallest reversible option; it blocked `tsc` and `next build`, which the close bar requires |
| 5 | Spend a second batch after the confirm? | Only the three execution-only items the confirm named; no third measurement | Brief: a second batch only if the first removed a P1 (it did); never a batch to turn a 1 into a 0 |

## Decisions recorded

Admin ships as #231 decided: rail **Admin**, Controls folded in, `/settings/*` and `/automation/*` redirect, two captioned nav groups, Configuration (Fields table driving pane + queue columns, Autonomy, Checks, Report, Intake, Tax, Payments, What's on), Approval Flows / PO Mismatch Flows placeholders, Suppliers, Integrations (with #248's ConnectionCard behaviour), Users, Companies, Account page + menu with Security and the tour. **One save grammar** — every Admin setting saves from the sticky bar with Discard, unsaved marker, `beforeunload` and an in-app leave dialog for links and Back; row lists (members, suppliers, category rows, invitations) act per row. Members see Configuration read-only with "Ask an owner: ‹names›". Phone renders content-only under the one-line note with the company named above every page.

## Tickets created / changed

- **#252** closed with the resolution comment.
- **#257** (mobile approval build) gained an input: on a phone the company name now appears twice within 60px (shell header switcher + Admin caption); decide whether the Admin caption renders only when the shell header does not name the company.
- **#283** (new, `wayfinder:grilling`): does a typed queue's Details tab still ask "What type of document is this?" now the Configuration field table drives the pane — graduated from the map's fog ("document-type chooser… sharpens when the Configuration field table (#252) decides what the Details tab shows"). Blocked by #259 (pane header).
- **#282** (Finance sign-off) was blocked by #252 and is now unblocked.
- #253 (Flows) and #254 (Organization/Users) are the remaining parts; the project lessons file notes two legacy forms outside Configuration still on their own save model for them to fold.

## Scores and counts (rendered surfaces only)

Critique (from the earlier sessions on this ticket, not re-run per the hand-off): first pass **28/40** → last fresh-reviewer **34/40**, all ≥ 3, no P0/P1. Stored under the ticket's WIP commit message; no `.impeccable/critique/` snapshot exists for Admin (the earlier session did not archive one — recorded as a gap).

`evaluate` (the KPI for this attempt):

| | Predicted (hand-off) | First pass | Close |
|---|---|---|---|
| Health | ≥ 85 assumed | **76** | **87** |
| P0 / P1 | 0 / 0 | 0 / **1** (H4 three save grammars) | 0 / 0 |
| Anti-patterns | Clean | Clean | Clean |
| H1 status | ≤ 1 | 2 (no loading state) | 1 |
| H2 real world | ≤ 1 | 2 (workspace/company mixed on Users) | 1 |
| H3 control | ≤ 1 | 2 (`window.confirm`, silent Discard) | 1 |
| H4 consistency | ≤ 1 | **3** | 1 |
| H5 error prevention | ≤ 1 | 1 | 0 |
| H6 recognition | ≤ 1 | 2 (lock reason sr-only; phone caption) | 1 |
| H7 efficiency | ≤ 1 | 2 (menu keys) | 1 |
| H8 minimalist | ≤ 1 | 2 (Users overloaded) | 1 |
| H9 recovery | ≤ 1 | 1 | 1 |
| H10 help | ≤ 1 | 2 (lock reason) | 1 |

Gap analysis: no `preflight.md` was filled for this ticket (the earlier sessions predate the rule), so the "prediction" is the hand-off's assumption. Every heuristic under prediction traces to Part B rows that would have been marked covered by comment rather than grep: "one term/one primitive per concept" (H4, H8 — the save-bar header *claimed* every form used it), "shell-primitive reuse" (H3 — `window.confirm` beside `ConfirmDialog`), "focus/keys per interactive surface" (H7 — `role=menu` without keys), and the `fortify` state inventory (H1 — no loading state). The P1 is the H4 row filled optimistically.

In-page detector (per state, 1440 · 390), baseline from the ticket: settings/workspace 10/9, settings/modules 11/8, Controls settings 10/5. First pass this attempt: every Admin state **5 · 5** (app-wide residue: switcher palette ×2, overused-font, dev-overlay `layout-transition` + `dark-glow`); `account`/`account-security` 6 at 1440 (`text-overflow` on the expanded rail's truncated workspace name — shell); `account-menu` 11 at 1440 (menu popover covering rail labels + the Close page under it). Close: unchanged residue; the new `leave-dialog` state went 6 → **5** after the ConfirmDialog footer fix; `tax-dirty`, `companies`, `users` 5 · 5.

`include` (keyboard, live, both widths, `capture-round3c.log`): Tab reaches the doc-type tablist (ArrowRight moves), the field controls (32 Tabs at 1440 / 18 at 390 from the top — the rail and the Admin nav are each one stop), Space toggles and the bar appears with "Unsaved changes" (polite live region), Tab past the last row lands on Save changes, Discard hides the bar; the leave dialog opens with Cancel focused, Escape keeps the page, "Leave without saving" navigates; Back is guarded (Cancel stays, Leave goes back); Ctrl+S saves; phone save bar sits at y=702 above the 72px tab bar; landmarks: navs "Workspace", "Admin", "Primary workspace navigation", one `main`, one `h1`, `aria-current` on the rail item, nav item and Account tab; account menu: open → first item, ↑/↓, Escape returns focus, Tab closes.

Close checks (dev server stopped, `logs/scratch-252/close.log`): `tsc` clean (25s) · `eslint` clean on touched files (14s) · full `vitest` **2568/2568, 262 files** (99s) · `next build` succeeded with every Admin and Account route in the manifest.

Weak pre-build step: D/E (the independent spec critic and the `evaluate` prediction) were never run for this ticket; the first `evaluate` this attempt was the first time anyone scored the surface on Nielsen's ten.

## Needs the owner

- Nothing blocking. Owner sign-offs already open: #256 (unplug Settings › Templates and › Reports — both still render as Configuration sections), #282 (Finance entry/route, now unblocked).
- #257's caption question and #283's Details-tab chooser question are decisions for their tickets, not for the owner directly.
