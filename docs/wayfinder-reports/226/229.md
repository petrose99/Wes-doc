# Autopilot report — #229 Shape payment runs that never move money: Bill Pay queue and Payment Batches

- Map: #226 Map: Make the operator app work like Vic.ai (grammar, not skin)
- Type: wayfinder:grilling · Started: 2026-09-15T23:07Z · Finished: 2026-09-15T23:20Z
- Outcome: resolved

## What I did

1. Claimed #229 (assigned to petrose99) and loaded the map, the ticket and #237's comment about "Open invoices by age".
2. Loaded `grilling`, `domain-modeling`, `intent` (context, then `journey`) and `impeccable shape`. The `impeccable context` launcher could not run in this unattended sandbox (needs an interactive approval); read `PRODUCT.md` directly as the skill's fallback prescribes. `DESIGN.md` does not exist. `craft-floor.md` was not read: this ticket is planning-only and changed no UI.
3. Read the evidence: tour §6 and the three payments screenshots, `CONTEXT.md`, `models/payment-runs.ts`, the payable rule in the Invoices page, `Supplier`/`PaymentRun`/`Institution` schema, `countdown-badge.tsx` (#188), `aging-summary.tsx` (#225), the Finance critique 14/40 and Invoices 25/40 baselines, the operator journey (#239) and tickets #248, #249, #233, #223, #232, #188.
4. Ran a background `impeccable critique` of the incumbent "Prepare payment run" flow: 13/40, snapshot `.impeccable/critique/2026-09-15T23-16-19Z__invoices-prepare-payment-run-incumbent.md`. The live in-page pass worked against the running dev server (:3000) and the already-running live-server (:8400); the static `impeccable detect` was denied by the session's permission policy and is recorded as not run. The dialog was dismissed with Escape, so no payment run was written.
5. Ran three grilling rounds (24 questions) and answered each with its recommendation; the rounds are in the session transcript and their gist in the resolution comment.
6. Committed the glossary (CONTEXT.md: Bill Pay, Payment batch, Payer account, Payment terms, Payment record; Invoice updated) and ADR 0001 on the derived paid state.
7. Created execution ticket #251, wired it as a sub-issue of #226, blocked by #229; added #251 as a blocker of #248.
8. Posted the resolution comment on #229 (https://github.com/petrose99/Wes-doc/issues/229#issuecomment-5689453977), closed it, appended the decision to the map's Decisions so far, graduated Bill Pay's Open/Closed sub-states out of the fog and added three fog lines.

## Questions I answered on the owner's behalf

| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| Q1 | Batch entity | Extend `payment_runs` into Payment batch, additive columns | Smallest reversible; CONTEXT.md vocabulary |
| Q2 | Lifecycle | Pending approval → Approved → Paid; Rejected; download is a fact | Vic wins ties (Confirm & Create → Pending); Server-confirmed action |
| Q3 | Who submits/approves | Member submits, owner decides; self-approval allowed and labelled | #227 Approver rule; real conditions (solo owner) |
| Q4 | Pay From | Named Payer account, additive model; one batch = one account = one currency | Notes "never move money"; Vic's one-bank-per-batch footer |
| Q5 | Terms and discount on Supplier | Yes, nullable percent + days; rendered "2/10 net 30" | Destination names the discounted cell; heuristic 2 for the label; #188's held-back state |
| Q6 | Mark as paid | Payment records; derived paid state, ledger first; reversible with reason | Autonomy/reversal; ADR 0001 |
| Q7 | Bill Pay membership | Approved, unpaid, not cancelled; batched under Scheduled; bank-less rows undecidable | #239 Q14 gate; #227 Not eligible; no silent drops |
| Q8 | Home of "Open invoices by age" | Bill Pay's one metric; Early Payment Savings only with discount data | Tour lesson 10; §6 one bar not cards |
| Q9 | Navigation | One rail item Payments, two queues | #236 pattern; PRODUCT.md no duplicate routes |
| Q10 | Bill Pay columns | Vic's minus Est. arrival/method; no processing glyph | Vic wins ties minus data DocuBite lacks; minimalism |
| Q11 | Amount to pay editable | Yes on desktop, bounded; read-only on phone | Vic; ledger has partial amounts |
| Q12 | Create batch dialog | Vic's shape; mixed selection splits | Vic; Q4 |
| Q13 | Batches views | Pending approval · Approved · Paid; Rejected under Closed | DocuBite has a real middle state (documented exception) |
| Q14 | Batch detail | 60% Detail pane; Approve then Download as separate acts | Notes' pane exception; #232 phone lane |
| Q15 | Bulk-bar button | Moves to Bill Pay; receipt line on Invoices | Owner-authored in the ticket question; listed for sign-off |
| Q16 | States and copy | Named empty/error copy; no optimistic removal | #227; anti-pattern catalog cat. 3 |
| Q17 | Success measure | Approval→export days, overdue share, discount captured, ledger contradictions | `measure` |
| Q18–Q24 | Rendering | Shell unchanged, 56px metric band, emerald over line-through with sr-only, tabular figures, 36px inputs, #235 below-md, quieter | Invoices baseline 6/5; craft floor contrast and target sizes |

## Decisions recorded

Payment batch (extended `payment_runs`) with Pending approval → Approved → Paid and Rejected; download as a fact; member submits, owner decides; Payer account as the meaning of Pay From, one per batch with one currency; Supplier gains early-payment discount fields, Terms shown as "2/10 net 30"; Mark as paid writes Payment records and paid state is derived ledger-first (ADR 0001); Bill Pay lists Approved unpaid invoices with bank-less rows undecidable and carries the aging bar as its metric; Payment Batches has three views and the savings bar only with discount data; one rail item Payments; the Invoices bulk-bar button moves. Action Summary: Intent `specify` → `fortify` → `articulate` → `include`; Impeccable `shape` → `layout` → `clarify` → `harden` → `adapt` → `audit` → `polish`.

## Tickets created / changed

- Created **#251** Build the Payments destination: Bill Pay and Payment Batches on the queue shell, with payer accounts, terms, discounts and payment records (`wayfinder:task`, sub-issue of #226, blocked by #229).
- **#248** Decide Finance's shape: now also blocked by #251.
- **#229** closed with the resolution comment.
- Map #226: Decisions so far gained #229; fog graduated (Bill Pay Open/Closed sub-states → #251); fog added (payments approval flow and role after #231; SEPA and other file formats; Finance folding into Payments after #248).

## Scores and counts (rendered surfaces only)

No rendered surface changed on this ticket, so there are no after counts and no `evaluate` score. Incumbent baselines for #251 to beat:

| Surface | Critique | In-page detector 1440 | In-page detector 390 |
|---|---|---|---|
| Invoices bulk bar, two rows checked | 13/40 (flow) | 6 | 7 |
| Prepare-payment-run confirm open | 13/40 (flow) | 11 | 11 |
| Invoices queue (shell baseline) | 25/40 | 6 | 5 |
| Finance (payment-run seam) | 14/40 | 6 | 5 |

Shell baseline of 5 false positives applies at both widths. Static scan: not run (permission denied unattended).

## Needs the owner

- Confirm, under the removal protocol (#237), that "Prepare payment run" may leave the Invoices bulk bar once Bill Pay exists. The ticket question asks for exactly this replacement, so it was recorded as decided, but it is named here so it can be vetoed before #251 builds it.
- `impeccable context` could not run unattended (approval prompt). A human session should run it once before #251 starts.
