# Autopilot report — #251 Build the Payments destination: Bill Pay and Payment Batches on the queue shell, with payer accounts, terms, discounts and payment records

- Map: #226 Make the operator app work like Vic.ai (grammar, not skin)
- Type: wayfinder:task (execution of #229) · Started: 2026-09-16T02:27Z · Finished: 2026-09-16T04:05Z
- Outcome: resolved

## What I did

1. Claimed #251. Loaded the map, #229's resolution, ADR 0001, CONTEXT.md's five Payments entries, the #225 shell (`components/queue/*`), `models/payment-runs.ts`, the ZA EFT formatter and the download route. Confirmed that #236's Approvals code is not on this branch (memory warned), so the Payments destination mirrors the `(queue)` pattern directly.
2. Called `intent` (context, then `specify`; `fortify` state inventory carried through the build; `articulate` on every string; `include` on the closing pass) and `impeccable` (`shape` → build → `layout`/`clarify`/`harden`/`adapt` during the fix batches → `audit`/`polish` at the close). Ran `impeccable context` once; read `craft-floor.md` before the first UI edit.
3. **Schema (additive, migration `20260916020000_add_payment_batches_payer_accounts_payment_records`)**: `payment_runs` gains the batch facts (name, comment, submitted/approved/rejected/exported/paid by+at, rejectedReason, payFromAccountId, currencyCode); `suppliers` gains `earlyPaymentDiscountPercent/Days`; new `payer_accounts`, `invoice_payments` (Payment records, soft-removed with a reason), `bill_pay_preferences` (per-row Amount to pay / Pay From). Applied to the dev DB, client regenerated, all reversible.
4. **Pure libraries + tests** (`lib/payments/`): `terms.ts` ("2/10 net 30", discount window in cents), `paid-state.ts` (ledger → records → Scheduled → Unpaid, ADR 0001), `batch-split.ts` (one batch per payer account × currency; local-date names), `eligibility.ts` (on-queue rule, batch eligibility incl. `no_payer_account`, 0 < amount ≤ due), `batch-status.ts`. `payments.test.ts` 13 tests; `models/bills.test.ts` gained the derived-paid-state test.
5. **Models**: `models/payer-accounts.ts`, `models/bill-pay.ts` (the Bill Pay projection, aging summary, preferences, Early Payment Savings), `models/payment-batches.ts` (list/detail with supplier-grouped lines, remittance advices, Audit tab from `payment_batch.*` events, file problems, discount-expiry; create in one `$transaction`, approve, reject, export fact, mark paid → records, un-mark, manual records in one transaction, remove records). `models/bills.ts` now derives `paidState` on every `BillRow`; the Unpaid toggle and Paid chip read it.
6. **Routes and actions**: `(queue)/payments/bill-pay` (+ `[documentId]` deep link), `(queue)/payments/batches` (+ `[batchId]`), `/payments` → Bill Pay; `(queue)/payments/actions.tsx` (every refusal an `ActionState`, nothing thrown to `error.tsx`; the pane content action returns a batch-worded node when missing); the download route serves the batch's file only when Approved, returns 422 with every problem when the file can't be written, and stamps `exportedAt/By`.
7. **Components** (`components/payments/`): `bill-pay-queue.tsx` (columns per #229 Q10, Status/Discount/Aging facets, bulk bar Create batch · Mark as paid · Set Pay From with inline hints, pane actions incl. Set Pay From…, Add bank details, Open batch, Remove payment records…), `aging-band.tsx` (the one metric band; buckets filter the queue; chips hidden below md), `amount-to-pay.tsx` (emerald over struck due with sr-only "was"; inline 36px editor on desktop, read-only on the phone; validation and network failure inline), `create-batch-dialog.tsx` (real Dialog; eligibility strip; supplier-grouped lines with per-line remove; split notice previewing batch names; footer Bills · Total · Pay From; becomes the receipt with Submitted / Left out), `mark-paid-dialog.tsx`, `batch-queue.tsx` (views Pending approval · Approved · Paid · Rejected-under-Closed; sticky Reject… · Approve, Download payment file / Fix bank details · Mark batch as paid…, Remove payment records… on Paid; `decided` override so row, subtitle and footer agree the instant the server confirms; pane refresh), `batch-detail.tsx` (facts strip incl. self-approval and download fact, file explainer, Lines · Remittance · Audit tabs with the APG tabs pattern). Settings › Payments (`components/settings/payments-settings.tsx`, page + actions) for payer accounts and per-supplier terms/discount/bank account — the target of "Needs bank details" until Admin › Suppliers (#252).
8. **Shell touches**: `QueueScreen` gained a `band` slot and its key guard now respects any open modal (`[role=alertdialog]`, `aria-modal`); rail gained **Payments** after Exceptions with the pending-approval badge (layout counts it); settings nav gained Payments; Invoices lost "Prepare payment run" from its bulk bar and its Approve receipt gained "Approved invoices are ready in Bill Pay"; the Invoices pill speaks the derived paid state ("Paid (recorded)"); `ConfirmDialog` focuses a button when its children carry no field; `ReasonDialog` got a visible label, `role=alert` and a reset on cancel; sonner's success toast contrast fixed (4.3:1 → ≥4.5:1); `DiscountCountdownBadge` added.
9. Seeded the dev workspace (`scripts/dev/seed-bill-pay.ts`, `--reset` for the flows): two payer accounts, four suppliers (one with 2/10 net 30 and an open window, one without bank details), seven Approved invoices.
10. Verified live via Playwright (`/tmp/scratch229/pay251.mjs`, helpers in `.impeccable/live/verify251.mjs`, `dev.mjs`, `build.mjs`): create → approve → reject → mark paid → un-mark → bulk mark paid all driven end to end; in-page detector at 1440 and 390 (list, pane open, dialogs); critique A + `evaluate` twice (initial, then confirming); lint clean; `tsc` clean; 2559/2559 tests; `next build` succeeds with all six new routes in the manifest.
11. Wrote the critique snapshot `.impeccable/critique/2026-09-16T03-50-00Z__components-payments-bill-pay-queue-tsx.md`, sharpened CONTEXT.md's Bill Pay entry (Scheduled, Amount to pay), created #276, #277, #278, wired them, and posted the resolution.

## Questions I answered on the owner's behalf

| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Approvals (#236) is not on this branch — mirror what? | The `(queue)` pattern directly; Payments sits after Exceptions on the rail, not after Approvals | Map Notes build order; the ticket says "mirroring #236", which does not exist here — smallest reversible reading |
| 2 | Where does "Needs bank details" link, with no supplier page? | A Settings › Payments page (payer accounts + per-supplier terms/discount/bank), anchored per supplier | #229 Q4 puts payer accounts under Settings › Payments; Admin › Suppliers is #252's; the link had to land somewhere real |
| 3 | Early Payment Savings as a band or the inline stat? | The `stat` slot; leads with captured, else "Discount open on Bill Pay", else `unavailable` | #225 decision 3 (one inline figure, no band); #229 Q8 "no empty KPI" — the R 0,00 first render was fixed on critique |
| 4 | What is a legacy `draft`/`sent` run? | Reads as Approved (with the export fact from `sentAt`) | Smallest reversible mapping; an owner prepared it under the old owner-only rule |
| 5 | Where do Amount to pay and Pay From persist? | New `bill_pay_preferences` row per invoice, copied into the batch at creation | Additive schema only (#229); later edits must never rewrite a batch |
| 6 | Is a selection with no payer account batchable? | No — `no_payer_account` eligibility, one amber notice under the band | #229 Q4 one batch = one account; `evaluate` H5 P2 |
| 7 | "Create batch" vs "Submit for approval" naming | "Create batch" everywhere (button, dialog title, primary "Create batch (n)") | #229 Q10 names the bulk action; critique H4 asked for one verb |
| 8 | Ellipsis convention on the footer | "Reject…" (needs a reason) / "Approve" (a bare confirm), matching "Cancel invoice…" | Shell convention already in the codebase; critique flagged parity either way |
| 9 | Keep the Currency column on a single-currency workspace? | Yes | #229 Q10 lists it; the split rule is about currency |
| 10 | Discount pill vs due countdown in one cell | Both render | Critique H6: the discount pill was displacing the due date |
| 11 | Where does payment-record reversal live? | "Remove payment records…" (reason) on a Paid batch's footer and on recorded-paid Bill Pay rows; both Mark-paid dialogs say so | ADR 0001 "removal needs a reason"; critique H3 P1 / `evaluate` H3 P1 found it unreachable |
| 12 | Approve after a discount window lapses? | Show the countdown on pending batch rows and name the expiry in the Approve confirm; the file still pays the copied amount | `evaluate` H5 P1; restating the amount is a decision — left with the owner's confirm text, not automated |
| 13 | Non-transactional loops claiming atomicity | Batch creation, manual records and Set Pay From each in one `$transaction` | `evaluate` H5 P2; the dialogs' "Nothing changed" copy must be true |
| 14 | Selectable-then-excluded rows; single-owner speed bump | Kept as built; opened #276 and #278 | Both contain a real decision (critique provocative questions) — ticketed, not decided in an execution session |

## Decisions recorded

Execution of #229 as stated — no decision re-opened. Payments destination shipped: Bill Pay and Payment Batches on the Queue-screen shell, payer accounts and supplier terms/discounts under Settings › Payments, Payment records with the derived paid state on every invoice row, the batch lifecycle Pending approval → Approved → Paid / Rejected with the export as a fact, remittance advices surfaced, every incumbent critique defect from #229 fixed (receipt, inline failures, `validatePaymentInstructions` called, real Dialog, en-ZA money, revalidation on the queue the operator is on).

## Tickets created / changed

- #276 Decide whether Bill Pay rows that cannot be batched are selectable at all (grilling; blocked by #251, now clear).
- #277 Decide whether an approved Expense claim becomes a Bill Pay row for the Claimant (grilling; graduated from fog; blocked by #273).
- #278 Decide the single-owner speed bump (grilling; blocked by #251, now clear).
- #248 (Finance's shape) was blocked by #251 — now unblocked.
- Fog cleared: "Paying an approved Expense claim … chartable once #251 ships" → #277. Fog kept: payments approval flow / roles (after #253/#254); SEPA and other formats; Finance folding into Payments.

## Scores and counts (rendered surfaces only)

- **Critique (new UI)**: first measurement 27/40 (H1 2 · H2 3 · H3 2 · H4 2 · H5 3 · H6 3 · H7 3 · H8 3 · H9 3 · H10 3) → after the fix batches **34/40** (4 · 4 · 3 · 3 · 4 · 3 · 3 · 3 · 4 · 3), every heuristic ≥ 3. Snapshot in `.impeccable/critique/`.
- **evaluate**: 68/100 (heuristic sum 18; 4×P1 10×P2 16×P3) → **85/100** (sum 8; 0×P1, 1×P2, 6×P3, all then fixed in the polish pass: receipt snapshot, `useId`, phone-size buttons, Approve confirm as lines, transactional Set Pay From, "default" explained). Anti-pattern sweep: Clean, both rounds.
- **In-page detector** (residual 5 on every surface is app-wide — rail switcher gradient ×2, Inter share, body `transition: height`, `#ffba00` glow — carried from #250): Bill Pay list 5/5 → 5/5 (1440/390); Bill Pay pane 6/6 → 5/5; Create batch dialog 9 → 5 (1440); Mark as paid dialog 5, receipt 5; Batches list 5/5 → 5/5; Batches pane 5/6 → 5/5; batch Mark paid dialog 6 → 5; paid pane 6 → 5; Settings › Payments 8/7 → 6/5 (the 6th at 1440 is the rail's workspace switcher `text-overflow`, shell). Static `impeccable detect --json` over the new files: `[]`.
- Build: `tsc` clean, eslint clean on touched files, 2559/2559 vitest, `next build` exit 0 with the six new routes.

## Needs the owner

- **"Prepare payment run" left the Invoices bulk bar** (a move, as #229 named for veto): the code path (`preparePaymentRunAction`, `models/payment-runs.ts`) is kept, only the button is gone. Nothing was removed under the #237 protocol.
- Invoice rows still show the incumbent document pane header (#234/#259) and the below-md table scroller (#235/#261) inside Payments; both are on their own tickets.
- The sonner toast sits over the pane's sticky footer for ~4 s after a decision (shell-level position); noted, not changed.

scores: first-critique=27 close-critique=34 first-evaluate=68 close-evaluate=85
