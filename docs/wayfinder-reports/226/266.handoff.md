milestone: spec-done

## Plan
step: 1 — QueueScreen shell slots — todo — spec "Shell change" — components/queue/queue-screen.tsx (add `addAction` prop rendered between `{stat}` and the ⋯ Popover at line ~532; add `dropZone` prop: dragenter/dragleave/drop state + absolutely-positioned dashed frame over the list region, `md`+ only, 120ms fade, none under reduced motion) — check: `npx vitest run components/queue` stays green; new props are optional (no existing caller breaks).
step: 2 — Add documents dialog — todo — spec Screen 3 (incl. "Refused before staging" row: cap + format, checked in both `input.onChange` and `handleDrop` so drag-drop can't bypass `accept`), preflight B1/B2/B5 — new `components/intake/add-documents-dialog.tsx` (type select, drop/browse area, staged list, cap sentence client-side 200MB sum, `InboundAddressLine`, footer) + `components/extract/use-extraction-progress.ts` reused as-is + `uploadDocumentsAction` (`app/(app)/workspaces/[workspaceId]/actions.ts`) called directly, no new server action — check: `npx vitest run` on any new/touched test file; manual state walk (idle→staged→uploading→done, plus a rejected-format drop) against a seeded file.
step: 2b — Mobile inbound-address rediscovery — todo — spec Screen 6 — `components/shell/how-it-works.tsx` gains an `<InboundAddressLine>` line (third consumer) when `inboundEmail.enabled` — check: Account page → How DocuBite works shows the address on phone width.
step: 3 — Header button + drop wiring — todo — spec Screen 1/2 — new `components/intake/add-type-button.tsx` (button + owns the `useExtractionProgress` poller instance, same survives-unmount pattern as `FileHubUploadButton`; wires `QueueScreen`'s `dropZone` prop to open the dialog pre-staged) — check: button visible `md:inline-flex`, `hidden` `<md`.
step: 4 — Wire into the four queues — todo — spec Screen 5 — `components/queue/invoice-queue.tsx` + `components/queue/document-queue.tsx` (POs/Receipts/Bank statements) get `addAction`/`dropZone`/`inboundAddress` passed through; the three `(queue)/{purchase-orders,receipts,bank-statements}/page.tsx` gain the same `ensureInboundEmailToken` wiring `(queue)/invoices/page.tsx` already has; remove `invoice-queue.tsx`'s old "back to pipeline" link (line ~293) — check: `grep -rn "from=invoices" components/queue` finds nothing; all four queues render the Add button live.
step: 5 — `/pipeline` redirect — todo — spec Screen 4 — `app/(app)/workspaces/[workspaceId]/pipeline/page.tsx` becomes `redirect()` to `/workspaces/${workspaceId}/invoices`; leave `PipelineShell` code in place (unreached, follow-up cleanup, not this ticket) — check: `curl` (or probe script) on `/workspaces/<id>/pipeline` and `?stage=review` both land on `/invoices`.
step: G1 — round script — todo — capture round covering the 9 states in spec "Use cases"/state table at 1440 and 390 (list w/ Add button, list `<md` no button, drag-over frame, dialog idle/staged/uploading/partial-failure/refused-cap/done) — reuse `docs/wayfinder-reports/226/logs/scratch-257/pw/round261-r2.mjs` harness per queue-shell.md.
step: G2 — r0 and fixes — todo — first capture + detector + Part B grep checks; fix what they show.
step: G3 — build-done — todo — all steps done, gate met, hand-off updated to `milestone: build-done`.

## Artifacts
- Spec: docs/wayfinder-reports/226/logs/scratch-266/spec.md
- Pre-flight: docs/wayfinder-reports/226/logs/scratch-266/preflight.md
- Spec critic: docs/wayfinder-reports/226/logs/scratch-266/critic-266.md

## Open findings
None yet — build not started.

## Next step
Start step 1 (QueueScreen shell slots) in the next (build) session.
