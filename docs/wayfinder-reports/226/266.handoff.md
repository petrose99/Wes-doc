milestone: spec-done
milestone: build-started

## Plan
step: 1 — QueueScreen shell slots — done
step: 2 — Add documents dialog — done
step: 2b — Mobile inbound-address rediscovery — done
step: 3 — Header button + drop wiring — done
step: 4 — Wire into the four queues — done
step: 5 — `/pipeline` redirect — done
step: G1 — round script — done — round266.mjs
step: G2 — r0 and fixes — in progress — real app bug fixed this session:
  `add-documents-dialog.tsx` uploadAll()'s "all done" check read a stale `rowsRef.current`
  (not yet flushed by the setRows two lines above), so the dialog never showed the Done screen
  after a real successful upload — fixed to judge the just-uploaded batch from `succeeded`
  directly instead of the stale ref. Also fixed 2 round-script probe bugs (not app bugs):
  (a) `invoices-dialog-uploading`'s Escape-then-check race — added a 500ms `page.route` delay
  on the upload POST for just this state so the locked window is observable; (b) its
  `[role="dialog"]` locator collided in strict mode with Next dev's error-overlay dialog
  (triggered by a **pre-existing, unrelated** React key warning on `InvoicesPage` → confirmed
  via debug-console-err2.mjs to fire on plain navigation, nothing to do with this ticket) —
  scoped the locator to `getByRole("dialog", { name: /^Add /i })`. Last full gate run (shots-r3,
  before the locator fix) was 21/21 keyboard probes pass, 0 real detector findings; a shots-r4
  re-run with the locator fix was in flight when this session hit the hand-off line — check its
  result first.
step: G3 — build-done — todo — blocked on G2 confirming shots-r4 (or a fresh run) is 21/21 clean.
  After that: Part B contract checks over the touched surface, `milestone: build-done`.

## Artifacts
- Spec: docs/wayfinder-reports/226/logs/scratch-266/spec.md
- Pre-flight: docs/wayfinder-reports/226/logs/scratch-266/preflight.md
- Round script: docs/wayfinder-reports/226/logs/scratch-266/round266.mjs (now also deletes
  stale PDF1/PDF2 document rows before each run — real uploads accumulate in the dev DB across
  repeated round runs and hit the app's dedup rejection otherwise)
- Residue file: docs/wayfinder-reports/226/logs/scratch-266/residue.txt
- Latest complete run: shots-r3/ (1 probe fail, now fixed) — shots-r4/ may be finished or
  still running; check for its gate.mjs output before trusting it
- Debug scripts (already run, safe to ignore/delete later): debug-locked.mjs,
  debug-console-err.mjs, debug-console-err2.mjs, check-docs.mjs

## Open findings
Pre-existing, out-of-ticket-scope: `InvoicesPage` throws a React "unique key prop" console
warning (triggers Next dev's error overlay) on plain navigation, unrelated to Add-documents —
not investigated further, not blocking this ticket's gate (gate.mjs's page-error check doesn't
count console.error). Worth its own ticket if it recurs.

## Next step
Dev server + live-server (impeccable) are both already running (started this session via
start-dev.mjs / start-live-server.mjs) — don't restart them if still up. Check
`shots-r4/` for completion: `node scripts/wayfinder-autopilot/gate.mjs shots-r4
--residue-file residue.txt` (run from docs/wayfinder-reports/226/logs/scratch-266/). If it's
21/21 clean and 0 real findings, G2 is done — move straight to G3 (tsc --noEmit, Part B contract
checks, `milestone: build-done`, commit). If shots-r4 didn't finish or still fails, re-run
round266.mjs into a fresh shots-rN dir and gate it.
