milestone: spec-done
milestone: build-started

## Plan
step: 1 — QueueScreen shell slots — done
step: 2 — Add documents dialog — done
step: 2b — Mobile inbound-address rediscovery — done
step: 3 — Header button + drop wiring — done
step: 4 — Wire into the four queues — done — invoice-queue.tsx, document-queue.tsx, receipt-queue.tsx
  all get `addAction`/`dropZone`/`inboundAddress`/`fileId`/`templates` via `AddTypeButton`; new
  `getIntakeUpload(workspaceId, userId)` helper in models/files.ts (mirrors pipeline/page.tsx's
  old `uploadTemplates` build) is called from invoices/page.tsx, document-queue-page.tsx (POs +
  Bank Statements), receipts/page.tsx; old invoice-queue.tsx pipeline link removed
  (`grep -rn "from=invoices" components/queue` → no hits). tsc --noEmit -p . shows only the same
  pre-existing errors as before this step (unrelated files: dictation-actions, worker/job-worker,
  etc. — confirmed unchanged by this step's `grep` of the tsc output for touched files/paths).
  vitest not yet re-run for this step (next session should run
  `npx vitest run components/queue` before G1).
step: 5 — `/pipeline` redirect — done — app/(app)/workspaces/[workspaceId]/pipeline/page.tsx
  replaced with a `redirect()` to `/workspaces/${workspaceId}/invoices`; nothing else imports this
  page file (grepped, no hits) so the old data-fetching body was removed rather than kept dead in
  this file — `PipelineShell` itself (components/pipeline/pipeline-shell.tsx) is untouched, still
  unreached code per spec. Not yet curl-verified against a running dev server — do that at G2.
step: G1 — round script — done — docs/wayfinder-reports/226/logs/scratch-266/round266.mjs
  (run via `node round266.mjs <out-dir>`, resolves `playwright` from the repo root's node_modules,
  no NODE_PATH needed). Covers all 29 states: 4 queues × {list, drop-frame, dialog-idle/staged/
  refused-format} @1440 + invoices-only {uploading, done} @1440, 4 queues × list @390,
  pipeline-redirect @1440/390, account-how-it-works @390. First full run (shots-r0) completed
  end-to-end, 0 errored states, 187 raw findings (residue not filtered yet — no --residue passed
  to round(), G2 uses gate.mjs --residue-file residue.txt instead). keyboard.json has 21 probes;
  several read `false` but look like harness bugs, not app bugs — flagged for G2, not yet fixed:
  `s.focused()` returns a descriptive string ("select:Invoice…") not an id, so the
  dialog-open-focus checks compare against the wrong shape; the refused-format row counts (`li`
  count of the whole page, not scoped to the dialog) inflate to 22-25; uploading-locked/done-state
  probes may be genuine timing issues (poller took >6s) or the same button-regex mismatch — needs
  a look with the dialog actually open, not just JSON, before trusting either verdict.
step: G2 — r0 and fixes — todo — re-run round266.mjs as shots-r0 (fresh — the prior run's
  keyboard probes are suspect per G1 notes above, PNGs may be stale); run
  `node scripts/wayfinder-autopilot/gate.mjs <out-dir> --residue-file
  docs/wayfinder-reports/226/logs/scratch-266/residue.txt`; first fix the round script's own
  focus/li-scoping bugs (G1 note) so probes are trustworthy, THEN fix what real detector/keyboard
  findings remain (186 raw findings incl. ai-color-palette ×86, low-contrast ×23 need triage —
  likely mostly residue/dialog-chrome, not yet confirmed); re-verify /pipeline redirect and
  re-run `npx vitest run components/queue`.
step: G3 — build-done — todo — all steps done, gate met, hand-off updated to `milestone: build-done`.

## Artifacts
- Spec: docs/wayfinder-reports/226/logs/scratch-266/spec.md
- Pre-flight: docs/wayfinder-reports/226/logs/scratch-266/preflight.md
- Spec critic: docs/wayfinder-reports/226/logs/scratch-266/critic-266.md
- Round script: docs/wayfinder-reports/226/logs/scratch-266/round266.mjs
- Residue file: docs/wayfinder-reports/226/logs/scratch-266/residue.txt
- First run: docs/wayfinder-reports/226/logs/scratch-266/shots-r0/ (PNGs, detector.json,
  keyboard.json) + docs/wayfinder-reports/226/logs/scratch-266/round-run.log (console summary)

## Open findings
See round-run.log's per-state detector counts and shots-r0/keyboard.json — not yet triaged
against residue.txt or fixed. G1's handoff notes above list the harness-vs-app ambiguity on 4
keyboard probes that must be resolved before trusting their verdicts.

## Next step
Start step G2: seed + servers up, re-run round266.mjs into a fresh shots-r0, run gate.mjs with
--residue-file, fix the round script's own probe bugs first, then fix real findings.
