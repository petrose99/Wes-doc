milestone: spec-done
milestone: build-started

## Plan
step: 1 — QueueScreen shell slots — done
step: 2 — Add documents dialog — done
step: 2b — Mobile inbound-address rediscovery — done
step: 3 — Header button + drop wiring — done
step: 4 — Wire into the four queues — done — vitest for `components/queue` still not
  re-run since this step; do before G3.
step: 5 — `/pipeline` redirect — done
step: G1 — round script — done — round266.mjs at
  docs/wayfinder-reports/226/logs/scratch-266/round266.mjs.
step: G2 — r0 and fixes — in progress — detector clean (0 real findings after adding the
  pre-existing PWA install-prompt gradient/contrast — components/shell/install-prompt.tsx, unrelated
  to this ticket — to residue.txt). Fixed: dialog-idle/done-focus-return probe bugs (compared
  against wrong focused-element shape), refused-format `li` count unscoped to dialog, dialog
  drop-zone `nested-cards` (border-y-2, dropped rounded box — craft-floor "nested cards always
  wrong" + payments-build lesson), -dialog-done fixture reusing PDF1 (real dedup collision) now
  uses PDF2. Two probes still fail after all that: `invoices-uploading-locked-1440` and
  `invoices-done-state-1440` — both show the dialog's Uploading row landing on "‹file› — couldn't
  upload. Retry" against the real dev DB/server, not completing. Root cause NOT yet confirmed:
  leading theory is `requireWorkspaceJurisdiction` throwing `JurisdictionRequiredError` because the
  dev workspace (af91555d-7450-4b21-a8ac-73db092617c8) has no `jurisdictionCode` set — actions.ts
  catches that into `{success:false, error:"Pick a jurisdiction…"}`, but
  components/intake/add-documents-dialog.tsx's failed-row JSX (~line 166) hard-codes "couldn't
  upload. Retry" instead of rendering `row.error`, so the real reason never reaches the screenshot.
  Un-run script `docs/wayfinder-reports/226/logs/scratch-266/check-ws.mjs` was mid-way toward
  confirming the workspace's jurisdictionCode via Prisma when the session hit its context line —
  its `@/prisma/client` import path is right (matches models/files.ts) but hadn't been executed.
  **Next session: run it first** (`npx tsx --env-file .env
  docs/wayfinder-reports/226/logs/scratch-266/check-ws.mjs`). If jurisdictionCode is null, this is a
  dev-fixture gap (set it, e.g. via `npx tsx --env-file .env scripts/seed-sample-docs.ts` args or a
  direct Prisma update) — not a ticket defect, and the round script's uploading/done states will
  pass once the workspace has a jurisdiction. If jurisdictionCode is already set, keep digging (add
  console logging around `createIngestionItem`'s try/catch in lib/ingestion.ts, or check
  `scanDocumentBuffer` malware-scan rejection). Either way, consider whether
  add-documents-dialog.tsx's hardcoded "couldn't upload" (dropping `row.error`) is itself a real
  H9 gap worth a fix — spec critic already flagged the sibling format-rejection gap as P1.
step: G3 — build-done — todo — blocked on G2's two probes; after they resolve: `tsc --noEmit`,
  Part B contract checks, `npx vitest run components/queue`, `milestone: build-done`.

## Artifacts
- Spec: docs/wayfinder-reports/226/logs/scratch-266/spec.md
- Pre-flight: docs/wayfinder-reports/226/logs/scratch-266/preflight.md
- Spec critic: docs/wayfinder-reports/226/logs/scratch-266/critic-266.md
- Round script: docs/wayfinder-reports/226/logs/scratch-266/round266.mjs
- Residue file: docs/wayfinder-reports/226/logs/scratch-266/residue.txt (now includes the
  app-wide PWA install-prompt gradient/contrast, out of this ticket's scope)
- Latest run: docs/wayfinder-reports/226/logs/scratch-266/shots-r0/ (PNGs, detector.json,
  keyboard.json) + round-run.log; gate output: gate-r0.log / gate-r0.json (real findings 0,
  probes pass 19 fail 2)
- Unfinished: docs/wayfinder-reports/226/logs/scratch-266/check-ws.mjs (workspace jurisdiction
  check, not yet run)

## Open findings
Detector: clean (0 real findings, both widths) after the residue update above. Two keyboard
probes fail — see step G2 above for the full root-cause writeup and the exact next command.

## Next step
Restart the dev server (`npm run dev -- -p 3000`, background) and devdb
(`docker start docubite-devdb` if needed) and the impeccable live-server (see step G2's own
history in this file's git log for the exact restart incantation used this session — the launcher
script at .claude/skills/impeccable/scripts/impeccable is denied by the autopilot sandbox; run the
binary at ~/.impeccable/bin/0.1.5/impeccable directly with `live-server --background` via
`execFileSync`). Then run check-ws.mjs to confirm/refute the jurisdiction theory for the two
failing probes, fix accordingly, re-run round266.mjs into shots-r0, re-run gate.mjs — once probes
pass 21/21 and detector stays clean, finish G2 (vitest) and move to G3.
