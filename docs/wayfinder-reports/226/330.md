# Autopilot report — #330 Swap the module-pill toggle to the shared Switch primitive

- Map: #226 Make the operator app work like Vic.ai (grammar, not skin)
- Type: wayfinder:task · Started: 2026-09-20 · Finished: 2026-09-20
- Outcome: resolved

## What I did
1. Claimed #330.
2. Read `components/workspace/module-row.tsx` and `components/ui/switch.tsx` (the primitive #293 decided this pill should adopt).
3. Swapped the bespoke `role=switch` pill button for `<Switch checked={checked} onCheckedChange={toggle} label={name} busy={pending} />`; changed `toggle()` to take the next boolean as a parameter (from `onCheckedChange`) instead of deriving it from closure state, keeping the owner guard/optimistic-update/rollback logic unchanged. Commit 78b0adc.
4. `tsc --noEmit` clean.
5. Started the dev server (`node .impeccable/live/dev.mjs start`) and the in-page detector live-server; captured before (pill, via `git stash`) and after (Switch) screenshots + in-page detector JSON for owner (dev workspace) and non-owner (Northwind Traders, member-only) views, both 1440 and 390 — `docs/wayfinder-reports/226/logs/scratch-330/{before,after}/`.
6. Ran `gate.mjs` with the admin area primer's residue set, baselined against the pre-swap capture: **CLEAN — 0 real findings, 0 new since baseline** (`gate-r1.json`).
7. Built a contact sheet and ran the three readers (`critique`, `evaluate`, `include`) as fresh foreground `sonnet` agents against the after-capture; full output in `readers-r1/{critique,evaluate,include}.md`.
8. Two critique heuristics scored under 3 (Flexibility/efficiency — no bulk toggle; Help/documentation — no deeper inline help), both explicitly identified by the reader as pre-existing page-level gaps unrelated to the Switch swap, not something a primitive-swap task should absorb. Opened two follow-up decision tickets and wired them as sub-issues of map #226: [Decide whether module rows need bulk toggle / owner efficiency controls (#345)](https://github.com/petrose99/Wes-doc/issues/345) and [Decide whether modules need deeper inline help beyond the one-line description (#346)](https://github.com/petrose99/Wes-doc/issues/346).
9. Posted the resolution comment on #330, closed it, appended a Decisions-so-far line to map #226.
10. Stopped the dev server and the in-page live-server.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Fix the two sub-3 critique heuristics in this ticket, or split them out? | Split out as follow-up decision tickets | The ticket's own scope note says "same-concept swap, not a new rendering decision"; bulk-toggle and deeper inline help are feature additions to the whole page, not part of the Switch swap. `close.md`: "a finding with a real decision behind it becomes a Wayfinder ticket." |

## Decisions recorded
Module pill → shared `Switch` primitive, same-concept swap per #293; owner guard/optimistic logic unchanged, `busy` (not `disabled`) keeps the control focusable mid-save. No visual or behavioural regression on either owner or non-owner (Request-only/plain-text) view.

## Tickets created / changed
- [Decide whether module rows need bulk toggle / owner efficiency controls (#345)](https://github.com/petrose99/Wes-doc/issues/345) — new, child of #226, unblocked.
- [Decide whether modules need deeper inline help beyond the one-line description (#346)](https://github.com/petrose99/Wes-doc/issues/346) — new, child of #226, unblocked.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=n/a first-critique=31 close-critique=31 first-evaluate=96 close-evaluate=96

Gate (in-page detector, residue = `docs/agents/areas/admin.md`): 5 findings per state at both 1440 and 390, owner and member — identical before and after, all listed in the area primer's Detector residue section (workspace-switcher avatar palette, `overused-font`, `layout-transition`, `dark-glow`); 0 real findings, 0 new since baseline. `include`: PASS at 1440, PASS at 390 (two P3s noted: `describedBy` unused, no mid-focus capture — neither blocking). `evaluate`: 96/100, Clean, 0 P0/P1. `critique`: 31/40 — two heuristics (Flexibility/efficiency, Help/documentation) at 2/4, both pre-existing and out of this ticket's scope, split to #345/#346.

## First-pass findings → lessons (rendered surfaces only)
No lesson needed — the two sub-3 heuristics are pre-existing page-level gaps the readers correctly attributed to scope outside this ticket, not something a pre-flight check on a primitive swap would or should have caught.

## Needs the owner
Nothing. #345 and #346 are open decision tickets for the owner (or a future grilling session) to resolve; neither blocks this ticket's close.
