# Hand-off — #295: schema/model foundation + self-service Bank details panel

milestone: spec-done

## Artifacts
- Spec: `docs/wayfinder-reports/226/logs/scratch-295/spec.md`
- Pre-flight (filled, gate PASS): `docs/wayfinder-reports/226/logs/scratch-295/preflight.md`
- Build notes (detail behind each done step): `docs/wayfinder-reports/226/logs/scratch-295/build-notes.md`

Spawned/blocked: [Build claim rows in Bill Pay, Reimbursements group, claim-surface copy
(#331)](https://github.com/petrose99/Wes-doc/issues/331).

## Build plan (one step per session)

step: 1 — schema migration — done — spec.md §1; see build-notes.md.
step: 2 — listBillPay claim rows — done — spec.md §2; see build-notes.md.

step: 3 — PaymentInstruction + payment writes — done — spec.md §3–4; see build-notes.md.

step: 4 — self-service Bank details Panel — done — spec.md §5, preflight.md Parts A–C; new
`components/account/bank-details-panel.tsx` wired into
`app/(app)/workspaces/[workspaceId]/account/page.tsx`; reused `saveMemberBankDetailsAction`
directly (already authorises self) — no wrapper action needed. See build-notes.md.

step: G1 — round script — done — `round-295.mjs`/`run-round.mjs` at scratch-295/, smoke run
executed end to end (20 states × both widths); one non-blocking error (`02-edit` Cancel click
after offline toggle) recorded, not yet fixed — see build-notes.md.

step: G2 — r0 and fixes — done — fixed two real defects (submit() unhandled rejection left the
form stuck in "Saving…" forever on any network failure; edit-open didn't move focus to the first
field per preflight B5) and widened the ticket-local residue regex for the admin.md rail
text-overflow (class-name based, not literal "workspace"/"rail" text) — see build-notes.md.
Gate: `node scripts/wayfinder-autopilot/gate.mjs shots-r0 --residue-file residue.txt` →
**CLEAN — 0 real findings, probes pass 20/20, 0 errored, 0 page errors**.

step: G3 — build-done — todo — `critique` + `evaluate` + `include` against preflight.md's gate
(≥ 32/40 no heuristic under 3, evaluate ≥ 85 0 P0/P1 Clean, keyboard PASS both widths) — this
ticket's only rendered surface, not #331's claim UI.

step: G3 — build-done — done — `tsc --noEmit` full project (servers stopped): all errors are
pre-existing, in unrelated files (dictation-actions.ts, document-processing.ts, ingestion.ts,
readiness/refresh.ts, automation-rules.ts, document-checks.ts, document-matches.ts,
documents.ts, inbound-email.ts, review-tasks.ts, job-worker.ts) — none touch
`components/account/bank-details-panel.tsx` or the account page. Part B contract checks over
the built surface: B1 reachability confirmed (`saveMemberBankDetailsAction` called from both
the new self-service panel and the existing admin panel, same signature); B3 vocabulary
verbatim-matches the admin panel ("Bank details", "Bank name", "Account number", "Branch code");
B4 confirms `Panel` shell reused from `components/automation/automation-ui.tsx`, `BankField`
input pattern matches admin's; B5 confirms `autoFocus` on the first field and a `catch` block
for the failure path. No findings.

milestone: build-done

## Scores: r1 (MEASURE phase)

**Critique**: 36/40 (H1:4 H2:4 H3:4 H4:3 H5:4 H6:4 H7:4 H8:3 H9:3 H10:3) — exceeds gate ✓
**Evaluate**: 88/100, Clean — exceeds gate ✓
**Include**: 1440 PASS, 390 PASS — both widths ✓
**Gate**: CLEAN — 0 real findings, 20/20 probes pass, 0 errored ✓

All readers report **0 P0/P1** findings. Minor P2 evidence gaps (Enter-to-submit unprobed, tab-walk sequence cap) noted in readers' full analysis; detector findings are shell/chrome-level (cyan gradient, overused font, text-overflow on workspace name at 1440 only, dark-glow), pre-existing and not attributable to the Bank details Panel itself.

milestone: measured

## Close (session 4)

r1 already exceeded the close bar — no fix batch, r1 stands as the close round, no re-score.
Triage, gate/check results, and the reasoning are in `docs/wayfinder-reports/226/logs/scratch-295/close.md`.
`tsc --noEmit` clean of new errors; eslint clean on the two changed files; `next build` fails on
a pre-existing, unrelated error in `dictation-actions.ts:75` (not this ticket's diff). Area
primer updated: `docs/agents/areas/admin.md` §"Account — self-service Bank details (#295)".
Report: `docs/wayfinder-reports/226/295.md`. Ticket closed.

milestone: closed
