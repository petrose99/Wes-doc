# Hand-off — #295: schema/model foundation + self-service Bank details panel

milestone: spec-done

## Scope (trimmed at spec time — see spec.md "Sizing note")
Kept on #295: schema (`ExpenseClaimPayment`, `PaymentRunItem.expenseClaimId`), `listBillPay`
claim-row eligibility, `PaymentInstruction` for a claim, Mark-as-paid/Remove-payment writing
`ExpenseClaimPayment`, and the self-service **Bank details** Panel on
`app/(app)/workspaces/[workspaceId]/account/page.tsx` (`AccountPage`) — one component serves
both the desktop profile panel and the phone Account tab already (its own doc-comment says so).

Spawned and blocked on this ticket: [Build claim rows in Bill Pay, the Reimbursements group in
Payment batches, and claim-surface copy (#331)](https://github.com/petrose99/Wes-doc/issues/331)
— owns Bill Pay queue rendering, batch pane grouping, Receipt/Approvals copy, fortify states,
the Payments primer doc, and the full detector/critique/evaluate/include bar.

## Artifacts
- Spec: `docs/wayfinder-reports/226/logs/scratch-295/spec.md`
- Pre-flight (filled, gate PASS): `docs/wayfinder-reports/226/logs/scratch-295/preflight.md`

## Build plan (one step per session)

step: 1 — schema migration — done — spec.md §1; touches `prisma/schema.prisma` (add
`ExpenseClaimPayment` model, `PaymentRunItem.expenseClaimId` + unique index), run
`prisma migrate dev` (or the project's migration command) + `prisma generate`; check: migration
applies clean on devdb, `tsc` passes.
  - `prisma migrate dev` refuses non-interactive in this environment; migration SQL hand-written
    at `prisma/migrations/20260920010000_add_expense_claim_payments/migration.sql` (naming
    conventions copied from `20260916020000_add_payment_batches_...`), applied clean via
    `prisma migrate deploy`. `prisma generate` fails on the pre-existing, unrelated
    `next-admin-generator-prisma` bin (`not found` — not installed on PATH for this generator
    invocation); worked around by commenting out the `nextAdmin` generator block, running
    `generate`, then restoring the block (schema.prisma on disk is back to its original generator
    list). Client at `prisma/client/models/ExpenseClaimPayment.ts` confirmed generated.
  - `tsc --noEmit` has 20 pre-existing errors unrelated to this change (dictation-actions.ts,
    document-processing.ts, automation-rules.ts, job-worker.ts, etc. — none touch
    prisma/schema.prisma, bill-pay, or payments files); present before this session's edits.

step: 2 — listBillPay claim rows — todo — spec.md §2; touches `models/bill-pay.ts` (discriminated
`BillPayRow`, claim eligibility/reason logic, derived paid state reading `ExpenseClaimPayment`);
check: a unit/integration test seeding one eligible + one ineligible (no bank details) + one
ineligible (left workspace) + one ineligible (null currency) claim asserts rows + facet reasons.

step: 3 — PaymentInstruction + payment writes — todo — spec.md §3–4; touches
`lib/payments/remittance.ts`, `lib/payments/za-eft-csv.ts` (claim instruction builder), and the
Mark-as-paid / batch-paid / remove-payment call sites (branch on `documentId` vs
`expenseClaimId`); check: `lib/payments/za-eft-csv.test.ts`-style test covers a claim line
producing a valid CSV row and `validatePaymentInstructions` catching a claim with no bank
details.

step: 4 — self-service Bank details Panel — todo — spec.md §5, preflight.md Parts A–C; touches
`app/(app)/workspaces/[workspaceId]/account/page.tsx` (+ a small server action reusing
`saveMemberBankDetailsAction`'s shape scoped to `userId: currentUser.id`, in
`app/(app)/workspaces/[workspaceId]/admin/users/actions.ts` or a sibling `account/actions.ts` —
decide at build time by whether the existing action already authorizes "self"); check: detector
(`impeccable detect --json`) on the Account page at 1440/390, before/after counts recorded.

step: G1 — round script — todo — capture the Account page (view state, edit state, saved state)
at 1440 + 390; no queue/batch/claim-surface captures here, those are #331's.

step: G2 — r0 and fixes — todo — run detector + Part B checks (B1 reachability grep, B3 string
extraction, B5 focus probe); fix what they show.

step: G3 — build-done — todo — `critique` + `evaluate` + `include` against preflight.md's gate
(≥ 32/40 no heuristic under 3, evaluate ≥ 85 0 P0/P1 Clean, keyboard PASS both widths); this is
the only rendered surface in #295 so the bar applies to it alone, not to claim rows in Bill Pay
(that's #331's bar).

## Next step
Fresh session: build phase, step 1 (schema migration). Read this file, then spec.md §1 and
preflight.md, then `prisma/schema.prisma:260-283` (WorkspaceMember, already has bank fields) and
`:2510-2578` (PaymentRunItem, InvoicePayment) by range — do not re-read the whole schema file.
