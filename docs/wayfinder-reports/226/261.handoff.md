# Hand-off — #261 Build the phone Queue screen: card rows and the Filters sheet on all five queues

Map: #226. Type: task (execution). Branch: `codex/post-login-ux-implementation`.

## Milestones
milestone: spec-done

## Pointers (read these, not the codebase)
- Spec: `docs/wayfinder-reports/226/logs/scratch-261/spec.md` (§1 primitive changes · §2 slot matrix · §4 states · §5 copy · §6 keys · §8 test plan/capture states)
- Pre-flight, filled A–E, critic reconciled: `docs/wayfinder-reports/226/logs/scratch-261/preflight.md` — predicted critique 32 (H1, H5 = 4), evaluate ≈ 90, P1 0, Clean
- Area primer: `docs/agents/areas/approvals.md` (primitives, seed, dev-server, capture harness, residue). Close phase refreshes it (queue-card, Filters wording, tab-bar order) — or writes `queue-shell.md` if the primer README asks for one per family
- Capture harness to reuse: `docs/wayfinder-reports/226/logs/scratch-257/pw/` + `scripts/wayfinder-autopilot/capture-round.mjs`; seed `.impeccable/live/seed257b.ts`
- Decision ticket: #235 (twelve decisions); inputs on #261 from #240 (tab-bar DOM order) and #250 (PoChip on the card)

## Decisions taken in the spec phase (owner delegated; rule per line)
- Filters wording: button **Filters / Filters · n** (CONTEXT.md "Queue screen" + #235 d11 beat #257's "Filter"); sheet title **Sort and filter** (#235 d11); footer stays **Clear · Show n rows** (#257 shipped; Apply-with-consequence, H1/H5; Close/Escape/Back is Cancel). Deviation from #235's "Cancel / Apply" recorded here and on the resolution comment.
- Bank Statements card uses Amount / Received date / Category (no closing balance or period on the row — #235 d7 "no new data"); Exceptions pill is escalation status (no severity field).
- Cards render generically from column `phone` slots (#235 d7) via a new shared `QueueCard`; ApprovalCard/MismatchCard migrate onto it (critic D1, mandatory).
- Views on the phone: `SavedViewPicker variant="select"` (native select), hidden with zero views; management stays desktop (#232 §5).
- Tab bar moves before `#main` in the layout (fixed; DOM-order only) — #240's input.

## Build phase (next session) — do exactly this
1. Read spec §1 top to bottom; build in this order: `lib/client/queue-filters.ts` (clearFilterParams) → `queue-card.tsx` → `queue-screen.tsx` (phone slots, priority hiding, default filteredAction, viewsPhone, drop min-w-[720px], `#queue-title`/`#queue-filters-trigger`/`#queue-facets` ids) → `filter-sheet.tsx` strings → `dialog.tsx` close hit-area → `saved-view-picker.tsx` select variant → five queues' columns (§2) + `viewsPhone` → `queue-loading.tsx` + five `loading.tsx` → layout tab-bar order → migrate ApprovalCard/MismatchCard → PO Mismatches drop its Link → `goods_received` label.
2. Tests from §8; `npx vitest run components/queue lib/client` only. `tsc --noEmit` once at the end (dev server off).
3. Run Part B checks (B1/B3/B4 greps, B5 by code read) and fix. No captures, no dev server.
4. Write `milestone: build-done` below, commit `wip(autopilot): #261 build`.

## Measure phase — states (27)
5 queues × {list, pane-open, filters-open, filtered-empty} @390 · 5 × {list, pane-open} @1440 · Invoices list @900. Readers: critique, evaluate, include (sonnet) on the contact sheet + detector.json.

## Scores
(none yet — spec phase)
