# Hand-off — #261 Phone Queue screen (card rows + Filters sheet, five queues)

Map: #226. Type: task (execution). Branch: `codex/post-login-ux-implementation`.

## Milestones
milestone: spec-done

(Build phase: two sessions so far, both cut by the context line before real code. Session 2
(2026-09-16) crossed 150K on skill loads + reading the ten files below; it wrote only item 1.
**Next build session: do NOT re-read spec/primer/lessons in full and do NOT load `intent`/
`impeccable` skill bodies again — they are ~60K each. Open `build-plan.md`, read only the line
ranges named there, and write code from turn 2.**)

## Done so far (build)
- Item 1 ✅ `clearFilterParams(params, facets, sortParam)` — placed in `lib/queue/filters.ts`
  (beside `activeFilterCount`, not the plan's `lib/client/queue-filters.ts`; same one function)
  with two tests appended to `lib/queue/filters.test.ts` (import updated). Not yet run.
- Items 2–12: not started. No other file changed.

## Pointers
- Spec: `docs/wayfinder-reports/226/logs/scratch-261/spec.md` (§2 slot matrix, §5 copy, §6 keys)
- Pre-flight: `docs/wayfinder-reports/226/logs/scratch-261/preflight.md`
- **Build plan (the todo list)**: `docs/wayfinder-reports/226/logs/scratch-261/build-plan.md`
- Area primer: `docs/agents/areas/approvals.md`
- Capture harness (measure phase only): `docs/wayfinder-reports/226/logs/scratch-257/pw/` + `scripts/wayfinder-autopilot/capture-round.mjs`

## Facts learned this session (save the re-read)
- `queue-screen.tsx`: `cards` prop at L107–116 (`render` required today); card `<ul>` loop L409–417;
  table `min-w-[720px]` L418; th/td loops L425/L443; filtered-empty block L400–407; phone filter
  row L378–381; h1 L334. `BELOW` const L38–41 needs no change.
- `filter-sheet.tsx`: Dialog title L86; FilterButton label L131–133.
- `dialog.tsx` close X button L117 (`className="-mr-1 rounded p-1 …"`).
- `saved-view-picker.tsx`: `selectView` L68–73; Popover JSX starts L91 — branch `variant==="select"` before it.
- `approval-invoice-queue.tsx`: ApprovalCard L220–249, `cards={{…render}}` L141; filteredAction Link L157.
  `po-mismatch-queue.tsx`: MismatchCard L191–216, cards L124, filteredAction L133.
- Existing pill markup: `document-queue.tsx` StatusPill/consumption span; `receipt-queue.tsx` ClaimPill;
  `exception-queue.tsx` status span; `row-cells.tsx` `StatePills`, `TitleCell`, `formatDate`, `formatMoney`.
- Views pickers exist only on Invoices and Receipts pages (`(queue)/invoices/page.tsx` L78,
  `(queue)/receipts/page.tsx` L57) — `viewsPhone` is threaded from those two only.
- `layout.tsx` L92–94: `<div id="main">` then `<MobileTabBar>`; swap order.

## Exact next step
Execute `build-plan.md` items 2–12 in order (queue-card.tsx, queue-screen.tsx edits, filter-sheet
strings, dialog X hit-area, SavedViewPicker `variant="select"`, QueueLoading + 5 loading.tsx,
layout tab-bar order, five queue components, tests, Part B grep, one `tsc --noEmit`). Then add
`milestone: build-done` under the Milestones line and commit `wip(autopilot): #261 build`.
