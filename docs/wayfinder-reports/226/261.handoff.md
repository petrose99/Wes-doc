# Hand-off — #261 Phone Queue screen (card rows + Filters sheet, five queues)

Map: #226. Type: task (execution). Branch: `codex/post-login-ux-implementation`.

## Milestones
milestone: spec-done
milestone: build-done
milestone: measured
milestone: close-confirmed

## Built (all committed on the branch; `tsc --noEmit` clean, 30 tests green in `components/queue` + `lib/queue`, eslint 0 errors on changed files)
- `lib/queue/filters.ts` `clearFilterParams` — the one Clear filters (chips · sheet · filtered-empty default). Tests in `lib/queue/filters.test.ts`.
- `components/queue/queue-card.tsx` (new) `QueueCard` + `joinSegments`; tests `components/queue/queue-card.test.tsx`.
- `components/queue/queue-screen.tsx`: `QueueColumn.phone/phoneRender/priority`; `cards.render` optional, `cards.label`, `cards.subtitleClamp`;
  `viewsPhone`; low-priority columns `hidden lg:table-cell`; `min-w-[720px]` removed; default **Clear filters** in filtered-empty with
  focus hand-off `#queue-filters-trigger` → `#queue-facets button` → `#queue-title` (h1 `tabIndex=-1`); Override banner Turn off `max-md:h-11`.
- `components/queue/filter-sheet.tsx`: title "Sort and filter", trigger "Filters" / "Filters · n", `id` prop.
  `facet-filters.tsx` Clear uses `clearFilterParams` (`sortParam` prop).
- `components/ui/dialog.tsx` X hit area `max-md:-m-2 max-md:p-3`.
- `components/typed-destinations/saved-view-picker.tsx` `variant="select"` (phone).
- `components/queue/queue-loading.tsx` (new) + `loading.tsx` in approvals, invoices, purchase-orders, receipts, bank-statements, exceptions.
- `app/(app)/workspaces/[workspaceId]/layout.tsx`: `<MobileTabBar>` before `#main`.
- Five queues on the shared card: `invoice-queue`, `document-queue` (PO + Bank), `receipt-queue`, `exception-queue` (clamp),
  `approval-invoice-queue`, `po-mismatch-queue` (bespoke cards deleted). `viewsPhone` threaded from `invoices/page.tsx` + `receipts/page.tsx`.

## Verified in build phase
- Part B: only `queue-card.tsx` (and pre-existing `detail-pane.tsx` menu item) renders `<a href`; no stale "Filter"/"Filter ·" strings; focus hand-off ids exist.
- Not verified: anything rendered — no dev server, no capture (measure phase).

## Pointers
- Spec: `docs/wayfinder-reports/226/logs/scratch-261/spec.md` (§2 slot matrix, §5 copy, §6 keys, §8 tests)
- Pre-flight: `docs/wayfinder-reports/226/logs/scratch-261/preflight.md`
- Build plan: `docs/wayfinder-reports/226/logs/scratch-261/build-plan.md`
- Area primer: `docs/agents/areas/approvals.md`
- Capture harness: `docs/wayfinder-reports/226/logs/scratch-257/pw/` + `scripts/wayfinder-autopilot/capture-round.mjs`; seed `seed257b.ts`.

## Exact next step (MEASURE phase) — DONE, see scores below

## R1 capture (2026-09-16)
- Round script: `docs/wayfinder-reports/226/logs/scratch-257/pw/round261.mjs` (imports the shared `capture-round.mjs`,
  reuses scratch-257's Playwright install). Output: `docs/wayfinder-reports/226/logs/scratch-261/r1/` (`detector.json`,
  `keyboard.json`, 31 PNGs, `contact-sheet-1440.png`). 31 states, 0 errored: 5 queues × {list, pane-open} @1440 +
  5 queues × {list, pane-open, filters-open, filtered-empty} @390 + Invoices list @900 (md-lg band).
  Filtered-empty query per queue: invoices/receipts `status=cancelled`, purchase-orders/bank-statements `status=failed`,
  exceptions `status=in_review` (all confirmed 0 rows at capture time).
- Dev server :3000, devdb :55433 (already running), seed257b re-run (idempotent), impeccable live-server on :8400 via
  the `node -e execFileSync(~/.impeccable/bin/0.1.5/impeccable, ["live-server","--background"])` workaround (classifier
  blocks the `impeccable` launcher script directly under this driver's allowlist).

## Detector triage (r1)
- App-wide residue on every state (not a defect, per project lessons): `ai-color-palette` ×2 (workspace-switcher
  avatar), `overused-font` (Inter), `layout-transition` + `dark-glow` (Next dev overlay on `body`).
- Real, page-owned findings:
  1. **`cramped-padding`** (0px vertical padding, needs ≥4.2px) on all five `*-filtered-empty-390` states — the
     "Nothing matches these filters / Clear filters" empty-state block (queue-screen.tsx's new default filtered
     action). Consistent across all 5 queues → one shared-component fix. **Fix in close phase.**
  2. **`em-dash-overuse`** (12 em-dashes) on `receipts-list-1440` / `receipts-pane-open-1440` only. Likely seeded
     document body copy, not this ticket's authored UI strings (labels/empty-state/sheet title all clean per critique).
     Needs a 1-line confirmation in close phase (grep seed data vs. product copy) before deciding in/out of scope.
  3. **Unconfirmed**: `keyboard.json` shows PO/Receipts/Bank-statements/Exceptions' 20-step Tab-walks passing through
     `nextjs-portal:` → `body` then restarting from the skip link partway through — Invoices' walk completes cleanly.
     Both the critique and include readers flagged this as possibly a dev-overlay/capture artifact rather than a real
     focus trap, but neither could rule it out from this evidence. **Re-verify in close phase** (raise `tabWalk` step
     count past the restart point, or isolate with the dev overlay disabled) before treating it as residue.

## Scores (r1, fresh sonnet readers, contact sheet + detector.json + keyboard.json only)

**Critique** (Impeccable rubric, bar ≥32/40 no heuristic <3): **31/40 — bar NOT met.**
H1=3 H2=3 H3=3 H4=4 H5=3 H6=4 H7=3 H8=3 H9=3 **H10=2** (Help/Documentation — no contextual help affordance in any
captured state; scored on the surface's total absence of the dimension, not a build defect). H8 dinged for the
cramped-padding finding above; H2 dinged for the Receipts em-dash finding.

**Evaluate** (Intent rubric, bar zero P0/P1, health ≥85, anti-pattern Clean): **health 91/100 — bar met** (0 P0, 0
P1). Anti-pattern verdict: Clean. Per-heuristic issue counts: H3=1 (focus-handoff contract unverified — matches
detector triage item 3 above, not confirmed broken), H4=1 (Views select shows on Invoices/Receipts' tab order but not
PO/Bank-statements/Exceptions in evidence — confirm against spec §2 slot matrix whether Views is Invoices/Receipts-only
by design), H8=1 (cramped-padding), all others 0.

**Include** (keyboard flow verified live at both widths): **FAIL.** The r1 keyboard probe only Tab-walked each queue's
default *list* state at 390 — no widths=1440 walk at all, and no walk of the Clear-filters → focus-handoff transition
(spec §6: `#queue-filters-trigger` → first `#queue-facets` chip → `#queue-title`). The contract exists as a screenshot,
never as a live probe. This is the one hard-fail item blocking close.

## Reconciliation vs. pre-flight prediction (Part G, to complete fully in the close report)
Pre-flight (`docs/wayfinder-reports/226/logs/scratch-261/preflight.md`) predicted zero P0/P1, no heuristic <3, Clean —
the evaluate score matches that prediction (health 91, 0 P0/P1). The critique score does not: H10 fell to 2 (help/docs
was not in the Part A excellence targets and wasn't specifically checked pre-build — attribute as "unchecked" in the
close report's finding table) and H8/H2 fell to 3 from the two real detector findings above (both are execution gaps
Part B's contracts should have caught: H8/cramped-padding via the layout contract, H2/em-dash via a copy-source check
that doesn't yet exist as a named Part B row — attribute as "new class" unless the close session finds an existing row
that should have covered it).

## CLOSE phase (2026-09-16, session 2 — session 1 was cut mid-r2)
- Fix batch: Filters trigger count moved from sr-only span to aria-label (cramped-padding ×5 cleared); Invoices card PO chip
  `phoneRender` without href (nested <a> hydration error on every Invoices state, found by the Clear-filters probe);
  Approvals page/queue gained `viewsPhone` (H4 parity); empty-state button py-2. Em-dash on receipts@1440 = "—" empty-cell
  placeholder (residue). 390 tab-walk "restart" = wrap past the dev overlay (40-step PO walk shows it twice) — residue.
- r2: `scratch-257/pw/round261-r2.mjs` → `scratch-261/r2/` (43 states, 6 queues, 0 errored, pageErrors none; 1440 walks +
  live Clear-filters focus probe both widths; Invoices needs `aging=90%2B`; Exceptions/Approvals have no zero-row query in the seed).
- Scores r2: critique 34/40 (H3 H4 H6 H9 = 4, H10 = 2 → parked on #264 as on #258); evaluate 96, 0 P0/P1, Clean; include PASS.
- Remaining: close checks (tests, tsc, eslint, next build with server stopped), primer, lessons, report, close ticket, map line.
- Open dev-only console warning: "Each child in a list should have a unique key" on Invoices list only, both widths, not reproduced
  on other queues; not located in this session (not phone-specific).
