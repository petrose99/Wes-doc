# Hand-off — #261 Phone Queue screen (card rows + Filters sheet, five queues)

Map: #226. Type: task (execution). Branch: `codex/post-login-ux-implementation`.

## Milestones
milestone: spec-done

(Build phase: session 3 (2026-09-16) wrote all the code for build-plan items 1–9 and was cut at the
context line before running anything. **Next build session: do NOT reload `intent`/`impeccable`
skill bodies or re-read spec/primer/lessons in full — the code is written; this session is
verification only.** Start at "Exact next step".)

## Done so far (build) — all code written, NOTHING compiled or tested yet
- Item 1 ✅ `clearFilterParams` in `lib/queue/filters.ts` (+2 tests appended to `lib/queue/filters.test.ts`, not run).
- Item 2 ✅ `components/queue/queue-card.tsx` (new): `QueueCard` + `joinSegments` helper.
- Item 3 ✅ `queue-screen.tsx`: `QueueColumn.phone/phoneRender/priority`; `cards.render` optional,
  `cards.label`, `cards.subtitleClamp`; `viewsPhone` prop; `phoneSlot`/`cardLabel`/`lowPriority`
  helpers; card `<ul>` renders `QueueCard` when no `render`; `min-w-[720px]` removed; `hidden lg:table-cell`
  on low-priority th/td; default **Clear filters** button in filtered-empty with focus hand-off
  (`#queue-filters-trigger` → `#queue-facets button` → `#queue-title`); h1 `id="queue-title" tabIndex=-1`;
  `viewsPhone` rendered in the phone filter row; Override banner Turn off `max-md:h-11`.
- Item 4 ✅ `filter-sheet.tsx`: "Sort and filter" title, "Filters"/"Filters · n", `id` prop, clear via `clearFilterParams`.
  `facet-filters.tsx` chips' Clear also uses `clearFilterParams` (new `sortParam` prop, passed from queue-screen).
- Item 5 ✅ `dialog.tsx` X: `max-md:-m-2 max-md:p-3`.
- Item 6 ✅ `saved-view-picker.tsx` `variant="select"` (null when no views; `<select aria-label="View">` h-11 text-base).
- Item 7 ✅ `components/queue/queue-loading.tsx` (new); `approvals/loading.tsx` uses it; new `loading.tsx` in
  invoices, purchase-orders, receipts, bank-statements, exceptions.
- Item 8 ✅ `layout.tsx`: `<MobileTabBar>` before `#main`.
- Item 9 ✅ five queues: `invoice-queue`, `document-queue` (PO + Bank branches, "Goods received" relabel),
  `receipt-queue`, `exception-queue` (`subtitleClamp`), plus `approval-invoice-queue` / `po-mismatch-queue`
  (bespoke ApprovalCard/MismatchCard **deleted**, slots + `cards.label` added, `filteredAction` Links removed
  so both fall to the default Clear filters). `viewsPhone` threaded from `invoices/page.tsx` + `receipts/page.tsx`.
- Items 10–12 ❌ not started: tests (`components/queue/queue-card.test.tsx` per spec §8), Part B grep, `tsc --noEmit`.
- Edit scripts used: `docs/wayfinder-reports/226/logs/scratch-261/edit1.py … edit9.py` (already applied; do not re-run).

## Known risks to check first (likely tsc errors)
- `approval-invoice-queue.tsx`: `Link` import may now be unused (filteredAction removed) → eslint; `ProcessingStateGlyph`
  / `processingState` still used by `leading`. `po-mismatch-queue.tsx`: same for `Link`, `ProcessingStateGlyph`, `processingState`.
- `document-queue.tsx` spread column arrays use `as const` on `phone`/`priority` — verify they type-check against `QueueColumn`.
- `receipt-queue.tsx` label uses `receiptState` — defined above columns (L~74) ✓. `invoice-queue.tsx` uses `renderedAt`,
  `billState`, `poSubtitle`, `PROCESSING_STATE_LABELS` — all in scope.
- `queue-screen.tsx` `phoneSlot` filters null nodes; `subtitle` uses `joinSegments`; check `Fragment` import used.

## Pointers
- Spec: `docs/wayfinder-reports/226/logs/scratch-261/spec.md` (§2 slot matrix, §5 copy, §6 keys, §8 tests)
- Pre-flight: `docs/wayfinder-reports/226/logs/scratch-261/preflight.md`
- Build plan: `docs/wayfinder-reports/226/logs/scratch-261/build-plan.md`
- Area primer: `docs/agents/areas/approvals.md`
- Capture harness (measure phase only): `docs/wayfinder-reports/226/logs/scratch-257/pw/` + `scripts/wayfinder-autopilot/capture-round.mjs`

## Exact next step
1. `npx tsc --noEmit` (no dev server is running) — fix what it shows (see risks above).
2. Write `components/queue/queue-card.test.tsx` (spec §8) and run `npx vitest run components/queue lib/queue`.
3. Part B: `grep -n "<a href" components/queue/*.tsx` → only `queue-card.tsx`; `grep -rn "Filter · \|\"Filter\"" components/queue` → none.
4. `npx eslint components/queue app/\(app\)/workspaces` on the changed files only.
5. Add `milestone: build-done` under the Milestones line, commit `wip(autopilot): #261 build`, stop.
