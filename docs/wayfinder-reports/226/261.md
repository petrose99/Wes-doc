# Autopilot report — #261 Build the phone Queue screen: card rows and the Filters sheet on all five queues

- Map: #226 Vic.ai-grammar operator app by surface
- Type: wayfinder:task (execution, four-phase) · Started: 2026-09-16 (spec phase) · Finished: 2026-09-16 (spec phase)
- Outcome: partial — **spec phase done** (`milestone: spec-done`); build, measure and close follow in fresh sessions

## What I did
1. Claimed #261; read the map Notes, #235's resolution, the three inputs posted on #261 (#240, #250, #257), the Approvals area primer, both lessons files, `craft-floor.md`, the pre-flight template.
2. Loaded `intent` (`specify`, with `fortify`/`articulate`/`include` folded into the spec) and `impeccable adapt` (the `impeccable context` launcher needs interactive approval — read `PRODUCT.md`/`CONTEXT.md` directly, as the project lessons prescribe).
3. Read the shipped primitives in code (`QueueScreen.cards`, `FilterButton`/`FilterSheet`, `Dialog` sheet, `SavedViewPicker`, `QueueStat`, the five queues' columns, `MobileTabBar` placement, `approvals/loading.tsx`).
4. Wrote `logs/scratch-261/spec.md` (primitive changes, slot matrix per queue, states, copy, keys, residue, test plan) and `preflight.md` Parts A–C, E.
5. Ran the Part D spec critic (sonnet, fresh context): it scored H3/H4 at 2 with two predicted P1s (optional card migration; a focus fallback branch) and three P2s. All six of its changes were applied to the spec (`edit-spec.mjs` records them). Reconciled gate: critique 32, P1 0, Clean, evaluate ≈ 90.
6. Wrote the hand-off with `milestone: spec-done`; committed `wip(autopilot): #261 spec`.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | "Filter" (#257 shipped) vs "Filters · n" (#235 d11, CONTEXT.md)? | **Filters / Filters · n**; sheet title **Sort and filter** | CONTEXT.md glossary + map decision beat a shipped string; one primitive so Approvals inherits |
| 2 | Cancel / Apply (#235 d11) vs Clear · Show n rows (#257)? | keep **Clear · Show n rows** | #235 d12 says reuse #257's primitive; "Show n rows" is Apply naming its consequence (H1/H5); Close/Esc/Back is Cancel — smallest reversible option |
| 3 | Bank Statements "closing balance / period", Exceptions "severity" (#235 d7) — the row has neither | use Amount / Received · Category, and escalation status | d7's own rule "no new data"; `models/exceptions.ts`, `DocumentQueueRow` |
| 4 | Per-queue card renderers (#257 comment) or generic from column slots (#235 d7)? | generic `QueueCard` from `phone` slots; #257's two cards migrate onto it | #235 d7 wins (map decision); B4 one primitive; critic made the migration mandatory |
| 5 | Views on the phone: hide (as #257 does) or compact select (#235 d5)? | native select variant, hidden with zero views | #235 d5; #232 §5 keeps management on desktop |
| 6 | Tab bar: move before the queue or add a landmark? (#240) | move before `#main` (it is `fixed`; DOM-only change) | smallest reversible; it already is a `<nav>` landmark |

## Decisions recorded
Spec phase only — no resolution comment yet. The hand-off carries the six decisions above; the close phase posts them with the scores.

## Tickets created / changed
None. #261 stays open and assigned; `Autopilot: continue —` comment posted with the phase state.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=32 first-critique= close-critique= first-evaluate= close-evaluate=
Predicted evaluate ≈ 90, P1 0, Clean. No surface built yet; no detector counts.

## First-pass findings → lessons (rendered surfaces only)
Not yet — the build has not run. Critic findings against the spec (recorded in preflight Part D): optional-migration escape hatch, focus-fallback branch, bespoke Link beside the default action, truncating segment carrying the key signal, one word with two referents.

## Needs the owner
Nothing in this phase. No removals proposed.
