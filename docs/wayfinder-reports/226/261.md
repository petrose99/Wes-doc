# Autopilot report — #261 Build the phone Queue screen: card rows and the Filters sheet on all five queues

- Map: #226 Post-login UX — the queue, the pane, approvals and the phone lane
- Type: wayfinder:task (execution) · Started: 2026-09-16 18:30 UTC (close phase, session 2; spec/build/measure and a cut close session 1 preceded it) · Finished: 2026-09-16 19:20 UTC
- Outcome: resolved

## What I did
1. Read the hand-off (`261.handoff.md`, `milestone: measured`) and the remains of close session 1 (driver-killed mid-r2: an uncommitted `py-2` on the empty-state button and a half-written `r2/`). Claimed the ticket.
2. Fix batch (one, no P1 in r1): (a) `cramped-padding` ×5 was the Filters trigger's `sr-only` ", n active" span, not the empty-state block — count moved onto `aria-label` (`filter-sheet.tsx`); (b) Receipts `em-dash-overuse` = the "—" empty-cell placeholder (seed has 0 em-dashes) — residue; (c) H4 Views select: by design only on queues with desktop saved views; Approvals had desktop views but no `viewsPhone` — wired (`approval-invoice-queue.tsx`, `approvals/invoices/page.tsx`); (d) the 390 tab-walk "restart" is the walk wrapping past the dev overlay (`nextjs-portal → body`) — a 40-step walk shows it twice; residue.
3. Extended the round script (`scratch-257/pw/round261-r2.mjs`): 1440 tab walks per queue, a live Clear-filters → focus probe at both widths (two readings + URL), Approvals as a sixth queue. Probing found two real defects r1 had not: every Invoices state threw a **hydration error** (the PO chip's own `<a>` nested inside the card `<a>`) → `phoneRender` without `href` (`invoice-queue.tsx`); and the Invoices zero-row query was wrong (`status=cancelled` now has a row; the approval facet is `approval=`) → `aging=90%2B` via `probe-empty261.mjs`.
4. r2 confirm round: 43 states, 0 errored, `pageErrors: []`, detector real findings 0 (em-dash residue only). Contact sheet + three fresh Sonnet readers (critique, evaluate, include).
5. Dev server and live-server stopped; close checks in one script (`scratch-261/close-checks.mjs`): vitest affected 30/30 · full 2691/2691 (274 files) · `tsc --noEmit` 0 errors · eslint ticket files (25) 0 errors, 1 pre-existing #257 warning · eslint repo 27 errors / 48 warnings (pre-existing, outside the queues, per the #258 lesson) · `next build` ok.
6. Wrote the area primer `docs/agents/areas/queue-shell.md` (+ README row); appended 3 generic and 4 project lessons with their `check:`; commits `bb982d1` (fix batch) and the close commit.
7. Resolution comment, ticket closed, map #226 Decisions so far appended, H10 score posted on #264.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | H10 Help = 2 at close (34/40 headline met): hold open or close? | Close; park H10 on #264 with its score | Same call as #258: the help affordance is decided on #241 and built by #264 (open); a heuristic scored on a dimension outside the ticket's scope is not a build defect. Brief: "confirm under the bar with no P1s: close and record the gap." |
| 2 | Views select on PO/Bank/Exceptions too? | No — only queues with desktop saved views (Invoices, Receipts, Approvals) | Spec §2: `SavedViewPicker variant="select"` renders nothing with zero views (critic D6); one term per concept means phone parity with desktop, not a control that explains nothing. Approvals was the real gap and is wired. |
| 3 | Receipts em-dash: fix or leave? | Leave, residue | The 12 "—" are the empty-cell placeholder every queue table uses (33 in `queue-screen.tsx`); the seed and all authored #261 copy contain none. Changing the placeholder is a table-wide decision, not this ticket's. |
| 4 | Exceptions/Approvals filtered-empty not probeable (no zero-row query in the seed) | Ship on the four probed queues; note the gap | The empty state, its Clear filters and the focus hand-off are one shared block in `queue-screen.tsx`, verified live on 4/6 queues at both widths; extending the seed is recorded in the primer for the next probe. |
| 5 | Dev-only "unique key" console warning on the Invoices list (both widths) | Not fixed; recorded on the primer as residue to locate | Fires on the desktop table as well, so it is not the phone card; not located within the close budget; no rendered effect; not a reader finding. |

## Decisions recorded
Five queues (plus Approvals) render the one `QueueCard` from column phone slots below `md`; sort/facets behind Filters + "Sort and filter" sheet; filtered-empty has a default Clear filters with a focus hand-off (Filters trigger → first chip → h1); Views is a native select on queues with saved views; loading skeletons per route; tab bar before `#main`. Bar met except H10 (parked on #264).

## Tickets created / changed
- #261 closed with the resolution comment. No new tickets: every finding was execution (fixed) or already ticketed (#264 carries H10 = 2 from this ticket as from #258).
- Map #226: one Decisions-so-far line appended. No fog graduated (the phone lane's remaining fog is #264's empty-queue guidance, already a ticket).

## Scores and counts (rendered surfaces only)
scores: predicted-critique=32 first-critique=31 close-critique=34 first-evaluate=91 close-evaluate=96

| Critique | H1 | H2 | H3 | H4 | H5 | H6 | H7 | H8 | H9 | H10 | Total |
|---|---|---|---|---|---|---|---|---|---|---|---|
| Predicted (pre-flight) | 4 | 3 | 3 | 3 | 4 | 3 | 3 | 3 | 3 | 3 | 32 |
| First pass (r1) | 3 | 3 | 3 | 4 | 3 | 4 | 3 | 3 | 3 | 2 | 31 |
| Close (r2) | 3 | 3 | 4 | 4 | 3 | 4 | 3 | 3 | 4 | 2 | 34 |

Evaluate: predicted ≈90 / 0 P0 / 0 P1 / Clean → r1 health 91, 0 P0/P1, Clean (issues H3=1, H4=1, H8=1) → r2 health 96, 0 P0/P1, Clean (H6=1 P3: Exceptions card's bare "—" amount).
In-page detector (real findings, residue excluded): r1 — 5 × `cramped-padding` (all `*-filtered-empty-390`), 2 × `em-dash-overuse` (receipts@1440); r2 — 0 at 390 on all 24 phone states, 0 at 1440 except the 2 em-dash placeholder states (residue), 0 at 900. Residue on every state: avatar palette ×2, Inter, dev overlay ×2.
Page errors: r1 not recorded; r2 first run — hydration error on all 6 Invoices states (fixed); r2 final — none.
Include: PASS — tab order at 1440 and 390 on all six queues matches spec §6; Clear-filters focus verified live on Invoices/PO/Receipts/Bank at both widths (390 → "Filters" trigger, 1440 → "Status · All" chip, URL stripped); the 390 wrap past `nextjs-portal` is the dev overlay, not a trap.
Weak pre-build step: Part B4 (primitive reuse) was filled as "PoChip reused" without listing what the reused cell renders inside the card anchor; Part B5's focus contract was a screenshot, not a probe, in the measure round (H1 and H5 came in at 3, not the predicted 4 — the readers wanted the mutation-freshness and confirm evidence visible in the capture, which a queue with no ⚠ action on the phone cannot show).

## First-pass findings → lessons (rendered surfaces only)
| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| Nested `<a>` in the Invoices card → hydration error on every Invoices state (H9, P1-class; found only by the r2 click probe) | Part B4 primitive reuse: "no `<a>` inside the card" was grepped by file, not by ancestry | wrong | Rewritten as a generic lesson with a per-state `pageerror`/`console.error` check; project lesson names `PoChip`/`phoneRender` |
| `cramped-padding` ×5 on filtered-empty-390 (H8, P2; detector) | Project lesson (#250) "sr-only in scrollers is detector noise" existed as prose, not applied to buttons | unchecked | Converted to a project check (`grep sr-only` inside `<button>`); fixed via `aria-label` |
| Focus hand-off unverified (H3, P2; include FAIL) | Part B5 focus/keys row said "probe" but the measure round only screenshot it | unchecked | Generic lesson: a keyboard contract is a probe entry in `keyboard.json` per width before the first measurement |
| Views select absent on PO/Bank/Exceptions (H4, P3) | Spec §2 already scoped it to queues with saved views; Approvals was the real miss | none (spec correct; coverage table lacked Approvals) | Approvals wired; primer lists which queues pass `viewsPhone` |
| Receipts em-dash (H2, residue) | Copy-source check | new class — the placeholder convention predates the ticket; no spec-time check would have changed the build | Recorded as residue in the primer |
| No help affordance (H10 = 2) | Decided on #241, built by #264 | new class (already ticketed) | Parked on #264 with its score, as #258 did |
| Wrong zero-row query / no zero-row query for two queues (probe gap) | None | none | Generic + project lesson with `probe-empty261.mjs` as the check |

## Needs the owner
- Nothing blocking. Residue for a shell ticket: the five app-wide detector findings; the dev-only "unique key" warning on the Invoices list (unlocated); repo-wide eslint 27 pre-existing errors outside the queues; the `react-hooks/set-state-in-effect` warning in `filter-sheet.tsx` (#257 code).
- Seed gap: Exceptions and Approvals have no facet value with zero rows, so their filtered-empty state and focus hand-off are verified only through the shared block — extend `seed257b.ts` before the next queue ticket's probe.
