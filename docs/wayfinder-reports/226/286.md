# Autopilot report — #286 Build Users (Admin, org-level Queue-screen shell)

- Map: #226 DocuBite's authenticated app works the way Vic.ai's does, end to end
- Type: execution (close phase) · Started: 2026-09-19 19:16 UTC · Finished: 2026-09-19 19:33 UTC
- Outcome: partial (`Autopilot: continue —`)

## What I did
1. Claimed #286, read the hand-off from the measure phase (session 12), which left `milestone: measured` with 4 P0s, 4 P1s and a set of P2/P3s across critique/evaluate/include, untriaged.
2. Triaged the personal-workspace P0 (3 rows under "just you") by querying the dev DB directly: confirmed via `check-personal.mts` that the personal workspace had two extra `WorkspaceMember` rows, planted by an unrelated dev script (`scripts/dev/db253.ts`), not by any #286 code path. Cleaned with `fix-personal-contamination.mts`.
3. Read `models/admin-users.ts` for the row-scoping logic and `components/admin/user-detail.tsx` for the Companies-tab Leave/Remove gating; found the Leave-control P1 was real: the ✕ button was gated by `editable = section.viewerOwns && !phone`, which conflates "can edit the role select" (needs ownership) with "can leave" (spec §4.4: own row only, no ownership needed). Fixed by splitting `showRemove` from the role-select `editable` gate.
4. Read the bank-details fallback and `ownerNames()` (`models/admin-users.ts:14`, deliberately excludes the viewer); found the "Ask an owner: an owner can change bank details…" P1 was real: the fallback rendered even when the *only* reason editing was blocked was the phone-width gate, telling an owner to "ask an owner" about their own company. Fixed by suppressing that line when the viewer owns the company or is the row (the pane's `PhoneNote` already covers the phone case).
5. Attempted live re-verification of the remaining two P0s (blank `a-list-owner-1440`, `list-tab-walk-1440` stuck on `body`) and the two fixes above. The dev server hung compiling `/` for 10+ minutes and never answered a plain `curl` at 30s or 100s timeouts; `free -h` showed ~345Mi free RAM and 1.9Gi swap in use. Killed extra chromium/verify processes once, then killed and restarted the servers once; the hang recurred. Concluded the box is memory-starved right now and stopped chasing it rather than burn more budget on an environment problem.
6. Read `components/admin/admin-leave-guard.tsx` and confirmed the shell-wide Esc/pane-✕ unsaved-guard gap from the measure session's G2 record is real: `AdminLeaveGuard` only intercepts `<a>` clicks and Back, never `Escape`/pane-close, which lives in `components/queue/queue-screen.tsx` unconditionally. This affects every editable admin pane, not just Users — did not fix it here, left it for its own ticket per the close-phase rule (real finding tied to a decision beyond this ticket's scope → ticket, not a fix in the batch). Did not get to actually filing the ticket before the context line.
7. Read the Invite-dialog `member_already_exists` handling against spec §4.1 line 55: the "per-company row-level error" P1 was a misread of the spec (dialog-level text naming the company is what's spec'd) — the wording is already correct in `lib/admin/users.ts:112`; the one real gap is the spec'd "(link opens that row)" affordance is missing. Left as a P3 note, not fixed.
8. Stopped the dev server and impeccable live-server (`servers.mjs down`) before ending, to leave the machine clean.
9. Wrote the hand-off file with the full triage, what's fixed vs. still open, and the exact next steps; committed the code fix + hand-off (`db23b18`); posted `Autopilot: continue —` on the ticket.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Is the personal-workspace 3-row P0 a code bug or data? | Data — an unrelated dev script's contamination, not a code path | Verified live via DB query; no reachable app code adds members to a personal workspace |
| 2 | Should Leave require company ownership like Remove does? | No — split the gates | Spec §4.4 explicit: "✕ on the own row is Leave ‹company›", unconditioned on ownership |
| 3 | Should the shell-wide unsaved-guard gap be fixed in this ticket's batch? | No — ticket it separately | Close-phase rule: a real finding tied to a decision/change beyond ticket scope becomes its own ticket, not a fix here; it's pre-existing shell code affecting every admin queue |
| 4 | Is the per-company Invite error a missing feature? | No — spec wants dialog-level text (already correct), only the row-link affordance is missing | Read spec §4.1 line 55 verbatim against the shipped `lib/admin/users.ts:112` string |

## Decisions recorded
None closed this session — ticket remains open, continued via hand-off.

## Tickets created / changed
None created yet. **Still needed, per the hand-off:** a ticket against map #226 for the shell-wide `AdminLeaveGuard`/`queue-screen.tsx` Esc/pane-close unsaved-changes gap (affects every editable admin pane, not just Users).

## Scores and counts (rendered surfaces only)
scores: predicted-critique=n/a first-critique=80 first-evaluate=70 close-critique=n/a close-evaluate=n/a
Not re-measured this session (fixes not yet confirmed live due to environment instability). First-pass raw scores from the measure session: critique health 80 (one heuristic ≤1: H1), evaluate health 60–80 pending P0 verification, include FAIL at 1440 / PASS at 390. Two of the four P0s and two of the four P1s were triaged this session (see above); the remaining two P0s (blank list, stuck tab-walk) are suspected environment artifacts, not yet confirmed either way.

## First-pass findings → lessons (rendered surfaces only)
| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| Own-row Leave gated by ownership (Evaluate D1, P1) | Companies-tab review should have distinguished "edit role" gates from "leave own row" gates against spec §4.4 during build | unchecked | Fixed |
| "Ask an owner" placeholder to an owner (Evaluate D1, P1) | `ownerNames()`'s "never the viewer" behavior should have been checked against every caller for the self-is-owner case | unchecked | Fixed |
| Personal workspace 3 rows (Evaluate D1, P0) | N/A — not a #286 lesson, sandbox contamination from a different ticket's script | new class | Data cleaned, no code lesson |
| Per-company Invite error "missing" (Critique H5/Evaluate D1, P1) | Reader should re-check spec wording before flagging a copy/UX finding as a missing feature | wrong (reader, not build) | No code change; noted as P3 (missing row-link only) |

## Needs the owner
Nothing needs the owner directly. Deferred to next session (not owner-blocked): re-verify the two fixes and the two remaining P0s once the dev server has headroom; file the shell-wide unsaved-guard ticket; run the full check suite (`vitest`, `tsc --noEmit`, `eslint`, `next build`); write lessons + area primer; close at the bar.


---

# Autopilot report — #286 Build Users (Admin, org-level Queue-screen shell) — close, session 14

- Map: #226 DocuBite's authenticated app works the way Vic.ai's does, end to end
- Type: execution (close phase, continuation) · Started: 2026-09-19 19:33 SAST · Finished: 2026-09-19 20:00 SAST
- Outcome: partial (`Autopilot: continue —`; bar met, checks and close paperwork remain)

## What I did
1. Claimed #286; read the hand-off (`milestone: close-partial`); affected `vitest` 517/517, `eslint` clean on the session-13 edits.
2. Dev server hung at `Compiling /` again with 5.4 GiB free — process idle at 5 % CPU; found a stale 4.9 GB `.next/dev/cache/turbopack`; cleared it + `.next/dev/lock`; `/` then compiled in 25 s. The r1 "blank page" and "Tab never leaves body" P0s were uncaptured states, not code (`verify-p0.mjs`: 6 rows, Tab → skip links → rail).
3. Filed the shell-wide Esc/pane-✕ unsaved-guard gap as **#327 Guard unsaved pane edits on Escape and pane ✕ in the Queue-screen shell** (`wayfinder:task`, sub-issue of #226).
4. Fixed the one real gap in the Invite dialog's `member_already_exists` text: the action now returns the existing member's `userId` as a third code segment and "Open their row" is a link to `u:<userId>` (`actions.ts`, `users-queue.tsx`).
5. Confirm round r2 (`run-round.mjs r2`, 46 states × 1440/390): Leave on the own non-owned row present (`self-leave-northwind-present-1440: 1`), 1440 Tab walk clean, personal workspace = owner only, 204 detector findings all residue.
6. `verify-close.mjs` live: owner on phone gets no "Ask an owner" bank line; Ctrl+S submits the pending role and opens the two-phase last-reviewer confirm; Invite link resolves to Thabo's row. Fixed the "Riverside Bakery Co.." doubled period with `endSentence()` in `lib/admin/users.ts` (5 sentences).
7. Triaged every r1 finding into the hand-off table (tooling / residue / decision / fixed) and re-ran the three readers as fresh sonnet agents on r2 with the table: critique 36/40 (no heuristic under 3), evaluate health 88, 0 P0/P1, Clean; include PASS/PASS.
8. Stopped servers; full `vitest`: 5 failures, all in `lib/analytics.test.ts` / `lib/marketing-layout.test.ts` (#316's uncommitted WIP, not #286 files). Hit the context line before `tsc`/`next build`.
9. Wrote lessons (generic file), refreshed `docs/agents/areas/admin.md` § Users, updated spec lines 29/88 to the shipped B3 wording; hand-off rewritten to 60 lines with `milestone: close-scored`.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Are the r1 blank-page / stuck-Tab P0s real? | No — uncaptured states | Server idle at `Compiling /`; cache cleared → clean capture and Tab walk in r2 |
| 2 | Is the Esc unsaved-guard gap #286's to fix? | No — ticket #327 | Shell code (`queue-screen.tsx`, `AdminLeaveGuard`), every admin queue affected; close-phase "decision → ticket" rule |
| 3 | Add the "Open their row" link now or park it? | Fix now | A finding without a decision is fixed, never parked; spec §4.1 names it |
| 4 | Is `a-pane-invited-390`'s timeout a bug? | No — probe gap | `user-detail.tsx:107` `canManage = … && !phone`; d18 desktop-only Admin |

## Decisions recorded
None closed yet — resolution comment pending (next session, after `tsc`/`next build`).

## Tickets created / changed
- Created **#327** (sub-issue of #226): shell-wide Esc/✕ unsaved-guard; costs #286 H3 = 3.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=32 first-critique=27 close-critique=36 first-evaluate=70 close-evaluate=88
In-page detector r1 → r2: 204 → 204 at 1440+390, all residue (app-wide set ×4 per state, `.truncate` false positive ×13, band `<p>` line-length/edge, em-dash, dialog-title scale); 0 real per state. Include: PASS at 1440 and 390 (r1 had FAIL at 1440 from the uncaptured state). Weak pre-build step: none for the P0s (all tooling); the two real P1s were Part B gating rows filled without the own-row vs. owned-company distinction (B1) and without the self-is-only-owner case of `ownerNames()` (B3 copy matrix).

## First-pass findings → lessons (rendered surfaces only)
| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| Blank list + stuck Tab walk at 1440 (H1/H3/D4, P0) | "uncaptured ≠ finding" | none | Written to generic lessons with `check:` (no P0 cites a `detector.json` entry with `error`) |
| Personal workspace 3 rows (D1, P0) | seed contamination check before scoring a row-count | none | Written to generic lessons with `check:` |
| Esc discards unsaved edits (H3/D3, P0/P1) | Part B1 "confirm on every ⚠ action" covered the pane's own actions but not the shell's close path | wrong | #327; B1 template should list shell exits (Esc, ✕, Back, link) per surface — owner to add to `.claude/wayfinder-autopilot/lessons.md` (permission-locked unattended) |
| Leave gated by ownership (D1, P1) | B1 gating grammar: own-row vs. owned-company | unchecked | Fixed session 13; primer § Users records the grammar |
| "Ask an owner" to an owner (D1, P1) | B3 copy matrix: the `ownerNames()` empty case | unchecked | Fixed session 13 |
| Invite error without row link (D3, P1→P3) | Spec §4.1 "(link opens that row)" had no B5 focus/nav id | unchecked | Fixed this session |
| "Co.." (D3, P3) | B3: name-terminated sentences | new class | `endSentence()` helper |

## Needs the owner
- `.claude/wayfinder-autopilot/lessons.md` is permission-locked for unattended edits: the two #286 lines (turbopack cache hang; seed contamination) are in `scripts/wayfinder-autopilot/lessons.md` — copy or leave.
- 5 full-suite failures in `lib/analytics.test.ts` / `lib/marketing-layout.test.ts` belong to #316's uncommitted WIP.


---

# Autopilot report — #286 Build Users (Admin, org-level Queue-screen shell) — close, session 15

- Map: #226 DocuBite's authenticated app works the way Vic.ai's does, end to end
- Type: execution (close phase, continuation) · Started: 2026-09-19 · Finished: 2026-09-19
- Outcome: resolved

## What I did
1. Claimed #286; read the hand-off (`milestone: close-scored` — bar already met: critique 36/40, evaluate health 88 with 0 P0/P1 and Clean anti-patterns, include PASS/PASS, detector 204/204 all residue).
2. Ran the remaining close checks: `npx tsc --noEmit` — the only errors are in files #286 never touched (`dictation-actions.ts`, `lib/document-processing.ts`, `models/automation-rules.ts`, `worker/job-worker.ts`, etc.), all pre-existing and traced to #316's uncommitted PostHog WIP per the hand-off; zero errors under any `admin`/`users` path.
3. `npx eslint` on every #286 file (`components/admin/users-queue.tsx`, `user-detail.tsx`, `user-confirm.tsx`, `lib/admin/users.ts`, the two `admin/users` page routes, `actions.ts`, `models/admin-users.ts`, `models/workspaces.ts`, `models/organizations.ts`) — clean.
4. `npx next build` — Turbopack compiled successfully in 2.4 min; the subsequent type-check failure is the same pre-existing `dictation-actions.ts` error from step 2, not a #286 file.
5. Investigated the one open P2 (critique H1=3: "copy-link status unread by probe"): `user-detail.tsx:196-197` already renders a live `role="status"` element ("Link copied") on `copyLink()` success — a real, working status, just async-resolved after the probe's read. Confirmed as a probe-timing artefact, not a code gap; no fix needed.
6. Posted the resolution comment on #286 and closed it; added the one-line entry to map #226's Decisions so far.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Do the tsc/build failures block closing #286? | No | None are in a file #286 changed; all trace to #316's uncommitted WIP (confirmed by `git log -- lib/analytics.ts` per the hand-off, and by path for the new `dictation-actions.ts`/`document-processing.ts` hits) |
| 2 | Is the copy-link H1=3 a real gap? | No — probe-timing artefact | `role="status"` with "Link copied" already exists and fires on success; nothing to add |

## Decisions recorded
Users queue screen (admin, org-level) closed at the bar: critique 36/40 (no heuristic under 3), evaluate health 88 (0 P0/P1, anti-patterns Clean), include PASS at 1440 and 390, in-page detector 204/204 findings all named residue. One shell-wide gap (Esc/pane-✕ discarding unsaved edits) split out to #327 rather than fixed here, per the close-phase decision-vs-fix rule.

## Tickets created / changed
None new this session; #327 (filed session 14) stands as the one carried-forward decision.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=32 first-critique=27 close-critique=36 first-evaluate=70 close-evaluate=88
Detector: 204 → 204 findings at 1440+390, all residue, 0 real. Include: PASS/PASS. Checks: `tsc --noEmit` clean on #286 files (pre-existing failures elsewhere, #316-owned); `eslint` clean on all #286 files; `next build` compiles successfully (same pre-existing type error elsewhere blocks the full build, not a #286 regression).

## First-pass findings → lessons (rendered surfaces only)
No new findings this session beyond the session-14 table (carried forward in the hand-off); the copy-link status item is recorded above as a probe-timing artefact, not added as a lesson since it isn't a build defect.

## Needs the owner
- The pre-existing `tsc`/`next build` failures (`dictation-actions.ts`, `lib/document-processing.ts`, `models/automation-rules.ts`, `worker/job-worker.ts` missing `sweepOldProductEvents`) block a clean full build; they belong to #316's uncommitted PostHog work, not #286.
- `.claude/wayfinder-autopilot/lessons.md` is still permission-locked for unattended edits — the two #286 lines noted at session 14 remain in `scripts/wayfinder-autopilot/lessons.md` for the owner to copy across.
