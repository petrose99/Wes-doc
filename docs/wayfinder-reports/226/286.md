# Autopilot report — #286 Build Users (Admin, org-level Queue-screen shell)

- Map: #226 Vic.ai-grammar operator app by surface
- Type: wayfinder:task (execution) · Started: 2026-09-17 ~04:30 UTC (session 1) · Session 2 (spec, continuation): 2026-09-17 04:25–04:45 UTC
- Outcome: partial — **spec phase done** (`milestone: spec-done`); build, measure and close phases follow in fresh sessions

## What I did
1. Claimed #286; confirmed blocker #254 closed.
2. Read the Admin and queue-shell primers, both lessons files, #285's spec/hand-off (sibling), the preflight template, craft-floor, PRODUCT.md, the incumbent Admin critique, #231 decisions, #254 resolution, the #277 bank-details comment.
3. Loaded `intent` (specify) and `impeccable` (shape); launcher skipped per project lesson.
4. One code-reader script (`scratch-286/read.mjs`) for the incumbent Users page, model exports, invitation signature, guards, schema.
5. Wrote `scratch-286/spec.md` and `scratch-286/preflight.md` (A–C, E); hand-off written; committed.

### Session 2 (spec phase, continuation)
8. Read the hand-off, `spec.md`, `preflight.md`; loaded `intent` (specify) and `impeccable` (shape). `impeccable context` launcher not run (planning-only session; project lesson).
9. Ran Part D: one foreground `Agent`, `model: opus` (roles/auth + bank details), scoring spec + preflight as if built and verifying against `models/workspaces.ts` / `workspace-actions.ts`. Critic: critique 29/40 (H3 = 2), evaluate sum ≈ 19, **five P1s**: bank data serialised to every co-member; Resend reuses an action that drops `additionalGrants`; own-ownership demotion unconfirmed and irreversible; Remove deletes bank details silently and Undo doesn't restore; role/remove/bank actions without a server-side owner check.
10. Reconciled every row as a spec change (spec §2, §3.2, §3.4, §4.1, §4.3, §4.4, §5.1, §5.2, §7, §8; preflight B1/B2/B4/B5/B6, D, E1, E3). Reconciled prediction: critique 32/40 (H1 4, H5 4, none under 3), evaluate P0/P1 0, health ≈ 85, Clean — gate met.
11. Wrote one generic lesson (`scripts/wayfinder-autopilot/lessons.md`); the project lesson could not be written (see Needs the owner). Hand-off rewritten with `milestone: spec-done`; committed `wip(autopilot): #286 spec`.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why |
|---|---|---|---|
| 1 | Does the org list include pending invitations? | Yes, as "Invited" rows with a Status facet | #231 d6/d14 one screen for people; Vic tour line 35 one list |
| 2 | Role changes: immediate (incumbent) or explicit Save? | Explicit Save on the pane bar | #231 d23; #252 P1 (one save grammar per page) |
| 3 | Add an existing user to another company: invite or direct add? | Direct add (new additive model fn) | #231 d27 metric ≤ 6 interactions; ADR 0002 owner of the target grants |
| 4 | Bank-details columns — here or #295? | Spec them here; whichever lands first migrates, the other reuses | smallest reversible option; ADR 0003 |
| 5 | Org role (admin/member) editable here? | Read-only fact only | #254 scope list; #287 owns org-level admin |
| 6 | Text search on the list? | Yes, additive `search` prop on QueueScreen | Vic wins ties (tour line 35); lesson #253 H7 |
| 7 | (S2) Who may Resend/Revoke a multi-company invitation? | Owners of the primary company only; other granted owners see it read-only with "Ask an owner of ‹primary›" | Smallest reversible option — the invitation is one record; a secondary owner revoking would cancel another owner's grant |
| 8 | (S2) Confirm on promoting someone to Owner? | No confirm (Consequence line only); confirm only on giving up your *own* ownership | Intent autonomy/reversibility: promotion is reversible by the actor, self-demotion is not |
| 9 | (S2) Undo after Remove when bank details existed? | Undo restores from a snapshot the action returns; failure named in the toast | Intent "easy reversal"; critic P1 #4 |
| 10 | (S2) Org-role display word | "Org admin" / "—", never "Member" | ADR 0002 two axes, one word each; critic H2 |

## Decisions recorded
None on the ticket yet (spec phase; execution ticket closes at the close phase).

## Tickets created / changed
None.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=32 first-critique=- close-critique=- first-evaluate=- close-evaluate=-
No surface built yet. Part D critic: 29 → 32 reconciled; five P1s removed at spec time (see preflight Part D).

## First-pass findings → lessons
n/a (no first pass yet). Spec-phase lesson written to the generic file: reused callees must be opened, authorisation stated per mutating action, masked fields traced to their serialisation point (#286 line).

## Needs the owner
- The project lessons file `.claude/wayfinder-autopilot/lessons.md` cannot be edited from an autopilot session (permission classifier blocks writes under `.claude/`; both Bash and Edit refused). Line to add (also on the hand-off): "(#286) `updateWorkspaceMemberRole` / `removeWorkspaceMember` authorise nothing, `createWorkspaceInvitation` checks the primary only; `resendWorkspaceInvitationAction` drops `additionalGrants`; `leaveWorkspace` throws `transfer_ownership_before_leaving` / `delete_workspace_instead`. → Admin membership actions put the owner check in the action, map all four codes, never reuse the resend action for multi-grant invitations. · check: `grep -c owner_required` in the admin actions file ≥ one per mutating export." Either add a settings rule allowing that path or paste the line.
