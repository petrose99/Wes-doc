# Autopilot report — #286 Build Users (Admin, org-level Queue-screen shell)

- Map: #226 Post-login UX implementation
- Type: wayfinder:build · Started: 2026-09-17 09:33 · Finished: 2026-09-17 09:45
- Outcome: partial (build phase, step 2 of 4 done; ticket stays open)

## What I did
1. Read the hand-off (`286.handoff.md`), spec §3.4–§8 and the pre-flight `Check:` lines by range; grepped
   the shell (`queue-screen.tsx`, `detail-pane.tsx`, `expense-claim-queue.tsx` tabs), `AdminSaveBar`,
   `useAsyncConfirm` (`user-confirm.tsx`, already committed), `Dialog`/`ConfirmDialog`/`NativeSelect`,
   `usePhoneLane`, and the actions' signatures.
2. Loaded `fortify` and `impeccable clarify` (continuation: named skills, not the routers); read
   `craft-floor.md`. The `impeccable context` launcher is denied unattended — noted, read project context directly.
3. Wrote the hand-off's missing `step:` plan (4 steps) from its "Next steps".
4. Built step 2: `components/admin/user-detail.tsx` (pane: header pills, invited body with
   Resend/Revoke/Copy link, member body with Information · "Companies, n" tabs, role sections,
   bank details group, one in-pane `AdminSaveBar`, Add-to-company dialog, every confirm from spec §4
   with two-phase last-reviewer and Cancel-only last-owner, Undo toasts with bank snapshot restore,
   Leave-current hard navigation, phone read-only). Wired it into `users-queue.tsx` (helpers ref,
   ⋯ menu registry: Copy email · Resend · Revoke invitation · Leave ‹current›).
5. Checks: `tsc --noEmit` clean; `eslint` clean (fixed a set-state-in-effect warning by mounting the
   Add-to-company form only while open, and two ref-in-render errors); `vitest run lib/queue
   components/queue` 38/38; Part B lint script (`logs/scratch-286/partb-step2.mjs`): every action has a
   caller, no `window.confirm`/`alert(`, no member-as-noun (fixed one string in each file), no "please",
   focus ids present.
6. Marked step 2 `done` on the hand-off, committed `wip(autopilot): #286 build step 2`.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Where does the pane save bar render — `paneActions` slot or inside the content? | Inside the content (`AdminSaveBar inPane`, sticky bottom of the pane scroll) | The `inPane` classes session 6 added are written for the scroll area; keeps dirty state inside `UserDetail` (smallest reversible option) |
| 2 | How do ⋯ menu items (Resend/Revoke/Leave) reach the pane's confirms? | `register` callback → `paneApi` state in `UsersQueue`, stable wrappers over refs | Spec §5.1 lists them on the ⋯; `loadDetail` receives no helpers, `paneMenu` does |
| 3 | Phone read-only in the pane: CSS or behaviour? | `usePhoneLane()` (below `lg`, the shell's one phone query) hides editors, ✕ and the bar | B3: one definition of "phone lane" (`use-phone-lane.ts`); spec §6 `phoneReadOnly` row |
| 4 | "Not a member here" for a non-member of the current company | "Not in this company" | Spec §7: Member is a role, never a noun for a person |

## Decisions recorded
None on the tracker this session (build step; no resolution comment yet).

## Tickets created / changed
None.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=n/a first-critique=n/a close-critique=n/a first-evaluate=n/a close-evaluate=n/a
No captures this session (build step 2; the in-page detector round is step 4). Static
`impeccable detect` launcher denied unattended.

## First-pass findings → lessons (rendered surfaces only)
None yet — no measurement round in this session.

## Needs the owner
Nothing. Next session: build step 3 (page rewrite + `[userKey]` route), then step 4 (build gate).
