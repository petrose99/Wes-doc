# Autopilot report — #286 Build Users (Admin, org-level Queue-screen shell)

- Map: #226 Post-login UX implementation
- Type: wayfinder:measure (phase MEASURE) · Started: 2026-09-17 12:45 · Finished: 2026-09-17 14:30
- Outcome: measured (P0/P1 blockers found; not closed, pending close phase review)

## What I did

1. Read hand-off file (session 11, build-done milestone recorded)
2. Launched servers (`node servers.mjs up`, dev:3000, live-server:8400)
3. Ran capture round r1 (`node run-round.mjs r1`, 46 states × 1440/390, detector JSON, keyboard probes) — 37 images collected
4. Generated contact sheet (`node contact-sheet.mjs shots-r1 --out contact-r1.png --playwright /tmp/scratch229`)
5. Launched three independent readers in parallel:
   - **critique** (Nielsen H1–H10 heuristics) — model sonnet, full output returned
   - **evaluate** (D1–D7 correctness/quality dimensions) — model sonnet, full output returned
   - **include** (keyboard accessibility at 1440/390) — model sonnet, full output returned
6. Recorded all findings verbatim on hand-off file (no triage, no dismissal)
7. Stopped servers (`node servers.mjs down`); committed `wip(autopilot): #286 measured`

## Questions I answered on the owner's behalf

None — this is a plumbing phase. The phase brief says "Do the steps below in order… Run the tooling, launch the readers, and record what came back — exactly, in full, with no opinion on it."

## Decisions recorded

No decisions made. Hand-off file now records every finding from the three readers:
- Critique: H1–H10 scores, P0–P3 findings, health 80
- Evaluate: D1–D7 scores, P0–P3 defects, health 60–80 pending P0 verification
- Include: 390 PASS / 1440 FAIL verdict, P0–P3 accessibility findings

## Tickets created / changed

None. Findings are recorded on the hand-off file and waiting for the close phase to triage and file.

## Scores and counts (rendered surfaces only)

scores: predicted-critique=80 first-critique=80 close-critique=pending first-evaluate=60-80 close-evaluate=pending include-390=pass include-1440=fail

**Critique (Nielsen H1–H10):**
- H1=1, H2=3, H3=2, H4=3, H5=4, H6=3, H7=3, H8=2, H9=3, H10=3
- health: 80 (one heuristic ≤1: H1 blank page on `a-list-owner-1440`)

**Evaluate (D1–D7):**
- D1=3, D2=4, D3=2, D4=3, D5=4, D6=4, D7=3
- health: 60–80 (pending P0 verification of Tab order and Esc guard)

**Include (keyboard):**
- 1440 (desktop): FAIL — `list-tab-walk-1440` shows 20 Tab presses never leaving `body`; header controls and row list not Tab-reachable
- 390 (mobile): PASS — full Tab walk succeeds cleanly with correct skip links, nav collapse, row-as-link exposure

**Detector r1 findings:**
- 37 PNGs captured (23 states × 2 widths)
- r1 detector: same residue classes as r0 (text-overflow, body-text-viewport-edge, em-dash-overuse, flat-type-hierarchy, line-length, dark-glow, cyan gradient, overused-font)
- Keyboard probes: 46 states tested; P0 blocker on `list-tab-walk-1440`, P0/P1 findings on unsaved-guard, Leave control, personal-workspace scoping, per-company invite errors

## First-pass findings → lessons (rendered surfaces only)

| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| H1/H3 P0: `a-list-owner-1440` blank page, script-injection fail, form timeout, Tab-walk stuck on body | Hydration contract: server-rendered RSC must produce a synchronous, interactive page before Playwright probes it | unchecked | Record verbatim; close phase re-verifies live |
| D1 P0: Personal workspace renders 3 rows instead of "just you" | Data-scoping contract: row query must restrict to `viewer only` for personal workspace | unchecked | Record verbatim; close phase fixes |
| D1 P1: Leave control absent for non-owner own-row company | Control-wiring contract: own-row actions don't gate by `viewerOwns` | unchecked | Record verbatim; close phase fixes |
| D1 P1: Copy placeholder "an owner" not interpolated | Copy matrix: "Ask an owner: ‹names›" must interpolate real names | unchecked | Record verbatim; close phase fixes |
| D4 P0: Tab order at 1440 non-functional | Keyboard contract: Tab must traverse all interactive controls per spec §8 | unchecked | Record verbatim; close phase re-verifies live |
| H8 P1: `.truncate` overflow 86px on 9 mobile states | CSS contract: truncate class must contain overflow | unchecked | Record verbatim; close phase verifies live on in-page detector |

## Needs the owner

**Blocking close — re-verify live before proceeding:**
1. **P0 — `a-list-owner-1440` blank page + Tab-walk stuck**: Reload dev server, open Users as org owner at 1440px, verify page renders (not blank) and Tab key advances through header controls and rows.
2. **P0 — Personal workspace shows 3 rows instead of "just you"**: Open dev server as personal-workspace user, verify Users screen shows only one row (You, Owner).
3. **P1 — Leave control missing on own non-owner company**: Open Users as Dev User, open own row pane, check Northwind Traders section for ✕ / Leave button.
4. **P1 — Copy placeholder not interpolated**: Open any row pane at 390px, scroll to Bank details, check "Ask an owner: " shows actual owner name, not literal "an owner".
5. **P1 — Tab order at 1440 (re-verify live)**: Same as #1; focus on whether Tab reaches search, facets, Invite button, and rows. If stuck on body for 20 presses, same P0.

**Not blocking close, document for next phase:**
- P1 per-company Invite error rendering (needs live multi-company invite repro)
- P2 Ctrl+S shortcut testing (add keyboard probe)
- P2 `.truncate` overflow (live in-page detector check)
- P3 copy-pass on em-dashes and line-length for (b)/(c) sentences

**Out of scope / shell-wide — file separately:**
- Cyan gradient, overused Inter font, layout-transition, dark-glow, flat-type-hierarchy (all 44 states including unrelated pages; log once against shell)
