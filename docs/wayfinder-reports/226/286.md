# Autopilot report — #286 Build Users (Admin, org-level Queue-screen shell)

- Map: #226 Post-login UX implementation
- Type: wayfinder:build (phase BUILD, step G3 — build gate, last step) · Started: 2026-09-17 12:43 · Finished: 2026-09-17 12:50
- Outcome: partial (build phase complete — `milestone: build-done`; measure and close are the next sessions')

## What I did
1. Read the hand-off (`286.handoff.md`); step G3 was the first `todo`. No servers were running.
2. `npx tsc --noEmit` over the whole project with servers stopped: exit 0, no diagnostics (log `logs/scratch-286/tsc-g3.log`).
3. Wrote `logs/scratch-286/partb-g3.mjs` — Part B contract checks over the whole surface (`users-queue.tsx`, `user-detail.tsx`, `user-confirm.tsx`, both page files, `lib/admin/users.ts`): B1 reachability (8/8 actions have callers, no native confirm/alert, 9 owner gates), B3 string extraction, B4 primitive diff (no raw `<select>/<dialog>/<table>`, exactly one `AdminSaveBar`, no clickable `Pill`), B5 focus ids. First run: 3 FAILs — one lint false positive (a doc comment mentioning `window.confirm`) and two real B3 hits.
4. Fixed the two B3 hits: "you're not a member of it/them" → "you don't belong to it/them" (`components/admin/users-queue.tsx:126`) and "a company you're a member of" → "a company you belong to" (`admin/users/page.tsx:27`). Re-run: ALL PASS. `eslint` clean on both files.
5. Fixed the `a-pane-invited@390` probe in `round-286.mjs` (`paneAction` helper: inline button ≥ 1024, ⋯ menu item below) — the G2 script error. Syntax-checked; not re-run (measure reuses the script).
6. Rewrote the hand-off to 97 lines (was 175): finished step records condensed to a gist, G3 record added, `milestone: build-done` appended below `spec-done`, next steps point at the measure phase.
7. Committed `wip(autopilot): #286 build`.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Spec §3.1/§5.4 sentences use "member of", which pre-flight B3 forbids (Member is a role name). Which wins? | B3; reworded to "belong to" | Pre-flight contracts are lint per the build brief; step 2 set the precedent ("not a member here" → "not in this company"). Spec lines 29 and 88 to be updated at close. |
| 2 | Is `window.confirm` in a doc comment a B1 failure? | No — lint now skips comment lines | `grep` shows no call site; the comment states the ban. |

## Decisions recorded
None on the ticket this session (build phase; no resolution comment yet).

## Tickets created / changed
None. Pending for close: a shell-wide ticket for "Esc / pane ✕ bypass the unsaved-changes guard" (recorded on the hand-off since G2).

## Scores and counts (rendered surfaces only)
scores: predicted-critique=n/a first-critique=n/a close-critique=n/a first-evaluate=n/a close-evaluate=n/a
No captures or readers this session (build gate G3 has none by design). r0 detector triage from G2 stands on the hand-off; first measured scores come from the measure phase.

## First-pass findings → lessons (rendered surfaces only)
| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| B3 "member of" in two copied-from-spec sentences | Pre-flight B3 vocabulary check should run over the spec's own sentences at spec time, not only over code at build | new class | Fixed in code; noted on hand-off for close to update the spec lines |

## Needs the owner
Nothing this session. Still parked from earlier sessions: the shell-wide unsaved-guard gap (its own ticket at close), org-role editing and org rename (#287).
