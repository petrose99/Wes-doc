# Autopilot report — #286 Build Users (Admin, org-level Queue-screen shell)

- Map: #226 DocuBite's authenticated app works the way Vic.ai's does, end to end
- Type: execution (close phase) · Started: 2026-09-19 19:16 UTC · Finished: 2026-09-19 19:33 UTC
- Outcome: partial (`Autopilot: continue —`)

## What I did
1. Claimed #286, read the hand-off from the measure phase (session 12), which left `milestone: measured` with 4 P0s, 4 P1s and a set of P2/P3s across critique/evaluate/include, untriaged.
2. Triaged the personal-workspace P0 (3 rows under "just you") by querying the dev DB directly: confirmed via `check-personal.mts` that the personal workspace had two extra `WorkspaceMember` rows, planted by an unrelated dev script (`scripts/dev/db253.ts`), not by any #286 code path. Cleaned with `fix-personal-contamination.mts`.
3. Read `models/admin-users.ts` for the row-scoping logic and `components/admin/user-detail.tsx` for the Companies-tab Leave/Remove gating; found the Leave-control P1 was real: the ✕ button was gated by `editable = section.viewerOwns && !phone`, which conflates "can edit the role select" (needs ownership) with "can leave" (spec §4.4: own row only, no ownership needed). Fixed by splitting `showRemove` from the role-select `editable` gate.
4. Read the bank-details fallback and `ownerNames()` (`models/admin-users.ts:14`, deliberately excludes the viewer); found the "Ask an owner: an owner can change bank details…" P1 was real: the fallback rendered even when the *only* reason editing was blocked was the phone-width gate, telling an owner to "ask an owner" about their own company. Fixed by suppressing that line when the viewer owns the company or is the row (the pane's `PhoneNote` already covers the phone case).
5. Attempted live re-verification of the remaining two P0s (blank `a-list-owner-1440`, `list-tab-walk-1440` stuck on `body`) and the two fixes above. The dev server hung compiling `/` for 10+ minutes and never answered a plain `curl` at 30s or 100s timeouts; `free -h` showed ~345Mi free RAM and 1.9Gi swap in use. Killed extra chromium/verify processes once, then killed and restarted the servers once; the hang recurred. Concluded the box is memory-starved right now and stopped chasing it rather than burn more budget on an environment problem.
6. Read `components/admin/admin-leave-guard.tsx` and confirmed the shell-wide Esc/pane-✕ unsaved-guard gap from the measure session's G2 record is real: `AdminLeaveGuard` only intercepts `<a>` clicks and Back, never `Escape`/pane-close, which lives in `components/queue/queue-screen.tsx` unconditionally. This affects every editable admin pane, not just Users — did not fix it here, left it for its own ticket per the close-phase rule (real finding tied to a decision beyond this ticket's scope → ticket, not a fix in the batch). Did not get to actually filing the ticket before the context line.
7. Read the Invite-dialog `member_already_exists` handling against spec §4.1 line 55: the "per-company row-level error" P1 was a misread of the spec (dialog-level text naming the company is what's spec'd) — the wording is already correct in `lib/admin/users.ts:112`; the one real gap is the spec'd "(link opens that row)" affordance is missing. Left as a P3 note, not fixed.
8. Stopped the dev server and impeccable live-server (`servers.mjs down`) before ending, to leave the machine clean.
9. Wrote the hand-off file with the full triage, what's fixed vs. still open, and the exact next steps; committed the code fix + hand-off (`db23b18`); posted `Autopilot: continue —` on the ticket.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Is the personal-workspace 3-row P0 a code bug or data? | Data — an unrelated dev script's contamination, not a code path | Verified live via DB query; no reachable app code adds members to a personal workspace |
| 2 | Should Leave require company ownership like Remove does? | No — split the gates | Spec §4.4 explicit: "✕ on the own row is Leave ‹company›", unconditioned on ownership |
| 3 | Should the shell-wide unsaved-guard gap be fixed in this ticket's batch? | No — ticket it separately | Close-phase rule: a real finding tied to a decision/change beyond ticket scope becomes its own ticket, not a fix here; it's pre-existing shell code affecting every admin queue |
| 4 | Is the per-company Invite error a missing feature? | No — spec wants dialog-level text (already correct), only the row-link affordance is missing | Read spec §4.1 line 55 verbatim against the shipped `lib/admin/users.ts:112` string |

## Decisions recorded
None closed this session — ticket remains open, continued via hand-off.

## Tickets created / changed
None created yet. **Still needed, per the hand-off:** a ticket against map #226 for the shell-wide `AdminLeaveGuard`/`queue-screen.tsx` Esc/pane-close unsaved-changes gap (affects every editable admin pane, not just Users).

## Scores and counts (rendered surfaces only)
scores: predicted-critique=n/a first-critique=80 first-evaluate=70 close-critique=n/a close-evaluate=n/a
Not re-measured this session (fixes not yet confirmed live due to environment instability). First-pass raw scores from the measure session: critique health 80 (one heuristic ≤1: H1), evaluate health 60–80 pending P0 verification, include FAIL at 1440 / PASS at 390. Two of the four P0s and two of the four P1s were triaged this session (see above); the remaining two P0s (blank list, stuck tab-walk) are suspected environment artifacts, not yet confirmed either way.

## First-pass findings → lessons (rendered surfaces only)
| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| Own-row Leave gated by ownership (Evaluate D1, P1) | Companies-tab review should have distinguished "edit role" gates from "leave own row" gates against spec §4.4 during build | unchecked | Fixed |
| "Ask an owner" placeholder to an owner (Evaluate D1, P1) | `ownerNames()`'s "never the viewer" behavior should have been checked against every caller for the self-is-owner case | unchecked | Fixed |
| Personal workspace 3 rows (Evaluate D1, P0) | N/A — not a #286 lesson, sandbox contamination from a different ticket's script | new class | Data cleaned, no code lesson |
| Per-company Invite error "missing" (Critique H5/Evaluate D1, P1) | Reader should re-check spec wording before flagging a copy/UX finding as a missing feature | wrong (reader, not build) | No code change; noted as P3 (missing row-link only) |

## Needs the owner
Nothing needs the owner directly. Deferred to next session (not owner-blocked): re-verify the two fixes and the two remaining P0s once the dev server has headroom; file the shell-wide unsaved-guard ticket; run the full check suite (`vitest`, `tsc --noEmit`, `eslint`, `next build`); write lessons + area primer; close at the bar.
