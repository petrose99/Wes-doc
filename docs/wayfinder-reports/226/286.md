# Autopilot report — #286 Build Users (Admin, org-level Queue-screen shell)

- Map: #226 Post-login UX implementation
- Type: wayfinder:build (phase BUILD, step 3 of 4) · Started: 2026-09-17 09:40 · Finished: 2026-09-17 10:40
- Outcome: partial (step 3 done; build gate is the next session's)

## What I did
1. Read the hand-off (`286.handoff.md`), spec §2 / §3.3 / §5.4 by range, the incumbent page, the #285 Companies page + `[companyId]` route as the pattern, and `UsersQueueProps`.
2. Rewrote `app/(app)/workspaces/[workspaceId]/admin/users/page.tsx` as `UsersScreen({ params, selectedId })` → `getAdminContext` → `loadUsersPage` → `<UsersQueue>` with the §5.4 nameless `initialMissing` notice. Added `admin/users/[userKey]/page.tsx` (deep link, `decodeURIComponent`). Added `UserDetail` to `admin/layout.tsx` `CLIENT_MANIFEST_ANCHORS` (spec §2).
3. Fixed a step-1 deviation found while checking: personal mode rendered a sentence-only section; spec §3.3 (c) is a one-row list whose pane opens for bank details. `users-queue.tsx` now renders `QueueScreen` in every mode (personal: no sort/facets/search, the §3.1 sentence as `band`, no `ReadOnlyBand`).
4. Checks: `tsc --noEmit` clean, `eslint` clean on the touched files. Live under `DEV_AUTH_BYPASS` (scripts in `logs/scratch-286/`: `servers.mjs`, `step3-check.mjs`, `step3-text.mjs`): personal workspace renders rows; Northwind (org) renders 2 rows; `/users/u:<id>` keeps the URL and opens the pane with its tabs; `/users/not-a-real-key` and `/users/inv%3Anope` show "That person isn't in a company you're a member of."; no page or console errors.
5. Stopped the dev server and live-server; updated the hand-off (step 3 `done`, records, seeded ids, machine note); committed `wip(autopilot): #286 build step 3`.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Personal workspace: keep step 1's sentence-only section or the spec's one-row list? | One-row list with the pane | Spec §3.3 (c) and #277 ("bank details editable by the member") — the pane is the only way to your own bank details |
| 2 | Add `UserDetail` to the layout's manifest anchors although the pane is client-rendered? | Yes | Spec §2 names it; same trick #285 needed; harmless if unused |

## Decisions recorded
None (build step; no resolution comment yet).

## Tickets created / changed
None.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=n/a first-critique=n/a close-critique=n/a first-evaluate=n/a close-evaluate=n/a
Detector and probes run at the build gate (step 4); none this session.

## First-pass findings → lessons (rendered surfaces only)
| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| Personal mode dropped the row + pane (H2/H7, would have been P2) | Step 1's check was `tsc`/vitest only; the state table §3.3 wasn't ticked per state | unchecked | Fixed in place; gate must capture (c) as a state |

## Needs the owner
Nothing. Note for measure: on a deep-link miss the shell announces via `role=status` and leaves focus on body; spec §5.4 says focus `#queue-title` — shell-wide behaviour shared with #285, triage at measure.
