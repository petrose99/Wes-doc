# Autopilot report — #292 Decide the ledger stance: DocuBite posts to the client's books through the provider they connect — no primary built-in ledger

- Map: #226 Map: Make the operator app work like Vic.ai (grammar, not skin)
- Type: wayfinder:grilling · Started: 2026-09-19 · Finished: 2026-09-19
- Outcome: resolved

## What I did

1. Claimed #292 (`gh issue edit 292 --add-assignee @me`).
2. Read the map (#226) body, #248 (Decide Finance's shape once Bill Pay exists), #281 (Build Post to ledger — execution), #282 (owner sign-off: retire the Finance rail entry), and CONTEXT.md's Built-in ledger / Ledger mark / Finance / Close the books entries.
3. Established code facts before questioning: `IntegrationConnection` (`prisma/schema.prisma:1276-1427`) is already provider-agnostic (one row per `(workspaceId, provider)`); `lib/finance/actions.ts` already resolves "the" connection generically across bigcapital/xero/quickbooks; `IntegrationsManager` already lets a workspace connect Xero or QuickBooks by choice. The one place Bigcapital is special-cased: it auto-provisions for every workspace with no choice (`admin/integrations/page.tsx`'s standalone "Ledger connection" `Panel`), and the rail/phone-tab gate `accountingEnabled` (`layout.tsx:91,105`) is keyed on `config.integrations.bigcapital.enabled` alone.
4. Ran the grilling rounds (Intent `strategize` → `journey` → `articulate`; Impeccable `shape`/`clarify`/`distill`), 8 questions across 4 rounds, each with a grounded `➡️` recommendation, taken per the owner's standing delegation.
5. Posted the resolution comment: https://github.com/petrose99/Wes-doc/issues/292#issuecomment-5744896444
6. Closed #292: https://github.com/petrose99/Wes-doc/issues/292
7. Created the execution ticket [Build the ledger-stance reversal (#329)](https://github.com/petrose99/Wes-doc/issues/329), labelled `wayfinder:task` (this map's convention for execution tickets — #281/#251/#273 all carry it), and wired it as a sub-issue of the map (#226) via the `sub_issues` API.
8. Commented on #281 and #282 pointing them at the resolution and #329, since both were blocked by #292 and are now unblocked; noted that #281's own vocabulary needed no rework beyond the "Open ledger" → "Open in ‹Provider›" link label.
9. Updated the map's Decisions-so-far with a one-line entry for #292, linking #329.
10. No code was changed — this ticket is a decision only.

## Questions I answered on the owner's behalf

| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Adopt no-primary-ledger stance or keep Built-in ledger as default? | Adopt no-primary-ledger | Architecture already generic; keeping the fiction costs two live defects (auto-provision-for-everyone, Bigcapital-only gate) for an unneeded claim |
| 2 | Does Bigcapital survive as an offered connector? | Yes, demoted to a fourth choice | Full client/mapper exists (`lib/integrations/bigcapital/*`); dropping it destroys working capability for no gain |
| 3 | What replaces the Provisioning step? | Not connected → choose provider → Authorising… → Connected/Needs reconnect | Matches the ticket's own proposed state set; provisioning was plumbing, not a client-facing state |
| 4 | Does every workspace still get auto-provisioned at signup? | No — "Not connected" is the true starting state | Consistent with no-primary-ledger; no connector without a client choice |
| 5 | Provider-neutral or provider-named copy? | Action words (Post/Posted/Ledger mark/facet) stay neutral; the destination link becomes provider-named ("Open in Xero") | #281's words already provider-neutral by design; naming the destination once connected is honest, not decoration |
| 6 | What happens to "Built-in ledger"? | Term retires from CONTEXT.md; Close the books capability copy drops "the built-in ledger," | Direct consequence of the stance change |
| 7 | One connector panel or two? | One — fold Bigcapital into `IntegrationsManager`'s provider list | `distill`: cut the special-cased standalone Panel and its auto-provision framing |
| 8 | What replaces the Bigcapital-only `accountingEnabled` gate? | `bigcapital.enabled \|\| xero.enabled \|\| quickbooks.enabled` | Matches #281's stated meaning of "ledger flag off" as "no ledger capability at all," not "Bigcapital specifically is off" |

## Decisions recorded

No primary built-in ledger: DocuBite posts to whichever provider the client connects and authorises (Xero, QuickBooks, or Bigcapital — Sage stays fog, no client exists). Auto-provisioning retires; the connection surface becomes one provider picker with states Not connected → choose provider → Authorising… → Connected (provider-named) · Needs reconnect. Action vocabulary (Post, Posted, Ledger mark, Ledger facet) stays provider-neutral per #281; the destination link becomes provider-named ("Open in ‹Provider›"). CONTEXT.md's Built-in ledger term retires. Full detail on the ticket's resolution comment.

## Tickets created / changed

- Created: [Build the ledger-stance reversal: connector picker, provider-named links, accountingEnabled gate fix (#329)](https://github.com/petrose99/Wes-doc/issues/329) — execution ticket, `wayfinder:task`, wired as a sub-issue of map #226.
- Changed: #281 (Build Post to ledger) — comment pointing to the resolution and #329; no spec rewrite needed (its own vocabulary was already provider-neutral).
- Changed: #282 (owner sign-off: retire the Finance rail entry) — comment noting "Open ledger" → "Open in ‹Provider›" wording change inherited from #292.
- Map #226: Decisions-so-far line added for #292.
- Fog: no new fog added; Sage-as-fourth-connector called out explicitly as out of scope (no client/mapper exists) rather than left implicit.

## Scores and counts (rendered surfaces only)

Not applicable — this is a decision ticket; no surface was built or measured this session.

## First-pass findings → lessons (rendered surfaces only)

Not applicable.

## Needs the owner

Nothing deferred. This decision required no removal (Bigcapital's hosting/infra is kept; only its automatic/default status is dropped) and no credentials, so it did not trigger the removal-protocol carve-out. The execution ticket (#329) is unclaimed and ready for a future session.
