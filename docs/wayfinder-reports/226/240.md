# Autopilot report — #240 Decide keyboard accelerators across the operator app: jumping between queues and rows

- Map: #226 Map: Make the operator app work like Vic.ai (grammar, not skin)
- Type: wayfinder:grilling · Started: 2026-09-16T00:02Z · Finished: 2026-09-16T00:20Z
- Outcome: resolved

## What I did

1. Loaded map #226 and ticket #240; claimed #240 (assigned).
2. Called `grilling`, `domain-modeling`, `intent` (→ `journey`, `include`), `impeccable` (critique of the incumbent keyboard flow, then `shape`). `impeccable context` did not run — the launcher call was denied by the session's permission rules; read `PRODUCT.md` directly (no `DESIGN.md` exists).
3. Gathered incumbent evidence in code (`components/queue/queue-screen.tsx` ↑/↓/Esc guard, `use-field-nav.ts` Enter, `global-search.tsx` ⌘K — unmounted) and live: a Playwright keyboard walk + in-page detector on the Invoices queue at 1440 and 390 (`/tmp/scratch229/keywalk240.mjs`, dev server and impeccable live-server were already running). Tour §6 has no record of Vic's shortcuts.
4. Ran three grilling rounds (12 questions), answering each with its `➡️` recommendation.
5. Added the glossary term **Keyboard shortcut** to `CONTEXT.md`.
6. Created execution ticket [#262](https://github.com/petrose99/Wes-doc/issues/262) and wired it as a sub-issue of #226 (no blocker: sequences register from the rail so unbuilt destinations have no key).
7. Posted the resolution comment on #240, closed it, appended the decision line to the map's Decisions so far.
8. Posted inputs to #245 (search key / ⌘K revival) and #261 (phone tab order).

## Questions I answered on the owner's behalf

| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Whose day do the keys serve | Desktop Reviewer only; tablets ≥md get the same set | #232 phone = Approver, no keyboard; #239 Reviewer order |
| 2 | The one job | Reach + traverse rows, then jump queues; filters in; search out | 28 Tabs to first row measured; ⌘K unmounted → #245 |
| 3 | Any key acting on data | None; pane Enter field-confirm stays (writes nothing) | #227 no single-key approve, #234 Delete behind ⋯; catalog "Destructive defaults" |
| 4 | Row traversal | Roving tabindex (Tab, ↑/↓, Home/End, Enter); pane ↑/↓/Esc stays | Include: composite-widget pattern; focus-scoped → 2.1.4 exempt |
| 5 | Queue switching | `g`+letter with off switch; Ctrl/Alt+digit rejected | Browser tab chords; 2.1.4 turn-off mechanism (Gmail/GitHub/Linear) |
| 6 | Filters / help keys | `f` first facet chip, `?` dialog; same input/dialog guard | Header band is the 10-Tab wall; guard already in `queue-screen.tsx` |
| 7 | Off switch | On by default, per user in localStorage, in dialog + account menu; letters only | Smallest reversible; server setting can follow with #252 |
| 8 | SR / voice conflicts | Accepted as designed; focus first row after a jump | Browse mode consumes letters; route announcer; Include |
| 9 | Discoverability | `?` dialog + account-menu item + rail tooltips, one source | Operate native expectation; H10 belongs to #241 |
| 10 | Dialog shape | Centred dialog on `ui/dialog.tsx`, 3 groups, `<kbd>` hairline | craft-floor: no faux key caps |
| 11 | Focus rendering | Existing emerald focus-visible ring + aria-current | Already what Esc returns to; 3:1 ring |
| 12 | Motion | None beyond shared dialog transition | Operate mode |

## Decisions recorded

Desktop-only shortcut layer for the Reviewer. Rows become a roving-tabindex group (always on, not a shortcut). `g`+letter jumps queues (i p r b e a y d), `f` focuses filters, `?` opens the Keyboard shortcuts dialog; all three under a per-user off switch that satisfies WCAG 2.1.4. No key acts on data. No search key. Three discoverability surfaces from one source (dialog, account menu, rail tooltips). Glossary: Keyboard shortcut.

## Tickets created / changed

- Created [#262 Build the keyboard shortcut set](https://github.com/petrose99/Wes-doc/issues/262) (`wayfinder:task`), sub-issue of #226, unblocked.
- Closed #240 with the resolution comment.
- Comments added to #245 (search key input) and #261 (phone tab order at 390).
- Fog: none to graduate — the "Keyboard shortcuts" patch had already become #240.

## Scores and counts (rendered surfaces only)

No score; the shortcut layer is not built yet. Incumbent evidence for #262's baseline: in-page detector Invoices list/pane-open 3/4 at 1440, 3/3 at 390; 28 Tabs from load to the first row at 1440 (15 at 390); no key acts with the pane closed; pane ↑/↓/Esc works with focus returning to the row.

## Needs the owner

- Nothing blocking. The ticket's claim that Vic has a "Shortcuts" account-menu item is not in the tour record; the decision stands without it.
- Optional later: whether the off switch should become a server-side per-user setting (#252).
