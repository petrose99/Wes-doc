# Autopilot report — #257 Build the phone approval lane: Approvals-first tab bar, Ready to Approve cards, full-screen Detail sheet with the decision bar

- Map: #226 Post-login UX — the operator app after Vic's tour
- Type: wayfinder:task · Started: 2026-09-16 (spec session 1) · this close session 2026-09-16T14:25Z → 2026-09-16T15:30Z
- Outcome: resolved

Ten sessions in three phases (spec 1 · build 2–6 · close 7–10); the per-session history is in
`logs/scratch-257/handoff-history.md`, the spec in `logs/scratch-257/spec.md`, the filled pre-flight in
`logs/scratch-257/preflight.md`.

## What I did

1. Spec phase: `intent specify`/`fortify`/`articulate`/`include` → `spec.md`; `impeccable shape`/`layout`/`typeset`/`clarify`; pre-flight A–E filled, opus spec critic (12 findings applied, `apply-critic.mjs`); predicted critique 32, evaluate ≈85, 0 P1.
2. Build phase (commits `9fc7d17`, `2df22d6`, `91ae42b` and WIPs): tab bar Approvals · Invoices · Exceptions · Account; `QueueScreen` cards/Filter sheet/pushState pane; `QueueSegments`; Dialog `placement="sheet"`; `DetailPane` as a modal sheet below `lg` with `inert`; split-pane tablist + source toggle; `ApprovalTab` timeline + "From this supplier" (`getApprovalDetailFacts`); `DecisionResultStrip`; client-side `lib/approvals/filters.ts`; Part B contract checks 36/36 (`partb-check.mjs`).
3. Close phase: capture harness `logs/scratch-257/pw/round.mjs` (20 states × 1440/390, in-page detector JSON, keyboard probe); round 1 first measurement; fix batch 1 (six fixes, `75dee81`); rounds 2–3 confirm; readers on r3.
4. This session: servers stopped; `tsc --noEmit` clean; `models/approvals.test.ts` fixed for the payment-gate query (mock `integrationPush`, +2 tests); eslint errors in our files cleared (`queue-screen.tsx` `pushedRef` → state for `react-hooks/refs`; unused imports in `split-pane.tsx`); full vitest; `next build` OK; evaluate + include readers on r3 and an H8 re-score; include's P2/P3 fixed in place (`aria-live` row count in the Filter sheet, 44px Filter trigger); hand-off cut to pointers (was 525 lines); lessons; primer `docs/agents/areas/approvals.md`; resolution comment; close.

## Questions I answered on the owner's behalf

| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | "Audit" tab keeps its name or takes the spec's "History" below `lg`? | Keep "Audit" | B3 one term per concept; #236/#259 named it |
| 2 | Does the non-empty list hint that others are waiting? | No — empty state only | spec 3.6; evaluate H7 P2 accepted, recorded here |
| 3 | The server refuses Approve for unconfirmed payment — surface how? | Row eligibility "Confirm whether this has been paid before approving it.", Approve disabled; server still re-checks | H1/H5 honesty about a hard gate; not a decision (the gate exists, #227's server-confirmed rule) |
| 4 | "No approval flow yet" — its own empty block? | No; "Nothing needs your approval" + "Anything you can decide will show here." | smallest reversible option; the model returns no rows without a workflow, the copy stays true |
| 5 | `ApprovalsQueuePicker` now unused — delete? | Not deleted | removal protocol #237 → owner sign-off |
| 6 | Filter sheet row-count announcement / 40px Filter trigger (include P2/P3) | Fixed in place | pure execution, no decision |

## Decisions recorded

Phone lane below `lg`: tab bar Approvals (badge) · Invoices · Exceptions · Account; one Ready to Approve list with segments (Invoice approvals · PO mismatches) as cards; Filter · n sheet replaces chips; the Detail pane is a full-screen modal sheet with its own history entry (Back/gesture/Escape all pop it); Approve/Reject/Override are bottom sheets that state the consequence; after a decision the row stays pinned with a result strip (Approved/Rejected · just now, Back to list, Next to approve) — no toast on phone, no Undo (#227); offline disables submit with one sentence; server refusals show in the strip; the payment gate is row eligibility. Send back for review is desktop-only. Client-side facet filtering on Approvals. One `QueueSegments` picker at every width.

## Tickets created / changed

- #257 closed. Map Decisions so far: one line appended; #289's over-long entry compacted.
- #261 (Build the phone Queue screen on all five queues) — comment: `QueueScreen.cards`/`FilterSheet`/`usePhoneLane` and the capture harness exist; wire them, don't rebuild.
- New: "Sign off: remove `ApprovalsQueuePicker` and `MobileTabBar.accountingEnabled`" (owner sign-off, #237 protocol) — blocked by nothing, labelled `wayfinder:task`.
- No fog graduated (the answer sharpened nothing new in Not yet specified).

## Scores and counts (rendered surfaces only)

scores: predicted-critique=32 first-critique=34 close-critique=35 first-evaluate=73 close-evaluate=95

| Heuristic | Predicted | First (r1) | Close (r3 + post-r3 probes) |
|---|---|---|---|
| H1 | 4 | 3 | 3 |
| H2 | 3 | 4 | 4 |
| H3 | 3 | 3 | 3 |
| H4 | 3 | 3 | 3 |
| H5 | 4 | 4 | 4 |
| H6 | 3 | 4 | 4 |
| H7 | 3 | 4 | 4 |
| H8 | 3 | 2 | 3 (re-scored with the overflow fixed) |
| H9 | 3 | 3 | 3 |
| H10 | 3 | 4 | 4 |

- evaluate: first 73 (P1 1: Approve enabled on rows the server refuses; P2 4; P3 1; Clean) → close **95** (P0 0 · P1 0 · P2 1 · P3 2 · Clean). Remaining P2 = the source-strip overflow, fixed after r3 and verified by `probe-detect.mjs` (no `overflow|cramped` findings); P3s: tab-strip flush on the border (by design), network-error state not captured (source-verified: sheet stays open, `NETWORK_ERROR` shown).
- include: first Pass with 1 P1 (card/tab accessible names concatenated) → close **Pass, P1 0, P2 1, P3 2**; the P2 (live row count) and one P3 (Filter trigger 40px) fixed this session; Home/End on the Filter radiogroup left (APG nicety, not a SC). Keyboard flow verified live at 390 and 1440 (`shots-r3/keyboard.json`: tab walk, dialog/inert 36, roving tabs, sheet focus on textarea, Escape innermost-only, focus back to the card, result-strip focus → Back to list → Next to approve).
- In-page detector (non-residue): r1 **69 @1440 / 65 @390** over 15/18 states → r3 7 @1440 / 23 @390, all of which were the h2 suffix, the filename strip (both fixed after r3, probe clean) and the tab-strip "flush against border-bottom" (residue, by design). Close: **0 @1440, 0 @390** beyond the named residue (app-wide five; rail hover occlusion; `hidden lg:table` wrapper; tab strip; source-panel overflow chain; "Could not render this PDF" seed).
- Weak pre-build step: Part B rows B1 (action reachability — the payment precondition) and B5 (focus paths — three faults) were filled from the spec, not from the server action and the live probe; `typeset` looked at desktop only (H8 at 390).
- Gap: `components/**` is outside the vitest include, so spec §6's `queue-screen.test.tsx`/`history-tabs.test.tsx` cannot run; the pure parts are tested in `lib/approvals/filters.test.ts`, `lib/queue/filters.test.ts`, `models/approvals.test.ts` (17 tests). The design-detector hook did not fire during the build sessions (edits via `.mjs` scripts); the in-page rounds covered it.

## First-pass findings → lessons (rendered surfaces only)

| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| Approve enabled on a row the server refuses for payment status (evaluate H1/H5, P1) | Part B1 action reachability — filled from the spec's reasons, not the action's asserts | unchecked | Generic lesson with `check:` (assert names ↔ row-eligibility); project lesson naming `review-tasks.ts` ↔ `row-eligibility.ts`; fixed |
| Card `<a>` / tab names concatenated without separators (include, P1) | none | none | Generic lesson with `check:` (probe prints names; glued words fail); fixed |
| Phone h1 18px on 16px body; segment tabs 0px vertical padding; h2/filename overflow (critique H8 = 2) | `typeset`/`layout` per breakpoint — only desktop was inspected | none | Generic lesson with `check:` (detector `flat-type-hierarchy`/`cramped-padding` at 390 before first measurement); fixed |
| Typed reason dropped at 390 (Dialog effect keyed on `onClose`); "Maximum update depth" loop (context object in deps); focus on body after the result strip replaced its trigger; Back at 390 didn't pop history (B5) | Part B5 focus/keys/failure path — filled without a live probe | unchecked | Generic lesson with `check:` (probe `activeElement` after decisions; eslint react-hooks per batch); project lesson (pushState Back assertion); fixed |
| Filter sheet row count not announced; Filter trigger 40px (include P2/P3) | `include` plan (announcements) listed the h1 count only | unchecked | Fixed in place; covered by the same B5 probe check |
| Model test broke on a new `prisma.integrationPush` query | none (project) | new class | Project lesson: run the model's test after any model edit; fixed |
| evaluate H7 P2 "no hint others are waiting when the list is non-empty" | spec 3.6 decided empty-state only | — | Accepted, recorded (question 2) |

## Needs the owner

- Removal sign-off (#237 protocol): `components/typed-destinations/approvals-queue-picker.tsx` (`ApprovalsQueuePicker`, unused since `QueueSegments`) and the unused `accountingEnabled` prop on `MobileTabBar` — on the new sign-off ticket.
- Pre-existing eslint error not from this ticket: `app/(app)/workspaces/[workspaceId]/worksheets/page.tsx:7` unused `ArrowDownToLine` (commit `bd6233d`). Left untouched.
- The `impeccable` launcher (`context`, `detect`) needs an interactive approval the driver cannot give; the static scan was never run on this ticket. The in-page detector rounds stand in for it.
