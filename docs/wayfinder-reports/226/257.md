# Autopilot report — #257 Build the phone approval lane

- Map: #226 Post-login UX
- Type: wayfinder:task (execution) · Started: 2026-09-16 (session 5, build phase) · Finished: 2026-09-16
- Outcome: partial — build phase continued, hand-off at the context line; `milestone: build-done` not yet reached

## What I did
1. Read the hand-off (sessions 1–4); ran `tsc --noEmit` — 9 syntax errors in `queue-screen.tsx` from session 4's template-literal Tailwind classes.
2. Rewrote those with a literal `BELOW` class map; wired `filterRows`, `pinned`, `initialMissingNotice`, `cards.title`, `empty.action/filteredAction`, `PaneHelpers.next`, Filter row (`FilterButton`/`FilterSheet`), band placement, `aria-live` count — commit `2df22d6`.
3. DetailPane: phone-lane `role="dialog" aria-modal` + `inert` siblings, `backLabel`, footer grid selectors, slide-in keyframe — same commit.
4. split-pane: source Show/Hide toggle, real tablist (roving, ←/→, panels), Approval-first order below `lg`; history-tabs: `ApprovalTimeline` + `ApprovalTab`; wired `getApprovalDetailFacts` into `getSelectionAuditPanelDataAction` and `actorAvatar` into stage decisions — commit `91ae42b`.
5. `tsc --noEmit` clean (0 errors) at the end of the session. Hand-off updated with the exact surface-wiring plan (item 6) and tests (item 7).

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Keep `ApprovalStepChain` as a wrapper or drop it? | Dropped (no other callers) | B4 one primitive; not a feature removal — internal export only |
| 2 | Tab order below `lg` | Approval · Details · Checks · Audit · Note | Spec 3.5; label "Audit" kept (B3, #236/#259) |
| 3 | `facts` load failure | `.catch(() => null)`, tab degrades to #218 data | fortify: partial state must not fail the pane |

## Decisions recorded
None on the ticket yet (build phase). Design decisions are listed in the hand-off "Session 3/5" sections for the close-phase resolution comment.

## Tickets created / changed
None.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=34 first-critique=n/a close-critique=n/a first-evaluate=n/a close-evaluate=n/a
No capture this phase (build); measuring is the close phase's.

## First-pass findings → lessons (rendered surfaces only)
Not yet measured. Lesson to carry to the project file at close: Tailwind classes built from template literals (`${bp}:hidden`) never generate — use a literal map (caught by `tsc` only because the JSX also broke).

## Needs the owner
Nothing this session.
