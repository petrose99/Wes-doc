# Autopilot report — #257 Build the phone approval lane

- Map: #226 Post-login UX
- Type: wayfinder:task (execution) · Started: 2026-09-16 (session 6, build phase) · Finished: 2026-09-16
- Outcome: partial — **build phase complete (`milestone: build-done`)**; the close phase (capture, scoring, fix batch, close) is the next session's, in a fresh context per the phase rule

## What I did
1. Read the hand-off (sessions 1–5); loaded `intent` (specify) and `impeccable` (polish) and read `craft-floor.md`; the `impeccable` launcher (`context`, `detect`) needs an approval this session did not have, so project context was read directly from the spec.
2. `lib/approvals/filters.ts` — the one facet predicate shared by both pages (segment counts), both surfaces (`QueueScreen.filterRows`, own-segment count via `useSearchParams`) and the empty state's "N waiting on other approvers".
3. `components/queue/decision-result.tsx` — shared `DecisionResultStrip` primitive + the single `OFFLINE_REASON` / `NETWORK_ERROR` sentences (B3/B4).
4. `QueueScreen.onOpenChange` (reported once per open-row change) so a surface drops its result strip when another row opens.
5. Rewrote both surfaces to the spec: decision exits (success / server-refused → strip + `router.refresh()`; network throw → sheet stays open with the reason, bar `role="alert"`), `pinned` decided row, `initialMissingNotice`, `cards.title="Ready to Approve"`, empty/filtered copy per spec 3.6 with links, copy-matrix sort labels, Send back disabled below `lg` with the desktop hint, `ApproveCommentDialog` rebuilt (labelled textarea, `initialFocus`, 16px/48px on phone, inline error, offline `disabledReason`, "Approving…", next-stage description). Both pages now hand over all rows.
6. Tests: `lib/approvals/filters.test.ts`, `lib/queue/filters.test.ts` (badge/sum test already existed in `models/approvals.test.ts`) — 25 tests green.
7. Part B contract checks as one script (`logs/scratch-257/partb-check.mjs`): 36/36 pass. `eslint` on the touched files clean (fixed one ref-in-render error and two `Date.now` purity warnings it raised). `tsc --noEmit` whole repo: 0 errors.
8. Hand-off updated with `milestone: build-done`, the close-phase recipe and the known gaps; committed as `wip(autopilot): #257 build`.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Show the desktop toast as well as the result strip? | Toast only when not in the phone lane; strip at every width | Spec 3.7 "Toasts: none new — desktop keeps the toast" |
| 2 | Own-segment count from all rows or the filtered set? | Filtered by the same predicate as the other segment | Hand-off session 4 decision: counts stay comparable across Me/Anyone |
| 3 | PO Mismatches empty body | Spec's sentence + one line on how a mismatch appears | Spec 3.6 copy, B3 one term ("Nothing needs your approval" on both segments) |

## Decisions recorded
No resolution comment yet — the ticket closes in the close phase. Decisions to carry: "Audit" tab label kept over spec's "History" (B3, #236/#259); "No approval flow yet" state falls through to "Nothing needs your approval" until a `hasWorkflow` flag is judged from the capture.

## Tickets created / changed
None this session.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=34 first-critique=n/a close-critique=n/a first-evaluate=n/a close-evaluate=n/a
No capture this phase (build phase; measuring is the close session's). Detector: hook did not fire (edits applied through `.mjs` scripts) and the static launcher was not runnable — the close phase's in-page round is the first measurement.

## First-pass findings → lessons (rendered surfaces only)
Not applicable until the first measurement. Lesson to carry into the project file at close: edits applied by script bypass the design detector hook — run the static scan explicitly afterwards (or apply UI edits with Edit so the hook fires).

## Needs the owner
- Removal sign-off (#237): `components/typed-destinations/approvals-queue-picker.tsx` (`ApprovalsQueuePicker`) is now unused after `QueueSegments` replaced it on both surfaces.
- `impeccable` launcher approval for autopilot sessions (`context`/`detect` were refused this session) — a `settings.local.json` allow rule, as with the autopilot runner.
