# Autopilot report — #257 Build the phone approval lane: Approvals-first tab bar, Ready to Approve cards, full-screen Detail sheet with the decision bar

- Map: #226 Post-login UX
- Type: wayfinder:task (execution) · Started: 2026-09-16 (session 4, build phase) · Finished: 2026-09-16
- Outcome: partial — build phase continued, not done; handed off at the context line

## What I did
1. Claimed the ticket (assignee petrose99), read the hand-off, spec §3.3–§7, project + generic lessons, craft-floor.md, and the code the remaining build touches (queue-screen, both queue components, detail-pane, history-tabs, dialog, reason-dialog, facet-filters, split-pane, models/approvals, actions).
2. Built: `lib/client/use-phone-lane.ts`, `lib/queue/filters.ts` (`activeFilterCount` — the one function the Filter button and sheet share), `Dialog.initialFocus`, `ReasonDialog` `disabledReason`/`pendingLabel`/16px textarea/48px halves, and the new `components/queue/filter-sheet.tsx` (`FilterSheet` + `FilterButton`).
3. Hit the 150K hand-off line (the system prompt plus the necessary reads consumed it before wiring began). Wrote the full wiring plan into the hand-off so the next session codes from it without re-reading the codebase; committed WIP.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Tab label "History" (spec) vs the existing "Audit" | Keep "Audit" at every width | #236/#259 already named it; B3 one term per concept; map decisions win over the spec's later copy |
| 2 | Where the segments render relative to the title | After row 1 when `cards` is set (title → segments → Filter → list); Bill Pay's band unchanged | Spec 3.3 order; smallest reversible change |
| 3 | Deep link to a decided row | A `role="status"` notice above the list rather than opening the sheet | The model cannot load a decided row without a schema-level change; smallest option, recorded as a gap |
| 4 | Component tests named in spec §6 | Pure parts as `lib/**` tests; component tests recorded as not runnable | vitest config includes only lib/models/…, environment node |

## Decisions recorded
None closed — ticket stays open; decisions above are in the hand-off for the close phase to record.

## Tickets created / changed
None.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=n/a first-critique=n/a close-critique=n/a first-evaluate=n/a close-evaluate=n/a
No capture this phase (build phase; measuring is the close phase's).

## First-pass findings → lessons (rendered surfaces only)
Not yet measured. One build-time lesson for the project file (to be written at close): the shared `Dialog` focused its header × first — the earlier hand-off's "textarea is first-focusable by construction" was an unchecked assumption; B5 focus rows need a selector check, not a reading of the DOM order.

## Needs the owner
Nothing new. `ApprovalsQueuePicker` is now unused (replaced by `QueueSegments`) — removal pending owner sign-off per the removal protocol.
