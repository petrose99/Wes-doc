# Autopilot report — #257 Build the phone approval lane

- Map: #226 Post-login UX — phone lane and Approvals
- Type: wayfinder:task (execution) · Started: 2026-09-16 ~12:30 UTC · Finished: 2026-09-16 ~12:50 UTC
- Outcome: partial (build phase, continuation; hand-off hook fired at the context line early in the session)

## What I did
1. Read the hand-off (`docs/wayfinder-reports/226/257.handoff.md`), spec §3.3–§7 and pre-flight; read the code the remaining build touches (`queue-screen.tsx`, both Approval queue components, `detail-pane.tsx`, `history-tabs.tsx`, `split-pane.tsx` tab region, `dialog.tsx`, `reason-dialog-button.tsx`, `facet-filters.tsx`, the approvals page, `models/approvals.ts`, the history loader in `actions.ts`, the ReviewTask/Supplier/Gate schema).
2. S4: removed the Admin layout's below-`md` company-name caption (`app/(app)/workspaces/[workspaceId]/admin/layout.tsx`).
3. S7 data layer: `models/approvals.ts` — `ApprovalDetailFacts` + `getApprovalDetailFacts()` (approval start/waiting facts, supplier facts, near duplicate, PO mismatch figures); `ApprovalInvoiceRow.nextStageName`.
4. The HAND-OFF NOW hook fired after the next tool call; typechecked the two edited files (clean), updated the hand-off with the session's additions and the design decisions for the remaining build, committed, posted `Autopilot: continue —`.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Is `AdminPage`'s `PhoneNote` the duplicate caption S4 removes? | No — the caption was the `md:hidden` company-name `<p>` in the Admin layout; `PhoneNote` is the "Admin is a desktop area" note and stays. | Spec S4 text names the layout's below-`md` caption; removal protocol (no feature removal without sign-off). |
| 2 | Filter rows server-side (today) or client-side (spec 3.4 "Show n rows")? | Client-side via a `QueueScreen.filterRows` predicate, pages pass all rows. | Spec 3.4 / critic finding 12; smallest reversible change. |
| 3 | Result strip below `lg` only, or every width? | Every width, desktop keeps its toast too. | B4 one code path; recorded as a documented deviation for the close. |

## Decisions recorded
None closed; ticket stays open (build phase not done). Decisions above are in the hand-off's "Design decisions taken this session".

## Tickets created / changed
None.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=n/a first-critique=n/a close-critique=n/a first-evaluate=n/a close-evaluate=n/a
No capture this phase (build); measurement is the close session's.

## First-pass findings → lessons (rendered surfaces only)
Not applicable yet (no measurement).

## Needs the owner
Nothing new. `ApprovalsQueuePicker` (`components/typed-destinations/approvals-queue-picker.tsx`) is now unused and awaits owner sign-off before deletion (noted in the previous session's hand-off).
