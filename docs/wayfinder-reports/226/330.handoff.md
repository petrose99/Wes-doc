# Hand-off — #330 Swap the module-pill toggle to the shared Switch primitive

milestone: build-done

## What changed
`components/workspace/module-row.tsx`: owner-only module toggle swapped
from a bespoke `role=switch` pill button to `components/ui/switch.tsx`
(`<Switch checked={checked} onCheckedChange={toggle} label={name}
busy={pending} />`). `toggle()` now takes the next boolean as a param
instead of deriving it from closure state. Committed 78b0adc.

## Artifacts
- Scratch: docs/wayfinder-reports/226/logs/scratch-330/
- Capture (before = pill, after = Switch), owner + member (Northwind
  Traders, non-owner) × 1440/390: `before/`, `after/` (PNGs +
  `detector.raw.json` + gate-shaped `detector.json`)
- Gate: `gate-r1.json` — CLEAN, 0 real findings, 0 new vs baseline
- Contact sheet: `sheet.png`
- Readers dispatched (foreground sonnet agents) writing full output to
  `readers-r1/{critique,evaluate,include}.md` — in flight as of this
  write.

## Open findings
None triaged yet — waiting on the three readers' scores.

## Next step
Once the three reader agents return, record the `scores:` line, write
the report at docs/wayfinder-reports/226/330.md, post the resolution
comment on #330, close it, and update map #226's Decisions so far.
