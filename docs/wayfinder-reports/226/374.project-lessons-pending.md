# Lessons pending — append to `.claude/wayfinder-autopilot/lessons.md`

Sandbox refuses editing `.claude/wayfinder-autopilot/lessons.md` directly (same as #342);
append these two entries from a session with write access.

- (#374) `action-helpers.ts`'s generic error-code fallback (`replaceAll("_", " ")`) turns any
  model-level error code without a `BILLING_MESSAGES` entry into a system-oriented pseudo-sentence
  ("phone number invalid") instead of a real one — a model adding a new error code
  (`models/inbound-whatsapp.ts`'s `phone_number_invalid`/`label_required`) surfaced this way
  because nothing forced the addition through the message map. → Any new error code a model can
  throw gets its own `BILLING_MESSAGES` entry in the same change; the generic fallback is a
  last-resort, not a copy source. · check: `grep -n '"<code>"'` for every string literal a new
  model throws/returns as an error code appears in `action-helpers.ts`'s `BILLING_MESSAGES`.
- (#374) The r1 critique reader scored on evaluate's descending 0-100-minus-sum scale instead of
  critique's ascending 0-4 scale (`critique.md:392`), producing an internally inconsistent
  9/40 alongside uniformly positive prose — caught only by re-reading the skill file against the
  reader's own notes. → State the scale explicitly and its direction (ascending, 4=excellent) in
  the critique reader's prompt every round, not just once at spec. · check: the critique reader
  prompt includes the literal phrase "ascending 0-4, 4 = excellent" before each run.
