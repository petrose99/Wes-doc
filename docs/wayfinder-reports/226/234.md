# Autopilot report — #234 Decide the Detail pane's header stack (pane header + embedded document header + stepper) and where Delete lives

- Map: #226 Map: Make the operator app work like Vic.ai (grammar, not skin)
- Type: wayfinder:grilling · Started: 2026-09-15 23:50 UTC · Finished: 2026-09-16 00:20 UTC
- Outcome: resolved

## What I did

1. Loaded map #226 (Notes, Decisions so far) and #234 (unblocked by #233, unassigned, blocking #258); claimed it (assignee petrose99).
2. Called `grilling`, `domain-modeling`, `intent` (wireframe/journey) and `impeccable` (distill → layout → polish). `impeccable context` was denied by the driver's allowlist (the launcher path is not on it, see memory note); read `PRODUCT.md`/`CONTEXT.md` directly and the `distill` reference. `craft-floor.md` not needed — planning only, no UI edit.
3. Inspected the incumbent: `components/queue/detail-pane.tsx`, `components/pipeline/document-detail/split-pane.tsx`, `stage-indicator.tsx`, `delete-document-button.tsx`, `document-actions.tsx`, the `?full=1` deep-link route; the incumbent critique `.impeccable/critique/2026-09-15T19-30-56Z__…invoices…md` (25/40, H3 = 3, H8 = 2, P1 "three stacked headers and Delete above Approve"); the tour §6 and `invoices-row-open-split.png` / `payments-batch-detail-sheet.png`.
4. Took one live measurement on the running dev server (Playwright from `/tmp/scratch229`, `shot234.mjs`): at 1440×900 the pane stacks 53 + 51 + 39 px on top of the 48px queue header (192px before the source; first field y=375); at 390, 143px before the source (first field y=646). Screenshots `/tmp/scratch229/234-pane-1440.png`, `-390.png` (not committed).
5. Looked up facts rather than asking: Delete already exists on the bulk bar; the Flag has no consumer on any queue; `/review*` 308-redirects so the header's review-task link is dead; `?full=1` is the standalone header's only caller.
6. Ran three grilling rounds (17 questions), answering each with its `➡️`.
7. Sharpened **Detail pane** in `CONTEXT.md` (one header, stepper beneath, actions in ⋯, full-width same pane; _Avoid_ "document header").
8. Created [#259](https://github.com/petrose99/Wes-doc/issues/259) (execution, task) and [#260](https://github.com/petrose99/Wes-doc/issues/260) (owner sign-off: unplug the Flag); parented both to #226; wired #259 blocked by #234, #260 blocked by #259, #258 blocked by #259.
9. Posted the [resolution comment](https://github.com/petrose99/Wes-doc/issues/234#issuecomment-5689805715), closed #234, appended it to the map's Decisions so far, added one fog line (type chooser on typed queues).

## Questions I answered on the owner's behalf

| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Fold the embedded document header into the pane header? | Yes — one header: row name · 1 of N · ↑↓ · ⋯ · × over the Status line; filename to the source strip | Vic's source pane has one toolbar and no filename band (§6); critique H8 = 2; #233 Status line |
| 2 | Stepper: single second band, and where? | Full-width band under the header; one line (current node + n of N) below `lg` | #233 kept the stepper; Status line = what, stepper = where incl. Sync/Pay; "Pay node clipped at 390" |
| 3 | Where does Delete live? | ⋯ last item, red, behind confirm; bulk-bar Delete stays | Vic: Delete on the bulk bar; Intent *Destructive Defaults*; critique H3 "two ⋯ menus split one job" |
| 4 | Archive and Flag? | Both to ⋯; Flag is a removal candidate pending owner sign-off (#260) | #239 stay-on-row, #227 no optimistic removal; Flag has no queue reader; #237 removal protocol |
| 5 | Type chip, payment pill, review-task link? | Chip retires (queue is typed); ledger mark follows Status line; link folds into Approval tab | #233 d.3; `/review*` is a 308; distill: say it once |
| 6 | Split/details/source toggle? | Moves to the source strip, three states kept | Vic's toolbar owns view controls; no removal |
| 7 | What does `?full=1` keep? | Same DetailPane frame at full width, Back for ×, no ↑/↓ | "Built once"; full width still gives ~750px source |
| 8 | Phone header stack? | Title + Status line; one-line stepper; ≤ 100px above the source strip | #232 phone pane; measured 143px today |
| 9 | Hierarchy/height budget | 14/600 title, 13 status, 12 stepper; ≤ 96px pane chrome | Measured boxes; Vic 48px toolbar |
| 10 | Colour in the header | Only the state pill; red only on the Delete item | Craft floor: colour carries meaning; emerald Approve must win |
| 11 | Motion | None added | Focus landing on `h2` is the feedback (#225) |
| 12 | ⋯ copy and order | Open in a new tab · Archive · Flag ⇄ Remove flag · ── · Cancel invoice… · Delete… | `articulate`: ellipsis = dialog; disabled hint = reason |
| 13 | State inventory | Missing source, cancelled row, loading, long filename, deleted on `?full=1` | `fortify`; no states beyond #233's |
| 14 | Accessibility | Focus order unchanged; named strip buttons; `include` + `audit` at close | Delete leaves the tab sequence |
| 15 | Success | H3 ≥ 3, H8 ≥ 3, chrome ≤ 96px, no pane detector findings, evaluate ≥ 80 | CLAUDE.md closing bar |
| 16 | Every queue's pane? | Yes | One `DetailPane`; Exceptions critique flagged the same Delete |
| 17 | Sequencing with #258 | #259 first, #258 lands into it | "Built once" (#233 → #234 edge) |

## Decisions recorded

One pane header (row name + controls over the Status line); stepper the single band beneath (one line on the phone); filename and layout toggle on a source strip; Delete/Archive/Flag leave the header for the ⋯ with Delete last and red, bulk-bar Delete kept; type chip and review-task link retire, ledger mark follows the Status line; `?full=1` renders the same frame full-width; ≤ 96px of pane chrome; the Flag's removal awaits the owner. Action Summary: Intent `specify` → `fortify` → `include`; Impeccable `distill` → `layout` → `polish`, `audit`.

## Tickets created / changed

- #259 Build the folded Detail pane header… (task) — child of #226, blocked by #234; now blocks #258 and #260.
- #260 Owner sign-off: unplug the document Flag (task, HITL) — child of #226, blocked by #259.
- #258 gained a blocking edge from #259.
- Map #226: Decisions so far +1; Not yet specified + the document-type chooser on typed queues.
- #234 closed.

## Scores and counts (rendered surfaces only)

No score; surface not built yet (grilling). Incumbent evidence cited: critique 25/40 (H3 = 3, H8 = 2); live-measured chrome 192px at 1440 / 143px at 390 above the source. Close bar set on #259.

## Needs the owner

- #260: sign off (or decline) unplugging the Flag.
- `impeccable context` is denied under the driver's allowlist; the resolution proceeded on the read context. Fix the allowlist path or accept the workaround in the memory note.
