# Autopilot report — #273 Build expense claims in place

- Map: #226 Post-login UX
- Type: wayfinder:task (execution) · Started: 2026-09-17 (session 7, BUILD phase) · Finished: 2026-09-17
- Outcome: partial — build continues; hand-off at the 150K context line

## What I did
1. Read the hand-off (`273.handoff.md`), spec §1/§2/§6/§7, the Approvals area primer, the project lessons, and the incumbent `approval-invoice-queue.tsx` / its page / `queue-segments.tsx` / `lib/approvals/filters.ts`.
2. Built **S4**: `components/queue/expense-claim-queue.tsx` (`ExpenseClaimQueue` + `ExpenseClaimDetail` + `ClaimEvents`), routes `(queue)/approvals/expense-claims/page.tsx` and `approvals/expense-claims/[claimId]/page.tsx`.
3. Built **S5**: `QueueSegments.shortLabel`; third segment (gated on the `expense-approvals` module) in the Invoices and PO-mismatch queues and their pages; origin labels for the new route.
4. `tsc --noEmit` clean on the whole tree.
5. Wrote `logs/scratch-273/db-recon.ts` (not yet run) for the seed step; hand-off updated with the exact next steps and the known deviations from spec.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | S4 pane source strip needs a `SourceDocument` the claim facts don't carry — add server work now or ship the pane without it? | Ship without; record as deviation for measure/close | Smallest reversible option; the decision bar and claim card are the pane's contract (spec §6.3/6.4), the strip is a convenience |
| 2 | `/[claimId]` to a decided claim: read-only pane (spec) vs notice | Notice via `queueArrival.missingText` naming the outcome and date | `QueueScreen` opens panes only on rows; the notice keeps the sentence the spec asked for ("Approved ‹date› by…" reduced to outcome + date). Recorded as a gap |

## Decisions recorded
None on the ticket this session (build phase).

## Tickets created / changed
None.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=33 first-critique=n/a close-critique=n/a first-evaluate=n/a close-evaluate=n/a
No capture this session (no r0 yet); seed and round script are the next session's first steps.

## First-pass findings → lessons (rendered surfaces only)
Not yet measured.

## Needs the owner
- Carried from the hand-off: owner deciding their own claim; Reopen-approved-claim ticket at close; #274 legacy removal.
