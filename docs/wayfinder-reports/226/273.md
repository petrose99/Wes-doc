# Autopilot report — #273 Build expense claims in place: Add to claim on Receipts, the claim on the Approval tab, an Expense claims view on Approvals

- Map: #226 Vic.ai-fidelity operator app (surface-by-surface)
- Type: wayfinder:task · Started: 2026-09-17 (session 1, SPEC phase) · Finished: 2026-09-17 (hand-off at the context line)
- Outcome: partial — spec written; pre-flight and spec critic pending; ticket left open with `Autopilot: continue —`

## What I did
1. Claimed #273; read the ticket, #247's resolution, the map Notes and Decisions, the three area primers, both lessons files, the pre-flight template, `PRODUCT.md`, `craft-floor.md`, `operate.md`.
2. Loaded `intent` (specify) and `impeccable` (shape); `impeccable context` skipped per the project lesson (interactive approval).
3. Recon of the incumbent code with three one-shot scripts (`logs/scratch-273/recon*.mjs`) instead of file-by-file reads.
4. Wrote the spec: `docs/wayfinder-reports/226/logs/scratch-273/spec.md` (surfaces, data contracts with single shared functions, every state, every confirm's consequence copy, focus contracts, phone rules, closures, pending removals).
5. Hit the 150K hand-off line (skill loads + primers + lessons ≈ 120K before any spec work); wrote `273.handoff.md`, committed, posted the continue comment.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Three segments at 390: keep "Expense claims" or the ticket's "Claims (n)"? | `shortLabel` below `md` ("Invoices · PO mismatches · Claims"), full label ≥md and in `aria-label` | Ticket/#247 d13 names "Claims (n)" for the phone; one control at every width (#257 B4); one term per concept kept via the accessible name |
| 2 | `receipts?mode=claims` bookmark: ignore the param or 404? | In-shell `notFound()` for both it and `/expenses` | #247 d14 literal; removal protocol keeps the components unreferenced |
| 3 | Rejection reason storage without a schema change? | Audit event `detail.reason`, read back for the Approval tab | Smallest reversible; #227 decision 10 (reason required) honoured |
| 4 | Remove-this-receipt confirm? | No confirm unless it is the last receipt (then the draft is deleted — confirmed with that consequence) | Autonomy/easy reversal; B1 consequence rule |
| 5 | Owner deciding their own claim | Allowed (no new rule); flagged for the owner | #247 d6 "any owner decides"; #278 precedent for Payments |

## Decisions recorded
None closed — the ticket stays open; spec decisions are in `spec.md` and will be reconciled by the Part D critic next session.

## Tickets created / changed
None. #273 assigned; `Autopilot: continue —` comment posted.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=n/a first-critique=n/a close-critique=n/a first-evaluate=n/a close-evaluate=n/a
No surface built yet (spec phase).

## First-pass findings → lessons (rendered surfaces only)
None yet.

## Needs the owner
- #274 sign-off on the removals (`ExpenseClaimsPage`, `ExpenseClaimForm`, `ExpenseClaimRow`, workflow picker, module nav entry).
- Whether an owner may decide their own expense claim when no flow applies (spec §0 assumes yes, per #247 d6).
- Process: this session's first-turn context was ~120K before the spec (two full skill loads + three primers + both lessons files); the spec phase for a three-surface ticket does not fit in one session at that overhead.
