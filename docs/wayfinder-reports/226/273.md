# Autopilot report — #273 Build expense claims in place

- Map: #226 Post-login UX
- Type: wayfinder:task (execution, BUILD phase, session 8 — continuation) · Started: 2026-09-17T01:50Z · Finished: 2026-09-17T02:20Z
- Outcome: partial (hand-off at the context line; ticket left open)

## What I did
1. Read the hand-off (`273.handoff.md`, milestone build-partial), the Approvals area primer and the project lessons.
2. DB recon (`logs/scratch-273/db-recon2.ts`): dev workspace had no `expense_receipt` documents, no claims, `expense-approvals` module off; members Dev User (owner), Priya, Tomás.
3. Wrote and ran the seed `logs/scratch-273/seed273.ts` — it exposed two server bugs in the earlier session's model layer, fixed in `models/expense-claims.ts`: `classifyClaimableDocuments` selected Document columns that do not exist (`approvalStatus`, `blockedByCheck`, `touchless` — runtime Prisma error on every add/create), now derived from ReviewTask/check results exactly as `models/receipts.ts` does; `asNumber` rejected the string totals extraction stores, so every submit refused with `expense_claim_no_amounts`.
4. Affected tests: fixtures updated (`reviewTask` mock, `claim.createdAt`, new `claimFilter` vocabulary) → `models/expense-claims.test.ts`, `models/receipts.test.ts`, `lib/claims`, `lib/approvals` 70/70.
5. Part B checks `logs/scratch-273/part-b-checks.mjs` → 0 FAIL after routing S4's decision words through `CLAIM_STATUS_LABELS`; justified exclusions recorded in the script.
6. Servers up, routes warmed (`warm.mjs`, all 200) — HAND-OFF NOW fired; servers stopped, hand-off rewritten with the exact round-script plan and seed ids.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Should `needs_attention` for claims mirror the Receipts list derivation or keep a bespoke read? | Mirror `receipts.ts` (open `check_failed` task / escalated check) | Lesson #257 B1: row eligibility and server re-check must share one definition |
| 2 | Legacy `addExpenseClaimItemsAction` unreferenced — remove? | Leave; excluded from the reachability check, pointed at #274 | Removal needs owner sign-off (#237) |

## Decisions recorded
None on the ticket this session (build phase).

## Tickets created / changed
None. `Autopilot: continue —` comment posted on #273.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=33 first-critique=n/a close-critique=n/a first-evaluate=n/a close-evaluate=n/a
No capture round yet (r0 is the next session's first step). Detector counts: none yet.

## First-pass findings → lessons (rendered surfaces only)
Not yet measured. Two pre-measurement lessons for the project file (written at close): (a) `Document` has no `approvalStatus`/`blockedByCheck` columns — a model that needs them derives them like `receipts.ts`; `tsc` did not catch the bad `select` because the extended Prisma client type is loose, so a seed/run is the check. (b) Extracted totals are strings; any new `asNumber` must parse them (copy `receipts.ts`'s).

## Needs the owner
- Owner deciding their own no-flow claim: build labels it, does not block.
- Reopen an approved claim → Wayfinder ticket at close. Legacy claims components removal → #274.
