# Autopilot report — #273 Build expense claims in place

- Map: #226 Make the operator app work like Vic.ai (grammar, not skin)
- Type: wayfinder:task (execution) · Started: 2026-09-17 (session 10, BUILD phase, continuation) · Finished: 2026-09-17
- Outcome: partial — BUILD phase done (`milestone: build-done`); MEASURE is the next session

## What I did
1. Read the hand-off (`273.handoff.md`, milestone `build-partial`); resumed at its "Next step" — no re-spec, no rebuild.
2. Started the dev server + detector live server, warmed the routes, ran the full round `shots-r0 --decide` (39 states × 1440/390, detector JSON, keyboard probes) and `triage.mjs`.
3. Every keyboard probe passed (the Approve-confirm Esc-return fix from session 9 is confirmed live at both widths; bulk-add no longer errors; no occlusions, no low-contrast, no segment overflow at 390).
4. One real finding left in our code: the S2 Add-to-claim dialog body had no inset (form content flush to the dialog edges). Fixed (`px-5 py-4`, matching every other `Dialog` caller); re-captured the four `bulk-add-*` states (`shots-r0c`) and folded them into `shots-r0`.
5. Classified the remainder as named residue (app-wide five, `ItemizedRecapTable` nested-card/`max-h-64`, truncated-subtitle overflow, shell notice line-length) — written to the hand-off for the measure session.
6. Re-seeded after the `--decide` run; built the contact sheet `shots-r0/sheet.png`; stopped servers and Chromium; `tsc --noEmit` clean (two scratch scripts patched); affected tests 70/70.
7. Hand-off rewritten with `milestone: build-done`, pointers and r0 counts; committed `wip(autopilot): #273 build`; `Autopilot: continue —` posted on the ticket.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Are the `nested-cards`/`cramped-padding "max-h-64"` findings in the S2 dialog ours? | No — leave as residue | They come from the shared `ItemizedRecapTable` (`components/typed-destinations/bulk-approve-receipt.tsx`), used unchanged by Payments/Approvals confirms; shell-primitive reuse (Part B) beats a local fork |
| 2 | Is the 100px/47px `text-overflow` on claim rows at 390 real? | No — residue | Truncated card subtitle with correct ellipsis (checked `claims-anyone-390.png`); same pattern on Invoices/PO queues |

## Decisions recorded
None new this session (build phase). Deviations from spec to decide at close are listed on the hand-off.

## Tickets created / changed
None. #273 stays open with an `Autopilot: continue —` comment.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=33 first-critique=n/a close-critique=n/a first-evaluate=n/a close-evaluate=n/a
Build-gate round r0 (not a scored round): 1440 = 20 states / 115 findings / 59 after residue filter → 0 ours; 390 = 19 / 101 / 46 → 0 ours. Keyboard probes: all PASS at both widths. Readers run in the measure session.

## First-pass findings → lessons (rendered surfaces only)
Deferred to the measure/close sessions (no readers ran). One build-gate observation worth a project lesson at close: the shell `Dialog` does not pad its body — every caller adds `px-5 py-4`; a new caller that forgets it renders flush and the detector flags it as `cramped-padding`.

## Needs the owner
- May an owner decide their own claim with no workflow? (build labels, does not block). Legacy claims removal (#274) pending sign-off.
