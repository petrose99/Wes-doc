# Autopilot report — #273 Build expense claims in place

- Map: #226 Post-login UX implementation
- Type: wayfinder:task (execution) · Started: 2026-09-17 (session 4, phase BUILD) · Finished: 2026-09-17
- Outcome: partial — handed off at the context line before any code was written this session

## What I did
1. Read the hand-off (`milestone: build-partial`), `spec.md`, `preflight.md`, the three area primers and the project lessons file.
2. Code recon of every seam the build needs (model, actions, receipt queue, invoice approval queue, history tabs, detail-pane reload focus, dialog/confirm primitives, engine, schema). The hook reported context at 158K (past the 150K line) before the first edit.
3. Condensed the recon into the hand-off's "Build plan" (file paths, line numbers, signatures, decisions on where `claim` facts live) so session 5 writes code from its first turn.
4. Committed the hand-off + this report; posted `Autopilot: continue —` on the ticket.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Where do claim facts ride into the pane — `facts.claim` (spec §2) or a top-level `DocumentHistory.claim`? | Top-level `DocumentHistory.claim` + `claimsEnabled` | `facts` is typed `ApprovalDetailFacts` (models/approvals.ts); smallest reversible change, same loader (`getSelectionAuditPanelDataAction`). |

## Decisions recorded
None new; ticket still open.

## Tickets created / changed
None. #273 stays open with an `Autopilot: continue —` comment.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=33 first-critique=- close-critique=- first-evaluate=- close-evaluate=-
No surface built yet; no capture.

## First-pass findings → lessons (rendered surfaces only)
Not applicable yet. Process lesson for the generic file at close: a BUILD continuation that reads primers + spec + pre-flight + the seams' full files spends ~150K before the first edit — the hand-off must carry the seam facts (it now does).

## Needs the owner
- Self-approval of an owner's own claim (labelled, not blocked).
- Reopen an approved claim → ticket at close.
- Legacy claim components removal: #274.
