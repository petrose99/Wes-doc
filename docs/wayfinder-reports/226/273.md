# Autopilot report — #273 Build expense claims in place: Add to claim on Receipts, the claim on the Approval tab, an Expense claims view on Approvals

- Map: #226 Multi-page marketing site redesign + SEO / distilled app map
- Type: wayfinder:task (execution) · Started: 2026-09-17 (session 12, close phase) · Finished: 2026-09-17
- Outcome: resolved

## What I did
1. Read the hand-off file (`273.handoff.md`) — sessions 1–11 had completed spec, build, and measure phases; measure found critique 37/40, evaluate health 84 (0 real P0/P1 after investigating two reader flags as dev-tooling/probe artifacts), include PASS, detector 59/46 (all named app-wide residue). Triage recommended no fix batch, straight to close checks.
2. Re-confirmed the triage read (git tree clean, no uncommitted UI changes since session 10/11) and proceeded straight to close checks per the hand-off's own recommendation.
3. Ran affected tests (70/70 pass), then the full suite — found a real regression: `models/approvals.test.ts` failed 3 tests because #273's server layer (commit `b4f5a18`) had wired `countReadyToApprove` through the new `getWorkspaceCapabilities` capability gate without updating that test's bare `prisma` mock.
4. Fixed the test mock (`db.workspace`, `db.workspaceModule`, `db.expenseClaim`) and an eslint `no-unused-vars` finding in `models/expense-claims.ts` (`addCents`/`fromCents`/`RESOLVED_STATUSES`, both unused). Commit `ccb88a2`.
5. Re-ran affected tests, full suite (282/282 files, 2754/2754 tests), `tsc --noEmit` (clean), `eslint .` (clean on all files #273 touched — remaining errors are pre-existing, in files unrelated to this ticket), `next build` (succeeds), with dev servers stopped throughout.
6. Refreshed `docs/agents/areas/approvals.md` with an Expense claims section (files, the capability-gate test-mock gotcha, routes, breakpoint, legacy-removal pointer).
7. Merged a project lesson into the existing `(#271)` entry in `.claude/wayfinder-autopilot/lessons.md`.
8. Posted the resolution comment, closed #273, updated map #226's Decisions so far.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Is a UI fix batch needed before close? | No — re-confirmed session 11's finding that both flagged P1/P2 were dev-tooling/probe artifacts, verified against source, not product defects. | Hand-off's own triage (session 11), re-verified: nextjs-portal is a dev-only overlay absent in production; dialog-focus flag was a probe-labeling artifact matching spec exactly at `add-to-claim-dialog.tsx:112`. |
| 2 | Does the close-phase test regression count as a fix batch or a close-checks fix? | Treated as a close-checks fix (not a UI re-capture trigger) — it's a server/test-only change, no rendered surface changed. | craft-floor / bar rules: fix batch is for P1s and heuristics-under-3 found by the capture readers; this was a full-suite failure outside that scope, fixed as part of "the closing checks", no re-measure needed since no UI code changed. |
| 3 | Chase the pre-existing eslint errors in unrelated files (`lib/gates/*`, `prisma/seed.ts`, etc.)? | No — confirmed via `git log` that those files predate #273's commits entirely; out of this ticket's scope. | CLAUDE.md: don't refactor beyond what the task requires; scope match to what was requested. |

## Decisions recorded
Resolution comment on #273: build shipped, close checks pass (tests/tsc/eslint/build), scores hold at critique 37/40, evaluate health 84 (0 real P0/P1), include PASS, detector 59/46 residue-only; one real regression (approvals capability-gate test mock) found and fixed at close.

## Tickets created / changed
None created. Existing tickets referenced, not reopened: #274 (legacy expense-claim code removal, owner sign-off) carries the "Reopen an approved claim" and dead-code-removal follow-ups already logged in the hand-off.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=33 first-critique=37 close-critique=37 first-evaluate=84 close-evaluate=84
in-page detector: first-pass (r0/r1) 59/46 at 1440/390 → close (unchanged, no UI fix batch) 59/46, all named app-wide/shared-component residue, none attributable to #273. include: PASS both widths (confirmed against completed `keyboard.json`, all 9 named behaviors incl. post-approve focus). Pre-build step: spec/pre-flight solid (critic 33/40, 0 P1, Clean) — the gap between predicted-33 and measured-37 critique is upside, not a miss; no heuristic came in under predicted.

## First-pass findings → lessons (rendered surfaces only)
| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status: none · unchecked · wrong · new class | Action taken |
|---|---|---|---|
| evaluate P1: nextjs-portal in tab order between claim row and nav | none (dev-server-only overlay, absent in production) | new class — capture-tooling artifact, not a spec/contract gap | Investigated against source + capture tooling, dismissed as false positive (session 11); no lesson needed, it's a known Next-dev quirk already implicit in "residue" handling |
| evaluate P2: dialog initial focus lands on "unnamed input" | none (probe helper doesn't surface the element's accessible name in its label) | new class — probe-labeling gap, not a build defect | Verified `add-to-claim-dialog.tsx:112` matches spec exactly; dismissed, no code change |
| Full-suite regression: `models/approvals.test.ts` 3 failures from unmocked `getWorkspaceCapabilities` call | the `(#271)` lesson on updating a model's own test after adding a field — didn't extend to "update the *caller's* test when wiring a new cross-model call" | unchecked — the existing lesson was too narrow (same-file update, not caller-file) | Fixed the mock at close; merged an extended lesson into `(#271)`'s entry naming the broader rule (full suite before the fix-batch decision, not just the touched file's own test) |

## Needs the owner
- Reopen-an-approved-claim path: not built, deferred per spec §10 to a future ticket (carried from measure phase, not new).
- Legacy `ExpenseClaimsPage`/`ExpenseClaimForm`/`ExpenseClaimRow`/`addExpenseClaimItemsAction` removal: pending owner sign-off on #274, per the removal protocol (#237) — this session did not touch them.
