# Autopilot report — #273 Build expense claims in place

- Map: #226 Post-login UX
- Type: wayfinder:task (execution) · Started: 2026-09-17 (session 9, BUILD phase) · Finished: 2026-09-17
- Outcome: partial — BUILD continues (hand-off at the context line)

## What I did
1. Read the hand-off; started the dev server, the detector live server, warmed the routes.
2. Wrote `logs/scratch-273/facts.ts` (seed ids → `facts.json`), `round-273.mjs` on `capture-round.mjs` (34 states × 1440/390, B5 keyboard probes), `run-round.mjs`, `run-scratch.mjs`, `triage.mjs`.
3. Ran `shots-r0`: 1440 = 113 findings / 67 non-app-wide; 390 = 93 / 44; 1 errored state. Keyboard: all B5 probes pass except Approve-confirm Esc → body.
4. Fixed in place: `ConfirmDialog` opener capture (Esc now returns to the trigger, probed live); segments wrapping at 390; low-contrast emerald-600 buttons; S2 dialog disabled-reason placement; round script (rail hover-expand swallowed checkbox clicks; wait for pane load before tab clicks).
5. Hit the 150K line; hand-off written, tree committed, `Autopilot: continue —` posted.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Mixed-currency receipts into a *new* claim — hold back or allow? | Allow; Submit is disabled with the reason | spec §98 (mixed draft → Submit disabled with reason) |
| 2 | Shell notice `line-length` at 1440 | Leave; record as shell residue | queue-screen `role=status` is full-width on every queue; not this ticket's surface |

## Decisions recorded
None — ticket still open.

## Tickets created / changed
None this session.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=33 first-critique=- close-critique=- first-evaluate=- close-evaluate=-
r0 (pre-fix) in-page detector: 1440 67 non-app-wide across 17 states · 390 44 across 17 states; clean re-run pending (next session). Include: B5 probes pass (S2 focus/Esc, pane tabs ←/→, segments ←/→, Reject sheet, 390 Back) after the ConfirmDialog fix.

## First-pass findings → lessons (rendered surfaces only)
Deferred to the close phase (measure has not run).

## Needs the owner
- May an owner decide their own claim without a workflow? (labelled, not blocked)
- Legacy claims removal (#274) — removal needs sign-off.
