# Autopilot report — #273 Build expense claims in place: Add to claim on Receipts, the claim on the Approval tab, an Expense claims view on Approvals

- Map: #226 Vic.ai-fidelity operator app (surface-by-surface)
- Type: wayfinder:task · Started: 2026-09-17 (session 1, SPEC phase) · Finished: 2026-09-17T01:20Z (session 2, `milestone: spec-done`)
- Outcome: partial — SPEC phase complete (spec + pre-flight + critic gate met); BUILD, MEASURE and CLOSE phases follow in fresh sessions; ticket stays open

## What I did
Session 1:
1. Claimed #273; read the ticket, #247's resolution, the map Notes and Decisions, the three area primers, both lessons files, the pre-flight template, `PRODUCT.md`, `craft-floor.md`, `operate.md`.
2. Loaded `intent` (specify) and `impeccable` (shape); `impeccable context` skipped per the project lesson (interactive approval).
3. Recon of the incumbent code with three one-shot scripts (`logs/scratch-273/recon*.mjs`).
4. Wrote the spec `logs/scratch-273/spec.md`; hit the 150K line; hand-off + `Autopilot: continue —`.

Session 2 (this one):
5. Read the hand-off, spec, pre-flight template, both lessons files in full; loaded `intent` (specify) and `impeccable` (shape) again; `impeccable context` still needs interactive approval (not run).
6. Filled `logs/scratch-273/preflight.md` Parts A–E from the spec (A: H1 + H5; B1–B6 contracts with grep checks; C by component type; E1 worst-issue, E2 five walkthroughs, E3 sweep).
7. Ran the Part D spec critic (fresh `Agent`, `opus`, foreground) on spec + pre-flight. Before fixes: critique 30/40, evaluate sum 22, **4 P1s** (focus → body after Delete draft; mixed currencies summed; Approve irreversible; missing amounts frozen as 0 silently + non-atomic add), health ≈ 64, verdict "hidden consequence ×2".
8. Applied all ten required spec changes with `logs/scratch-273/fix-spec-after-critic.mjs` (per-currency totals + `currency_mismatch` reason + mixed drafts cannot submit; missing amounts named in the Submit confirm; focus fallback chain after Delete/Remove-last; Reopen → Wayfinder ticket at close; transactional add + submit re-check; S4 pane card `readOnly`; "Your own claim" labelling; one creation breakpoint `md`; `/[claimId]` decided/withdrawn/unknown states; S4 Stage reads `stageLabel`) and fixed the pre-flight's optimistic rows (B5 Delete focus target, B2 Approve row split, B6 currency + double-add rows, C count, E1 H3/H6/H9, E2 Delete walkthrough).
9. Reconciled Part D: critic's after-line **33/40 (H1 4, H5 4, rest 3) · sum 7 · P0 0 · P1 0 · health ≈ 86 · Clean** — gate met.
10. Rewrote `273.handoff.md` with `milestone: spec-done`; committed `wip(autopilot): #273 spec`.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Three segments at 390: keep "Expense claims" or the ticket's "Claims (n)"? | `shortLabel` below `md`, full label ≥md and in `aria-label` | #247 d13; one control at every width (#257 B4) |
| 2 | `receipts?mode=claims` bookmark: ignore the param or 404? | In-shell `notFound()` for it and `/expenses` | #247 d14; removal protocol keeps components unreferenced |
| 3 | Rejection reason storage without a schema change? | Audit event `detail.reason` | Smallest reversible; #227 decision 10 |
| 4 | Remove-this-receipt confirm? | None unless last receipt (then the draft deletion is the named consequence) | Autonomy/easy reversal; B1 rule |
| 5 | Owner deciding their own claim | Allowed, now labelled ("Your own claim", confirm prefix); rule flagged for the owner | #247 d6; critic #7 (hidden consequence) |
| 6 | Mixed currencies in one claim: sum, block, or split? | Block at add (`currency_mismatch` held back) and at submit (mixed draft cannot submit, per-currency totals shown) | Critic P1 (H2/H5); Intent "transparent consequences"; no schema change needed |
| 7 | Receipts without an amount: block submit or freeze as 0? | Freeze as 0 but name it in the card and the Submit confirm; block only when *no* receipt has an amount | #247 d8 (frozen total); hidden-consequence anti-pattern removed by naming it |
| 8 | Reversal for Approve (Reopen)? | Not built; a Wayfinder ticket at close | Real decision (owner-only reopen changes the approval model) — findings with a decision become tickets |
| 9 | Creation breakpoint `md` or `lg`? | `md` everywhere | Critic #4 (one rule, two thresholds = H4 P2); the bulk bar already hides below `md` (shell) |

## Decisions recorded
None closed — ticket open; spec decisions live in `spec.md`/`preflight.md` (Part D reconciliation table).

## Tickets created / changed
None yet. To create at close: **Reopen an approved expense claim** (decision); **Approval notice for expense claims** (follow-up). #273 assigned; hand-off carries `milestone: spec-done`.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=33 first-critique=n/a close-critique=n/a first-evaluate=n/a close-evaluate=n/a
Predicted evaluate health ≈ 86 (sum 7, P1 0, Clean). No surface built yet (spec phase); detector/include counts come with MEASURE.

## First-pass findings → lessons (rendered surfaces only)
Not yet (no first pass). Spec-critic findings that the pre-flight itself should have caught, for Part G later: (a) B5 named a focus target that the post-action state does not render — a *Delete*-class action's return-to must be an element of the *after* state; (b) B6 had no currency row on a money surface; (c) E1 scored an irreversible act 1 where the project mapping says 3. These become lessons only if the first pass confirms them.

## Needs the owner
- #274 sign-off on the removals (`ExpenseClaimsPage`, `ExpenseClaimForm`, `ExpenseClaimRow`, workflow picker, module nav entry).
- Whether an owner may decide their own expense claim when no flow applies (labelled, not blocked, in this build).
- Reopen an approved claim: ticket at close; H3 stays at 3 until decided.
