# Autopilot report — #258 Build the one status vocabulary

- Map: #226 Post-login UX
- Type: wayfinder:task (execution, BUILD phase, session 4 — continuation) · Started: 2026-09-16 16:30 UTC · Finished: 2026-09-16 16:40 UTC
- Outcome: partial — BUILD phase complete (`milestone: build-done`); ticket stays open for the MEASURE phase

## What I did
1. Read the hand-off; `npx tsc --noEmit` on the previous session's edits — clean, no fallout.
2. Wrote the spec §4 tests: `lib/documents/processing-fact.test.ts` (every §1.2 row and degrade step), extended `processing-state.test.ts` (five keys, labels, order, ledger words), `lib/documents/retired-status-words.test.ts` (comment-stripped grep over `components/**` and `app/**`), `components/queue/{status-line,row-cells,history-tabs}.test.tsx` (react-dom/server markup, `vitest.config.ts` now includes `components/queue/**/*.test.tsx`), `models/bills.test.ts` / `receipts.test.ts` for the `statusFilter` keys.
3. Fixes the tests surfaced: report page "Synced untouched" → "Posted untouched"; facet option lists in `invoice-queue.tsx` / `receipt-queue.tsx` now built from `PROCESSING_STATES` + `PROCESSING_STATE_LABELS`; `approval-invoice-queue.tsx` row name uses the map; stale "No approval steps yet" comment corrected.
4. Part B contract checks (`scratch-258/part-b-checks.mjs`): B1 callers, B2 `stay`/`useActionState`/`onMutated` present, B3 clean, B4 `StatePills` non-interactive.
5. Final `tsc --noEmit` clean; 91 tests pass across the affected files. Hand-off rewritten with `milestone: build-done`; committed `wip(autopilot): #258 build`.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Spec writes dates "12 Sep 2026"; the shell primitives (`row-cells.tsx`, `history-tabs.tsx`) format "Sep 12, 2026" | keep the shell's `Intl "en"` format | Part B4 primitive reuse; one date format per surface (B3 one term per concept) |
| 2 | Retired-words grep hits close-period "signed off" (2 files) and the accounting "Synced" toast | allow-list those three files | different concepts (close-item sign-off, account sync) — not document status; spec §5 keeps `stages.ts` out of scope |
| 3 | Receipts `statusFilter: "cancelled"` | accepted, matches nothing | spec §1.2: a Receipt never reaches Cancelled |

## Decisions recorded
None new; ticket still open.

## Tickets created / changed
None.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=32 first-critique=n/a close-critique=n/a first-evaluate=n/a close-evaluate=n/a
Not measured — build phase. Next session runs the capture round.

## First-pass findings → lessons (rendered surfaces only)
n/a — no measurement yet.

## Needs the owner
Nothing.
