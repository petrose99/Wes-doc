# Autopilot report — #258 Build the one status vocabulary

- Map: #226 Post-login UX
- Type: wayfinder:task (execution, BUILD phase, session 3) · Started: 2026-09-16 (see run-log) · Finished: 2026-09-16
- Outcome: partial — build continued to the context hand-off line; steps 6–12 of the build checklist done, tests/tsc not yet run on them

## What I did
1. Read hand-off, spec, pre-flight, Detail pane primer, both lessons files.
2. `tsc --noEmit` on resume: one error (duplicate `PaneDocumentContext` export) — fixed.
3. Built: `models/processing-state.ts` loader; `saveDocumentReviewAction` `stay` path; `FieldNavForm` on `useActionState` with toasts + `onMutated`/`router.refresh()`; decision registration in `SplitPane`; full-mode history + server-rendered `StatusLine`; stepper labels from `PROCESSING_STATE_LABELS`/`LEDGER_FACT_LABELS`; `ApprovalTimeline` no-flow cases without the retired sentence; `queueTitle` threading; `row-signals.tsx` label map deduped; `StatusLine` upgrade through `processingFact`.
4. Hit the 150K hand-off line; wrote `258.handoff.md`, committed WIP, posted `Autopilot: continue —`.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | `stay` flag: `embedded \|\| full` or always? | always `stay: true` from `DocumentDetailPage` | every render is the pane or `?full=1`; `/review` inbox retired (#236); redirect kept in the action for any other caller |
| 2 | Approved doc with no `reviewed` record in the Approval tab | render the row with the DocuBite mark and no date | spec §1.4 "never empty on Approved"; §2.4 partial column |

## Decisions recorded
None new; ticket still open.

## Tickets created / changed
None.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=32 first-critique=n/a close-critique=n/a first-evaluate=n/a close-evaluate=n/a
Not measured — build phase.

## First-pass findings → lessons (rendered surfaces only)
n/a — no measurement yet.

## Needs the owner
Nothing.
