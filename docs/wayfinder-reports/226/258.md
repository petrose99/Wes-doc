# Autopilot report — #258 Build the one status vocabulary: StatePills from processingState(), the pane status line, stepper words, Approval tab from the audit log

- Map: #226 Post-login operator app on the tour's shell
- Type: wayfinder:task (execution) · Started: 2026-09-16 17:20 UTC (close session; ticket spanned seven sessions) · Finished: 2026-09-16 17:50 UTC
- Outcome: resolved

## What I did
1. Read the hand-off (`258.handoff.md`, at `milestone: close-fix-applied`): fix batch and confirm round were already done; the open item was the H10 scope call plus the close checks, primer, Part G, lessons, report.
2. Claimed #258 (was unassigned after the previous session).
3. Close checks with the dev server stopped: full vitest (273 files / 2,682 tests pass), `tsc --noEmit` clean, `eslint` on the ticket's changed files 0 errors (3 warnings from #257/#259 lines; repo-wide 27 pre-existing errors outside the ticket), `next build` succeeds.
4. Verified the remaining 32 non-residue detector findings on the close round against the ticket's diff (`git blame`): none originate from #258.
5. Decided H10 (see table): no new ticket — the help affordance is already decided on #241 and built by #264; posted the score it carries on #264.
6. Refreshed the Detail-pane area primer (`docs/agents/areas/detail-pane.md`): vocabulary source, `#save-review-submit`, reload-focus contract, capture mouse note.
7. Filled `preflight.md` Part G; appended three generic lessons with `check:` lines to `scripts/wayfinder-autopilot/lessons.md`.
8. Posted the [resolution comment](https://github.com/petrose99/Wes-doc/issues/258#issuecomment-5701854061), closed #258, appended one line to map #226's Decisions so far (and compacted #259's entry to one line).
9. Rewrote the hand-off to its final state; committed everything on `codex/post-login-ux-implementation`.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | H10 Help = 2 at close: hold #258 open until a help affordance exists, or close and park? | Close; park H10 on the existing #264 with the score it costs (+1). No new ticket. | CLAUDE.md: the only findings that stay open at close are those with a real decision behind them, on a ticket named at close with the score it costs. That decision was already made on #241 (*How DocuBite works* dialog, execution #264, open and unblocked) — a fresh ticket would duplicate it. H10 is untouched by this ticket's scope (status vocabulary). Evaluate bar (0 P0/P1, health 92, Clean) and critique headline (34 ≥ 32, two 4s) are met. |
| 2 | Are the 32 remaining detector findings "real findings to clear" or residue? | Residue for this ticket. | `git blame` places every one outside #258's diff: #259 pane title (`text-overflow` at 390), #257 avatar initials and pre-existing detail-form controls (`tiny-text`, `low-contrast`), a `display:none` div (`cramped-padding`). Brief: cleared to the named residue. |
| 3 | 27 repo-wide eslint errors — block the close? | No; report as residue. | All in files the ticket never touched (`models/expense-claims.ts`, `models/health.ts`, `prisma/seed.ts`, …); the ticket's own files lint with 0 errors. Smallest reversible option. |

## Decisions recorded
One source for the five Processing-state words (`processingState()`); pills, Status line, stepper, Approval tab and facet chip all read it. Save review gained a stay path that refreshes row + pane + stepper + tab in one pass and re-focuses the button after the remount. First-pass P0 was a capture-tooling false positive (mouse at (0,0) hover-expanding the rail), fixed in the shared runner; the real P1 (focus loss on reload) fixed in `DetailPane`. H10 parked on #264 with its score.

## Tickets created / changed
- #258 closed (resolution comment + close comment).
- #264 — comment: carries H10 = 2 from #258; expected 2 → 3 when *How DocuBite works* ships.
- #226 map — Decisions so far: one line for #258; #259's entry compacted to one line. No fog graduated (nothing newly specifiable); no new tickets.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=32 first-critique=32 close-critique=34 first-evaluate=72 close-evaluate=92

- Critique close 34/40: H1 = 4, H4 = 4, H10 = 2, rest ≥ 3. First pass 32 (H1, H5 = 4, rest 3). Predicted 32.
- Evaluate close: health 92, 0 P0/P1, Clean, two P2 (#259 shell residue). First pass 72 (P0 false / P1 real). Predicted 88.
- In-page detector, non-residue, 36 states (18 × 1440, 18 × 390): first pass 68 → close 32. `text-occlusion` 29 → 0; `text-overflow` 20 → 6 (all #259 pane title at 390); `cramped-padding` 15 → 16 (`display:none` div, pane states at 1440); `tiny-text` 4 → 4 (in-review detail form); `low-contrast` 0 → 6 (in-review detail form, surfaced once occlusion cleared). Per-state numbers in `logs/scratch-258/shots-r{3,6}/detector.json`. None of the 32 is in this ticket's diff.
- Include: keyboard walk sane at both widths (r4); save-review-focus contract PASS at close (`shots-r6/keyboard.json`, `BUTTON`).
- Weak pre-build step: Part B5 — the focus contract was ticked from the code path, not from a probe that exercised the remount.

## First-pass findings → lessons (rendered surfaces only)
| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| Full-mode Status line occluded (H1/H4, P0) — false positive | none; capture tooling, not spec | new class | `capture-round.mjs` parks the mouse before each snap; generic lesson with `check:` |
| Save review focus falls to a wrapper div after reload (H9, P1) | preflight B5 "focus stays on Save review" — existed as prose, ticked without exercising the remount | unchecked | Fixed in `DetailPane` (`pendingReloadFocus`, `#save-review-submit`); generic lesson with `check:` (grep for reload keys on mutation paths + probe asserts `activeElement.tagName`); project lesson pending owner append |
| Loading skeleton captured as 37 findings (round tooling) | none — fixed wait after a mutating click | none → written | Round waits for the toast; generic lesson with `check:` (`waitForTimeout` after a mutating click) |
| No contextual help affordance (H10, critique 2) | Not a #258 spec gap; decided on #241, built by #264 | new class (already ticketed) | Parked on #264 with its score |
| 390 pane-title `text-overflow`, detail-form contrast (H8, P2 ×2) | #259 / pre-existing shell residue | new class (out of scope) | Named as residue on the ticket and in the primer |

## Needs the owner
- Append the four project lessons in `docs/wayfinder-reports/226/258.project-lessons-pending.md` to `.claude/wayfinder-autopilot/lessons.md` — the autopilot's Edit/Bash writes to that path were denied as sensitive.
- 27 pre-existing repo-wide eslint errors outside this ticket (unused vars in `models/expense-claims.ts`, `models/health.ts`, `prisma/seed.ts`, …) — not this map's scope; a lint-hygiene pass someday.
- No removals were performed or needed.
