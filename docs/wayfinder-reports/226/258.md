# Autopilot report — #258 Build the one status vocabulary: StatePills from processingState(), the Status line, stepper, Approval tab, facets

- Map: #226 Vic-grammar operator app
- Type: wayfinder:task · Started: 2026-09-16T16:00Z (approx.) · Finished: 2026-09-16 (spec phase)
- Outcome: partial — phase SPEC (1 of 4) complete; `milestone: spec-done`

## What I did
1. Claimed #258; read map Notes, #233/#234 decisions, CONTEXT.md glossary, the Detail pane primer, both lessons files, the pre-flight template, the incumbent critique (H1 2 / H4 2, P0 "four contradictory statuses") and #259's close report.
2. Loaded `intent` (specify) and `impeccable` (clarify; launcher skipped per project lesson, PRODUCT.md read directly); read craft-floor's Verify list.
3. Read the code the ticket names (`processing-state.ts`, `StatePills`, `PaneFrame` status slot, `ApprovalTimeline`, stepper in `documents/[documentId]/page.tsx`, facets, `statusFilter`, `PaneDocumentContext`).
4. Wrote the spec (`logs/scratch-258/spec.md`) and the filled pre-flight A–E (`logs/scratch-258/preflight.md`).
5. Ran the spec critic (opus, fresh context, with code access): 26/40, 4 P1s — all codebase assumptions. Verified each in code, changed the spec, filled Part D; gate met (32, 0 P1, Clean).
6. Wrote the hand-off with `milestone: spec-done`; committed.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why |
|---|---|---|---|
| 1 | Ledger word: ticket says "Synced · Paid", #248 says Synced retires | *Posted* now, source unchanged until #281 | Decisions so far (#248) beats older ticket text |
| 2 | Save review redirects to `/review` from the pane — fix here or ticket? | Fix here (`stay` option) | H1 = 4 is impossible without it; pure execution, no decision |
| 3 | Full mode has no history — scope Approval tab to pane mode? | Load history on the four `?full=1` routes | CONTEXT.md: "the same pane at full width" |
| 4 | Held-back set: pill vs glyph disagree | `heldBack` input on the one function | #250 lesson: one function both read from |
| 5 | PO line-status words (#250 comment) | unchanged | Check vocabulary, four distinct states, not processing states |

## Decisions recorded
Spec phase only; resolution comment comes at close.

## Tickets created / changed
None.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=32 first-critique=- close-critique=- first-evaluate=- close-evaluate=-
Not built yet. Predicted evaluate ≈ 88, 0 P0/P1, Clean.

## First-pass findings → lessons (rendered surfaces only)
Not yet measured. Spec-phase lesson candidate (for close): the critic's four P1s were all "existing mechanism assumed" (redirecting action, missing payload in one mode, silent failure) — Part B rows that say "existing" must cite file:line.

## Needs the owner
Nothing this phase.
