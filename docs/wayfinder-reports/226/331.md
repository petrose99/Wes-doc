# Autopilot report — #331 Build claim rows in Bill Pay, the Reimbursements group in Payment batches, and claim-surface copy (#295 follow-on)

- Map: #226 DocuBite operator app (Vic.ai grammar, DocuBite skin)
- Type: Build · Started: (spec session, prior) · Finished: 2026-09-20 (this close session)
- Outcome: resolved

## What I did (this close session; earlier phases in prior sessions per the hand-off)
1. Claimed #331, read the hand-off — fix batch 1 (seed `ensureApprovalEvent()`) and fix batch 2
   (`paymentEligibility` branch on the claim status line) were already committed (1981b27); batch 2
   was un-gated and un-confirmed.
2. Ran `tsc --noEmit` on the whole project — clean.
3. Started the dev server (`.impeccable/live/dev.mjs start`) and the in-page detector server
   (`.impeccable/live/livesrv257.mjs`, needed a restart — round c1's process wasn't still up).
4. Re-ran the capture round (`round-331.mjs c2`, 8 states × 1440/390) and the gate
   (`scripts/wayfinder-autopilot/gate.mjs shots-c2 --baseline shots-c1 --residue-file residue.txt`)
   — **CLEAN**: 0 real findings, 0 new since baseline, 18/18 keyboard probes, 0 errored states, 0
   page errors (`gate-c2.json`).
5. Built a contact sheet and dispatched three fresh `sonnet` reader agents (critique, evaluate,
   include) on `shots-c2`, told specifically to re-check the prior round's open P1 (ineligible-claim
   pane's status line contradicting its own CTA). Full output: `readers-c2/*.md`.
6. Bar met — stopped the dev/detector servers, ran checks once: affected tests (36 passed),
   full `vitest run` (288 files / 2,803 tests passed), `tsc --noEmit` (clean), `eslint` on the four
   changed files (found and fixed one unused-import error left by the fix — a duplicate `import
   type` of `ClaimEligibilityReason` already re-exported two lines above), `next build` (succeeded).
7. Wrote two lessons to the project lessons file (seed-fixture gap that masked a real P1; a fix
   that makes a branch render for the first time can surface a pre-existing copy bug the same
   round).
8. Updated `docs/agents/areas/payments.md`: documented the `paymentEligibility` addition, folded
   in the three residue entries (`clipped-overflow-container`, `em-dash-overuse`, `text-overflow`)
   the prior hand-off had noted but not folded in.
9. Appended #331 to map #226's Decisions so far and updated the fog line that referenced it
   ("only on operator demand, once #331 has shipped" → "now that #331 has shipped").
10. Committed the eslint fix, lessons, primer and map update; closed #331 with the resolution
    comment below.

## Questions I answered on the owner's behalf
None this session — the close-phase plan was fully specified on the hand-off (numbered next
steps), nothing needed a fresh call.

## Decisions recorded
Bill Pay's claim rows ship with a claim-card status line that branches on payment eligibility:
approved-but-blocked claims show the specific blocking reason (amber, e.g. "Needs bank details
before paying") instead of a bare "Ready to pay" (emerald) that contradicted the pane's own
"Add bank details" call-to-action. The Reimbursements group in Payment batches and mixed-selection
batching remain out of #331's scope, tracked on #347 (blocked by #331, now unblocked).

## Tickets created / changed
- #331 closed (this session).
- #347 (Reimbursements group in Payment batches + mixed-selection batching) was already spawned
  and blocked-by #331 from the spec session; no change needed here beyond #331 closing, which
  unblocks it.
- Map #226: #331 appended to Decisions so far; fog line about a claimant-paid notice / reimbursement
  terms updated from "once #331 has shipped" to "now that #331 has shipped."

## Scores and counts (rendered surfaces only)
scores: predicted-critique=34 first-critique=27 close-critique=35 first-evaluate=87 close-evaluate=92

Gate: shots-r1 (measure baseline) → shots-c1 (batch-1 confirm) → shots-c2 (batch-2 confirm), all
16 states (8 × 1440/390) captured each round. gate-c1.json and gate-c2.json both **CLEAN**: 0 real
findings outside `residue.txt`, 0 new-since-baseline findings, 18/18 keyboard probes pass, 0
errored states, 0 page errors. `include` (c2): keyboard 18/18 pass; the new amber status text on
the ineligible-claim pane (08) renders cleanly at both 1440 and 390 (wraps to two lines at 390,
no clipping). Pre-build weak spot: Part D's H4 self/critic/reconciled (3/2/3 post-fix) assumed the
shared-`paidState`-derivation fix would be enough; it didn't anticipate that the derivation's
*input* (a seed fixture skipping the real approval audit event) could be wrong rather than the
derivation itself.

## First-pass findings → lessons (rendered surfaces only)
| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| H4 P1 (measure round): claim-card paid/scheduled confirmation line never rendered, read as a missing feature | Part B/preflight had no check that seed fixtures write the same audit event a real state transition writes | none | Traced root cause to `seed331.mjs` setting `status: "approved"` directly; added `ensureApprovalEvent()`; wrote new lesson (project lessons.md) with a `check:` grepping for bare `status: "approved"` fixtures with no matching audit-event insert |
| H6 P1 (c1 confirm round): ineligible-claim pane's status line said "Ready to pay" while its own CTA said "Add bank details" | Part D/E predicted H6 as 3/3/3, "none — reason text already visible, not hover-only" — correct for the *row* but not verified for the *pane*, and invisible pre-fix since the line never rendered at all | wrong (predicted no issue; a real self-contradiction existed but was masked by the H4 bug) | Added `paymentEligibility` to `DocumentClaimFacts`, reusing `BillPayClaimRow.eligibility`'s existing check; status line now branches to the specific reason (amber) instead of "Ready to pay"; wrote new lesson: a fix that makes a blank branch render for the first time needs a same-round CTA-consistency check |
| H8 (measure round): `clipped-overflow-container`/`text-overflow` detector findings across most states | Already named as residue for other queues (`queue-shell.md`/`detail-pane.md`) but not yet folded into `payments.md`'s residue block | unchecked (existed elsewhere, not copied into this area's primer) | Folded into `payments.md`'s residue section and `residue.txt` this session |

## Needs the owner
None. No removal, no credential, no ambiguous call this session — the close-phase plan was fully
determined by the hand-off and the bar was met on the second confirm round (2 of the 3 allowed gate
rounds used).
