milestone: build-in-progress (not build-done)

## Step
step: 1 — guard `open`/`close` in queue-screen.tsx via shared ConfirmDialog — done (needs Escape-path debug, see below)

## What's built
- `components/queue/queue-screen.tsx`: added `guardLeave`/`pendingLeave`, routed `open()` and
  `close()` through it instead of `confirmLeave()` (window.confirm). Added a `ConfirmDialog`
  ("Leave without saving?", same copy as `AdminLeaveGuard`) rendered at the bottom of the JSX
  (~line 695), `onConfirm` calls `clearUnsaved()` then runs the stashed action.
- Removed the `confirmLeave` import (now unused anywhere in the repo — left the export in
  `lib/client/unsaved-changes.ts` alone, out of scope to delete it this session).
- `tsc --noEmit` clean.

## Verified live (Riverside seed, `/admin/users`, Thabo Mokoena row, dev server + Playwright)
- **Pane ✕ (lg+, "Close" button) while dirty → dialog appears** (`role=alertdialog`, "Leave
  without saving?" copy), confirming discards and returns focus to the row's own trigger button.
  Confirmed working end to end (dialog:1, then afterDiscard focus = the row button). **Decision 2
  and decision 4 (discard → row) verified.**
- **Escape while dirty → dialog does NOT appear, pane does not close either** (`dialog:0`,
  `stillOpen:1` — i.e. nothing happens at all). This is a bug, not yet understood: `close()` is
  the *same* function reference/logic used by the ✕ button (`onClose={() => close()}` at
  queue-screen.tsx:687) which works. Debug console.log added temporarily inside the `onKey`
  keydown handler (now removed, not committed) showed: the keydown reaches the handler
  (`target=BODY` after blurring the field, as a real operator's focus would be), none of the
  early-return guards fire (`hasUnsaved()` reads `true` right before the `Escape` branch), and
  `close()` is called — but no dialog renders and the pane stays open. Not yet root-caused. Two
  live hypotheses to check next session: (a) whether Playwright's synthetic `Escape` `keydown` on
  `document` is somehow not the same trusted path a real user's key produces for this app's CSP
  setup (there's a report-only CSP blocking `eval`, unlikely related but noted in console noise);
  (b) whether there are *two* `close` identities in play — verify by grepping for a second
  `const close` or a shadowed `close` prop passed into `DetailPane`'s pane internals that could
  intercept `Escape` before the document-level listener the ticket names.
- **390 width untested this session** (ran out of context mid-debug of the 1440 Escape path;
  the `getByLabel("Role in Riverside Bakery Co.")` selector needs `waitFor` on the
  `Information` tab after opening the row — see scratch script, already fixed there).

## Artifacts
- Verification script (reusable, fix the Escape mystery here first):
  `docs/wayfinder-reports/226/logs/scratch-327/verify-guard.mjs`
- Latest run output: `docs/wayfinder-reports/226/logs/scratch-327/verify-guard.out`
- Dev server log: `docs/wayfinder-reports/226/logs/scratch-327/dev.log` (server was left running
  in the background this session; kill it — `pkill -f "next dev --turbopack"` — before starting
  a fresh one next session)

## Open findings
- **P0 for this ticket**: Escape does not trigger the guard (or the close) at all in live
  verification, even though the ✕ button using the identical `close()` does. Must be fixed before
  this ticket can close — the ticket is explicitly about Escape *and* ✕.
- Decision 3 (below-`lg` sheet: Esc + sheet's ✕/back-arrow) not yet verified live at all.
- Decision 5 (keyboard probe in the shared round script) not started — no existing shared script
  named `pane-esc-unsaved-guard`; `verify-guard.mjs` in scratch-327 is the closest candidate,
  written for this ticket. Decide next session whether it should be promoted into a shared script
  other queue builds import, or whether the ticket's "shared round script" refers to something
  under `scripts/wayfinder-autopilot/` not yet located.

## Next step
Reproduce the Escape non-response with a minimal isolated case (a bare button dispatching a
synthetic `Escape` keydown on `document` vs a real `page.keyboard.press`), or instrument
`close()`/`guardLeave` itself (not just the `onKey` handler) with a temporary log to see whether
`guardLeave` is even reached — then fix, remove all debug logs, re-run `verify-guard.mjs` for
Escape at 1440, extend it to the ✕/back-arrow and Escape at 390, then continue to decision 5 and
the closing bar (evaluate/critique/detector) before closing #327.
