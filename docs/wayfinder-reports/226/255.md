# Autopilot report — #255 Close the scope-guard gap: guard the 32 workspace-keyed models outside WORKSPACE_SCOPED_MODELS, or record why each is global

- Map: #226 Map: Make the operator app work like Vic.ai (grammar, not skin)
- Type: wayfinder:task · Started: 2026-09-16T09:20Z · Finished: 2026-09-16T09:53Z
- Outcome: resolved

## What I did
1. Claimed the ticket (assigned to petrose99 — GitHub does not allow non-user actor assignment).
2. Read `lib/workspace-scope.ts` to understand the guard mechanism and existing exclusion-comment style.
3. Delegated the per-model investigation and implementation to a background agent, since it required reading 32 Prisma model definitions, grepping call sites for each, and running the test suite — too large for inline work. Gave it explicit instructions to leave alone unrelated uncommitted WIP already in the working tree (from ticket #253's in-flight admin build).
4. Agent committed [e060a95](https://github.com/petrose99/Wes-doc/commit/e060a95c7aaae44b4398482d64831b77e1e62e4b) — `WORKSPACE_SCOPED_MODELS` grew from 39 to 68 models.
5. Posted the [resolution comment](https://github.com/petrose99/Wes-doc/issues/255#issuecomment-5695537464) and closed #255.
6. Appended a one-line gist to map #226's Decisions so far.

## Questions I answered on the owner's behalf
This is an AFK task ticket, not a grilling round — no HITL questions were posed. The one judgment call made per model (guard vs. document-as-excluded) was decided by the ticket's own stated criteria (does the model need to be readable before a workspace is known / is it reached only through an already-scoped parent) and, for the one new exclusion (ProductEvent), by inspecting its schema (nullable `workspaceId`) and its retention-sweep call site (`lib/analytics.ts`), which deliberately spans all workspaces.

## Decisions recorded
`WORKSPACE_SCOPED_MODELS` now covers 68 of 71 workspaceId-carrying models. 29 of the 32 named models were newly guarded; WorkspaceMember and WorkspaceInvitation stay excluded per the file's pre-existing documented reason; ProductEvent is newly excluded and documented (nullable workspaceId, cross-workspace retention sweep). Guarding surfaced and fixed four real missing-workspaceId-filter bugs (health auto-resolve, coding-correction cap, close reopen/read, and two functions — `category-account-mappings.ts`, `document-sheet-placements.ts` — that took a client-supplied id without re-checking it against the caller's workspace, the most serious of the four since the mapping-delete action could reach another workspace's row by id).

## Tickets created / changed
None created. No fog graduated — this ticket closes a data-integrity gap that #231 named as a prerequisite; it does not open a new decision.

## Scores and counts (rendered surfaces only)
Not applicable — this is a backend/data-model task, not a rendered surface. No `intent`/`impeccable` skills apply.

## Needs the owner
None. `tests/security/cross-tenant.db.test.ts` (the RLS-focused test the ticket mentioned) could not be run live against the devdb in this sandbox; the guard logic itself is covered by new unit tests in `lib/workspace-scope.test.ts` instead. Not a blocker — the RLS test enumerates its own table list from `pg_class` and needed no changes for this ticket.
