# Autopilot report — #259 Build the folded Detail pane header

- Map: #226 Post-login UX (Vic tour findings)
- Type: wayfinder:task (execution) · Started: 2026-09-16 ~12:00 UTC · Finished: 2026-09-16 ~12:45 UTC (build phase done; close phase next)
- Outcome: partial — **build-done** (`milestone: build-done`); the close phase (capture, scores, fix batch, primer) is the next session's

## What I did
1. Read the hand-off (`259.handoff.md`): spec phase already complete (abf798a), build in progress (683e1a3). The driver labelled this session SPEC because the milestone line was `build-in-progress` (unparseable); re-spec'ing would have redone recorded work, so I continued the build from the hand-off's next step. The hand-off now carries a parseable `milestone: spec-done` plus an explicit "next session is a build continuation" note.
2. Loaded `intent` (specify) and `impeccable` (shape); the `impeccable context` launcher needs interactive approval (known project lesson) — read the project context directly. Read `craft-floor.md`, both lessons files, spec §1–7.
3. Rewrote `components/queue/detail-pane.tsx` → `PaneFrame` + `DetailPane` + `PaneMenuItem` (`role=menuitem`, `aria-disabled`, `href` variant, `busy`), inner `PaneMenu` with roving keys; S1/S2/S3 states per spec §4 (S2 gains **Refresh queue**).
4. `document-actions-menu.tsx`: Open review task as a real link; `busy` prop; menu closes after a busy action resolves (`closeMenu`); cancelled hint generalised to "documents".
5. `stage-indicator.tsx`: `h-9` band, one-line form below `lg` with sr-only "Step n of N, ".
6. `split-pane.tsx`: top bar and standalone branch removed, source strip (filename · Open file · Split/Details/Source radiogroup, sessionStorage) added, `useRegisterDocumentActions` wired, `embedded`/nav props removed (script `logs/scratch-259/edit-split-pane.mjs`).
7. Hand-off written with the exact remaining steps (queue-screen wiring, six callers, page.tsx full-mode wrap, delete `delete-document-button.tsx`, Part B checks, `tsc`).

### Build session 3 (this session, phase BUILD 2 of 3)
8. Read the hand-off, spec, craft floor, both lessons files; loaded `intent` (specify) and `impeccable` (shape); launcher skipped per project lesson. No `docs/agents/areas/` primer exists for the Detail pane — the close phase writes it.
9. `queue-screen.tsx`: `rowName` replaces `rowTitle`/`rowSubtitle`; `paneStatus`, `fullHref`, `archivedToast`, `onMutated` wired to `DetailPane`; `[role=radiogroup]` added to the keydown-ignore selector (`logs/scratch-259/edit-queue-screen.mjs`).
10. Eight callers migrated, not six — `approval-invoice-queue.tsx` and `po-mismatch-queue.tsx` (#236) also used the old props. Hand-rolled *Open in a new tab* items and `ExternalLink` imports removed; one `statePills(row)` on Invoices/Receipts for the column and the Status line; Closed-facet toast on Invoices/Documents; Exceptions `fullHref` override (`edit-callers.mjs`).
11. `split-pane.tsx`: finished the previous session's partial edit — stale prop types/destructuring removed, imports pruned (`Link`, `useRouter`, ten icons) and the missing `useEffect`/`KeyboardEvent`/`ExternalLink` imports added; `fieldsReadOnly` re-derived without `embedded` (`edit-split-pane-2.mjs`).
12. `documents/[documentId]/page.tsx`: neighbour/stage computation deleted; `header` carries `archived/cancelled/cancelledReason`; `!embedded` wraps in `<PaneFrame mode="full" name backHref>` with `backHref = readOrigin(query) ?? typed queue path` (`edit-page.mjs`).
13. Deleted `components/documents/delete-document-button.tsx` (zero importers).
14. Part B greps (B1/B3 clean; action callers = menu + bulk bar + pre-existing Extract panel), `state-check.mjs` (40/40 spec states and strings present), stopped the leftover dev server, `npx tsc --noEmit` clean. No vitest file covers `components/**`.
15. Hand-off rewritten for the close phase; committed `wip(autopilot): #259 build`.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Driver says SPEC phase, hand-off says build in progress — which? | Continue the build | Brief: "do not redo what the hand-off records as done"; spec committed at abf798a |
| 2 | Cancelled-item hint noun ("invoices") on every queue? | "Cancelled documents are already closed." | Spec S4 says the noun follows the queue; one shared string that is true everywhere beats a per-queue prop for now (B3 one term per concept) |
| 3 | Menu stays open while Archiving… — who closes it? | The frame, after the action resolves (success or failure) | Spec §2 item 2 + S10 (menu closed, focus on ⋯) |
| 4 | Full-mode *Back to queue* — the list, or the queue with the pane open on this row? | The list (typed destination minus the row id), `from` first | Spec §1 "else the typed queue path"; the label says *queue*, and the pane copy is already open in the first tab (smallest reversible) |
| 5 | Full-mode name when the document has no supplier? | Filename as title, invoice number as suffix if any | Spec §2 line 1 (`name.title` = who/what); mirrors `document-queue.tsx`'s row name |
| 6 | Two extra callers (#236's Approvals queues) not in the hand-off's list — migrate or leave? | Migrate | They would not compile otherwise; same `QueueScreen` contract |
| 7 | `fieldsReadOnly` used `embedded`, which is gone — lock in full mode too? | Never lock in full mode (`history` is null there) | Full mode has no decision bar to lock underneath (#236 decision #6 is about a decision in flight on the same screen) |

## Decisions recorded
None new; execution of #234 per spec. Simplifications to name on the ticket at close: stepper rendered by `SplitPane`, not lifted into `PaneFrame`; full mode = read + secondary actions (no decision footer); full-mode Back lands on the list.

## Tickets created / changed
None this session.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=34 first-critique=- close-critique=- first-evaluate=- close-evaluate=-
Build phase — no capture, no scores. The design detector hook is not configured in `.claude/settings.json` (only the context-guard hook), so no per-edit findings arrived; the close phase's in-page detector is the check.

## First-pass findings → lessons (rendered surfaces only)
Not yet measured.

## Needs the owner
- Nothing blocking. `impeccable context` launcher still needs an interactive approval to run unattended.
