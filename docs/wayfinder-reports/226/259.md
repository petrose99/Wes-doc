# Autopilot report — #259 Build the folded Detail pane header: one header + Status line, stepper as the single second band, Archive/Flag/Delete in ⋯, source strip, ?full=1 on the same frame

- Map: #226 Map: Vic's interaction grammar, DocuBite's skin — the whole operator app
- Type: wayfinder:task (execution) · Started: 2026-09-16 ~11:34 UTC · Finished: 2026-09-16 (spec session, handed off at the 150K context line)
- Outcome: partial — spec phase; spec and pre-flight A–C/E written, spec critic (Part D) not yet run

## What I did
1. Claimed #259; read the map, #234's resolution, #258 and #260, the baseline Invoices critique (25/40; H3 3, H8 2), both lessons files, `craft-floor.md`, PRODUCT.md; no area primer exists for the Detail pane (close phase writes one).
2. Called `intent` (specify) and `impeccable` (shape → distill/layout/clarify playbooks); `impeccable context` not attempted (project lesson: needs interactive approval).
3. Probed the pane code with two scripts (`logs/scratch-259/probe.mjs`, `probe2.mjs`) instead of reading files whole.
4. Wrote `logs/scratch-259/spec.md` and `logs/scratch-259/preflight.md` (Parts A, B1–B6, C, E).
5. Context crossed the hand-off line before Part D; wrote `259.handoff.md` (`milestone: spec-written`), committed, posted `Autopilot: continue —`.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Where do Archive/Flag/Delete get their data when the ⋯ lives in the frame but the document loads async? | Embedded SplitPane registers them through a `PaneDocumentContext`; one implementation in `document-actions-menu.tsx` | #234 d.3/9: one ⋯, every queue; B4 no bespoke copies |
| 2 | "Open in a new tab" on both the ⋯ (pane) and the strip (file)? | Strip control is **Open file** | H4 one term per concept (B3); the ticket's strip wording refined, not reversed |
| 3 | Archive: stay on the row vs list refresh? | Pane reloads now; the list refreshes on close/↑/↓; toast says "now under Closed" | #239 stay on row + #227 no optimistic removal |
| 4 | Layout toggle when the source is hidden (Details layout)? | The strip stays rendered; source panel collapses to the strip | H3 exit always visible; replaces two ad-hoc "Split view" buttons |
| 5 | Full mode footer | No decision footer; ⋯ carries the rest; flagged for the critic and the ticket | #227 decision is made on the queue; smallest reversible option |
| 6 | ⋯ keyboard model | `role=menu` + arrow keys in the pane's ⋯ only; queue header ⋯ unchanged | include; smallest change, incumbent grammar kept elsewhere |

## Decisions recorded
None on the ticket yet (spec phase; resolution comment comes at close).

## Tickets created / changed
None. Comment `Autopilot: continue —` posted on #259.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=32 first-critique=n/a close-critique=n/a first-evaluate=n/a close-evaluate=n/a
Prediction is self-scored only (Part D pending): H1 4 · H5 4 · others 3; evaluate sum 9, 0 P1, Clean, ≈ 86. No captures this session.

## First-pass findings → lessons (rendered surfaces only)
n/a — nothing built.

## Needs the owner
Nothing blocking. Note for the owner: full mode (`?full=1`) is specified without the Approve/Reject footer (decision stays on the queue); the critic's verdict on that goes on the ticket at spec-done.
