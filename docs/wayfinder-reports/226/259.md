# Autopilot report — #259 Build the folded Detail pane header: one header + Status line, stepper as the single second band, Archive/Flag/Delete in ⋯, source strip, ?full=1 on the same frame

- Map: #226 Map: Vic's interaction grammar, DocuBite's skin — the whole operator app
- Type: wayfinder:task (execution) · Started: 2026-09-16 ~11:34 UTC · Finished: 2026-09-16 ~12:00 UTC (session 2: Part D critic, spec-done)
- Outcome: partial — spec phase complete (`milestone: spec-done`); build is the next session's

## What I did
1. Claimed #259; read the map, #234's resolution, #258 and #260, the baseline Invoices critique (25/40; H3 3, H8 2), both lessons files, `craft-floor.md`, PRODUCT.md; no area primer exists for the Detail pane (close phase writes one).
2. Called `intent` (specify) and `impeccable` (shape → distill/layout/clarify playbooks); `impeccable context` not attempted (project lesson: needs interactive approval).
3. Probed the pane code with two scripts (`logs/scratch-259/probe.mjs`, `probe2.mjs`) instead of reading files whole.
4. Wrote `logs/scratch-259/spec.md` and `logs/scratch-259/preflight.md` (Parts A, B1–B6, C, E).
5. Context crossed the hand-off line before Part D; wrote `259.handoff.md` (`milestone: spec-written`), committed, posted `Autopilot: continue —`.
6. Session 2: re-claimed; called `intent` (specify) and `impeccable` (shape; the context launcher was blocked by the sandbox, PRODUCT.md read directly last session). Ran pre-flight Part D — a foreground `opus` critic on spec.md + preflight.md: 29/40, one P1 (H4), four P2, Clean. Verified its route claims with two greps (six typed `[documentId]/page.tsx` gates, dashboard L195 `?stage=review`, queue keydown ignore list L158).
7. Applied the critic's eight spec changes with `logs/scratch-259/spec-edit.mjs`; filled Part D with the reconciliation and post-reconciliation prediction (32/40, 0 P1, Clean — gate met). Posted the full-mode ruling on the ticket (comment 5696927206). Hand-off updated to `milestone: spec-done`.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Where do Archive/Flag/Delete get their data when the ⋯ lives in the frame but the document loads async? | Embedded SplitPane registers them through a `PaneDocumentContext`; one implementation in `document-actions-menu.tsx` | #234 d.3/9: one ⋯, every queue; B4 no bespoke copies |
| 2 | "Open in a new tab" on both the ⋯ (pane) and the strip (file)? | Strip control is **Open file** | H4 one term per concept (B3); the ticket's strip wording refined, not reversed |
| 3 | Archive: stay on the row vs list refresh? | Pane reloads now; the list refreshes on close/↑/↓; toast says "now under Closed" | #239 stay on row + #227 no optimistic removal |
| 4 | Layout toggle when the source is hidden (Details layout)? | The strip stays rendered; source panel collapses to the strip | H3 exit always visible; replaces two ad-hoc "Split view" buttons |
| 5 | Full mode footer | No decision footer; ⋯ carries the rest; flagged for the critic and the ticket | #227 decision is made on the queue; smallest reversible option |
| 6 | ⋯ keyboard model | `role=menu` + arrow keys in the pane's ⋯ only; queue header ⋯ unchanged | include; smallest change, incumbent grammar kept elsewhere |
| 7 | Full mode without Approve — P1 or acceptable? | Acceptable once `?full=1` is the only trigger; legacy `?stage=`/`?page=` links open the queue + pane | Critic ruling (Part D); #227 decision on the queue; the dashboard's "Blocked in review" links were the only path that lost Approve |
| 8 | Where does the review-task link go when the top bar is deleted? | ⋯ item *Open review task* | B1 every action keeps a caller; critic finding |
| 9 | Archive/Flag: close the menu on click or show pending? | Item shows *Archiving…* with `aria-busy`, menu closes on resolve | H1 4 by construction needs visible in-flight state; critic P2 |
| 10 | Toast "now under Closed" on every queue? | Only where the queue has a Closed facet (Invoices, Documents); plain elsewhere | B3 grounded in `facet-filters` data, not assumed |

## Decisions recorded
On the ticket: full mode = read + secondary actions; `?full=1` sole trigger for full mode; the ⋯ item list (Open in a new tab · Open review task · Archive · Flag · surface items · Delete…). Resolution comment comes at close.

## Tickets created / changed
None. Comment with the Part D result and ruling posted on #259.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=32 first-critique=n/a close-critique=n/a first-evaluate=n/a close-evaluate=n/a
Critic on the first spec: 29/40, 1 P1 (H4 full-mode deep links), 4 P2. After reconciliation: H1 4 · H5 4 · others 3 = 32; evaluate 0 P0/P1, Clean, ≈ 86. Which pre-build step was weak: Part C/E2 only walked arrival via *Open in a new tab* and B1 missed the review-task link — both now converted to checks in the pre-flight. No captures (spec phase).

## First-pass findings → lessons (rendered surfaces only)
n/a — nothing built.

## Needs the owner
Nothing blocking. For the owner's awareness: full mode stays without an Approve/Reject footer and legacy `?stage=`/`?page=` document links will open the queue with the pane instead of the standalone page (critic ruling, recorded on #259). No removal involved — the standalone page still exists at `?full=1`.
