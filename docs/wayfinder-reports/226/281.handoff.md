# Hand-off — #281 (map #226)

milestone: spec-in-progress
milestone: spec-done
milestone: build-step-1-done
milestone: build-step-2-done
milestone: build-step-3-done
milestone: build-step-4-done

## Where it stands
Build plan: `docs/wayfinder-reports/226/logs/scratch-281/build-plan.md`. This
session worked step 5 (connection-failure band + pane ⋯ "Post to ledger" +
Checks tab Retry) but did **not** finish it — hit the context hand-off line
mid-step. Step 5 is NOT marked `done` on build-plan.md; do not start step 6.

## This session's progress on step 5 (uncommitted work is in the tree, not yet committed as its own commit — see below)
Done:
- `lib/integration-push.ts`: new `getLedgerConnectionBandStatus(workspaceId)`
  → `"disconnected" | "needs_reauth" | "no_default_account" | null` (null =
  connected+healthy, or integrations off). Exported `LedgerBandStatus` type.
- `components/queue/connection-band.tsx` (new): `ConnectionBand` — amber,
  `OriginStrip`-shaped, not a link, "Reconnect" (owner, links to Admin ›
  Integrations) / "Ask an owner" (non-owner). Copy per spec.md §6.
- `components/queue/queue-screen.tsx`: new `connectionBand?: ReactNode` prop,
  rendered right after `OriginStrip`, before the existing `band` (Bill Pay's
  metric band — left untouched, still the only `band` user).
- Wired into all three postable queues + their pages: `invoice-queue.tsx` +
  `invoices/page.tsx`, `receipt-queue.tsx` + `receipts/page.tsx`,
  `document-queue.tsx` + `document-queue-page.tsx` (gated on `postable`
  i.e. Bank Statements only, same gate as `connectionId` already used).
  Each page now also fetches `getLedgerConnectionBandStatus` alongside
  `getActiveIntegrationConnectionId` and passes `isOwner={membership.role
  === "owner"}` down.
- Pane ⋯ "Post to ledger" (spec.md §7) added to **Invoices**
  (`invoice-queue.tsx`: `postIneligibleReason(bill)` + `PaneMenuItem` in
  `paneMenu`, reuses existing `posting`/`setPosting`/`PostConfirmDialog`
  state — `setPosting([bill.documentId])` for a 1-row selection, no new
  dialog needed) and **Receipts** (`receipt-queue.tsx`, same pattern,
  `postIneligibleReason(receipt)`). Both reuse `PaneMenuItem`'s existing
  `disabled`/`hint` → `aria-describedby` wiring (detail-pane.tsx:287-311)
  for H6, no new a11y code needed.
- `tsc --noEmit` run once mid-session on the whole project: **zero errors**
  in any file this step touched (grep for the touched filenames in tsc
  output came back empty — full log not saved, re-run at G3/tsc-clean check
  if in doubt, but nothing outstanding was seen).

NOT done yet (this is the next step's work, in order):
1. **Bank Statements pane "Post to ledger"** — `document-queue.tsx` has no
   `paneMenu` prop passed from its page at all yet (grep `paneMenu` in
   `document-queue.tsx`/`document-queue-page.tsx` — likely needs adding,
   confirm at build time). Mirror the Invoices/Receipts pattern: a
   `postIneligibleReason(row)` using whatever eligibility signal
   `DocumentQueueRow`/the generic document model exposes (status ===
   "reviewed" is the minimum bar, same as Receipts) gated on `postable`
   (Purchase Orders never gets this item).
2. **Checks tab Retry** (spec.md §7 second bullet) — not started. Recon §4
   says no reason-specific Retry renderer was found for `push_preflight`;
   grep `components/pipeline/document-detail/split-pane.tsx` and
   `live-checks.tsx` at build time for how a check reason renders (or add
   one) — Retry calls `attemptIntegrationPush` for the row (import from
   `lib/integration-push.ts`, already used internally by
   `pushDocumentToConnection`). Needs its own server action
   (`app/(app)/workspaces/[workspaceId]/...-actions.ts`, follow the
   `postSelectedDocumentsAction` pattern from step 1). On success: task
   closes, pane's Ledger mark updates same-tick (existing revalidation
   path per spec). On failure: detail text updates with the new failure
   reason (never a bare "failed again").
3. **Keyboard-only pass** (step 5's check) — Tab to a disabled "Post to
   ledger" item reads its reason (should already work via `aria-describedby`
   — confirm live); Retry reachable and announces result (needs #2 first).
4. Confirm `postIneligibleReason` copy against spec.md §7's exact reason
   strings ("Category not confirmed", "Already posted", etc.) — this
   session used the client-guess reasons already established by
   `postEligible`/`cancelInfo` in each queue (consistent with §2's "client
   guess, server re-resolves" framing), not a verbatim copy of every
   spec-listed reason string; confirm this reads fine or tighten at G2.

## Key decisions locked (do not re-open)
#248 is the source of truth for words/eligibility (quoted in spec.md §0).
CONTEXT.md's Post/Posted/Ledger mark/Finance/Bill Pay/Payee entries are the
exact copy. spec.md §1b lists every spec-critic fix already applied — do not
re-litigate H1/H9/H5/H4/H6.

## Next session (build phase, step 5 continued)
Read this hand-off, then re-read spec.md §6/§7/§10 (already read this
session, short) and recon.md §4 (Checks tab). Pick up at "NOT done yet"
item 1 (Bank Statements pane menu — quick, mirrors what's shipped), then
item 2 (Checks tab Retry — the bigger remaining piece), then run step 5's
check, mark `step: 5 — done` on build-plan.md, commit, and only then start
step 6 (Finance page rewrite) in a fresh session.
