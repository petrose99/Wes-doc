# Hand-off — #281 (map #226)

milestone: spec-in-progress
milestone: spec-done
milestone: build-step-1-done
milestone: build-step-2-done
milestone: build-step-3-done
milestone: build-step-4-done

## Where it stands
Spec phase complete. Spec: `docs/wayfinder-reports/226/logs/scratch-281/spec.md`.
Pre-flight: `.../scratch-281/preflight.md`. Spec critic (opus): `.../spec-critic.md`.
Recon: `.../recon.md`. Build plan (step-by-step, one per build session):
`.../scratch-281/build-plan.md` — status lines mirrored below.

Critic scored the first draft at critique 31/40, evaluate sum 18 (3 P1s: H1
durability, H9 silent eligibility-exclusion, H5 missing bulk consequence
copy). All fixed directly in spec.md (§1b lists them). Reconciled: critique
34/40 (three 4s), evaluate sum 4, P0/P1 zero, verdict Clean. Gate passes.
One residual build-time confirm (not a blocker): receipts account-column
uniformity, resolved in build step 2 — see build-plan.md.

## Build plan status
step: 1 — server action: selection-driven bulk Post — done
step: 2 — bulk bar Post button + confirm dialog (Invoices) — todo
step: 3 — extend to Receipts + Bank Statements — todo
step: 4 — row Ledger mark + facet rename (all 3 queues) — todo
step: 5 — connection-failure band + pane ⋯ Post + Checks Retry — todo
step: 6 — Finance page rewrite + housekeeping deletion — todo
step: G1 — round script (capture) — todo
step: G2 — r0 and fixes — todo
step: G3 — build-done — todo

## Key decisions locked (do not re-open)
#248 is the source of truth for words/eligibility (quoted in spec.md §0).
CONTEXT.md's Post/Posted/Ledger mark/Finance/Bill Pay/Payee entries are the
exact copy. spec.md §1b lists every spec-critic fix already applied — do not
re-litigate H1/H9/H5/H4/H6.

## Next session (build phase, step 4)
Step 1 shipped: `lib/integration-push-selection.ts` (pure eligibility
re-resolve + override filter, tested) and
`app/(app)/workspaces/[workspaceId]/post-selected-documents-actions.ts`
(`postSelectedDocumentsAction`; `pushDocumentToConnection` is now exported
from `integration-push-actions.ts` for reuse).

Step 2 shipped: `components/queue/post-confirm-dialog.tsx` (new —
`PostConfirmDialog`, reuses `ItemizedRecapTable`/`EligibilityStrip` +
account-select column), `bulk-approve-receipt.tsx` extended (`struck`,
`extraColumn`), `invoice-queue.tsx` wired (Post button in `extra`,
`postEligible` guess, dialog mount), `invoices/page.tsx` passes
`connectionId` (active-connection lookup). Receipts account-column question
resolved (build-plan.md step 2: applies uniformly, no docType branch in
`pushDocumentToConnection`'s account resolution) — steps 3-4 reuse the same
`extraColumn` unconditionally. `tsc --noEmit` and `impeccable detect --json`
on the new dialog both clean; full details in build-plan.md step 2.

Step 3 shipped (full detail in build-plan.md step 3): Receipts
(`receipt-queue.tsx`) and Bank Statements (`document-queue.tsx`, gated on
`todayOutcome !== undefined` so Purchase Orders — same generic component —
never shows Post) both now have the Post button + `PostConfirmDialog`
mount, same component as Invoices, unchanged. `RECEIPT_FACETS` and new
`BANK_STATEMENT_FACETS` both gained a Ledger facet section (value "synced"/
label "Posted" — matches Invoices' current shape; step 4 renames the value
vocabulary once, across all three, rather than twice). Hoisted the
active-connection lookup into `getActiveIntegrationConnectionId` in
`lib/integration-push.ts` on this, the third copy — `invoices/page.tsx`,
`receipts/page.tsx` and `document-queue-page.tsx` (bank statements only)
all call it now. `tsc --noEmit` and `impeccable detect --json` both clean.

Step 4 shipped: `LEDGER_FACT_LABELS` (processing-state.ts) is now
`Record<LedgerFact, string>` with keys `posting|posted|failed|paid` — the
internal key is `posted`, not just the label, so "synced" can't leak back
in. `StatePills` (row-cells.tsx) gained per-value tones (`LEDGER_PILL_TONE`)
so a `failed` ledger mark is visually distinct from the row's own red state
pill. `INVOICE_FACETS`/`RECEIPT_FACETS`/`BANK_STATEMENT_FACETS` all list
posting/posted/failed(/paid on Invoices) in one "Ledger" section (no
Open/Closed split — kept one section, one vocabulary, simplest read).
Renamed the sentinel at the actual data source too, not just labels: the
"succeeded push, no confirmed ledger status" sentinel in
`models/ledger-payments.ts` is now `"posted"` (was `"synced"`), so
`models/bills.ts`'s `statusFilter`, `invoices/page.tsx`'s status-param
parsing, `invoice-queue.tsx`'s `postEligible`/`cancelInfo` (copy: "Already
posted to your ledger…"), the document detail pane's `ledger`/`syncDetail`
derivation, and `models/documents.ts`'s cancel-block check all match on
`"posted"` now, not `"synced"` (that rename would otherwise have silently
broken the Post/cancel eligibility and the Status filter chip). Did NOT
touch `lib/documents/stages.ts`'s `PIPELINE_STAGES` "synced" stage (the
Pipeline page's own vocabulary, explicitly out of this ticket's scope per
`retired-status-words.test.ts`'s own allow-list comment) or wire
Receipts/Bank-Statements' `ledger` prop to real push data (they don't pass
`ledger` to `StatePills` at all yet — no regression, just not yet lit up;
not in step 4's file list, revisit only if G2 flags it).
Updated tests: `row-cells.test.tsx`, `processing-state.test.ts`,
`bills.test.ts`, `ledger-payments.test.ts` all updated to the new vocab and
pass (36/34 green). `tsc --noEmit` shows only pre-existing unrelated errors
(confirmed identical on `git stash` baseline — dictation-actions.ts,
job-worker.ts, etc., nothing in a file this step touched).
`retired-status-words.test.ts` (the actual B3 grep) passes.

Read this hand-off, then build-plan.md's step 5 block, then spec.md §6/§7/§10.
Next: connection-failure band (amber, `OriginStrip`-shaped) + pane ⋯ menu
"Post to ledger" (single-doc) + Checks tab Retry wired to
`attemptIntegrationPush`.
