# Hand-off — #281 (map #226)

milestone: spec-in-progress
milestone: spec-done
milestone: build-step-1-done
milestone: build-step-2-done
milestone: build-step-3-done
milestone: build-step-4-done
milestone: build-step-5-done

## Where it stands
Build plan: `docs/wayfinder-reports/226/logs/scratch-281/build-plan.md` — all
5 build steps `done`. Step 5 (connection band + pane "Post to ledger" +
Checks tab Retry) finished this session: Bank Statements pane menu item and
the Checks-tab Retry (new `getOpenLedgerRetryTask`, `retryLedgerPushAction`,
`ChecksTab` Retry row) are shipped. `pushDocumentToConnection` now closes any
open `push_preflight` task on a succeeded push (root-cause fix, covers every
caller). `tsc --noEmit` clean on all touched files (two pre-existing,
unrelated arity errors elsewhere, confirmed present before this session's
changes). `vitest` unchanged, still green.

## Next session (build phase)
Step 6 — Finance page rewrite + housekeeping deletion (spec.md §8): reuse
`Panel` + `LedgerConnectionPanel`, add "Open ledger" action, delete
`AccountingDashboard`'s dead chrome and `push-to-accounting-card.tsx`. Then
G1/G2/G3 (round script → r0 fixes → build-done), per the phase brief — G1
needs states: bulk dialog, row pill, facet, connection band, pane ⋯ item
(disabled+reason — now on all 3 postable queues), Checks Retry, Finance
page. No dev server was run this session; the first live keyboard/detector
pass is G1's job.

## Key decisions locked (do not re-open)
#248 is the source of truth for words/eligibility (quoted in spec.md §0).
CONTEXT.md's Post/Posted/Ledger mark/Finance/Bill Pay/Payee entries are the
exact copy. spec.md §1b lists every spec-critic fix already applied — do not
re-litigate H1/H9/H5/H4/H6.
