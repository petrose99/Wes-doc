# Hand-off — #281 (map #226)

milestone: spec-in-progress
milestone: spec-done
milestone: build-step-1-done
milestone: build-step-2-done
milestone: build-step-3-done
milestone: build-step-4-done
milestone: build-step-5-done
milestone: build-step-6-done
milestone: build-step-G1-done
milestone: build-step-G2-done
milestone: build-done
milestone: measured
milestone: closed

## CLOSED — #281 shipped
Final scores: evaluate 90/100 (0 P0/P1, Clean), critique 36/40 (no heuristic < 3), gate CLEAN on
shots-r5 vs shots-r1. Fixed: contrast, duplicate "Open ledger" CTA, duplicate Post-dialog
consequence copy. Full detail in `docs/wayfinder-reports/226/logs/scratch-281/close.md`. Area
primer at `docs/agents/areas/finance.md`. Map #226 Decisions-so-far updated. No further action.

## Where it stands (close phase, this session — continue here, do not re-measure)
Full triage, fix batches, gate rounds and confirm-reader results are all in
`docs/wayfinder-reports/226/logs/scratch-281/close.md` — read that file first, it has the "Next
step" section with the exact resume point. Do not redo anything it lists as done.

Short version: the r1 P1 ("Open ledger" missing on Finance) was a dev-seed data artifact, not a
code bug (stale connection row from an older ticket's seed script, status:"active" with
externalTenantId: null — a shape production code never produces). Repaired the dev DB row, which
exposed a real latent contrast bug (fixed) and — after a confirm reader round scored 90/100
evaluate / 36/40 critique, both above bar — a real duplicate "Open ledger" CTA (page header +
reused panel both rendering it) which was also fixed in `finance/page.tsx`, but **not yet
re-captured or re-gated** — that capture round (`r4`) is the next action, per close.md's numbered
list.

## Next session
1. Start dev server + impeccable live-server (`node .impeccable/live/dev.mjs start`, then
   `node .impeccable/live/livesrv257.mjs` from inside `scratch-281/`) — both were stopped at the
   end of this session.
2. Follow `close.md`'s "Next step" section exactly (capture r4, gate vs r1, resolve the
   finance-page text-overflow triage question, run the once-each checks, write lessons, write/update
   the Finance area primer, close the ticket).

## Key decisions locked (do not re-open)
#248 is the source of truth for words/eligibility (quoted in spec.md §0). CONTEXT.md's
Post/Posted/Ledger mark/Finance/Bill Pay/Payee entries are the exact copy. spec.md §1b lists every
spec-critic fix already applied.
