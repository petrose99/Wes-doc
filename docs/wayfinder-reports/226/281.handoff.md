# Hand-off — #281 (map #226)

milestone: spec-in-progress

## Where it stands
Spec phase, session 1. Claimed #281. Read #248 (decision ticket, do not re-decide),
CONTEXT.md glossary (Post/Posted/Ledger mark/Finance/Bill Pay/Payee — verbatim, already
written), incumbent Finance critique (14/40, `.impeccable/critique/2026-09-15T22-25-51Z…`),
shipped Payments critique for pattern reference (34/40), queue-shell + detail-pane area
primers, both lessons files, specify/fortify/articulate/include/impeccable digests,
craft-floor.md, product-requirements skill.

**Recon done and saved**: `docs/wayfinder-reports/226/logs/scratch-281/recon.md` — code facts
for every file this ticket touches (integration-push-actions.ts, documents.ts,
doc-types.ts, review-tasks.ts/Checks tab, row-cells.tsx, invoice/receipt/bank-statement
queue facets+bulk `extra` slots, origin-strip.tsx shape, Admin `Panel` (automation-ui.tsx:96),
and — the big finding — **Admin › Integrations already has a ledger connection `Panel`**
(`admin/integrations/page.tsx` + `LedgerConnectionPanel` in accounting-dashboard.tsx:229)
built on #252. This shrinks the Finance-page step to reusing that component, not building new
connection UI. `PushToAccountingCard` is already dead code (zero importers) — housekeeping
deletion, not a sign-off removal. `documents.ts`'s "silent drop" bug #248 named already has a
`droppedCount` field tracking it (verify still holds, don't re-fix blindly).

## Not yet done (this is where the next session picks up)
1. Write the full spec (`docs/wayfinder-reports/226/logs/scratch-281/spec.md`) using
   `specify`/`fortify`/`articulate`/`include` structure, #248's decided words verbatim, the
   recon facts above. Screens: Invoices/Receipts/Bank-Statements bulk bar Post + confirm
   dialog (extend `ItemizedRecapTable` with an optional Account-select column + `EligibilityStrip`),
   row Ledger mark (extend `StatePills`' ledger union to `posting|synced|failed|paid` with new
   tones), pane ⋯ "Post to ledger", connection-failure band (origin-strip shape, amber),
   Ledger facet on 3 queues, Checks-tab Retry for `push_preflight`, Finance page rewrite
   (`Panel` + `LedgerConnectionPanel` reuse + `AccountingErrorBanner`, strip `AccountingDashboard`/
   `ReadyToPushList`).
2. Fill `preflight.md` (copy from `scripts/wayfinder-autopilot/preflight.md`) Parts A–E — not
   started.
3. Run the spec critic (fresh `Agent`, model **opus** — ticket touches money/ledger) against
   the finished spec + preflight; reconcile Part D.
4. Write the build plan onto this hand-off (ordered `step: N` lines) — target ≤6 steps + 3
   gates, ≤10 captured states (tight but was pre-sized to fit at the start of this session:
   row status+facet · bulk Post+dialog · confirm-dialog partial/zero-eligible states · band ·
   pane ⋯ + Checks Retry + deep link · Finance page — that's 6).
5. Commit spec+preflight+hand-off, then `milestone: spec-done`.

## Why this session stopped here
Hit the 150K context hand-off line right after finishing recon (a foreground Explore agent
call) and writing recon.md. No code has been touched. No commit exists yet for this ticket in
this session — this hand-off + recon.md are the first artifacts to commit.

## Key decisions already locked (do not re-open)
Ticket #248's body is the source of truth for words/eligibility/state inventory — read it in
full again next session (`gh issue view 248`) rather than trusting a re-summary. CONTEXT.md's
Post/Posted/Ledger mark/Finance entries are the exact copy to use.
