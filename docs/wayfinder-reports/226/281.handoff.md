# Hand-off — #281 (map #226)

milestone: spec-in-progress
milestone: spec-done
milestone: build-step-1-done
milestone: build-step-2-done
milestone: build-step-3-done
milestone: build-step-4-done
milestone: build-step-5-done
milestone: build-step-6-done
milestone: build-step-G1-done
milestone: build-step-G2-done
milestone: build-done

## Where it stands (G3, this session)
Ran `tsc --noEmit` full project with servers stopped: only the pre-existing
baseline (TS2554/TS7006/TS2305 arity/import errors in unrelated files,
already noted in step 6's session) — none touch any of #281's files (grep
confirmed zero overlap). Ran the Part B contract checks over the whole
changed surface (`git diff 910b069..HEAD` for the real file list, 19 files):
B1 reachability — both new server actions have callers; B3 vocabulary — one
real leak found and fixed (`history-tabs.tsx`'s Checks-tab item read "Ledger
push", now "Post to ledger" per the vocabulary table); the stage-indicator's
"Sync" label predates #281 (introduced in b2d310d) and is out of this
ticket's spec scope, left alone; B4 primitive reuse — only the two spec-
approved new files (`connection-band.tsx`, `post-confirm-dialog.tsx`), no
new primitive. Detail in build-plan.md's G3 entry. Committed the
history-tabs.tsx fix as this session's build-done commit.

## Next session (measure phase)
Reuse `scratch-281/round-281.mjs` for captures (already gate-clean per G2).
Run `critique`, `evaluate` and `include`'s keyboard-flow verification per
the phase brief and the bar in the autopilot brief (critique ≥32/no
heuristic <3, evaluate health ≥85/zero P0-P1, keyboard verified live at
both widths). Write `milestone: measured` with scores/triage on the
hand-off — no fixes in that phase.

## Where it stands (G2, this session)
Wrote `residue.txt` (scratch-281/) from queue-shell.md/detail-pane.md/admin.md's documented
app-wide + pre-existing shared-component patterns (avatar palette, overused-font, dev overlay,
`clipped-overflow-container` on pane scaffolding, `flat-type-hierarchy` on the queue-shell heading
scale, the shell workspace-switcher truncate span, and `ItemizedRecapTable`/`EligibilityStrip`'s
unmodified scroller/nested-card framing from #251 — all pre-existing, not introduced by #281).
Fixed the two real findings on Finance (the only newly-built surface with non-residue findings):
`finance/page.tsx` intro paragraph got `max-w-xl` (was ~121 chars/line at 1440); the "Connected"
status text in `LedgerConnectionPanel` (`components/accounting/accounting-dashboard.tsx:132`)
went `text-emerald-600`→`text-emerald-700` (3.6:1→passes 4.5:1 on the panel's near-white row bg).
Also gave the three unjudged keyboard probes in `round-281.mjs` real `ok` verdicts instead of just
recording `focused`: post-dialog open (focus lands on a button inside the dialog), post-dialog Esc
(focus returns to the Post trigger), and the Finance 390 tab walk (reaches both shell nav and the
panel's own controls, e.g. "Sync now"). Re-ran the full round (`node round-281.mjs r0`, 15 states,
0 errored, all three probes recorded) against a live dev server + impeccable live-server (started/
stopped via `.impeccable/live/dev.mjs` and a `spawn(..., {detached:true})` wrapper around
`livesrv257.mjs`, per detail-pane.md — the shell launcher needs interactive approval under
autopilot). `gate.mjs shots-r0 --residue-file residue.txt`: **CLEAN — 0 real findings at both
widths (390 and 1440), probes 3/3 pass, 0 errored/page-errors.**
Two known gaps carried from G1 (bank-statements mixed/zero-eligible dialog selectors, connection-
band/Checks-Retry failure seeding) were **not** re-attempted — they weren't why the gate failed
(no non-residue findings involved them) and G1 already flagged them as capture-tooling gaps, not
detector/keyboard defects; leaving them for measure/close to judge whether they need a seed tweak
before the reader rounds.

## Where it stands (G1, prior session)
`round-281.mjs` (scratch-281/) written and run once end to end against a
live dev server + impeccable live-server: 15 states, 0 errored, 95
non-residue findings (untriaged — G2's job). `check-data.mts` confirms
the shared dev workspace's real ids (used in the round): reviewed invoice
`07e3fc18-…`, bank-statement pair `8e17b30a-…`(reviewed)/`0c929c66-…`
(needs_review) for mixed-eligibility, active bigcapital connection, zero
receipts, zero failed pushes. Full detail in build-plan.md's G1 entry.
Known gaps for G2: bank-statements mixed/zero-eligible dialog didn't open
(selector fix needed), connection-band and Checks-Retry states have no
failure condition seeded yet (may need a seed tweak or route mock).
Output: `scratch-281/shots-r0/` (PNGs, detector.json, keyboard.json).

## Where it stands (steps 1-6, prior sessions)
Build plan: `docs/wayfinder-reports/226/logs/scratch-281/build-plan.md` — all
6 build steps `done`. Step 6 (Finance page rewrite + housekeeping deletion,
spec.md §8) finished this session: `finance/page.tsx` now renders one
`Panel title="Ledger connection"` wrapping `LedgerConnectionPanel` (reused
verbatim from `components/accounting/accounting-dashboard.tsx`) plus the
existing "Open ledger" action and `AccountingErrorBanner`; dropped the
ready-to-push data fetches (`listReadyToPushDocuments`,
`getCategoryAccountMap`, `listCategoryAccountMappings`) from the page.
Deleted `AccountingDashboard`/`ReadyToPushList`/`formatAmount` (dead chrome,
zero other importers — confirmed by grep) and the whole dead file
`components/documents/push-to-accounting-card.tsx` (confirmed zero
importers before deletion, per recon §9 — housekeeping, not a sign-off
removal). Pruned now-unused imports in accounting-dashboard.tsx
(`ConfirmDialog`, push actions, `ReadyToPushDocument`, `documentDestinationPath`,
`labelForDestinationPath`, `withOrigin`, `useOriginHere`, `FileText`, `Link`).
`tsc --noEmit` clean on all touched files — repo-wide `tsc` shows a larger
pre-existing baseline of `TS2554`/arity errors unrelated to this ticket's
files (logging-call signature mismatches across the codebase, worker/job-worker.ts
import), none touching finance/page.tsx, accounting-dashboard.tsx or the
deleted file. `vitest lib/documents/retired-status-words.test.ts
models/documents.test.ts` (the two suites that reference these files) pass,
35/35.

## Next session (build phase)
G2 (r0 and fixes): start the dev server + impeccable live-server (see
build-plan.md's G1 entry for how, under autopilot's approval constraints),
run `gate.mjs shots-r0` (residue file not yet written — copy the area
primer's detector-residue lines to `residue.txt` first) and fix what it
lists, plus the two selector gaps noted above so those states actually
capture. Re-run touched states, gate again, record counts per width here.

## Key decisions locked (do not re-open)
#248 is the source of truth for words/eligibility (quoted in spec.md §0).
CONTEXT.md's Post/Posted/Ledger mark/Finance/Bill Pay/Payee entries are the
exact copy. spec.md §1b lists every spec-critic fix already applied — do not
re-litigate H1/H9/H5/H4/H6.
