Stop retrying `gh issue edit --add-project` — that is GitHub Projects, not sub-issues, and it will never work. Finish charting map #302 with these exact mechanics, one Bash command per turn, and nothing else:

1. **Wire each ticket as a sub-issue of the map.** For each of 303 304 305 306 307:
   `id=$(gh api repos/petrose99/Wes-doc/issues/<n> --jq .id); gh api -X POST repos/petrose99/Wes-doc/issues/302/sub_issues -F sub_issue_id=$id`
   (`-F`, and the child's database `.id`, not its number.)
2. **Unassign every ticket:** `gh issue edit <n> --remove-assignee petrose99` for each. An assigned ticket is a *claimed* ticket; unclaimed tickets are the frontier.
3. **Fix the charting substance.** Tickets must be *decisions about this marketing site* — its hero message, page order, what proof it can honestly show, which CTA (sign up vs book a demo) leads, pricing presentation — each one answerable in one session from the code, the existing maps #157 and #142, and the `marketing:positioning-and-messaging` / `product:ux-product-auditor` skills. Generic marketing-programme work (CRM integration, A/B testing framework, competitive analysis of the market) is not a decision about the site: close #304, #306 and #307 with a one-line comment and move each to **Out of scope** on the map (gist + why, linking the closed ticket). Retitle/rewrite #303 and #305 as concrete decisions if they can be, else close them the same way. Then create 3–5 replacement `wayfinder:grilling` tickets (bodies via Write to `docs/wayfinder-reports/charting/<slug>.md`, then `gh issue create --title "..." --label wayfinder:grilling --body-file <path>`), wire each per step 1, and add blocking where one decision waits on another: `gh api -X POST repos/petrose99/Wes-doc/issues/<blocked>/dependencies/blocked_by -F issue_id=<blocker database id>`.
4. **Fix the map body** (Write the full body to a file, then `gh issue edit 302 --body-file <path>`): Destination stays; Notes stays; add an empty `## Decisions so far`; **Not yet specified** must not repeat anything that is now a ticket — keep only what is still too foggy to ticket; **Out of scope** gains the closed tickets and a line relating this map to #157 and #142.
5. End with the single line `map: #302 https://github.com/petrose99/Wes-doc/issues/302`.
