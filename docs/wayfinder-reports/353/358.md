# Autopilot report — #358 Decide the Purchase Order document in the Bill template: matched-invoices table where the PO combobox sits, fulfilment, what drops

- Map: #353 Map: Document detail modelled on Ramp — full takeover, editable Bill and Receipt templates, coding that learns
- Type: wayfinder:grilling · Started/Finished: single session, 2026-09-21
- Outcome: resolved

## What I did
1. Claimed #358 (`gh issue edit 358 --add-assignee @me`).
2. Read the map (#353), `docs/agents/design-tickets.md` §"Grilling a design ticket", the `grilling` and `domain-modeling` skill files.
3. Established facts from code before asking anything: `models/po-matching.ts` (PO↔invoice matching, `matchType: "po_to_invoice"`), `lib/checks/po-line-consumption.ts` (per-line consumption/mismatch detail), `document-queue-page.tsx:86` (`fullyInvoiced` is the actual derived field — CONTEXT.md's **Fully invoiced**, not the ticket's looser "Fulfilled"), `models/documents.ts` `cancelDocument` (the only document-level action available to a PO — no #258 approval/posting lifecycle applies to it), and confirmed no PO-specific detail surface exists yet (today's pane is the generic one; #362/#363 haven't built the Bill shell yet).
4. Ran one grilling round (8 questions) covering Intent's `wireframe`/`fortify` agenda and Impeccable's `shape`/`layout` agenda; answered every question with its recommendation per the map's standing owner delegation, each grounded in the code above or an existing map decision (#355/#356/#228).
5. Posted the [resolution comment](https://github.com/petrose99/Wes-doc/issues/358#issuecomment-5757065383) with the Q&A table, the decision list and an ordered Action Summary, and closed #358.
6. Created [Build the Purchase Order document branch of the Bill template (#365)](https://github.com/petrose99/Wes-doc/issues/365), blocked on #362 and #363 (verified via `issue_dependencies_summary`), attached as a sub-issue of #353.
7. Updated the map: appended #358's Decisions-so-far line, removed the now-graduated "Purchase Order as the opened document" entry from Not yet specified.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| Q1 | Matched-invoices table placement/columns | Replaces the PO-combobox slot; invoice·date·amount·status, read-only, opens the invoice; named empty state | #228's existing column shape; ticket's own framing of the slot swap |
| Q2 | How Fully-invoiced surfaces | Open→Fully invoiced/Cancelled track (not the 5 Bill words); sub-line = invoiced-of-ordered lines | CONTEXT.md: derived, never set by hand; POs have no #258 lifecycle |
| Q3 | Editable line-item columns | description/qty/unit/amount editable; add read-only Invoiced column; drop category/account | Map Notes "every field is editable" for the doc's own data; Invoiced is arithmetic over other documents |
| Q4 | Over-invoiced line display | Reuse existing `≠` mismatch glyph/colour from `checkPoLineConsumption` | Avoid inventing a second vocabulary for the same check |
| Q5 | Cancelled PO behaviour | Read-only form, reason where footer verb would be, table stays | Existing repo-wide cancellation convention |
| Q6 | Footer verb | None — Cancel only, from the existing actions menu | Day ticket (#354) names no PO step; PO has no approval/posting state machine |
| Q7 | Bill sections dropped | Payment Details drops (no Bill Pay relationship); supplier recent-invoices drops (duplicates Q1's table) | No #258 state to key Payment Details off; #355 caps duplicate disclosure |
| Q8 | Layout consequence | Reuse existing slot sizing and line-item table row style; no new breakpoint | #356's combobox slot already sized for a compact control/table |

## Decisions recorded
Matched-invoices table (invoice·date·amount·status) in the PO-combobox slot; Open→Fully invoiced/Cancelled status track, derived only, sub-line = invoiced-of-ordered; line items keep description/qty/unit/amount, add read-only Invoiced column, drop category/account; no footer verb (Cancel only); Payment Details and supplier-card recent-invoices both drop for this branch. Full detail and the Action Summary (Intent: `wireframe`→`fortify`; Impeccable: `shape`→`layout`→`polish`) are on the [resolution comment](https://github.com/petrose99/Wes-doc/issues/358#issuecomment-5757065383).

## Tickets created / changed
- Closed #358 with the resolution above.
- Created [#365 Build the Purchase Order document branch of the Bill template](https://github.com/petrose99/Wes-doc/issues/365), blocked_by #362 and #363, sub-issue of #353. Sized to 5 build steps, ≤8 captured states.
- Map #353: Decisions-so-far gained the #358 line; Not yet specified lost the "Purchase Order as the opened document" fog entry (now #365).

## Scores and counts (rendered surfaces only)
No score — this ticket decides content for a surface not yet built. #362 (Bill form sections) and #363 (PO match combobox/coding row) build the Bill shell first; #365 executes this decision once both land.

## First-pass findings → lessons (rendered surfaces only)
N/A — no rendered surface changed this session.

## Needs the owner
Nothing deferred. No removal decisions were required (Payment Details and supplier recent-invoices were never built for the PO branch — this is scoping a not-yet-built surface, not unplugging a shipped one).
