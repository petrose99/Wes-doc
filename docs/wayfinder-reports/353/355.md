# Autopilot report — #355 Decide the Bill template: takeover layout, header, status track, supplier card, line-item table, dates, footer verb

- Map: #353 Map: Document detail modelled on Ramp — full takeover, editable Bill and Receipt templates, coding that learns
- Type: wayfinder:grilling · Started: 2026-09-21 · Finished: 2026-09-21
- Outcome: resolved

## What I did
1. Claimed #355 (`gh issue edit 355 --add-assignee @me`).
2. Read the map (#353) Notes/Decisions-so-far, the day ticket (#354)'s resolution (view obligations O0–O5, density budget, signed unplug list, Action Summary), #233/#258 (status-vocabulary decision and its build), #329 (`Open in ‹provider›` pattern), #292 (ledger-stance gate), #259 (PaneFrame header build) — to ground every recommendation in an already-settled fact rather than taste.
3. Read the incumbent code: `components/pipeline/document-detail/split-pane.tsx`, `line-items-section.tsx`, and `app/(app)/workspaces/[workspaceId]/admin/suppliers/page.tsx` (the four Admin facts: trust, coding history, rules, payments) to see what a supplier card could draw from.
4. Confirmed scope against siblings already on the map — #356 owns the PO-combobox/coding-row internals, #358 owns the PO-as-document case — so this ticket places them without re-deciding their content.
5. Ran the grilling rounds per `docs/agents/design-tickets.md`'s agenda (Intent's `wireframe`/`fortify`, Impeccable's `critique`/`shape`/`layout`), 11 questions across 4 rounds, each with a grounded `➡️` recommendation; took every recommendation per the owner's standing delegation.
6. Posted the [resolution comment](https://github.com/petrose99/Wes-doc/issues/355#issuecomment-5756911233) (structure decisions, footer-verb state table, supplier-card scope, line-item table columns, dates row, Payment Details link, the pane-leftovers disposition, Class/Customer:Job placement, a `fortify` state inventory, and the Action Summary) and closed #355.
7. Created two chained build tickets and wired them: [Build the Bill takeover shell (#361)](https://github.com/petrose99/Wes-doc/issues/361) as a sub-issue of #353, and [Build the Bill form sections (#362)](https://github.com/petrose99/Wes-doc/issues/362) as a sub-issue of #353, blocked_by #361.
8. Updated #353's body: added the #355 line to Decisions so far, removed the now-resolved "Supplier card contents" and "Keyboard flow" fog bullets (the latter folds into the build tickets' `include` close-bar check, not a standalone decision), left "Receipt template detail" and "Purchase Order as the opened document" as-is (already own tickets #357/#358, outside this ticket's resolution) with a pointer added to #358, and kept "Phone" as fog.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| Q1 | Drag grip vs fixed split | Draggable grip, no persisted width | #354's signed section verdict ("a drag grip replaces it"); YAGNI on persistence until a real complaint |
| Q2 | Header composition | `Supplier — number`, `Open in ‹provider›` link (omitted with no provider/ledger line), `⋯` for the leftovers | #329's provider-link pattern; #234/#329's existing `⋯` fold |
| Q3 | Viewer chrome / Open file | Unchanged zoom/page/toggle; Open file becomes a persistent viewer icon | #354's section verdict + Action Summary `harden` step |
| Q4 | Status-track wording incl. Paid | Paid rides the fact sentence as a ledger fact, not a sixth state word; checks summary + Approval block underneath | #233 decision 2 (ledger facts stay separate from the state word); #354 O3 |
| Q5 | Footer verb per state | Approve (reviewer flow, disabled with reason) / Post (approved-reopened only) / Resolve (Exceptions) / none (touchless·cancelled·paid, read-only) | #354's day resolution: "Post is the end-of-day bulk act... the footer verb reads Post only when an approved document is reopened" |
| Q6 | Supplier card contents | Trust standing + payment terms only, capped recent-invoices disclosure; rules/full history stay in Admin | #354: "the card serve O1/O2 only"; day ticket already unplugged the create-a-rule card |
| Q7 | Line-item table columns | description·qty·unit·amount editable; account + PO-match as read-only chips; total as footer row, mismatch → inline check | Map Notes' line-items decision; #354's unplug of the duplicate-totals block |
| Q8 | Dates row | Invoice date · computed/overridable due date · posting date (Bill Pay only), one row not a card | #354: "regrouped supplier card · dates · money" replaces the 13-row field list |
| Q9 | Payment Details link content | Link + bank-account-state subtitle, opens the Bill Pay row, no duplicate form | Map Notes: "as a link to the Bill Pay row (no duplicate form)" |
| Q10 | Pane-leftovers disposition | Confirmed placement only — radiogroup→grip, source strip→viewer icon, tab strip→inline checks/Approval block/History disclosure | Already signed off by the owner on #354; no new leftover found |
| Q11 | Class/Customer:Job placement | Extra columns in the line-item coding row, absent (not disabled) when the provider doesn't expose them | #292's capability gate; #354's honesty-about-absence bar |

## Decisions recorded
Full detail in the [resolution comment](https://github.com/petrose99/Wes-doc/issues/355#issuecomment-5756911233): takeover shell structure (drag grip, header, viewer chrome, status track, footer verb switch, History disclosure), the Bill form's sections (supplier card scope, line-item table structure, dates row, Payment Details link, Class/Customer:Job placement), a six-row `fortify` state inventory, and the ordered Action Summary (Intent: `wireframe` → `fortify` → `articulate` → `specify`; Impeccable: `critique` (reused) → `shape` → `layout` → `clarify` → `adapt` → `polish`).

## Tickets created / changed
- Closed: [#355](https://github.com/petrose99/Wes-doc/issues/355) (this ticket).
- Created, sub-issues of #353: [Build the Bill takeover shell (#361)](https://github.com/petrose99/Wes-doc/issues/361); [Build the Bill form sections (#362)](https://github.com/petrose99/Wes-doc/issues/362), blocked_by #361.
- Map #353: Decisions-so-far gained the #355 line; Not-yet-specified lost "Supplier card contents" (resolved here) and "Keyboard flow" (folds into the build tickets' close bar); "Purchase Order as the opened document" annotated with its existing ticket #358.
- No fog newly surfaced beyond what #356/#357/#358 already cover.

## Needs the owner
None — nothing here required a removal beyond what #354 already had signed off, and no credential or judgment call fell outside the standing delegation.
