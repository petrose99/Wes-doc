# Autopilot report — #360 Build the first subtraction

- Map: #353 Ship the Ramp-style document view
- Type: Build · Started: 2026-09-20 (spec) · Finished: 2026-09-21 (close)
- Outcome: resolved

## What I did
1. Spec session: wrote the spec and pre-flight (`docs/wayfinder-reports/353/logs/scratch-360/spec.md`,
   `preflight.md`) for the #354 unplug list's stand-alone removals — Direction control, layout
   radiogroup → drag grip, source strip, tab strip flattening, Save review/Save note → autosave,
   rule card, duplicate totals, stepper folded into the status track.
2. Build sessions (steps 1, 3, 4–5, G1–G3, commits `e5d76d8`…`992fa47`): retired Direction end to
   end (UI, model default, tests); replaced the layout radiogroup with a new shared
   `components/queue/pane-resize-grip.tsx` (`PaneResizeGrip`/`usePaneResize`) drag divider; removed
   the source strip, rule card and duplicate totals; flattened the tab strip; wired autosave in
   place of Save review/Save note.
3. Measure session (commit `fa71022`): captured round r1 (5 states × 1440/390, 19/19 keyboard
   probes pass), ran `critique`/`evaluate`/`include` as fresh readers — critique 30/40 (H9 lowest,
   2), evaluate health 84/100 Clean with two P1s (grip affordance, a 390px clipped-overflow set),
   include 1440 Pass / 390 Fail.
4. Close session (this + prior): triaged r1's findings in `close.md` — F1 (grip affordance) real,
   fixed in `pane-resize-grip.tsx` (bigger hit target, visible border/glyph at rest, hover/drag
   states); F2 (390px clipped-overflow ×4) triaged as pre-existing detector residue already named
   in the area primer, verified against `git show` of the only #360 commit touching
   `split-pane.tsx`; H9=2 triaged as a round-script gap (the error/busy Send-for-review states were
   already implemented, just never captured) — closed by adding a
   `04-send-for-review-error` state to the round script, not a code fix.
5. Confirm rounds c1 (after the F1 fix) and c2 (after adding the missing capture state): gate CLEAN
   both times against their baselines (0 real findings, 0 new); readers re-run fresh — c1 evaluate
   96/100 Clean, c1 include Pass/Pass both widths, c1 critique 34/40; c2 critique (H9 re-scored
   only) 35/40, every heuristic ≥3.
6. Ran the full check suite once, dev server stopped first: `vitest run` (288 files / 2803 tests,
   all pass), `tsc --noEmit` clean, `eslint` on the 4 changed/touched files (0 errors), `next build`
   green.
7. Refreshed `docs/agents/areas/detail-pane.md`: added `PaneResizeGrip`/`usePaneResize` as the
   shared drag-divider primitive for #362/#364/#365 to reuse; residue section needed no change.
8. Appended the ticket's line to map #353's Decisions so far and closed #360.

## Questions I answered on the owner's behalf
None this session — the fix batch, triage and gate rounds all resolved on evidence already in
`close.md`/the gate JSON; nothing needed an owner call.

## Decisions recorded
#360 shipped the stand-alone removals from #354's unplug list plus the shared
`PaneResizeGrip`/`usePaneResize` drag-divider primitive that replaces the layout radiogroup.
Critique moved 20/40 → 35/40, evaluate 84 → 96 (Clean), no P0/P1 open, gate clean at both rounds.

## Tickets created / changed
No new tickets. No fog graduated (map #353's "Not yet specified" was already empty going in). No
findings crossed into out-of-scope.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=33 first-critique=30 close-critique=35 first-evaluate=94 close-evaluate=96
Gate: r1 CLEAN (19/19 probes) vs r0 baseline · c1 CLEAN (0 real, 0 new) vs r1 · c2 CLEAN (0 real, 0
new, 21/21 probes incl. new `send-for-review-error-{1440,390}` both `ok:true`) vs c1. Include: r1
1440 Pass / 390 Fail (F2) → c1 1440 Pass / 390 Pass. Weak pre-build step: Part B/edge table (B5)
named the Send-for-review error/busy states but the build's own round script (G1) never turned them
into a captured state — a scripting gap, not a spec gap (lesson recorded below).

## First-pass findings → lessons (rendered surfaces only)
| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| F1: grip has no visible affordance at rest (evaluate, P1) | Pre-flight B4/craft-floor has no check naming "a draggable divider must read as a control before hover/focus" | none | Fixed (visible border/glyph at rest); no existing lesson file line matched this class of finding — none written, as the fix is now baked into the new shared primitive itself (any future caller inherits the visible state) |
| F2: 390px clipped-overflow ×4 (evaluate P1 / include P1) | Area primer's pre-existing "Detector residue" entry for `clipped-overflow-container` on the pane's scroll/motion scaffolding | wrong-severity-not-wrong-lesson — the entry existed and correctly predicted this class, but the reader still had to be told at close time it wasn't a new regression | Verified via `git show` that #360's diff touches none of the flagged classes; triaged as residue, no fix, no ticket |
| H9 = 2: Send-for-review error/busy states uncaptured (critique) | Part B/edge table named the states; nothing forced the round script to include them | unchecked — existed as prose in the spec's edge table, never converted into a required round-script state | Lesson written to `scripts/wayfinder-autopilot/lessons.md` (docubite #360 entry): "every row in Part B/edge naming a busy/error state has a matching state name in the round script" — check named. Also fixed the immediate gap by adding the `04-send-for-review-error` capture state, which re-scored H9 to 3/4. |

## Needs the owner
Nothing deferred. No removal beyond what #354 already had the owner sign off by name (Direction,
radiogroup, source strip, tab strip, Save review/note, rule card, duplicate totals, stepper) — all
executed as specified, no additional removal proposed.
