milestone: spec-done
milestone: build-step-1
milestone: build-step-2
milestone: build-step-3
milestone: build-step-4

Ticket: Build the Bill takeover shell (#361), map #353. Spec + preflight
filled, gate cleared (critique 32/40 no H<3, evaluate P0/P1=0 Clean).
Scratch artifacts (spec.md, preflight.md) are gitignored and were not
readable from this worktree — later sessions must work from this
hand-off's prose + the doc comments in bill-pane.tsx/split-pane.tsx, which
carry the spec detail that mattered for each step.

## Artifacts
- components/pipeline/document-detail/bill-pane.tsx (+ .test.tsx)
- components/pipeline/document-detail/split-pane.tsx (FieldNavForm exported
  + BillSplitPane added, not yet wired to a route)

## Open findings
Preflight Part D's three severity-1 build-time notes still apply to the
rest of step 5 and step 6. `providerLink` (#355 Q2 "Open in <Provider>")
has no data source yet — no pushed-record external URL field exists on
`IntegrationPush`/`BillRow` — `BillSplitPane` always passes `providerLink:
null` until a later ticket adds that field; this is a real gap, not a
guess, flag it if spec §2 expected otherwise.

## Step plan (build sessions run one `todo` step each)

step: 1 — Split + header — done
step: 2 — Viewer icon row — done (folded into step 1's component)
step: 3 — Status track — done
step: 4 — Footer verb switch — done

step: 5 — History disclosure + Invoices wiring — todo (partly built this session)
  Done: `BillHistoryDisclosure` (Audit + Note, native `<details>` — no
  existing disclosure primitive fit; `components/ui/accordion.tsx` is the
  marketing FAQ's single-item, `question`-labelled component) added to
  bill-pane.tsx. `FieldNavForm` exported from split-pane.tsx with a new
  `submitId?: string` prop (default unchanged; `undefined` drops the id so
  it doesn't collide with the footer's `#save-review-submit`). New
  `BillSplitPane` in split-pane.tsx composes `BillPane` + `BillStatusTrack`
  + the reused `FieldNavForm` + `BillHistoryDisclosure` — the Invoices
  detail content, own `useRegisterDocumentActions` call (not double-called
  with `BillPane`'s). `tsc --noEmit` clean on both files (verified this
  session).
  Still open (not started): wire it in. `queueTitle === "Invoices"`
  uniquely marks the Invoices detail path (both embedded via
  `getQueueDetailAction`/`invoice-queue.tsx:312` and the `?full=1` route at
  `app/.../documents/[documentId]/page.tsx:294-363`) — swap the `splitPane`
  variable there to `<BillSplitPane>` when `queueTitle === "Invoices"`,
  building its `providerLink`/`fact`/`ledger`/`paidAt`/`blockedByCheck`/
  `escalated`/`approvalStatus`/`rejectedByActor`/`openReviewTaskId` props
  from the `processing`/`state`/`openReviewTask`/`ledgerPaid`/
  `confirmedPaid` values already computed in that file (~lines 92, 168,
  234-238) the same way the existing `stageIndicator` block does — no new
  queries needed. Full mode (`?full=1`) has no decision footer per #259;
  scope the swap to `embedded` only unless spec says otherwise. Then swap
  `invoice-queue.tsx`'s `paneActions` (line ~330, currently
  `<DocumentPaneActions .../>`) to `<BillFooterActions mode={...}
  connectionId={connectionId} .../>`, deriving `mode` from `BillRow`:
  approve when `!cancelledAt && !(status==="reviewed" && !openReviewTaskId)
  && status!=="queued" && status!=="failed"`, else `"post"` when
  `postEligible(bill)` (helper already in that file), else `"read-only"`.
  Check when done: `tsc --noEmit` clean; `vitest
  components/pipeline/document-detail/bill-pane.test.tsx
  components/pipeline/document-detail/split-pane.test.tsx` (if the latter
  exists — grep first).

step: 6 — Capture round + fixes — todo
step: G1 — round script — todo
step: G2 — r0 and fixes — todo
step: G3 — build-done — todo

## Next step
Build session finishes step 5: wire `BillSplitPane` into
`documents/[documentId]/page.tsx` (embedded path) and `BillFooterActions`
into `invoice-queue.tsx`'s `paneActions`, per the notes above, then `tsc
--noEmit` clean.
