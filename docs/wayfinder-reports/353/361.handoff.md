milestone: spec-done
milestone: build-step-1
milestone: build-step-2
milestone: build-step-3
milestone: build-step-4

Ticket: Build the Bill takeover shell (#361), map #353. Spec + preflight
filled, gate cleared (critique 32/40 no H<3, evaluate P0/P1=0 Clean).

## Artifacts
- docs/wayfinder-reports/353/logs/scratch-361/spec.md
- docs/wayfinder-reports/353/logs/scratch-361/preflight.md
- docs/wayfinder-reports/353/logs/scratch-361/spec-critic.md
- docs/wayfinder-reports/353/logs/scratch-361/step1-2-notes.md
- components/pipeline/document-detail/bill-pane.tsx (+ .test.tsx)

## Open findings
None blocking. See step1-2-notes.md for the spec.md §4 correction (ledger
"paid" is an existing pill prop, not a sentence suffix to add to
processing-fact.ts — simplifies step 3). Preflight Part D's three
severity-1 build-time notes still apply to steps 3-5.

## Step plan (build sessions run one `todo` step each)

step: 1 — Split + header — done
step: 2 — Viewer icon row — done (folded into step 1's component; see step1-2-notes.md)

step: 3 — Status track — done
  `BillStatusTrack` added to bill-pane.tsx: `StatusLine` (+ `trailing` Paid
  date node) + persistent checks-summary line + Approval block (Blocked/
  Escalated/Rejected sentence + Reject button when `approvalStatus ===
  "in_progress"`, reusing `updateReviewTaskStatusAction` + `ConfirmDialog`
  with document-actions.tsx's existing reject copy). Escalated sentence is
  generic ("waiting on a reviewer") — no per-document escalation-actor field
  in `BillRow`/spec allows no new query. Exported for step 5 to compose into
  `BillPane`'s `form` (BillPane itself still only owns the split/header/
  viewer-icon shell). 10 tests pass, `tsc`/`eslint` clean on both files.

step: 4 — Footer verb switch — done
  `BillFooterActions` added to bill-pane.tsx: one button for `"approve"`
  (reuses `updateReviewTaskStatusAction`/`moveDocumentsToStageAction`, same
  logic as `DocumentPaneActions.approve`, disabled while `blocked`) or
  `"post"` (`postSelectedDocumentsAction`, disabled with no `connectionId`),
  `null` for `"read-only"` so `actions={mode === "read-only" ? undefined :
  ...}` drops `PaneFrame`'s footer entirely. `"resolve"` needs no code here
  — `ExceptionQueue` already supplies its own `paneActions` (Start review /
  Resolve-with-reason popover) for a Bill opened from Exceptions; confirmed
  by reading `components/queue/exception-queue.tsx`. Kept `#save-review-
  submit` id on the one button in every enabled mode (#258). Added
  `BillReadOnlyContext`/`useBillReadOnly` (defaults to editable) for #362's
  form-section components to read without prop-drilling through `form`;
  the caller wires the Provider (step 5 / #362, not here). 13 tests pass
  (3 new), `tsc`/`eslint` clean.

step: 5 — History disclosure + Invoices wiring — todo
  Grep for an existing disclosure primitive before adding a `<details>`
  fallback (spec §6); move Audit+Note content into it. Switch Invoices queue
  to render `BillPane` instead of `SplitPane` (Bill Pay rows / PO documents
  stay on `SplitPane` until #363/#365). Check: `tsc --noEmit` clean.

step: 6 — Capture round + fixes — todo
  Round script covering the 10 states in spec's fortify table at 1440 and
  390 (`scripts/wayfinder-autopilot/capture-round.mjs`); fix anything the
  round or in-page detector surfaces against the #354 baseline residue
  counts named on the ticket.

step: G1 — round script — todo
step: G2 — r0 and fixes — todo
step: G3 — build-done — todo

## Next step
Build session runs step 5 — History disclosure + Invoices wiring.
