milestone: spec-done

Ticket: Build the Bill takeover shell (#361), map #353. Spec + preflight
filled, gate cleared (critique 32/40 no H<3, evaluate P0/P1=0 Clean).

## Artifacts
- docs/wayfinder-reports/353/logs/scratch-361/spec.md
- docs/wayfinder-reports/353/logs/scratch-361/preflight.md
- docs/wayfinder-reports/353/logs/scratch-361/spec-critic.md

## Open findings
None blocking. Three severity-1 build-time notes carried in preflight Part D
(disclosure-primitive grep-first, verb affordance beyond text, cite existing
Post/Resolve failure-toast copy rather than inventing new copy).

## Step plan (build sessions run one `todo` step each)

step: 1 — Split + header — todo
  New `components/pipeline/document-detail/bill-pane.tsx` (`BillPane`):
  `usePaneResize("bill-pane-split")` + `PaneResizeGrip` row (spec §1); header
  `<Supplier> — <number>` + `Open in <Provider>` link gated on capability +
  ledger line, omitted not greyed; `⋯` menu reusing `PaneMenu`/
  `DocumentMenuTopItems` + existing Send-for-review/Create-review-task/Delete
  actions (spec §2). Check: `tsc --noEmit`, no new menu/dialog primitive.

step: 2 — Viewer icon row — todo
  Extract existing zoom/page-nav/Invoice·Documents toggle out of
  `split-pane.tsx` into a shared `document-viewer-chrome.tsx` if not already
  isolated; add `Open file` persistent icon with visible `aria-label` (spec
  §3, lesson #261). Check: `grep -rn "sr-only" ...` finds none inside the new
  button.

step: 3 — Status track — todo
  Compose `StatusLine` + checks-summary line + Approval block under the
  header (spec §4); add `paidAt` to `processingFact`'s detail builder
  mirroring the existing `· Synced` suffix (`lib/documents/processing-fact.ts`).
  Check: no new state word outside `processingState()`'s map (lesson #258).

step: 4 — Footer verb switch — todo
  Wire `PaneFrame`'s `actions` slot per spec §5 table (Approve/Post/Resolve/
  none-read-only), keep `#save-review-submit` id (#258 reload-focus
  contract), derive+pass the shared `readOnly` boolean via context for #362.
  Check: `grep -rn "postAction\|sendForReviewAction\|createReviewTaskAction"`
  each has a pre-existing caller.

step: 5 — History disclosure + Invoices wiring — todo
  Grep for an existing disclosure primitive before adding a `<details>`
  fallback (spec §6); move Audit+Note content into it. Switch Invoices queue
  to render `BillPane` instead of `SplitPane` (Bill Pay rows / PO documents
  stay on `SplitPane` until #363/#365). Check: `tsc --noEmit` clean.

step: 6 — Capture round + fixes — todo
  Round script covering the 10 states in spec's fortify table at 1440 and
  390 (`scripts/wayfinder-autopilot/capture-round.mjs`); fix anything the
  round or in-page detector surfaces against the #354 baseline residue
  counts named on the ticket.

step: G1 — round script — todo
step: G2 — r0 and fixes — todo
step: G3 — build-done — todo

## Next step
Build session 1 of 4 (phase: build) runs step 1 — Split + header.
