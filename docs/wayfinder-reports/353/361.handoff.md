milestone: spec-done
milestone: build-step-1
milestone: build-step-2
milestone: build-step-3
milestone: build-step-4
milestone: build-step-5
milestone: build-step-6-g1

Ticket: Build the Bill takeover shell (#361), map #353. Spec + preflight
filled, gate cleared (critique 32/40 no H<3, evaluate P0/P1=0 Clean).
Scratch artifacts (spec.md, preflight.md) are gitignored and were not
readable from this worktree — later sessions must work from this
hand-off's prose + the doc comments in bill-pane.tsx/split-pane.tsx, which
carry the spec detail that mattered for each step.

## Artifacts
- components/pipeline/document-detail/bill-pane.tsx (+ .test.tsx)
- components/pipeline/document-detail/split-pane.tsx (FieldNavForm exported
  + BillSplitPane, wired)
- app/(app)/workspaces/[workspaceId]/documents/[documentId]/page.tsx
  (embedded Invoices path renders BillSplitPane; `ledger` const hoisted)
- components/queue/invoice-queue.tsx (paneActions now BillFooterActions)
- next.config.ts: `turbopack.root` now resolves two levels up when the
  checkout sits in a `*-lanes` dir (see the file's own comment) — the dev
  server would not start in this lane worktree at all otherwise (Turbopack
  panic: "Symlink [project]/node_modules is invalid, it points out of the
  filesystem root" — `node_modules` is symlinked back to the main checkout,
  outside a root pinned to the worktree itself). Verified: server listens
  on :3000, warmed `/invoices`, `/invoices/<id>` and `?full=1` (200s).
- docs/wayfinder-reports/353/logs/scratch-361/round.mjs — the ticket's G1
  round script (imports the shared `capture-round.mjs`); doc ids for
  approve/post-or-read-only/reject-eligible modes found via
  `query-bills.mjs` in the same folder (both scratch, gitignored, stay in
  this worktree). Ran once end-to-end against a warm server: 10 states
  captured, `by type` residue only (`ai-color-palette`, `overused-font`,
  `layout-transition`, `dark-glow`, `text-overflow`,
  `clipped-overflow-container` — all named in the area primer's residue
  list) before the run was killed intentionally partway through 390 width
  (proof-of-execution only, per G1 scope — not a real r0).

## Open findings
`providerLink` (#355 Q2 "Open in <Provider>") has no data source yet — no
pushed-record external URL field exists on `IntegrationPush`/`BillRow` —
`BillSplitPane` always passes `providerLink: null` until a later ticket
adds that field; this is a real gap, not a guess, flag it if spec §2
expected otherwise.

## Step plan (build sessions run one `todo` step each)

step: 1 — Split + header — done
step: 2 — Viewer icon row — done (folded into step 1's component)
step: 3 — Status track — done
step: 4 — Footer verb switch — done

step: 5 — History disclosure + Invoices wiring — done

step: 6 — Capture round + fixes — done (folded into G1, per hand-off history)
step: G1 — round script — done
step: G2 — r0 and fixes — todo
step: G3 — build-done — todo

## Next step
Build session runs step G2: start servers the area primer's way (`node
.impeccable/live/dev.mjs start`, `node .impeccable/live/livesrv257.mjs`),
run `node docs/wayfinder-reports/353/logs/scratch-361/round.mjs
docs/wayfinder-reports/353/logs/scratch-361/shots-r0`, then `node
scripts/wayfinder-autopilot/gate.mjs
docs/wayfinder-reports/353/logs/scratch-361/shots-r0 --residue-file
<residue-list-from-detail-pane.md>` and fix what it lists in place; stop
servers when done.
