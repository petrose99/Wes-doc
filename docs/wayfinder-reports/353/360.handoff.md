milestone: spec-done
milestone: build-step-1-done
milestone: build-step-2-code-done
milestone: build-step-3-done
milestone: build-step-4-5-done
milestone: build-step-G1-done
milestone: build-step-G2-done
milestone: build-done
milestone: measured

## Artifacts
- Spec: docs/wayfinder-reports/353/logs/scratch-360/spec.md
- Preflight: docs/wayfinder-reports/353/logs/scratch-360/preflight.md
- Baseline critique: .impeccable/critique/2026-09-21T00-00-00Z__split-pane-whole-pane-incumbent-353.md
- Area primer: docs/agents/areas/detail-pane.md
- tsc logs (clean): docs/wayfinder-reports/353/logs/scratch-360/tsc-step1.txt … tsc-step4-5.txt
- Round script: docs/wayfinder-reports/353/logs/scratch-360/round360.mjs (`node round360.mjs <round>`)
- Residue list: docs/wayfinder-reports/353/logs/scratch-360/residue.txt
- DB lookup helper: docs/wayfinder-reports/353/logs/scratch-360/find-docs.mjs

## What's done and verified
- Steps 1-5: see prior hand-off history (git log on split-pane.tsx, document-actions-menu.tsx,
  detail-pane.tsx, page.tsx) — Direction removal, duplicate totals fix, layout radiogroup → drag
  grip, source strip → floating Open-file button, rule card removed, Send for review → ⋯ menu.
  All tsc-clean, committed.
- G1 (round script): `round360.mjs` covers all five steps at 1440/390 against seeded invoice
  `fe267644-c4e9-417f-91ce-c6474c8869a4` (`invoices/<id>?full=1`) — no-Direction-row, one-totals-
  block, grip-present/source-strip-gone/floating-open-file (>=lg) vs. phone-strip-untouched
  (<lg), rule-card-gone, grip mouse-drag + Home/End/ArrowLeft keyboard probes, Send-for-review
  menu item + dialog-open + Escape focus-restore. Ran once end to end (`node round360.mjs test`):
  all probes passed except the dialog-open selector, which used `role=dialog` — fixed to match
  the actual `role=alertdialog` (`ConfirmDialog`'s real role); not re-run after the fix (that's
  G2's r0). Servers (`dev.mjs`, `livesrv257.mjs`) stopped after the test run.

## Open findings
None blocking. G3's B4 check found the resize grip was inline in split-pane.tsx, not the shared
component the spec required (B4: "keep it a single small component ... so #362/#364/#365 can
reuse it") — extracted to `components/queue/pane-resize-grip.tsx` (`usePaneResize` hook +
`PaneResizeGrip`), re-verified live (grip drag/keyboard probes pass, detector residue-only).
`tsc --noEmit` clean full-project. B1/B3 greps clean (no orphaned `AutomationRuleForm`/
`canCreateRule` import, one "Send for review" label). Steps 1-2's categoryConfirmed/other_charges
fixes still have no dedicated unit test (small, don't block on it alone — carry to close/measure).

## Measurement results (round 1)
Capture: 5 states, 0 errors, all keyboard probes pass (19/19).
Gate: CLEAN — 0 real findings (all residue per residue.txt).
Detector counts (r1 vs r0 baseline):
- 1440: all 3 states at 5 real findings each (ai-color-palette ×10, overused-font ×5, dark-glow ×5, 
  layout-transition ×5, text-overflow ×3) — down from #354 baseline (invoice 1440 = 9, lower as spec 
  predicts).
- 390: 2 states captured with 8 real findings each (4 × clipped-overflow-container plus the same 
  ai-color-palette/overused-font/layout-transition/dark-glow set) — **new, not in #354 baseline 390 = 0.**

Reader scores (r1):
- **Critique:** score 30/40, lowest H9 (Error recovery) at 2, P0/P1: none. All spec-verifiable checks 
  pass (Direction removed, totals dedup, grip present/draggable/keyboard, source strip gone, floating 
  Open-file, rule card gone, Send-for-review in menu/dialog/focus-restore). Gap: error/busy states for 
  Send-for-review not captured.
- **Evaluate:** health 84, verdict Clean, P0/P1: **F1 (P1 — grip control has no visible affordance 
  on desktop, undermines Use Case 2 discoverability), F2 (P1 — new clipped-overflow-container at 390px, 
  0→8 against spec's own baseline target).** All 13/13 functional/keyboard assertions pass. Edge cases 
  not all captured (stale sessionStorage fallback, Send-for-review error path, reviewLink-already-set, 
  PO pane, Escape-with-text preservation).
- **Include:** 1440 Pass (0 failures), 390 Fail with 2 issues (same clipped-overflow regression on both 
  states). P0/P1: P1 — 390-width clipped-overflow (4 selectors, both `01-invoice-pane-390` and 
  `03-send-for-review-dialog-390`, likely root cause in `lg:flex-row`/`overflow-hidden` chain shared 
  with grip change). Tab order/focus verified working both widths; pre-existing focus-ring visibility 
  gap noted (P3 coverage).

## Next step (resume here)
Measure phase complete (`milestone: measured`). Round 1 reveals two P1s (grip visual affordance, 
390px clipped-overflow regression) and one design-decision gap (error-path captures missing). Next 
session is close: triage the P1s, finalize findings, decide which are blockers vs. which carry to 
follow-up tickets, draft the report.
