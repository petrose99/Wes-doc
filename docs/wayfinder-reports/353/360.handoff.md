milestone: spec-done
milestone: build-step-1-done
milestone: build-step-2-code-done
milestone: build-step-3-done

## Artifacts
- Spec: docs/wayfinder-reports/353/logs/scratch-360/spec.md
- Preflight: docs/wayfinder-reports/353/logs/scratch-360/preflight.md
- Baseline critique: .impeccable/critique/2026-09-21T00-00-00Z__split-pane-whole-pane-incumbent-353.md
- Area primer: docs/agents/areas/detail-pane.md
- tsc logs (clean): docs/wayfinder-reports/353/logs/scratch-360/tsc-step1.txt, tsc-step2.txt, tsc-step3.txt

## What's done and verified
- Step 1 (Direction removal, commit e5d76d8): Direction radiogroup, categoryConfirmed/directionLockedFor
  gate on Save review, PO "always payable" footnote removed; `updateDocumentReview` sets
  `codingData.categoryConfirmed = true` on save for direction-bearing types. tsc clean;
  `vitest run models/documents.test.ts` pass.
- Step 2 (duplicate totals, commit already on branch — was committed as part of the spec-phase
  commit history, confirmed via `git log -- split-pane.tsx` at session start, no stray diff):
  `summaryFields={field.key === "line_items" ? summaryFields : []}` scopes the totals block to the
  line_items array field only, since `other_charges` is also array-type and was rendering the same
  summaryFields prop a second time. tsc clean.
- Step 3 (layout radiogroup → drag grip, commit 1fd224b): `PanelLayout` enum/`selectLayout`/
  `onLayoutKeyDown`/`layoutOption` removed; new `splitPct` state (35-65, default 52) replaces it.
  `sessionStorage["pane-layout"]` now stores a percentage; `parseSplitPct` tolerates an old
  `"split"`/`"details-only"`/`"source-only"` string from a still-open tab (falls back to default).
  Grip: `role="separator"` `aria-orientation="vertical"`, pointerdown/pointermove/pointerup drag
  (sessionStorage write on pointerup only, B2), keyboard Home/End/Arrow±2% (WCAG 2.5.7). Removed
  the collapse-only-source/collapse-only-details render branches entirely (out of scope per spec —
  source and details panels always both render now) and their five dead `max-w-2xl` conditionals.
  `sourceShown`/`toggleSource`/`SOURCE_KEY` (phone-lane source strip) untouched, still works
  identically — that mechanism is step 4's territory, not touched here. tsc clean (tsc-step3.txt,
  0 lines); eslint clean of new findings (one pre-existing set-state-in-effect warning carried
  over unchanged from before this ticket).

## Open findings
None blocking. Untested: no live-DOM verification yet of the grip drag/keyboard behavior — build
happened by code reading + tsc/eslint only (no dev server this session, time-boxed). Verify live at
G1/G2 capture round. Steps 1-2's categoryConfirmed/other_charges fixes still have no dedicated unit
test (flagged in prior hand-offs, unchanged — small, budget, don't block the ticket on it alone).

## Next step (resume here)
1. Build step 4 (spec §Section 4, spec.md:179-209): source strip → icon-button "Open file".
   Filename/Open-file-link/Hide-source-toggle strip at split-pane.tsx (now in the source panel,
   grep `header.filename` to relocate) becomes an icon-only floating button over `SourceViewer`
   (desktop only — phone lane's `sourceShown`/`toggleSource`/collapsible band from #257/#359 stays
   exactly as is, spec explicitly warns not to regress it). Grep
   `absolute.*top-\d.*right-\d` in `components/viewer/` first for an existing floating-chrome
   pattern to reuse (B4) before inventing one.
2. Then step 5 (spec §Section 5, spec.md:211-265): rule card removal + Send-for-review → ⋯ menu,
   mirroring `MoveDocumentMenuItem`/`MoveDocumentDialog` in `components/queue/document-actions-menu.tsx`
   + `components/queue/detail-pane.tsx`.
3. Then gates G1 (round script) → G2 (r0 + fixes) → G3 (tsc/Part-B/build-done) per the phase brief.
