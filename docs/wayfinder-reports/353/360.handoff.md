milestone: spec-done
milestone: build-step-1-done
milestone: build-step-2-code-done

## Artifacts
- Spec: docs/wayfinder-reports/353/logs/scratch-360/spec.md
- Preflight: docs/wayfinder-reports/353/logs/scratch-360/preflight.md
- Baseline critique: .impeccable/critique/2026-09-21T00-00-00Z__split-pane-whole-pane-incumbent-353.md
- Area primer: docs/agents/areas/detail-pane.md
- tsc logs (clean): docs/wayfinder-reports/353/logs/scratch-360/tsc-step1.txt, tsc-step2.txt

## What's done and verified
- Step 1 (Direction removal, commit e5d76d8): Direction radiogroup, categoryConfirmed/directionLockedFor
  gate on Save review, PO "always payable" footnote all removed from split-pane.tsx; dead imports
  cleaned. Server-side: models/documents.ts `updateDocumentReview` now sets
  `codingData.categoryConfirmed = true` on save for direction-bearing types (hasDirectionField),
  replacing the old two-step Direction-then-save confirm, so push/readiness eligibility isn't
  silently blocked. tsc clean; `vitest run models/documents.test.ts` (34) pass. No dedicated new
  test added for the categoryConfirmed branch — budget; flag at close if not added by then.
- Step 2 (duplicate totals, code done, NOT YET committed): root cause found by reading code, not
  live DOM (time-boxed) — `other_charges` is itself an array-type field ("array of
  {description, amount}", lib/doc-types.ts:90) alongside `line_items`. split-pane.tsx's
  `formFields.map` renders **every** array-type field through `<LineItemsSection>`, and was passing
  the same `summaryFields` (subtotal/tax_total/shipping_total/total) prop to all of them — so a
  template with both `other_charges` and `line_items` rendered the totals block twice, exactly
  matching #354's "once after line items, once after Other charges." Fixed the same way `po` was
  already scoped: `summaryFields={field.key === "line_items" ? summaryFields : []}`
  (split-pane.tsx ~line 440). tsc clean (tsc-step2.txt, 0 lines). **Not yet committed** — do that
  first in the next session, then run `npx vitest run components/pipeline/document-detail` (or the
  relevant test glob) if one exists before moving on.

## Open findings
None blocking. Untested: the categoryConfirmed server default (step 1) and the other_charges
double-render fix (step 2) have no direct unit/component test yet — both are small, mechanically
obvious diffs verified by tsc + existing suite; add a focused test if a session has budget, but
don't block the ticket on it alone.

## Next step (resume here)
1. `git add -A -- components/pipeline/document-detail/split-pane.tsx` and commit step 2
   (`wip(autopilot): #360 build step 2 — one totals block`), citing the other_charges root cause.
2. Continue the build plan from docs/wayfinder-reports/353/logs/scratch-360/spec.md §3–§5:
   step 3 (layout radiogroup → drag grip), step 4 (source strip → icon Open-file button), step 5
   (rule card removal + Send-for-review → ⋯ menu, mirroring MoveDocumentMenuItem/MoveDocumentDialog
   in components/queue/document-actions-menu.tsx + components/queue/detail-pane.tsx).
3. Then gates G1–G3 (capture round, detector/critique/evaluate vs the #354 baseline, tsc+tests+eslint).
Full step list and file targets are on the spec's "Assets" section and were also itemized in this
file before this rewrite — re-derive from spec.md §3–§5 if needed, it's authoritative.
