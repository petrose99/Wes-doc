milestone: spec-done
milestone: build-step-1-done
milestone: build-step-2-code-done
milestone: build-step-3-done
milestone: build-step-4-5-done

## Artifacts
- Spec: docs/wayfinder-reports/353/logs/scratch-360/spec.md
- Preflight: docs/wayfinder-reports/353/logs/scratch-360/preflight.md
- Baseline critique: .impeccable/critique/2026-09-21T00-00-00Z__split-pane-whole-pane-incumbent-353.md
- Area primer: docs/agents/areas/detail-pane.md
- tsc logs (clean): docs/wayfinder-reports/353/logs/scratch-360/tsc-step1.txt, tsc-step2.txt,
  tsc-step3.txt, tsc-step4-5.txt

## What's done and verified
- Steps 1-3: see prior hand-off history (git log on split-pane.tsx) — Direction removal, duplicate
  totals fix, layout radiogroup → drag grip. All tsc-clean, committed.
- Step 4 (source strip → floating icon button, spec §4): desktop (≥lg) strip (filename + Open-file
  link + Hide-source toggle) now `lg:hidden` — phone lane keeps it byte-for-byte (still reads
  `sourceShown`/`toggleSource`/`SOURCE_KEY`, #257/#359 untouched). New `relative` wrapper around
  `SourceViewer` carries an absolute `lg:flex` (hidden below `lg`) icon-only `<a>` (`ExternalLink`,
  `aria-label`+`title="Open file in a new tab"`, same `fileHref`/`target=_blank`/`rel`) top-right,
  `h-9 w-9` rounded-full `bg-white/90 backdrop-blur`. No existing floating-chrome pattern found in
  `components/viewer/` (grepped per B4) so this is the first one.
- Step 5 (rule card removed, Send for review → ⋯ menu, spec §5):
  - `canCreateRule`/`AutomationRuleForm` card deleted from `split-pane.tsx`; `canCreateRule` prop
    and its computation deleted from `page.tsx` (grepped, no other consumer); `defaultSupplier`
    prop pass deleted (the `supplier` var itself stays — still used for the pane title at
    `page.tsx:373`). `AutomationRuleForm` itself untouched, still used by Admin › Suppliers.
  - `CreateReviewTaskButton`'s inline form replaced by `SendForReviewMenuItem` →
    `SendForReviewDialog` in `components/queue/document-actions-menu.tsx`, same
    request-after-menu-closes / `ConfirmDialog` shell / `restoreFocusTo` shape as
    `MoveDocumentMenuItem`/`MoveDocumentDialog`. Wired into `PaneMenu`
    (`components/queue/detail-pane.tsx`): `sendForReviewOpen` state, item placed after Move (before
    the separator), dialog rendered above the popover alongside Delete/Move's. Absent when
    `doc.reviewLink` is set (existing "Open review task" item covers that case, untouched). Old
    `components/documents/create-review-task-button.tsx` deleted (dead, no other consumer after the
    swap — grepped first).
  - Copy unchanged: "Send for review" (menu item + dialog title), "Why does this need review?
    (optional)" placeholder, "Sent for review" success toast, existing error strings from
    `createReviewTaskAction`, untouched.
- `tsc --noEmit`: clean (tsc-step4-5.txt, 0 lines). `eslint` on the 4 changed files: 0 errors, the
  same 2 pre-existing `react-hooks/set-state-in-effect` warnings already present before this ticket
  (split-pane.tsx:128, detail-pane.tsx:63) — no new findings.

## Open findings
None blocking. Still no live-DOM verification (no dev server run this session, time-boxed, same as
steps 1-3) — everything built by code reading + tsc/eslint only. Verify all five steps live at
G1/G2. Steps 1-2's categoryConfirmed/other_charges fixes still have no dedicated unit test
(unchanged from prior hand-offs, small, don't block on it alone).

## Next step (resume here)
Context meter read 64866/110000 after step 4-5 code — over the 55K continue-building threshold, so
gates start next session. Build G1 (round script) → G2 (r0 + fixes) → G3 (tsc/Part-B/build-done) per
the phase brief, live-verifying all five steps (Direction removal, totals fix, drag grip, floating
Open-file button, Send-for-review menu item/dialog) at 1440 and 390, including the grip's
keyboard-resize probe and the Send-for-review dialog's focus-restore probe.
