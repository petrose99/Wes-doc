# Hand-off — #363 Build the PO match combobox (execution of #356)

milestone: spec-done
milestone: build-step-4
milestone: build-step-5

## Scope note
Split at spec time (sizing): #363 kept item 1 only (PO match combobox +
Match manually narrowing). Items 2–4 moved to #369, blocked on #363.

## step
step: 1 — model enrichment — done
step: 2 — `PoMatchCombobox` component — done
step: 3 — wire into `match-manually.tsx` — done
step: 4 — copy + keyboard pass — done
step: 5 — G1 round script (states 1–10, both widths, keyboard probe) — done
step: 6 — G2 r0 capture + fix batch against Part B checks — todo
step: 7 — G3 build-done (tsc clean, gate clean) — todo

## Artifacts
- Spec: `docs/wayfinder-reports/353/logs/scratch-363/spec.md`
- Round script (proven end-to-end):
  `docs/wayfinder-reports/353/logs/scratch-363/round-363.mjs`,
  residue list `docs/wayfinder-reports/353/logs/scratch-363/residue.txt`

## Open findings
Fixed at step 5: `findRowAndOpen`'s candidate gate checked
`[id$="-view-po"]`, which only renders once a row already has a confirmed
PO link (`canCompare` in `line-items-section.tsx`) — wrong gate for the
`po=none` (suggested/unlinked) states. Swapped the gate to the "Match
manually" button itself (renders whenever `po` + a summary with a
link/suggestions/removed exist) and switched navigation from
`domcontentloaded`+500ms to `networkidle` (the pane hydrates client-side
after DOM load; the shorter wait was reading the queue list before the
selected-row pane painted). Candidate pool capped at 10 rows per shape,
with a per-shape cache so states sharing a filter (1↔2, etc.) scan once.
`shots-r0-smoke` (now deleted — it was a smoke run, not r0) confirmed all
four keyboard probes fire (`arrow-nav-focus-on-input`,
`arrowdown-moves-to-option`, `escape-closes-returns-focus-to-trigger`,
`reject-po-present-when-linked`) and real detector findings surface at
both widths; one error (`02-confirm-suggested@390` — suggestion-row click
timeout) and the real findings are G2's to fix, not G1's.

## Next step
Start servers, run `node round-363.mjs shots-r0` from the scratch folder
as the real r0 capture, then `node
scripts/wayfinder-autopilot/gate.mjs shots-r0 --residue-file residue.txt`
and fix what it lists (including the 390-width suggestion-row click
timeout seen in the smoke run) — mark step 6 `done`, commit, continue to
G3.
