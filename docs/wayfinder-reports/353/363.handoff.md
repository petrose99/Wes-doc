# Hand-off — #363 Build the PO match combobox (execution of #356)

milestone: spec-done

## Scope note
Split at spec time (sizing): #363 kept item 1 only (PO match combobox +
Match manually narrowing). Items 2–4 (per-line coding row, Class/Job
columns, per-cell autosave) moved to [#369](https://github.com/petrose99/Wes-doc/issues/369),
blocked on #363. [#350](https://github.com/petrose99/Wes-doc/issues/350) should
be re-blocked on #369 (comment posted there; native dependency API 404'd
this session, needs doing via UI or a working token).

## step
step: 1 — model enrichment (`invoicedPercent`/`invoicedAmount` per PO) — todo
  **Correction to spec.md's "Model change" section**: do NOT call
  `summarizePoConsumption` from `summarizeInvoicePoLinks`/`listPoCandidates`
  — `summarizePoConsumption` itself calls `summarizeInvoicePoLinks`
  (`models/po-matching.ts:324`), so that would be infinite mutual
  recursion. Instead add a new standalone function (no recursive call),
  e.g. `summarizeInvoicedByPo(workspaceId, poIds): Promise<Map<string,
  {invoicedAmount: number; invoicedPercent: number | null}>>`, mirroring
  `summarizePoConsumption`'s per-PO math (`models/po-matching.ts:333-358`)
  directly against `documentMatch`+`target` rows (`matchType:
  "po_to_invoice"`, `sourceId: {in: poIds}`, `status: {not:
  REJECTED_MATCH_STATUS}`), using `poLinkKind`/`isComparedLink` inline per
  match (needs each PO's own `po_number` — fetch `pos` alongside).
  Then: (a) add `invoicedPercent: number | null` to the `PoLink` type
  (`models/po-matching.ts:19-29`) and `PoCandidate` type (line 143); (b) in
  `summarizeInvoicePoLinks`, after building `out` for all invoices, collect
  every `link.poDocumentId`/`suggestions[].poDocumentId` across the batch,
  call the new function once, and set `invoicedPercent` on each `PoLink`
  object (mutate — they're plain objects); (c) in `listPoCandidates`, do the
  same after `all.slice(0, CANDIDATE_PAGE)` (enrich only the returned page,
  not the full `all` array, to avoid the extra query on unreturned rows).
step: 2 — `PoMatchCombobox` component (trigger + popover + Suggested/Open groups + row markup) — todo
step: 2 — `PoMatchCombobox` component (trigger + popover + Suggested/Open groups + row markup) — todo
step: 3 — wire into `match-manually.tsx` (replace lines 137–191, keep Reject PO) — todo
step: 4 — copy + keyboard pass (arrow nav across groups, aria-labels, focus-restore ref) — todo
step: 5 — G1 round script (states 1–10, both widths, keyboard probe) — todo
step: 6 — G2 r0 capture + fix batch against Part B checks — todo
step: 7 — G3 build-done (tsc clean, gate clean) — todo

## Artifacts
- Spec: `docs/wayfinder-reports/353/logs/scratch-363/spec.md`
- Spec critic output: `docs/wayfinder-reports/353/logs/scratch-363/spec-critic.md`
  (critique 34/40, evaluate P0/P1=0/0 P2=1 P3=4, anti-pattern Clean — gate passed)

## Open findings
None outstanding — critic's one P2 (dedupe locus) resolved in spec.md
directly (client-side dedupe, `PoMatchCombobox`).

## Next step
Build session: implement step 1 exactly as corrected above (new
`summarizeInvoicedByPo` helper, no recursive call into
`summarizeInvoicePoLinks`), run `npx vitest run models/po-matching.test.ts`
if it exists, then step 2 (new component), reading spec.md's "Component:
PoMatchCombobox" section in full before editing. No code has been written
yet this ticket — this session only read the model file and found the
recursion hazard before editing.
