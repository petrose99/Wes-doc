# Hand-off — #363 Build the PO match combobox (execution of #356)

milestone: spec-done
milestone: build-step-4

## Scope note
Split at spec time (sizing): #363 kept item 1 only (PO match combobox +
Match manually narrowing). Items 2–4 moved to #369, blocked on #363.

## step
step: 1 — model enrichment — done
step: 2 — `PoMatchCombobox` component — done
step: 3 — wire into `match-manually.tsx` — done
step: 4 — copy + keyboard pass — done
step: 5 — G1 round script (states 1–10, both widths, keyboard probe) — todo
step: 6 — G2 r0 capture + fix batch against Part B checks — todo
step: 7 — G3 build-done (tsc clean, gate clean) — todo

## Artifacts
- Spec: `docs/wayfinder-reports/353/logs/scratch-363/spec.md`
- Round script (in progress, not yet proven end-to-end):
  `docs/wayfinder-reports/353/logs/scratch-363/round-363.mjs`,
  residue list `docs/wayfinder-reports/353/logs/scratch-363/residue.txt`

## Open findings
The combobox/Match manually bar only renders for an invoice whose template
has a `line_items` array field (`split-pane.tsx` `field.type === "array"`
gate) — the `?po=none`/`?po=mismatch` queue filters do not guarantee that
field exists on the row they return, so the round's first two attempts
(`?full=1` route, then a single `po=` filtered row) found rows with no
combobox at all. Current `round-363.mjs` scans candidate rows (the `po=`
filter's list plus the unfiltered newest 40) and opens Match manually on
each until one shows the wanted trigger text — this is written but its
last full-scan run (`shots-smoke4`) did not finish inside two 590s
foreground windows (scanning up to 40 candidate rows × opening the pane is
slow) and was still running when the context line hit; server processes
were killed via `dev.mjs stop` before hand-off, so no shots directory from
that run is trustworthy — delete `shots-smoke*` in the scratch folder
before the next attempt.

## Next step
Before re-running: narrow `findRowAndOpen`'s candidate pool (e.g. cap at
10–15 rows, or query `models/bills`/`po-matching` list functions directly
in a small one-off script instead of a live DOM scan) so the round finishes
well inside one foreground call. Then: start servers
(`node .impeccable/live/dev.mjs start af91555d-7450-4b21-a8ac-73db092617c8`),
run `node round-363.mjs shots-r0-smoke` from the scratch folder to prove it
executes end to end with the combobox actually reached (check
`keyboard.json` has more than the one `reject-po-present-when-linked`
probe — it must also show `arrow-nav-focus-on-input`,
`arrowdown-moves-to-option`, `escape-closes-returns-focus-to-trigger`),
stop servers, mark step 5 `done`, commit
(`wip(autopilot): #363 build step 5`), continue to G2.
