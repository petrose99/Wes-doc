# Hand-off — #362 Build the Bill form sections

milestone: spec-done
milestone: build step 1 in progress — supplier-summary query landed, wiring not started
milestone: build step 1 done — SupplierCard wired
milestone: build step 2 done — line-item table restructure
milestone: build step 3 done — dates row
milestone: build step 4 done — Payment Details link
milestone: build step 5 done — Class/Job columns + BillReadOnlyContext wiring
milestone: build step G1 done — round script written, smoke-run clean
milestone: build step G2 done — regression fixed, fixture swapped, gate clean
milestone: build-done
milestone: measured

## Artifacts
- `components/pipeline/document-detail/split-pane.tsx` — three real fixes, all committed:
  (1) gated the duplicate `DatesRow`/`PaymentDetailsLink` render to `field.key === "line_items"`
  (`other_charges` is also `type: "array"`); (2) added the missing
  `BillReadOnlyContext.Provider` import/wrap (context existed in `bill-pane.tsx` but nothing
  provided it — every `useBillReadOnly()` call read the `false` default); (3) fixed (2)'s import —
  `BillReadOnlyContext` wasn't in the `bill-pane` import list, causing a `ReferenceError` →
  render-error-boundary regression on all three bill states. All three verified via the round
  script, not by eye.
- `docs/wayfinder-reports/353/logs/scratch-362/round-362.mjs` — G2 fixes: swapped `EDITABLE` doc id
  (the original `038e009c…` was actually **Touchless** — read-only — not "editable" as labelled;
  new id `f8aae653…`, status `needs_review`, confirmed "In review" live); scoped the read-only tab-
  walk probe to *this ticket's sections* only (`line_items-*`/date/add/remove-row inputs) per
  spec.md:182-185 — the generic `FieldNavForm` fields (vendor, address, etc.) are #361's contract,
  not #362's, so a probe failing on `input#vendor` there is a false positive for this ticket;
  raised the editable-probe tab count 24→40 (table sits after ~10 header fields).
- `docs/wayfinder-reports/353/logs/scratch-362/find-editable.mjs` — one-off query script (devdb via
  `.impeccable/live` DATABASE_URL) that found the fixture bug; not needed again, gitignored scratch.
- Gate result: `node gate.mjs shots-r0 --residue-file residue.txt` → **CLEAN** — real findings 0,
  probes pass 4/4, errored states 0, page errors 0. 12 states × 1440/390.
- Both servers stopped (`dev.mjs stop`; `impeccable live-server` pid killed) at session end.

## Open findings
None outstanding for G2. Minor non-blocking curiosity for G3 (not a gate failure): the editable
tab-walk sequence shows `input#issue_date`/`input#due_date` each appearing 4× in a row — likely
`FieldNavForm` rendering per-page provenance duplicates rather than a real focus bug (same pattern
wasn't flagged by the detector or the probe's pass condition); worth a glance during G3 but not
required to close the ticket.

## Measure phase results

scores: critique 24/40, evaluate health ~15, include 1440@Pass / 390@Fail

- **Critique**: 24/40, lowest H8 (1). Key P1 findings: legacy field-dump rendering (02-bill-readonly-paid-390), legacy FieldNavForm fields still keyboard-reachable, first-viewport-column-overflow on desktop, low-contrast text at 390.
- **Evaluate**: health ~15 (bar ≥80), P0: 3, P1: 3, anti-pattern Duplicate summary. P0s: duplicate totals block (spec B8 bans), legacy field-dump instead of Supplier card on 02-bill-readonly-paid-390, read-only gating gap (vendor/supplier_address fields focusable on paid doc). P1s: unspecced "Other charges" table, low-contrast 2.5:1 at 390, header title truncation.
- **Include**: 1440@Pass, 390@Fail. Real finding: low-contrast 2.5:1 text (#90a1b9 on #f8fafc) on readonly-paid state — WCAG 1.4.3 fail. Keyboard tab order correct per spec B5; chips correctly non-interactive; unconfirmed: Payment Details link accessible name, Remove-line trash icon hover-only affordance.

Detector: r0 0 real, r1 0 real; new findings 0; all residue (ai-color-palette, text-overflow, clipped-overflow-container, overused-font, layout-transition, dark-glow, first-viewport-column-overflow, low-contrast).

## Raw findings (untriaged)
- `/home/ubuntu/Dev/Wes-doc-lanes/353-362/docs/wayfinder-reports/353/logs/scratch-362/readers-r1/critique.md`
- `/home/ubuntu/Dev/Wes-doc-lanes/353-362/docs/wayfinder-reports/353/logs/scratch-362/readers-r1/evaluate.md`
- `/home/ubuntu/Dev/Wes-doc-lanes/353-362/docs/wayfinder-reports/353/logs/scratch-362/readers-r1/include.md`

## Next step
Measure complete. Results show critical issues requiring close-phase triage: 3 P0 findings (duplicate totals block, legacy field-dump, read-only gating gap), 3 P1 findings, anti-pattern detected, evaluate health ~15 (far below bar of ≥80), critique score 24/40 (vs predicted 32). Close session will triage findings and determine path to closure.

## Build plan (one step per session; gates last, per phases/build.md)

- step: 1 — supplier card + `getSupplierSummaryForDocument` — done
- step: 2 — line-item table restructure: column order, read-only ledger-account/PO-match chips as inert spans, Add/remove line, footer total + mismatch check — done
- step: 3 — dates row (Invoice date, computed/override Due date via existing `lib/bills/due-date.ts` `inferDueDate`, Posting date gate) — done
- step: 4 — Payment Details link (verify Bill Pay route exists first — spec B5) — done
- step: 5 — Class/Customer:Job columns gated on #292 capability; wire BillReadOnlyContext across all new editable cells — done
- step: G1 — round script (capture-round.mjs, 10 states × 1440/390) — done
- step: G2 — r0 capture + fixes against Part B checks — done
- step: G3 — build-done (tsc clean, eslint on new files, affected vitest) — done
