# Hand-off — #362 Build the Bill form sections

milestone: spec-done
milestone: build step 1 in progress — supplier-summary query landed, wiring not started

## Artifacts
- `docs/wayfinder-reports/353/logs/scratch-362/spec.md` — full spec (sections 1-5, states, Part B contracts)
- `docs/wayfinder-reports/353/logs/scratch-362/preflight.md` — Parts A/B/D/E filled, gate met (32/40 predicted, Clean, critic-confirmed, no split needed)
- `models/supplier-summary.ts` — new, `getSupplierSummaryForDocument(workspaceId, supplierName, templateCode)`: resolves supplier via `resolveSupplier`, trust standing via `supplierThreshold` (mirrors `getSupplierTrust`'s arithmetic), payment terms via `formatTerms` (reused, `lib/payments/terms.ts`), bank-account fact same derivation as Admin's Panel, recent invoices (cap 5) by scanning 200 most-recent same-template documents and matching the extracted supplier field against the resolved supplier's alias set (`Document` has no `supplierId` column — confirmed against schema). `tsc --noEmit` clean, `eslint models/supplier-summary.ts` clean. **Not yet called by any UI file** — no caller yet, so this is dead code until step 1 finishes.

## Open findings
None new. Spec's named build-time check (Payment Details route existence, spec B5) still pending — belongs to step 4, not started.

## Next step
Finish build step 1: in `app/(app)/workspaces/[workspaceId]/documents/[documentId]/page.tsx`
(the sole `BillSplitPane` caller, ~line 371), call `getSupplierSummaryForDocument(workspaceId,
supplier, templateCode)` (both `supplier` and `templateCode` vars already exist in this file,
lines 114/165) gated to the `embedded && queueTitle === "Invoices"` branch only (avoid the extra
query on every document-page render), pass the result as a new `supplierSummary` prop through
`BillSplitPane` (`split-pane.tsx:447`) into a new `SupplierCard` component added to
`bill-pane.tsx` (reuse `Panel`/`Pill` from `components/automation/automation-ui.tsx`, `<details>`
native disclosure for recent invoices, per spec §1) — render it inside `BillSplitPane`'s `form=`
JSX (`split-pane.tsx:505-521`) above `FieldNavForm`. Then `tsc --noEmit` + `eslint` on all touched
files, screenshot at 1440/390 (matched + no-supplier-matched + disclosure open/closed states),
and only then move to build step 2 (line-item table restructure). Steps 2-5 and gates G1-G3 are
unstarted — see the full plan below, unchanged from spec.

## Build plan (one step per session; gates last, per phases/build.md)

- step: 1 — supplier card + `getSupplierSummaryForDocument` — **query landed, UI wiring todo**
- step: 2 — line-item table restructure: column order, read-only ledger-account/PO-match chips as inert spans, Add/remove line, footer total + mismatch check — todo
- step: 3 — dates row (Invoice date, computed/override Due date via existing `lib/bills/due-date.ts` `inferDueDate`, Posting date gate) — todo
- step: 4 — Payment Details link (verify Bill Pay route exists first — spec B5) — todo
- step: 5 — Class/Customer:Job columns gated on #292 capability; wire BillReadOnlyContext across all new editable cells — todo
- step: G1 — round script (capture-round.mjs, 10 states × 1440/390) — todo
- step: G2 — r0 capture + fixes against Part B checks — todo
- step: G3 — build-done (tsc clean, eslint on new files, affected vitest) — todo
