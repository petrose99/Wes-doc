# Hand-off — #362 Build the Bill form sections

milestone: spec-done
milestone: build step 1 in progress — supplier-summary query landed, wiring not started
milestone: build step 1 done — SupplierCard wired
milestone: build step 2 done — line-item table restructure
milestone: build step 3 done — dates row
milestone: build step 4 done — Payment Details link
milestone: build step 5 done — Class/Job columns + BillReadOnlyContext wiring
milestone: build step G1 done — round script written, smoke-run clean
milestone: build step G2 done — regression fixed, fixture swapped, gate clean
milestone: build-done
milestone: measured
milestone: close in progress — c1 fix batch + confirm round done, found the read-only-gating fix
  was broken; c2 fix applied (tsc clean), not yet captured/gated/re-read

## Artifacts
- `docs/wayfinder-reports/353/logs/scratch-362/close.md` — full triage, the c1 fix batch, the c1
  confirm-round result and why the read-only-gating fix was broken, and the c2 fix. Read this
  before anything else — do not re-derive the triage.
- `docs/wayfinder-reports/353/logs/scratch-362/{shots-c1,gate-c1.json,contact-sheet-c1.png,
  readers-c1/*.md}` — c1 round, superseded by c2 but kept as the regression record.
- `docs/wayfinder-reports/353/logs/scratch-362/round-362.mjs` — reusable round script, unchanged.
- Uncommitted: `components/pipeline/document-detail/{line-items-section,split-pane,
  field-row}.tsx` — all fixes (c1 + c2) are in the working tree, not yet committed.
- Both servers stopped at hand-off (`dev.mjs stop`, live-server pid killed).

## Open findings
Pointer only — see `close.md`'s "c1 confirm round result and c2 fix" section for the full story.
Headline: c1's `field-row.tsx` fix used the HTML `readOnly` attribute (stays focusable) instead of
`disabled` (removed from tab order) for bill-locked generic fields — critique's H5 caught it via
`keyboard.json`. c2 reworked it to use `disabled`; not yet verified against a live capture.

## Next step
1. Start servers (`docs/agents/areas/detail-pane.md` recipe).
2. `node round-362.mjs shots-c2` from the scratch folder, then
   `node gate.mjs shots-c2 --baseline shots-r0 --residue-file residue.txt --json gate-c2.json`.
3. Check `shots-c2/keyboard.json` `bill-readonly-tab-walk` no longer reaches `input#vendor` etc.,
   and `detector.json` no longer shows `undersized-ui-text` on `03-bill-cancelled`.
4. If clean: confirm round (contact sheet + 3 fresh sonnet critique/evaluate/include agents, same
   prompts as `readers-c1`, pointed at `shots-c2`) — this is gate round 2 of the 3-round cap.
5. If bar met (critique ≥32/40 no heuristic <3; evaluate ≥80, 0 P0/P1 in-scope; include Pass/Pass):
   run checks (tests → tsc → eslint → next build, server stopped), write lessons + area primer,
   write the report, close.
6. If a real finding remains: one more fix batch allowed (gate round 3, the cap), else hand off
   as a named gap.

## Build plan (one step per session; gates last, per phases/build.md)
- step: 1 — supplier card + `getSupplierSummaryForDocument` — done
- step: 2 — line-item table restructure — done
- step: 3 — dates row — done
- step: 4 — Payment Details link — done
- step: 5 — Class/Customer:Job columns + BillReadOnlyContext wiring — done
- step: G1 — round script — done
- step: G2 — r0 capture + fixes — done
- step: G3 — build-done (tsc/eslint/vitest) — done
- step: C1 — close triage + fix batch (duplicate totals, other-charges scope, read-only gating
  attempt) — done, superseded by C2 on the read-only-gating part
- step: C2 — fix `field-row.tsx` to use `disabled` not `readOnly` for bill-locked fields — code
  done, tsc clean, not yet captured/gated/re-read — todo
- step: C3 — confirm round 2 (capture, gate, 3 fresh readers) against the bar — todo
- step: C4 — checks, lessons, area primer, report, close — todo
