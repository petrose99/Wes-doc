# Hand-off — #362 Build the Bill form sections

milestone: spec-done
milestone: build step 1 in progress — supplier-summary query landed, wiring not started
milestone: build step 1 done — SupplierCard wired
milestone: build step 2 done — line-item table restructure
milestone: build step 3 done — dates row
milestone: build step 4 done — Payment Details link
milestone: build step 5 done — Class/Job columns + BillReadOnlyContext wiring

## Artifacts
- `docs/wayfinder-reports/353/logs/scratch-362/spec.md` — full spec (sections 1-5, states, Part B contracts)
- `docs/wayfinder-reports/353/logs/scratch-362/preflight.md` — Parts A/B/D/E filled, gate met
- `models/supplier-summary.ts` — `getSupplierSummaryForDocument`; `bankAccountFact` (already present,
  unchanged this step) feeds `PaymentDetailsLink`'s subtitle.
- `components/pipeline/document-detail/bill-pane.tsx` — step 4 adds `PaymentDetailsLink` export:
  link to `/workspaces/{workspaceId}/payments/bill-pay/{documentId}` (route confirmed to exist at
  `app/(app)/workspaces/[workspaceId]/(queue)/payments/bill-pay/[documentId]`, spec B5), subtitle
  mask `Account ····{last4}` / `"Needs bank details"` reused verbatim from
  `components/settings/payments-settings.tsx:129-130` (not a new mask, not paraphrased).
- `components/pipeline/document-detail/split-pane.tsx` — step 4: `FieldNavForm` gains
  `supplierBankAccountFact` prop, renders `PaymentDetailsLink` right after `DatesRow` in bill mode;
  `BillSplitPane` threads `supplierSummary.bankAccountFact` through.
- `tsc --noEmit`: no new errors from `bill-pane.tsx`/`split-pane.tsx`/`supplier-summary.ts` (grepped
  explicitly — pre-existing `@/prisma/client` cascade elsewhere, environment-only, unrelated).
  `eslint` on both touched files: 0 errors (1 pre-existing warning, unrelated `sessionStorage`
  effect line, unchanged). `vitest run models/bills.test.ts models/exceptions.test.ts`: 23 passed.
- No screenshots this session — headless capture is gate step G1/G2, round script not written yet.
- `components/documents/line-items-editor.tsx`: step 5 adds `classJob`/`readOnly` props to
  `LineItemsEditor`. `classJob` renders two Class/Customer:Job chip columns (reusing
  `LedgerAccountChip`, B4) but defaults `false` everywhere — grepped the codebase for a per-#292
  provider-capability check for class/job tracking and none exists (`lib/modules/capabilities.ts`
  is workspace-module gating, unrelated); ponytail comment on the columns names the ceiling
  (never render until that capability lands) and the upgrade path (pass the real flag once it
  exists). `readOnly` disables every editable cell (text/select/checkbox inputs, the Match-manually
  PO-line picker) and the Add/Remove-row buttons.
- `components/pipeline/document-detail/line-items-section.tsx`: `readOnly` prop passthrough to
  `LineItemsEditor` (no `classJob` threaded — stays at its `false` default, nothing sources it yet).
- `components/pipeline/document-detail/split-pane.tsx`: `LineItemsSection` call now passes
  `readOnly={billMode && billReadOnly}`, reusing the same `useBillReadOnly()` already wired to
  `DatesRow` — no second read-only flag (B4).
- `grep -rn "getSupplierSummaryForDocument" components app` → 2 hits (still wired). No `onClick` on
  the Class/Job chips — confirmed inert, matches B1's placeholder check.
- `tsc --noEmit`: no new errors. `eslint` on the three touched files: 0 errors (1 pre-existing
  warning, unrelated `sessionStorage` effect line in `split-pane.tsx`, unchanged).
  `vitest run models/bills.test.ts models/exceptions.test.ts`: 23 passed.

## Open findings
None new.

## Next step
Build step G1: write `scripts/wayfinder-autopilot/capture-round.mjs` (10 states × 1440/390,
detector JSON, keyboard probes for Part B focus/keyboard contracts).

## Build plan (one step per session; gates last, per phases/build.md)

- step: 1 — supplier card + `getSupplierSummaryForDocument` — done
- step: 2 — line-item table restructure: column order, read-only ledger-account/PO-match chips as inert spans, Add/remove line, footer total + mismatch check — done
- step: 3 — dates row (Invoice date, computed/override Due date via existing `lib/bills/due-date.ts` `inferDueDate`, Posting date gate) — done
- step: 4 — Payment Details link (verify Bill Pay route exists first — spec B5) — done
- step: 5 — Class/Customer:Job columns gated on #292 capability; wire BillReadOnlyContext across all new editable cells — done
- step: G1 — round script (capture-round.mjs, 10 states × 1440/390) — todo
- step: G2 — r0 capture + fixes against Part B checks — todo
- step: G3 — build-done (tsc clean, eslint on new files, affected vitest) — todo
