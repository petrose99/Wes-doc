# Hand-off — #362 Build the Bill form sections

milestone: spec-done
milestone: build step 1 in progress — supplier-summary query landed, wiring not started
milestone: build step 1 done — SupplierCard wired
milestone: build step 2 done — line-item table restructure
milestone: build step 3 done — dates row
milestone: build step 4 done — Payment Details link
milestone: build step 5 done — Class/Job columns + BillReadOnlyContext wiring
milestone: build step G1 done — round script written, smoke-run clean
milestone: build step G2 in progress — two real bugs found and fixed, one regression from the second fix needs diagnosis

## Artifacts
- `docs/wayfinder-reports/353/logs/scratch-362/round-362.mjs` — G2: fixed `--only` matching, added
  wait-for-attached before the Payment Details link check, a hydration wait on the read-only
  state, and `ok`/`reason` verdicts on all 3 keyboard probes (was "unjudged").
- `docs/wayfinder-reports/353/logs/scratch-362/residue.txt` — added 3 justified entries: the
  closed-`<details>` "View all" link's class chain (zero-size element, false-positive occlusion —
  confirmed absent when the disclosure is open), `first-viewport-column-overflow` (primer already
  flags this as pre-existing/structural for #361; same signature here), `#90a1b9 on #f8fafc`
  (traced to `stage-indicator.tsx`, unrelated shell component, not touched by #362).
- **Bug 1 (fixed)**: `components/pipeline/document-detail/split-pane.tsx` — `other_charges` is
  also `type: "array"` (`lib/domains/finance.ts:38`), so the `field.type === "array"` branch ran
  twice and rendered `DatesRow`/`PaymentDetailsLink` twice. Gated both to `field.key ===
  "line_items"`. Verified via a throwaway Playwright script (deleted) — one "Payment Details" link
  after the fix, was two before.
- **Bug 2 (fixed, needs re-verification)**: `BillReadOnlyContext` (`bill-pane.tsx:28`) had no
  `.Provider` anywhere in the app — `useBillReadOnly()` always read the `false` default. A
  read-only-pane tab walk landed on `input#vendor` (real B4 violation) confirmed this. Added
  `<BillReadOnlyContext.Provider value={billReadOnly}>` around `BillSplitPane`'s return in
  `split-pane.tsx` (~line 532), computed as `header.cancelled || state === "touchless" || paidAt
  !== null` (matches the three states line-items-editor.tsx's doc comment names: Cancelled/Paid/
  Touchless).
- **Regression from Bug 2's fix, NOT YET DIAGNOSED**: after adding the Provider, all three bill
  states (`01-bill-editable`, `02-bill-readonly-paid`, `03-bill-cancelled`) render the pane's error
  boundary ("Couldn't show this document's detail. Try again…") instead of the form — round went
  from 12 states/0 findings to 6 states with a broken pane in every screenshot. No `pageerror`/
  server stack trace surfaced yet (checked `node .impeccable/live/dev.mjs log`, only a Prisma
  workspace-scope warning unrelated to this). `docs/wayfinder-reports/353/logs/scratch-362/
  check-error.mjs` is written (adds `page.on("console", ...)` and `pageerror` listeners) but
  **not yet run** — that's the next step. Suspect the JSX restructure: `return
  <BillReadOnlyContext.Provider value={billReadOnly}>\n  <BillPane ... form={...} />\n
  </BillReadOnlyContext.Provider>` — check the diff for a missing/misplaced closing tag or a
  fragment issue (`BillPane` returns one element; wrapping it in `.Provider` without a fragment
  around a single child should be fine, but verify against the actual current file content, not
  this description).
- Servers stopped this session's end (`dev.mjs stop`); `livesrv257.mjs` was not explicitly
  killed — check `ps` for a stray node process on :8400 before starting a new round next session.
- No `git add`/commit this session — the two fixes are uncommitted working-tree changes in
  `components/pipeline/document-detail/split-pane.tsx`. Do not lose them; diagnose the regression
  from those exact edits (both are small, described above) rather than reverting blind.

## Open findings
See `check-error.mjs` above — run it first next session to get the actual browser console/page
error text, which will point straight at the regression's cause.

## Next step
Run `node .impeccable/live/dev.mjs start`, `node .impeccable/live/livesrv257.mjs`, then `node
check-error.mjs` in the scratch folder to capture the real error; fix it (likely a small JSX/scope
issue in the `BillReadOnlyContext.Provider` wrap just added); re-run `node round-362.mjs shots-r0`
(expect ~12 states again) and `node ../../../../../scripts/wayfinder-autopilot/gate.mjs shots-r0
--residue-file residue.txt` until it passes; then continue to step G3.

## Build plan (one step per session; gates last, per phases/build.md)

- step: 1 — supplier card + `getSupplierSummaryForDocument` — done
- step: 2 — line-item table restructure: column order, read-only ledger-account/PO-match chips as inert spans, Add/remove line, footer total + mismatch check — done
- step: 3 — dates row (Invoice date, computed/override Due date via existing `lib/bills/due-date.ts` `inferDueDate`, Posting date gate) — done
- step: 4 — Payment Details link (verify Bill Pay route exists first — spec B5) — done
- step: 5 — Class/Customer:Job columns gated on #292 capability; wire BillReadOnlyContext across all new editable cells — done
- step: G1 — round script (capture-round.mjs, 10 states × 1440/390) — done
- step: G2 — r0 capture + fixes against Part B checks — todo (two real bugs found & fixed; one regression from the fix needs diagnosis first, see Open findings)
- step: G3 — build-done (tsc clean, eslint on new files, affected vitest) — todo
