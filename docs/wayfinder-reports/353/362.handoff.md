# Hand-off — #362 Build the Bill form sections

milestone: spec-done
milestone: build step 1 in progress — supplier-summary query landed, wiring not started
milestone: build step 1 done — SupplierCard wired
milestone: build step 2 done — line-item table restructure

## Artifacts
- `docs/wayfinder-reports/353/logs/scratch-362/spec.md` — full spec (sections 1-5, states, Part B contracts)
- `docs/wayfinder-reports/353/logs/scratch-362/preflight.md` — Parts A/B/D/E filled, gate met
- `models/supplier-summary.ts` — `getSupplierSummaryForDocument`, now called from the document
  detail page's `embedded && queueTitle === "Invoices"` branch only.
- `components/pipeline/document-detail/bill-pane.tsx` — `SupplierCard` export (step 1).
- `components/documents/line-items-editor.tsx` — step 2: new optional `bill` prop (Bill-only,
  `null` for every other caller — Receipts/generic `FieldRow` unchanged). Adds inert
  `LedgerAccountChip` ("—" always, no coding data source yet) and `PoLineMatchChip` (Matched/
  Suggested/No match — only "none" reachable from Bill's current callers, spec §2) columns in
  both the wide table and narrow card layout, plus a footer total row (amber + mismatch sentence
  when the summed `amount` column disagrees with `bill.extractedTotal`, reusing `TotalField`'s
  variance-sentence pattern, not the component). Footer total is a state map keyed by row id,
  updated via the amount cell's own `onChange` (no ref reads during render — avoids the
  `react-hooks/refs` lint error the first attempt hit) and pruned on row removal — same-tick, per
  B2.
- `components/pipeline/document-detail/line-items-section.tsx` — new optional `billMode` prop;
  derives `{ extractedTotal, currency }` from `summaryFields`/`fieldValues` and passes `bill` to
  `LineItemsEditor` only when set.
- `components/pipeline/document-detail/split-pane.tsx` — `FieldNavForm` takes optional `billMode`
  (forwarded to `LineItemsSection`); `BillSplitPane` passes `billMode` (always true) on its
  `FieldNavForm` call. `SupplierCard` wired into `form=` above `FieldNavForm` (step 1).
- `app/(app)/workspaces/[workspaceId]/documents/[documentId]/page.tsx` — fetches the summary only
  in the Invoices-embedded branch, passes it through (step 1).
- `tsc --noEmit`: no new errors from any touched file (pre-existing `@/prisma/client`
  module-not-found cascade across unrelated `models/*.ts` files — Prisma client not generated in
  this environment, not caused by this ticket). `eslint` on all touched files: 0 errors (1
  pre-existing warning in `split-pane.tsx`, unrelated line — `sessionStorage` effect).
- No screenshots this session (headless capture deferred to gate step G1/G2 — round script not
  yet written). B1 check run: `grep -n onClick` on the two new chip components returns nothing —
  confirmed inert.

## Open findings
None new. Spec's named build-time check (Payment Details route existence, spec B5) still pending —
belongs to step 4, not started.

## Next step
Build step 3: dates row (Invoice date, computed/override Due date via `lib/bills/due-date.ts`
`inferDueDate`, Posting date gate) — spec §3, lines 130-141. Steps 4-5 and gates G1-G3 unstarted.

## Build plan (one step per session; gates last, per phases/build.md)

- step: 1 — supplier card + `getSupplierSummaryForDocument` — done
- step: 2 — line-item table restructure: column order, read-only ledger-account/PO-match chips as inert spans, Add/remove line, footer total + mismatch check — done
- step: 3 — dates row (Invoice date, computed/override Due date via existing `lib/bills/due-date.ts` `inferDueDate`, Posting date gate) — todo
- step: 4 — Payment Details link (verify Bill Pay route exists first — spec B5) — todo
- step: 5 — Class/Customer:Job columns gated on #292 capability; wire BillReadOnlyContext across all new editable cells — todo
- step: G1 — round script (capture-round.mjs, 10 states × 1440/390) — todo
- step: G2 — r0 capture + fixes against Part B checks — todo
- step: G3 — build-done (tsc clean, eslint on new files, affected vitest) — todo
