# Hand-off — #362 Build the Bill form sections

milestone: spec-done
milestone: build step 1 in progress — supplier-summary query landed, wiring not started
milestone: build step 1 done — SupplierCard wired
milestone: build step 2 done — line-item table restructure
milestone: build step 3 done — dates row

## Artifacts
- `docs/wayfinder-reports/353/logs/scratch-362/spec.md` — full spec (sections 1-5, states, Part B contracts)
- `docs/wayfinder-reports/353/logs/scratch-362/preflight.md` — Parts A/B/D/E filled, gate met
- `models/supplier-summary.ts` — `getSupplierSummaryForDocument`; step 3 added `paymentTermsDays`
  (raw net-days) to `SupplierSummary`'s matched variant, alongside the already-formatted
  `paymentTerms` string — `DatesRow`'s Due-date computation needs the number.
- `components/pipeline/document-detail/bill-pane.tsx` — `SupplierCard` (step 1); step 3 adds
  `DatesRow` export: Invoice date (`<input type="date">`, same markup class `FieldRow` uses, B4) ·
  Due date (read-only span + hidden input, computed via `lib/bills/due-date.ts` `inferDueDate`,
  recomputed client-side on Invoice-date change — B2 same-tick — when the supplier has payment
  terms; plain editable date input, same as Invoice date, when not) · optional Posting date cell
  (`postingDate` prop, not yet passed by any caller — Bill Pay wiring is #363/#365's).
- `components/documents/line-items-editor.tsx`, `line-items-section.tsx` — step 2 (see prior
  commits `wip(autopilot): #362 build step 2`).
- `components/pipeline/document-detail/split-pane.tsx` — step 3: `FieldNavForm` locates the
  invoice-date/due-date fields by key (`issue_date`/`date`, `due_date`), excludes them from the
  plain `FieldRow` loop in bill mode, and renders `DatesRow` right after `LineItemsSection`
  instead; new `supplierPaymentTermsDays` prop threaded from `BillSplitPane`'s `supplierSummary`;
  reads `useBillReadOnly()` directly (no new read-only prop, B4).
- `app/(app)/workspaces/[workspaceId]/documents/[documentId]/page.tsx` — step 1 (unchanged this
  session).
- `tsc --noEmit`: no new errors from any touched file (pre-existing `@/prisma/client`
  module-not-found cascade, environment-only, not caused by this ticket). `eslint` on all touched
  files: 0 errors (1 pre-existing warning in `split-pane.tsx`, unrelated `sessionStorage` effect
  line). `vitest run models/bills.test.ts models/exceptions.test.ts`: 23 passed (`models/
  approvals.test.ts` fails only on the same pre-existing Prisma-client import, unrelated).
- No screenshots this session — headless capture is gate step G1/G2, round script not written yet.

## Open findings
None new. Spec's named build-time check (Payment Details route existence, spec B5) still pending
— belongs to step 4, not started. Posting date cell is wired but unreachable (no Bill Pay caller
yet) — expected per spec §3, not a gap in this step.

## Next step
Build step 4: Payment Details link (verify Bill Pay route exists first — spec B5, §4).

## Build plan (one step per session; gates last, per phases/build.md)

- step: 1 — supplier card + `getSupplierSummaryForDocument` — done
- step: 2 — line-item table restructure: column order, read-only ledger-account/PO-match chips as inert spans, Add/remove line, footer total + mismatch check — done
- step: 3 — dates row (Invoice date, computed/override Due date via existing `lib/bills/due-date.ts` `inferDueDate`, Posting date gate) — done
- step: 4 — Payment Details link (verify Bill Pay route exists first — spec B5) — todo
- step: 5 — Class/Customer:Job columns gated on #292 capability; wire BillReadOnlyContext across all new editable cells — todo
- step: G1 — round script (capture-round.mjs, 10 states × 1440/390) — todo
- step: G2 — r0 capture + fixes against Part B checks — todo
- step: G3 — build-done (tsc clean, eslint on new files, affected vitest) — todo
