# Hand-off — #362 Build the Bill form sections

milestone: spec-done
milestone: build step 1 in progress — supplier-summary query landed, wiring not started
milestone: build step 1 done — SupplierCard wired

## Artifacts
- `docs/wayfinder-reports/353/logs/scratch-362/spec.md` — full spec (sections 1-5, states, Part B contracts)
- `docs/wayfinder-reports/353/logs/scratch-362/preflight.md` — Parts A/B/D/E filled, gate met
- `models/supplier-summary.ts` — `getSupplierSummaryForDocument`, now called from the document
  detail page's `embedded && queueTitle === "Invoices"` branch only.
- `components/pipeline/document-detail/bill-pane.tsx` — new `SupplierCard` export (reuses
  `Panel`/`Pill` from `automation-ui.tsx`, native `<details>` recent-invoices disclosure per
  spec §1, "No supplier matched" fallback line verbatim from #355).
- `components/pipeline/document-detail/split-pane.tsx` — `BillSplitPane` takes an optional
  `supplierSummary` prop, renders `SupplierCard` inside `form=` above `FieldNavForm`.
- `app/(app)/workspaces/[workspaceId]/documents/[documentId]/page.tsx` — fetches the summary only
  in the Invoices-embedded branch, passes it through.
- `tsc --noEmit`: no new errors from these 4 files (pre-existing `@/prisma/client` module-not-found
  cascade across many unrelated `models/*.ts` files — Prisma client not generated in this
  environment, not caused by this ticket). `eslint` on all 4 touched files: 0 errors (1 pre-existing
  warning in `split-pane.tsx` unrelated to this change).
- No screenshots this session (headless capture deferred to gate step G1/G2 per the phase brief —
  round script not yet written).

## Open findings
None new. Spec's named build-time check (Payment Details route existence, spec B5) still pending —
belongs to step 4, not started.

## Next step
Build step 2: line-item table restructure (column order, read-only ledger-account/PO-match chips
as inert `<span>`s, Add/remove line, footer total + mismatch check) — spec §2, lines 88-129;
`components/documents/line-items-editor.tsx` is the existing table shell to restructure (B4 — no
new table primitive), `line-items-section.tsx:27-60`'s `TotalField` variance-sentence pattern to
reuse for the extracted-total mismatch. Steps 3-5 and gates G1-G3 unstarted.

## Build plan (one step per session; gates last, per phases/build.md)

- step: 1 — supplier card + `getSupplierSummaryForDocument` — done
- step: 2 — line-item table restructure: column order, read-only ledger-account/PO-match chips as inert spans, Add/remove line, footer total + mismatch check — todo
- step: 3 — dates row (Invoice date, computed/override Due date via existing `lib/bills/due-date.ts` `inferDueDate`, Posting date gate) — todo
- step: 4 — Payment Details link (verify Bill Pay route exists first — spec B5) — todo
- step: 5 — Class/Customer:Job columns gated on #292 capability; wire BillReadOnlyContext across all new editable cells — todo
- step: G1 — round script (capture-round.mjs, 10 states × 1440/390) — todo
- step: G2 — r0 capture + fixes against Part B checks — todo
- step: G3 — build-done (tsc clean, eslint on new files, affected vitest) — todo
