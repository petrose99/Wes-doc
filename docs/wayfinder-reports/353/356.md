# Autopilot report — #356 Decide the PO match combobox and the per-line coding row

- Map: #353 Map: Document detail modelled on Ramp — full takeover, editable Bill and Receipt templates, coding that learns
- Type: wayfinder:grilling · Started: 2026-09-21 · Finished: 2026-09-21
- Outcome: resolved

## What I did
1. Claimed #356 (`gh issue edit 356 --add-assignee @me`).
2. Read the map (#353) and the ticket, plus `docs/agents/design-tickets.md`'s "Grilling a design ticket" section for the Intent/Impeccable round agendas.
3. Established facts from code before asking anything: `match-manually.tsx` (existing Suggested list + search panel + confirm/replace/reject actions from #228/#250), `line-items-section.tsx` (PO chip + View PO/Match manually toggles), `models/po-matching.ts` (`InvoicePoSummary`/`PoLink` fields — no requester/buyer, confirming #228 Q8), `rationale-popover.tsx` (`SOURCE_BADGE`, closed #334's badge/Keep decisions), `models/coding-corrections.ts` (`recordCodingCorrection`), and the open #362 (Build the Bill form sections) which already stubs the PO-match chip and Class/Job column this ticket had to supply content for.
4. Ran one grilling round, six questions, all recommendations taken (owner standing delegation). Posted the resolution as a comment and closed #356: https://github.com/petrose99/Wes-doc/issues/356#issuecomment-5756957926
5. Created the execution ticket [Build the PO match combobox and per-line coding row (#363)](https://github.com/petrose99/Wes-doc/issues/363), wired as a sub-issue of #353, blocked on #362.
6. Re-pointed [Build the in-place coding memory (#350)](https://github.com/petrose99/Wes-doc/issues/350)'s blocking dependency from the now-closed #356 to #363, so it still waits on the actual build rather than a resolved decision ticket.
7. Updated #353's Decisions so far with a one-line gist and link for #356 and #363.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | What the combobox's two groups are | Suggested = `summary.suggestions`; Open = `listPoCandidatesAction` results, reusing "Open" as the PO lifecycle word | #228 Q8 already uses Open/Fully invoiced on the PO queue — one vocabulary, not a new one |
| 2 | What the secondary line says | `"{invoicedPercent}% of {poTotal} invoiced"`, requester dropped | No requester field exists; #228 Q8 already ruled out Buyer on this object; `fortify`'s real-conditions check |
| 3 | Combobox vs #228's Match manually mode | Combobox replaces the bar's own picker (calls the same `confirmPoMatchAction`/`replacePoMatchAction`); Match manually narrows to per-line reassignment + Reject PO | Nielsen H4 consistency; reuse over duplicate; #228 Q7/Q10/Q13/Q14 untouched |
| 4 | Category/account/badge/Keep placement | #334's design unchanged, scoped to one line's Category cell | #334 closed and named as an input on #356, not reopened |
| 5 | Class/Customer:Job columns | Same row, same control shape as Category; new minimal provider-capability + synced-list model mirroring `CategoryAccountMapping`; absent not disabled | Matches #362 Q5's language already on the table-structure ticket |
| 6 | Autosave contract | Per-field blur save, per-cell spinner→check affix, per-cell revert+error on failure, `recordCodingCorrection` unchanged | Map Notes' "autosave per edit" narrowed to field level; H1 visibility of system status |

## Decisions recorded
See the resolution comment (https://github.com/petrose99/Wes-doc/issues/356#issuecomment-5756957926) for the full Q&A and Action Summary. Gist: one PO-pick combobox (Suggested/Open groups, invoiced-percent secondary line, no requester) replaces the existing two-entry-point picker in `match-manually.tsx`; the per-line coding row reuses #334's Category/account/badge/Keep design scoped to a table row; Class/Customer:Job ride the same control shape, gated on provider capability; every line-row field autosaves per cell with inline spinner/check/error feedback.

## Tickets created / changed
- Created [Build the PO match combobox and per-line coding row (#363)](https://github.com/petrose99/Wes-doc/issues/363) — sub-issue of #353, blocked on [#362](https://github.com/petrose99/Wes-doc/issues/362).
- Re-blocked [#350](https://github.com/petrose99/Wes-doc/issues/350) onto #363 (in addition to its existing, now-closed #356 dependency).
- Closed #356 with the resolution comment.
- No fog graduated beyond what's already covered by #363; no scope ruled out.

## Scores and counts (rendered surfaces only)
Not applicable — decision ticket, no rendered surface built or changed this session.

## First-pass findings → lessons (rendered surfaces only)
Not applicable.

## Needs the owner
Nothing deferred. No removal, credential, or genuinely ambiguous call arose — every recommendation was grounded in an existing map decision (#228, #334, #362) or a fact already in the codebase (no requester field, no existing Class/Job model).
