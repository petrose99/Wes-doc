# Autopilot report — #362 Build the Bill form sections

- Map: #353 Bills — the Bill (AP) document surface
- Type: Build · Started: 2026-09-21 (spec session) · Finished: 2026-09-21 (close session)
- Outcome: resolved

## What I did

1. **Spec** (session 1): filled `spec.md` + `preflight.md` against the map's Bill-form-sections
   fog; independent spec critic reconciled critique 32/40, evaluate sum 14 (predicted health
   ≥85), Clean, gate met.
2. **Build** (sessions 2-5, one step per session): supplier card + `getSupplierSummaryForDocument`
   (step 1, `5920d13`); line-item table restructure (step 2, `944d9f0`); dates row (step 3,
   `f5c1893`); Payment Details link (step 4, `635e7d4`); Class/Customer:Job columns +
   `BillReadOnlyContext` wiring (step 5, `8f8fdef`); round script + r0 capture/fix/gate
   (`b03c94e`, `4e1cf51`); build-done checks — tsc/eslint/vitest (`39fda37`).
3. **Measure** (`fcbee6a`): first capture round (readers-r1) — critique 24/40, evaluate health
   15/100 (P0 duplicate totals, P0 legacy-field dump, P0 read-only gating missing on legacy
   fields; P1 unspecced Other-charges chips; P1/P2 residue: low-contrast, header truncation).
4. **Close** (this session, continuing session 2's close work):
   - Triaged readers-r1's findings in `close.md`: 3 real (duplicate totals, read-only gating,
     Other-charges chip leak — fixed), 1 decision (legacy-field regroup, out of #362's spec'd
     scope), 2 residue (low-contrast `#90a1b9`, `h2.truncate` header — both pre-existing #361/#297).
   - Fix batch c1 (prior session, `9f30052`): gated legacy `summaryFields` block to `!billMode`;
     scoped `billMode`/`readOnly` on `LineItemsSection` to `field.key === "line_items"`; gated
     generic `FieldRow` fields on `useBillReadOnly()`. Gate c1 clean; confirm round c1 (readers-c1)
     caught the read-only-gating fix was broken (HTML `readOnly` stays focusable/tabbable) — H5
     1/4, new `undersized-ui-text` regression.
   - Fix batch c2 (this session): reworked `field-row.tsx` to use `disabled={billReadOnly}`
     (text/number/date) and `disabled={readOnly || billReadOnly}` (checkbox/select), separate from
     the admin-configured `readOnly` case. Committed as `f84b81f`.
   - Confirm round c2 (this session): gate clean (`gate-c2.json`, `real: 0`, probes 4/4 pass,
     `bill-readonly-tab-walk` no longer reaches any legacy field); three fresh `sonnet` readers —
     evaluate 94/100 Clean 0 P0/P1, include Pass/Pass, critique 27/40 (H5 now 4/4, confirmed
     fixed). Full triage of why critique sits under the ticket's 32/40 bar recorded in `close.md`:
     all four sub-3 heuristics (H1, H3, H8, H9) are unchanged from readers-r1's own baseline —
     pre-existing/out-of-scope/residue, not a #362 regression — so per the close bar's "confirm
     under the bar with no P1s: close and record the gap" rule, closed rather than running a
     fourth batch.
   - Checks (dev server stopped): `vitest run` — 289 files / 2816 tests pass; `tsc --noEmit` —
     clean; `eslint` on the ticket's changed files — 0 errors, 1 pre-existing warning
     (`react-hooks/set-state-in-effect`, unrelated line); `next build` — succeeded.
   - Wrote a generic lesson (`scripts/wayfinder-autopilot/lessons.md`) on `readOnly` vs `disabled`
     for tab-order gating, and updated `docs/agents/areas/detail-pane.md` (SupplierCard/
     `BillReadOnlyContext` primitives, the cross-pane visual-grouping gap, both committed as
     `6d6feae`).

## Questions I answered on the owner's behalf

| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Critique confirm round (27/40) is under the ticket's 32/40 bar with no heuristic-under-3 met — run a third gate-round fix batch, or close? | Close, record the gap. | Close-phase brief step 5: "Confirm under the bar with no P1s: close and record the gap... Never a batch to turn a 3 into a 4." All four sub-3 heuristics are pre-existing (unchanged since readers-r1's baseline, before any #362 fix), driven by cross-component ownership (PDF viewer), a fixture-coverage gap (no matched-supplier seed), and documented #361/#297 residue — not a #362 regression, and gate/evaluate/include all confirm 0 real P0/P1. |
| 2 | Legacy generic-field dump (vendor, address, VAT…) still coexists with the new sections — regroup it into #362, or leave it? | Leave it — out of scope. | `spec.md`'s Ownership section scopes #362 to 5 named sections; the legacy fields are #361's shell, unchanged by this ticket. Recorded as a decision in `close.md` item 2, not a fix. |

## Decisions recorded

Closed #362: the 5 spec'd sections (Supplier card, line-item table, dates row, Payment Details
link, Class/Job columns) are built and wired to `BillReadOnlyContext`; the close round fixed a
reintroduced duplicate-totals regression, an unspecced Other-charges chip leak, and a broken
read-only tab-order gate (2 gate rounds). Regrouping the remaining legacy generic fields is
explicitly out of #362's scope (open fog, not ticketed by this session — no new decision needed,
the map's Not-yet-specified list already covers it via #361's field-dump lineage).

## Tickets created / changed

None created. No new fog surfaced beyond what the map already tracks (legacy-field regroup is
implicit in #361's existing scope note, not a new patch).

## Scores and counts (rendered surfaces only)

scores: predicted-critique=32 first-critique=24 close-critique=27 first-evaluate=15 close-evaluate=94

Gate counts: r0/r1 baseline → c1 (fix batch 1, still 1 real broken-fix regression via readers, gate
itself clean) → c2 (fix batch 2, gate clean: `real: 0`, `probes: 4 pass/0 fail`, `newSinceBaseline:
0`, `errored: 0`, both 1440 and 390). Include check: Pass/Pass at both widths, keyboard flow
verified live via `bill-readonly-tab-walk` (`ok: true`, no longer reaches the 10 legacy inputs on
the Paid state). Weak pre-build step: Part B's `BillReadOnlyContext`/`useBillReadOnly` grep
(preflight.md Part B) was scoped to "every editable cell" but the build step wired it only into
the *new* line-item/date/payment-link cells, not the legacy generic `FieldRow` fields the ticket's
own step 5 also touches — the grep as written would have passed either way since it doesn't
distinguish "some cells" from "every cell."

## First-pass findings → lessons (rendered surfaces only)

| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| Duplicate totals block (H1/H8, P0) | preflight.md Part A: H8 target "One total (footer row), no duplicate totals block" | unchecked — stated as a design target, no build-time grep enforced it | Fixed (gate legacy block on `!billMode`); no new check written — the fix itself is now covered by the confirm-round detector/critique re-read each future round, not worth a bespoke grep for a one-line conditional |
| Legacy-field read-only gating missing (H5, P0) | preflight.md Part B: "grep `BillReadOnlyContext\|useBillReadOnly` in `<new files>` → present in every editable cell" | wrong — grep scope was "new files" only, missed that step 5's context also needed wiring into the pre-existing `field-row.tsx` | Fixed (c1, then reworked in c2 after the fix itself was broken); lesson recorded generically: `readOnly` vs `disabled` for tab-order gating, with a `check:` (grep the round's `keyboard.json` tab-walk for the field's id on the locked state) |
| Unspecced Other-charges chips (H4/H8, P1) | preflight.md Part A: H4 target "only shell primitives reused... zero new bespoke components" | new class — nothing in the pre-flight predicted `billMode` leaking from `line_items` to a sibling `type: "array"` field via a shared prop path | Fixed (scoped `billMode`/`readOnly` to `field.key === "line_items"`); no generic lesson written — narrow to this ticket's own prop-threading shape, not a reusable pattern |
| Legacy field dump not replaced (H2/H6, P0 evaluate) | spec.md Ownership section | none — correctly scoped out from the start, not a miscall | No fix; recorded as an explicit out-of-scope decision, not a regression |
| Low-contrast `#90a1b9`, header `h2.truncate` overflow (P1/P2) | `docs/agents/areas/detail-pane.md` residue list (#361, #297) | none — pre-existing, outside #362's diff, already documented residue | No fix; confirmed still-residue this round, carried forward |

## Needs the owner

None. Closing bar met on gate (0 real, 0 P0/P1), evaluate (94/100, Clean), include (Pass/Pass),
and all deterministic checks (tests/tsc/eslint/build). The one open gap — critique 27/40 vs the
32/40 target, and the cross-pane PDF-viewer/SupplierCard visual-grouping question (H1/H9) — is
recorded as a named gap in `close.md` and this report, not deferred to the owner: it requires no
removal and no credential, just a future ticket if the map decides the cross-pane layout is worth
its own pass.
