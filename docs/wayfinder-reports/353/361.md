# Autopilot report — #361 Build the Bill takeover shell

- Map: #353 Map: Document detail modelled on Ramp — full takeover, editable Bill and Receipt templates, coding that learns
- Type: wayfinder:task · Started: 2026-09-21 (spec session) · Finished: 2026-09-21 (close session)
- Outcome: resolved

## What I did

1. **Spec** (`b3b0c61`): filled spec.md/preflight.md against the #355 decision
   comment (drag-grip split, header with Open-in-‹provider›, status track with
   Paid as a ledger fact, footer verb switch, supplier card, dates row,
   Payment Details link, viewer chrome) and #359's keyboard/phone-sheet
   contracts.
2. **Build**, one step per session:
   - `5daed7c` step 1-2 — split + header shell, viewer icon row folded in.
   - `6524589` step 3 — status track.
   - `53e182a` step 4 — footer verb switch.
   - `6889972` step 5 — history disclosure + Invoices wiring.
   - `7d95cc0` step 6/G1 — capture round script, every keyboard probe judged.
   - `8d9f022` step G2 — r0 capture + gate; two remaining detector findings
     (`low-contrast` #90a1b9/#f8fafc, `first-viewport-column-overflow`)
     confirmed unlocated after a targeted live-DOM scan and residue-listed
     (`g2-open-findings.md`); a real, unrelated PaneFrame Back/Close bug at
     390px found and scoped out of the diff.
   - `7acb844` build-done (G3) — `tsc --noEmit` clean, `#save-review-submit`
     id contract confirmed intact by grep.
3. **Measure** (`e401fe2`): capture round r1 (12 states × 2 widths), gate
   clean (0 real findings, 0 new since baseline, keyboard probes pass 2/2),
   three fresh readers (critique 34/40, evaluate 80/100 health/Clean,
   include Pass/Pass both widths).
4. **Close** (this session):
   - Triaged r1's findings (`close.md`): the low-contrast and
     clipped-overflow/text-overflow findings are residue (already in
     `residue.txt`); the reject-confirm/reject-escape coverage gap is a
     seed-data limitation, not a defect (no reject-eligible document left
     in devdb); the PaneFrame Back+Close-at-390px finding (critique F1,
     driving heuristic 4 to 2/5) is a confirmed pre-existing, app-wide,
     out-of-diff bug.
   - Filed [#367 Fix: PaneFrame shows both Back and Close at 390px](https://github.com/petrose99/Wes-doc/issues/367)
     for that bug and re-ran the critique reader with the triage named in
     its prompt (protocol: a heuristic under 3 driven by a dismissed
     finding gets the reader re-run, not a manual score bump). Re-score:
     **36/40**, lowest heuristics 3/5 (User control and freedom, Error
     prevention, Aesthetic and minimalist design, Help and error handling),
     no heuristic under 3.
   - No code fix batch needed — 0 real detector findings on the gate, 0
     P0/evaluate-P1, and the one non-residue P1 (PaneFrame) is out of this
     ticket's diff and now tracked on #367 instead of block-fixed here.
   - Ran the close-phase checks once, dev server stopped: `tsc --noEmit`
     clean; `eslint` on the ticket's two changed files (0 errors, 1
     pre-existing `react-hooks/set-state-in-effect` warning on code #361
     didn't touch); full `vitest run` (289 files / 2816 tests, all pass);
     `next build` (exit 0).
   - Wrote lessons back to the project lessons file and the
     `docs/agents/areas/detail-pane.md` primer (residue additions, the
     #367 known-regression note, and the seed-data-drift lesson).

## Questions I answered on the owner's behalf

None — no open question required owner judgment this session; the two
seed-data coverage gaps and the PaneFrame bug were resolved by the
recommended path (record as a named gap / file as its own ticket) per the
close-phase protocol, not by a discretionary call.

## Decisions recorded

Map #353's Decisions so far already carries #361's one-line summary from
build (added at `7acb844`/close of build phase, per protocol) — see the
map body. No new Wayfinder decision was made this session; the close
phase records execution outcomes, not new decisions.

## Tickets created / changed

- Created [#367 Fix: PaneFrame shows both Back and Close at 390px (shared
  header, app-wide)](https://github.com/petrose99/Wes-doc/issues/367) —
  `bug`, `accessibility` — standalone, not blocked/blocking (shared
  component bug, pre-existing, reproduces outside #361's diff).
- Closing #361.

## Scores and counts (rendered surfaces only)

scores: predicted-critique=32 first-critique=34 close-critique=36 first-evaluate=80 close-evaluate=80

Gate (shots-r1 vs shots-r0 baseline): real 0, new-since-baseline 0, probes
pass 2/fail 0, errored 0, pageErrors 0 — clean at both 1440 and 390.
Include: 1440px Pass (K4/S3/C3/M3), 390px Pass (K4/S3/C3/M3), no P0.
Detector residue by type (r1, all pre-existing/out-of-diff): ai-color-palette
34, overused-font 12, dark-glow 12, layout-transition 12,
clipped-overflow-container 17, text-overflow 8, first-viewport-column-overflow
2, low-contrast 2.

## First-pass findings → lessons (rendered surfaces only)

| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| Critique F1 — PaneFrame Back+Close both visible @390px (Consistency, P1) | `docs/agents/areas/detail-pane.md`'s "one ←/×  per breakpoint" contract names the rule but had no `check:` verifying it live on a *shared* component a build ticket doesn't touch | unchecked | Filed #367; added a "known regression (#367)" note directly on the primer's Conventions section so no future ticket re-diagnoses it; re-ran critique with the dismissal named, 2/5→ heuristic dropped from the score once the finding was correctly attributed as out-of-diff |
| Evaluate/include P2 — reject-confirm dialog and `reject-escape-focus-return` never actually exercised (seed doc no longer reject-eligible) | No existing lesson said a status-gated round state needs re-verified eligibility per session | new class | Added a lesson (project lessons file + primer's "Seed data is mutable across sessions" section) naming the fix: re-query eligibility immediately before the capture, not once at spec time |
| Include P2/P3 — low-contrast #90a1b9/#f8fafc, first-viewport-column-overflow on the reject-confirm fallback state | `residue.txt`/g2-open-findings.md already carried these as unidentified | none (already covered) | No action — carried forward as residue, added to the primer's Detector residue section for the next ticket to inherit |

## Needs the owner

None. #367 (the PaneFrame Back/Close bug) is a straightforward fix, not a
removal, and is filed as a standalone ticket rather than requiring
sign-off. The reject-confirm/reject-escape coverage gap is a data-seeding
limitation recorded on the primer, not a design decision.
