# Autopilot report — #359 Decide keyboard flow and the phone sheet for the takeover: field nav, viewer ↔ form focus, ↑/↓ next document, what the phone shows

- Map: #353 Map: Document detail modelled on Ramp — full takeover, editable Bill and Receipt templates, coding that learns
- Type: wayfinder:grilling · Started: 2026-09-21 · Finished: 2026-09-21
- Outcome: resolved

## What I did

1. Claimed #359 (`gh issue edit 359 --add-assignee @me`).
2. Read map #353 in full and the ticket body to scope the question.
3. Established facts from code before grilling (no interviews needed — grounded in the repo): `use-field-nav.ts` (confidence-ordered nav, explicit `[data-line-items-editor]` opt-out on Enter), `line-items-section.tsx` (not wired to the nav hook today), `split-pane.tsx` (no ArrowUp/ArrowDown document nav yet, only the layout-toggle radiogroup and a details-tab-order roving tabindex; source-above-fields stacking at `:241-242` below `lg`), `match-manually.tsx` (Escape scoped to its own sub-flow only), and confirmed via `gh issue view` that none of #360–#365 (the takeover build tickets) are built yet.
4. Ran the two grilling rounds named on the ticket — Intent `include` (keyboard/SR contract) and `transpose` (the phone sheet) — recommendations grounded in the `include` digest, the existing code, and #354's density obligations. Took every `➡️` recommendation per the standing delegation.
5. Posted the resolution comment (Q&A + Action Summary) and closed #359: https://github.com/petrose99/Wes-doc/issues/359#issuecomment-5757102296
6. Updated map #353: replaced the body with a new Decisions-so-far line for #359, and cleared **Not yet specified** (the phone-sheet fog item is now resolved and nothing else was undecided).
7. Wired the decision into the five build tickets it folds into, since the ticket specifies execution rides their last step rather than a separate build: [#361](https://github.com/petrose99/Wes-doc/issues/361#issuecomment-5757112459), [#362](https://github.com/petrose99/Wes-doc/issues/362#issuecomment-5757112632), [#363](https://github.com/petrose99/Wes-doc/issues/363#issuecomment-5757113236), [#364](https://github.com/petrose99/Wes-doc/issues/364#issuecomment-5757113488), [#365](https://github.com/petrose99/Wes-doc/issues/365#issuecomment-5757113720).

No score: the takeover shell is not built (all of #360–#365 are still open), so there is no rendered surface to critique — per the autopilot brief, a decision ticket on an unbuilt surface reports "no score."

## Questions I answered on the owner's behalf

| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | Extend `use-field-nav` to line-item cells and the coding row? | Yes — second suspect tier per editable cell, visited after scalar fields clear | `orderSuspectFields`'s array exclusion is about a *global* confidence signal, not a reason to skip cell-level nav; #356 gives every line its own confidence/badge |
| 2 | Viewer ↔ form focus handoff | One-way Tab, viewer is a roving-tabindex composite (one Tab stop), never steals focus back mid-edit | include digest §Understandable — no unexpected context changes |
| 3 | ↑/↓ next document vs. unsaved edits | Autosave makes it moot; flush the pending debounce, no confirm dialog | Map's autosave-per-edit decision already retired the Save/Continue pattern a confirm dialog would reintroduce |
| 4 | Escape from the takeover | Reuses the existing `×`/#244 origin-restore handler; a nested dialog (Match manually, Move) closes first | include digest §3 focus-trap precedence; one exit affordance, two triggers |
| 5 | SR order and badge announcement | DOM order header→status→form; sr-only text on ⚡ Touchless and coding badges | include digest §Reading order, §Color independence |
| 6 | Phone: same sections or a distilled subset | Distilled subset — header, status track, line items, PO match expanded; supplier card / dates+Payment collapsed to disclosures | #354's density obligations (≤8 controls, ≤1.5 screens, no tabs) already rule out full parity; the current tab strip is on #354's signed-off unplug list |
| 7 | Where the viewer goes at 390 | Reuses the existing stacked-source band (`split-pane.tsx:241-242`), not a second sheet | Map's "does this follow Ramp? default yes, grill only what's missing" + don't duplicate a working pattern |

## Decisions recorded

[Decide keyboard flow and the phone sheet for the takeover (#359)](https://github.com/petrose99/Wes-doc/issues/359): field nav extends to line-item cells as a second suspect tier; the viewer is a roving-tabindex composite Tab stop that never steals focus back; ↑/↓ flushes the pending autosave and navigates with no confirm dialog; Escape reuses the `×`/#244 origin-restore handler; SR order is header→status→form with sr-only text on badges/marks. The phone sheet is a distilled section subset (not full desktop parity), reusing the existing stacked-source band for the viewer at 390. Execution folds into #361/#362/#363/#364/#365's last build step — no separate build ticket.

## Tickets created / changed

- No new tickets — the ticket body specified execution folds into the existing template build tickets rather than spawning one.
- Map #353: Decisions-so-far gained the #359 line; **Not yet specified** cleared (the phone-sheet fog item graduated into this ticket's answer; nothing else remained).
- #361, #362, #363, #364, #365: each got a comment naming the keyboard/phone work as its last build step, pointing back to #359's resolution comment for the full reasoning.

## Scores and counts (rendered surfaces only)

n/a — no rendered surface exists yet for this decision; nothing to score.

## First-pass findings → lessons (rendered surfaces only)

n/a — decision ticket, no build in this session.

## Needs the owner

None. The standing delegation covered every question; no removal and no credential/access gap arose.
