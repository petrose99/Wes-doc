# Autopilot report — #458 Build: line Tax code, Tax basis and Tracking

- Map: #445 Line-level coding — Tax code, Tax basis and Tracking
- Type: Wayfinder execution ticket (`kind: backend` apart from step 6's one Accounting surface) ·
  Started: 2026-09 (spec session) · Finished: 2026-09-26
- Outcome: resolved

## What I did

1. **Spec** (`e73ba16`) — wrote `docs/wayfinder-reports/445/458-spec.md` (rev 2) executing ADR 0014
   and #448's Q6 table; posted the plan-gate comment (`458-plan-comment.md`) before any code, per
   the map's Notes.
2. **Build**, six numbered steps plus two gate/fix steps, each its own commit:
   - Step 1 (`0418844`) — ledger capabilities + reference sync (QBO `CompanyInfo`/`Preferences`,
     Xero `TrackingCategories`, `TaxRates` fix).
   - Step 2 (`73d2c3a`) — line/bill coding model additions (`taxCode`, `tracking`, `customer`,
     `billable`, `location`, `taxBasis`).
   - Step 3 (`407790c`) — the blocking Checks (VAT off, tax code missing, tracking off, etc.).
   - Step 4, four parts (`d91a841`, `60862a4`, `92d6b7b`, `ef1975f`) — check/snapshot/context wiring,
     refresh on Save review and sync, eligibility and snapshot callers.
   - Step 5 (`ceb7c25`) — push gate (`gateLineCoding` in `lib/integration-push.ts`).
   - Step 6 (`1aefe1e`, `no-test:` select widening only) — the one rendered surface: the supplier
     accounts table and Forget confirm; ran Intent `articulate`/`include` and Impeccable `clarify` +
     `craft-floor.md` per the hand-off's design-pass note.
   - Steps G1/G2 (`6d10656`, `7f0c01e`) — capture-round harness and gate fixes.
3. **Measure** (`16e87ed`) — r1 capture round + gate: CLEAN at both widths (0 real findings, 46/46
   keyboard probes pass). Readers: critique 36/40, evaluate 94/100 (0 P0, 0 P1, Clean verdict),
   include PASS/PASS.
4. **Close**:
   - Code review (Standards + Spec, base `9acaa96`) — 1 P1 (message duplication between
     `BILLING_MESSAGES` and `lib/checks/line-coding.ts`), 1 eslint error (unused `direction` param),
     1 P2 fixed while in the file (`billable_off_in_quickbooks` gating). Fixed in `5c01a72`.
   - Surface triage — r1 was already CLEAN/PASS at measure; two open notes (native `disabled` on
     dialog buttons; residue gradient not behind new text) verified against source as non-issues,
     nothing dismissed from the readers' counts, no re-score needed.
   - Checks (affected → full suite → tsc → eslint → next build), each once, dev server stopped:
     271+ affected tests, 306 files / 3052 tests full suite, tsc clean, eslint clean on changed
     files, `next build` succeeds.
   - Area primer updated (`299e8a6`): `docs/agents/areas/integrations.md` — supplier table +
     fallback-text lesson.
   - Lessons written back this session: generic file (`scripts/wayfinder-autopilot/lessons.md`,
     Build section) — the project-specific half was refused by the sandbox as a "sensitive file"
     edit (same block hit at #342) and recorded inline in the generic entry instead.
   - This report and ticket close.

## Questions I answered on the owner's behalf

None — #458 was execution of a decision already made (#448 / ADR 0014); "there is nothing left to
decide" per the ticket body. The only judgement calls were the code-review P2/P3 items, all
recorded as non-blocking below.

## Decisions recorded

Built ADR 0014 end to end: ledger capability + reference sync, the line/bill coding model (tax
code, tax basis, tracking, customer, billable, location), the blocking Checks, push-gate wiring,
and the one supplier-accounts UI surface. No new decision was made in this ticket.

## Tickets created / changed

None created. No fog graduated or added — #458 was pure execution against an existing ADR.

## Scores and counts (rendered surfaces only)

scores: predicted-critique=n/a first-critique=36 close-critique=36 first-evaluate=94 close-evaluate=94
review: P0=0 P1=0 findings=docs/wayfinder-reports/445/logs/scratch-458/review-standards.md,review-spec.md

r1 capture/gate (both widths, 1440/390): 0 real findings, CLEAN; 46/46 keyboard probes pass.
include: PASS/PASS. No confirm round was needed — r1 was already at the bar and code-review's fixes
touched no rendered surface (action-helpers.ts message text, integration-push.ts param, model
gating), so the close-phase captures/scores stand unchanged from measure. Pre-build step: none
weak — first pass already cleared the bar (critique ≥32 no heuristic <3, evaluate ≥85 Clean).

## First-pass findings → lessons (rendered surfaces only)

| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| (none — r1 measure was CLEAN, 0 P0/P1, no heuristic under 3) | n/a | n/a | n/a |

Backend-only finding (code-review P1, not a reader finding): `BILLING_MESSAGES` message text
duplicated from `lib/checks/line-coding.ts` verbatim. Status: **none** — no existing check names
"a user-facing string re-typed instead of imported" as a Standards violation to catch before
code-review; written back as a new lesson in `scripts/wayfinder-autopilot/lessons.md` (Build
section, #458 entry) with a `grep`-based check for future sessions.

## Needs the owner

Nothing. Non-blocking judgement calls left open per the review (all P2/P3, documented in the
hand-off's Open findings and not re-litigated here): a `getPrimaryConnection()` helper would
de-duplicate a 4x-repeated query shape; mild Feature Envy in `gateLineCoding` (a documented,
intentional push-gate seam); mild Primitive Obsession in the tracking-selection shape; a stray
`// ponytail:` comment label in `lib/finance/line-coding.ts` worth renaming.
