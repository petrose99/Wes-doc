milestone: spec-done
milestone: build-step-1
milestone: build-step-2
milestone: build-step-3
milestone: build-step-4
milestone: build-step-5
milestone: build-done

## Ticket
#461 — Build: attach the source file to the posted ledger bill. `kind: backend` throughout.
Executes #450/ADR 0016; nothing left to decide. Design pass (Intent/Impeccable) not owed — no
`app/**`/`components/**` rendering files in any step (autopilot spec brief's path gate).

## Step plan
step: 1 — Nango binary proxy + Xero scope constant — kind: backend — done
step: 2 — Attach limits + source-file rendition (pure first) — kind: backend — done
step: 3 — QBO/Xero client attachFile + listAttachments — kind: backend — done
step: 4 — IntegrationAttachment model + claim/attempt/drain trio — kind: backend — done
step: 5 — Pre-post Check, Retry attaching, Owner back-fill, health-check group — kind: backend —
  done. Built: `lib/checks/attachment-limit.ts` (+ .test.ts) wired into
  `models/document-checks.ts::runDeterministicChecks` via new `checkAttachmentLimitForDocument`
  (skips silently with no connected ledger; fast path uses `document.sizeBytes` directly, falls
  back to `loadAttachmentRendition` only for a split child or HEIC/WEBP). `PERMANENT_ATTACH_ERROR_CODES`
  added to `lib/integration-attach-policy.ts`. New
  `app/(app)/workspaces/[workspaceId]/integration-attach-actions.ts`:
  `retryAttachingAction` (any member, refuses a permanent-errorCode failure),
  `countBackfillableAttachmentsAction`/`backfillAttachSourceFilesAction` (owner-only). New
  `models/integrations.ts::countBackfillableAttachments`/`queueBackfillAttachments` (+ tests) —
  `IntegrationPush` rows with `status: "succeeded", attachment: null`. Health: `CheckAttachSlice` +
  `attachHistory` added to `lib/health/types.ts`; `attachFailuresCheck` added to
  `lib/health/checks/push-failures.ts` (own checkCode `attach_failures`, `groupByErrorCode` made
  generic to serve both checks) and registered `hidden()` in `lib/health/registry.ts`;
  `loadAttachHistory` added to `models/health.ts` alongside `loadPushHistory`. Every existing
  health-check test's `baseCtx` updated with `attachHistory: []` (mechanical, scripted).
  No action-layer test file: this repo has no precedent for testing `app/(app)/.../*-actions.ts`
  (checked — `integration-push-actions.ts` itself is untested the same way); the hook's test
  requirement only covers `lib/`, `models/`, `worker/`, all of which have tests in this commit.
step: G1 — round script — kind: backend — done (n/a for surface capture; no app/components
  rendering files on this ticket)
step: G2 — fixes — kind: backend — done (full suite + tsc run; nothing to fix)
step: G3 — build-done — kind: backend — done. `tsc --noEmit` clean full project. `vitest run`:
  312 test files, 3152 tests, all passed. No P0/P1 found; no fixes needed.

## Artifacts
- Ticket plan comment: https://github.com/petrose99/Wes-doc/issues/461#issuecomment-5848182374
- ADR: docs/adr/0016-source-file-attached-after-the-post-warned-not-blocked-backfilled-on-owner-request.md
- Design notes (steps 1-4 background): /home/ubuntu/Dev/Wes-doc/docs/wayfinder-reports/445/logs/scratch-461/design-notes.md

## Open findings
None. Build gate complete: `tsc --noEmit` clean, full `vitest run` green (312 files, 3152 tests).

## Next step
Measure session: run `/code-review` (Standards + Spec) over the ticket's diff, confirm P0=P1=0,
then close per the close phase brief (resolution comment, Decisions so far line).
