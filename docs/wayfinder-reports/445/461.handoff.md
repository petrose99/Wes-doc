milestone: spec-done
milestone: build-step-1
milestone: build-step-2
milestone: build-step-3
milestone: build-step-4
milestone: build-step-5
milestone: build-done
milestone: measured
milestone: close-fixes-done

## Ticket
#461 — Build: attach the source file to the posted ledger bill. `kind: backend` throughout.
Executes #450/ADR 0016; nothing left to decide. Design pass (Intent/Impeccable) not owed — no
`app/**`/`components/**` rendering files in any step.

## Step plan
step: 1-5 — build — kind: backend — done
step: G1-G3 — build gate — kind: backend — done (tsc/vitest at build-done)
step: close review + fix batch — kind: backend — done, this session (commit 6356d0b)

## Artifacts
- Ticket plan comment: https://github.com/petrose99/Wes-doc/issues/461#issuecomment-5848182374
- ADR: docs/adr/0016-source-file-attached-after-the-post-warned-not-blocked-backfilled-on-owner-request.md
- Close triage + fix log (prior session): /home/ubuntu/Dev/Wes-doc/docs/wayfinder-reports/445/logs/scratch-461/close.md
- Code review (Standards + Spec), run since da937b0: review-standards.md, review-spec.md, same
  scratch folder.
- Fix commit (this session): 6356d0b — provider-404 → `attach_bill_gone` rewrap in
  `lib/integration-attach.ts`; `ATTACH_ERROR_SENTENCES` map + `describeAttachError` in
  `lib/health/checks/push-failures.ts` (fixes the CODING_STANDARDS #9 raw-errorCode P1); dropped an
  unused `CheckPushSlice` import eslint caught. Tests added in both `.test.ts` files.

## Review status — all findings from close.md now resolved
- Spec P0 (QBO upload-then-link fallback): fixed in an earlier session, tests green.
- Spec P1 (provider 404 not classified as `attach_bill_gone`): fixed this session
  (`lib/integration-attach.ts`, catch block rewraps `IntegrationPermanentError("http_404")`).
- Standards P1 (raw errorCode in `attachFailuresCheck` description): fixed this session — every
  named attach errorCode now has a real sentence; unmapped/transient codes fall back to the
  existing humanized-code convention.
- **`review: P0=0 P1=0 findings=docs/wayfinder-reports/445/logs/scratch-461/review-{standards,spec}.md`**
  — all findings fixed, no documented exceptions needed.

## Checks (all green, this session, dev server stopped throughout)
Full `vitest run`: 312 files / 3157 tests passed. `tsc --noEmit`: clean. `eslint` on every file this
ticket touched: clean (only pre-existing `no-explicit-any` warnings in test mock plumbing, not
errors). `next build`: clean (exit 0). No surface/rendering files touched — no readers, no
gate.mjs, no detector round needed (kind: backend throughout).

## Open findings
None outstanding. Project lessons file (`.claude/wayfinder-autopilot/lessons.md` in this worktree)
was refused by the sandbox as a "sensitive file" (same as #342/#458/#459) — the two lessons this
session earned are recorded in the generic file
(`/home/ubuntu/Dev/Wes-doc/scripts/wayfinder-autopilot/lessons.md`, docubite #461 entries) with a
note for a future session to move the docubite-specific half into the project file.

## Next step
Write the close report at `docs/wayfinder-reports/445/461.md` (template:
`scripts/wayfinder-autopilot/report.md`) — Sessions list from `git log --format='%h %s%n%b' --
lib/integration-attach.ts lib/integration-attach.test.ts lib/integrations/quickbooks/client.ts
lib/integrations/quickbooks/client.test.ts lib/health/checks/push-failures.ts
lib/health/checks/push-failures.test.ts` plus prisma/models/worker files from the build steps
(`git log --oneline -- lib/integration-attach*.ts` etc.), `scores:` line is n/a-surface but
`review: P0=0 P1=0 findings=...` goes on it per the phase brief. Then: one Decisions-so-far line on
map #445 ("Build: attach the source file to the posted ledger bill (#461) — closed: QBO/Xero attach
queue built per ADR 0016, code-review clean"), commit the report, and `gh issue close 461` with the
resolution comment. No area primer update needed (backend-only ticket, no `docs/agents/areas/*`
page is scoped to this surface).
