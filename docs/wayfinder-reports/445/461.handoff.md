milestone: spec-done
milestone: build-step-1

## Ticket
#461 — Build: attach the source file to the posted ledger bill. `kind: backend` throughout.
Executes #450/ADR 0016; nothing left to decide. Design pass (Intent/Impeccable) not owed — no
`app/**`/`components/**` rendering files in any step (autopilot spec brief's path gate).
Plan gate satisfied: full plan posted as a ticket comment (2026-09-26) before any code, per the
map's Notes and the standing delegation (owner takes the recommended path in an unattended run).

## Step plan
step: 1 — Nango binary proxy + Xero scope constant — kind: backend — done
  files: lib/nango.ts (new nangoProxyBinary), lib/integrations/xero/config.ts (XERO_SCOPES +
  accounting.attachments)
  seams: nangoProxyBinary → lib/nango.test.ts
step: 2 — Attach limits + source-file rendition (pure first) — kind: backend — todo
  files: lib/integrations/attach-limits.ts (new), lib/integration-attach-rendition.ts (new),
  package.json (+pdf-lib)
  seams: attachmentLimitViolation, renderSourceFileForAttach, fixedAttachFilename → co-located
  .test.ts files
step: 3 — QBO/Xero client attachFile + listAttachments — kind: backend — todo
  files: lib/integrations/quickbooks/client.ts, lib/integrations/xero/client.ts
  seams: attachFile, listAttachments (both providers) → existing client.test.ts, extended
step: 4 — IntegrationAttachment model + claim/attempt/drain trio — kind: backend — todo
  files: prisma/schema.prisma (+migration), lib/workspace-scope.ts, lib/integration-attach-
  policy.ts (new), lib/integration-attach.ts (new), lib/integration-push.ts (enqueue on success),
  worker/job-worker.ts (drain wiring)
  seams: computeAttachUpdate/outcome → integration-attach-policy.test.ts; attemptIntegrationAttach-
  ment (success/auth-pause/each permanent code/retryable-backoff) → integration-attach.test.ts
step: 5 — Pre-post Check, Retry attaching, Owner back-fill, health-check group — kind: backend — todo
  files: lib/checks/attachment-limit.ts (new), models/document-checks.ts, app/(app)/workspaces/
  [workspaceId]/integration-attach-actions.ts (new), models/integrations.ts, lib/health/types.ts,
  models/health.ts, lib/health/checks/push-failures.ts
  seams: checkAttachmentLimit → .test.ts; retryAttachingAction/backfillAttachSourceFilesAction →
  integration-attach-actions.test.ts (new); countBackfillableAttachments/queueBackfillAttachments
  → models/integrations.test.ts (extended); attach-failure group → push-failures.test.ts (extended)
step: G1 — round script — kind: backend — todo (n/a for surface capture; this gate is npm test on
  touched paths + tsc --noEmit per step, run at each step's close)
step: G2 — fixes — kind: backend — todo
step: G3 — build-done — kind: backend — todo (full suite green, tsc clean, /code-review Standards
  + Spec, P0=P1=0)

## Artifacts
- Ticket plan comment: https://github.com/petrose99/Wes-doc/issues/461#issuecomment-5848182374
- Design notes (this session, not committed): /home/ubuntu/Dev/Wes-doc/docs/wayfinder-reports/445/logs/scratch-461/design-notes.md
- #450 resolution (size/type/count table, header status-line spec for #462): ticket #450 comments
- ADR: docs/adr/0016-source-file-attached-after-the-post-warned-not-blocked-backfilled-on-owner-request.md

## Open findings
None yet — pointer: scratch design-notes.md above has the full interface/invariant/error-mode
write-up per step, and the wiring points found this session (enqueue call site in
lib/integration-push.ts's success branch, drain wiring in worker/job-worker.ts, Check wiring in
models/document-checks.ts, health slice wiring in models/health.ts).

## Next step
Build session: start step 2 (attach limits + source-file rendition, pure first: attachmentLimitViolation, renderSourceFileForAttach, fixedAttachFilename; adds pdf-lib dependency).
