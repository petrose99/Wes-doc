milestone: spec-done
milestone: build-step-1
milestone: build-step-2
milestone: build-step-3
milestone: build-step-4
milestone: build-step-5
milestone: build-done
milestone: measured

## Ticket
#461 — Build: attach the source file to the posted ledger bill. `kind: backend` throughout.
Executes #450/ADR 0016; nothing left to decide. Design pass (Intent/Impeccable) not owed — no
`app/**`/`components/**` rendering files in any step (autopilot spec brief's path gate).

## Step plan
step: 1-5 — build — kind: backend — done (see prior commits/hand-off history on this file for
  per-step detail; unchanged this session)
step: G1-G3 — build gate — kind: backend — done. `tsc --noEmit` clean, full `vitest run` 312
  files/3152 tests green at build-done.

## Artifacts
- Ticket plan comment: https://github.com/petrose99/Wes-doc/issues/461#issuecomment-5848182374
- ADR: docs/adr/0016-source-file-attached-after-the-post-warned-not-blocked-backfilled-on-owner-request.md
- Design notes: /home/ubuntu/Dev/Wes-doc/docs/wayfinder-reports/445/logs/scratch-461/design-notes.md
- Close triage + fix log: /home/ubuntu/Dev/Wes-doc/docs/wayfinder-reports/445/logs/scratch-461/close.md
- Fresh code-review (this close session, since `da937b0`): review-standards.md, review-spec.md
  in the same scratch folder.

## Open findings (close phase, in progress — see close.md for full detail)
This close session ran `/code-review` (Standards + Spec) fresh per the phase brief (the prior
"measured" milestone's review claim was a lighter self-check, not the two-subagent skill run) and
found real findings the earlier pass missed:
- **Spec P0 (fixed and tested this session):** QBO `attachFile` had no upload-then-link fallback
  for Nango losing `file_metadata_01`'s JSON content-type. Fixed in
  `lib/integrations/quickbooks/client.ts` (`attachFile` now checks the upload response's
  `AttachableRef` for a link to `billId`; falls back to new `linkAttachableToBill` via
  `POST /attachable` if absent). Three tests added/updated in `client.test.ts` (linked-on-upload
  path, unlinked→fallback path, no-double-call-when-linked). `npx vitest run
  lib/integrations/quickbooks/client.test.ts` — 23 passed. `tsc --noEmit` clean full project.
- **Spec P1 (not yet fixed):** `attach_bill_gone` only fires for a missing local `Document` row;
  a real provider 404 classifies as `http_404` instead, so `retryAttachingAction`'s
  `PERMANENT_ATTACH_ERROR_CODES` check doesn't catch it. Smallest fix named in close.md: in
  `attemptIntegrationAttachment`'s provider-call try/catch (`lib/integration-attach.ts`), rewrap a
  caught `IntegrationPermanentError` with `code === "http_404"` as `attach_bill_gone`.
- **Standards P1 (triaged, not fixed — see close.md for full reasoning):** two error codes
  surface raw (`error: ${errorCode}`) in the new `attachFailuresCheck` health-finding description
  instead of a mapped sentence. One code is reused verbatim from the pre-existing push side (never
  had a mapped sentence there either); the check itself is a structural copy of the pre-existing
  `pushFailuresCheck`'s same raw-code pattern. User-facing attach-error copy is explicitly deferred
  to #462 (confirmed in design-notes.md). Recorded as a documented exception, not silently dropped;
  re-decide before writing `P1=0` on the report if this reasoning doesn't hold up on re-read.

## Next step
Spec P0 is fixed and tested. Pick up at close.md's "Next step" list, item 1 onward: fix the
`attach_bill_gone`/`http_404` P1 in `lib/integration-attach.ts` (smallest fix named there), add a
test in `lib/integration-attach.test.ts` (provider-404 → `attach_bill_gone`, not `http_404`), run
that file's tests, then the full close gate once (full vitest → tsc --noEmit → eslint on changed
files → next build, dev server stopped throughout — no dev server running now). Then write the
report, lessons, area primer, Decisions-so-far line, and close at `review: P0=0 P1=0` (or P1=1
named-exception if the Standards triage above is confirmed correct on re-read).
