# Autopilot report — #462 Build: source-file state and Post failed on the Bill header

- Map: #445 (docubite Decide-attaching-source-document follow-through)
- Type: surface (build) · Started: spec session (see commits) · Finished: 2026-09-26
- Outcome: resolved

## What I did

1. **Spec** (`03aa3a1`): `fortify` state table + `articulate` copy, verbatim from #450's
   glossary/decision. Full pre-flight Parts A–F written; Part D spec-critic run scored 34/40,
   no heuristic under 3, verdict Clean. 6-step build plan + 3 gate rounds on the hand-off.
2. **Build** (`7367db0`, `38bc97e`, `ad587c8`, `3d64f8d`, `1034945`, `5e77449`, `015ab13`):
   6 build steps + gates G1–G3. Wired the ledger pill truth (Posting…/Posted/Post failed/Paid),
   StatusLine's source-file trailing slot (none/pending/reconnect/transient/permanent), and the
   Owner-scoped backfill control. `tsc --noEmit` clean, keyboard probes pass, build-done.
3. **Measure** (`70d38d2`, `65bba31`): 18 states × 2 widths captured, gate CLEAN. First-pass
   readers: critique 27/40, evaluate 77/100 (below the 80 bar), include Partial/Pass/Partial.
   1 P0 (pill-failed state rendering no visible content) + 6 P1s flagged, with a caveat that some
   were likely capture-timing artifacts.
4. **Close** (`1a98c41`, `b59d5a9`, `34e89df`, `0d96047`, this session):
   - Ran `/code-review` (Standards + Spec) fixed at `1a98c41`: fixed the one P1 it found.
   - Triaged all r1 findings: every P0/P1 was a capture-timing artifact (verified live via
     `probe-08.mjs`/`probe-08b.mjs`), not a product bug — root-caused and fixed the round script's
     wait condition.
   - Gate rounds c1, c2 vs baselines: CLEAN both.
   - Verified `em-dash-overuse` and `cramped-padding` on `09-admin-backfill` as pre-existing
     residue (checked against source: `integrations-manager.tsx`'s copy, `button.tsx`'s shared
     `sm` variant), not #462 regressions.
   - Filed follow-up ticket [#508](https://github.com/petrose99/Wes-doc/issues/508) (backend,
     child of map #445) for a genuine but out-of-scope gap surfaced during triage.
   - Confirm round on shots-c2 (3 fresh readers): critique 32/40, evaluate 86/100, include clean —
     but two of three readers independently caught a **genuine new P1** the triage narrative had
     missed: `08-pill-failed-390` rendered only a bare route-level Suspense spinner.
   - Root-caused (not dismissed): `app/(app)/loading.tsx`'s `Loader2 animate-spin` fallback was
     still showing when the old capture wait — keyed only on the pane's own `.animate-pulse`
     skeleton — resolved true, on the one state/width combo the earlier fix didn't reach.
     Live-verified with `probe-08-390.mjs` (0.8s vs 2.8s screenshots), fixed `capture-round.mjs`'s
     `waitPaneReady` to wait for both loading indicators plus real header text.
   - Gate round 3 (shots-c3): CLEAN, 3rd/final gate round used.
   - Final confirm round on shots-c3 (fresh readers, `readers-c2/`): **critique 36/40**, **evaluate
     96/100**, no P0/P1 in either — the authoritative close scores, superseding the c2 read that
     caught the real defect.
   - Checks, each once, dev server stopped: `vitest run` (315 files, 3187 tests) pass; `tsc
     --noEmit` clean; `eslint` on changed files (0 errors, 1 accepted pre-existing-pattern
     warning); `next build` succeeds.
   - Refreshed area primers `docs/agents/areas/detail-pane.md` and
     `docs/agents/areas/integrations.md` (committed with the build/close commits).
   - Wrote generic lesson on the two-loading-states capture gotcha to
     `scripts/wayfinder-autopilot/lessons.md` (`0d96047`); the project-file half was refused by
     the sandbox as a "sensitive file" (same pattern as #342/#458/#459/#461) and recorded inline
     in the generic file instead, for a future session with write access to move.

## Questions I answered on the owner's behalf

None — this ticket's build/spec decisions were carried over verbatim from #450's already-settled
glossary and state machine; no new judgment calls arose in this close session beyond fix-vs-dismiss
triage, which the gate/live-probe evidence decided directly.

## Decisions recorded

Ticket #462 shipped the ledger pill truth (Posting…/Posted/Post failed/Paid), the seven-state
source-file StatusLine trailing copy (none/pending/reconnect/transient/permanent + Retry/
reconnect-link affordances), and the Owner-scoped backfill control on the Bill header, per #450's
decision — closes clean at 36/40 critique, 96/100 evaluate, 0 P0/P1.

## Tickets created / changed

- Filed [#508](https://github.com/petrose99/Wes-doc/issues/508) (backend, child of map #445,
  blocked on nothing): persist the ledger's exact size/type-violation sentence through to the
  post-attach terminal failure so the StatusLine trailing can show #450's literal example string
  instead of the generic `ATTACH_ERROR_SENTENCES` fallback.

## Scores and counts (rendered surfaces only)

scores: predicted-critique=34 first-critique=27 close-critique=36 first-evaluate=77 close-evaluate=96 review: P0=0 P1=0

Gate counts: r1 shots — 1 P0 + 6 P1 (all capture-timing artifacts, none real); c1/c2 gate rounds —
CLEAN vs baseline; c3 (final) gate round — CLEAN, 0 real findings across 18 states, 8/8 keyboard
probes pass at both 1440 and 390, 0 new since baseline. Include: Partial/Pass/Partial (r1, timing
artifacts) → clean (c1 confirm, not re-run at c3 — no keyboard/responsive/contrast change since).
Pre-build step that was weak: none in the spec/build itself — the gap was entirely in the capture
harness's readiness wait, not the product code, caught only because the confirm round re-ran
fresh readers against real pixels instead of trusting the triage narrative.

## First-pass findings → lessons (rendered surfaces only)

| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| P0: pill-failed state renders no visible content (r1) | Capture-wait contract for a post-mutation/async pane state | unchecked — existed as the general "wait for outcome, not a fixed ms" lesson (docubite #258/#271) but wasn't applied to a *two-stage* loading sequence | Root-caused as capture-timing (fixed wait resolved before real content painted); fixed the round script's wait condition; new lesson written naming the two-loading-states class specifically |
| P1 ×6: backfill visibility, missing reconnect link, skeletons at 390px, padding/copy (r1) | Same capture-timing class | unchecked, same root cause | Same fix; all confirmed live via probes as artifacts, none were product bugs |
| P1: `08-pill-failed-390` bare spinner (c2 confirm, missed by the r1 triage) | The r1 fix only waited for `.animate-pulse` to clear; it didn't account for `app/(app)/loading.tsx`'s separate route-level `Loader2 animate-spin` Suspense fallback rendering first | new class — no existing lesson named a *second*, route-level loading indicator distinct from the pane's own skeleton | Live-verified with `probe-08-390.mjs`, fixed `waitPaneReady` to wait for both spinners to clear plus real header text; new lesson written to `scripts/wayfinder-autopilot/lessons.md` (project-file half blocked by sandbox, noted inline) |

## Needs the owner

None. No removal, credential, or ambiguous-judgment item arose in this ticket.
