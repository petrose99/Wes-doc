# Autopilot report — #461 Build: attach the source file to the posted ledger bill

- Map: #445 Ramp Bill Pay gaps
- Type: Build (backend, executes #450 / ADR 0016) · Started: 2026-09-25 (spec) · Finished: 2026-09-26
- Outcome: resolved

## What I did

1. **Spec** (3121042): read #450's decision and ADR 0016; wrote the build plan (files, schema,
   ADRs touched, tests) as the ticket plan comment — no rendering files in any step, so no
   Intent/Impeccable pass was owed (`kind: backend` throughout).
2. **Build steps 1–5** (744d11c, e1208b4, 138933b, 1b3a992, cbc3b59): `IntegrationAttachment`
   Prisma model (additive migration, `@@unique([pushId])`), the claim/attempt/drain trio mirroring
   the existing push queue, source-file rendition (pdf-lib page cut for a split child; HEIC/WEBP→JPEG
   via sharp), the QBO/Xero provider clients' attach calls, the pre-post warn Check
   (`lib/checks/attachment-limit.ts`), Retry action, Owner-only back-fill action, and the
   `attach_failures` Health-tab group.
3. **Measure** (284980e): full `vitest`, `tsc --noEmit` clean; no capture round (backend-only, no
   `app/**`/`components/**` touched).
4. **Close — code review** (this and a prior session): ran `/code-review` (Standards + Spec) since
   `da937b0` as two sub-agents. Standards: P0=0, P1=1 (raw error codes in a health-check
   description). Spec: P0=1 (QBO multipart had no upload-then-link fallback when Nango's proxy
   drops the metadata part's JSON content-type), P1=1 (a provider 404 flowed through as generic
   `http_404` instead of `attach_bill_gone`, so `retryAttachingAction`'s permanent-code check missed
   it and could offer Retry on an actually-gone bill).
5. **Close — fix batch**: earlier session fixed the Spec P0 (`lib/integrations/quickbooks/client.ts`
   `attachFile` now checks the upload response's `AttachableRef` and falls back to
   `linkAttachableToBill`). This session fixed the two remaining findings in 6356d0b:
   `lib/integration-attach.ts`'s `attemptIntegrationAttachment` now rewraps a caught
   `IntegrationPermanentError("http_404")` as `attach_bill_gone`; `lib/health/checks/push-failures.ts`
   gained an `ATTACH_ERROR_SENTENCES` map + `describeAttachError`, so every named attach error code
   gets a real sentence (falls back to the existing humanized-code convention for
   transient/unclassified codes). Tests added in both `.test.ts` files. Also dropped an unused
   `CheckPushSlice` import eslint caught.
6. **Checks, once, dev server stopped throughout**: full `vitest run` (312 files / 3157 tests,
   green), `tsc --noEmit` (clean), `eslint` on every file this ticket touched (clean — only
   pre-existing `no-explicit-any` warnings in test-mock plumbing, not errors), `next build` (clean,
   exit 0).
7. Wrote a project-lessons entry (refused by the sandbox as a "sensitive file" this session, same as
   #342/#458/#459) — the docubite-specific detail is recorded in the generic lessons file instead
   (`scripts/wayfinder-autopilot/lessons.md`, docubite #461 entries).

## Questions I answered on the owner's behalf

None — the ticket executed #450's decision and ADR 0016 with nothing left open; the two review
findings were code defects to fix, not design questions.

## Decisions recorded

Build: attach the source file to the posted ledger bill (#461) closed — the `IntegrationAttachment`
queue (claim/attempt/drain, source-file rendition, QBO/Xero attach calls, warn Check, Retry, Owner
back-fill, Health group) is built per ADR 0016; both code-review axes are clean after the fix batch.

## Tickets created / changed

None created. No fog graduated (the ticket executed an already-specified decision). No scope ruled
out.

## Scores and counts (rendered surfaces only)

n/a — backend-only ticket, no `app/**`/`components/**` files in any step; no readers, no gate.mjs,
no detector round.

review: P0=0 P1=0 findings=docs/wayfinder-reports/445/logs/scratch-461/review-standards.md,docs/wayfinder-reports/445/logs/scratch-461/review-spec.md

## First-pass findings → lessons (rendered surfaces only)

n/a (no rendered surface). Backend findings → lessons instead:

| Finding (axis, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| Spec P0: QBO multipart had no upload-then-link fallback | Part B (build plan) named the fallback as spec item 1 but the build step's own gate (`tsc`/vitest) doesn't check spec completeness, only type/test correctness | unchecked — existed as a spec line, never converted to a grep/test check | Fixed in an earlier session (`attachFile`'s `AttachableRef` check + `linkAttachableToBill`); lesson: a spec item describing a fallback branch needs its own test asserting the fallback path is taken, not just the happy path |
| Spec P1: provider 404 not rewrapped to `attach_bill_gone` | `PERMANENT_ATTACH_ERROR_CODES` is a by-name `Set`; nothing checked that every provider-native error code a call site can throw is rewrapped into the domain's own permanent-error vocabulary before reaching the by-name check | new class — no existing lesson covered "provider-native code bypasses the domain Set" | Fixed this session (`attemptIntegrationAttachment` rewraps `http_404`); lesson written to the generic lessons file (docubite #461 entry) |
| Standards P1: raw `errorCode` in a health-check description | CODING_STANDARDS #9 (error codes get a user-facing sentence) existed as a written rule but the build step copied a pre-existing sibling check's same-shaped violation as precedent instead of checking the rule itself | wrong — the rule was known but not applied, precedent read as permission | Fixed this session (`ATTACH_ERROR_SENTENCES` map + `describeAttachError`); lesson written to the generic lessons file (docubite #461 entry) |

## Needs the owner

Nothing. No removal, no credential, no undecided question.
