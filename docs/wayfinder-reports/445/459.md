# Autopilot report — #459 Build: item lines on QuickBooks and Xero bills

- Map: #445 (item lines on QuickBooks and Xero bills)
- Type: wayfinder:build (kind: backend, no surface steps) · Started: 2026-09-25 (spec) · Finished: 2026-09-26
- Outcome: resolved

## What I did
1. **Spec** (017368c): read #445's execution plan (settled by #449/ADR 0015) and #458's line
   model to extend; wrote `docs/wayfinder-reports/445/459-spec.md` — item sync/capability, the
   `item_code`/`itemExternalId`/`item_source` line model, `SupplierItemPairing` learning,
   pre-flight Checks, QBO/Xero mappers, and line-match item-pairing. `kind: backend` throughout —
   no `app/**` or `components/**` files, so no design pass owed.
2. **Build steps 1–5** (092c896, e32fca8, 63e25c5, 6db9c00, 50391e3, plus per-step hand-offs):
   item entity sync and capability; line model + `SupplierItemPairing` + learning; blocking
   pre-flight Checks and account-correction refusal; QBO/Xero bill mappers; line-match item
   pairing. Each step's own gate: `tsc --noEmit` clean, that step's tests green.
3. **Measure** (3f78433): skipped — no rendering surfaces touched, so no capture round or
   readers; the hand-off records `measure phase: skipped`.
4. **Close — code review** (261421e): ran `/code-review` (Standards + Spec) as parallel
   sub-agents against base `8009099` (the commit before #459's spec started — not `master`,
   which pulls in ~470 unrelated files already landed from other tickets). Standards: P0=0,
   P1=0, two P2 judgement calls left as-is (documented in
   `docs/wayfinder-reports/445/logs/scratch-459/close.md`). Spec: P0=1, P1=1, P2=1 before fixes.
   Fixed the P0 (item-first PO-line match built and unit-tested at build step 5 never reached
   production — `models/document-checks.ts`'s `parseLineItemsForConsumption` never read
   `codingData.items[].item_external_id`, so `itemExternalId` was always `undefined` at the real
   call site) and the P1 (a Xero "ItemCode stripped" push correctly failed but discarded
   `created.id`, making a genuinely-posted, wrong-shaped bill unfindable — `IntegrationPermanentError`
   now carries an optional `externalId`). Left the P2 (a spec-wording nuance around
   `resolveDocumentCodingItems`'s intended null for a manual item selection gone inactive) as a
   documented gap, not a defect — no product decision in it.
5. **Close — checks** (this session, re-verified): `tsc --noEmit` clean; full `vitest` suite —
   307 files / 3100 tests passed; the two touched test files re-run directly — 36/36 passed;
   `eslint` on the fix batch's changed files — 0 errors; `next build` clean (no rendering files
   in the diff, run before the fix batch, not re-run).
6. **Lessons**: added a generic lesson (`scripts/wayfinder-autopilot/lessons.md`, "wiring never
   tested" class — a new pure function's tested signature must show a real caller's diff, not
   just a unit test constructing the input directly) with the docubite specifics folded in,
   since editing this repo's own `.claude/wayfinder-autopilot/lessons.md` was refused by the
   sandbox as a "sensitive file" this session (same refusal recorded at #342/#458).
7. Resolution comment on #459, closing the ticket, and one line on map #445's Decisions so far.

## Questions I answered on the owner's behalf
None — #459 executes a plan already settled by #449/ADR 0015; nothing in the ticket asked for a
fresh product decision. The one open item (the P2 spec-wording nuance) is a documentation
observation with a tested, intended behaviour behind it, not a question needing an answer.

## Decisions recorded
Item lines post to QuickBooks and Xero bills per ADR 0015: item sync/capability, canonical
`item_code`/`itemExternalId`/`item_source` on line items, learned `SupplierItemPairing`,
blocking pre-flight Checks, QBO `ItemBasedExpenseLineDetail` / Xero `ItemCode` mappers, and
item-first PO-line pairing — all shipped and wired end-to-end (the code-review P0 fixed the one
place the wiring was incomplete).

## Tickets created / changed
None created. #459 was already blocking #460 (item in the coding row's Account cell), which is
now unblocked now that #459 is closed.

## Scores and counts (rendered surfaces only)
Not applicable — backend-only ticket, no rendering surfaces (`app/**`/`components/**`) touched;
design pass not owed per CLAUDE.md's path-based gate. No detector, critique or evaluate run.

review: P0=0 P1=0 findings=docs/wayfinder-reports/445/logs/scratch-459/close.md

## First-pass findings → lessons (rendered surfaces only)
Not applicable (backend-only ticket). The code-review P0/P1 findings and their lessons are
listed in "What I did" above and in `scripts/wayfinder-autopilot/lessons.md` (the "wiring never
tested" entry).

## Needs the owner
Nothing. The one deferred item (P2 spec-wording nuance on `resolveDocumentCodingItems`) is
recorded in `close.md` as a documentation gap with a tested, intended contract behind it — not a
decision needing owner sign-off.

## Sessions
- 017368c wip(autopilot): #459 spec
- 4feb794 wip(autopilot): #459 hand-off at the context line
- 092c896 wip(autopilot): #459 build step 1
- e32fca8 wip(autopilot): #459 build step 2
- b98728f wip(autopilot): #459 hand-off at the context line
- 63e25c5 wip(autopilot): #459 build step 3
- 6db9c00 wip(autopilot): #459 build step 4
- 50391e3 wip(autopilot): #459 build step 5
- ef67140 wip(autopilot): #459 build step 5 hand-off
- bfb80f9 wip(autopilot): #459 build
- 3f78433 wip(autopilot): #459 measured
- 261421e wip(autopilot): #459 hand-off at the context line — code review P0/P1 fixed
