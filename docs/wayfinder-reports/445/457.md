# Autopilot report — #457 Build: company currency — LSL/ZAR only, ledger home-currency check, lock after first posting

- Map: #445 Close the Ramp Bill Pay gaps — approval rules, ledger line dimensions and items, credit notes, 3-way match, remittance, cash flow
- Type: wayfinder:task (phased build) · Started: 2026-09-26 13:31 · Finished: 2026-09-26 15:30
- Outcome: resolved

## What I did
1. Spec (c60b5ee, c4a6f33, f46df28): spec `457-spec.md` + pre-flight `457-preflight.md` A–E; opus spec critic 27/40 with 4 P1s, all reconciled into spec §9 (ZA hidden input, serialised change + in-flight refusal, re-queue after change, pane refresh, recorded lock, every push kind checked, honest blocked copy). Plan: 5 build steps + G1–G3, 10 states.
2. Build step 1 (ded7bc9, 1ff6cba, acf9606), backend: Company currency rules `lib/geo/company-currency.ts` (LS/LSL, LS/ZAR, ZA/ZAR; no USD), additive migration 20260926090000 (recorded lock on Workspace, ledgerCurrency/ReadAt on IntegrationConnection), `models/company-currency.ts` (lock, change under FOR UPDATE, re-conversion), gates read `GateContext.baseCurrency` instead of a USD literal, `changeCompanyCurrencyAction`. Tests red first; 329 pass; tsc clean.
3. Build step 2 (561998b, e0ca3cf), backend: `migrateCompanyCurrencies` + `scripts/migrate-company-currency.ts` (locked companies to a support list; Owner notice emailed, retried while pending); seed updated. 79 pass.
4. Build step 3 (c007ce9), backend: QBO home / Xero base currency reads, `readLedgerCurrency`, `checkLedgerCurrency`, push gate on every push kind (unreadable = retryable, mismatch = one push_preflight task + terminal), lock recorded in the same transaction as the first successful push, re-queue after a change. 342 pass.
5. Build step 4 (2680051), surface: one formatter `lib/money.ts formatCurrency` ("LSL 1,234.50"); every inline currency `Intl.NumberFormat` replaced; analytics no longer picks the currency from the tax profile.
6. Build step 5 (d45760f, 3e2d9fd), surface: Country + Company currency fields in both creation forms, Company currency row with Change + lock reason, ChangeCurrencyDialog, ledger-currency line on the connection card with an Owner Switch.
7. Gate G1–G3 (8f8bfda, 6bb8759, 374dc56): round script `scripts/wayfinder-autopilot/round-457.mjs` + seed `scripts/dev/seed-457.ts`; r0 had 10 real findings + 11 failing probes (en-GB "Sept" lock date, line length, cramped padding, probe selectors), fixed; r2 CLEAN; tsc clean; Part B contracts B1–B5 checked.
8. Measure (3bd7558): shots-m1 gate CLEAN (0 real, 52 probes pass); readers critique 38/40, evaluate 95, include PASS at both widths.
9. Close C1 (295c84e): triage (0 real findings on #457); code review Standards + Spec vs 3351eee: Standards P1 (push gate read the currency without waiting on the change's row lock) fixed with `readCompanyCurrencyForPush` (FOR SHARE), tests red first; stale test mock fixed; polish pass, no changes; admin + integrations area primers refreshed.
10. Close C2 (16fe626 + this commit): full vitest 2936/2936 (302 files); tsc --noEmit clean; eslint on changed files found 4 unused type imports this ticket left in `lib/gates/*` (fixed, lib/gates 197/197). One remaining error, `direction` unused in `lib/integration-push.ts ledgerHasDuplicate`, was already there at 3351eee and is not from this ticket. `next build` passes (2 Turbopack tracing warnings in `lib/document-storage.ts`, pre-existing). Report written, map line updated, ticket closed.

## Questions I answered on the owner's behalf
| # | Question | Answer taken | Why (rule/evidence) |
|---|---|---|---|
| 1 | The map's plan gate: may the build start without the owner approving the plan? | Yes. Plan recorded on the hand-off (step lines) and in the spec's §1–§5 | Standing delegation: take the recommended answer |
| 2 | Currency-less rows: bare number or em dash? | Bare number (deviation noted in scratch step4-notes.md) | Smallest reversible option; never invent a code |
| 3 | Lock date format given ICU "Sept" | Day + en-US short month + year ("12 Sep 2026") | Spec copy + hydration safety (server/browser ICU drift) |
| 4 | S8 duplicate "ask an owner" message (evaluate P2) | Kept; not #457 | The page banner is pre-existing admin shell (#252); the card line is spec §5.4 content |

## Decisions recorded
Company currency is LSL or ZAR only, set by the country at creation. The ledger's home currency is read before every push, and a mismatch fails *Ledger currency differs* (never converted). An Owner can switch the currency until the first successful posting or Payment batch records the lock. Existing companies are migrated, and locked ones go to a support list. All amounts go through one formatter (ADR 0013).

## Tickets created / changed
None created. The map's line for [Decide the company currency](https://github.com/petrose99/Wes-doc/issues/446) marks this build as shipped.

## Scores and counts (rendered surfaces only)
scores: predicted-critique=34 first-critique=38 close-critique=38 first-evaluate=95 close-evaluate=95

Gate: r0 had 10 real findings (integrations card) and 11 failing probes. r2 and m1 are CLEAN at 1440 and 390: 0 real findings, 52/52 probes pass. The residue is unchanged (ai-color-palette, overused-font, layout-transition, dark-glow, span.truncate, pre-existing line-length). Include: PASS at 1440 and at 390. The close is on the measured tree: no UI changed after measure, since the C1/C2 fixes were backend and imports only. Critique: H7 3, H10 3, rest 4. Evaluate: P0 0, P1 0, anti-pattern Clean. Review: P0=0 P1=0.

## First-pass findings → lessons (rendered surfaces only)
| Finding (heuristic, severity) | Lesson / contract that should have caught it | Status | Action taken |
|---|---|---|---|
| Lock date rendered "Sept" (en-GB short month on this ICU) | Dates in copy: never trust en-GB `month: "short"`; assemble day + en-US short month + year | new class | Fixed; lesson here (the project lessons file is edit-protected in this sandbox) |
| Probe missed the Change dialog | ConfirmDialog renders `role=alertdialog`, not `dialog`; probes must query both | new class | Round script fixed; lesson here |
| line-length + cramped-padding on the card line | craft-floor 65ch cap / sm button padding | unchecked | Fixed at r0 |

## Needs the owner
- Run `scripts/migrate-company-currency.ts --dry-run` on production before the real run. Locked companies on an unsupported pair land in `logs/company-currency-support-list.json` for support to handle.
- Pre-existing lint error `direction` unused in `lib/integration-push.ts` (not this ticket).
