# #462 hand-off

No milestone complete yet — spec phase in progress, research done, spec text not yet written.

## What this session found (recon complete, no code/spec written)

- #461 (backend, blocking) is closed and shipped: `IntegrationAttachment` queue, `lib/integration-attach*.ts`, `lib/integrations/attach-limits.ts`, `lib/checks/attachment-limit.ts`, and the two server actions this ticket's UI calls already exist:
  - `retryAttachingAction(workspaceId, documentId)` — `app/(app)/workspaces/[workspaceId]/integration-attach-actions.ts` (any member; refuses if not `failed` or errorCode is permanent).
  - `countBackfillableAttachmentsAction(workspaceId)` / `backfillAttachSourceFilesAction(workspaceId)` — owner-gated.
- #450's resolution comment (map #445) has the exact table strings for the Bill header trailing text (7 states) and the ledger-pill states — copy verbatim, do not reword.
- Ledger pill bug to fix: `app/(app)/workspaces/[workspaceId]/documents/[documentId]/page.tsx:243` (`const ledger = succeededPushCount > 0 ? "posted" : ...`) never produces `"posting"`/`"failed"` even though `LEDGER_FACT_LABELS`/`StatePills`/`LEDGER_PILL_TONE` (`lib/documents/processing-state.ts`, `components/queue/row-cells.tsx`) already support all four. Need push status (`pushes` already loaded above) to also detect `"posting"` (pending push) and `"failed"` (failed push, with reason).
- `StatusLine` (`components/queue/status-line.tsx`) already takes a `trailing` node; today only `BillStatusTrack` (`components/pipeline/document-detail/bill-pane.tsx:96-140`) uses it, for `· Paid {date}`. The new source-file-state text goes in the same slot, appended after the paid text per #450.
- Attach row data needed: `IntegrationAttachment` (status, attempts, errorCode, connectionId) keyed by `pushId` — need a per-document read (none exists yet in `page.tsx`); `PERMANENT_ATTACH_ERROR_CODES` (`lib/integration-attach-policy.ts`) distinguishes retryable vs permanent failure for the Retry-attaching gate.
- Reason text sources: `lib/integrations/attach-limits.ts`'s `attachmentLimitViolation(...).text` gives the exact "‹Ledger› takes files up to N MB; this one is M MB." wording (already used pre-post by `lib/checks/attachment-limit.ts`); `lib/health/checks/push-failures.ts`'s private `ATTACH_ERROR_SENTENCES`/`describeAttachError` gives humanized text for the other permanent codes — **not exported**, needs exporting (or a shared helper) so the header can reuse it rather than re-typing (project lesson: docubite #458 P1 was exactly this duplication).
- No existing push-errorCode humanizer for "Post failed (with its reason)" (item 1) — precedent (Health tab) shows raw `errorCode`; plan to reuse/extend the same sentence-map pattern rather than invent a second one.
- "Retry attaching" precedent: `components/queue/history-tabs.tsx` `ChecksTab`'s `ledgerRetry` block (lines ~313-357) is the closest existing inline-retry-with-announcement pattern; for the header's *inline* trailing button, the tighter precedent is the underline-text-button style used in `components/queue/add-to-claim-dialog.tsx:140` / `components/documents/line-items-editor.tsx:421` (fits inside a 13px status line better than a bordered `Button`).
- Owner reconnect link precedent: `components/queue/connection-band.tsx` (`ConnectionBand`) — Owner gets a `Link` to `/workspaces/{id}/admin/integrations`, non-owner gets "Ask an owner" text. Same shape fits the header's "waiting on reconnect" row; `isOwner` is already threaded into `InvoiceQueue`/etc., needs threading into `page.tsx`'s bill-detail render path (not currently passed to `BillSplitPane`/`StatusLine`).
- Admin › Integrations back-fill control: lands in `components/integrations/integrations-manager.tsx`'s `AccountingConnectionCard` (owner-gated, `connection.status === "connected"` section, same area as the existing "Sync accounts" button ~line 203). No existing "count-first, then outcome" UI pattern in this file to copy verbatim — design as: load count on mount (owner+connected only), hide control entirely if count is 0 (never a dead button), button reads "Attach source files to N bills already posted", on click runs backfill and replaces button with an outcome line ("Queued N attaches.").
- Relevant project lessons (already read, don't re-read in full): never hand-write a ledger/status word (extend `LEDGER_FACT_LABELS`, don't inline strings); a duplicated user-facing string across two files is a P1 at close (export one constant); `sr-only` text inside a `<button>` misreads on the detector as `cramped-padding` — use visible text + `aria-label`, not `sr-only`; disabled vs `readOnly` for tab-order removal; H1/H5 are the two default excellence targets per `preflight.md` Part A.
- Area primers read in full: `docs/agents/areas/detail-pane.md` (StatusLine/BillPane primitives, known #367 `←`/`×` regression — not this ticket's to fix), `docs/agents/areas/integrations.md` (`AccountingConnectionCard`, `isOwner` gating pattern, Ledger currency line as another "state-dependent line under the card" precedent).

## Not yet done
- Spec text (fortify state table + clarify copy table for the three items) — not written.
- `preflight.md` Parts A–E, including the Part D independent spec-critic `Agent` call — not started.
- Build plan (`step:` lines) on this hand-off — not written.
- Sizing check (≤6 build steps / ≤10 states) — not done, but this looks like 1 backend-touch (export/extend a sentence map) + 3 UI areas (ledger pill fix, StatusLine trailing block + Retry, Admin back-fill control) — likely fits in ≤6 steps; confirm when the plan is drafted.

## Next step
Write the spec (fortify's state table + clarify's copy table) for the three items in #462's body, using the exact #450 table strings; then fill `preflight.md` (copy to `docs/wayfinder-reports/445/logs/scratch-462/462-preflight.md`), run the Part D spec-critic `Agent`, reconcile, then write the `step:` build plan onto this hand-off and commit with `milestone: spec-done`.
