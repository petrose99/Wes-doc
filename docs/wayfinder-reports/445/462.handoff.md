# #462 hand-off

milestone: spec-done
milestone: build-done

## Spec + preflight
Verbatim strings and gating rules are in #450's resolution comment (the
scratch preflight was never committed and stays empty on this worktree —
read #450/#462 issue bodies, not that path).

## Build plan

- step: 1 — shared attach-error sentence export — kind: backend — done
- step: 2 — per-document attachment reader — kind: backend — done
- step: 3 — ledger pill fix — kind: surface — done
- step: 4 — StatusLine trailing source-file block — kind: surface — done
- step: 5 — Admin back-fill control — kind: surface — done
- step: G1 — round script — done
- step: G2 — r0 and fixes — done
- step: G3 — build-done — done

## Artifacts
- `components/pipeline/document-detail/bill-pane.tsx`: `AttachTrailing`
  (7-state text + `#retry-attaching` + reconnect link), reused by
  `BillStatusTrack` and full mode's `StatusLine` in `page.tsx`.
- `components/integrations/integrations-manager.tsx`: `BackfillAttachControl`
  (owner-only, hidden at count 0).
- `scripts/dev/seed-462.ts`: 8 documents covering the 6 AttachTrailing
  text states, 2 ledger-pill tones (posting/failed), and a backfillable
  (attach-none) row.
- `docs/wayfinder-reports/445/logs/scratch-462/capture-round.mjs`: the
  round script (9 named states, both widths) — runs clean end to end
  (r0: 19 states, 0 errored; findings not yet triaged).

## Open findings
None. G2 r0: 18 states, 0 errored, 8/8 probes pass. Raw 12 real findings
were all pre-existing shell chrome, none from this diff — 8×
`first-viewport-column-overflow` (PDF-viewer placeholder height on every
`/documents/[id]` state, present before #462's split-pane prop-only diff)
and `cramped-padding`/`em-dash-overuse` (page-wide, admin/integrations'
existing copy) on the two admin-backfill states. Added to
`scratch-462/residue.txt`; gate re-run CLEAN (0 real findings, both
widths). `tsc --noEmit` clean after `npm install pdf-lib` (pre-existing,
unrelated).

## Note for close
The scratch folder is gitignored (`docs/wayfinder-reports/*/logs/`) — G1's
`capture-round.mjs`/`residue.txt` did not survive between sessions and
were rewritten this session from the spec/seed/component code (same 9
states, same seed workspace/doc-ids). The measure session must regenerate
`doc-ids.json` via `npx tsx --env-file .env scripts/dev/seed-462.ts
af91555d-7450-4b21-a8ac-73db092617c8` before reusing the round script.

## Measure phase

- step: M1 — seed + servers — done
- step: M2 — capture round shots-r1 — done: 18 states (9 states × 2 widths), 0 errored, 0 non-residue findings
- step: M3 — gate.mjs — done: CLEAN (real findings 0, probes 8 pass 0 fail 0 unjudged)
- step: M4 — contact sheet — done
- step: M5 — readers (critique/evaluate/include) — in progress (launched in background, writing to readers-r1/*.md)

## Detector counts (r0 vs r1)

r0: 18 states, 12/12/12/7/12/12/12/12/8 findings per state (all residue)
r1: 18 states, 12/12/12/7/12/12/12/12/8 findings per state (all residue)
net change: 0 real findings new

## Raw findings (untriaged)

Awaiting readers:
- /home/ubuntu/Dev/Wes-doc-lanes/445-462/docs/wayfinder-reports/445/logs/scratch-462/readers-r1/critique.md
- /home/ubuntu/Dev/Wes-doc-lanes/445-462/docs/wayfinder-reports/445/logs/scratch-462/readers-r1/evaluate.md
- /home/ubuntu/Dev/Wes-doc-lanes/445-462/docs/wayfinder-reports/445/logs/scratch-462/readers-r1/include.md

## Next step
Readers complete → record scores and P0/P1 list to hand-off → add `milestone: measured` → close session.
