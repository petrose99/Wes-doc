# #462 hand-off

milestone: spec-done
milestone: build-done
milestone: measured
milestone: close-review-done
milestone: close-gate-c2-clean

## Step plan

- step: code-review (Standards+Spec, fixed point 1a98c41), fix P0/P1 — done
- step: triage r1 findings (all timing artifacts) — fix round-script wait — done
- step: gate rounds c1, c2 vs baselines — CLEAN both, 2 of 3 rounds used — done
- step: verify em-dash-overuse and cramped-padding on 09-admin-backfill are residue not real — todo (em-dash checked, residue; cramped-padding not yet)
- step: file follow-up Wayfinder ticket for permanent-failure exact-string gap — todo
- step: confirm round — 3 fresh readers on shots-c2 (contact sheet already built) — todo
- step: checks once (dev server stopped): vitest → tsc → eslint → next build — todo
- step: lessons + area primer + report + close — todo

## Artifacts

- `components/pipeline/document-detail/bill-pane.tsx`: dedup'd `ATTACH_PROVIDER_LABELS`
  (now in `lib/integrations/attach-errors.ts`), `useEffect` resync fix on `AttachTrailing`.
- `models/audit-events.ts` + `.test.ts`: `integration_attach_failed/_succeeded` audit labels
  with reason (fixes Spec P1 "full text ... on the Audit tab").
- `docs/wayfinder-reports/445/logs/scratch-462/`: `close.md` (full triage + code-review detail
  + session-2 notes), `review-standards.md`, `review-spec.md`, `capture-round.mjs` (now with
  `waitPaneReady` fix), `shots-c1/`, `shots-c2/`, `gate-c1.json`, `gate-c2.json`,
  `contact-sheet-c2.png`, `probe-08.mjs`/`probe-08b.mjs`/`probe-08b-final.png` (live-verify
  evidence that r1's P0/P1s were capture-timing artifacts).

## Open findings

See `<scratch>/close.md` (full triage, code-review results, session-2 continuation notes at
the bottom) and `review-standards.md`/`review-spec.md` for the untriaged code-review detail.
One real gap deferred as a decision (not fixed): the permanent-failure trailing text can't show
#450's exact templated string (real ledger name + MB numbers) without a schema change to
`IntegrationAttachment` — needs a follow-up ticket, not yet filed.

## Next step

Finish the cramped-padding residue check the same way em-dash-overuse was checked (grep the
component's own markup vs the surrounding page); file the follow-up ticket; then run the
confirm-round readers on shots-c2 (contact sheet already built, do not recapture or re-gate).
