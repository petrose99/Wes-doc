# #462 hand-off

milestone: spec-done

## Spec + preflight
Full spec (screen-by-screen, fortify state table, clarify copy — all
strings verbatim from #450), preflight Parts A-F filled, Part D spec-critic
run (fresh-context Agent, sonnet: 34/40, no heuristic under 3, Clean-with-
caveat, 4 gaps reconciled into the spec) at
`docs/wayfinder-reports/445/logs/scratch-462/462-preflight.md`.

Gate met: predicted zero P0/P1, no heuristic under 3, two 4s (H1, H5),
Clean.

## Build plan

- step: 1 — shared attach-error sentence export — kind: backend — todo
  Extract `ATTACH_ERROR_SENTENCES`/`describeAttachError` out of
  `lib/health/checks/push-failures.ts` into a new `lib/integrations/
  attach-errors.ts` (doc comment states the clause-fragment contract:
  values read as the second half of "... — {sentence}."); re-export from
  push-failures.ts or update its one caller. seams: `describeAttachError` →
  `lib/integrations/attach-errors.test.ts` (new).
- step: 2 — per-document attachment reader — kind: backend — todo
  Add `getDocumentAttachment(workspaceId, documentId)` to
  `models/integrations.ts`: latest `IntegrationAttachment` for the
  document's push, selecting status/attempts/errorCode/provider and
  `connection: { status }`. seams: `getDocumentAttachment` →
  `models/integrations.test.ts` (extend existing file).
- step: 3 — ledger pill fix — kind: surface — todo
  `page.tsx:243` per spec Surface 1 (paid > posted > failed > posting >
  null), typed `LedgerFact | null`. Shared by full-mode `StatusLine` and
  `BillSplitPane`'s `BillStatusTrack` (same `ledger` binding, no duplicate
  logic).
- step: 4 — StatusLine trailing source-file block — kind: surface — todo
  Wire step 2's reader + step 1's sentences into `page.tsx`'s render path
  (both full mode and the `BillSplitPane` branch) and `BillStatusTrack`'s
  `trailing` prop per spec Surface 2: 7-state text, Retry-attaching button
  (`id="retry-attaching"`, gated on `canPush`, calls `retryAttachingAction`),
  reconnect link/text gated on `isOwner` (thread `isOwner =
  membership.role === "owner"` into the `BillSplitPane` render path — not
  currently passed).
- step: 5 — Admin back-fill control — kind: surface — todo
  `components/integrations/integrations-manager.tsx`: owner-only control
  below the connections `<ul>` per spec Surface 3 (count-first, hidden at
  0, "Queued N attaches." on click), calling
  `countBackfillableAttachmentsAction`/`backfillAttachSourceFilesAction`.
- step: G1 — round script — todo
  Capture 9 visual states at two widths (2 pill tones + 4 StatusLine
  states + 3 back-fill states, per preflight's sizing section); grep-verify
  the remaining string-literal states against source instead of capturing
  them.
- step: G2 — r0 and fixes — todo
- step: G3 — build-done — todo

## Artifacts
- `docs/wayfinder-reports/445/logs/scratch-462/462-preflight.md`

## Open findings
None yet — build not started.

## Next step
Build session: start step 1 (shared attach-error sentence export), run its
test, mark done, commit `wip(autopilot): #462 step 1`.
