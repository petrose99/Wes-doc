# #457 Build: company currency — hand-off

milestone: spec-done

Title starts `Build:` (colon), so run.sh may drive it as SINGLE sessions: each session reads the
milestone and runs the matching phase brief (`scripts/wayfinder-autopilot/phases/*.md`).

design pass: owed for steps 4–5 (surface); steps 1–3 backend (CODING_STANDARDS.md, tests red first).
skills: Intent fortify · articulate · include; Impeccable clarify → harden → polish; craft-floor.md.
Headcount: product:product-requirements (spec only; later sessions load none).
Spec §9 (critic reconciliation) overrides §1–§7 where they differ — read it with every step.

step: 1 — model + recorded lock + change fn — kind: backend — done
step: 2 — migration + Owner notice + seed — kind: backend — done
step: 3 — ledger currency read + Check + push gate + re-queue — kind: backend — done
step: 4 — shared formatter + callers + analytics — kind: surface — done
step: 5 — surfaces: creation fields, Company currency row, Change dialog, connection card — kind: surface — done
step: G1 — round script — todo — 10 states spec §6, seed scripts/dev/seed-457.ts, residue from docs/agents/areas/admin.md
step: G2 — r0 and fixes — todo
step: G3 — build-done — todo

## Artifacts
- Spec: docs/wayfinder-reports/445/457-spec.md
- Pre-flight (A–E filled, D reconciled 32/40, 0 P1): docs/wayfinder-reports/445/457-preflight.md
- Scratch (gitignored): docs/wayfinder-reports/445/logs/scratch-457/{recon,446-resolution,critic,plan-comment,edit-card}.md|mjs
- Backend: prisma/migrations/20260926090000_add_company_currency_lock; lib/geo/company-currency.ts; models/company-currency.ts; models/company-currency-migration.ts; scripts/migrate-company-currency.ts [--dry-run] (run it on the dev DB before G1 — seeded rows stay US/USD until then).
- Surfaces: components/workspace/company-country-currency-fields.tsx; components/admin/{company-detail,companies-queue,change-currency-dialog}.tsx; components/integrations/integrations-manager.tsx (LedgerCurrencyLine, provider-name focus §9.9); admin/integrations/page.tsx passes `company` {currency,country,lock,unpostedCount}; lib/integrations/ledger-currency-outcome.ts (+test).

## Open findings
- Step 4 notes (deviation: currency-less rows print a bare number; G2 watch list): logs/scratch-457/step4-notes.md
- Copy deviation from #446 flagged on the ticket (blocked-outcome line, spec §5.4): owner may overrule.
- Pre-existing lint warnings (set-state-in-effect) in companies-queue.tsx:213, new-workspace-form.tsx:43 — not errors.

## Next step
Build step G1: write the capture-round script and scripts/dev/seed-457.ts for the 10 states of spec §6.
