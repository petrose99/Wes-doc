# #457 Build: company currency — hand-off

No `milestone:` line yet: the spec phase is in progress (not spec-done).

Note: the title starts `Build:` (colon), so run.sh's `^Build ` regex misses it and the driver
runs this ticket as a SINGLE session. Continue the spec phase per
`scripts/wayfinder-autopilot/phases/spec.md`, then build/measure/close in later sessions,
reading the milestone off this file.

## Done so far (session 1)
- Ticket claimed. Read: #446 resolution (the decision), ADR 0013, CONTEXT.md "Company currency"
  (CONTEXT.md:316-318), both lessons files, CODING_STANDARDS.md, craft-floor.md, area primers
  `docs/agents/areas/admin.md` (Companies §#285, save grammar) and `integrations.md`
  (`AccountingConnectionCard`), skill digests specify/fortify/articulate/include(1-90)/impeccable,
  `impeccable/reference/{shape,clarify}.md`. No critique snapshot exists for Admin › Companies.
- Plan gate (owner): under the standing delegation, the next session posts the plan as a ticket
  comment and proceeds (no waiting).

## Facts gathered
- Inline currency formatters (14 files): document-queue-page.tsx, dashboard/page.tsx,
  admin/approval-flows/page.tsx, components/queue/{row-cells,exception-queue}.tsx,
  components/payments/format.ts (en-ZA), components/integrations/account-correction-dialog.tsx,
  components/documents/{fx-conversion-badge,line-items-editor}.tsx,
  components/typed-destinations/bulk-approve-receipt.tsx,
  components/pipeline/{synced,paid}-stage-header.tsx, lib/matching/format.ts, models/documents.ts.
- Non-test `"USD"` literals: lib/gates/{supplier-trust,match-variance,smb-ceiling,confidence-band}.ts,
  lib/geo/{iso-lists,country-currency}.ts, lib/claims/totals.ts, lib/fx/rates.ts,
  lib/tax/regions/us.ts, lib/notices/fixtures.ts, models/{approval-notices,organizations,workspaces}.ts,
  components/workspace/new-workspace-form.tsx, components/admin/companies-queue.tsx,
  plus the formatter files above and prisma/seed.ts.
- A recon agent was writing the full touchpoint map to
  `docs/wayfinder-reports/445/logs/scratch-457/recon.md` (gitignored). If absent, re-run a
  foreground Explore with the 12-point brief: schema lines, creation paths, pickers, Companies
  Currency row, FX re-conversion + "unposted", lock facts (first posted bill / first Payment
  batch), QBO /preferences + Xero /Organisation reads, push entry, lib/checks registry,
  connection card, formatters, analytics (lib/analytics/workspace-analytics.ts:171-182),
  Owner notice mechanism, owner guard.

## Artifacts
- Spec (to write, committed): `docs/wayfinder-reports/445/457-spec.md`
- Pre-flight (to fill, committed): `docs/wayfinder-reports/445/457-preflight.md`
- Scratch: `docs/wayfinder-reports/445/logs/scratch-457/`

## Open findings
- none yet

## Next step
Read recon.md (or re-run recon), then write the spec + build plan (steps from the ticket body:
model/lock, migration+notice+seed, ledger currency read + Check, shared formatter, surfaces,
gates) with kind per step, fill pre-flight A–E with an opus spec critic, post the plan comment,
write `milestone: spec-done`, commit `wip(autopilot): #457 spec`.
