# #457 Build: company currency — hand-off

milestone: spec-done

Title starts `Build:` (colon), so run.sh may drive it as SINGLE sessions: each session reads the
milestone and runs the matching phase brief (`scripts/wayfinder-autopilot/phases/*.md`).

design pass: owed for steps 4–5 (surface); steps 1–3 backend (CODING_STANDARDS.md, tests red first).
skills: Intent fortify · articulate · include; Impeccable clarify → harden → polish; craft-floor.md.
Headcount: product:product-requirements (spec only; later sessions load none).
Spec §9 (critic reconciliation) overrides §1–§7 where they differ — read it with every step.

step: 1 — model + recorded lock + change fn — kind: backend — todo (part built, see step1-remaining) — spec §1, §9.1–9.3, §9.8 — lib/geo/company-currency.ts (new), models/company-currency.ts (new), models/{workspaces,organizations}.ts, PaymentRun create model fn, prisma schema (Workspace lock cols, IntegrationConnection ledgerCurrency/ReadAt) + additive migration, lib/gates/* + lib/claims/totals.ts + models/approval-notices.ts USD fallbacks, action-helpers.ts, admin/companies/actions.ts changeCompanyCurrencyAction — seams: allowedCurrencies/defaultCurrency/isAllowedPair/personalCountry → lib/geo/company-currency.test.ts; getCurrencyLock/recordCurrencyLock/countUnpostedDocuments/changeCompanyCurrency (FOR UPDATE, in-flight refusal, rate date kept) → models/company-currency.test.ts; create refusals → models/{workspaces,organizations}.test.ts
step1-remaining: (a) USD fallbacks — lib/gates/{supplier-trust:120,match-variance:150,confidence-band:177} read `ctx.document.baseCurrency ?? "USD"` but Document has no baseCurrency, so it is always USD: fix at the root in lib/gates/registry.ts runOnArrival (read workspace.baseCurrency once via client, pass `{...ctx, document:{...ctx.document, baseCurrency}}`), drop the literals, update the three gate tests' baseCtx to set baseCurrency "ZAR" (supplier-trust.test:168 expects "USD"); smb-ceiling.ts:132 `?? "USD"`; lib/claims/totals.ts:26 receipt currency fallback → caller passes company currency; models/approval-notices.ts:293. (b) changeCompanyCurrencyAction in app/(app)/workspaces/[workspaceId]/admin/companies/actions.ts (requireMember owner on companyId, errorMessage, revalidatePath companies list+[companyId]+integrations). (c) tsc --noEmit (dev server stopped), simplify pass, mark step 1 done, commit.
step: 2 — migration + Owner notice + seed — kind: backend — todo — spec §2, §9.7 — models/company-currency-migration.ts, scripts/migrate-company-currency.ts, prisma/seed.ts (seed.ts:97,116 still pass US/USD and now throw — fix here), lib/notices/fixtures.ts — seams: migrateCompanyCurrencies (support list via getCurrencyLock, lock backfill, noticePending two-pass) → models/company-currency-migration.test.ts
step: 3 — ledger currency read + Check + push gate + re-queue — kind: backend — todo — spec §3, §9.1 (lock on push success: call recordCurrencyLock in attemptIntegrationPush), §9.4 (add requeued to changeCompanyCurrency's return + audit), §9.5 — quickbooks/client.ts, xero/client.ts, lib/integrations/ledger-currency.ts, lib/checks/ledger-currency.ts, lib/integration-push.ts, app/api/webhooks/nango/route.ts:104, action-helpers.ts — seams: checkLedgerCurrency → lib/checks/ledger-currency.test.ts; readLedgerCurrency (60 s reuse) + requeueLedgerCurrencyFailures → lib/integrations/ledger-currency.test.ts; attemptIntegrationPush mismatch/unreadable/lock-record → lib/integration-push.test.ts
step: 4 — shared formatter + callers + analytics — kind: surface — todo — spec §4 — lib/money.ts formatCurrency + the files in logs/scratch-457/recon.md §9, components/payments/format.ts (8 importers), lib/analytics/workspace-analytics.ts resolveCurrency — check: lib/money.test.ts; the two §4 greps; tsc
step: 5 — surfaces: creation fields, Company currency row, Change dialog, connection card — kind: surface — todo — spec §5.1–5.4, §7, §9.2, §9.6, §9.9, §9.10 — company-country-currency-fields.tsx, new-workspace-form.tsx, companies-queue.tsx, company-detail.tsx, change-currency-dialog.tsx, lib/admin/companies.ts + loadCompanyDetailAction, lib/integrations/ledger-currency-outcome.ts (+ test), integrations-manager.tsx, admin/integrations/page.tsx — check: ledgerCurrencyOutcome test, tsc, eslint on touched files, pre-flight B1/B3 greps
step: G1 — round script — todo — 10 states spec §6, seed scripts/dev/seed-457.ts, residue from docs/agents/areas/admin.md
step: G2 — r0 and fixes — todo
step: G3 — build-done — todo

## Artifacts
- Spec: docs/wayfinder-reports/445/457-spec.md
- Pre-flight (A–E filled, D reconciled 32/40, 0 P1): docs/wayfinder-reports/445/457-preflight.md
- Scratch (gitignored): docs/wayfinder-reports/445/logs/scratch-457/{recon,446-resolution,critic,plan-comment}.md
- Step 1 so far: migration prisma/migrations/20260926090000_add_company_currency_lock; lib/geo/company-currency.ts (+test, companyCountryAndCurrency resolver); models/company-currency.ts (+test); lock on PaymentRun create in models/payment-runs.ts + payment-batches.ts; 4 error codes in action-helpers.ts. 88 affected tests green; `npx prisma generate` run.

## Open findings
- Copy deviation from #446 flagged on the ticket (blocked-outcome line, spec §5.4): owner may overrule.

## Next step
Finish step 1 per `step1-remaining` (a)→(c), then mark it done.
