# #457 Build: company currency — hand-off

milestone: spec-done

Title starts `Build:` (colon), so run.sh may drive it as SINGLE sessions: each session reads the
milestone and runs the matching phase brief (`scripts/wayfinder-autopilot/phases/*.md`).

design pass: owed for steps 4–5 (surface); steps 1–3 backend (CODING_STANDARDS.md, tests red first).
skills: Intent fortify · articulate · include; Impeccable clarify → harden → polish; craft-floor.md.
Headcount: product:product-requirements (spec only; later sessions load none).
Spec §9 (critic reconciliation) overrides §1–§7 where they differ — read it with every step.

step: 1 — model + recorded lock + change fn — kind: backend — todo (only step1-remaining left) — spec §1, §9.1–9.3, §9.8 — lib/geo/company-currency.ts, models/company-currency.ts, models/{workspaces,organizations}.ts, PaymentRun create, prisma migration, gates/claims/notices USD fallbacks, action-helpers.ts, admin/companies/actions.ts changeCompanyCurrencyAction
step1-remaining: (c) only — run `npx vitest run lib/gates lib/claims lib/geo models/company-currency models/expense-claims models/approval-notices models/po-matching models/workspaces models/organizations models/payment-runs models/payment-batches`; `npx tsc --noEmit` (dev server stopped) and fix what it lists (GateContext.baseCurrency is now required — any GateContext literal missing it; unused `Document` type imports in lib/gates/{supplier-trust,match-variance,confidence-band}.ts); simplify pass over `git diff HEAD~1`; flip step 1 to done; commit `wip(autopilot): #457 build step 1`. (a) and (b) are done and committed in the hand-off commit.
step: 2 — migration + Owner notice + seed — kind: backend — todo — spec §2, §9.7 — models/company-currency-migration.ts, scripts/migrate-company-currency.ts, prisma/seed.ts (seed.ts:97,116 still pass US/USD and now throw — fix here), lib/notices/fixtures.ts — seams: migrateCompanyCurrencies (support list via getCurrencyLock, lock backfill, noticePending two-pass) → models/company-currency-migration.test.ts
step: 3 — ledger currency read + Check + push gate + re-queue — kind: backend — todo — spec §3, §9.1 (lock on push success: call recordCurrencyLock in attemptIntegrationPush), §9.4 (add requeued to changeCompanyCurrency's return + audit), §9.5 — quickbooks/client.ts, xero/client.ts, lib/integrations/ledger-currency.ts, lib/checks/ledger-currency.ts, lib/integration-push.ts, app/api/webhooks/nango/route.ts:104, action-helpers.ts — seams: checkLedgerCurrency → lib/checks/ledger-currency.test.ts; readLedgerCurrency (60 s reuse) + requeueLedgerCurrencyFailures → lib/integrations/ledger-currency.test.ts; attemptIntegrationPush mismatch/unreadable/lock-record → lib/integration-push.test.ts
step: 4 — shared formatter + callers + analytics — kind: surface — todo — spec §4 — lib/money.ts formatCurrency + the files in logs/scratch-457/recon.md §9, components/payments/format.ts (8 importers), lib/analytics/workspace-analytics.ts resolveCurrency; also the `?? "USD"` literals in app/(app)/workspaces/[workspaceId]/{(queue)/document-queue-page.tsx:77,dashboard/page.tsx:87,admin/approval-flows/page.tsx:38} — check: lib/money.test.ts; the two §4 greps; tsc
step: 5 — surfaces: creation fields, Company currency row, Change dialog, connection card — kind: surface — todo — spec §5.1–5.4, §7, §9.2, §9.6, §9.9, §9.10 — company-country-currency-fields.tsx, new-workspace-form.tsx, companies-queue.tsx, company-detail.tsx, change-currency-dialog.tsx (calls changeCompanyCurrencyAction(workspaceId, companyId, currency)), lib/admin/companies.ts + loadCompanyDetailAction, lib/integrations/ledger-currency-outcome.ts (+ test), integrations-manager.tsx, admin/integrations/page.tsx — check: ledgerCurrencyOutcome test, tsc, eslint on touched files, pre-flight B1/B3 greps
step: G1 — round script — todo — 10 states spec §6, seed scripts/dev/seed-457.ts, residue from docs/agents/areas/admin.md
step: G2 — r0 and fixes — todo
step: G3 — build-done — todo

## Artifacts
- Spec: docs/wayfinder-reports/445/457-spec.md
- Pre-flight (A–E filled, D reconciled 32/40, 0 P1): docs/wayfinder-reports/445/457-preflight.md
- Scratch (gitignored): docs/wayfinder-reports/445/logs/scratch-457/{recon,446-resolution,critic,plan-comment}.md
- Step 1 code: migration prisma/migrations/20260926090000_add_company_currency_lock; lib/geo/company-currency.ts (+test); models/company-currency.ts (+test; getCompanyCurrency, getCurrencyLock, recordCurrencyLock, countUnpostedDocuments, changeCompanyCurrency); GateContext.baseCurrency filled by registry.runOnArrival + every reevaluate path; lib/claims/totals.ts (+new test); approval-notices notifySentBack; changeCompanyCurrencyAction in admin/companies/actions.ts.

## Open findings
- Copy deviation from #446 flagged on the ticket (blocked-outcome line, spec §5.4): owner may overrule.

## Next step
Finish step 1 per `step1-remaining` (c): tests, tsc, simplify, mark done, commit.
