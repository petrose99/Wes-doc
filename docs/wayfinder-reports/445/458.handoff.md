# #458 Build: line Tax code, Tax basis and Tracking — hand-off

(no milestone yet — SPEC phase in progress; the next session is SPEC again)

Title starts `Build:` — four phases per the autopilot brief. Ticket claimed (assignee set).
design pass: owed for step 6 only (Accounting rule line, `components/settings/supplier-accounts-table.tsx`);
steps 1–5 backend (CODING_STANDARDS.md, tests red first).
skills: Intent specify · fortify · articulate · include (digests); Impeccable shape · layout · typeset · clarify, craft-floor.md
— all read in session 1. Headcount: product:product-requirements (read; later sessions load none).
Plan gate (map Notes): post the plan as a ticket comment before build; owner's delegation takes the recommended answer.

Draft plan (finalise in spec session 2; step order differs from the ticket's numbering — see spec DRAFT section):
step: 1 — capabilities + reference sync — kind: backend — todo
step: 2 — supplier rules learn the set (schema + learn) — kind: backend — todo
step: 3 — line/bill coding model + pre-fill + tax basis — kind: backend — todo
step: 4 — coding Checks, eligibility, push-time gate — kind: backend — todo
step: 5 — mappers + post read-back + correction resend test — kind: backend — todo
step: 6 — Accounting rule line + Forget confirm — kind: surface — todo
step: G1 — round script — todo
step: G2 — r0 and fixes — todo
step: G3 — build-done — todo

## Artifacts
- Spec (draft, §0 written + DRAFT decisions): docs/wayfinder-reports/445/458-spec.md
- Code recon (file:line): docs/wayfinder-reports/445/458-recon.md
- Template: scripts/wayfinder-autopilot/preflight.md · precedent: docs/wayfinder-reports/445/457-spec.md, 457-preflight.md

## Open findings
- None yet. Spec-phase work left: see Next step.

## Next step
Spec session 2: turn the spec's DRAFT section into §1–§7 (per-step seams + test files, surface copy/states/a11y),
fill 458-preflight.md (A–E), run the Part D critic (opus: schema), post the plan comment, write the final step lines
with seams here, add `milestone: spec-done`, commit `wip(autopilot): #458 spec`.
