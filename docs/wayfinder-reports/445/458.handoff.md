# #458 Build: line Tax code, Tax basis and Tracking — hand-off

milestone: spec-done

design pass: owed for step 6 only (supplier table + Forget confirm); steps 1–5 backend (CODING_STANDARDS.md, tests red first).
skills for step 6: Intent articulate · include (digests); Impeccable clarify + craft-floor.md. Headcount: none.
Plan gate: posted as a ticket comment at spec (458-plan-comment.md); owner's delegation takes the recommended answer.

step: 1 — ledger capabilities + migration — kind: backend — done
step: 2 — reference sync — kind: backend — done — spec §2 — {quickbooks,xero}/client.ts, sync.ts, models/accounting-entities.ts — seams: paging + widened reads → {quickbooks,xero}/client.test.ts, new types/TaxType key → sync.test.ts, list widening → models/accounting-entities.test.ts
step: 3 — rules learn the set + line/bill coding model — kind: backend — done
step: 4 — snapshot + coding Checks + eligibility + push gate + 5030 — kind: backend — todo (parts 1–4 done: d91a841, b1470c4, 60862a4, 92d6b7b; see ## Step 4 remaining) — spec §4 — lib/integration-push.ts, lib/nango.ts, quickbooks/{client,errors}.ts, action-helpers.ts — seams: gateLineCoding/5030 → integration-push.test.ts, parse → quickbooks/errors.test.ts (new)
step: 5 — mappers + post read-back + correction resend — kind: backend — todo — spec §5 — {quickbooks,xero}/bill-mapper.ts, client.ts createBill, lib/integration-push.ts — seams: mappers → {quickbooks,xero}/bill-mapper.test.ts, createBill → client.test.ts, read-back warn → integration-push.test.ts, resend → {quickbooks,xero}/correction.test.ts
step: 6 — supplier table reads the set + Forget confirm — kind: surface — todo — spec §6 — components/settings/supplier-accounts-table.tsx, admin/integrations/page.tsx — check: static detect on both files, preflight B1–B5 rows, eslint
step: G1 — round script — todo — states S1–S6 × 1440/390 + 3 keyboard probes (spec §6 States)
step: G2 — r0 and fixes — todo
step: G3 — build-done — todo

## Artifacts
- Spec (rev 2): docs/wayfinder-reports/445/458-spec.md
- Pre-flight (A–E filled, D reconciled): docs/wayfinder-reports/445/458-preflight.md
- Spec critic: docs/wayfinder-reports/445/458-critic.md
- Code recon: docs/wayfinder-reports/445/458-recon.md
- Plan comment: docs/wayfinder-reports/445/458-plan-comment.md
- Capture precedent: docs/wayfinder-reports/226/logs/scratch-329/round-329.mjs (integrations area primer)

## Open findings
- None open. Engineering question noted in spec §5 (QBO TaxInclusive semantics; read-back warn guards).

## Step 4 remaining (done so far: pure check + snapshot fields + loadLineCodingContext; refresh on extraction/Save review/sync; pure firstLineCodingFail in lib/checks/line-coding.ts; resolveSelectionEligibility opts.lineCodingFail wired in post-selected; pushDocumentToConnection throws the fail's checkCode; it and autopublish pass billCoding+names(+lineAccounts))
- gateLineCoding(push, connection, bill, now, reuseMs = LEDGER_CAPABILITIES_REUSE_MS) in lib/integration-push.ts, called after gateLedgerCurrency for payloads with documentType !== "bank_statement" and taxBasis !== undefined (pre-458 snapshot skips): readLedgerCapabilities({...connection, workspaceId}, now, reuseMs) (throws retryable when unreadable) → loadLineCodingContext(ws, connection, caps) → checkLineCoding({...context, ...lineCodingInputFromBill(bill)})[0] → failPreflight(push, code, String(detail.text)). Add ledgerCapabilities + ledgerCapabilitiesReadAt to the connection select. Tests: mock @/lib/integrations/ledger-capabilities and @/models/accounting-entities in integration-push.test.ts (existing payloads have no taxBasis so are unaffected); cases: refuse+review task+no createBill, unreadable → pending, pre-458 skip, 5030 re-read (reuse 0) names the Check, 5030 with clean re-run → terminal quickbooks_feature_not_supported with review task.
- 5030: nangoProxy(…, init?, classify = classifyHttpStatus) reads response.text() on !ok and throws classify(status, body); QBO apiRequest passes quickbooksApiError, which parses Fault.Error[].code "5030" → IntegrationPermanentError("quickbooks_feature_not_supported"), else classifyHttpStatus (body may be non-JSON, e.g. "bill_not_found"). New quickbooks/errors.test.ts. In attemptIntegrationPush the quickbooks case catches that code → gateLineCoding(…, 0) (a Permanent from it rethrows; unreadable swallowed) → failPreflight(push, "quickbooks_feature_not_supported", sentence).
- BILLING_MESSAGES (app/(app)/workspaces/[workspaceId]/action-helpers.ts): a generic "your ledger" sentence per LINE_CODING_CHECK_CODES + quickbooks_feature_not_supported (spec §4 table, lines 148–162); grep that every code has one.
- Then vitest the touched test files, tsc --noEmit, simplify pass, flip step 4 to done.

## Next step
Finish step 4 per "Step 4 remaining" (tests red first), then mark it done and commit build step 4.
