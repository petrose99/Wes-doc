# #458 Build: line Tax code, Tax basis and Tracking — hand-off

milestone: spec-done
milestone: build-done
milestone: measured

design pass: owed for step 6 only (supplier table + Forget confirm); steps 1–5 backend.
skills for step 6: Intent articulate · include (digests); Impeccable clarify + craft-floor.md.
Plan gate: posted as a ticket comment at spec (458-plan-comment.md).

step: 1–6 — see prior hand-off history in git log — done
step: measure r1 capture + gate + readers — done — critique 36/40, evaluate 94/100 Clean, include PASS/PASS
step: close code-review (Standards + Spec, base 9acaa96) — done — 1 P1 fixed (BILLING_MESSAGES/
  line-coding text duplication → exported `LINE_CODING_FALLBACK_TEXT`), 1 eslint error fixed
  (unused `direction` param, integration-push.ts), 1 P2 fixed while in the file
  (`billable_off_in_quickbooks` now gates on provider + hardcodes "QuickBooks" per spec, was
  latent/unreachable). Commits 5c01a72 (fix), 299e8a6 (primer). review-standards.md/review-spec.md
  in scratch: both now P0=0 P1=0.
step: close surface triage — done — r1 was already CLEAN/PASS at measure (no fixes needed); the
  two open notes (native `disabled` on dialog buttons; residue gradient/glow not behind new text)
  verified against source, both confirmed non-issues (no re-score needed, nothing was dismissed
  from the readers' counts).
step: close checks (affected tests → full suite → tsc → eslint → next build), each once, dev
  server not running — done — 271+ affected tests pass, full suite 306 files/3052 tests pass,
  tsc clean, eslint clean on all changed files, `next build` succeeds.
step: primer — done — docs/agents/areas/integrations.md updated (supplier table + fallback-text
  lesson), committed 299e8a6.
step: lessons — todo
step: report — todo
step: close ticket — todo

## Artifacts
- Spec (rev 2): docs/wayfinder-reports/445/458-spec.md
- Pre-flight: docs/wayfinder-reports/445/458-preflight.md
- Plan comment: docs/wayfinder-reports/445/458-plan-comment.md
- r1 captures/gate/readers: docs/wayfinder-reports/445/logs/scratch-458/{shots-r1,gate-r1.json,readers-r1}/
- Close review (this session): docs/wayfinder-reports/445/logs/scratch-458/review-standards.md,
  review-spec.md — both P0=0 P1=0 after the fix commit (5c01a72)
- Base point for the review diff: commit 9acaa96 (land(#457), the commit before #458's work started)

## Open findings
- review-standards.md P2/P3 (judgement calls, not fixed): a 4x-repeated "primary connected
  connection" query shape across models/documents.ts + document-checks.ts (a
  `getPrimaryConnection()` helper would remove it); mild Feature Envy in `gateLineCoding`
  (documented push-gate seam, not an accident); mild Primitive Obsession in the tracking-selection
  shape (already partially named `TrackingSelection`, four ad hoc inline variants remain); a stray
  `// ponytail:` comment label in lib/finance/line-coding.ts worth renaming. None block close (all
  P2/P3, judgement calls per the skill's own rule).
- review-spec.md: no other findings beyond the billable_off_in_quickbooks P2 already fixed.

## Next step
Write the two lessons files (generic + project) from the code-review P1 (message/fallback-text
duplication across two files with nothing tying them together — a "one source of truth" lesson),
using the paths named in the phase brief. Then write the report at
docs/wayfinder-reports/445/458.md per scripts/wayfinder-autopilot/report.md's format, building the
session history from `git log --format='%h %s%n%b' -- <ticket files>`; scores line should read:
`review: P0=0 P1=0 findings=docs/wayfinder-reports/445/logs/scratch-458/review-standards.md,review-spec.md`
plus the measure-phase `scores:` line already recorded (critique 36/40, evaluate 94/100 0 P0 0 P1
Clean, include PASS/PASS). Commit the report, then `gh issue close 458` with a resolution comment,
append one line to map #445's Decisions so far, and stop any running dev processes (none were
started this session).
