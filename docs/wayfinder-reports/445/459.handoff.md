# #459 Build: item lines on QuickBooks and Xero bills — hand-off

milestone: spec-done
milestone: build-done
milestone: measured
milestone: close-review-fixed

design pass: not owed (no surface steps — all files under lib/, models/, prisma/, app/**/*actions.ts).
measure phase: skipped (no rendering surfaces touched).

step: 1-5 — backend build steps — done (see prior milestone lines)
step: G1/G3 — build-done gates — done (see prior milestone lines)
step: C1 — close code review (Standards + Spec, parallel sub-agents against base 8009099,
  the commit before #459's spec, not master) — done. Fixed the P0 (item-first PO-line match
  never reached production — codingData.items[].item_external_id never flowed into
  parseLineItemsForConsumption) and the P1 (Xero item-code-stripped push discarded
  created.id). One P2 (spec-wording nuance, resolveDocumentCodingItems's intended null) left
  as a documented gap, not a defect. Full triage, fixes and verification in
  docs/wayfinder-reports/445/logs/scratch-459/close.md.
step: C2 — full checks re-run after the fix batch — done: tsc clean, vitest full suite 307
  files/3100 tests passed, eslint 0 errors on touched files, next build clean (pre-fix run,
  no rendering files in the fix batch).

## Artifacts
- Spec: docs/wayfinder-reports/445/459-spec.md
- Close triage + fixes: docs/wayfinder-reports/445/logs/scratch-459/close.md
- Review reports: docs/wayfinder-reports/445/logs/scratch-459/review-{standards,spec}.md
- Checks: docs/wayfinder-reports/445/logs/scratch-459/{tsc-close2,vitest-full2,eslint-close2,next-build}.log

## Open findings
None blocking. review: P0=0 P1=0 (post-fix). One P2 spec-wording gap recorded in close.md,
not a ticket (no product decision in it).

## Next step
Uncommitted fix batch still on disk (models/document-checks.ts, models/document-checks.test.ts,
lib/integration-push.ts, lib/integration-push.test.ts, lib/integrations/errors.ts). Next session:
commit the fix batch, then finish the close session — write the report at
docs/wayfinder-reports/445/459.md (scores line: `review: P0=0 P1=0
findings=docs/wayfinder-reports/445/logs/scratch-459/close.md`), lessons files (see close.md's
P0/P1 for what a Part B check should have caught — the P0 is a "wiring never tested" class:
a new pure function unit-tested but never verified reachable from its real caller), resolution
comment, close #459, one line on map #445 Decisions so far.
