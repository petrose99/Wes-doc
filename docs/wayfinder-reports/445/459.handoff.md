# #459 Build: item lines on QuickBooks and Xero bills — hand-off

milestone: spec-done

design pass: not owed (no surface steps — all files under lib/, models/, prisma/, app/**/*actions.ts).

step: 1 — Item sync and capability — kind: backend — done
step: 2 — Line model, extraction and learning — kind: backend — done
step: 3 — Checks and correction — kind: backend — in progress (this session)
  Done so far, not yet committed as `done`:
  - lib/finance/line-coding.ts: CodingReferences.items (Map externalId → {trackedInventory}),
    codingReferencesFrom builds it from entityType "item".
  - models/accounting-entities.ts: loadLineCodingContext widened to entityType "item", names map
    includes item names. Test updated + passing.
  - lib/checks/line-coding.ts: LineCodingInput.lines[] gains item_external_id/quantity; 3 new codes
    (item_lines_not_supported, item_not_in_ledger, item_quantity_needed) + LINE_CODING_FALLBACK_TEXT
    entries + checkLineCoding logic; item_lines_not_supported's detail carries
    {actionLabel, actionCode: "code_to_item_account"}. lineCodingInputFromDocument/FromBill wired.
    lib/checks/line-coding.test.ts extended with an "item lines (#459)" describe block — passing.
  - lib/integration-bill-mapping.ts: NormalizedLineItem.itemExternalId + lineCoding() row mapping.
    No test file exists for this module yet (none did before this ticket either) — untouched.
  - models/documents.ts: setDocumentLineToAccount (new, clears item_external_id/item_source, sets
    account_source "manual", leaves account_external_id) next to recordDocumentLineAccountsCorrected;
    findBillsAffectedByAccountChange and findAccountCorrectionReminders both skip item-sourced lines
    (account_source === "item") but keep a document affected-with-empty-lines if EVERY affected line
    was an item line (matchedAnyLine vs lines.length distinction) rather than dropping it silently.
  - app/(app)/workspaces/[workspaceId]/account-correction-actions.ts: new setLineToAccountAction
    (owner-gated via existing `guard`, calls setDocumentLineToAccount + refreshLineCodingChecks);
    updateSelectedBillAccountsAction refuses a document whose only affected lines were item lines
    with `status: "failed", error: "coded to an item — change it in ‹Provider›"` (ADR 0015's exact
    text); updateDocumentLineAccountsAction filters out any change targeting an item-sourced line and
    refuses with the same text if none remain.
  - `npx tsc --noEmit`: clean. `npx vitest run lib/checks/line-coding.test.ts
    models/accounting-entities.test.ts`: 35 passed.

  **Not yet done** (remaining before step 3 can be marked `done`):
  - No test file yet for: models/documents.ts's new/changed functions (setDocumentLineToAccount,
    the two correction-scan guards) — extend models/documents.test.ts.
  - No test file yet for app/(app)/workspaces/[workspaceId]/account-correction-actions.ts at all
    (none existed before this ticket) — new account-correction-actions.test.ts per the spec's Seams
    list (setLineToAccountAction owner-gated + clears fields + re-runs Checks; the two refusal paths).
  - Grep check that LINE_CODING_FALLBACK_TEXT has all 3 new codes — already true and covered by a
    test in line-coding.test.ts ("every item check code has a LINE_CODING_FALLBACK_TEXT entry"); no
    further action needed.
  - Run the FULL test suite once (`npx vitest run`) to check nothing else broke (e.g.
    lib/integration-bill-mapping's callers, models/document-checks.ts) — only ran the two touched
    test files above this session, not the full suite.

step: 4 — Mappers — kind: backend — todo
step: 5 — Line match — kind: backend — todo
step: G1 — round script — todo (not applicable to this all-backend ticket the way a surface ticket
  needs it — confirm at G-time whether G1–G3 reduce to just tsc/tests/`/code-review` since no
  rendering surface exists; the plan's own G3 line already says so)
step: G3 — build-done (npm test, tsc --noEmit, /code-review Standards+Spec, P0=P1=0) — todo

## Artifacts
- Spec: docs/wayfinder-reports/445/459-spec.md

## Open findings
None yet.

## Next step
Build session: finish step 3's tests (models/documents.test.ts extensions,
account-correction-actions.test.ts new file), run `npx vitest run` (full suite) once to confirm
nothing else broke, then mark step 3 `done` and commit. Continue to step 4 only if context allows.
