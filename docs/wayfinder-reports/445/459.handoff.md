# #459 Build: item lines on QuickBooks and Xero bills — hand-off

milestone: spec-done

design pass: not owed (no surface steps — all files under lib/, models/, prisma/, app/**/*actions.ts).

step: 1 — Item sync and capability — kind: backend — done
  files: prisma/schema.prisma (+migration 20260928090000_add_item_lines; SupplierItemPairing model
  goes after SupplierAccountRule at line ~2256; add its back-relation next to
  `supplierAccountRules SupplierAccountRule[]` on Workspace (line 253) and IntegrationConnection
  (line 1367)), lib/workspace-scope.ts (WORKSPACE_SCOPED_MODELS, line ~29),
  lib/integrations/ledger-capabilities.ts, lib/integrations/{quickbooks,xero}/client.ts,
  lib/integrations/sync.ts, models/accounting-entities.ts
  seams: deriveQuickBooksCapabilities/deriveXeroCapabilities/parseLedgerCapabilities →
  ledger-capabilities.test.ts (existing file, 143 lines — every `toEqual`/`off` fixture needs
  `itemLines` added, e.g. the `off` const at line 24); listItems (both) →
  {quickbooks,xero}/client.test.ts; item rows synced gated by itemLines → sync.test.ts; widened
  AccountingEntityType → accounting-entities.test.ts
step: 2 — Line model, extraction and learning — kind: backend — todo
  files: lib/domains/finance.ts, lib/doc-types.ts, lib/finance/item-line-resolution.ts (new),
  lib/finance/line-coding.ts, models/documents.ts, prisma/schema.prisma (schema comment)
  seams: resolveItemLine (pairing/code-match/none/manual-kept/never-fuzzy) →
  item-line-resolution.test.ts; resolveLineCoding item-tax-code precedence → line-coding.test.ts;
  resolveDocumentCodingItems override + pairing upsert + rule scan excludes item lines →
  models/documents.test.ts
step: 3 — Checks and correction — kind: backend — todo
  files: lib/checks/line-coding.ts, models/accounting-entities.ts (loadLineCodingContext),
  lib/integration-bill-mapping.ts (NormalizedLineItem.itemExternalId only),
  app/(app)/workspaces/[workspaceId]/account-correction-actions.ts, models/documents.ts
  (findBillsAffectedByAccountChange, findAccountCorrectionReminders)
  seams: 3 new Check codes + item references/names → line-coding.test.ts,
  accounting-entities.test.ts; setLineToAccount action → account-correction-actions.test.ts;
  correction scans skip item lines + per-doc refusal text → models/documents.test.ts,
  account-correction-actions.test.ts; LINE_CODING_FALLBACK_TEXT has all 3 new codes (grep)
step: 4 — Mappers — kind: backend — todo
  files: lib/integrations/{quickbooks,xero}/bill-mapper.ts, lib/integration-bill-mapping.ts,
  lib/integration-push.ts (Xero item-code-stripped warning → failed post)
  seams: item vs account line, tracked/untracked quantity fallback →
  {quickbooks,xero}/bill-mapper.test.ts; item-code-stripped warning → failed →
  integration-push.test.ts; correction resend leaves ItemRef/ItemCode untouched →
  {quickbooks,xero}/correction.test.ts
step: 5 — Line match — kind: backend — todo
  files: lib/matching/line-match.ts
  seams: item-match-first, item-match-ambiguous-falls-through, item-mismatch-never-pairs,
  no-item-unchanged → lib/matching/line-match.test.ts
step: G3 — build-done (npm test, tsc --noEmit, /code-review Standards+Spec, P0=P1=0) — todo

## Artifacts
- Spec: docs/wayfinder-reports/445/459-spec.md

## Open findings
None yet — spec only.

## Next step
Build session: implement step 2 (line model, extraction and learning), commit, stop.
