import { QueueLoading } from "@/components/queue/queue-loading"

/** #261 spec §4: the queue's route-level loading state. */
export default function ExceptionsLoading() {
  return <QueueLoading title="Exceptions" />
}
